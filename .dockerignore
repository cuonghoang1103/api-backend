node_modules
frontend/node_modules
# `.next*` chứ không phải `.next`: NEXT_DIST_DIR cho phép đổi thư mục build
# (`.next-verify`, `.next-dev-isolated`), và một thư mục dev bỏ quên sẽ chui
# thẳng vào build context qua `COPY . .` — 245 MB image phình vô ích.
frontend/.next*
bin
.git
*.log
.env
postgres
redis
uploads
# host-cache is pre-warmed by deploy-vps.sh and used as
# offline fallback when npm registry / GitHub are blocked.
# MUST be included in build context.
# !host-cache

# Firmware sources are not part of the server image
firmware/
tools/
