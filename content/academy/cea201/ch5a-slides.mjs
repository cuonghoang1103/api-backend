/**
 * CEA201 · Chương 5 trên web (= Chapter 6 bản 11e) — Internal Memory,
 * học theo từng slide, PHẦN A: slide 1–20 của deck 'cea6' (tổng 40 slide).
 *
 * ⚠️ ĐÁNH SỐ: syllabus của trường theo bản 9th ed gọi khối này là "Chapter 5:
 * Internal Memory"; bộ slide là 11th ed nên in "Chapter 6". Cùng một nội dung.
 * Xem bảng quy đổi trong _slides.mjs.
 *
 * Nội dung bám ĐÚNG chữ trích từ CH06-COA11e.pptx (/tmp/cea201-text/cea6.txt).
 * Các slide chỉ có tiêu đề + hình/bảng (2, 3, 5, 10, 11, 12, 13, 14, 15, 17,
 * 18, 20) đã được ĐỌC THẲNG TỪ ẢNH render để lấy đúng từng nhãn trên sơ đồ.
 *
 * ⚠️ PHẠM VI THẬT của slide 1–20 RỘNG HƠN mô tả "bộ nhớ bán dẫn + DRAM/SRAM +
 * ROM + logic chip + module": slide 15 là Interleaved Memory và slide 16–20 là
 * TOÀN BỘ phần Error Correction (hard/soft failure, sơ đồ ECC, mã Hamming,
 * Table 6.2, Figure 6.9). Tiêu đề bài đã sửa cho khớp.
 *
 * ⚠️ MỌI con số ghép chip trong bài đã kiểm bằng python3 TRƯỚC khi viết, mỗi
 * bài dựng ngược lại để đối chiếu (rows×cw = tw, cols×cb = tb, tổng bit khớp):
 *   · 256K×8  → 1M×16 : 4 hàng × 2 cột = 8 chip · 20 = 18 + 2 đường địa chỉ
 *   · 512K×4  → 2M×8  : 4 hàng × 2 cột = 8 chip · 21 = 19 + 2 đường địa chỉ
 *   · 1M×1    → 4M×16 : 4 hàng × 16 cột = 64 chip · 22 = 20 + 2 đường địa chỉ
 *   · 16K×4   → 64K×16: 4 hàng × 4 cột = 16 chip · 16 = 14 + 2 đường địa chỉ
 *   · Figure 6.5: 8 chip 512×512 bit = 2 097 152 bit = 256 KByte, MAR 9+9 = 18 bit
 *   · Figure 6.6: 32 chip (4 nhóm × 8) = 1 MB, MAR 9+9+2 = 20 bit
 *   · RAS/CAS: 22 bit địa chỉ → 11 chân thay vì 22, tiết kiệm đúng 50%
 *   · Refresh: 64 ms / 4096 hàng = 15,625 µs mỗi hàng; phí tổn ≈ 0,32% thời gian
 *     (8192 hàng → 7,8125 µs và ≈ 0,64%)
 *   · Table 6.2 dựng lại HOÀN TOÀN từ điều kiện 2^K ≥ M + K + 1 → 4·5·6·7·8·9
 *     cho M = 8·16·32·64·128·256, và % tăng 50 · 31,25 · 18,75 · 10,938 ·
 *     6,25 · 3,5156 — khớp từng ô với slide 19.
 *   · Figure 6.9: bit kiểm ở vị trí luỹ thừa 2 (1,2,4,8); C1 phủ {3,5,7,9,11},
 *     C2 phủ {3,6,7,10,11}, C4 phủ {5,6,7,12}, C8 phủ {9,10,11,12}.
 *   · Xen kẽ 4 bank, chu kỳ 60 ns, bus 10 ns/từ: 240 ns → 90 ns, nhanh 2,67 lần.
 *
 * ⚠️ PHÉP ĐO THẬT (chạy trên chính máy viết bài — Apple M1 Max, 32 GB LPDDR5
 * Samsung theo system_profiler SPMemoryDataType; cc -O2):
 *   · Đuổi con trỏ vòng ngẫu nhiên, bước 64 B (đo ĐỘ TRỄ, không phải băng thông):
 *     16 kB → 2,17 ns · 64 kB → 5,82 ns · 512 kB → 9,71 ns · 4 MB → 10,67 ns ·
 *     64 MB → 118,60 ns · 512 MB → 133,93 ns. Tỉ số DRAM/L1 = 133,93/2,17 ≈ 62.
 *   · Đọc tuần tự 512 MB một luồng: 0,0648 s → 8,3 GB/s.
 *
 * Chỗ slide gốc SAI/LỆCH SÁCH — nêu rõ trong bài, không im lặng chép, không sửa:
 *   · slide 9 (PROM) kết bằng "Attractive for high volume production runs".
 *     Trong sách Stallings câu đó nói về ROM (mặt nạ đắt nên phải chia cho sản
 *     lượng lớn); PROM ngược lại, hợp với sản lượng VỪA và NHỎ. Bullet này bị
 *     đặt nhầm slide.
 *   · slide 3 (Table 6.1) gộp ô: ROM và PROM chung ô Erasure "Not possible",
 *     nhưng Write Mechanism thì ROM = "Masks" còn PROM = "Electrically" —
 *     bản trích chữ làm phẳng nên nhìn dễ nhầm.
 *   · slide 12 (Figure 6.4a) vẽ EPROM 1M×8 = 8 Mbit với A0–A19, nhưng chú thích
 *     dưới hình ghi "8 Mbit EPROM" trong khi hình 6.4b ghi "16 Mbit DRAM" cho
 *     4M×4 — hai cách gọi dung lượng theo BIT, không theo byte. Dễ đọc nhầm.
 */
import { walk, walkHead } from './_slides.mjs';

const D = 'cea6';

export default {
  title: '5.0a — Slide by slide: Semiconductor memory, DRAM vs SRAM, ROM types, chip logic, module organization and error correction (slides 1–20)|||5.0a — Slide bài giảng: Bộ nhớ bán dẫn, DRAM vs SRAM, các loại ROM, logic chip, ghép module & sửa lỗi (slide 1–20)',
  slug: 'cea201-5-0a-slides-dram-sram-rom',
  type: 'DOCUMENT',
  description: 'Nửa đầu Chương 5 — Bộ nhớ trong (slide 1–20 của deck Ch.6 bản 11e). Đi từ một ô nhớ ba đầu dây (Figure 6.1) tới bảng phân loại bộ nhớ bán dẫn (Table 6.1), rồi mổ xẻ hai công nghệ RAM: DRAM 1 transistor + 1 tụ phải làm tươi, SRAM 6 transistor flip-flop không cần làm tươi — có bảng so sánh đầy đủ và SỐ ĐO THẬT trên máy (L1 2,17 ns so với DRAM 133,93 ns). Tiếp là ROM/PROM/EPROM/EEPROM/Flash, logic một chip DRAM 16 Mbit với RAS/CAS và bộ đếm làm tươi, cách ghép chip thành module 256 KByte và 1 MB (kèm BỐN bài tính ghép chip giải trọn, đã kiểm bằng máy), bộ nhớ xen kẽ, và trọn phần sửa lỗi: hỏng cứng/lỗi mềm, sơ đồ ECC, mã Hamming trên biểu đồ Venn, Table 6.2 và bố trí bit dữ liệu/bit kiểm.',
  content: [
    walkHead(D, 1, 20),
    walk(D, [

      [1, 'Chapter 6 — Internal Memory (title slide, Stallings 11th Edition Global Edition)',
        `<p class="y-chinh">🎯 Chapter 4 said <em>why</em> a hierarchy must exist and Chapter 5 said <em>how</em> a cache is organised. This chapter finally opens the box and asks: <strong>what is a bit physically made of, and what does that physical choice cost you?</strong> Everything here is silicon, charge and transistor count.</p>
<ul>
<li><strong>Numbering warning first.</strong> Your syllabus follows the 9th edition and calls this block <strong>"Chapter 5: Internal Memory"</strong>; the slide header says "Chapter 6" because the 11th edition split the cache material into two chapters earlier on. Same content, different number. Whenever a lecturer says "chapter 5" and the slide says "6", this is why.</li>
<li><strong>"Internal" means on the memory bus, not on a cable.</strong> Internal memory = registers, cache, main memory — anything the processor can address directly. External memory (Chapter 7 in this deck's numbering) = disk, SSD, tape, reached through an I/O module. The dividing line is <em>addressability</em>, not physical distance.</li>
<li><strong>The road through slides 1–20.</strong> Slides 2–3: the memory cell and the master classification table. Slides 4–7: DRAM versus SRAM, the single most examined comparison in the course. Slides 8–10: the ROM family. Slides 11–14: chip logic, chip packaging and how chips are combined into a module. Slide 15: interleaving. Slides 16–20: error correction and the Hamming code.</li>
<li><strong>What it connects to.</strong> The cache of Chapter 5 is built from <strong>SRAM</strong>; the main memory it caches is built from <strong>DRAM</strong>. This chapter is where those two words stop being labels and become circuits you can count transistors in. Chapter 7 then takes the Flash cell from slide 10 and builds an SSD out of it.</li>
<li><strong>Real machine, right now.</strong> The laptop this lesson was written on reports <code>Memory: 32 GB, Type: LPDDR5, Manufacturer: Samsung</code> from <code>system_profiler SPMemoryDataType</code>. That is 32 GB of the DRAM cell on slide 5(a) — roughly 275 billion capacitors, every one of which must be recharged about 15 times a second or the machine forgets everything.</li>
</ul>
<p class="meo">💡 One sentence to carry through the whole chapter: <strong>a DRAM bit is a leaking bucket of charge, an SRAM bit is a latch that holds itself, and a ROM bit is a wire.</strong> Density, speed, cost and volatility all fall out of those three pictures.</p>`,
        `<p class="y-chinh">🎯 Chương 4 nói <em>VÌ SAO</em> phải có phân cấp, Chương 5 nói cache được <em>TỔ CHỨC</em> ra sao. Chương này mới mở nắp hộp ra hỏi: <strong>một bit được làm bằng cái gì về mặt VẬT LÝ, và lựa chọn vật lý đó tính giá bao nhiêu?</strong> Ở đây toàn là silic, điện tích và số transistor.</p>
<ul>
<li><strong>Cảnh báo đánh số trước đã.</strong> Syllabus của trường theo bản 9th ed nên gọi khối này là <strong>"Chương 5: Internal Memory"</strong>; đầu slide ghi "Chapter 6" vì bản 11th ed đã tách phần cache thành hai chương ở phía trên. Cùng nội dung, khác số. Thầy nói "chương 5" mà slide ghi "6" là vì vậy.</li>
<li><strong>"Trong" nghĩa là nằm trên bus bộ nhớ, không phải nằm gần.</strong> Bộ nhớ trong = thanh ghi, cache, bộ nhớ chính — mọi thứ CPU đánh địa chỉ thẳng được. Bộ nhớ ngoài (Chương 7 theo cách đánh của deck này) = đĩa, SSD, băng từ, phải đi qua module vào/ra. Ranh giới là <em>ĐÁNH ĐỊA CHỈ ĐƯỢC HAY KHÔNG</em>, không phải khoảng cách vật lý.</li>
<li><strong>Lộ trình slide 1–20.</strong> Slide 2–3: ô nhớ và bảng phân loại gốc. Slide 4–7: DRAM so với SRAM — câu hỏi bị hỏi nhiều nhất cả môn. Slide 8–10: họ ROM. Slide 11–14: logic một chip, đóng gói chân chip, và cách ghép chip thành module. Slide 15: xen kẽ. Slide 16–20: sửa lỗi và mã Hamming.</li>
<li><strong>Nó nối vào đâu.</strong> Cache của Chương 5 làm bằng <strong>SRAM</strong>; bộ nhớ chính mà nó đệm làm bằng <strong>DRAM</strong>. Chương này là chỗ hai chữ đó thôi làm nhãn dán mà trở thành mạch điện đếm được transistor. Chương 7 sẽ lấy ô Flash ở slide 10 để dựng nên cái SSD.</li>
<li><strong>Máy thật, ngay lúc này.</strong> Cái laptop viết bài này báo <code>Memory: 32 GB, Type: LPDDR5, Manufacturer: Samsung</code> qua <code>system_profiler SPMemoryDataType</code>. Đó là 32 GB đúng cái ô DRAM ở slide 5(a) — cỡ 275 tỉ cái tụ điện, mỗi cái phải được nạp lại khoảng 15 lần mỗi giây, nếu không máy quên sạch.</li>
</ul>
<p class="meo">💡 Một câu mang theo suốt chương: <strong>bit DRAM là cái xô nước rò, bit SRAM là cái chốt tự giữ mình, bit ROM là một sợi dây.</strong> Mật độ, tốc độ, giá và tính bay hơi đều rơi ra từ ba hình ảnh đó.</p>`],

      [2, 'Figure 6.1 — Memory Cell Operation (a) Write, (b) Read',
        `<p class="y-chinh">🎯 The atom of this entire chapter: one cell = one bit, with <strong>exactly three functional terminals</strong>. The figure draws the same cube twice — once being written, once being read — and the only difference is which way the right-hand arrow points.</p>
<table>
<tr><th>Terminal</th><th>Direction</th><th>What it does</th></tr>
<tr><td><strong>Select</strong></td><td>in</td><td>Says "this cell, not the millions of others". Driven by the row decoder — slide 11 shows it as "Row Decoder"</td></tr>
<tr><td><strong>Control</strong></td><td>in</td><td>Says <em>which</em> operation: read or write. In a real chip this is the <code>WE</code> (write enable) line of slide 12</td></tr>
<tr><td><strong>Data in</strong> (write) / <strong>Sense</strong> (read)</td><td>in / out</td><td>The bit itself. Same physical bit line, used in both directions</td></tr>
</table>
<ul>
<li><strong>Three properties every cell must have</strong>, and the figure is their picture: (1) it exhibits <em>two stable states</em> usable to represent 0 and 1; (2) it is <em>capable of being written into</em>, at least once, to set the state; (3) it is <em>capable of being read</em> to sense the state. A device missing any one of the three cannot be memory.</li>
<li><strong>Why the read wire is labelled "Sense" and not "Data out".</strong> What comes off a DRAM cell is not a clean logic level — it is a tiny voltage wobble on a long bit line, produced by a capacitor a few femtofarads in size. It has to be <em>sensed</em> and amplified. That is why every real chip has <strong>sense amplifiers</strong>, and why the DRAM read is <em>destructive</em>: sensing drains the charge, so the chip must write the value straight back.</li>
<li><strong>Select and Control together are the whole address logic.</strong> Millions of cells share the same data lines; only the selected one drives or listens. Everything on slides 11–14 — row decoders, column decoders, chip-enable pins, chip groups — is just "Select" built at four different scales.</li>
<li><strong>Same three terminals, five different technologies.</strong> DRAM, SRAM, ROM, EPROM and Flash all present this identical interface to the outside world; they differ only in what sits inside the cube. That is exactly why they can be dropped into the same socket and the same address bus.</li>
</ul>
<p class="meo">💡 Remember the cell as a <strong>light switch in a hotel corridor</strong>: Select = your room key card enabling this switch, Control = whether you are flipping it or just looking, Data/Sense = the state of the bulb.</p>
<p class="pitfall">⚠️ Exam trap: "Data in" and "Sense" are drawn on the same side of the cube because in a real chip they are usually <strong>the same physical bit line</strong>, not two separate wires. Counting them as two extra pins on a chip is a classic mistake — look at slide 12 and you will see <code>D0</code>–<code>D3</code> drawn with <em>double-headed</em> arrows on the DRAM.</p>`,
        `<p class="y-chinh">🎯 Nguyên tử của cả chương này: một ô nhớ = một bit, với <strong>ĐÚNG BA đầu dây chức năng</strong>. Hình vẽ cùng một khối lập phương hai lần — một lần đang ghi, một lần đang đọc — và khác nhau duy nhất ở chiều của mũi tên bên phải.</p>
<table>
<tr><th>Đầu dây</th><th>Chiều</th><th>Nó làm gì</th></tr>
<tr><td><strong>Select</strong> (chọn)</td><td>vào</td><td>Nói "ĐÚNG ô này, không phải hàng triệu ô kia". Do bộ giải mã hàng lái — slide 11 vẽ nó thành "Row Decoder"</td></tr>
<tr><td><strong>Control</strong> (điều khiển)</td><td>vào</td><td>Nói ĐANG LÀM GÌ: đọc hay ghi. Trong chip thật đây là chân <code>WE</code> (write enable) ở slide 12</td></tr>
<tr><td><strong>Data in</strong> (ghi) / <strong>Sense</strong> (đọc)</td><td>vào / ra</td><td>Chính cái bit. Cùng một đường bit line vật lý, dùng cho cả hai chiều</td></tr>
</table>
<ul>
<li><strong>Ba tính chất mọi ô nhớ đều phải có</strong>, và hình này chính là bức tranh của chúng: (1) có <em>HAI trạng thái bền</em> dùng được để biểu diễn 0 và 1; (2) <em>GHI ĐƯỢC</em>, ít nhất một lần, để đặt trạng thái; (3) <em>ĐỌC ĐƯỢC</em> để nhận biết trạng thái. Thiếu một trong ba thì không thể làm bộ nhớ.</li>
<li><strong>Vì sao dây đọc ghi là "Sense" (cảm nhận) chứ không phải "Data out".</strong> Thứ ra khỏi một ô DRAM không phải mức logic sạch sẽ — nó là một cái rung điện áp tí hon trên một đường bit line dài, sinh ra bởi một cái tụ cỡ vài femtofarad. Phải <em>CẢM NHẬN</em> rồi khuếch đại lên. Vì thế chip thật nào cũng có <strong>bộ khuếch đại cảm nhận (sense amplifier)</strong>, và vì thế lần đọc DRAM là <em>PHÁ HUỶ</em>: cảm nhận xong là tụ cạn, chip buộc phải ghi trả giá trị lại ngay.</li>
<li><strong>Select và Control gộp lại chính là toàn bộ logic địa chỉ.</strong> Hàng triệu ô dùng chung đường dữ liệu; chỉ ô được chọn mới lái hoặc lắng nghe. Mọi thứ ở slide 11–14 — giải mã hàng, giải mã cột, chân chip-enable, nhóm chip — chỉ là chữ "Select" dựng ở bốn quy mô khác nhau.</li>
<li><strong>Cùng ba đầu dây, năm công nghệ khác nhau.</strong> DRAM, SRAM, ROM, EPROM và Flash đều trưng ra đúng giao diện này với bên ngoài; chúng chỉ khác nhau ở thứ nằm TRONG khối lập phương. Đó chính là lý do chúng cắm được vào cùng một đế và cùng một bus địa chỉ.</li>
</ul>
<p class="meo">💡 Nhớ ô nhớ như <strong>công tắc đèn ở hành lang khách sạn</strong>: Select = thẻ phòng của bạn cho phép công tắc này hoạt động, Control = bạn đang bật/tắt hay chỉ đang nhìn, Data/Sense = trạng thái bóng đèn.</p>
<p class="pitfall">⚠️ Bẫy đề thi: "Data in" và "Sense" được vẽ cùng một phía khối lập phương vì trong chip thật chúng thường là <strong>CÙNG MỘT đường bit line vật lý</strong>, không phải hai sợi dây riêng. Đếm chúng thành hai chân thừa trên chip là lỗi kinh điển — nhìn slide 12 sẽ thấy <code>D0</code>–<code>D3</code> của DRAM được vẽ bằng mũi tên <em>HAI ĐẦU</em>.</p>`],

      [3, 'Table 6.1 — Semiconductor Memory Types (the master classification table)',
        `<p class="y-chinh">🎯 Six memory types, four questions asked of each. Learn this table and roughly a third of the chapter's exam questions answer themselves. The four questions are: <strong>what category, how is it erased, how is it written, is it volatile?</strong></p>
<table>
<tr><th>Memory Type</th><th>Category</th><th>Erasure</th><th>Write Mechanism</th><th>Volatility</th></tr>
<tr><td>Random-access memory (RAM)</td><td>Read-write memory</td><td>Electrically, byte-level</td><td>Electrically</td><td><strong>Volatile</strong></td></tr>
<tr><td>Read-only memory (ROM)</td><td rowspan="2">Read-only memory</td><td rowspan="2">Not possible</td><td><strong>Masks</strong></td><td rowspan="5"><strong>Nonvolatile</strong></td></tr>
<tr><td>Programmable ROM (PROM)</td><td rowspan="4">Electrically</td></tr>
<tr><td>Erasable PROM (EPROM)</td><td rowspan="3">Read-mostly memory</td><td><strong>UV light, chip-level</strong></td></tr>
<tr><td>Electrically Erasable PROM (EEPROM)</td><td>Electrically, <strong>byte-level</strong></td></tr>
<tr><td>Flash memory</td><td>Electrically, <strong>block-level</strong></td></tr>
</table>
<ul>
<li><strong>Read the table as one moving axis, not six unrelated rows.</strong> Going down, the erasure granularity gets coarser then finer again: byte (RAM) → impossible (ROM, PROM) → whole chip under UV (EPROM) → byte again but electrically (EEPROM) → block (Flash). Flash sits deliberately <em>between</em> EPROM and EEPROM in cost and function, which is exactly what slide 34 of the full deck says.</li>
<li><strong>The three categories are about how often you write, not about whether you can.</strong> <em>Read-write</em> = written constantly during normal operation. <em>Read-only</em> = written once, at manufacture or programming time, never again. <em>Read-mostly</em> = normally read, but writable occasionally, in a special operation that is far slower than a read.</li>
<li><strong>Only RAM is volatile.</strong> Every one of the other five keeps its contents with the power off. This single column is why your machine can boot at all: the processor's first fetch after reset lands in nonvolatile memory (BIOS/UEFI Flash), because DRAM contains nothing but noise at that instant.</li>
<li><strong>Flash's "block-level" erasure is the most consequential cell in the table.</strong> You cannot rewrite one byte of Flash in place; you must erase a whole block (tens of kB to several MB) and rewrite it. That single fact forces every SSD to contain a translation layer and a <strong>wear-levelling</strong> algorithm, which Chapter 7 (External Memory) spends pages on.</li>
</ul>
<p class="pitfall">⚠️ Reading trap in the original slide: <strong>ROM and PROM share one merged cell for Erasure ("Not possible"), but they do NOT share Write Mechanism</strong> — ROM is written by <em>Masks</em> (during fabrication), PROM is written <em>Electrically</em> (after fabrication). The flat text extraction of the .pptx loses the cell merging, so it is easy to attribute "Masks" to both. The image is unambiguous: "Masks" occupies only ROM's row.</p>
<p class="meo">💡 Mnemonic for the erasure column, in order ROM→PROM→EPROM→EEPROM→Flash: <strong>"không · không · đèn tím · từng byte · từng khối"</strong>. Five words, and they are worth several marks each year.</p>`,
        `<p class="y-chinh">🎯 Sáu loại bộ nhớ, bốn câu hỏi đặt cho mỗi loại. Thuộc bảng này thì khoảng một phần ba câu hỏi thi của chương tự trả lời. Bốn câu hỏi là: <strong>thuộc nhóm nào, xoá bằng cách nào, ghi bằng cách nào, có bay hơi không?</strong></p>
<table>
<tr><th>Loại bộ nhớ</th><th>Nhóm</th><th>Xoá (Erasure)</th><th>Cơ chế ghi</th><th>Bay hơi</th></tr>
<tr><td>Bộ nhớ truy cập ngẫu nhiên (RAM)</td><td>Đọc–ghi</td><td>Bằng điện, mức BYTE</td><td>Bằng điện</td><td><strong>CÓ bay hơi</strong></td></tr>
<tr><td>Bộ nhớ chỉ đọc (ROM)</td><td rowspan="2">Chỉ đọc</td><td rowspan="2">KHÔNG THỂ</td><td><strong>Mặt nạ (Masks)</strong></td><td rowspan="5"><strong>KHÔNG bay hơi</strong></td></tr>
<tr><td>ROM lập trình được (PROM)</td><td rowspan="4">Bằng điện</td></tr>
<tr><td>PROM xoá được (EPROM)</td><td rowspan="3">Chủ yếu đọc</td><td><strong>Tia cực tím, cả CHIP</strong></td></tr>
<tr><td>PROM xoá bằng điện (EEPROM)</td><td>Bằng điện, mức <strong>BYTE</strong></td></tr>
<tr><td>Bộ nhớ Flash</td><td>Bằng điện, mức <strong>KHỐI</strong></td></tr>
</table>
<ul>
<li><strong>Đọc bảng như MỘT trục đang dịch chuyển, đừng đọc như sáu dòng rời.</strong> Đi từ trên xuống, độ mịn của phép xoá thô dần rồi mịn lại: byte (RAM) → không thể (ROM, PROM) → cả chip dưới đèn tia cực tím (EPROM) → lại từng byte nhưng bằng điện (EEPROM) → từng khối (Flash). Flash nằm CỐ Ý ở <em>GIỮA</em> EPROM và EEPROM cả về giá lẫn chức năng, đúng như slide 34 của deck đầy đủ nói.</li>
<li><strong>Ba nhóm nói về TẦN SUẤT ghi, không phải về việc có ghi được hay không.</strong> <em>Đọc–ghi</em> = ghi liên tục trong lúc chạy bình thường. <em>Chỉ đọc</em> = ghi một lần, lúc sản xuất hoặc lúc nạp, rồi thôi. <em>Chủ yếu đọc</em> = bình thường thì đọc, nhưng thỉnh thoảng ghi được, bằng một thao tác đặc biệt CHẬM HƠN đọc rất nhiều.</li>
<li><strong>Chỉ RAM là bay hơi.</strong> Năm loại còn lại đều giữ nội dung khi mất điện. Đúng một cột này là lý do máy bạn khởi động được: lệnh đầu tiên CPU nạp sau khi reset rơi vào bộ nhớ không bay hơi (Flash chứa BIOS/UEFI), vì ngay khoảnh khắc đó DRAM chỉ chứa nhiễu.</li>
<li><strong>Ô "xoá theo KHỐI" của Flash là ô có hậu quả lớn nhất bảng.</strong> Bạn KHÔNG ghi đè được một byte Flash tại chỗ; phải xoá cả một khối (từ vài chục kB tới vài MB) rồi ghi lại. Đúng một sự thật đó buộc mọi SSD phải có một lớp dịch địa chỉ và một thuật toán <strong>san đều hao mòn (wear levelling)</strong>, thứ mà Chương 7 (Bộ nhớ ngoài) dành hẳn nhiều trang để nói.</li>
</ul>
<p class="pitfall">⚠️ Bẫy ĐỌC ở slide gốc: <strong>ROM và PROM dùng chung MỘT ô gộp cho cột Erasure ("Not possible"), nhưng KHÔNG dùng chung cột Write Mechanism</strong> — ROM ghi bằng <em>MẶT NẠ</em> (trong lúc chế tạo), PROM ghi bằng <em>ĐIỆN</em> (sau khi chế tạo). Bản trích chữ từ .pptx làm phẳng ô gộp nên rất dễ gán "Masks" cho cả hai. Nhìn ảnh thì rõ: "Masks" chỉ chiếm đúng dòng của ROM.</p>
<p class="meo">💡 Mẹo nhớ cột xoá, theo thứ tự ROM→PROM→EPROM→EEPROM→Flash: <strong>"không · không · đèn tím · từng byte · từng khối"</strong>. Năm chữ, mà năm nào cũng đáng vài điểm.</p>`],

      [4, 'Dynamic RAM (DRAM) — charge on a capacitor, and why it leaks',
        `<p class="y-chinh">🎯 RAM technology splits into exactly two technologies — <strong>DRAM</strong> and <strong>SRAM</strong> — and this slide defines the first one. A DRAM cell stores data as <strong>charge on a capacitor</strong>; the presence or absence of charge is interpreted as a binary 1 or 0.</p>
<ul>
<li><strong>The word "dynamic" is the whole slide.</strong> It refers to the <em>tendency of the stored charge to leak away, even with power continuously applied</em>. A capacitor is not a perfect container: charge drains through the transistor and through the dielectric. So DRAM <strong>requires periodic charge refreshing</strong> to maintain data storage. Static RAM leaks nothing because nothing is stored as charge.</li>
<li><strong>DRAM is fundamentally an analog device.</strong> It does not store a "1"; it stores a voltage, and a threshold comparator decides afterwards whether that voltage counts as a 1. Everything awkward about DRAM — sense amplifiers, destructive reads, refresh — follows from that single fact.</li>
<li><strong>Refresh, in numbers you should be able to produce.</strong> The standard retention time is <strong>64 ms</strong>: every row must be recharged at least that often. A chip with 4096 rows must therefore refresh one row every 64 ms ÷ 4096 = <strong>15,625 µs</strong>; with 8192 rows, every <strong>7,8125 µs</strong>. Refreshing is done a whole row at a time, not a cell at a time — which is why slide 11 shows a <em>Refresh Counter</em> feeding the row address path.</li>
<li><strong>Refresh costs less time than students expect.</strong> If one row refresh occupies the array for ~50 ns, the cost is 4096 × 50 ns ÷ 64 ms = <strong>0,32%</strong> of the time for a 4096-row chip and <strong>0,64%</strong> for 8192 rows. The real cost of refresh is not throughput — it is <em>complexity and power</em>: extra circuitry on every chip and current drawn even when the machine is idle.</li>
<li><strong>This is why closing the laptop lid does not lose your work but pulling the battery does.</strong> Sleep keeps a trickle of power alive purely so the DRAM refresh cycle keeps running; that is literally what "suspend to RAM" spends its milliwatts on.</li>
</ul>
<p class="meo">💡 Picture a <strong>row of leaky buckets</strong>. "1" = bucket has water, "0" = bucket is empty. Someone must walk the row topping up every full bucket before it drains — 15 625 times a second is fast enough. Reading a bucket means tipping it out to see, so you must immediately refill it: that is the destructive read.</p>
<p class="pitfall">⚠️ Trap: refresh is <em>not</em> the same thing as volatility. DRAM would still be volatile without refresh (cut the power and it forgets), and SRAM is volatile too despite never refreshing. Refresh is about leakage <strong>while powered</strong>; volatility is about what happens <strong>when unpowered</strong>. Exam questions deliberately blur the two.</p>`,
        `<p class="y-chinh">🎯 Công nghệ RAM chia ra ĐÚNG hai nhánh — <strong>DRAM</strong> và <strong>SRAM</strong> — và slide này định nghĩa nhánh thứ nhất. Ô DRAM lưu dữ liệu dưới dạng <strong>ĐIỆN TÍCH TRÊN MỘT TỤ ĐIỆN</strong>; có điện tích hay không có điện tích được hiểu là bit 1 hay bit 0.</p>
<ul>
<li><strong>Chữ "dynamic" (động) chính là cả slide.</strong> Nó chỉ <em>xu hướng điện tích đã lưu bị RÒ MẤT, ngay cả khi vẫn cấp điện liên tục</em>. Tụ điện không phải cái bình kín hoàn hảo: điện tích rò qua transistor và qua lớp điện môi. Cho nên DRAM <strong>BẮT BUỘC phải làm tươi (refresh) định kỳ</strong> để giữ được dữ liệu. SRAM không rò gì cả vì nó không lưu gì dưới dạng điện tích.</li>
<li><strong>DRAM về bản chất là thiết bị TƯƠNG TỰ (analog).</strong> Nó không lưu "số 1"; nó lưu một mức điện áp, rồi một bộ so sánh ngưỡng mới quyết định sau đó rằng mức đó có được tính là 1 hay không. Mọi thứ lằng nhằng của DRAM — khuếch đại cảm nhận, đọc phá huỷ, làm tươi — đều chảy ra từ đúng sự thật đó.</li>
<li><strong>Làm tươi, bằng con số bạn phải tự bật ra được.</strong> Thời gian giữ dữ liệu chuẩn là <strong>64 ms</strong>: mỗi hàng phải được nạp lại ít nhất từng đó một lần. Chip 4096 hàng vì thế phải làm tươi một hàng mỗi 64 ms ÷ 4096 = <strong>15,625 µs</strong>; chip 8192 hàng thì mỗi <strong>7,8125 µs</strong>. Làm tươi theo NGUYÊN HÀNG chứ không theo từng ô — đó là lý do slide 11 vẽ một <em>Refresh Counter</em> bơm vào đường địa chỉ hàng.</li>
<li><strong>Làm tươi tốn ít thời gian hơn sinh viên vẫn tưởng.</strong> Nếu một lượt làm tươi hàng chiếm mảng ô nhớ ~50 ns thì chi phí là 4096 × 50 ns ÷ 64 ms = <strong>0,32%</strong> thời gian với chip 4096 hàng, và <strong>0,64%</strong> với 8192 hàng. Cái giá thật của refresh không nằm ở thông lượng — nó nằm ở <em>ĐỘ PHỨC TẠP VÀ ĐIỆN NĂNG</em>: mạch phụ trên mỗi chip, và dòng điện tiêu thụ ngay cả lúc máy ngồi không.</li>
<li><strong>Đây là lý do gập nắp laptop thì không mất việc đang làm, nhưng rút pin thì mất.</strong> Chế độ ngủ giữ một dòng điện rỉ chỉ để chu kỳ làm tươi DRAM tiếp tục chạy; "suspend to RAM" tiêu mấy chục miliwatt đúng vào việc đó.</li>
</ul>
<p class="meo">💡 Hình dung <strong>một hàng xô nước bị rò</strong>. "1" = xô có nước, "0" = xô rỗng. Phải có người đi dọc hàng châm thêm cho mọi xô đầy trước khi nó cạn — 15 625 lần mỗi giây là đủ nhanh. Đọc một cái xô nghĩa là DỐC NÓ RA để nhìn, nên phải đổ đầy lại ngay: đó chính là đọc phá huỷ.</p>
<p class="pitfall">⚠️ Bẫy: làm tươi <em>KHÔNG</em> phải là tính bay hơi. DRAM vẫn bay hơi kể cả nếu không cần refresh (cắt điện là quên), và SRAM cũng bay hơi dù chẳng bao giờ refresh. Refresh nói về rò rỉ <strong>KHI ĐANG CÓ ĐIỆN</strong>; bay hơi nói về chuyện gì xảy ra <strong>KHI MẤT ĐIỆN</strong>. Đề thi cố tình trộn hai khái niệm này.</p>`],

      [5, 'Figure 6.2 — Typical Memory Cell Structures: (a) DRAM cell, (b) SRAM cell',
        `<p class="y-chinh">🎯 The two circuits side by side. Count the transistors and the entire SRAM-versus-DRAM comparison of slide 7 becomes obvious: <strong>DRAM = 1 transistor + 1 capacitor (1T1C). SRAM = 6 transistors (6T).</strong></p>
<p class="nhan">📐 (a) DRAM cell — labels on the figure: <em>Address line</em> across the top, <em>Transistor</em> in the middle, <em>Storage capacitor</em> on the right going to <em>Ground</em>, <em>Bit line B</em> down the left.</p>
<ul>
<li><strong>The transistor is a gate, not storage.</strong> Raise the address line and the transistor conducts, connecting the storage capacitor to bit line B. Write: drive B high or low and the capacitor charges or discharges. Read: let B float at a precharged mid-level, then open the transistor — the capacitor nudges B slightly up or down, and the sense amplifier decides which.</li>
<li><strong>The read is destructive, and the figure shows why.</strong> The capacitor's tiny charge is shared with the huge bit line capacitance, so after sensing, the cell's charge is gone. The chip must therefore write the sensed value straight back into the cell — every DRAM read is secretly a read <em>and</em> a write.</li>
</ul>
<p class="nhan">📐 (b) SRAM cell — labels on the figure: <em>dc voltage</em> at the top, transistors <strong>T1, T2</strong> (bottom pair), <strong>T3, T4</strong> (top pair, drawn with the circle = p-type), <strong>T5, T6</strong> (the access pair on the outside), nodes <strong>C1</strong> and <strong>C2</strong> cross-connected, <em>Ground</em>, <em>Address line</em> at the bottom, and <strong>two</strong> bit lines: <em>B</em> on the left and <em>B̄</em> (B-bar) on the right.</p>
<ul>
<li><strong>T1–T4 form a flip-flop; the cross is the memory.</strong> The diagonal lines joining C1 to the gates of the opposite pair are what make it self-sustaining: if C1 is high it forces C2 low, and C2 being low forces C1 high. The state holds itself forever, as long as the dc voltage is there. <em>No capacitor, no leak, no refresh.</em></li>
<li><strong>T5 and T6 are the access transistors, driven by the address line.</strong> They connect the two internal nodes to bit lines B and B̄. Note the <strong>differential</strong> pair — SRAM reads by comparing B against B̄, which is both faster and more noise-tolerant than DRAM's single-ended sense.</li>
<li><strong>Six transistors versus one transistor plus a capacitor — that ratio IS the price difference.</strong> Silicon area is what you pay for, so an SRAM bit costs roughly 4–6× the area of a DRAM bit. Everything on slide 7 (density, cost per bit, "used for main memory" versus "used for cache") is a consequence of this picture, not an independent fact to memorise.</li>
</ul>
<p class="dap-an">✅ Worked check: on a <strong>32 GB</strong> machine like the one this lesson was measured on, the DRAM array holds 32 × 2<sup>30</sup> × 8 = <strong>274 877 906 944 cells</strong> — about 275 billion capacitors, one transistor each. Building the same 32 GB from 6T SRAM would need ~1,65 × 10<sup>12</sup> transistors and several times the die area; that is why no laptop has 32 GB of cache.</p>
<p class="pitfall">⚠️ Trap: the circle on the gate of T3 and T4 is not decoration — it marks <strong>p-channel</strong> transistors (the pull-ups). A question asking "how many transistors in an SRAM cell" wants <strong>6</strong>; answering "4" (counting only the cross-coupled flip-flop and forgetting the two access transistors) is the single most common wrong answer.</p>`,
        `<p class="y-chinh">🎯 Hai mạch đặt cạnh nhau. Đếm transistor là toàn bộ so sánh SRAM–DRAM ở slide 7 tự hiện ra: <strong>DRAM = 1 transistor + 1 tụ (1T1C). SRAM = 6 transistor (6T).</strong></p>
<p class="nhan">📐 (a) Ô DRAM — nhãn trên hình: <em>Address line</em> (đường địa chỉ) chạy ngang trên đỉnh, <em>Transistor</em> ở giữa, <em>Storage capacitor</em> (tụ lưu trữ) bên phải nối xuống <em>Ground</em> (đất), <em>Bit line B</em> chạy dọc bên trái.</p>
<ul>
<li><strong>Transistor là cái CỔNG, không phải chỗ lưu.</strong> Kéo đường địa chỉ lên cao thì transistor dẫn, nối tụ lưu trữ vào bit line B. Ghi: lái B lên cao hoặc xuống thấp, tụ nạp hoặc xả theo. Đọc: để B thả nổi ở mức nạp sẵn giữa chừng rồi mở transistor — tụ đẩy B nhích lên hoặc nhích xuống một tí, và bộ khuếch đại cảm nhận quyết định là bên nào.</li>
<li><strong>Lần đọc là PHÁ HUỶ, và hình cho thấy vì sao.</strong> Điện tích tí hon của tụ bị chia sẻ với điện dung khổng lồ của bit line, nên cảm nhận xong là điện tích trong ô biến mất. Chip vì thế buộc phải ghi trả giá trị vừa cảm nhận vào lại ô — mọi lần đọc DRAM đều ngầm là một lần đọc <em>VÀ</em> một lần ghi.</li>
</ul>
<p class="nhan">📐 (b) Ô SRAM — nhãn trên hình: <em>dc voltage</em> ở trên đỉnh, các transistor <strong>T1, T2</strong> (cặp dưới), <strong>T3, T4</strong> (cặp trên, vẽ có vòng tròn = loại p), <strong>T5, T6</strong> (cặp truy cập ở hai bên ngoài), hai nút <strong>C1</strong> và <strong>C2</strong> nối chéo nhau, <em>Ground</em>, <em>Address line</em> ở dưới đáy, và <strong>HAI</strong> đường bit: <em>B</em> bên trái và <em>B̄</em> (B đảo) bên phải.</p>
<ul>
<li><strong>T1–T4 tạo thành một flip-flop; chính cái CHÉO mới là bộ nhớ.</strong> Hai đường chéo nối C1 sang cực cổng của cặp đối diện là thứ làm nó tự nuôi mình: C1 cao thì ép C2 thấp, mà C2 thấp thì lại ép C1 cao. Trạng thái tự giữ mãi mãi, chừng nào còn nguồn dc. <em>Không tụ, không rò, không phải làm tươi.</em></li>
<li><strong>T5 và T6 là hai transistor truy cập, do đường địa chỉ lái.</strong> Chúng nối hai nút bên trong ra bit line B và B̄. Để ý cặp <strong>VI SAI</strong> — SRAM đọc bằng cách SO SÁNH B với B̄, vừa nhanh hơn vừa chống nhiễu tốt hơn kiểu cảm nhận một đầu của DRAM.</li>
<li><strong>Sáu transistor so với một transistor cộng một tụ — chính tỉ lệ đó LÀ chênh lệch giá.</strong> Thứ bạn trả tiền là DIỆN TÍCH SILIC, nên một bit SRAM tốn cỡ 4–6 lần diện tích một bit DRAM. Mọi dòng ở slide 7 (mật độ, giá mỗi bit, "dùng cho bộ nhớ chính" so với "dùng cho cache") đều là HỆ QUẢ của bức hình này, không phải một sự kiện rời phải học thuộc.</li>
</ul>
<p class="dap-an">✅ Kiểm bằng số: trên máy <strong>32 GB</strong> như cái máy đo bài này, mảng DRAM chứa 32 × 2<sup>30</sup> × 8 = <strong>274 877 906 944 ô</strong> — khoảng 275 tỉ cái tụ, mỗi cái một transistor. Dựng đúng 32 GB đó bằng SRAM 6T thì cần ~1,65 × 10<sup>12</sup> transistor và diện tích đế gấp nhiều lần; đó là lý do không laptop nào có 32 GB cache.</p>
<p class="pitfall">⚠️ Bẫy: cái vòng tròn ở cực cổng T3 và T4 không phải trang trí — nó đánh dấu transistor <strong>kênh p</strong> (cặp kéo lên). Câu hỏi "ô SRAM có mấy transistor" muốn đáp án <strong>6</strong>; trả lời "4" (chỉ đếm flip-flop nối chéo mà quên hai transistor truy cập) là đáp án sai phổ biến nhất.</p>`],

      [6, 'Static RAM (SRAM) — a flip-flop made of the processor\'s own logic',
        `<p class="y-chinh">🎯 Three short bullets, one big idea: an SRAM cell is a <strong>digital device that uses the same logic elements used in the processor</strong>. Binary values are stored using traditional <strong>flip-flop logic gate configurations</strong>, and the cell <strong>will hold its data as long as power is supplied to it</strong>.</p>
<ul>
<li><strong>"Same logic elements as the processor" is not a throwaway line.</strong> It means SRAM can be fabricated in exactly the same CMOS process as the CPU logic, on the same die, in the same manufacturing step. DRAM needs a different process (deep trench or stacked capacitors) that does not coexist well with fast logic. <em>That</em> is the real reason cache is on-chip and main memory is not — not merely speed, but manufacturability.</li>
<li><strong>"Static" is the exact opposite of slide 4's "dynamic".</strong> Nothing decays, so nothing needs topping up. The flip-flop actively drives its own state from the power rails, continuously. The cost of that active drive is standby current in every single cell — which is why a large SRAM array leaks power even doing nothing.</li>
<li><strong>Still volatile — this is the most-missed point of the slide.</strong> "As long as power is supplied" is the qualifier. Remove power and an SRAM cell forgets just as completely as a DRAM cell. Table 6.1 puts both under the single row "Random-access memory (RAM) — Volatile" for exactly this reason.</li>
<li><strong>Where you have already met it.</strong> Every cache level in Chapter 5 is SRAM: the split L1 I-cache and D-cache, the unified L2, the shared L3. So are the processor's registers (Chapter 16) and the TLB (Chapter 9). When Chapter 5 says "cache access takes 1–4 cycles", it is SRAM that makes that number possible.</li>
</ul>
<p class="nhan">📐 Measured on this machine (Apple M1 Max, random pointer chase, 64-byte stride, so it measures <strong>latency</strong> not bandwidth):</p>
<table>
<tr><th>Working set</th><th>Time per access</th><th>Technology answering</th></tr>
<tr><td>16 kB</td><td><strong>2,17 ns</strong></td><td>L1 — SRAM</td></tr>
<tr><td>64 kB</td><td>5,82 ns</td><td>L1/L2 boundary — SRAM</td></tr>
<tr><td>512 kB</td><td>9,71 ns</td><td>L2 — SRAM</td></tr>
<tr><td>4 MB</td><td>10,67 ns</td><td>L2 — SRAM</td></tr>
<tr><td>64 MB</td><td>118,60 ns</td><td>Main memory — DRAM</td></tr>
<tr><td>512 MB</td><td><strong>133,93 ns</strong></td><td>Main memory — DRAM</td></tr>
</table>
<p class="dap-an">✅ The SRAM/DRAM speed gap is not theory: <strong>133,93 ÷ 2,17 ≈ 62×</strong> on real silicon, measured today. The whole of Chapter 5 exists to hide that factor of 62.</p>
<p class="meo">💡 Mnemonic: <strong>S = Static = Same as processor logic = Speedy = Six transistors = Small capacity</strong>. Five S's, and each one is a separate exam-answerable fact.</p>`,
        `<p class="y-chinh">🎯 Ba gạch đầu dòng ngắn, một ý lớn: ô SRAM là một <strong>thiết bị SỐ dùng chính những phần tử logic mà bộ xử lý dùng</strong>. Giá trị nhị phân được lưu bằng cấu hình <strong>flip-flop bằng cổng logic</strong> cổ điển, và ô <strong>giữ dữ liệu chừng nào còn được cấp điện</strong>.</p>
<ul>
<li><strong>"Cùng phần tử logic với bộ xử lý" không phải câu nói cho có.</strong> Nó nghĩa là SRAM chế tạo được bằng ĐÚNG quy trình CMOS của phần logic CPU, trên cùng một đế, trong cùng một bước sản xuất. DRAM cần một quy trình KHÁC (tụ rãnh sâu hoặc tụ xếp chồng) mà quy trình đó sống chung rất tệ với logic tốc độ cao. <em>ĐÓ</em> mới là lý do thật khiến cache nằm trong chip còn bộ nhớ chính thì không — không chỉ vì tốc độ, mà vì CHẾ TẠO ĐƯỢC hay không.</li>
<li><strong>"Static" (tĩnh) là đối cực chính xác của chữ "dynamic" ở slide 4.</strong> Không có gì phân rã nên không có gì phải châm thêm. Flip-flop chủ động lái trạng thái của chính nó từ nguồn, liên tục. Cái giá của việc lái chủ động đó là dòng rỉ ở MỌI ô — vì thế một mảng SRAM lớn vẫn hao điện dù đang ngồi không.</li>
<li><strong>VẪN BAY HƠI — đây là điểm bị bỏ sót nhiều nhất của slide.</strong> "Chừng nào còn được cấp điện" mới là mệnh đề điều kiện. Cắt điện thì ô SRAM quên sạch y hệt ô DRAM. Table 6.1 xếp cả hai chung một dòng "Random-access memory (RAM) — Volatile" đúng vì lý do này.</li>
<li><strong>Bạn đã gặp nó ở đâu rồi.</strong> Mọi mức cache ở Chương 5 đều là SRAM: L1 tách đôi I-cache và D-cache, L2 hợp nhất, L3 dùng chung. Thanh ghi của bộ xử lý (Chương 16) và TLB (Chương 9) cũng vậy. Khi Chương 5 nói "truy cập cache mất 1–4 chu kỳ" thì chính SRAM làm cho con số đó khả thi.</li>
</ul>
<p class="nhan">📐 Đo thật trên máy này (Apple M1 Max, đuổi con trỏ ngẫu nhiên, bước 64 byte, nên nó đo <strong>ĐỘ TRỄ</strong> chứ không phải băng thông):</p>
<table>
<tr><th>Vùng làm việc</th><th>Thời gian mỗi lần truy cập</th><th>Công nghệ đang trả lời</th></tr>
<tr><td>16 kB</td><td><strong>2,17 ns</strong></td><td>L1 — SRAM</td></tr>
<tr><td>64 kB</td><td>5,82 ns</td><td>Ranh giới L1/L2 — SRAM</td></tr>
<tr><td>512 kB</td><td>9,71 ns</td><td>L2 — SRAM</td></tr>
<tr><td>4 MB</td><td>10,67 ns</td><td>L2 — SRAM</td></tr>
<tr><td>64 MB</td><td>118,60 ns</td><td>Bộ nhớ chính — DRAM</td></tr>
<tr><td>512 MB</td><td><strong>133,93 ns</strong></td><td>Bộ nhớ chính — DRAM</td></tr>
</table>
<p class="dap-an">✅ Khoảng cách tốc độ SRAM/DRAM không phải lý thuyết suông: <strong>133,93 ÷ 2,17 ≈ 62 lần</strong> trên silic thật, đo hôm nay. Cả Chương 5 tồn tại chỉ để giấu đi cái hệ số 62 đó.</p>
<p class="meo">💡 Mẹo nhớ: <strong>S = Static (tĩnh) = Same as processor (cùng logic CPU) = Speedy (nhanh) = Six transistors (6T) = Small capacity (dung lượng nhỏ)</strong>. Năm chữ S, mỗi chữ là một ý trả lời thi được.</p>`],

      [7, 'SRAM versus DRAM — the comparison the exam asks for',
        `<p class="y-chinh">🎯 The slide's own structure: <strong>both volatile</strong>, then a list of six properties of the <em>dynamic</em> cell, then two properties of the <em>static</em> cell. Reformatted into the table below, this is the single highest-yield item in the chapter.</p>
<table>
<tr><th>Property</th><th>SRAM</th><th>DRAM</th></tr>
<tr><td><strong>Cell structure</strong></td><td>6 transistors (flip-flop)</td><td>1 transistor + 1 capacitor</td></tr>
<tr><td><strong>Stores a bit as</strong></td><td>Latch state — actively driven</td><td>Charge on a capacitor</td></tr>
<tr><td><strong>Refresh needed?</strong></td><td><strong>No</strong></td><td><strong>Yes</strong> — requires supporting refresh circuitry</td></tr>
<tr><td><strong>Speed</strong></td><td><strong>Faster</strong> (measured here: 2,17 ns)</td><td>Slower (measured here: 133,93 ns)</td></tr>
<tr><td><strong>Density</strong></td><td>Lower</td><td><strong>More dense</strong> — smaller cells = more cells per unit area</td></tr>
<tr><td><strong>Cost per bit</strong></td><td>Higher</td><td><strong>Less expensive</strong></td></tr>
<tr><td><strong>Complexity to build</strong></td><td>More complex cell, simpler chip (no refresh logic)</td><td><strong>Simpler to build, smaller</strong> cell; chip needs refresh logic</td></tr>
<tr><td><strong>Power</strong></td><td>Standby leakage in every cell; no refresh current</td><td>Lower static power per bit, but constant refresh current</td></tr>
<tr><td><strong>Read is destructive?</strong></td><td>No</td><td>Yes — value must be written back</td></tr>
<tr><td><strong>Favoured for</strong></td><td><strong>Cache memory (both on and off chip)</strong></td><td><strong>Large memory requirements — main memory</strong></td></tr>
<tr><td><strong>Volatile?</strong></td><td colspan="2" style="text-align:center"><strong>BOTH volatile</strong> — power must be continuously supplied to preserve the bit values</td></tr>
</table>
<ul>
<li><strong>Every row is derivable from one picture, so do not memorise eleven facts.</strong> Start from Figure 6.2: 6T versus 1T1C. Fewer devices ⇒ smaller cell ⇒ denser ⇒ cheaper per bit ⇒ good for big memories. Charge storage ⇒ leaks ⇒ refresh ⇒ extra circuitry ⇒ extra latency. Latch ⇒ no sensing of a tiny charge ⇒ faster ⇒ good for cache.</li>
<li><strong>"Simpler to build, smaller" refers to the CELL, not the chip.</strong> A DRAM <em>chip</em> is arguably more complex than an SRAM chip — it carries refresh counters, sense amplifiers and RAS/CAS multiplexing (all visible on slide 11) that an SRAM chip does not need. The simplicity is per-cell, and per-cell is what multiplies by a billion.</li>
<li><strong>Note the parenthesis "both on and off chip".</strong> In Stallings' era, L2/L3 caches were sometimes separate SRAM chips on the motherboard. Today they are on-die, but the statement stays true: wherever a cache lives, it is SRAM. The exception the 11th edition adds later (slide 31) is <strong>eDRAM</strong> — DRAM embedded on the processor die, used as a huge slow last-level cache, sitting deliberately between the two columns of this table.</li>
<li><strong>Connect it to your own machine.</strong> Your DDR4/DDR5 stick (or the 32 GB LPDDR5 measured here) is the right-hand column; your CPU's L1/L2/L3 is the left-hand column. The <em>same</em> program touches both within nanoseconds of each other, and the 62× gap measured on slide 6 is the entire justification for Chapters 4 and 5.</li>
</ul>
<p class="pitfall">⚠️ The three traps, in order of how often they are fallen for. (1) <strong>"SRAM is nonvolatile because it needs no refresh"</strong> — false, both are volatile; the slide opens with exactly this sentence to pre-empt it. (2) <strong>"DRAM is cheaper because it is slower"</strong> — the causation runs the other way: it is cheaper because its cell is smaller, and the small cell is also what makes it slower. (3) <strong>"SRAM is simpler"</strong> — the slide says the <em>dynamic</em> cell is the simpler one.</p>
<p class="meo">💡 Two words to hang it on: <strong>DRAM = ĐÔNG (đông đúc, rẻ, chậm, phải bơm nước)</strong>, <strong>SRAM = SẴN (sẵn sàng ngay, nhanh, đắt, ít chỗ)</strong>.</p>`,
        `<p class="y-chinh">🎯 Chính cấu trúc của slide: <strong>cả hai đều bay hơi</strong>, rồi sáu tính chất của ô <em>động</em>, rồi hai tính chất của ô <em>tĩnh</em>. Sắp lại thành bảng dưới đây thì đây là mục đáng điểm nhất cả chương.</p>
<table>
<tr><th>Tính chất</th><th>SRAM</th><th>DRAM</th></tr>
<tr><td><strong>Cấu tạo ô</strong></td><td>6 transistor (flip-flop)</td><td>1 transistor + 1 tụ điện</td></tr>
<tr><td><strong>Lưu bit dưới dạng</strong></td><td>Trạng thái chốt — được lái chủ động</td><td>Điện tích trên tụ</td></tr>
<tr><td><strong>Có phải làm tươi?</strong></td><td><strong>KHÔNG</strong></td><td><strong>CÓ</strong> — cần thêm mạch làm tươi hỗ trợ</td></tr>
<tr><td><strong>Tốc độ</strong></td><td><strong>NHANH HƠN</strong> (đo ở đây: 2,17 ns)</td><td>Chậm hơn (đo ở đây: 133,93 ns)</td></tr>
<tr><td><strong>Mật độ</strong></td><td>Thấp hơn</td><td><strong>DÀY ĐẶC HƠN</strong> — ô nhỏ hơn = nhiều ô hơn trên cùng diện tích</td></tr>
<tr><td><strong>Giá mỗi bit</strong></td><td>Cao hơn</td><td><strong>RẺ HƠN</strong></td></tr>
<tr><td><strong>Độ phức tạp chế tạo</strong></td><td>Ô phức tạp hơn, chip đơn giản hơn (không có mạch refresh)</td><td>Ô <strong>đơn giản hơn, nhỏ hơn</strong>; chip cần mạch refresh</td></tr>
<tr><td><strong>Điện năng</strong></td><td>Rò chờ ở mọi ô; không có dòng refresh</td><td>Điện tĩnh mỗi bit thấp hơn, nhưng luôn có dòng refresh</td></tr>
<tr><td><strong>Đọc có phá huỷ?</strong></td><td>Không</td><td>Có — phải ghi trả giá trị về</td></tr>
<tr><td><strong>Được ưa dùng cho</strong></td><td><strong>Bộ nhớ đệm cache (cả trong chip lẫn ngoài chip)</strong></td><td><strong>Nhu cầu dung lượng lớn — bộ nhớ chính</strong></td></tr>
<tr><td><strong>Bay hơi?</strong></td><td colspan="2" style="text-align:center"><strong>CẢ HAI đều bay hơi</strong> — phải cấp điện liên tục mới giữ được giá trị bit</td></tr>
</table>
<ul>
<li><strong>Mọi dòng đều suy ra được từ MỘT bức hình, nên đừng học thuộc mười một sự kiện.</strong> Bắt đầu từ Figure 6.2: 6T so với 1T1C. Ít linh kiện hơn ⇒ ô nhỏ hơn ⇒ dày đặc hơn ⇒ rẻ hơn mỗi bit ⇒ hợp cho bộ nhớ lớn. Lưu bằng điện tích ⇒ rò ⇒ phải refresh ⇒ thêm mạch ⇒ thêm độ trễ. Dùng chốt ⇒ không phải cảm nhận một điện tích tí hon ⇒ nhanh hơn ⇒ hợp cho cache.</li>
<li><strong>"Đơn giản hơn, nhỏ hơn" nói về Ô, không phải về CHIP.</strong> Một <em>CHIP</em> DRAM có thể còn phức tạp hơn chip SRAM — nó mang bộ đếm refresh, bộ khuếch đại cảm nhận và cơ chế dồn kênh RAS/CAS (nhìn thấy hết ở slide 11) mà chip SRAM không cần. Sự đơn giản nằm ở MỖI Ô, mà mỗi ô mới là thứ được nhân lên hàng tỉ lần.</li>
<li><strong>Để ý cụm trong ngoặc "cả trong chip lẫn ngoài chip".</strong> Thời Stallings viết, cache L2/L3 đôi khi là chip SRAM riêng trên bo mạch. Nay nó nằm trong đế, nhưng phát biểu vẫn đúng: cache nằm ở đâu thì ở đó là SRAM. Ngoại lệ mà bản 11th ed thêm vào sau này (slide 31) là <strong>eDRAM</strong> — DRAM nhúng thẳng trên đế bộ xử lý, dùng làm cache mức cuối cực lớn nhưng chậm, nằm CỐ Ý ở giữa hai cột của bảng này.</li>
<li><strong>Nối vào chính máy bạn.</strong> Thanh DDR4/DDR5 của bạn (hoặc 32 GB LPDDR5 đo ở đây) là cột bên phải; L1/L2/L3 trong CPU là cột bên trái. <em>CÙNG MỘT</em> chương trình đụng vào cả hai chỉ cách nhau vài nano giây, và khoảng cách 62 lần đo ở slide 6 chính là toàn bộ lý do tồn tại của Chương 4 và Chương 5.</li>
</ul>
<p class="pitfall">⚠️ Ba cái bẫy, xếp theo mức độ hay sập. (1) <strong>"SRAM không bay hơi vì nó không cần refresh"</strong> — SAI, cả hai đều bay hơi; slide mở đầu bằng đúng câu này để chặn trước. (2) <strong>"DRAM rẻ vì nó chậm"</strong> — nhân quả chạy ngược: nó rẻ vì ô nhỏ, và chính cái ô nhỏ đó cũng làm nó chậm. (3) <strong>"SRAM đơn giản hơn"</strong> — slide nói ô <em>ĐỘNG</em> mới là cái đơn giản hơn.</p>
<p class="meo">💡 Hai chữ để móc vào: <strong>DRAM = ĐÔNG (đông đúc, rẻ, chậm, phải bơm nước)</strong>, <strong>SRAM = SẴN (sẵn sàng ngay, nhanh, đắt, ít chỗ)</strong>.</p>`],

      [8, 'Read Only Memory (ROM) — data wired in at fabrication',
        `<p class="y-chinh">🎯 ROM contains a <strong>permanent pattern of data that cannot be changed or added to</strong>. It is nonvolatile — <em>no power source is required to maintain the bit values</em> — and the data is <strong>actually wired into the chip as part of the fabrication process</strong>.</p>
<ul>
<li><strong>"Wired in" is literal, and it is the key to every other property.</strong> The bit pattern is etched by a photographic <em>mask</em> during manufacture — a connection either exists at that cell or it does not. That is why Table 6.1 lists ROM's write mechanism as <strong>"Masks"</strong> and its erasure as <strong>"Not possible"</strong>. There is no charge, no latch, nothing to lose; hence no power needed, ever.</li>
<li><strong>The advantage the slide names.</strong> Data or program is <em>permanently in main memory and never needs to be loaded from a secondary storage device</em>. This matters for the boot problem: at power-on there is no operating system, no driver, no filesystem — something addressable must already contain valid instructions. Also for embedded systems with no disk at all.</li>
<li><strong>Disadvantage 1 — no room for error.</strong> <em>If one bit is wrong the whole batch of ROMs must be thrown out.</em> Not one chip: the whole batch, because they all came off the same mask. A single-character typo in a firmware string means scrapping tens of thousands of parts.</li>
<li><strong>Disadvantage 2 — a relatively large fixed cost for the data insertion step.</strong> Making the mask set is expensive regardless of how many chips you then print. So the economics are pure fixed-cost amortisation: cost per chip = (mask cost ÷ volume) + per-chip cost. At 1 000 000 units a $100 000 mask adds 10 cents each; at 1 000 units it adds $100 each. <strong>ROM is therefore only attractive at high volume</strong> — which is precisely the sentence that got mis-placed onto slide 9.</li>
<li><strong>Classic uses from the book.</strong> Microprogramming (the control store of Chapter 19), library subroutines for frequently wanted functions, system programs, and function tables. Modern equivalents: the mask ROM boot loader inside a microcontroller or a game cartridge.</li>
</ul>
<p class="dap-an">✅ Worked economics, to make "large fixed cost" concrete. Mask set = $100 000, silicon = $0,50/chip. At 1 000 chips → $100,50 each. At 100 000 chips → $1,50 each. At 10 000 000 chips → $0,51 each. <strong>The fixed cost falls by a factor of 10 000 across that range</strong> — this is the entire argument for why PROM exists.</p>
<p class="pitfall">⚠️ Trap: "read-only" does <em>not</em> mean "cannot be written, ever". It means cannot be written <strong>by the processor during normal operation</strong>. ROM was written once — by a mask. PROM will be written once — by a programmer device. The distinction that matters on an exam is <em>who</em> writes it and <em>when</em>, not whether writing is physically conceivable.</p>`,
        `<p class="y-chinh">🎯 ROM chứa một <strong>mẫu dữ liệu VĨNH VIỄN, không thể thay đổi cũng không thể thêm vào</strong>. Nó không bay hơi — <em>không cần nguồn điện nào để giữ các giá trị bit</em> — và dữ liệu được <strong>ĐẤU THẲNG VÀO CHIP như một phần của quy trình chế tạo</strong>.</p>
<ul>
<li><strong>"Đấu thẳng vào" là nghĩa đen, và nó là chìa khoá cho mọi tính chất khác.</strong> Mẫu bit được khắc bằng một <em>MẶT NẠ</em> quang trong lúc sản xuất — tại ô đó hoặc có một mối nối, hoặc không. Vì thế Table 6.1 ghi cơ chế ghi của ROM là <strong>"Masks"</strong> và phép xoá là <strong>"Not possible"</strong>. Không có điện tích, không có chốt, không có gì để mất; nên không bao giờ cần điện.</li>
<li><strong>Cái lợi mà slide nêu.</strong> Dữ liệu hoặc chương trình <em>nằm vĩnh viễn trong bộ nhớ chính và không bao giờ phải nạp từ thiết bị lưu trữ thứ cấp</em>. Điều này quan trọng cho bài toán khởi động: lúc bật nguồn chưa có hệ điều hành, chưa có trình điều khiển, chưa có hệ thống tệp — phải có sẵn thứ gì đó ĐÁNH ĐỊA CHỈ ĐƯỢC mà đã chứa lệnh hợp lệ. Cũng quan trọng cho hệ nhúng không hề có đĩa.</li>
<li><strong>Nhược điểm 1 — không có chỗ cho sai sót.</strong> <em>Sai một bit là phải vứt cả LÔ ROM.</em> Không phải một con chip: cả lô, vì chúng cùng ra từ một bộ mặt nạ. Gõ sai một ký tự trong chuỗi firmware nghĩa là bỏ đi hàng chục nghìn linh kiện.</li>
<li><strong>Nhược điểm 2 — bước nhồi dữ liệu có CHI PHÍ CỐ ĐỊNH tương đối lớn.</strong> Làm bộ mặt nạ tốn kém bất kể sau đó bạn in ra bao nhiêu chip. Nên kinh tế học ở đây thuần tuý là chia đều chi phí cố định: giá mỗi chip = (giá mặt nạ ÷ sản lượng) + giá từng chip. Với 1 000 000 chip, bộ mặt nạ 100 000 $ thêm 10 xu mỗi con; với 1 000 chip, nó thêm 100 $ mỗi con. <strong>Vậy ROM CHỈ hấp dẫn khi sản lượng LỚN</strong> — đúng là cái câu bị đặt nhầm sang slide 9.</li>
<li><strong>Công dụng kinh điển theo sách.</strong> Vi chương trình (control store của Chương 19), thư viện chương trình con hay dùng, chương trình hệ thống, và bảng hàm. Tương đương ngày nay: ROM mặt nạ chứa bộ nạp khởi động trong một vi điều khiển, hoặc một băng game.</li>
</ul>
<p class="dap-an">✅ Bài tính kinh tế, để chữ "chi phí cố định lớn" thành con số. Bộ mặt nạ = 100 000 $, silic = 0,50 $/chip. Với 1 000 chip → 100,50 $ mỗi con. Với 100 000 chip → 1,50 $ mỗi con. Với 10 000 000 chip → 0,51 $ mỗi con. <strong>Chi phí cố định giảm 10 000 lần trên dải đó</strong> — đây chính là toàn bộ lý lẽ cho sự tồn tại của PROM.</p>
<p class="pitfall">⚠️ Bẫy: "chỉ đọc" <em>KHÔNG</em> nghĩa là "không bao giờ ghi được". Nó nghĩa là không ghi được <strong>BỞI BỘ XỬ LÝ TRONG LÚC CHẠY BÌNH THƯỜNG</strong>. ROM đã được ghi một lần — bằng mặt nạ. PROM sẽ được ghi một lần — bằng máy nạp. Điều đáng phân biệt khi thi là AI ghi và GHI LÚC NÀO, không phải việc ghi có khả thi về vật lý hay không.</p>`],

      [9, 'Programmable ROM (PROM) — written once, electrically, after fabrication',
        `<p class="y-chinh">🎯 PROM is the <strong>less expensive alternative</strong> to mask ROM. It is <strong>nonvolatile and may be written into only once</strong>, and crucially the writing process is <strong>performed electrically and may be performed by supplier or customer at a time later than the original chip fabrication</strong>.</p>
<ul>
<li><strong>The decisive word is "later".</strong> A PROM leaves the factory blank. The pattern is burned in afterwards — by literally blowing tiny fusible links inside the chip with an over-current pulse. One chip, one pattern, one chance. That is why Table 6.1 lists PROM's erasure as "Not possible" but its write mechanism as "Electrically", <em>unlike</em> ROM's "Masks".</li>
<li><strong>What it buys you: no mask cost.</strong> The expensive custom manufacturing step of slide 8 disappears. Chips are mass-produced identical and blank; only the final burn is custom. Fix a firmware bug and you throw away the blank chips you have already burned, not a whole masked production run.</li>
<li><strong>What it costs you: special equipment is required for the writing process.</strong> A PROM programmer (a "burner") — not something the target system does by itself. This is the dividing line between PROM and the read-mostly family on slide 10: EEPROM and Flash can be rewritten <em>in place, in the system, by the system</em>.</li>
<li><strong>"Provides flexibility and convenience"</strong> — flexibility because the pattern is decided after silicon is made; convenience because ordinary distributors can stock one blank part instead of thousands of custom-masked ones.</li>
</ul>
<p class="pitfall">⚠️ <strong>Slide error, flagged not copied.</strong> The last bullet reads <em>"Attractive for high volume production runs"</em>. In Stallings' text that sentence belongs to <strong>ROM</strong>, not PROM: the mask cost only amortises at high volume, so high volume is exactly when you choose ROM <em>instead of</em> PROM. The correct statement for PROM is the opposite — it is attractive at <strong>low to moderate volume</strong>, and whenever the contents are not yet final. If an exam question offers "PROM is attractive for high-volume production" as an option, the expected answer follows the textbook, not this slide. Do not silently learn the bullet as printed.</p>
<p class="dap-an">✅ Decision rule you can defend in a written answer: let <em>F</em> = mask cost, <em>c<sub>R</sub></em> = per-chip cost of masked ROM, <em>c<sub>P</sub></em> = per-chip cost of PROM (blank chip + burning). ROM wins when F + N·c<sub>R</sub> &lt; N·c<sub>P</sub>, i.e. when <strong>N &gt; F ÷ (c<sub>P</sub> − c<sub>R</sub>)</strong>. With F = $100 000, c<sub>R</sub> = $0,50, c<sub>P</sub> = $1,50 the break-even is N = 100 000 units: below that, PROM; above that, ROM.</p>
<p class="meo">💡 Three-word summary of the family so far: <strong>ROM = nhà máy ghi · PROM = bạn ghi, MỘT lần · EPROM trở đi = bạn ghi LẠI được</strong>.</p>`,
        `<p class="y-chinh">🎯 PROM là <strong>phương án RẺ HƠN</strong> so với ROM mặt nạ. Nó <strong>không bay hơi và chỉ ghi được MỘT lần</strong>, và điểm mấu chốt là quá trình ghi được <strong>thực hiện BẰNG ĐIỆN và có thể do nhà cung cấp hoặc khách hàng làm, ở một thời điểm SAU khi chip đã được chế tạo</strong>.</p>
<ul>
<li><strong>Chữ quyết định là "SAU".</strong> Một con PROM rời nhà máy ở trạng thái TRẮNG. Mẫu bit được đốt vào sau đó — theo nghĩa đen là thổi đứt những cầu chì tí hon bên trong chip bằng một xung quá dòng. Một chip, một mẫu, một cơ hội duy nhất. Vì thế Table 6.1 ghi phép xoá của PROM là "Not possible" nhưng cơ chế ghi là "Electrically", <em>KHÁC</em> với "Masks" của ROM.</li>
<li><strong>Cái nó cho bạn: không mất tiền mặt nạ.</strong> Bước sản xuất riêng đắt đỏ ở slide 8 biến mất. Chip được sản xuất hàng loạt giống hệt nhau và để trắng; chỉ lần đốt cuối mới là riêng. Sửa một lỗi firmware thì bạn vứt đi mấy con chip trắng đã đốt, chứ không vứt cả lô đã làm mặt nạ.</li>
<li><strong>Cái nó lấy của bạn: cần thiết bị chuyên dụng cho quá trình ghi.</strong> Một máy nạp PROM ("burner") — không phải thứ mà hệ thống đích tự làm được. Đây chính là ranh giới giữa PROM và họ "chủ yếu đọc" ở slide 10: EEPROM và Flash ghi lại được <em>TẠI CHỖ, TRONG HỆ THỐNG, BỞI CHÍNH HỆ THỐNG</em>.</li>
<li><strong>"Cho tính linh hoạt và tiện lợi"</strong> — linh hoạt vì mẫu bit được quyết định SAU khi silic đã xong; tiện lợi vì nhà phân phối chỉ cần trữ một mã hàng trắng thay vì hàng nghìn mã đã làm mặt nạ riêng.</li>
</ul>
<p class="pitfall">⚠️ <strong>LỖI CỦA SLIDE, nêu ra chứ không chép theo.</strong> Gạch đầu dòng cuối ghi <em>"Attractive for high volume production runs"</em> (hấp dẫn cho sản xuất sản lượng lớn). Trong sách Stallings câu đó thuộc về <strong>ROM</strong>, không phải PROM: chi phí mặt nạ chỉ chia đều được khi sản lượng lớn, nên sản lượng lớn chính là lúc bạn chọn ROM <em>THAY CHO</em> PROM. Phát biểu đúng cho PROM thì ngược lại — nó hấp dẫn ở sản lượng <strong>NHỎ và VỪA</strong>, và bất cứ khi nào nội dung chưa chốt hẳn. Nếu đề thi đưa phương án "PROM hấp dẫn cho sản xuất sản lượng lớn", đáp án mong đợi bám SÁCH chứ không bám slide này. Đừng lặng lẽ học thuộc dòng in trên slide.</p>
<p class="dap-an">✅ Quy tắc quyết định bạn bảo vệ được trong bài tự luận: gọi <em>F</em> = giá bộ mặt nạ, <em>c<sub>R</sub></em> = giá mỗi chip ROM mặt nạ, <em>c<sub>P</sub></em> = giá mỗi chip PROM (chip trắng + công đốt). ROM thắng khi F + N·c<sub>R</sub> &lt; N·c<sub>P</sub>, tức là khi <strong>N &gt; F ÷ (c<sub>P</sub> − c<sub>R</sub>)</strong>. Với F = 100 000 $, c<sub>R</sub> = 0,50 $, c<sub>P</sub> = 1,50 $ thì điểm hoà vốn là N = 100 000 con: dưới mức đó chọn PROM, trên mức đó chọn ROM.</p>
<p class="meo">💡 Tóm cả họ tới đây bằng ba vế: <strong>ROM = nhà máy ghi · PROM = bạn ghi, MỘT lần · từ EPROM trở đi = bạn ghi LẠI được</strong>.</p>`],

      [10, 'Read-Mostly Memory — EPROM, EEPROM and Flash side by side',
        `<p class="y-chinh">🎯 Three panels on one slide, and together they are the third category of Table 6.1: memory that is <strong>read in normal operation but can be rewritten occasionally</strong>. The progression across the panels is a steady loosening of how hard rewriting is.</p>
<table>
<tr><th></th><th>EPROM</th><th>EEPROM</th><th>Flash Memory</th></tr>
<tr><td><strong>Name</strong></td><td>Erasable programmable read-only memory</td><td>Electrically erasable programmable read-only memory</td><td>—</td></tr>
<tr><td><strong>Erased by</strong></td><td><strong>UV light</strong> (Table 6.1)</td><td><strong>Electrically</strong></td><td><strong>Electrically</strong></td></tr>
<tr><td><strong>Erase granularity</strong></td><td>Whole <strong>chip</strong></td><td>Single <strong>byte</strong></td><td><strong>Block</strong> — "does not provide byte-level erasure"</td></tr>
<tr><td><strong>Erase in the system?</strong></td><td>No — remove the chip, put it under a UV lamp</td><td><strong>Yes</strong>, in place</td><td><strong>Yes</strong>, in place</td></tr>
<tr><td><strong>Slide's own bullets</strong></td><td>Erasure process can be performed repeatedly; more expensive than PROM but has the advantage of the multiple update capability</td><td>Can be written into at any time without erasing prior contents; combines the advantage of non-volatility with the flexibility of being updatable in place; more expensive than EPROM</td><td>Intermediate between EPROM and EEPROM in both cost and functionality; uses an electrical erasing technology; microchip is organized so that a section of memory cells are erased in a single action or "flash"</td></tr>
</table>
<ul>
<li><strong>Read the row "erase granularity" as the whole story.</strong> Chip → byte → block. EPROM is coarsest (erase everything, reprogram everything). EEPROM is finest (touch one byte) but that fineness costs area: each cell needs its own erase path, so EEPROM is the least dense and most expensive of the three. Flash takes the middle road deliberately — coarse enough to keep EPROM-like density, fine enough to be updated in the system.</li>
<li><strong>Why EPROM needs ultraviolet light.</strong> The cell traps charge on a <em>floating gate</em> insulated on all sides; only high-energy photons can give the trapped electrons enough energy to escape. That is why an EPROM has a quartz window on top of the package, and why a sticker over that window is not decoration — sunlight will slowly erase the chip.</li>
<li><strong>Flash's headline advantage: "uses only one transistor per bit so it achieves the high density of EPROM"</strong> (slide 34 of the full deck). EEPROM cells need extra devices per byte; Flash gives that up in exchange for block erasure and wins back the density. Density is why Flash, not EEPROM, became the storage of the world.</li>
<li><strong>The line that reaches into Chapter 7.</strong> <em>Flash is an EEPROM that erases by block.</em> Because a block (tens of kB up to several MB) must be erased before any byte inside it can be rewritten, an SSD cannot update data in place; it writes elsewhere and remaps. That forces a flash translation layer, garbage collection, and <strong>wear levelling</strong> to spread the limited erase cycles over all blocks — the whole architecture of Chapter 7's SSD section falls out of this one cell property.</li>
<li><strong>Where each lives on your own machine.</strong> EPROM: essentially museum pieces and old arcade boards. EEPROM: small configuration stores — a monitor's EDID, a motherboard's serial number, sensor calibration. Flash: your BIOS/UEFI chip, your SSD, every USB stick, every phone.</li>
</ul>
<p class="meo">💡 Mnemonic for the three erase methods: <strong>"EPROM tắm nắng cả người · EEPROM gãi từng chỗ · Flash dội cả xô"</strong>. The Vietnamese verbs encode chip-level, byte-level and block-level respectively.</p>
<p class="pitfall">⚠️ Trap: EEPROM "can be written into at any time <em>without erasing prior contents</em>" does NOT mean writing is free or fast. An EEPROM byte write takes milliseconds — thousands of times slower than a DRAM write — and each cell survives only ~10<sup>4</sup>–10<sup>6</sup> erase cycles. "Read-mostly" is a description of what the hardware can bear, not merely of what programmers happen to do.</p>`,
        `<p class="y-chinh">🎯 Ba khung trên một slide, và gộp lại chúng là nhóm thứ ba của Table 6.1: bộ nhớ <strong>ĐỌC trong lúc chạy bình thường nhưng thỉnh thoảng GHI LẠI được</strong>. Đi từ khung này sang khung kia là chuyện nới lỏng dần độ khó của việc ghi lại.</p>
<table>
<tr><th></th><th>EPROM</th><th>EEPROM</th><th>Bộ nhớ Flash</th></tr>
<tr><td><strong>Tên đầy đủ</strong></td><td>ROM lập trình được, xoá được</td><td>ROM lập trình được, xoá được BẰNG ĐIỆN</td><td>—</td></tr>
<tr><td><strong>Xoá bằng</strong></td><td><strong>TIA CỰC TÍM</strong> (Table 6.1)</td><td><strong>ĐIỆN</strong></td><td><strong>ĐIỆN</strong></td></tr>
<tr><td><strong>Đơn vị xoá</strong></td><td>Cả <strong>CHIP</strong></td><td>Từng <strong>BYTE</strong></td><td><strong>KHỐI</strong> — "không hỗ trợ xoá mức byte"</td></tr>
<tr><td><strong>Xoá ngay trong hệ thống?</strong></td><td>Không — phải tháo chip ra, đặt dưới đèn tử ngoại</td><td><strong>CÓ</strong>, tại chỗ</td><td><strong>CÓ</strong>, tại chỗ</td></tr>
<tr><td><strong>Gạch đầu dòng của slide</strong></td><td>Xoá đi xoá lại nhiều lần được; đắt hơn PROM nhưng bù lại có khả năng cập nhật nhiều lần</td><td>Ghi vào bất cứ lúc nào mà không cần xoá nội dung cũ; kết hợp cái lợi không bay hơi với sự linh hoạt cập nhật TẠI CHỖ; đắt hơn EPROM</td><td>Nằm giữa EPROM và EEPROM cả về giá lẫn chức năng; dùng công nghệ xoá bằng điện; vi mạch tổ chức sao cho một VÙNG ô nhớ bị xoá trong MỘT thao tác duy nhất, tức "flash" (loé)</td></tr>
</table>
<ul>
<li><strong>Đọc dòng "đơn vị xoá" là hiểu hết câu chuyện.</strong> Chip → byte → khối. EPROM thô nhất (xoá sạch, nạp lại sạch). EEPROM mịn nhất (đụng một byte) nhưng cái mịn đó tốn diện tích: mỗi ô cần đường xoá riêng, nên EEPROM là loại thưa nhất và đắt nhất trong ba. Flash chọn đường giữa một cách CỐ Ý — đủ thô để giữ mật độ kiểu EPROM, đủ mịn để cập nhật ngay trong hệ thống.</li>
<li><strong>Vì sao EPROM cần tia cực tím.</strong> Ô nhớ nhốt điện tích trên một <em>CỔNG NỔI</em> bị cách điện mọi phía; chỉ photon năng lượng cao mới đủ sức tiếp năng lượng cho electron bị nhốt thoát ra. Vì thế con EPROM có một cửa sổ thạch anh trên vỏ, và miếng nhãn dán che cửa sổ đó không phải để trang trí — ánh nắng sẽ từ từ xoá con chip.</li>
<li><strong>Lợi thế đinh của Flash: "chỉ dùng MỘT transistor mỗi bit nên đạt mật độ cao như EPROM"</strong> (slide 34 của deck đầy đủ). Ô EEPROM cần thêm linh kiện cho mỗi byte; Flash từ bỏ điều đó để đổi lấy xoá theo khối và giành lại mật độ. Chính mật độ là lý do Flash — chứ không phải EEPROM — trở thành bộ nhớ lưu trữ của cả thế giới.</li>
<li><strong>Câu nối thẳng sang Chương 7.</strong> <em>Flash là EEPROM xoá theo KHỐI.</em> Vì một khối (vài chục kB tới vài MB) phải bị xoá trước khi bất kỳ byte nào trong nó được ghi lại, SSD KHÔNG cập nhật dữ liệu tại chỗ được; nó ghi sang chỗ khác rồi ánh xạ lại. Điều đó bắt buộc phải có lớp dịch địa chỉ flash, thu gom rác, và <strong>SAN ĐỀU HAO MÒN (wear levelling)</strong> để rải số lần xoá có hạn ra khắp các khối — toàn bộ kiến trúc phần SSD của Chương 7 rơi ra từ đúng một tính chất ô nhớ này.</li>
<li><strong>Mỗi loại sống ở đâu trên máy của bạn.</strong> EPROM: gần như chỉ còn trong bảo tàng và bo máy game thùng cũ. EEPROM: các kho cấu hình nhỏ — EDID của màn hình, số sê-ri bo mạch chủ, dữ liệu hiệu chuẩn cảm biến. Flash: chip BIOS/UEFI, ổ SSD, mọi USB, mọi điện thoại.</li>
</ul>
<p class="meo">💡 Mẹo nhớ ba kiểu xoá: <strong>"EPROM tắm nắng cả người · EEPROM gãi từng chỗ · Flash dội cả xô"</strong>. Ba động từ đó mã hoá đúng mức chip, mức byte và mức khối.</p>
<p class="pitfall">⚠️ Bẫy: EEPROM "ghi vào bất cứ lúc nào <em>mà không cần xoá nội dung cũ</em>" KHÔNG có nghĩa là ghi rẻ hay ghi nhanh. Ghi một byte EEPROM mất hàng mili giây — chậm hơn ghi DRAM hàng nghìn lần — và mỗi ô chỉ chịu được cỡ 10<sup>4</sup>–10<sup>6</sup> chu kỳ xoá. "Chủ yếu đọc" là mô tả thứ PHẦN CỨNG CHỊU ĐƯỢC, không đơn thuần mô tả thói quen của lập trình viên.</p>`],

      [11, 'Figure 6.3 — Typical 16-Mbit DRAM (4M × 4): the internal logic of one chip',
        `<p class="y-chinh">🎯 One whole DRAM chip, drawn as a block diagram. It is <strong>16 Mbit organised as 4M × 4</strong>: 4 194 304 addressable words, 4 bits each. Every block on this figure exists to solve one of the problems raised on slides 2 and 4.</p>
<p class="nhan">📐 Blocks and labels, top-left to bottom-right: control pins <strong>RAS̄, CAS̄, WĒ, OĒ</strong> → <em>Timing and Control</em>; <em>Refresh Counter</em> → <em>MUX</em>; address pins <strong>A0 … A10</strong> (11 lines) → <em>Row Address Buffer</em> and <em>Column Address Buffer</em>; <em>Row Decoder</em> → <em>Memory array (2048 × 2048 × 4)</em>; <em>Refresh circuitry</em> and <em>Column Decoder</em> below it; <em>Data Input Buffer</em> / <em>Data Output Buffer</em> → data pins <strong>D1 D2 D3 D4</strong>.</p>
<ul>
<li><strong>Count the address pins and the mystery appears.</strong> 4M words needs log<sub>2</sub>(4 194 304) = <strong>22 address bits</strong>. The chip has only <strong>11</strong> address pins, A0–A10. The gap is closed by <em>time</em>, not by wires: the 22-bit address is sent in <strong>two halves on the same 11 pins</strong> — the row half first, latched by RAS̄ (Row Address Strobe), then the column half, latched by CAS̄ (Column Address Strobe). That is exactly why the figure has two separate buffers fed by one bundle of pins.</li>
<li><strong>Why bother multiplexing? Pins are the scarcest resource on a chip.</strong> Pins cost package size, board area and money, and they scale badly. Sending the address in two strobes halves the address pin count: <strong>22 → 11, a saving of 11 pins, exactly 50%</strong>. For a 1M-word chip it is 20 → 10, for a 16M-word chip 24 → 12 — always 50%. The price is a two-step address cycle, which is where the DRAM's "row access time" then "column access time" both come from.</li>
<li><strong>The array is square on purpose: 2048 × 2048 × 4.</strong> 2048 = 2<sup>11</sup>, so 11 bits select a row and 11 bits select a column — the two halves are equal, which is why one 11-pin bundle serves both. And 2048 × 2048 × 4 = 16 777 216 bits = 16 Mbit exactly. A square array also minimises the length of the longest wire, which minimises delay.</li>
<li><strong>The Refresh Counter and MUX implement slide 4's refresh, in hardware.</strong> The counter walks row addresses automatically; the MUX chooses between "the row the processor asked for" and "the row the refresh counter is up to". Refresh is therefore <em>invisible to the processor</em> — the chip does it to itself. The <em>Refresh circuitry</em> box under the array is the set of sense amplifiers that rewrite the whole row after sensing it.</li>
<li><strong>Four data pins, not one and not eight.</strong> "× 4" is the chip's <em>word width</em>. It matters because a module builder must combine chips side by side to reach the width the bus wants — 4 such chips give a 16-bit word, 8 give 32 bits. Slides 13–14 do exactly this arithmetic.</li>
</ul>
<p class="dap-an">✅ Full check of the figure's own numbers: 2048 rows × 2048 columns = 4 194 304 = 4M addresses ✓ · 4M × 4 bits = 16 777 216 bits = 16 Mbit ✓ · 16 Mbit ÷ 8 = 2 MByte of raw storage on one chip ✓ · address bits 11 (row) + 11 (column) = 22 = log<sub>2</sub>(4M) ✓.</p>
<p class="pitfall">⚠️ Trap: the overbars on RAS̄, CAS̄, WĒ, OĒ mean these signals are <strong>active LOW</strong> — the operation happens when the line is pulled to 0, not to 1. A timing question that assumes "RAS high = row being strobed" gets every edge backwards.</p>`,
        `<p class="y-chinh">🎯 Nguyên một con chip DRAM, vẽ dưới dạng sơ đồ khối. Nó là <strong>16 Mbit tổ chức kiểu 4M × 4</strong>: 4 194 304 từ đánh địa chỉ được, mỗi từ 4 bit. Mọi khối trên hình này tồn tại để giải đúng một vấn đề đã nêu ở slide 2 và slide 4.</p>
<p class="nhan">📐 Các khối và nhãn, từ trên-trái xuống dưới-phải: chân điều khiển <strong>RAS̄, CAS̄, WĒ, OĒ</strong> → <em>Timing and Control</em> (định thời và điều khiển); <em>Refresh Counter</em> (bộ đếm làm tươi) → <em>MUX</em> (bộ dồn kênh); chân địa chỉ <strong>A0 … A10</strong> (11 đường) → <em>Row Address Buffer</em> và <em>Column Address Buffer</em>; <em>Row Decoder</em> → <em>Memory array (2048 × 2048 × 4)</em>; bên dưới là <em>Refresh circuitry</em> và <em>Column Decoder</em>; <em>Data Input Buffer</em> / <em>Data Output Buffer</em> → chân dữ liệu <strong>D1 D2 D3 D4</strong>.</p>
<ul>
<li><strong>Đếm số chân địa chỉ là bí ẩn hiện ra ngay.</strong> 4M từ cần log<sub>2</sub>(4 194 304) = <strong>22 bit địa chỉ</strong>. Mà chip chỉ có <strong>11</strong> chân địa chỉ, A0–A10. Khoảng hụt được lấp bằng <em>THỜI GIAN</em>, không phải bằng dây: địa chỉ 22 bit được gửi làm <strong>HAI NỬA TRÊN CÙNG 11 CHÂN</strong> — nửa HÀNG trước, chốt bằng RAS̄ (Row Address Strobe), rồi nửa CỘT, chốt bằng CAS̄ (Column Address Strobe). Đúng vì thế mà hình có hai bộ đệm riêng nhưng chỉ có một bó chân nuôi cả hai.</li>
<li><strong>Dồn kênh làm gì cho mệt? Vì CHÂN là tài nguyên hiếm nhất trên một con chip.</strong> Chân tốn kích thước vỏ, tốn diện tích bo mạch, tốn tiền, và nó không nở ra dễ. Gửi địa chỉ làm hai nhịp thì số chân địa chỉ giảm một nửa: <strong>22 → 11, tiết kiệm 11 chân, đúng 50%</strong>. Chip 1M từ thì 20 → 10, chip 16M từ thì 24 → 12 — luôn luôn 50%. Cái giá là chu kỳ địa chỉ hai bước, và đó chính là nguồn gốc của hai con số "thời gian truy cập hàng" rồi "thời gian truy cập cột" của DRAM.</li>
<li><strong>Mảng ô nhớ VUÔNG là cố ý: 2048 × 2048 × 4.</strong> 2048 = 2<sup>11</sup>, nên 11 bit chọn hàng và 11 bit chọn cột — hai nửa BẰNG NHAU, vì thế một bó 11 chân phục vụ được cả hai. Và 2048 × 2048 × 4 = 16 777 216 bit = đúng 16 Mbit. Mảng vuông còn làm cho sợi dây dài nhất ngắn nhất có thể, tức là độ trễ nhỏ nhất có thể.</li>
<li><strong>Refresh Counter và MUX chính là phần cứng hoá việc làm tươi ở slide 4.</strong> Bộ đếm tự chạy qua các địa chỉ hàng; MUX chọn giữa "hàng mà bộ xử lý đang hỏi" và "hàng mà bộ đếm làm tươi đang tới". Nhờ đó làm tươi <em>VÔ HÌNH với bộ xử lý</em> — chip tự làm cho chính nó. Khối <em>Refresh circuitry</em> dưới mảng là tập các bộ khuếch đại cảm nhận, làm nhiệm vụ ghi trả nguyên hàng sau khi đã cảm nhận.</li>
<li><strong>Bốn chân dữ liệu, không phải một cũng không phải tám.</strong> "× 4" là <em>ĐỘ RỘNG TỪ</em> của chip. Nó quan trọng vì người dựng module phải ghép nhiều chip nằm cạnh nhau cho đủ độ rộng mà bus đòi — 4 con như vậy cho từ 16 bit, 8 con cho 32 bit. Slide 13–14 làm đúng phép tính đó.</li>
</ul>
<p class="dap-an">✅ Kiểm trọn bộ số của chính hình: 2048 hàng × 2048 cột = 4 194 304 = 4M địa chỉ ✓ · 4M × 4 bit = 16 777 216 bit = 16 Mbit ✓ · 16 Mbit ÷ 8 = 2 MByte dung lượng thô trên một chip ✓ · số bit địa chỉ 11 (hàng) + 11 (cột) = 22 = log<sub>2</sub>(4M) ✓.</p>
<p class="pitfall">⚠️ Bẫy: gạch ngang trên đầu RAS̄, CAS̄, WĒ, OĒ nghĩa là các tín hiệu này <strong>TÍCH CỰC MỨC THẤP</strong> — thao tác xảy ra khi đường dây bị kéo xuống 0, không phải lên 1. Câu hỏi về giản đồ thời gian mà giả định "RAS lên cao = đang chốt hàng" sẽ sai mọi sườn xung.</p>`],

      [12, 'Figure 6.4 — Typical Memory Package Pins and Signals: (a) 8 Mbit EPROM, (b) 16 Mbit DRAM',
        `<p class="y-chinh">🎯 The same two chips as seen from <em>outside</em> — every pin of the package, numbered. Put the two packages side by side and the pin-saving argument of slide 11 becomes something you can count: <strong>a 32-pin EPROM versus a 24-pin DRAM, and the DRAM is the bigger chip.</strong></p>
<table>
<tr><th></th><th>(a) 8 Mbit EPROM — 1M × 8</th><th>(b) 16 Mbit DRAM — 4M × 4</th></tr>
<tr><td><strong>Package</strong></td><td>32 Pin Dip, 0.6"</td><td>24 Pin Dip, 0.6"</td></tr>
<tr><td><strong>Address pins</strong></td><td><strong>A0 … A19</strong> = 20 pins, all present at once</td><td><strong>A0 … A10</strong> = 11 pins, multiplexed</td></tr>
<tr><td><strong>Data pins</strong></td><td>D0 … D7 = 8, <strong>output only</strong> (arrows point out)</td><td>D0 … D3 = 4, <strong>bidirectional</strong> (double-headed arrows)</td></tr>
<tr><td><strong>Control pins</strong></td><td>CE (chip enable), Vpp (programming voltage)</td><td>WE, RAS, CAS, OE</td></tr>
<tr><td><strong>Power</strong></td><td>Vcc, Vss</td><td>Vcc (×2), Vss (×2)</td></tr>
<tr><td><strong>Other</strong></td><td>—</td><td>NC (not connected) on pin 6</td></tr>
</table>
<ul>
<li><strong>The arithmetic that makes the figure worth studying.</strong> The EPROM is 1M × 8: log<sub>2</sub>(1 048 576) = <strong>20 address bits</strong>, and it has exactly 20 address pins — <em>no multiplexing</em>. The DRAM is 4M × 4: 22 address bits, but only 11 pins. So the <em>smaller-capacity</em> chip needs <em>nine more</em> address pins than the bigger one. Multiplexing is why.</li>
<li><strong>Vpp is the tell-tale of the EPROM.</strong> It is the elevated programming voltage used only when burning the chip; during normal reading it sits idle. A DRAM has no such pin because writing is an ordinary operation at ordinary voltage. If an exam shows you a pinout and asks what kind of memory it is, <strong>Vpp ⇒ programmable ROM family; RAS + CAS ⇒ DRAM</strong>.</li>
<li><strong>Read the arrow directions, they carry information.</strong> On the EPROM, D0–D7 arrows point <em>outward</em> only — during normal operation the chip can only be read. On the DRAM, D0–D3 are drawn with <em>double-headed</em> arrows — the same pins carry data in and data out, exactly as promised by the single "Data in / Sense" terminal of Figure 6.1.</li>
<li><strong>CE and OE are two different things, and students merge them.</strong> <strong>CE</strong> (chip enable, also written CS) selects <em>this chip out of many</em> — it is the pin that slides 13–14 wire to a decoder. <strong>OE</strong> (output enable) controls whether the chip drives the shared data bus. A chip can be enabled but not driving the bus; that is how many chips share one bus without a fight.</li>
<li><strong>"NC" on DRAM pin 6 is not a mistake.</strong> Not-connected pins exist so that one package footprint can serve several capacities in the same family: the larger member of the family uses that pin as an extra address line. Board designers route it anyway, so a memory upgrade needs no new PCB.</li>
</ul>
<p class="dap-an">✅ Pin-budget comparison, computed: if the 16 Mbit DRAM did <em>not</em> multiplex its address, it would need 22 address + 4 data + 4 control + 4 power = 34 pins, so its package would have to grow past 32 pins. With RAS/CAS multiplexing it fits in <strong>24</strong>. Saving = <strong>10 pins, ~29% of the package</strong>, on a part that is manufactured in the billions.</p>
<p class="pitfall">⚠️ Naming trap the slide itself sets: it labels (a) "<strong>8 Mbit</strong> EPROM" for a 1M × 8 part and (b) "<strong>16 Mbit</strong> DRAM" for 4M × 4. Memory chips are sized in <em>bits</em>, memory modules and systems in <em>bytes</em>. 1M × 8 = 8 Mbit = only 1 MByte. Reading "8 M" as megabytes inflates your answer eightfold — and this trips people on almost every chip-counting exercise.</p>`,
        `<p class="y-chinh">🎯 Vẫn hai con chip đó nhưng nhìn từ <em>BÊN NGOÀI</em> — từng chân của vỏ, có đánh số. Đặt hai cái vỏ cạnh nhau thì lý lẽ tiết kiệm chân ở slide 11 trở thành thứ đếm được: <strong>EPROM 32 chân so với DRAM 24 chân, mà DRAM mới là con chip LỚN HƠN.</strong></p>
<table>
<tr><th></th><th>(a) EPROM 8 Mbit — 1M × 8</th><th>(b) DRAM 16 Mbit — 4M × 4</th></tr>
<tr><td><strong>Vỏ</strong></td><td>32 Pin Dip, 0.6"</td><td>24 Pin Dip, 0.6"</td></tr>
<tr><td><strong>Chân địa chỉ</strong></td><td><strong>A0 … A19</strong> = 20 chân, có mặt CÙNG LÚC</td><td><strong>A0 … A10</strong> = 11 chân, DỒN KÊNH</td></tr>
<tr><td><strong>Chân dữ liệu</strong></td><td>D0 … D7 = 8, <strong>CHỈ RA</strong> (mũi tên hướng ra)</td><td>D0 … D3 = 4, <strong>HAI CHIỀU</strong> (mũi tên hai đầu)</td></tr>
<tr><td><strong>Chân điều khiển</strong></td><td>CE (chip enable), Vpp (điện áp nạp)</td><td>WE, RAS, CAS, OE</td></tr>
<tr><td><strong>Nguồn</strong></td><td>Vcc, Vss</td><td>Vcc (×2), Vss (×2)</td></tr>
<tr><td><strong>Khác</strong></td><td>—</td><td>NC (không nối) ở chân số 6</td></tr>
</table>
<ul>
<li><strong>Phép tính khiến hình này đáng học.</strong> EPROM là 1M × 8: log<sub>2</sub>(1 048 576) = <strong>20 bit địa chỉ</strong>, và nó có đúng 20 chân địa chỉ — <em>KHÔNG dồn kênh</em>. DRAM là 4M × 4: 22 bit địa chỉ, nhưng chỉ 11 chân. Nghĩa là con chip <em>DUNG LƯỢNG NHỎ HƠN</em> lại cần <em>NHIỀU HƠN CHÍN</em> chân địa chỉ so với con lớn hơn. Dồn kênh chính là lý do.</li>
<li><strong>Vpp là dấu hiệu nhận dạng của EPROM.</strong> Đó là điện áp nạp cao, chỉ dùng lúc đốt chip; lúc đọc bình thường nó nằm không. DRAM không có chân như vậy vì ghi với nó là thao tác bình thường ở điện áp bình thường. Đề thi đưa một sơ đồ chân rồi hỏi đây là loại bộ nhớ gì thì: <strong>thấy Vpp ⇒ họ ROM lập trình được; thấy RAS + CAS ⇒ DRAM</strong>.</li>
<li><strong>Đọc CHIỀU mũi tên, nó mang thông tin.</strong> Trên EPROM, mũi tên D0–D7 chỉ hướng <em>RA</em> — lúc chạy bình thường chip chỉ đọc được. Trên DRAM, D0–D3 vẽ bằng mũi tên <em>HAI ĐẦU</em> — cùng những chân đó mang dữ liệu vào và dữ liệu ra, đúng như lời hứa của một đầu dây "Data in / Sense" duy nhất ở Figure 6.1.</li>
<li><strong>CE và OE là hai thứ khác nhau, mà sinh viên hay gộp.</strong> <strong>CE</strong> (chip enable, còn viết CS) chọn <em>ĐÚNG CHIP NÀY trong nhiều chip</em> — đây là chân mà slide 13–14 sẽ đấu vào bộ giải mã. <strong>OE</strong> (output enable) quyết định chip có lái đường bus dữ liệu dùng chung hay không. Một chip có thể đang được chọn mà vẫn không lái bus; đó là cách nhiều chip dùng chung một bus mà không đánh nhau.</li>
<li><strong>Chữ "NC" ở chân 6 của DRAM không phải lỗi.</strong> Chân không-nối tồn tại để một kiểu chân đế phục vụ được nhiều dung lượng trong cùng một họ: thành viên lớn hơn của họ dùng chân đó làm một đường địa chỉ phụ. Người thiết kế bo vẫn đi dây cho nó, nên nâng cấp bộ nhớ không cần làm bo mới.</li>
</ul>
<p class="dap-an">✅ So sánh ngân sách chân, tính ra số: nếu con DRAM 16 Mbit <em>KHÔNG</em> dồn kênh địa chỉ thì nó cần 22 địa chỉ + 4 dữ liệu + 4 điều khiển + 4 nguồn = 34 chân, tức vỏ phải phình quá 32 chân. Có dồn kênh RAS/CAS thì nó vừa trong <strong>24</strong> chân. Tiết kiệm = <strong>10 chân, ~29% cái vỏ</strong>, trên một linh kiện sản xuất hàng tỉ con.</p>
<p class="pitfall">⚠️ Bẫy CÁCH GỌI TÊN do chính slide đặt ra: nó ghi (a) "EPROM <strong>8 Mbit</strong>" cho linh kiện 1M × 8 và (b) "DRAM <strong>16 Mbit</strong>" cho 4M × 4. Chip nhớ được tính theo <em>BIT</em>, còn module và hệ thống tính theo <em>BYTE</em>. 1M × 8 = 8 Mbit = chỉ 1 MByte. Đọc "8 M" thành megabyte là thổi đáp án lên gấp tám — và đây là chỗ vấp ở gần như mọi bài tập đếm chip.</p>`],

      [13, 'Figure 6.5 — 256-KByte Memory Organization: combining chips to widen the word',
        `<p class="y-chinh">🎯 The first of the two module-building figures. Eight identical chips, each <strong>512 words by 512 bits</strong>, are wired so that together they behave as one memory of <strong>256K addresses × 8 bits</strong>. The trick: each chip contributes <strong>one bit</strong> of every byte.</p>
<p class="nhan">📐 Labels on the figure: <em>Memory address register (MAR)</em> on the left, split into two fields marked <strong>9</strong> and <strong>9</strong>; each chip has a <em>Decode 1 of 512</em> block down its left side (row) and a <em>Decode 1 of 512 bit-sense</em> block along its bottom (column); chips are numbered <em>Chip #1</em> … <em>Chip #8</em>; on the right the <em>Memory buffer register (MBR)</em> with bit positions <strong>1 … 8</strong>.</p>
<ul>
<li><strong>Follow one address through the picture.</strong> The 18-bit MAR splits 9 + 9. The first 9 bits go to every chip's row decoder ("decode 1 of 512" — and 2<sup>9</sup> = 512 ✓). The second 9 bits go to every chip's column decoder. Each chip therefore selects exactly one cell out of 512 × 512 and emits <strong>one bit</strong>. Eight chips, eight bits, one byte, delivered into the MBR.</li>
<li><strong>Why not one chip holding whole bytes?</strong> Because bit-slicing is what lets you build <em>any</em> width from one part number. It also spreads a single-bit chip failure across eight different bytes rather than destroying one byte completely — which is precisely the arrangement that makes the Hamming ECC of slides 17–20 able to repair it.</li>
<li><strong>The MAR/MBR pair is Chapter 3 coming back.</strong> MAR holds the address the processor put on the address bus; MBR holds the data word travelling on the data bus. This figure is the inside of the "main memory" box you drew in the Chapter 3 top-level view.</li>
</ul>
<p class="dap-an">✅ <strong>Verified arithmetic for this figure.</strong> One chip = 512 × 512 = 262 144 bits = 32 kB of raw cells. Eight chips = 8 × 262 144 = <strong>2 097 152 bits = 262 144 bytes = 256 KByte</strong> ✓. Addresses = 2<sup>9+9</sup> = 2<sup>18</sup> = 262 144 = <strong>256K</strong> ✓, each 8 bits wide ✓. Total bits check both ways: 256K × 8 = 2 097 152 = 8 × (512 × 512) ✓.</p>
<p class="nhan">📐 <strong>Exam exercise 1 — the standard form.</strong> "You have <strong>256K × 8</strong> chips. Build a <strong>1M × 16</strong> memory. How many chips, in how many rows and columns? How many address lines, and where does each go?"</p>
<ul>
<li><strong>Step 1 — widen (columns).</strong> Target word 16 bits ÷ chip word 8 bits = <strong>2 chips per row</strong>. These two share the same address lines and the same chip-select; one supplies bits 0–7, the other bits 8–15.</li>
<li><strong>Step 2 — deepen (rows).</strong> Target 1M words ÷ chip 256K words = <strong>4 rows</strong>. Only one row may be active at a time, so the rows are distinguished by chip select.</li>
<li><strong>Step 3 — total chips.</strong> 4 rows × 2 columns = <strong>8 chips</strong>.</li>
<li><strong>Step 4 — address lines.</strong> 1M words needs log<sub>2</sub>(1 048 576) = <strong>20 lines</strong>. Each chip consumes log<sub>2</sub>(262 144) = <strong>18</strong> of them (the low-order 18, A0–A17), which go to <em>every</em> chip in parallel. The remaining <strong>20 − 18 = 2</strong> high-order lines (A18, A19) go to a <strong>2-to-4 decoder</strong> whose four outputs drive the chip-select (CE) pin of the four rows.</li>
</ul>
<p class="dap-an">✅ Checked by machine, and rebuilt backwards to confirm: 4 × 256K = 1M ✓ · 2 × 8 = 16 ✓ · total bits 1M × 16 = 16 777 216 = 8 × (256K × 8) = 8 × 2 097 152 ✓ · 2<sup>2</sup> = 4 = number of rows ✓. <strong>Answer: 8 chips, arranged 4 rows × 2 columns; 20 address lines, of which 18 go to all chips and 2 feed a 2-to-4 decoder driving the chip-select pins.</strong></p>
<p class="meo">💡 The recipe never changes, so learn it as two independent divisions: <strong>columns = target width ÷ chip width</strong> (widening), <strong>rows = target depth ÷ chip depth</strong> (deepening), <strong>chips = rows × columns</strong>, <strong>select lines = log<sub>2</sub>(rows)</strong>. Widening never consumes address lines; deepening always does.</p>`,
        `<p class="y-chinh">🎯 Hình đầu tiên trong hai hình dựng module. Tám con chip giống hệt nhau, mỗi con <strong>512 từ × 512 bit</strong>, được đấu sao cho gộp lại chúng hành xử như một bộ nhớ <strong>256K địa chỉ × 8 bit</strong>. Mẹo nằm ở chỗ: mỗi chip góp <strong>ĐÚNG MỘT BIT</strong> của mỗi byte.</p>
<p class="nhan">📐 Nhãn trên hình: <em>Memory address register (MAR)</em> bên trái, chia thành hai trường ghi <strong>9</strong> và <strong>9</strong>; mỗi chip có một khối <em>Decode 1 of 512</em> dọc cạnh trái (hàng) và một khối <em>Decode 1 of 512 bit-sense</em> dọc cạnh dưới (cột); các chip đánh số <em>Chip #1</em> … <em>Chip #8</em>; bên phải là <em>Memory buffer register (MBR)</em> với các vị trí bit <strong>1 … 8</strong>.</p>
<ul>
<li><strong>Bám theo MỘT địa chỉ đi qua bức hình.</strong> MAR 18 bit chia 9 + 9. Chín bit đầu đi vào bộ giải mã HÀNG của MỌI chip ("decode 1 of 512" — và 2<sup>9</sup> = 512 ✓). Chín bit sau đi vào bộ giải mã CỘT của MỌI chip. Nhờ đó mỗi chip chọn đúng một ô trong 512 × 512 và phát ra <strong>MỘT bit</strong>. Tám chip, tám bit, một byte, chuyển vào MBR.</li>
<li><strong>Sao không dùng một chip chứa nguyên byte?</strong> Vì cắt theo BIT mới cho phép dựng ra <em>BẤT KỲ</em> độ rộng nào từ cùng một mã linh kiện. Nó còn rải một sự cố hỏng một bit của chip ra tám byte KHÁC NHAU thay vì phá nát trọn một byte — và đó đúng là cách bố trí khiến mã Hamming ở slide 17–20 sửa được lỗi.</li>
<li><strong>Cặp MAR/MBR là Chương 3 quay lại.</strong> MAR giữ địa chỉ mà bộ xử lý đặt lên bus địa chỉ; MBR giữ từ dữ liệu đang chạy trên bus dữ liệu. Hình này chính là bên trong cái hộp "bộ nhớ chính" mà bạn đã vẽ ở bức tranh tổng thể Chương 3.</li>
</ul>
<p class="dap-an">✅ <strong>Kiểm số học của chính hình này.</strong> Một chip = 512 × 512 = 262 144 bit = 32 kB ô nhớ thô. Tám chip = 8 × 262 144 = <strong>2 097 152 bit = 262 144 byte = 256 KByte</strong> ✓. Số địa chỉ = 2<sup>9+9</sup> = 2<sup>18</sup> = 262 144 = <strong>256K</strong> ✓, mỗi địa chỉ rộng 8 bit ✓. Kiểm tổng bit theo cả hai đường: 256K × 8 = 2 097 152 = 8 × (512 × 512) ✓.</p>
<p class="nhan">📐 <strong>Bài tập thi số 1 — dạng chuẩn.</strong> "Bạn có chip <strong>256K × 8</strong>. Hãy dựng bộ nhớ <strong>1M × 16</strong>. Cần bao nhiêu chip, xếp mấy hàng mấy cột? Bao nhiêu đường địa chỉ, và mỗi đường đi đâu?"</p>
<ul>
<li><strong>Bước 1 — NỚI RỘNG (số cột).</strong> Từ đích 16 bit ÷ từ của chip 8 bit = <strong>2 chip mỗi hàng</strong>. Hai con này dùng chung y hệt các đường địa chỉ và y hệt tín hiệu chọn chip; một con cấp bit 0–7, con kia cấp bit 8–15.</li>
<li><strong>Bước 2 — ĐÀO SÂU (số hàng).</strong> 1M từ đích ÷ 256K từ của chip = <strong>4 hàng</strong>. Mỗi lúc chỉ một hàng được phép hoạt động, nên các hàng phân biệt nhau bằng tín hiệu chọn chip.</li>
<li><strong>Bước 3 — tổng số chip.</strong> 4 hàng × 2 cột = <strong>8 chip</strong>.</li>
<li><strong>Bước 4 — đường địa chỉ.</strong> 1M từ cần log<sub>2</sub>(1 048 576) = <strong>20 đường</strong>. Mỗi chip ăn log<sub>2</sub>(262 144) = <strong>18</strong> đường trong đó (18 đường THẤP, A0–A17), và chúng đi song song vào <em>MỌI</em> chip. Còn lại <strong>20 − 18 = 2</strong> đường CAO (A18, A19) đi vào một <strong>bộ giải mã 2 sang 4</strong>, bốn ngõ ra của nó lái chân chọn chip (CE) của bốn hàng.</li>
</ul>
<p class="dap-an">✅ Đã kiểm bằng máy, và dựng ngược lại để đối chiếu: 4 × 256K = 1M ✓ · 2 × 8 = 16 ✓ · tổng bit 1M × 16 = 16 777 216 = 8 × (256K × 8) = 8 × 2 097 152 ✓ · 2<sup>2</sup> = 4 = số hàng ✓. <strong>Đáp án: 8 chip, xếp 4 hàng × 2 cột; 20 đường địa chỉ, trong đó 18 đường đi vào tất cả các chip và 2 đường nuôi một bộ giải mã 2→4 lái các chân chọn chip.</strong></p>
<p class="meo">💡 Công thức không bao giờ đổi, nên hãy học nó thành hai phép chia độc lập: <strong>số cột = độ rộng đích ÷ độ rộng chip</strong> (nới rộng), <strong>số hàng = độ sâu đích ÷ độ sâu chip</strong> (đào sâu), <strong>số chip = hàng × cột</strong>, <strong>số đường chọn = log<sub>2</sub>(số hàng)</strong>. Nới rộng KHÔNG bao giờ tốn đường địa chỉ; đào sâu thì LUÔN tốn.</p>`],

      [14, 'Figure 6.6 — 1-MB Memory Organization: adding chip groups and a select-1-of-4 decoder',
        `<p class="y-chinh">🎯 The second module figure takes Figure 6.5 and multiplies it by four. Same 512 × 512 chips, but now arranged in <strong>four groups A, B, C, D of eight chips each — 32 chips, 1 MB total</strong> — and the MAR grows a third field.</p>
<p class="nhan">📐 Labels on the figure: <em>MAR</em> split <strong>9 / 9 / 2</strong>; chips named <strong>A1 … A8, B1 … B8, C1 … C8, D1 … D8</strong> laid out in a grid; each chip carries two <em>1/512</em> decoders and an <strong>E</strong> (enable) input; a box at the bottom left reads <em>Chip group enable</em> with outputs <em>Group A / B / C / D</em> and the caption <em>Select 1 of 4 groups</em>; on the right the <em>MBR</em> with positions 1 … 8; the note in the middle says <em>All chips 512 words by 512 bits. 2-terminal cells</em>; horizontal wires are labelled <em>Bit 1</em> … <em>Bit 8</em>.</p>
<ul>
<li><strong>The new field is the whole lesson.</strong> MAR is now <strong>9 + 9 + 2 = 20 bits</strong>. The two extra high-order bits do not go into any chip; they feed the "Select 1 of 4 groups" decoder, whose four outputs drive the <strong>E</strong> pin of every chip in one group. That is deepening, exactly as defined on slide 13, drawn in the textbook's own notation.</li>
<li><strong>Only one group answers at a time.</strong> All 32 chips receive the same 18 low-order address bits; all 32 sit on the same eight bit lines. What stops them colliding is E: three groups are disabled and hold their outputs in high impedance, so exactly eight chips drive Bit 1 … Bit 8. This is the chip-enable of slide 12 doing its job.</li>
<li><strong>Read the layout as rows = depth, columns = width.</strong> Within a group, the eight chips A1 … A8 are the <em>width</em> dimension (one bit each, feeding MBR positions 1 … 8). Across groups, A → B → C → D is the <em>depth</em> dimension. The picture is literally the rows-and-columns table you build in every exercise.</li>
<li><strong>"2-terminal cells" in the middle note.</strong> It signals that these are simple cells addressed by a coincidence of a row line and a column line, rather than cells with separate select and data terminals — a fabrication-level detail that explains why both decoders are "1 of 512" and why the output is a single bit.</li>
</ul>
<p class="dap-an">✅ <strong>Verified:</strong> 4 groups × 8 chips = <strong>32 chips</strong> ✓ · 32 × 512 × 512 = 8 388 608 bits = 1 048 576 bytes = <strong>1 MB</strong> ✓ · addresses = 2<sup>20</sup> = 1 048 576 = 1M ✓ · MAR width 9 + 9 + 2 = 20 = log<sub>2</sub>(1M) ✓ · the 2 group-select bits give 2<sup>2</sup> = 4 groups ✓. Note the module is exactly <strong>4×</strong> the 256 KByte of Figure 6.5, and it cost exactly 2 extra address bits.</p>
<p class="nhan">📐 <strong>Exam exercise 2.</strong> "Build a <strong>2M × 8</strong> memory from <strong>512K × 4</strong> chips."</p>
<ul>
<li>Columns = 8 ÷ 4 = <strong>2</strong>. Rows = 2M ÷ 512K = <strong>4</strong>. Chips = 4 × 2 = <strong>8</strong>.</li>
<li>Address lines = log<sub>2</sub>(2 097 152) = <strong>21</strong>. Into each chip: log<sub>2</sub>(524 288) = <strong>19</strong> (A0–A18). Left over for the decoder: 21 − 19 = <strong>2</strong> (A19, A20) → a 2-to-4 decoder → the CE pins of the four rows.</li>
</ul>
<p class="dap-an">✅ Checked and rebuilt: 4 × 512K = 2M ✓ · 2 × 4 = 8 ✓ · total bits 2M × 8 = 16 777 216 = 8 × (512K × 4) ✓ · 2<sup>2</sup> = 4 rows ✓. <strong>Answer: 8 chips in 4 rows × 2 columns; 21 address lines = 19 to every chip + 2 to a 2-to-4 decoder.</strong></p>
<p class="nhan">📐 <strong>Exam exercise 3 — the one that looks frightening.</strong> "Build a <strong>4M × 16</strong> memory from <strong>1M × 1</strong> chips."</p>
<ul>
<li>Columns = 16 ÷ 1 = <strong>16</strong> (a whole row of sixteen chips is needed just to make one 16-bit word). Rows = 4M ÷ 1M = <strong>4</strong>. Chips = 4 × 16 = <strong>64</strong>.</li>
<li>Address lines = log<sub>2</sub>(4 194 304) = <strong>22</strong>. Into each chip: log<sub>2</sub>(1 048 576) = <strong>20</strong>. Left over: 22 − 20 = <strong>2</strong> → 2-to-4 decoder → four row-enables, each driving <em>sixteen</em> CE pins at once.</li>
</ul>
<p class="dap-an">✅ Checked and rebuilt: 4 × 1M = 4M ✓ · 16 × 1 = 16 ✓ · total bits 4M × 16 = 67 108 864 = 64 × 1 048 576 ✓ · capacity = 8 MB ✓. <strong>Answer: 64 chips in 4 rows × 16 columns; 22 address lines = 20 to every chip + 2 to a 2-to-4 decoder.</strong> Note how a 1-bit-wide chip explodes the chip count — the same 8 MB from 1M × 8 chips would need only 4 × 2 = 8 chips.</p>
<p class="nhan">📐 <strong>Exercise 4, for pattern recognition.</strong> 16K × 4 chips → 64K × 16 memory: columns = 16 ÷ 4 = 4, rows = 64K ÷ 16K = 4, chips = <strong>16</strong>; address = log<sub>2</sub>(65 536) = 16 lines = <strong>14 to each chip + 2 to a 2-to-4 decoder</strong>. Verified: 4 × 16K = 64K ✓, 4 × 4 = 16 ✓, 64K × 16 = 1 048 576 bits = 16 × (16K × 4) ✓.</p>
<p class="pitfall">⚠️ The three mistakes that cost marks on this exact question type. (1) <strong>Using the total number of chips as the number of decoder outputs.</strong> The decoder selects <em>rows</em>, not chips — exercise 3 has 64 chips but still only a 2-to-4 decoder. (2) <strong>Subtracting the wrong log.</strong> Select lines = log<sub>2</sub>(rows) = log<sub>2</sub>(target depth ÷ chip depth), never anything to do with word width. (3) <strong>Mixing bits and bytes</strong> — "a 1 MB memory of 8-bit words" has 1M addresses (20 lines), but "a 1 MB memory of 16-bit words" has only 512K addresses (19 lines). Convert to <em>words</em> before taking any logarithm.</p>`,
        `<p class="y-chinh">🎯 Hình dựng module thứ hai lấy Figure 6.5 nhân lên bốn lần. Vẫn chip 512 × 512, nhưng nay xếp thành <strong>bốn NHÓM A, B, C, D mỗi nhóm tám chip — 32 chip, tổng 1 MB</strong> — và MAR mọc thêm một trường thứ ba.</p>
<p class="nhan">📐 Nhãn trên hình: <em>MAR</em> chia <strong>9 / 9 / 2</strong>; các chip tên <strong>A1 … A8, B1 … B8, C1 … C8, D1 … D8</strong> xếp thành lưới; mỗi chip mang hai bộ giải mã <em>1/512</em> và một ngõ vào <strong>E</strong> (enable — cho phép); một hộp dưới góc trái ghi <em>Chip group enable</em> với các ngõ ra <em>Group A / B / C / D</em> và chú thích <em>Select 1 of 4 groups</em>; bên phải là <em>MBR</em> với các vị trí 1 … 8; ghi chú ở giữa nói <em>All chips 512 words by 512 bits. 2-terminal cells</em>; các dây ngang ghi <em>Bit 1</em> … <em>Bit 8</em>.</p>
<ul>
<li><strong>Trường MỚI chính là toàn bộ bài học.</strong> MAR nay là <strong>9 + 9 + 2 = 20 bit</strong>. Hai bit CAO thêm vào KHÔNG đi vào chip nào cả; chúng nuôi bộ giải mã "Select 1 of 4 groups", bốn ngõ ra của nó lái chân <strong>E</strong> của mọi chip trong một nhóm. Đó chính là ĐÀO SÂU, đúng như định nghĩa ở slide 13, vẽ bằng chính ký hiệu của sách.</li>
<li><strong>Mỗi lúc chỉ một nhóm lên tiếng.</strong> Cả 32 chip nhận cùng 18 bit địa chỉ thấp; cả 32 con đều ngồi trên cùng tám đường bit. Thứ ngăn chúng đụng nhau là E: ba nhóm bị vô hiệu và giữ ngõ ra ở trạng thái trở kháng cao, nên đúng tám chip lái Bit 1 … Bit 8. Đây là chân chip-enable của slide 12 đang làm việc của nó.</li>
<li><strong>Đọc cách xếp: HÀNG = độ sâu, CỘT = độ rộng.</strong> Trong một nhóm, tám chip A1 … A8 là chiều <em>RỘNG</em> (mỗi con một bit, nuôi các vị trí MBR 1 … 8). Giữa các nhóm, A → B → C → D là chiều <em>SÂU</em>. Bức hình theo nghĩa đen chính là cái bảng hàng-cột bạn dựng trong mọi bài tập.</li>
<li><strong>Chữ "2-terminal cells" trong ghi chú giữa hình.</strong> Nó cho biết đây là loại ô đơn giản, được chọn bằng SỰ TRÙNG HỢP của một đường hàng và một đường cột, chứ không phải loại ô có đầu chọn và đầu dữ liệu riêng — một chi tiết ở mức chế tạo, giải thích vì sao cả hai bộ giải mã đều là "1 of 512" và vì sao ngõ ra chỉ có một bit.</li>
</ul>
<p class="dap-an">✅ <strong>Đã kiểm:</strong> 4 nhóm × 8 chip = <strong>32 chip</strong> ✓ · 32 × 512 × 512 = 8 388 608 bit = 1 048 576 byte = <strong>1 MB</strong> ✓ · số địa chỉ = 2<sup>20</sup> = 1 048 576 = 1M ✓ · độ rộng MAR 9 + 9 + 2 = 20 = log<sub>2</sub>(1M) ✓ · 2 bit chọn nhóm cho 2<sup>2</sup> = 4 nhóm ✓. Để ý module này lớn đúng <strong>4 lần</strong> so với 256 KByte của Figure 6.5, và cái giá là đúng 2 bit địa chỉ thêm.</p>
<p class="nhan">📐 <strong>Bài tập thi số 2.</strong> "Dựng bộ nhớ <strong>2M × 8</strong> từ chip <strong>512K × 4</strong>."</p>
<ul>
<li>Số cột = 8 ÷ 4 = <strong>2</strong>. Số hàng = 2M ÷ 512K = <strong>4</strong>. Số chip = 4 × 2 = <strong>8</strong>.</li>
<li>Đường địa chỉ = log<sub>2</sub>(2 097 152) = <strong>21</strong>. Vào mỗi chip: log<sub>2</sub>(524 288) = <strong>19</strong> (A0–A18). Còn dư cho bộ giải mã: 21 − 19 = <strong>2</strong> (A19, A20) → bộ giải mã 2 sang 4 → các chân CE của bốn hàng.</li>
</ul>
<p class="dap-an">✅ Đã kiểm và dựng ngược: 4 × 512K = 2M ✓ · 2 × 4 = 8 ✓ · tổng bit 2M × 8 = 16 777 216 = 8 × (512K × 4) ✓ · 2<sup>2</sup> = 4 hàng ✓. <strong>Đáp án: 8 chip xếp 4 hàng × 2 cột; 21 đường địa chỉ = 19 đường vào mọi chip + 2 đường vào bộ giải mã 2→4.</strong></p>
<p class="nhan">📐 <strong>Bài tập thi số 3 — bài nhìn đáng sợ.</strong> "Dựng bộ nhớ <strong>4M × 16</strong> từ chip <strong>1M × 1</strong>."</p>
<ul>
<li>Số cột = 16 ÷ 1 = <strong>16</strong> (phải cả một hàng mười sáu chip mới tạo nổi MỘT từ 16 bit). Số hàng = 4M ÷ 1M = <strong>4</strong>. Số chip = 4 × 16 = <strong>64</strong>.</li>
<li>Đường địa chỉ = log<sub>2</sub>(4 194 304) = <strong>22</strong>. Vào mỗi chip: log<sub>2</sub>(1 048 576) = <strong>20</strong>. Còn dư: 22 − 20 = <strong>2</strong> → bộ giải mã 2→4 → bốn tín hiệu cho phép hàng, mỗi tín hiệu lái <em>MƯỜI SÁU</em> chân CE cùng lúc.</li>
</ul>
<p class="dap-an">✅ Đã kiểm và dựng ngược: 4 × 1M = 4M ✓ · 16 × 1 = 16 ✓ · tổng bit 4M × 16 = 67 108 864 = 64 × 1 048 576 ✓ · dung lượng = 8 MB ✓. <strong>Đáp án: 64 chip xếp 4 hàng × 16 cột; 22 đường địa chỉ = 20 đường vào mọi chip + 2 đường vào bộ giải mã 2→4.</strong> Để ý chip rộng 1 bit làm số chip nổ tung ra sao — cũng 8 MB đó mà dùng chip 1M × 8 thì chỉ cần 4 × 2 = 8 con.</p>
<p class="nhan">📐 <strong>Bài tập 4, để nhận ra quy luật.</strong> Chip 16K × 4 → bộ nhớ 64K × 16: số cột = 16 ÷ 4 = 4, số hàng = 64K ÷ 16K = 4, số chip = <strong>16</strong>; địa chỉ = log<sub>2</sub>(65 536) = 16 đường = <strong>14 đường vào mỗi chip + 2 đường vào bộ giải mã 2→4</strong>. Đã kiểm: 4 × 16K = 64K ✓, 4 × 4 = 16 ✓, 64K × 16 = 1 048 576 bit = 16 × (16K × 4) ✓.</p>
<p class="pitfall">⚠️ Ba lỗi làm mất điểm ở đúng dạng bài này. (1) <strong>Lấy TỔNG SỐ CHIP làm số ngõ ra bộ giải mã.</strong> Bộ giải mã chọn <em>HÀNG</em>, không chọn chip — bài 3 có 64 chip mà vẫn chỉ cần bộ giải mã 2→4. (2) <strong>Lấy log sai đối tượng.</strong> Số đường chọn = log<sub>2</sub>(số hàng) = log<sub>2</sub>(độ sâu đích ÷ độ sâu chip), không bao giờ dính dáng gì tới độ rộng từ. (3) <strong>Lẫn bit với byte</strong> — "bộ nhớ 1 MB gồm các từ 8 bit" có 1M địa chỉ (20 đường), nhưng "bộ nhớ 1 MB gồm các từ 16 bit" chỉ có 512K địa chỉ (19 đường). Hãy đổi về <em>SỐ TỪ</em> trước khi lấy bất kỳ logarit nào.</p>`],

      [15, 'Interleaved Memory — K banks serving K requests at once',
        `<p class="y-chinh">🎯 Five concentric-ring bullets that build one idea: main memory is <strong>composed of a collection of DRAM chips</strong>, those chips are <strong>grouped together to form a memory bank</strong>, and because <strong>each bank is independently able to service a memory read or write request</strong>, <strong>K banks can service K requests simultaneously, increasing memory read or write rates by a factor of K</strong>.</p>
<ul>
<li><strong>Why banks help at all: a DRAM is busy long after it has answered.</strong> After a read, the array must precharge and rewrite the destructively-read row before it can accept another address. That dead time is the difference between <em>access time</em> and <em>memory cycle time</em>. One bank stalls; K banks let K−1 of them work through their dead time while the K-th answers.</li>
<li><strong>The fifth bullet is the one that makes it useful.</strong> "If <em>consecutive words</em> of memory are stored in <em>different banks</em>, the transfer of a block of memory is speeded up." Interleaving means address bit assignment: with 4 banks, addresses 0, 4, 8, … live in bank 0; 1, 5, 9, … in bank 1; and so on — the <em>low-order</em> address bits pick the bank. Now a sequential burst walks across all four banks in turn and never waits for the same one twice in a row.</li>
<li><strong>It is a direct payoff of spatial locality (Chapter 4).</strong> Cache line fills are sequential by construction: a 64-byte line is 8 or 16 consecutive words. Interleaving turns that predictable sequence into parallel bank activity. Without locality, requests would hit banks at random and roughly 1/K of them would collide anyway.</li>
<li><strong>Where you have already used it without knowing.</strong> "Dual channel" and "quad channel" on a motherboard, and the bank-address lines <code>BA0, BA1</code> on the SDRAM pin list of slide 26 — that is 4 internal banks per chip. Modern DDR4/DDR5 has bank <em>groups</em> on top of banks, for exactly the reason in the first bullet.</li>
</ul>
<p class="dap-an">✅ <strong>Worked example, verified.</strong> A memory with cycle time 60 ns per bank and a bus that can carry one word every 10 ns is asked for 4 consecutive words. <em>Without interleaving</em> (one bank): 4 × 60 = <strong>240 ns</strong>. <em>With 4-way interleaving</em>: bank 0 answers after 60 ns, and banks 1, 2, 3 were started in parallel so their words follow at bus speed — 60 + 3 × 10 = <strong>90 ns</strong>. Speed-up = 240 ÷ 90 = <strong>2,67×</strong>. Note it is <em>not</em> 4× — the first access still costs full latency. Interleaving raises <strong>bandwidth</strong>, it does not reduce <strong>latency</strong>.</p>
<p class="nhan">📐 Measured here for comparison: sequential read of 512 MB on this machine ran at <strong>8,3 GB/s</strong> single-threaded, while a single dependent random access cost <strong>133,93 ns</strong>. At 8 bytes per access, 133,93 ns of pure latency would give only 0,06 GB/s. The ~140× gap between the two numbers is what banking, interleaving, bursting and prefetching buy you together.</p>
<p class="pitfall">⚠️ Trap: the bank number must come from the <strong>low-order</strong> address bits. If you used the high-order bits instead (block-mapped rather than interleaved), consecutive addresses would all land in the <em>same</em> bank and the speed-up would vanish. This is the same low-order/high-order decision as cache index bits in Chapter 5, and for the same reason.</p>`,
        `<p class="y-chinh">🎯 Năm gạch đầu dòng toả ra từ vòng tròn đồng tâm, dựng lên một ý: bộ nhớ chính <strong>gồm một tập các chip DRAM</strong>, các chip đó <strong>gộp lại thành một NGĂN (bank)</strong>, và vì <strong>mỗi ngăn phục vụ được một yêu cầu đọc/ghi một cách ĐỘC LẬP</strong>, nên <strong>K ngăn phục vụ được K yêu cầu CÙNG LÚC, nâng tốc độ đọc/ghi lên K lần</strong>.</p>
<ul>
<li><strong>Vì sao chia ngăn lại ăn thua: một con DRAM còn BẬN rất lâu sau khi đã trả lời xong.</strong> Sau một lần đọc, mảng ô nhớ phải nạp lại đường bit và ghi trả cái hàng vừa bị đọc phá huỷ, rồi mới nhận được địa chỉ mới. Khoảng chết đó chính là chênh lệch giữa <em>thời gian truy cập</em> và <em>thời gian chu kỳ bộ nhớ</em>. Một ngăn thì phải đứng chờ; K ngăn thì K−1 con đi qua khoảng chết của mình trong lúc con thứ K đang trả lời.</li>
<li><strong>Gạch đầu dòng thứ NĂM mới là cái làm nó hữu dụng.</strong> "Nếu các <em>TỪ LIÊN TIẾP</em> của bộ nhớ được cất ở các <em>NGĂN KHÁC NHAU</em> thì việc truyền một khối bộ nhớ được tăng tốc." Xen kẽ chính là cách gán bit địa chỉ: với 4 ngăn, các địa chỉ 0, 4, 8, … nằm ở ngăn 0; 1, 5, 9, … ở ngăn 1; v.v. — các bit địa chỉ <em>THẤP</em> chọn ngăn. Thế là một đợt truy cập tuần tự đi vòng qua cả bốn ngăn và không bao giờ phải chờ cùng một ngăn hai lần liên tiếp.</li>
<li><strong>Đây là tiền lời trực tiếp của tính cục bộ KHÔNG GIAN (Chương 4).</strong> Việc nạp một dòng cache vốn dĩ là tuần tự: một dòng 64 byte là 8 hoặc 16 từ liên tiếp. Xen kẽ biến dãy đoán trước được đó thành hoạt động song song của các ngăn. Không có tính cục bộ thì các yêu cầu rơi vào ngăn một cách ngẫu nhiên, và khoảng 1/K trong số đó vẫn đụng nhau.</li>
<li><strong>Chỗ bạn đã dùng nó mà không biết.</strong> "Dual channel" và "quad channel" trên bo mạch chủ, và hai đường địa chỉ ngăn <code>BA0, BA1</code> trong bảng chân SDRAM ở slide 26 — tức 4 ngăn nội bộ mỗi chip. DDR4/DDR5 ngày nay còn có thêm <em>NHÓM ngăn</em> chồng lên trên ngăn, đúng vì lý do ở gạch đầu dòng đầu tiên.</li>
</ul>
<p class="dap-an">✅ <strong>Ví dụ giải trọn, đã kiểm.</strong> Một bộ nhớ có thời gian chu kỳ 60 ns mỗi ngăn, bus chở được một từ mỗi 10 ns, bị hỏi 4 từ liên tiếp. <em>KHÔNG xen kẽ</em> (một ngăn): 4 × 60 = <strong>240 ns</strong>. <em>Xen kẽ 4 ngăn</em>: ngăn 0 trả lời sau 60 ns, còn ngăn 1, 2, 3 đã được khởi động song song nên các từ của chúng nối đuôi theo tốc độ bus — 60 + 3 × 10 = <strong>90 ns</strong>. Tăng tốc = 240 ÷ 90 = <strong>2,67 lần</strong>. Để ý KHÔNG phải 4 lần — lần truy cập đầu vẫn trả đủ độ trễ. Xen kẽ nâng <strong>BĂNG THÔNG</strong>, nó KHÔNG giảm <strong>ĐỘ TRỄ</strong>.</p>
<p class="nhan">📐 Đo tại chỗ để đối chiếu: đọc tuần tự 512 MB trên máy này đạt <strong>8,3 GB/s</strong> một luồng, trong khi một lần truy cập ngẫu nhiên phụ thuộc nhau tốn <strong>133,93 ns</strong>. Với 8 byte mỗi lần, 133,93 ns độ trễ thuần chỉ cho ra 0,06 GB/s. Khoảng cách ~140 lần giữa hai con số đó chính là thứ mà chia ngăn, xen kẽ, truyền cụm và nạp trước cùng nhau mua về.</p>
<p class="pitfall">⚠️ Bẫy: số hiệu ngăn phải lấy từ các bit địa chỉ <strong>THẤP</strong>. Nếu lấy bit CAO (ánh xạ theo khối chứ không xen kẽ) thì các địa chỉ liên tiếp rơi hết vào <em>CÙNG MỘT</em> ngăn và mọi lợi ích bay sạch. Đây đúng là quyết định bit-thấp/bit-cao giống hệt chuyện bit chỉ số của cache ở Chương 5, và vì cùng một lý do.</p>`],

      [16, 'Error Correction — Hard Failure versus Soft Error',
        `<p class="y-chinh">🎯 Before you can correct an error you must know what kind of error exists. The slide names exactly two, and the distinction is <strong>permanent physical damage versus a transient flip with no damage at all</strong>.</p>
<table>
<tr><th></th><th>Hard Failure</th><th>Soft Error</th></tr>
<tr><td><strong>Slide's definition</strong></td><td>A <strong>permanent physical defect</strong>. The memory cell or cells affected cannot reliably store data but become <em>stuck at 0 or 1</em>, or <em>switch erratically</em> between 0 and 1</td><td>A <strong>random, non-destructive event</strong> that alters the contents of one or more memory cells</td></tr>
<tr><td><strong>Damage to the chip?</strong></td><td>Yes — the cell is broken</td><td><strong>No permanent damage to memory</strong></td></tr>
<tr><td><strong>Slide's causes</strong></td><td>Harsh environmental abuse · manufacturing defects · wear</td><td>Power supply problems · alpha particles</td></tr>
<tr><td><strong>Repeatable?</strong></td><td>Yes — the same address fails every time</td><td>No — rewrite the cell and it is correct again</td></tr>
<tr><td><strong>Right response</strong></td><td>Map the address out of use, or replace the module</td><td>Correct it with ECC and carry on</td></tr>
</table>
<ul>
<li><strong>"Stuck at 0 or 1" is the standard fault model of digital testing.</strong> It matters because it is <em>testable</em>: write the opposite value, read it back, and a stuck cell betrays itself immediately. The nastier variant the slide also lists — <em>switching erratically</em> — passes a single test and fails later, which is why memory diagnostics run many passes with changing patterns.</li>
<li><strong>Alpha particles deserve the name-check.</strong> A DRAM 1 holds only a few tens of thousands of electrons. A single alpha particle from trace radioactive decay in the packaging material, or a cosmic-ray neutron, can deposit enough charge to flip it. Nothing is broken; the next write to that cell is perfectly fine. This is why soft errors get <em>more</em> likely as cells shrink — less stored charge per bit means a smaller disturbance is enough.</li>
<li><strong>Soft errors are far more common than hard failures, and that is the whole justification for ECC.</strong> If errors were mostly permanent, you would swap the module. Because they are mostly transient, a circuit that repairs the word on the fly keeps a machine running for years — which is exactly what Figure 6.7 on the next slide draws.</li>
<li><strong>Connect to real life.</strong> Server and workstation DIMMs are sold as "ECC" precisely because at 32 GB and up, and running for months, soft errors become a statistical certainty rather than a curiosity. Consumer laptops (including the 32 GB LPDDR5 machine measured in this lesson) usually have no ECC: a flipped bit there shows up as a mysterious crash nobody can reproduce — which is the perfect signature of a soft error.</li>
</ul>
<p class="meo">💡 Mnemonic: <strong>HARD = hỏng thật, sửa bằng THAY. SOFT = sai tạm, sửa bằng GHI LẠI.</strong> If the question says "stuck at", "defect", "wear" → hard. If it says "alpha particle", "random", "no permanent damage" → soft.</p>
<p class="pitfall">⚠️ Trap: "soft error" does not mean "software error". It is a hardware event with a non-hardware cause. Likewise "hard failure" has nothing to do with hard disks. Both names are historical and both are deliberately used as distractors in multiple-choice questions.</p>`,
        `<p class="y-chinh">🎯 Trước khi sửa được lỗi thì phải biết lỗi thuộc loại nào. Slide nêu đúng hai loại, và ranh giới là <strong>HƯ HỎNG VẬT LÝ VĨNH VIỄN so với một cú lật bit THOÁNG QUA không hư hại gì cả</strong>.</p>
<table>
<tr><th></th><th>Hard Failure — hỏng CỨNG</th><th>Soft Error — lỗi MỀM</th></tr>
<tr><td><strong>Định nghĩa của slide</strong></td><td>Một <strong>khuyết tật vật lý VĨNH VIỄN</strong>. Ô nhớ bị ảnh hưởng không lưu dữ liệu tin cậy được nữa mà <em>kẹt cứng ở 0 hoặc 1</em>, hoặc <em>chập chờn nhảy</em> giữa 0 và 1</td><td>Một <strong>sự kiện NGẪU NHIÊN, KHÔNG PHÁ HUỶ</strong> làm thay đổi nội dung một hoặc nhiều ô nhớ</td></tr>
<tr><td><strong>Chip có hỏng không?</strong></td><td>Có — ô nhớ đã hỏng</td><td><strong>KHÔNG có hư hại vĩnh viễn nào</strong></td></tr>
<tr><td><strong>Nguyên nhân slide nêu</strong></td><td>Môi trường khắc nghiệt · lỗi sản xuất · hao mòn</td><td>Trục trặc nguồn cấp · hạt alpha</td></tr>
<tr><td><strong>Lặp lại được?</strong></td><td>Có — cùng một địa chỉ lỗi mọi lần</td><td>Không — ghi lại ô đó là nó đúng ngay</td></tr>
<tr><td><strong>Cách xử lý đúng</strong></td><td>Loại địa chỉ đó ra khỏi vùng dùng, hoặc thay thanh nhớ</td><td>Sửa bằng ECC rồi chạy tiếp</td></tr>
</table>
<ul>
<li><strong>"Kẹt ở 0 hoặc 1" là mô hình lỗi chuẩn của ngành kiểm thử mạch số.</strong> Nó quan trọng vì nó <em>KIỂM ĐƯỢC</em>: ghi giá trị ngược lại, đọc ra, ô kẹt lộ mặt ngay. Biến thể khó chịu hơn mà slide cũng nêu — <em>chập chờn</em> — thì qua được một lượt kiểm rồi mới hỏng sau, và đó là lý do các chương trình chẩn đoán bộ nhớ phải chạy nhiều lượt với nhiều mẫu bit khác nhau.</li>
<li><strong>Hạt alpha đáng được gọi tên.</strong> Một bit 1 của DRAM chỉ giữ vài chục nghìn electron. Một hạt alpha đơn lẻ từ phân rã phóng xạ vết trong vật liệu đóng gói, hay một neutron từ tia vũ trụ, có thể để lại đủ điện tích để lật nó. Không có gì hỏng cả; lần ghi kế tiếp vào ô đó hoàn toàn bình thường. Vì thế lỗi mềm <em>CÀNG DỄ XẢY RA</em> khi ô nhớ càng nhỏ đi — ít điện tích lưu mỗi bit nghĩa là một nhiễu động nhỏ hơn cũng đủ.</li>
<li><strong>Lỗi mềm phổ biến hơn hỏng cứng rất nhiều, và đó là toàn bộ lý lẽ cho ECC.</strong> Nếu lỗi chủ yếu là vĩnh viễn thì cứ thay thanh nhớ là xong. Chính vì chúng chủ yếu là thoáng qua nên một mạch sửa từ dữ liệu ngay lúc đọc mới giữ được máy chạy hàng năm trời — đúng là thứ Figure 6.7 ở slide sau vẽ ra.</li>
<li><strong>Nối vào đời thật.</strong> Thanh RAM cho máy chủ và máy trạm được bán dưới nhãn "ECC" đúng vì ở mức 32 GB trở lên và chạy liên tục hàng tháng, lỗi mềm trở thành điều chắc chắn về mặt thống kê chứ không còn là chuyện lạ. Laptop phổ thông (kể cả cái máy 32 GB LPDDR5 đo trong bài này) thường KHÔNG có ECC: một bit bị lật ở đó hiện ra thành một cú treo máy bí ẩn không ai tái hiện nổi — đúng chữ ký của lỗi mềm.</li>
</ul>
<p class="meo">💡 Mẹo nhớ: <strong>HARD = hỏng thật, sửa bằng THAY. SOFT = sai tạm, sửa bằng GHI LẠI.</strong> Đề nhắc "kẹt ở", "khuyết tật", "hao mòn" → hỏng cứng. Đề nhắc "hạt alpha", "ngẫu nhiên", "không hư hại vĩnh viễn" → lỗi mềm.</p>
<p class="pitfall">⚠️ Bẫy: "soft error" KHÔNG phải "lỗi phần mềm". Nó là sự kiện phần cứng với nguyên nhân không nằm ở phần cứng hỏng. Tương tự, "hard failure" chẳng liên quan gì tới ổ cứng. Cả hai cái tên đều mang tính lịch sử, và cả hai đều bị dùng làm phương án gây nhiễu trong câu hỏi trắc nghiệm.</p>`],

      [17, 'Figure 6.7 — Error-Correcting Code Function: the write path and the read path',
        `<p class="y-chinh">🎯 The block diagram of every ECC memory ever built. Two journeys share one picture: <strong>writing</strong> adds redundancy, <strong>reading</strong> recomputes it and compares. The comparison result is what repairs the word.</p>
<p class="nhan">📐 Labels on the figure: <em>Data In</em> enters at bottom left on a bus marked <strong>M</strong>; it branches into a box <strong>f</strong> which outputs <strong>K</strong> bits; both M and K enter the <em>Memory</em> block. Coming out of Memory, <strong>M</strong> goes to a second box <strong>f</strong> producing <strong>K</strong>, and the stored <strong>K</strong> goes straight across; both K buses meet at <em>Compare</em>. Compare has two outputs: one upward to <em>Error Signal</em>, one into <em>Corrector</em>, which also receives the stored <strong>M</strong> and emits <em>Data Out</em> on <strong>M</strong> lines.</p>
<ul>
<li><strong>Write path, left to right along the bottom.</strong> An M-bit data word arrives. The function <em>f</em> computes a K-bit <strong>code</strong> from it. The memory then stores <strong>M + K bits</strong>, not M. Those K extra bits are the entire cost of error correction, and slide 19 prices them exactly.</li>
<li><strong>Read path, right to left along the top.</strong> The stored M bits are read out and the <em>same</em> function <em>f</em> is applied again, giving a fresh K-bit code. If nothing went wrong, the fresh code equals the stored code. <em>Compare</em> checks that — and the output of Compare is not a yes/no bit, it is a K-bit <strong>syndrome</strong>.</li>
<li><strong>The syndrome is the whole magic.</strong> If it is all zeros, no error. If it is non-zero, its <em>value</em> is the bit position that went wrong. That is why <em>Corrector</em> can fix the word rather than merely complain: it is told exactly which wire to invert. Slide 20 shows how positions are numbered to make that work.</li>
<li><strong>Two outputs, two different customers.</strong> <em>Data Out</em> goes to the processor, already repaired, which is why ECC is invisible to software. <em>Error Signal</em> goes to the system — it lets the OS log a corrected error and, if one address keeps reporting, conclude it is a <em>hard failure</em> (slide 16) and retire the page.</li>
<li><strong>Note that f appears twice, deliberately identically.</strong> Both boxes carry the same letter because it is the same circuit design duplicated, not two different functions. Any question that draws them differently, or that puts the second f before the memory, is testing whether you understood the direction of the data flow.</li>
</ul>
<p class="dap-an">✅ Overhead, computed from the diagram: the memory stores M + K bits per word instead of M, so the word length grows by K/M. With the standard M = 64, K = 8 of a server DIMM that is 72 bits stored per 64 data bits = <strong>12,5% more memory</strong>, which matches the "Data Bits 64 → Check Bits 8, 12.5%" row of Table 6.2 on slide 19 (the SEC-DED column). That is the real price of an ECC DIMM, and it explains the price difference you see in shops.</p>
<p class="pitfall">⚠️ Trap: a code that only <em>detects</em> (like a single parity bit) stops at the Error Signal output — there is no Corrector, because one parity bit cannot say <em>where</em> the error is. The figure is specifically an error-<strong>correcting</strong> code; the presence of the Corrector box is the visual difference between detection and correction.</p>`,
        `<p class="y-chinh">🎯 Sơ đồ khối của mọi bộ nhớ ECC từng được dựng. Hai hành trình chung một bức hình: <strong>GHI</strong> thì thêm phần dư, <strong>ĐỌC</strong> thì tính lại phần dư đó rồi so sánh. Kết quả so sánh chính là thứ sửa được từ dữ liệu.</p>
<p class="nhan">📐 Nhãn trên hình: <em>Data In</em> đi vào ở dưới bên trái trên bus ghi <strong>M</strong>; nó rẽ vào một hộp <strong>f</strong> cho ra <strong>K</strong> bit; cả M và K cùng đi vào khối <em>Memory</em>. Ra khỏi Memory, <strong>M</strong> đi vào hộp <strong>f</strong> thứ hai sinh ra <strong>K</strong>, còn <strong>K</strong> đã lưu thì đi thẳng ngang; hai bus K gặp nhau ở <em>Compare</em>. Compare có hai ngõ ra: một đi lên <em>Error Signal</em>, một đi vào <em>Corrector</em> — khối này còn nhận <strong>M</strong> đã lưu và phát ra <em>Data Out</em> trên <strong>M</strong> đường.</p>
<ul>
<li><strong>Đường GHI, từ trái sang phải dọc phía dưới.</strong> Một từ dữ liệu M bit đi tới. Hàm <em>f</em> tính từ nó ra một <strong>mã</strong> K bit. Bộ nhớ sau đó lưu <strong>M + K bit</strong>, chứ không phải M. Đúng K bit dư đó là toàn bộ cái giá của việc sửa lỗi, và slide 19 báo giá chính xác.</li>
<li><strong>Đường ĐỌC, từ phải sang trái dọc phía trên.</strong> M bit đã lưu được đọc ra và <em>CHÍNH</em> hàm f được áp lại, cho ra một mã K bit mới tinh. Nếu không có gì sai thì mã mới bằng mã đã lưu. <em>Compare</em> kiểm đúng chuyện đó — và ngõ ra của Compare KHÔNG phải một bit đúng/sai, nó là một <strong>SYNDROME</strong> (hội chứng) K bit.</li>
<li><strong>Syndrome mới là phép màu.</strong> Nếu nó toàn 0 thì không có lỗi. Nếu nó khác 0 thì <em>GIÁ TRỊ</em> của nó chính là VỊ TRÍ BIT bị sai. Vì thế <em>Corrector</em> mới sửa được từ dữ liệu chứ không chỉ kêu ca: nó được chỉ đích danh phải đảo sợi dây nào. Slide 20 cho thấy cách đánh số vị trí để chuyện đó chạy được.</li>
<li><strong>Hai ngõ ra, hai khách hàng khác nhau.</strong> <em>Data Out</em> đi tới bộ xử lý, ĐÃ được sửa sẵn, vì thế ECC vô hình với phần mềm. <em>Error Signal</em> đi tới hệ thống — nó cho phép hệ điều hành ghi nhật ký một lỗi đã sửa, và nếu cùng một địa chỉ cứ báo mãi thì kết luận đó là <em>hỏng cứng</em> (slide 16) và cho nghỉ hưu trang nhớ đó.</li>
<li><strong>Để ý f xuất hiện HAI lần, cố ý giống hệt nhau.</strong> Hai hộp mang cùng một chữ vì đó là cùng một thiết kế mạch nhân đôi, không phải hai hàm khác nhau. Câu hỏi nào vẽ chúng khác nhau, hoặc đặt f thứ hai TRƯỚC bộ nhớ, là đang kiểm tra xem bạn có hiểu chiều của luồng dữ liệu hay không.</li>
</ul>
<p class="dap-an">✅ Phí tổn, tính từ chính sơ đồ: bộ nhớ lưu M + K bit mỗi từ thay vì M, nên độ dài từ tăng thêm K/M. Với bộ số chuẩn M = 64, K = 8 của một thanh DIMM máy chủ thì là 72 bit lưu cho 64 bit dữ liệu = <strong>nhiều hơn 12,5% bộ nhớ</strong>, khớp đúng dòng "Data Bits 64 → Check Bits 8, 12.5%" của Table 6.2 ở slide 19 (cột SEC-DED). Đó là giá thật của một thanh RAM ECC, và nó giải thích chênh lệch giá bạn thấy ngoài tiệm.</p>
<p class="pitfall">⚠️ Bẫy: một mã chỉ <em>PHÁT HIỆN</em> (như một bit chẵn lẻ đơn) thì dừng lại ở ngõ ra Error Signal — không có khối Corrector, vì một bit chẵn lẻ không nói được lỗi nằm ở ĐÂU. Hình này đặc trưng cho mã <strong>SỬA</strong> lỗi; sự có mặt của khối Corrector chính là khác biệt nhìn thấy được giữa phát hiện và sửa.</p>`],

      [18, 'Figure 6.8 — Hamming Error-Correcting Code drawn as three overlapping circles',
        `<p class="y-chinh">🎯 The Hamming code explained without a single equation: <strong>three overlapping circles A, B and C</strong>, four data bits living in the overlaps, three check bits living in the outer crescents, and each circle forced to contain an <em>even</em> number of 1s. When one bit flips, the set of circles that go odd names the culprit uniquely.</p>
<ul>
<li><strong>Panel (a) — the data.</strong> Four data bits are placed in the four inner regions: A∩B = <strong>1</strong>, A∩B∩C = <strong>1</strong>, A∩C = <strong>1</strong>, B∩C = <strong>0</strong>. Nothing has been computed yet; the crescents are empty.</li>
<li><strong>Panel (b) — the check bits are filled in.</strong> Each of the three outer crescents gets whatever value makes its own circle contain an even number of 1s. Result on the figure: A-only = <strong>1</strong>, B-only = <strong>0</strong>, C-only = <strong>0</strong>. Verify circle by circle — circle A holds 1 + 1 + 1 + 1 = four 1s (even ✓); circle B holds 0 + 1 + 1 + 0 = two 1s (even ✓); circle C holds 0 + 1 + 1 + 0 = two 1s (even ✓). This is the "f" box of Figure 6.7, drawn as geometry.</li>
<li><strong>Panel (c) — an error strikes.</strong> The bit in region A∩C flips from 1 to <strong>0</strong>. Nothing else changes. The word in memory is now wrong and, on its own, looks perfectly plausible.</li>
<li><strong>Panel (d) — the syndrome points at it.</strong> Recheck each circle: circle A now holds 1 + 1 + 1 + 0 = three 1s → <strong>ODD, fails</strong>. Circle B holds 0 + 1 + 1 + 0 = two → even, passes. Circle C holds 0 + 0 + 1 + 0 = one → <strong>ODD, fails</strong>. The faulty bit must be inside A <em>and</em> inside C but <em>outside</em> B — and exactly one region on the whole diagram satisfies all three conditions. The figure shades it grey.</li>
</ul>
<p class="dap-an">✅ Worked completely, and this is the answer an exam wants in words: <strong>A fails, B passes, C fails ⇒ the error is in region A∩C (not B) ⇒ invert that bit, from 0 back to 1, and the word is repaired.</strong> Note that the code did more than detect — it <em>located</em>, which is precisely why the Corrector box of Figure 6.7 can exist.</p>
<ul>
<li><strong>Why three circles give exactly seven usable regions.</strong> Three yes/no checks produce 2<sup>3</sup> = 8 possible syndromes. One of them (all pass) means "no error", leaving <strong>7</strong> to name positions — and three circles carve the plane into exactly 7 regions (3 crescents + 3 pairwise overlaps + 1 triple). That is not a coincidence, it is the Hamming condition 2<sup>K</sup> ≥ M + K + 1 with K = 3 and M = 4: 8 ≥ 4 + 3 + 1 = 8, satisfied with nothing to spare.</li>
<li><strong>The diagram generalises exactly.</strong> Circles become check bits, regions become bit positions, "even number of 1s" becomes even parity. Slides 19 and 20 take the same idea to 8, 16, 32, 64… data bits, where drawing circles stops being practical but the arithmetic is identical.</li>
</ul>
<p class="meo">💡 Remember the whole figure as a sentence: <strong>"mỗi vòng tròn phải CHẴN; vòng nào LẺ thì lỗi nằm trong nó; giao của các vòng lẻ, trừ đi các vòng chẵn, chỉ còn đúng MỘT ô."</strong></p>
<p class="pitfall">⚠️ Trap: this construction corrects <strong>exactly one</strong> bit error. If two bits flip, two or more circles go odd in a pattern that points confidently at a <em>different, innocent</em> region — the code will "correct" the wrong bit and make things worse. Detecting double errors needs one extra check bit (SEC-DED), which is the second half of Table 6.2 on the next slide.</p>`,
        `<p class="y-chinh">🎯 Mã Hamming giải thích mà không cần một phương trình nào: <strong>ba vòng tròn chồng lên nhau A, B và C</strong>, bốn bit dữ liệu nằm ở các phần giao, ba bit kiểm nằm ở ba vành ngoài, và mỗi vòng tròn bị ép phải chứa số bit 1 là <em>CHẴN</em>. Khi một bit bị lật, tập các vòng hoá LẺ sẽ chỉ đích danh thủ phạm.</p>
<ul>
<li><strong>Khung (a) — dữ liệu.</strong> Bốn bit dữ liệu đặt vào bốn vùng bên trong: A∩B = <strong>1</strong>, A∩B∩C = <strong>1</strong>, A∩C = <strong>1</strong>, B∩C = <strong>0</strong>. Chưa tính gì cả; ba vành ngoài còn trống.</li>
<li><strong>Khung (b) — điền bit kiểm.</strong> Mỗi vành ngoài nhận giá trị nào làm cho vòng tròn của chính nó có số bit 1 là chẵn. Kết quả trên hình: chỉ-A = <strong>1</strong>, chỉ-B = <strong>0</strong>, chỉ-C = <strong>0</strong>. Kiểm từng vòng — vòng A chứa 1 + 1 + 1 + 1 = bốn số 1 (chẵn ✓); vòng B chứa 0 + 1 + 1 + 0 = hai số 1 (chẵn ✓); vòng C chứa 0 + 1 + 1 + 0 = hai số 1 (chẵn ✓). Đây chính là cái hộp "f" của Figure 6.7, vẽ dưới dạng hình học.</li>
<li><strong>Khung (c) — lỗi ập tới.</strong> Bit ở vùng A∩C lật từ 1 xuống <strong>0</strong>. Không có gì khác thay đổi. Từ dữ liệu trong bộ nhớ giờ đã sai mà nhìn riêng nó thì vẫn hoàn toàn hợp lý.</li>
<li><strong>Khung (d) — syndrome chỉ thẳng vào nó.</strong> Kiểm lại từng vòng: vòng A nay chứa 1 + 1 + 1 + 0 = ba số 1 → <strong>LẺ, TRƯỢT</strong>. Vòng B chứa 0 + 1 + 1 + 0 = hai → chẵn, ĐẠT. Vòng C chứa 0 + 0 + 1 + 0 = một → <strong>LẺ, TRƯỢT</strong>. Vậy bit hỏng phải nằm TRONG A <em>VÀ</em> TRONG C nhưng <em>NGOÀI</em> B — và trên cả biểu đồ chỉ có ĐÚNG MỘT vùng thoả cả ba điều kiện đó. Hình tô xám nó lại.</li>
</ul>
<p class="dap-an">✅ Giải trọn, và đây đúng là câu trả lời mà đề thi muốn nghe bằng lời: <strong>A trượt, B đạt, C trượt ⇒ lỗi nằm ở vùng A∩C (không thuộc B) ⇒ đảo bit đó, từ 0 về lại 1, là từ dữ liệu được sửa xong.</strong> Để ý mã đã làm nhiều hơn phát hiện — nó <em>ĐỊNH VỊ</em>, và đó chính xác là lý do khối Corrector của Figure 6.7 tồn tại được.</p>
<ul>
<li><strong>Vì sao ba vòng tròn cho đúng bảy vùng dùng được.</strong> Ba phép kiểm đạt/trượt sinh ra 2<sup>3</sup> = 8 syndrome khả dĩ. Một trong số đó (đạt hết) nghĩa là "không lỗi", còn lại <strong>7</strong> để đặt tên cho các vị trí — mà ba vòng tròn chia mặt phẳng ra đúng 7 vùng (3 vành + 3 giao đôi + 1 giao ba). Đó không phải trùng hợp, đó là điều kiện Hamming 2<sup>K</sup> ≥ M + K + 1 với K = 3 và M = 4: 8 ≥ 4 + 3 + 1 = 8, vừa khít không dư một ly.</li>
<li><strong>Biểu đồ này tổng quát hoá y nguyên.</strong> Vòng tròn thành bit kiểm, vùng thành vị trí bit, "số 1 chẵn" thành parity chẵn. Slide 19 và 20 đem đúng ý tưởng này lên 8, 16, 32, 64… bit dữ liệu, chỗ mà vẽ vòng tròn không còn khả thi nhưng phép tính thì y hệt.</li>
</ul>
<p class="meo">💡 Nhớ cả bức hình bằng một câu: <strong>"mỗi vòng tròn phải CHẴN; vòng nào LẺ thì lỗi nằm trong nó; giao của các vòng LẺ, trừ đi các vòng CHẴN, chỉ còn đúng MỘT ô."</strong></p>
<p class="pitfall">⚠️ Bẫy: cách dựng này sửa được <strong>ĐÚNG MỘT</strong> bit lỗi. Nếu HAI bit cùng lật, hai vòng trở lên sẽ hoá lẻ theo một kiểu chỉ rất tự tin vào một vùng <em>KHÁC, VÔ TỘI</em> — mã sẽ "sửa" nhầm bit và làm mọi thứ tệ hơn. Muốn phát hiện lỗi đôi thì cần thêm một bit kiểm nữa (SEC-DED), và đó là nửa sau của Table 6.2 ở slide kế tiếp.</p>`],

      [19, 'Table 6.2 — Increase in Word Length with Error Correction',
        `<p class="y-chinh">🎯 The price list. For each data word length, how many check bits does single-error correction need, how many does single-error-correction/double-error-detection need, and what percentage does the memory grow by?</p>
<table>
<tr><th rowspan="2">Data Bits (M)</th><th colspan="2">Single-Error Correction (SEC)</th><th colspan="2">SEC / Double-Error Detection (SEC-DED)</th></tr>
<tr><th>Check Bits</th><th>% Increase</th><th>Check Bits</th><th>% Increase</th></tr>
<tr><td>8</td><td>4</td><td>50.0</td><td>5</td><td>62.5</td></tr>
<tr><td>16</td><td>5</td><td>31.25</td><td>6</td><td>37.5</td></tr>
<tr><td>32</td><td>6</td><td>18.75</td><td>7</td><td>21.875</td></tr>
<tr><td><strong>64</strong></td><td><strong>7</strong></td><td><strong>10.94</strong></td><td><strong>8</strong></td><td><strong>12.5</strong></td></tr>
<tr><td>128</td><td>8</td><td>6.25</td><td>9</td><td>7.03</td></tr>
<tr><td>256</td><td>9</td><td>3.52</td><td>10</td><td>3.91</td></tr>
</table>
<ul>
<li><strong>The whole table comes from one inequality, so do not memorise it — derive it.</strong> The syndrome must be able to name every possible single-bit error position (M data bits + K check bits) <em>plus</em> the "no error" case. A K-bit syndrome has 2<sup>K</sup> values, so the requirement is <strong>2<sup>K</sup> ≥ M + K + 1</strong>. Take the smallest K that satisfies it.</li>
<li><strong>SEC-DED is always exactly SEC + 1.</strong> One extra check bit — an overall parity bit across the whole encoded word — lets the circuit tell "one error, here it is" apart from "two errors, I can detect but not repair". That distinction matters enormously in practice: correcting the wrong bit silently is worse than reporting failure loudly.</li>
<li><strong>Check bits grow logarithmically while data bits grow exponentially — and that is why ECC is affordable.</strong> Doubling M adds only <em>one</em> check bit. So overhead collapses: 50% at M = 8, but only 3,5% at M = 256. Error correction is cheap precisely because you buy it on long words.</li>
<li><strong>The row that describes real hardware is M = 64.</strong> 64 data bits + 8 check bits = <strong>72 bits</strong>, which is exactly why server DIMMs are described as "72-bit" and carry nine chips per rank instead of eight. The 12,5% figure is the actual price premium built into ECC memory.</li>
</ul>
<p class="dap-an">✅ <strong>Rebuilt from scratch by machine and matched cell by cell.</strong> For M = 8: K = 4 because 2<sup>4</sup> = 16 ≥ 8 + 4 + 1 = 13 while 2<sup>3</sup> = 8 &lt; 8 + 3 + 1 = 12 ✓. M = 16 → K = 5 (32 ≥ 22) ✓ · M = 32 → 6 (64 ≥ 39) ✓ · M = 64 → 7 (128 ≥ 72) ✓ · M = 128 → 8 (256 ≥ 137) ✓ · M = 256 → 9 (512 ≥ 266) ✓. Percentages: 4/8 = 50%, 5/16 = 31,25%, 6/32 = 18,75%, 7/64 = 10,938%, 8/128 = 6,25%, 9/256 = 3,516% — <strong>every figure on the slide reproduced exactly</strong>, including the rounded 10.94 and 3.52.</p>
<p class="meo">💡 Fast mental method for an exam: try K and check 2<sup>K</sup> ≥ M + K + 1. For M = 64, try K = 6: 64 ≥ 71? No. Try K = 7: 128 ≥ 72? Yes. Done in two lines, no table needed.</p>
<p class="pitfall">⚠️ Two traps. (1) <strong>Forgetting the "+ K" in the inequality</strong> — the check bits can be corrupted too, so they must also be nameable by the syndrome. Using 2<sup>K</sup> ≥ M + 1 gives K = 6 for M = 64 and a wrong answer. (2) <strong>Computing the percentage against the wrong base</strong>: it is K/M (check bits ÷ <em>data</em> bits), not K/(M+K). For M = 64, K = 8 the answer is 8/64 = 12,5%, not 8/72 = 11,1%.</p>`,
        `<p class="y-chinh">🎯 Bảng báo giá. Với mỗi độ dài từ dữ liệu: sửa một lỗi cần bao nhiêu bit kiểm, sửa-một-lỗi/phát-hiện-hai-lỗi cần bao nhiêu, và bộ nhớ phình ra mấy phần trăm?</p>
<table>
<tr><th rowspan="2">Bit dữ liệu (M)</th><th colspan="2">Sửa một lỗi (SEC)</th><th colspan="2">Sửa một / phát hiện hai lỗi (SEC-DED)</th></tr>
<tr><th>Bit kiểm</th><th>% tăng</th><th>Bit kiểm</th><th>% tăng</th></tr>
<tr><td>8</td><td>4</td><td>50,0</td><td>5</td><td>62,5</td></tr>
<tr><td>16</td><td>5</td><td>31,25</td><td>6</td><td>37,5</td></tr>
<tr><td>32</td><td>6</td><td>18,75</td><td>7</td><td>21,875</td></tr>
<tr><td><strong>64</strong></td><td><strong>7</strong></td><td><strong>10,94</strong></td><td><strong>8</strong></td><td><strong>12,5</strong></td></tr>
<tr><td>128</td><td>8</td><td>6,25</td><td>9</td><td>7,03</td></tr>
<tr><td>256</td><td>9</td><td>3,52</td><td>10</td><td>3,91</td></tr>
</table>
<ul>
<li><strong>Cả bảng sinh ra từ MỘT bất đẳng thức, nên đừng học thuộc — hãy suy ra.</strong> Syndrome phải gọi tên được mọi vị trí lỗi một-bit khả dĩ (M bit dữ liệu + K bit kiểm) <em>CỘNG</em> trường hợp "không lỗi". Syndrome K bit có 2<sup>K</sup> giá trị, nên yêu cầu là <strong>2<sup>K</sup> ≥ M + K + 1</strong>. Lấy K nhỏ nhất thoả mãn.</li>
<li><strong>SEC-DED luôn luôn đúng bằng SEC + 1.</strong> Một bit kiểm thêm — một bit parity tổng thể trên toàn bộ từ đã mã hoá — cho phép mạch phân biệt "một lỗi, nó nằm đây" với "hai lỗi, tôi phát hiện được nhưng không sửa nổi". Phân biệt này cực kỳ quan trọng trong thực tế: sửa nhầm bit một cách im lặng còn tệ hơn báo hỏng thật to.</li>
<li><strong>Bit kiểm tăng theo LOGARIT trong khi bit dữ liệu tăng theo LUỸ THỪA — và đó là lý do ECC trả nổi.</strong> Nhân đôi M chỉ thêm <em>MỘT</em> bit kiểm. Nên phí tổn sụp xuống: 50% ở M = 8, nhưng chỉ còn 3,5% ở M = 256. Sửa lỗi rẻ chính vì bạn mua nó trên những từ dài.</li>
<li><strong>Dòng mô tả phần cứng thật là M = 64.</strong> 64 bit dữ liệu + 8 bit kiểm = <strong>72 bit</strong>, đúng là lý do thanh DIMM máy chủ được gọi là loại "72-bit" và mang CHÍN con chip mỗi rank thay vì tám. Con số 12,5% chính là phần chênh giá thật nằm sẵn trong RAM ECC.</li>
</ul>
<p class="dap-an">✅ <strong>Đã dựng lại từ đầu bằng máy và đối chiếu từng ô.</strong> Với M = 8: K = 4 vì 2<sup>4</sup> = 16 ≥ 8 + 4 + 1 = 13 trong khi 2<sup>3</sup> = 8 &lt; 8 + 3 + 1 = 12 ✓. M = 16 → K = 5 (32 ≥ 22) ✓ · M = 32 → 6 (64 ≥ 39) ✓ · M = 64 → 7 (128 ≥ 72) ✓ · M = 128 → 8 (256 ≥ 137) ✓ · M = 256 → 9 (512 ≥ 266) ✓. Phần trăm: 4/8 = 50%, 5/16 = 31,25%, 6/32 = 18,75%, 7/64 = 10,938%, 8/128 = 6,25%, 9/256 = 3,516% — <strong>tái tạo chính xác từng con số trên slide</strong>, kể cả hai số đã làm tròn 10,94 và 3,52.</p>
<p class="meo">💡 Cách nhẩm nhanh khi thi: thử K rồi kiểm 2<sup>K</sup> ≥ M + K + 1. Với M = 64, thử K = 6: 64 ≥ 71? Không. Thử K = 7: 128 ≥ 72? Có. Xong trong hai dòng, không cần bảng.</p>
<p class="pitfall">⚠️ Hai bẫy. (1) <strong>Quên chữ "+ K" trong bất đẳng thức</strong> — chính các bit kiểm cũng có thể hỏng, nên syndrome phải gọi tên được cả chúng. Dùng 2<sup>K</sup> ≥ M + 1 sẽ ra K = 6 cho M = 64 và sai đáp án. (2) <strong>Tính phần trăm trên MẪU SỐ SAI</strong>: nó là K/M (bit kiểm ÷ bit <em>DỮ LIỆU</em>), không phải K/(M+K). Với M = 64, K = 8 thì đáp án là 8/64 = 12,5%, không phải 8/72 = 11,1%.</p>`],

      [20, 'Figure 6.9 — Layout of Data Bits and Check Bits (8 data bits, 4 check bits)',
        `<p class="y-chinh">🎯 The bookkeeping that turns the Venn picture of slide 18 into something a circuit can build for any word size. Twelve bit positions, numbered 1 to 12; <strong>check bits go in the power-of-two positions, data bits fill everything else.</strong></p>
<table>
<tr><th>Bit Position</th><td>12</td><td>11</td><td>10</td><td>9</td><td>8</td><td>7</td><td>6</td><td>5</td><td>4</td><td>3</td><td>2</td><td>1</td></tr>
<tr><th>Position Number</th><td>1100</td><td>1011</td><td>1010</td><td>1001</td><td>1000</td><td>0111</td><td>0110</td><td>0101</td><td>0100</td><td>0011</td><td>0010</td><td>0001</td></tr>
<tr><th>Data Bit</th><td>D8</td><td>D7</td><td>D6</td><td>D5</td><td></td><td>D4</td><td>D3</td><td>D2</td><td></td><td>D1</td><td></td><td></td></tr>
<tr><th>Check Bit</th><td></td><td></td><td></td><td></td><td>C8</td><td></td><td></td><td></td><td>C4</td><td></td><td>C2</td><td>C1</td></tr>
</table>
<ul>
<li><strong>Why 12 positions for 8 data bits?</strong> Table 6.2 said M = 8 needs K = 4, so the encoded word is 8 + 4 = 12 bits. The four check bits are named C1, C2, C4, C8 after the positions they occupy — <strong>1, 2, 4 and 8, the powers of two</strong>.</li>
<li><strong>The "Position Number" row is the reason the scheme works.</strong> Each position is written in binary. A check bit C<sub>j</sub> covers <em>every position whose binary number has a 1 in bit j</em>. Read the table column-wise and the rule falls out.</li>
<li><strong>Which data bits does each check bit cover?</strong> Computed directly from the position numbers on the slide: <strong>C1</strong> covers positions 3, 5, 7, 9, 11 = D1, D2, D4, D5, D7. <strong>C2</strong> covers positions 3, 6, 7, 10, 11 = D1, D3, D4, D6, D7. <strong>C4</strong> covers positions 5, 6, 7, 12 = D2, D3, D4, D8. <strong>C8</strong> covers positions 9, 10, 11, 12 = D5, D6, D7, D8. Each check bit is set so its own group has even parity — exactly the three circles of Figure 6.8, now four of them.</li>
<li><strong>And here is the payoff: the syndrome IS the position number.</strong> Suppose the bit in position 10 flips. Position 10 = 1010 in binary, so it is covered by C2 and C8 but not C1 or C4. Recomputing gives C8 C4 C2 C1 = <strong>1 0 1 0</strong> = decimal <strong>10</strong> — the syndrome literally spells out the bit position to invert. All zeros means no error. That is the entire Corrector box of Figure 6.7 in one sentence.</li>
<li><strong>Why the powers of two, specifically?</strong> Because 1, 2, 4, 8 are the only positions covered by exactly <em>one</em> check bit each. That makes every check bit independently solvable when encoding — no simultaneous equations, just four separate parity sums. It is a design choice that makes the hardware trivial.</li>
</ul>
<p class="dap-an">✅ Verified by machine against the slide's own Position Number row: C1 → {3, 5, 7, 9, 11} ✓ · C2 → {3, 6, 7, 10, 11} ✓ · C4 → {5, 6, 7, 12} ✓ · C8 → {9, 10, 11, 12} ✓. Cross-check: every data position 3, 5, 6, 7, 9, 10, 11, 12 is covered by at least two check bits, and no two data positions are covered by the same set — which is precisely what makes single-error <em>location</em> possible. <strong>Worked answer for a typical exam item: "which check bits cover D4?" D4 sits at position 7 = 0111 → C1, C2 and C4.</strong></p>
<p class="meo">💡 The rule in one line: <strong>write the position number in binary; bit j of that number tells you whether check bit C2<sup>j</sup> looks at this position.</strong> Everything else — encoding, syndrome, correction — is that one line applied twice.</p>
<p class="pitfall">⚠️ Trap: positions are numbered from <strong>1</strong>, not 0, and the numbering in the figure runs right-to-left (position 1 at the far right). Start at 0 and every power-of-two slot shifts, the syndrome stops matching the position, and the code silently corrects the wrong bit. Also note the data bits are <em>not</em> contiguous — D1 is at position 3, D5 at position 9 — so you cannot just read the data straight out of the stored word without unpacking it.</p>`,
        `<p class="y-chinh">🎯 Phần sổ sách biến bức tranh Venn ở slide 18 thành thứ mạch điện dựng được cho từ dài bất kỳ. Mười hai vị trí bit, đánh số 1 tới 12; <strong>bit KIỂM nằm ở các vị trí luỹ thừa 2, bit DỮ LIỆU lấp vào mọi chỗ còn lại.</strong></p>
<table>
<tr><th>Vị trí bit</th><td>12</td><td>11</td><td>10</td><td>9</td><td>8</td><td>7</td><td>6</td><td>5</td><td>4</td><td>3</td><td>2</td><td>1</td></tr>
<tr><th>Số hiệu vị trí (nhị phân)</th><td>1100</td><td>1011</td><td>1010</td><td>1001</td><td>1000</td><td>0111</td><td>0110</td><td>0101</td><td>0100</td><td>0011</td><td>0010</td><td>0001</td></tr>
<tr><th>Bit dữ liệu</th><td>D8</td><td>D7</td><td>D6</td><td>D5</td><td></td><td>D4</td><td>D3</td><td>D2</td><td></td><td>D1</td><td></td><td></td></tr>
<tr><th>Bit kiểm</th><td></td><td></td><td></td><td></td><td>C8</td><td></td><td></td><td></td><td>C4</td><td></td><td>C2</td><td>C1</td></tr>
</table>
<ul>
<li><strong>Sao lại 12 vị trí cho 8 bit dữ liệu?</strong> Table 6.2 nói M = 8 cần K = 4, nên từ đã mã hoá dài 8 + 4 = 12 bit. Bốn bit kiểm được đặt tên C1, C2, C4, C8 theo đúng vị trí chúng chiếm — <strong>1, 2, 4 và 8, các luỹ thừa của 2</strong>.</li>
<li><strong>Dòng "Số hiệu vị trí" mới là lý do cả cơ chế chạy được.</strong> Mỗi vị trí được viết ra dưới dạng nhị phân. Bit kiểm C<sub>j</sub> phủ <em>MỌI vị trí mà số nhị phân của nó có bit thứ j bằng 1</em>. Đọc bảng theo CỘT là quy tắc tự hiện ra.</li>
<li><strong>Mỗi bit kiểm phủ những bit dữ liệu nào?</strong> Tính thẳng từ dòng số hiệu vị trí trên slide: <strong>C1</strong> phủ các vị trí 3, 5, 7, 9, 11 = D1, D2, D4, D5, D7. <strong>C2</strong> phủ 3, 6, 7, 10, 11 = D1, D3, D4, D6, D7. <strong>C4</strong> phủ 5, 6, 7, 12 = D2, D3, D4, D8. <strong>C8</strong> phủ 9, 10, 11, 12 = D5, D6, D7, D8. Mỗi bit kiểm được đặt sao cho nhóm của chính nó có parity chẵn — đúng ba vòng tròn của Figure 6.8, nay thành bốn vòng.</li>
<li><strong>Và đây là phần thưởng: SYNDROME CHÍNH LÀ SỐ HIỆU VỊ TRÍ.</strong> Giả sử bit ở vị trí 10 bị lật. Vị trí 10 = 1010 nhị phân, nên nó được phủ bởi C2 và C8, không phải C1 hay C4. Tính lại sẽ ra C8 C4 C2 C1 = <strong>1 0 1 0</strong> = <strong>10</strong> hệ mười — syndrome đánh vần ra đúng vị trí bit cần đảo. Toàn 0 nghĩa là không lỗi. Đó là trọn khối Corrector của Figure 6.7 gói trong một câu.</li>
<li><strong>Sao lại chọn đúng các luỹ thừa của 2?</strong> Vì 1, 2, 4, 8 là những vị trí duy nhất chỉ bị <em>MỘT</em> bit kiểm phủ. Nhờ đó khi mã hoá, mỗi bit kiểm giải được ĐỘC LẬP — không phải giải hệ phương trình, chỉ là bốn phép tính parity rời. Đó là một lựa chọn thiết kế làm phần cứng trở nên tầm thường.</li>
</ul>
<p class="dap-an">✅ Đã kiểm bằng máy, đối chiếu với chính dòng Số hiệu vị trí của slide: C1 → {3, 5, 7, 9, 11} ✓ · C2 → {3, 6, 7, 10, 11} ✓ · C4 → {5, 6, 7, 12} ✓ · C8 → {9, 10, 11, 12} ✓. Kiểm chéo: mọi vị trí dữ liệu 3, 5, 6, 7, 9, 10, 11, 12 đều được ít nhất hai bit kiểm phủ, và không có hai vị trí dữ liệu nào bị phủ bởi cùng một tập — đó chính xác là thứ khiến việc <em>ĐỊNH VỊ</em> lỗi đơn trở nên khả thi. <strong>Giải mẫu cho một câu hỏi thi điển hình: "những bit kiểm nào phủ D4?" D4 nằm ở vị trí 7 = 0111 → C1, C2 và C4.</strong></p>
<p class="meo">💡 Quy tắc gói trong một dòng: <strong>viết số hiệu vị trí ra nhị phân; bit thứ j của số đó cho biết bit kiểm C2<sup>j</sup> có soi vị trí này hay không.</strong> Mọi thứ còn lại — mã hoá, syndrome, sửa lỗi — chỉ là áp đúng một dòng đó hai lần.</p>
<p class="pitfall">⚠️ Bẫy: vị trí đánh số từ <strong>1</strong>, không phải từ 0, và trong hình thứ tự chạy từ PHẢI sang TRÁI (vị trí 1 nằm ngoài cùng bên phải). Bắt đầu từ 0 là mọi ô luỹ thừa 2 bị lệch, syndrome thôi khớp với vị trí, và mã sẽ lặng lẽ sửa nhầm bit. Cũng để ý các bit dữ liệu <em>KHÔNG</em> nằm liền nhau — D1 ở vị trí 3, D5 ở vị trí 9 — nên không thể đọc thẳng dữ liệu ra khỏi từ đã lưu mà không gỡ gói.</p>`],

    ]),
  ].join('\n'),
};
