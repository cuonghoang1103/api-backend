/**
 * CSI106 · Chương 8 — Software engineering, học theo từng slide: TRỌN BỘ 30 slide.
 * Deck 'csi8' (CSI8), 30 slide, ảnh đã render lên CDN images/academy/CSI106/v1/csi8/NNN.webp.
 *
 * Nội dung bám ĐÚNG chữ trích từ CSI_08.pptx của trường (/tmp/csi106-text/csi8.txt).
 * Các slide nặng sơ đồ (5, 7, 8, 10, 11, 12, 14, 15, 16, 18, 19, 20, 21, 22, 23, 25,
 * 26, 27, 28, 29, 30) đã được ĐỌC THẲNG TỪ ẢNH để lấy đúng từng nhãn trong hình.
 *
 * Hệ thống ví dụ xuyên suốt cả 30 slide: **LibTrack — phần mềm quản lý mượn/trả sách
 * của thư viện trường**. Mỗi pha cho thấy đúng sản phẩm của pha ấy với LibTrack:
 * bản đặc tả → DFD/ERD/sơ đồ trạng thái → sơ đồ cấu trúc module + lớp → mã C →
 * bộ test case thật.
 *
 * ĐÃ CHẠY THẬT (không phải đọc rồi đoán):
 *   · hàm C `phi_tre(so_ngay, uu_tien)` ở slide 26 — biên dịch `cc -Wall`, chạy 5 ca,
 *     và ĐO phủ bằng `clang -fprofile-instr-generate -fcoverage-mapping` + `llvm-cov`.
 *     Kết quả đo: 2 ca cho 100% dòng lệnh nhưng chỉ 6/8 nhánh; 4 ca cho 8/8 nhánh
 *     nhưng điều kiện con `phi > 0` chưa bao giờ nhận giá trị sai; phải thêm ca thứ 5
 *     mới phủ điều kiện. Mọi con số phủ trong bài là số ĐO ĐƯỢC.
 *   · hàm C `xep_loai(diem)` ở slide 27 & 30 — biên dịch `cc -Wall`, chạy 11 giá trị
 *     biên; bảng đầu ra trong bài là đầu ra thật của chương trình.
 *   · phép tính 10^14 test × 1 ms = 3.170 năm của slide 28 — kiểm lại bằng số học,
 *     slide ghi ĐÚNG.
 *
 * Những chỗ SLIDE GỐC SAI, TỰ MÂU THUẪN hoặc ĐÃ LỖI THỜI — đã nêu rõ trong bài,
 * KHÔNG im lặng chép lại và KHÔNG tự ý sửa slide:
 *   · slide 5: caption ghi "Figure 8.1 Evolution of programing" nhưng hình vẽ là bánh xe
 *     SDLC 6 giai đoạn, không liên quan gì tới "tiến hoá của lập trình"; và "programing"
 *     thiếu một chữ m. Nặng hơn: SÁU giai đoạn trong hình (Planning · Analysis · Design ·
 *     Implementation · Testing & Integration · Maintenance) KHÔNG trùng với sáu giai đoạn
 *     trong gạch đầu dòng (requirement analysis · design · development and testing ·
 *     implementation · documentation · evaluation).
 *   · slide 8: thân slide dẫn "Figure 10.3" trong khi caption ghi "Figure 8.3" — số cũ
 *     của Forouzan chương 10 còn sót.
 *   · slide 11 và slide 12 CÙNG mang nhãn "Figure 8.6" cho hai hình khác nhau
 *     (sơ đồ trạng thái ở 11, sơ đồ use case ở 12).
 *   · slide 12: một câu bị cắt làm hai gạch đầu dòng ("…uses an" / "object-oriented
 *     language…") — lỗi xuống dòng, không phải hai ý.
 *   · slide 16: chữ nói "Figure 10.9 … our four classes used in the design of the
 *     old-style elevator" nhưng hình thật là sơ đồ lớp của hệ Book Trading. Chữ chép từ
 *     Forouzan mà quên đổi theo hình.
 *   · slide 21: gạch đầu dòng liệt kê sáu đo lường operability (accuracy, efficiency,
 *     reliability, security, timeliness, usability) nhưng Figure 8.14 vẽ một cây HOÀN TOÀN
 *     KHÁC (Switchability/Availability → Controllability, Flexibility, Reliability,
 *     Maintainability → Stability, Optimality). Chữ và hình không khớp nhau.
 *   · slide 22: Figure 8.15 liệt kê Analyzability/Modularity/Modifiability/Reusability/
 *     Testability (bộ ISO/IEC 25010) trong khi Figure 8.13 ở slide 20 nói maintainability
 *     gồm changeability/correctability/flexibility/testability (bộ Forouzan). Riêng
 *     Reusability ở hình 8.13 nằm dưới Transferability còn ở hình 8.15 lại nằm dưới
 *     Maintainability.
 *   · slide 25: thân slide dẫn "(Figure 10.11)" trong khi caption là "Figure 8.17".
 *   · slide 29 caption "Figure 10.15" và slide 30 caption "Figure 10.16" — lẽ ra phải là
 *     8.21 và 8.22.
 *   · slide 30: câu "some condition in the module's code such as x . 100 is written as
 *     x . 100" vô nghĩa vì hai dấu so sánh đã rụng khi chuyển file. Nguyên văn Forouzan là
 *     "x >= 100 is written as x > 100".
 *   · slide 3 (Objectives) hứa dạy documentation (user/system/technical) nhưng KHÔNG có
 *     slide nào trong 30 slide nói về nó. Slide 6 liệt kê 8 mô hình SDLC nhưng deck chỉ
 *     dạy 2 (waterfall, incremental).
 */
import { walk, walkHead } from './_slides.mjs';

const D = 'csi8';

export default {
  title: '8.0 — Slide by slide: The software lifecycle — analysis, design, implementation and testing (30 slides)|||8.0 — Slide bài giảng: Vòng đời phần mềm — phân tích, thiết kế, hiện thực & kiểm thử (30 slide)',
  slug: 'csi106-8-0-slides-vong-doi-phan-mem',
  type: 'DOCUMENT',
  description: 'Trọn Chương 8 của CSI106 (30 slide) đi theo đúng bộ slide của trường: vòng đời phần mềm và tám mô hình SDLC, mô hình thác nước so với mô hình tăng trưởng, pha phân tích theo hai lối thủ tục và hướng đối tượng (DFD, sơ đồ trạng thái, use case, sơ đồ lớp), pha thiết kế (sơ đồ cấu trúc module, chi tiết lớp), pha hiện thực với ba nhóm chất lượng phần mềm, rồi pha kiểm thử với hộp kính và hộp đen. Cả bài dùng MỘT hệ thống ví dụ xuyên suốt — phần mềm quản lý thư viện LibTrack — nên mỗi pha đều cho ra một sản phẩm cụ thể sờ được. Đoạn mã C tính phí trả sách trễ đã được biên dịch và ĐO phủ nhánh thật bằng llvm-cov, nên bộ test và các con số phủ trong bài là số đo chứ không phải suy đoán. Những chỗ slide gốc ghi sai hoặc chữ không khớp hình đều được chỉ ra thay vì chép lại.',
  content: [
    walkHead(D, 1, 30),
    walk(D, [

      [1, '8. Software engineering',
        `<p class="y-chinh">🎯 The title slide. The two words to weigh are <strong>engineering</strong> and <strong>software</strong>: this chapter is about applying an <em>engineering discipline</em> — plan, specify, design, build, verify — to something that has no weight, no wear and no physics.</p>
<ul>
<li><strong>Where the phrase comes from</strong> — "software engineering" was chosen deliberately and provocatively as the title of a 1968 NATO conference in Garmisch, Germany, convened because large software projects were routinely late, over budget and broken. The organisers picked the word "engineering" to shame the field into growing up. The situation was called the <em>software crisis</em>.</li>
<li><strong>Why programming alone is not enough</strong> — you can write a 200-line program in PRF192 with no process at all: you hold the whole thing in your head. At 200,000 lines with 20 people, nobody holds it in their head, so the knowledge must live in <em>documents and diagrams</em> instead. That is the entire subject of this chapter.</li>
<li><strong>What this chapter is NOT</strong> — it does not teach you a programming language, and it does not teach you a specific methodology like Scrum. It teaches the five classic phases and the vocabulary that every later software course (SWE201, SWP391, SWT301) assumes you already have.</li>
<li><strong>The five phases, in one line</strong> — <em>lifecycle → analysis → design → implementation → testing</em>. Those are exactly the five sections of the deck, and exactly the five sessions 35–38 of the syllabus.</li>
<li><strong>The running example for all 30 slides</strong> — we will build one imaginary system all the way through: <strong>LibTrack</strong>, the borrow-and-return software of a university library. Every phase will produce a concrete artefact for LibTrack, so "analysis phase" stops being a phrase and becomes a document you can point at.</li>
</ul>
<p class="meo">💡 Keep one sentence in your pocket for the whole chapter: <strong>each phase answers exactly one question.</strong> Analysis answers <em>what?</em> Design answers <em>how?</em> Implementation answers <em>in which language?</em> Testing answers <em>did we get it right?</em> Almost every exam question in this chapter is a disguised version of "which phase answers this question?"</p>`,
        `<p class="y-chinh">🎯 Slide tiêu đề. Hai chữ đáng cân nhắc là <strong>engineering</strong> (công nghệ, kỹ nghệ) và <strong>software</strong>: chương này nói về việc áp một <em>kỷ luật kỹ thuật</em> — lên kế hoạch, đặc tả, thiết kế, dựng, nghiệm — lên một thứ không có khối lượng, không mài mòn, và không chịu định luật vật lý nào.</p>
<ul>
<li><strong>Cụm từ này từ đâu ra</strong> — "software engineering" được chọn một cách cố ý và có phần khiêu khích làm tên hội nghị NATO năm 1968 ở Garmisch (Đức), triệu tập vì các dự án phần mềm lớn cứ trễ hạn, vỡ ngân sách và chạy sai một cách đều đặn. Ban tổ chức chọn chữ "kỹ nghệ" để ép cái ngành này phải trưởng thành. Tình trạng ấy có tên riêng: <em>khủng hoảng phần mềm</em>.</li>
<li><strong>Vì sao biết lập trình thôi là chưa đủ</strong> — bạn viết được một chương trình 200 dòng ở PRF192 mà chẳng cần quy trình nào: cả chương trình nằm gọn trong đầu bạn. Tới 200.000 dòng với 20 người thì không ai ôm nổi nó trong đầu, nên tri thức buộc phải sống trong <em>tài liệu và sơ đồ</em>. Đó chính là toàn bộ nội dung chương này.</li>
<li><strong>Chương này KHÔNG dạy gì</strong> — nó không dạy bạn một ngôn ngữ lập trình, cũng không dạy một phương pháp cụ thể như Scrum. Nó dạy năm pha kinh điển và bộ từ vựng mà mọi môn phần mềm sau này (SWE201, SWP391, SWT301) mặc định bạn đã có.</li>
<li><strong>Năm pha, gói trong một dòng</strong> — <em>vòng đời → phân tích → thiết kế → hiện thực → kiểm thử</em>. Đúng năm mục của bộ slide, và đúng năm buổi 35–38 của đề cương.</li>
<li><strong>Hệ thống ví dụ chạy suốt 30 slide</strong> — ta sẽ dựng một hệ tưởng tượng đi hết cả chương: <strong>LibTrack</strong>, phần mềm mượn/trả sách của thư viện trường. Mỗi pha sẽ đẻ ra một sản phẩm cụ thể cho LibTrack, để "pha phân tích" thôi là một cụm chữ mà thành một tài liệu bạn chỉ tay vào được.</li>
</ul>
<p class="meo">💡 Bỏ túi một câu cho cả chương: <strong>mỗi pha trả lời đúng MỘT câu hỏi.</strong> Phân tích trả lời <em>cái gì?</em> Thiết kế trả lời <em>bằng cách nào?</em> Hiện thực trả lời <em>viết bằng ngôn ngữ nào?</em> Kiểm thử trả lời <em>làm đúng chưa?</em> Gần như mọi câu hỏi thi của chương này đều là biến thể nguỵ trang của "câu hỏi này thuộc pha nào?".</p>`],

      [2, 'Content',
        `<p class="y-chinh">🎯 Five sections. Read the list as a pipeline, not as five unrelated topics: section 8.1 gives the frame, and 8.2 → 8.5 are the frame's four boxes visited one at a time, in the order a real project visits them.</p>
<ul>
<li><strong>8.1 The software lifecycle</strong> (slides 4–8) — what a lifecycle is, the list of SDLC models, then the two the course actually teaches: waterfall and incremental. This is the only section with a comparison table in it, and comparison tables are exam magnets.</li>
<li><strong>8.2 Analysis phase</strong> (9–12) — produces the <em>specification document</em>. Two roads: procedure-oriented (DFD, state diagram) and object-oriented (use case diagram, class diagram).</li>
<li><strong>8.3 Design phase</strong> (13–16) — turns <em>what</em> into <em>how</em>. Same two roads again: structure chart for procedures, detailed classes for objects. Notice the deliberate parallelism between 8.2 and 8.3 — the deck repeats the same shape twice on purpose, so learn the shape once.</li>
<li><strong>8.4 Implementation phase</strong> (17–23) — the longest section, and surprisingly it is mostly <em>not</em> about writing code: one slide on language choice and five on <strong>quality</strong> (operability, maintainability, transferability). That ratio is the message: at this level, code is easy and quality is hard.</li>
<li><strong>8.5 Testing phase</strong> (24–30) — glass-box versus black-box, then three black-box methods: exhaustive, random, boundary-value. This section carries the only question in the whole chapter where you have to <em>do</em> something rather than recall something.</li>
</ul>
<p class="pitfall">⚠️ Notice what is missing from this content list: <strong>documentation</strong> and <strong>maintenance</strong>. Slide 3 promises documentation as an objective and the SDLC wheel on slide 5 shows "Maintenance" as stage 6, but no slide teaches either one. If your exam paper asks "distinguish user documentation, system documentation and technical documentation", the answer is not in this deck — it is in Forouzan §10.6, and you have to read it yourself.</p>`,
        `<p class="y-chinh">🎯 Năm mục. Hãy đọc danh sách này như một dây chuyền, đừng đọc như năm chủ đề rời rạc: mục 8.1 dựng cái khung, còn 8.2 → 8.5 là bốn cái hộp trong khung ấy, thăm lần lượt từng cái, theo đúng thứ tự một dự án thật đi qua.</p>
<ul>
<li><strong>8.1 Vòng đời phần mềm</strong> (slide 4–8) — vòng đời là gì, danh sách các mô hình SDLC, rồi hai mô hình môn này thật sự dạy: thác nước và tăng trưởng. Đây là mục duy nhất có bảng so sánh, mà bảng so sánh thì luôn hút đề thi.</li>
<li><strong>8.2 Pha phân tích</strong> (9–12) — đẻ ra <em>tài liệu đặc tả</em>. Hai con đường: hướng thủ tục (DFD, sơ đồ trạng thái) và hướng đối tượng (sơ đồ use case, sơ đồ lớp).</li>
<li><strong>8.3 Pha thiết kế</strong> (13–16) — biến <em>cái gì</em> thành <em>bằng cách nào</em>. Lại đúng hai con đường ấy: sơ đồ cấu trúc cho thủ tục, lớp chi tiết cho đối tượng. Để ý sự song song CỐ Ý giữa 8.2 và 8.3 — bộ slide lặp lại cùng một hình dạng hai lần có chủ đích, nên bạn chỉ cần học cái hình dạng đó một lần.</li>
<li><strong>8.4 Pha hiện thực</strong> (17–23) — mục dài nhất, và bất ngờ là phần lớn nó KHÔNG nói về viết mã: một slide về chọn ngôn ngữ và năm slide về <strong>chất lượng</strong> (operability, maintainability, transferability). Chính tỷ lệ đó là thông điệp: ở mức này, viết mã là phần dễ, chất lượng mới là phần khó.</li>
<li><strong>8.5 Pha kiểm thử</strong> (24–30) — hộp kính so với hộp đen, rồi ba phương pháp hộp đen: vét cạn, ngẫu nhiên, giá trị biên. Mục này chứa câu hỏi duy nhất của cả chương mà bạn phải <em>LÀM</em> chứ không phải <em>nhớ lại</em>.</li>
</ul>
<p class="pitfall">⚠️ Hãy để ý thứ THIẾU trong danh sách nội dung này: <strong>tài liệu (documentation)</strong> và <strong>bảo trì (maintenance)</strong>. Slide 3 hứa documentation là một mục tiêu, và bánh xe SDLC ở slide 5 vẽ "Maintenance" là giai đoạn 6, nhưng không slide nào dạy cả hai. Nếu đề thi hỏi "phân biệt user documentation, system documentation và technical documentation" thì câu trả lời KHÔNG nằm trong bộ slide này — nó nằm ở Forouzan §10.6, và bạn phải tự đọc.</p>`],

      [3, 'Objectives',
        `<p class="y-chinh">🎯 Seven objectives — and six of them are literally exam questions in disguise. Read the verbs: <em>understand, describe, understand, understand, describe, describe, recognize</em>. Not one says "apply" or "build". This chapter is examined by explanation, not by construction.</p>
<ul>
<li><strong>Objective 1 — "understand the concept of the software lifecycle"</strong> — answer: a software product goes through birth, growth, maturity and eventual replacement, and the development part of that life is organised into phases. Slides 4–5.</li>
<li><strong>Objective 2 — "describe two major types of development process, the waterfall and incremental models"</strong> — the single most likely essay question in the chapter. Prepare a four-line answer for each plus one line on when to use which. Slides 7–8.</li>
<li><strong>Objectives 3 and 4 — analysis and design, each with "two separate approaches"</strong> — note the perfect symmetry: procedure-oriented and object-oriented, twice. Four combinations to keep straight, and the exam loves to mix them up (e.g. "which diagram belongs to object-oriented <em>design</em>?").</li>
<li><strong>Objective 5 — "describe the implementation phase and recognize the quality issues"</strong> — "quality issues" means the three-branch tree of slide 20: operability, maintainability, transferability. Learn the tree, not the prose.</li>
<li><strong>Objective 6 — "distinguish between glass-box testing and black-box testing"</strong> — the other guaranteed question. One sentence each plus who performs it plus one method each is a full-mark answer.</li>
<li><strong>Objective 7 — documentation: user, system, technical</strong> — and, as noted, <em>no slide delivers it</em>. In Forouzan: <em>user documentation</em> teaches the end user to run the system (manuals, help screens); <em>system documentation</em> defines the software itself for the people who maintain it (specification, design, test plans); <em>technical documentation</em> describes installing, operating and supporting it (installation guide, operations guide). Write that on your sheet now, because the slides will never give it to you.</li>
</ul>
<p class="meo">💡 Turn each objective into a flashcard whose front is the objective sentence copied word for word. Seven cards is the whole chapter's recall load — this chapter has far fewer facts than Chapter 1 or Chapter 3, and most of its marks come from being <em>precise</em> about a handful of definitions rather than from remembering many.</p>`,
        `<p class="y-chinh">🎯 Bảy mục tiêu — và sáu trong số đó đúng nghĩa là câu hỏi thi trá hình. Đọc các động từ: <em>hiểu, mô tả, hiểu, hiểu, mô tả, mô tả, nhận biết</em>. Không một mục nào nói "áp dụng" hay "xây dựng". Chương này được thi bằng cách GIẢI THÍCH, không phải bằng cách LÀM RA.</p>
<ul>
<li><strong>Mục tiêu 1 — "hiểu khái niệm vòng đời phần mềm"</strong> — trả lời: một sản phẩm phần mềm đi qua ra đời, lớn lên, chín muồi rồi bị thay thế, và phần phát triển trong cái đời ấy được tổ chức thành các pha. Slide 4–5.</li>
<li><strong>Mục tiêu 2 — "mô tả hai kiểu quy trình phát triển chính: thác nước và tăng trưởng"</strong> — đây là câu tự luận dễ ra nhất của cả chương. Hãy chuẩn bị sẵn bốn dòng cho mỗi mô hình cộng một dòng "khi nào dùng cái nào". Slide 7–8.</li>
<li><strong>Mục tiêu 3 và 4 — phân tích và thiết kế, mỗi cái "hai cách tiếp cận riêng"</strong> — chú ý sự đối xứng hoàn hảo: hướng thủ tục và hướng đối tượng, lặp hai lần. Bốn tổ hợp phải phân biệt được, và đề thi rất thích trộn chúng lên (ví dụ "sơ đồ nào thuộc <em>THIẾT KẾ</em> hướng đối tượng?").</li>
<li><strong>Mục tiêu 5 — "mô tả pha hiện thực và nhận ra các vấn đề chất lượng"</strong> — "vấn đề chất lượng" nghĩa là cái cây ba nhánh ở slide 20: operability, maintainability, transferability. Hãy học CÁI CÂY, đừng học đoạn văn.</li>
<li><strong>Mục tiêu 6 — "phân biệt kiểm thử hộp kính và hộp đen"</strong> — câu hỏi chắc chắn có thứ hai. Mỗi loại một câu định nghĩa, cộng "ai làm", cộng một phương pháp tiêu biểu, là đủ điểm tối đa.</li>
<li><strong>Mục tiêu 7 — tài liệu: người dùng, hệ thống, kỹ thuật</strong> — và như đã nói, <em>không slide nào giao hàng cả</em>. Theo Forouzan: <em>tài liệu người dùng</em> dạy người dùng cuối cách chạy hệ thống (sách hướng dẫn, màn hình trợ giúp); <em>tài liệu hệ thống</em> định nghĩa chính phần mềm cho người bảo trì (đặc tả, thiết kế, kế hoạch kiểm thử); <em>tài liệu kỹ thuật</em> mô tả cách cài đặt, vận hành và hỗ trợ (hướng dẫn cài, hướng dẫn vận hành). Chép ngay vào tờ ôn của bạn, vì bộ slide sẽ không bao giờ đưa cho bạn.</li>
</ul>
<p class="meo">💡 Biến mỗi mục tiêu thành một thẻ ghi nhớ mà mặt trước là câu mục tiêu chép nguyên văn. Bảy thẻ là toàn bộ gánh nặng ghi nhớ của chương — chương này ít dữ kiện hơn hẳn Chương 1 hay Chương 3, và phần lớn điểm đến từ việc CHÍNH XÁC với vài định nghĩa chứ không phải nhớ được nhiều.</p>`],

      [4, '1 - The software lifecycle',
        `<p class="y-chinh">🎯 Section divider for 8.1. The idea it opens is deceptively simple: software has a <strong>life</strong>, and most of that life happens <em>after</em> the first release.</p>
<ul>
<li><strong>Why the word "cycle" and not "line"</strong> — because the end loops back to the beginning. A system is built, used, found wanting, modified, used again, and eventually replaced by a new system whose analysis phase begins the loop over. Forouzan draws it as four boxes: analysis → design → implementation → testing, with maintenance feeding back into analysis.</li>
<li><strong>The number that reframes the whole chapter</strong> — across many industry studies, <strong>60–80% of the total money spent on a software system is spent after it first ships</strong>, on maintenance. That is why maintainability (slide 22) gets its own slide: you are optimising the 70%, not the 30%.</li>
<li><strong>Development is a phase of the life, not the life</strong> — the deck teaches only the development part. Operation, support, and retirement exist but are out of scope here.</li>
<li><strong>What LibTrack's life looks like</strong> — the library asks for a system in 2026; analysis runs three weeks; design two weeks; coding eight weeks; testing four weeks; it goes live; then for six years someone changes the fine rate, adds e-books, adapts it to a new student-ID format, and fixes bugs. The six years are the lifecycle too.</li>
<li><strong>What comes next</strong> — slide 5 defines SDLC and gives six stages, slide 6 lists eight process models, slides 7–8 teach the two models you are examined on.</li>
</ul>
<p class="meo">💡 If you ever have to justify why software engineering exists, use this one line: <strong>a program is finished when it runs; a software product is finished when nobody needs it any more.</strong> Everything in this chapter is about the distance between those two sentences.</p>`,
        `<p class="y-chinh">🎯 Slide phân mục cho 8.1. Ý nó mở ra nghe đơn giản mà không đơn giản: phần mềm có một <strong>cuộc đời</strong>, và phần lớn cuộc đời ấy diễn ra <em>SAU</em> lần phát hành đầu tiên.</p>
<ul>
<li><strong>Vì sao là "chu trình" (cycle) chứ không phải "đường thẳng"</strong> — vì cái kết nối vòng về cái đầu. Hệ thống được dựng, được dùng, bị phát hiện thiếu, được sửa, dùng tiếp, rồi cuối cùng bị thay bằng một hệ mới mà pha phân tích của nó lại mở vòng lặp lần nữa. Forouzan vẽ thành bốn hộp: phân tích → thiết kế → hiện thực → kiểm thử, với bảo trì quay ngược về phân tích.</li>
<li><strong>Con số làm đổi cách nhìn cả chương</strong> — qua nhiều khảo sát công nghiệp, <strong>60–80% tổng chi phí của một hệ phần mềm là chi SAU khi nó ra mắt lần đầu</strong>, tức là chi cho bảo trì. Đó là lý do maintainability (slide 22) được cấp hẳn một slide: bạn đang tối ưu phần 70%, không phải phần 30%.</li>
<li><strong>Phát triển chỉ là MỘT pha của cuộc đời, không phải cả cuộc đời</strong> — bộ slide chỉ dạy phần phát triển. Vận hành, hỗ trợ, và khai tử vẫn tồn tại nhưng nằm ngoài phạm vi ở đây.</li>
<li><strong>Cuộc đời của LibTrack trông thế nào</strong> — thư viện đặt hàng năm 2026; phân tích ba tuần; thiết kế hai tuần; viết mã tám tuần; kiểm thử bốn tuần; lên chạy thật; rồi suốt sáu năm sau có người đổi mức phí trễ, thêm sách điện tử, chỉnh theo định dạng mã sinh viên mới, và vá lỗi. Sáu năm ấy cũng là vòng đời.</li>
<li><strong>Kế tiếp là gì</strong> — slide 5 định nghĩa SDLC và đưa sáu giai đoạn, slide 6 liệt kê tám mô hình quy trình, slide 7–8 dạy hai mô hình bạn bị thi.</li>
</ul>
<p class="meo">💡 Nếu có lúc bạn phải biện hộ vì sao công nghệ phần mềm tồn tại, hãy dùng một dòng này: <strong>một chương trình xong khi nó chạy được; một sản phẩm phần mềm xong khi không còn ai cần tới nó nữa.</strong> Mọi thứ trong chương này nằm trong khoảng cách giữa hai câu ấy.</p>`],

      [5, '1. Introduction (the software development life cycle)',
        `<p class="y-chinh">🎯 The SDLC is defined as <em>"a process for planning, creating, testing, and deploying an information system"</em>, it applies to hardware-only, software-only or mixed systems, and the slide says there are <strong>usually six stages</strong>.</p>
<ul>
<li><strong>The six stages as written in the text</strong> — requirement analysis · design · development and testing · implementation · documentation · evaluation. Memorise this list in this order if the exam quotes the slide.</li>
<li><strong>The six stages as drawn in the figure</strong> — the coloured wheel shows something different: 1 Planning · 2 Analysis · 3 Design · 4 Implementation · 5 Testing &amp; Integration · 6 Maintenance. <em>The text and the picture do not agree.</em> Both are legitimate textbook lists of SDLC stages, but they are not the same list, and the slide presents them as if they were.</li>
<li><strong>"SDLC also referred to as the application development life-cycle"</strong> — true, and a third name you will meet is simply <em>systems development life cycle</em>. Same thing; do not let the renaming worry you.</li>
<li><strong>Why "a system can be composed of hardware only"</strong> — because the discipline came out of systems engineering. Designing an ATM is an SDLC project even though half of it is a cash dispenser and a card reader.</li>
<li><strong>LibTrack through the wheel</strong> — <em>Planning</em>: is this worth doing, who pays, three months. <em>Analysis</em>: interview librarians, discover that a book can be renewed twice and that staff borrow with different limits. <em>Design</em>: tables, screens, modules. <em>Implementation</em>: write it. <em>Testing &amp; Integration</em>: prove the fine calculation, connect to the student database. <em>Maintenance</em>: six years of small changes.</li>
<li><strong>The caption is simply wrong</strong> — it reads "Figure 8.1 Evolution of programing". The figure is an SDLC wheel and has nothing to do with the evolution of programming; and "programing" is missing an m.</li>
</ul>
<p class="pitfall">⚠️ When a slide's text and its picture give two different lists, answer with the <strong>text</strong> unless the question explicitly says "according to the figure". Examiners write questions from the bullet points; the pictures on this deck are stock images pulled from the web, not from Forouzan.</p>`,
        `<p class="y-chinh">🎯 SDLC được định nghĩa là <em>"một quy trình để lập kế hoạch, tạo ra, kiểm thử và triển khai một hệ thống thông tin"</em>, nó áp dụng cho hệ chỉ-phần-cứng, chỉ-phần-mềm hoặc lai, và slide nói thường có <strong>sáu giai đoạn</strong>.</p>
<ul>
<li><strong>Sáu giai đoạn theo CHỮ trên slide</strong> — phân tích yêu cầu · thiết kế · phát triển và kiểm thử · triển khai · lập tài liệu · đánh giá. Nếu đề thi trích slide thì học thuộc đúng danh sách này, đúng thứ tự này.</li>
<li><strong>Sáu giai đoạn theo HÌNH trên slide</strong> — bánh xe màu vẽ một thứ khác: 1 Planning · 2 Analysis · 3 Design · 4 Implementation · 5 Testing &amp; Integration · 6 Maintenance. <em>Chữ và hình KHÔNG khớp nhau.</em> Cả hai đều là danh sách SDLC chính đáng trong sách vở, nhưng chúng không phải cùng một danh sách, mà slide lại bày ra như thể chúng là một.</li>
<li><strong>"SDLC còn được gọi là application development life-cycle"</strong> — đúng, và một tên thứ ba bạn sẽ gặp là <em>systems development life cycle</em>. Cùng một thứ; đừng để việc đổi tên làm bạn hoang mang.</li>
<li><strong>Vì sao "một hệ thống có thể chỉ gồm phần cứng"</strong> — vì kỷ luật này sinh ra từ kỹ nghệ hệ thống. Thiết kế một máy ATM vẫn là một dự án SDLC dù một nửa nó là cái khay nhả tiền và cái đầu đọc thẻ.</li>
<li><strong>LibTrack đi qua bánh xe</strong> — <em>Planning</em>: có đáng làm không, ai trả tiền, ba tháng. <em>Analysis</em>: phỏng vấn thủ thư, phát hiện ra một cuốn sách được gia hạn tối đa hai lần và nhân viên mượn với hạn mức khác sinh viên. <em>Design</em>: bảng dữ liệu, màn hình, module. <em>Implementation</em>: viết ra. <em>Testing &amp; Integration</em>: chứng minh phép tính phí trễ đúng, nối vào cơ sở dữ liệu sinh viên. <em>Maintenance</em>: sáu năm sửa vặt.</li>
<li><strong>Caption đơn giản là SAI</strong> — nó ghi "Figure 8.1 Evolution of programing". Hình là bánh xe SDLC, chẳng liên quan gì tới sự tiến hoá của lập trình; và "programing" thì thiếu một chữ m.</li>
</ul>
<p class="pitfall">⚠️ Khi CHỮ và HÌNH trên một slide đưa hai danh sách khác nhau, hãy trả lời theo <strong>CHỮ</strong>, trừ khi câu hỏi nói rõ "theo hình vẽ". Người ra đề lấy câu hỏi từ gạch đầu dòng; còn các hình trong bộ slide này là ảnh nhặt trên mạng, không phải ảnh của Forouzan.</p>`],

      [6, '2. Development process models',
        `<p class="y-chinh">🎯 One definition and one list: an <strong>SDLC model</strong> specifies <em>which</em> stages a process has and <em>in what order</em> they are carried out. Then eight named models — of which the deck goes on to teach only two.</p>
<ul>
<li><strong>The eight on the slide</strong> — Waterfall · V model · Incremental · RAD · Agile · Iterative · Spiral · Prototype. Know the names; you are only examined in depth on waterfall and incremental.</li>
<li><strong>One line each, so the names stop being noise</strong> — <em>Waterfall</em>: one pass, no going back. <em>V model</em>: waterfall bent into a V so each left-hand phase is paired with the test level that verifies it (requirements ↔ acceptance test, design ↔ integration test, code ↔ unit test). <em>Incremental</em>: build a crude whole system, then thicken it. <em>RAD</em> (Rapid Application Development): heavy use of prototypes and reusable components to compress the schedule. <em>Agile</em>: short iterations of 1–4 weeks with the customer in the room; Scrum is the best-known flavour. <em>Iterative</em>: repeat the whole cycle, improving each time. <em>Spiral</em> (Boehm, 1986): iterative with an explicit <strong>risk analysis</strong> at the start of every loop. <em>Prototype</em>: build a throwaway model first to pin down requirements.</li>
<li><strong>The one distinction students always blur</strong> — <em>incremental</em> adds functionality piece by piece (each release does more); <em>iterative</em> refines the same functionality round after round (each release does it better). Most real processes do both at once, which is exactly why the words get mixed up.</li>
<li><strong>Which model would LibTrack use?</strong> — in 2026, almost certainly an agile/iterative process in two-week sprints, because the librarians cannot tell you all the rules up front (they will remember the "book lost during the summer break" rule only when they see a screen). But <em>in the exam you answer with the deck</em>: waterfall and incremental.</li>
<li><strong>Why so many models exist at all</strong> — because the right process depends on how well the requirements are known and how expensive a mistake is. Nuclear reactor control software is built waterfall-with-V; a social app is built agile. Neither is "modern" or "old-fashioned"; they answer different risks.</li>
</ul>
<p class="meo">💡 Mnemonic for the eight: <strong>"W-V-I-R-A-I-S-P"</strong> → <em>Waterfall, V, Incremental, RAD, Agile, Iterative, Spiral, Prototype</em>. If you can only hold two, hold the two the exam asks for — but a one-line description of Agile and Spiral is cheap insurance for a "name two other models" sub-question.</p>`,
        `<p class="y-chinh">🎯 Một định nghĩa và một danh sách: <strong>mô hình SDLC</strong> quy định quy trình có <em>những</em> giai đoạn nào và chúng được thực hiện theo <em>thứ tự</em> nào. Rồi tám mô hình được gọi tên — mà bộ slide sau đó chỉ dạy hai.</p>
<ul>
<li><strong>Tám cái tên trên slide</strong> — Waterfall · V model · Incremental · RAD · Agile · Iterative · Spiral · Prototype. Nhớ tên; bạn chỉ bị thi sâu về waterfall và incremental.</li>
<li><strong>Mỗi cái một dòng, để mấy cái tên thôi là tiếng ồn</strong> — <em>Waterfall</em>: đi một lượt, không quay lại. <em>V model</em>: thác nước bẻ thành chữ V để mỗi pha bên trái ghép cặp với mức kiểm thử nghiệm lại nó (yêu cầu ↔ kiểm thử chấp nhận, thiết kế ↔ kiểm thử tích hợp, mã ↔ kiểm thử đơn vị). <em>Incremental</em>: dựng một hệ thô đủ cả, rồi đắp dày lên. <em>RAD</em> (phát triển ứng dụng nhanh): xài nhiều bản mẫu và thành phần dùng lại để nén lịch trình. <em>Agile</em>: các vòng ngắn 1–4 tuần với khách hàng ngồi ngay đó; Scrum là biến thể nổi tiếng nhất. <em>Iterative</em>: lặp lại cả chu trình, mỗi vòng làm tốt hơn. <em>Spiral</em> (Boehm, 1986): lặp nhưng có hẳn bước <strong>phân tích rủi ro</strong> ở đầu mỗi vòng. <em>Prototype</em>: dựng một bản mẫu dùng xong vứt để chốt cho ra yêu cầu.</li>
<li><strong>Chỗ sinh viên luôn nhập nhằng</strong> — <em>incremental</em> (tăng trưởng) thêm CHỨC NĂNG từng mảnh (mỗi bản làm được NHIỀU hơn); <em>iterative</em> (lặp) mài lại CÙNG chức năng qua từng vòng (mỗi bản làm TỐT hơn). Quy trình thật thường làm cả hai cùng lúc, và chính vì thế hai chữ này hay bị trộn.</li>
<li><strong>LibTrack sẽ dùng mô hình nào?</strong> — năm 2026 thì gần như chắc chắn là quy trình agile/lặp với sprint hai tuần, vì các cô thủ thư không thể kể hết luật ngay từ đầu (họ chỉ nhớ ra luật "sách mất trong kỳ nghỉ hè" khi nhìn thấy màn hình). Nhưng <em>trong bài thi thì bạn trả lời theo bộ slide</em>: thác nước và tăng trưởng.</li>
<li><strong>Vì sao lại lắm mô hình đến thế</strong> — vì quy trình đúng phụ thuộc vào việc yêu cầu đã rõ tới đâu và một sai lầm đắt cỡ nào. Phần mềm điều khiển lò phản ứng hạt nhân dựng theo waterfall-với-V; một app mạng xã hội dựng theo agile. Không cái nào "hiện đại" hay "lỗi thời"; chúng trả lời hai loại rủi ro khác nhau.</li>
</ul>
<p class="meo">💡 Mẹo nhớ tám cái: <strong>"W-V-I-R-A-I-S-P"</strong> → <em>Waterfall, V, Incremental, RAD, Agile, Iterative, Spiral, Prototype</em>. Nếu chỉ giữ nổi hai thì giữ đúng hai cái đề thi hỏi — nhưng thuộc thêm một dòng về Agile và Spiral là khoản bảo hiểm rẻ cho câu phụ "kể tên hai mô hình khác".</p>`],

      [7, '3. Waterfall model',
        `<p class="y-chinh">🎯 One model, one rule: <strong>the process flows in only one direction, and a phase cannot start until the previous phase is complete</strong>. Figure 8.2 draws six blocks stepping down like a staircase — Requirements → Design → Development → Testing → Deployment → Maintenance — with an arrow between each pair and none going back up.</p>
<ul>
<li><strong>Read the picture literally</strong> — every arrow points down-right. There is no arrow returning from Testing to Design. That absence <em>is</em> the model.</li>
<li><strong>Its one great advantage</strong> — each phase ends with a finished, signed-off document, so the project is easy to plan, budget, staff and audit. A manager can say "we are 40% done" and mean something.</li>
<li><strong>Its one fatal disadvantage</strong> — nothing runs until late, so a misunderstanding in Requirements is discovered in Testing, when it is at its most expensive to fix.</li>
<li><strong>The number that makes this concrete</strong> — the classic cost-of-change curve (Boehm, 1981, confirmed by later industry data) says a defect caught in the requirements phase costs about <strong>1 unit</strong> to fix; the same defect caught in design costs ~5; in coding ~10; in testing ~15–40; and after release <strong>50–100 times more</strong> — sometimes worse, because it now includes patching customers. The numbers vary by study, so quote them as an order of magnitude, not as gospel. The lesson does not vary: <em>the earlier you find it, the cheaper it is</em>, and that is the whole argument for spending three weeks on analysis.</li>
<li><strong>Where the name comes from</strong> — from Winston Royce's 1970 paper. The irony worth knowing: Royce drew the single-direction diagram as the <em>naive</em> version and spent the rest of the paper explaining why it "is risky and invites failure", proposing feedback loops instead. The industry kept his picture and threw away his argument.</li>
<li><strong>LibTrack under waterfall</strong> — you would write the complete specification (every rule about loans, fines, renewals, lost books) and get it signed before anyone writes a line of code. If in week 10 a librarian says "oh, staff can borrow 20 books not 5", you are changing a signed document, and that costs real money.</li>
<li><strong>When waterfall is still the right answer</strong> — requirements genuinely fixed and unlikely to change (a payroll change mandated by a new tax law), safety-critical or contractual work where the document <em>is</em> the deliverable, and small projects where the whole thing is understood up front.</li>
</ul>
<p class="pitfall">⚠️ Do not write "waterfall is bad and agile is good" in an exam. The deck teaches waterfall as a legitimate model, and the correct exam answer is a balanced one: strong on documentation, planning and control; weak when requirements change or when the customer needs to see something early.</p>`,
        `<p class="y-chinh">🎯 Một mô hình, một luật: <strong>quy trình chảy theo đúng MỘT chiều, và một pha không được bắt đầu khi pha trước chưa xong</strong>. Hình 8.2 vẽ sáu khối bậc thang đi xuống — Requirements → Design → Development → Testing → Deployment → Maintenance — mỗi cặp một mũi tên, và không mũi tên nào đi ngược lên.</p>
<ul>
<li><strong>Hãy đọc bức hình theo nghĩa đen</strong> — mọi mũi tên đều chỉ xuống-phải. Không có mũi tên nào quay từ Testing về Design. Chính sự VẮNG MẶT ấy <em>LÀ</em> mô hình.</li>
<li><strong>Ưu điểm lớn nhất của nó</strong> — mỗi pha kết thúc bằng một tài liệu hoàn chỉnh đã ký duyệt, nên dự án dễ lên kế hoạch, dễ tính tiền, dễ bố trí người và dễ kiểm toán. Người quản lý nói "đã xong 40%" và câu ấy có nghĩa thật.</li>
<li><strong>Nhược điểm chí mạng duy nhất</strong> — chẳng có gì chạy được cho tới tận cuối, nên một hiểu lầm ở pha Yêu cầu sẽ bị phát hiện ở pha Kiểm thử, đúng lúc nó đắt nhất để sửa.</li>
<li><strong>Con số làm điều đó thành cụ thể</strong> — đường cong chi phí thay đổi kinh điển (Boehm, 1981, được dữ liệu công nghiệp về sau xác nhận) nói: một lỗi bắt được ở pha yêu cầu tốn khoảng <strong>1 đơn vị</strong> để sửa; cũng lỗi ấy bắt ở thiết kế tốn ~5; ở viết mã ~10; ở kiểm thử ~15–40; và sau khi phát hành thì <strong>gấp 50–100 lần</strong> — đôi khi tệ hơn, vì lúc đó còn phải đi vá cho từng khách hàng. Các con số dao động theo từng khảo sát, nên hãy trích như một BẬC ĐỘ LỚN, đừng trích như kinh thánh. Bài học thì không dao động: <em>bắt càng sớm càng rẻ</em>, và đó là toàn bộ lý lẽ cho việc bỏ ba tuần ra phân tích.</li>
<li><strong>Cái tên từ đâu ra</strong> — từ bài báo năm 1970 của Winston Royce. Chỗ mỉa mai đáng biết: Royce vẽ cái sơ đồ một chiều ấy như phiên bản <em>ngây thơ</em>, rồi dành phần còn lại của bài để giải thích vì sao nó "đầy rủi ro và mời gọi thất bại", và đề nghị thêm các vòng phản hồi. Ngành công nghiệp giữ lại bức hình của ông và vứt đi lập luận của ông.</li>
<li><strong>LibTrack dưới mô hình thác nước</strong> — bạn sẽ viết trọn bản đặc tả (mọi luật về mượn, phí trễ, gia hạn, sách mất) và lấy chữ ký duyệt trước khi ai đó gõ dòng mã đầu tiên. Nếu tới tuần thứ 10 một cô thủ thư nói "à, nhân viên được mượn 20 cuốn chứ không phải 5", thì bạn đang sửa một tài liệu ĐÃ KÝ, và cái đó tốn tiền thật.</li>
<li><strong>Khi nào thác nước vẫn là câu trả lời đúng</strong> — khi yêu cầu thật sự cố định và khó đổi (sửa hệ lương theo một luật thuế mới), khi công việc thuộc loại an toàn-trọng yếu hoặc theo hợp đồng mà chính TÀI LIỆU là sản phẩm bàn giao, và khi dự án nhỏ, hiểu trọn ngay từ đầu.</li>
</ul>
<p class="pitfall">⚠️ Đừng viết trong bài thi rằng "thác nước dở còn agile hay". Bộ slide dạy thác nước như một mô hình chính đáng, và câu trả lời đúng phải cân bằng: mạnh ở tài liệu, kế hoạch và kiểm soát; yếu khi yêu cầu thay đổi hoặc khi khách hàng cần thấy sản phẩm sớm.</p>`],

      [8, 'The incremental model',
        `<p class="y-chinh">🎯 The alternative: software is developed <strong>in a series of steps</strong>. The developers first finish a <em>simplified version of the whole system</em> — the entire system, but without the details — and then add detail in later increments.</p>
<ul>
<li><strong>Read the figure carefully</strong> — Figure 8.3 draws three rows. Each row is a full mini-waterfall: Analysis → Design → Code → Test, ending in a yellow box "Increment-1", "Increment-2", "Increment-3", with arrows chaining one increment into the next. That is the model in one image: <em>a stack of small waterfalls</em>.</li>
<li><strong>The crucial word is "whole"</strong> — increment 1 is not "the login screen finished perfectly". It is the <em>entire</em> system in crude form: you can borrow, you can return, nothing else works well. Building one perfect module first is a different (and worse) strategy, because you learn nothing about how the pieces fit.</li>
<li><strong>What it buys you</strong> — something runs early, so the customer corrects you early, when correcting is cheap. Risk is spread across increments instead of concentrated at the end.</li>
<li><strong>What it costs you</strong> — the architecture must be good enough to survive being extended three or four times, and the total effort is usually higher than an ideal waterfall that got everything right first time. (Ideal waterfalls are rare.)</li>
<li><strong>LibTrack in three increments</strong> — <em>Increment 1</em>: borrow a book, return a book, one fixed fine rate, no reservations, one user type. <em>Increment 2</em>: reservations, renewals, different limits for students and staff. <em>Increment 3</em>: lost-book handling, reports for the librarian, email reminders. After increment 1 the library can already use the system for real, and every rule they forgot to tell you surfaces in week 4 instead of week 20.</li>
</ul>
<table>
<tr><th>Aspect</th><th>Waterfall</th><th>Incremental</th></tr>
<tr><td>Direction of flow</td><td>One way, no return</td><td>Repeats the whole cycle per increment</td></tr>
<tr><td>First working software</td><td>Very late (after testing)</td><td>Very early (end of increment 1)</td></tr>
<tr><td>Requirement changes</td><td>Expensive, resisted</td><td>Absorbed by the next increment</td></tr>
<tr><td>Customer feedback</td><td>Once, at the end</td><td>After every increment</td></tr>
<tr><td>Documentation weight</td><td>Heavy, signed off per phase</td><td>Lighter, revisited each round</td></tr>
<tr><td>Risk profile</td><td>All risk lands at the end</td><td>Risk spread evenly</td></tr>
<tr><td>Best when…</td><td>Requirements fixed, contract/safety critical</td><td>Requirements unclear or likely to change</td></tr>
</table>
<p class="pitfall">⚠️ The slide's own text says "Figure 10.3 shows the incremental model concept" while the caption underneath says "Figure 8.3". Leftover numbering from Forouzan's Chapter 10 — harmless here, but it is a reminder that this deck's figure numbers are not reliable cross-references.</p>`,
        `<p class="y-chinh">🎯 Lối đi thay thế: phần mềm được phát triển <strong>thành một chuỗi bước</strong>. Người phát triển trước hết hoàn tất một <em>phiên bản đơn giản hoá của TOÀN hệ thống</em> — nguyên cả hệ thống, nhưng không có chi tiết — rồi đắp chi tiết vào ở những vòng tăng trưởng sau.</p>
<ul>
<li><strong>Đọc kỹ bức hình</strong> — Hình 8.3 vẽ ba hàng. Mỗi hàng là một thác nước thu nhỏ trọn vẹn: Analysis → Design → Code → Test, kết ở ô vàng "Increment-1", "Increment-2", "Increment-3", có mũi tên nối vòng này sang vòng kia. Đó là cả mô hình gói trong một bức ảnh: <em>một chồng thác nước nhỏ</em>.</li>
<li><strong>Chữ mấu chốt là "TOÀN BỘ"</strong> — increment 1 KHÔNG phải là "làm xong hoàn hảo cái màn hình đăng nhập". Nó là <em>TRỌN</em> hệ thống ở dạng thô: mượn được, trả được, còn lại chạy tạm. Làm hoàn hảo một module trước là một chiến lược KHÁC (và tệ hơn), vì bạn chẳng học được gì về chuyện các mảnh ghép vào nhau ra sao.</li>
<li><strong>Nó mua cho bạn cái gì</strong> — có thứ chạy được sớm, nên khách hàng sửa lưng bạn sớm, lúc sửa còn rẻ. Rủi ro được rải đều qua các vòng thay vì dồn hết vào cuối.</li>
<li><strong>Nó lấy đi của bạn cái gì</strong> — kiến trúc phải đủ tốt để sống sót qua ba bốn lần bị mở rộng, và tổng công sức thường cao hơn một thác nước lý tưởng làm đúng ngay từ đầu. (Mà thác nước lý tưởng thì hiếm.)</li>
<li><strong>LibTrack chia làm ba vòng</strong> — <em>Vòng 1</em>: mượn sách, trả sách, một mức phí trễ cố định, chưa có đặt trước, một loại người dùng. <em>Vòng 2</em>: đặt trước, gia hạn, hạn mức khác nhau cho sinh viên và nhân viên. <em>Vòng 3</em>: xử lý sách mất, báo cáo cho thủ thư, nhắc hạn qua email. Xong vòng 1 là thư viện đã dùng thật được, và mọi luật họ quên kể nổi lên ở tuần 4 thay vì tuần 20.</li>
</ul>
<table>
<tr><th>Khía cạnh</th><th>Thác nước (Waterfall)</th><th>Tăng trưởng (Incremental)</th></tr>
<tr><td>Chiều chảy</td><td>Một chiều, không quay lại</td><td>Lặp trọn chu trình cho mỗi vòng</td></tr>
<tr><td>Phần mềm chạy được lần đầu</td><td>Rất muộn (sau khi kiểm thử)</td><td>Rất sớm (hết vòng 1)</td></tr>
<tr><td>Yêu cầu thay đổi</td><td>Đắt, bị cản</td><td>Hấp thụ vào vòng kế tiếp</td></tr>
<tr><td>Phản hồi của khách</td><td>Một lần, ở cuối</td><td>Sau mỗi vòng</td></tr>
<tr><td>Khối lượng tài liệu</td><td>Nặng, ký duyệt từng pha</td><td>Nhẹ hơn, làm lại mỗi vòng</td></tr>
<tr><td>Dáng rủi ro</td><td>Toàn bộ rủi ro đáp xuống ở cuối</td><td>Rủi ro rải đều</td></tr>
<tr><td>Hợp nhất khi…</td><td>Yêu cầu cố định, hợp đồng / an toàn trọng yếu</td><td>Yêu cầu chưa rõ hoặc dễ đổi</td></tr>
</table>
<p class="pitfall">⚠️ Chính chữ trên slide viết "Figure 10.3 shows the incremental model concept" trong khi caption bên dưới ghi "Figure 8.3". Số hình cũ từ Chương 10 của Forouzan còn sót — ở đây vô hại, nhưng nó nhắc bạn rằng số hình trong bộ slide này không dùng làm tham chiếu chéo được.</p>`],

      [9, '2 - Analysis phase',
        `<p class="y-chinh">🎯 Section divider for 8.2. From here, one question governs everything: <strong>WHAT must the system do?</strong> — and the discipline of the phase is to answer it without once saying <em>how</em>.</p>
<ul>
<li><strong>The output of the phase has a name</strong> — the <em>specification document</em> (also: requirements specification, SRS). It is a deliverable you can hold, not an activity.</li>
<li><strong>The hardest skill in the phase</strong> — refusing to design. "The system shall record the return date of every loan" is analysis. "The system shall store return_date as a DATE column in table loans" is design and does not belong here. Students mix these constantly, and so do professionals.</li>
<li><strong>Where requirements come from</strong> — interviews, watching people work, existing paper forms, the rules in a regulation, complaints about the old system. For LibTrack: sit at the loan desk for a morning and you will learn more than from any meeting.</li>
<li><strong>Two roads, and the fork is decided by something outside this phase</strong> — slide 10 says it plainly: which analysis you do depends on whether <em>implementation</em> will use a procedural or an object-oriented language. That is a strange dependency (a late phase deciding an early one), and it is worth noticing.</li>
<li><strong>The three slides ahead</strong> — 10 defines the phase and the fork; 11 is the procedure-oriented road (DFD, state diagram); 12 is the object-oriented road (use case diagram, class diagram).</li>
</ul>
<p class="meo">💡 The test for "is this sentence analysis or design?" — <em>could a librarian who has never seen a computer disagree with it?</em> If yes, it is analysis (it is about their world). If they would just shrug, it is design (it is about your world).</p>`,
        `<p class="y-chinh">🎯 Slide phân mục cho 8.2. Từ đây, một câu hỏi cai quản mọi thứ: <strong>hệ thống PHẢI LÀM GÌ?</strong> — và kỷ luật của pha này là trả lời câu ấy mà không một lần nói tới <em>làm bằng cách nào</em>.</p>
<ul>
<li><strong>Đầu ra của pha có tên riêng</strong> — <em>tài liệu đặc tả</em> (còn gọi: đặc tả yêu cầu, SRS). Nó là một sản phẩm bàn giao cầm được, không phải một hoạt động.</li>
<li><strong>Kỹ năng khó nhất của pha</strong> — TỪ CHỐI thiết kế. "Hệ thống phải ghi lại ngày trả của mỗi lượt mượn" là phân tích. "Hệ thống phải lưu return_date kiểu DATE trong bảng loans" là thiết kế và không thuộc về đây. Sinh viên trộn hai thứ này liên tục, mà dân chuyên nghiệp cũng vậy.</li>
<li><strong>Yêu cầu từ đâu mà có</strong> — phỏng vấn, ngồi xem người ta làm việc, các biểu mẫu giấy đang dùng, các điều khoản trong quy chế, những lời than phiền về hệ cũ. Với LibTrack: ngồi ở quầy mượn một buổi sáng, bạn học được nhiều hơn mọi cuộc họp.</li>
<li><strong>Hai con đường, và ngã rẽ lại do một thứ NGOÀI pha này quyết định</strong> — slide 10 nói thẳng: làm loại phân tích nào phụ thuộc vào chuyện pha <em>hiện thực</em> sẽ dùng ngôn ngữ thủ tục hay hướng đối tượng. Đó là một sự phụ thuộc kỳ lạ (pha muộn quyết định pha sớm), và đáng để ý.</li>
<li><strong>Ba slide sắp tới</strong> — 10 định nghĩa pha và cái ngã rẽ; 11 là con đường hướng thủ tục (DFD, sơ đồ trạng thái); 12 là con đường hướng đối tượng (sơ đồ use case, sơ đồ lớp).</li>
</ul>
<p class="meo">💡 Phép thử "câu này là phân tích hay thiết kế?" — <em>một cô thủ thư chưa từng đụng máy tính có thể PHẢN ĐỐI câu ấy không?</em> Nếu có, đó là phân tích (nó nói về thế giới của họ). Nếu họ chỉ nhún vai, đó là thiết kế (nó nói về thế giới của bạn).</p>`],

      [10, '1. Introduction (analysis phase)',
        `<p class="y-chinh">🎯 Two sentences that carry the whole phase: the analysis phase <strong>results in a specification document that shows WHAT the software will do without specifying HOW it will be done</strong>; and it takes one of two approaches depending on the implementation language.</p>
<ul>
<li><strong>The slide typesets "what" and "how" in red italics on purpose</strong> — that contrast is the single most examinable sentence in section 8.2. Learn it verbatim.</li>
<li><strong>Read the figure</strong> — Figure 8.4 shows an "Analysis" box feeding an "Analysis Model" oval, which fans out to three boxes: <em>Requirements (Customers &amp; Stakeholders)</em>, <em>Requirements Validation</em>, <em>Design Foundation</em>. The third box is the point: the analysis model is what the design phase stands on.</li>
<li><strong>"Requirements Validation" deserves a second look</strong> — validating means checking the requirements are <em>the right ones</em> and are consistent, complete and testable, before anybody builds. "The system should be fast" fails that test; "a search must return results in under 2 seconds for a catalogue of 100,000 titles" passes it.</li>
<li><strong>Functional versus non-functional</strong> — a distinction the slide does not make but every later software course does. <em>Functional</em>: what it does ("calculate the fine"). <em>Non-functional</em>: how well ("handle 50 simultaneous users", "run on the library's 2018 PCs", "be usable by staff after 30 minutes of training").</li>
<li><strong>LibTrack's specification, five real lines</strong> — (1) a student may hold at most 5 books at a time, staff at most 20; (2) the loan period is 14 days for students, 30 for staff; (3) an overdue book is fined 2,000 VND per day, capped at 200,000 VND; (4) a loan may be renewed at most twice, and not at all if another reader has reserved the title; (5) a book not returned within 90 days of its due date is recorded as lost and charged at its replacement value. Notice: not one word about screens, tables or languages. That is a specification.</li>
</ul>
<p class="pitfall">⚠️ The exam trap here is the fork itself. The slide says the analysis <em>approach</em> is chosen by the implementation <em>language</em>. So "we will write it in C" forces procedure-oriented analysis (DFD, state diagram), and "we will write it in Java" forces object-oriented analysis (use case, class diagram). A question like "a team decided on Java; which analysis tools will they use?" is asking you to run that arrow.</p>`,
        `<p class="y-chinh">🎯 Hai câu gánh cả pha này: pha phân tích <strong>cho ra một tài liệu đặc tả trình bày hệ thống sẽ LÀM GÌ mà không nói nó sẽ được LÀM NHƯ THẾ NÀO</strong>; và nó chọn một trong hai cách tiếp cận tuỳ theo ngôn ngữ hiện thực.</p>
<ul>
<li><strong>Slide cố ý in chữ "what" và "how" màu đỏ nghiêng</strong> — sự tương phản ấy là câu dễ ra đề nhất trong mục 8.2. Học thuộc nguyên văn.</li>
<li><strong>Đọc bức hình</strong> — Hình 8.4 vẽ một hộp "Analysis" đổ vào hình bầu dục "Analysis Model", rồi toả ra ba hộp: <em>Requirements (Customers &amp; Stakeholders)</em>, <em>Requirements Validation</em>, <em>Design Foundation</em>. Hộp thứ ba mới là điểm mấu chốt: mô hình phân tích chính là nền mà pha thiết kế đứng lên.</li>
<li><strong>"Requirements Validation" đáng nhìn lần thứ hai</strong> — thẩm định nghĩa là kiểm xem các yêu cầu có phải là <em>yêu cầu ĐÚNG</em> không, và có nhất quán, đầy đủ, kiểm thử được không, TRƯỚC khi ai đó bắt tay dựng. "Hệ thống phải nhanh" trượt phép thử ấy; "một lượt tìm phải trả kết quả dưới 2 giây trên kho 100.000 đầu sách" thì đạt.</li>
<li><strong>Yêu cầu chức năng và phi chức năng</strong> — một phân biệt slide không nêu nhưng mọi môn phần mềm sau này đều dùng. <em>Chức năng</em>: nó làm gì ("tính phí trễ"). <em>Phi chức năng</em>: làm tốt cỡ nào ("chịu được 50 người dùng cùng lúc", "chạy được trên máy đời 2018 của thư viện", "nhân viên dùng được sau 30 phút hướng dẫn").</li>
<li><strong>Bản đặc tả của LibTrack, năm dòng thật</strong> — (1) sinh viên giữ tối đa 5 cuốn cùng lúc, nhân viên tối đa 20; (2) hạn mượn 14 ngày với sinh viên, 30 ngày với nhân viên; (3) sách quá hạn bị phạt 2.000 đ mỗi ngày, trần 200.000 đ; (4) một lượt mượn được gia hạn tối đa hai lần, và không được gia hạn nếu có bạn đọc khác đã đặt trước đầu sách đó; (5) sách không trả trong vòng 90 ngày kể từ hạn bị ghi nhận là MẤT và tính tiền theo giá thay thế. Để ý: không một chữ nào về màn hình, bảng dữ liệu hay ngôn ngữ. ĐÓ mới là một bản đặc tả.</li>
</ul>
<p class="pitfall">⚠️ Bẫy thi ở đây chính là cái ngã rẽ. Slide nói <em>cách tiếp cận</em> phân tích được chọn bởi <em>ngôn ngữ</em> hiện thực. Vậy nên "chúng tôi sẽ viết bằng C" ép bạn phân tích hướng thủ tục (DFD, sơ đồ trạng thái), còn "chúng tôi sẽ viết bằng Java" ép bạn phân tích hướng đối tượng (use case, sơ đồ lớp). Một câu hỏi kiểu "nhóm đã chốt dùng Java; họ sẽ dùng công cụ phân tích nào?" chính là bảo bạn chạy theo mũi tên ấy.</p>`],

      [11, '2. Procedure-oriented analysis',
        `<p class="y-chinh">🎯 The procedural road, also called <strong>structured analysis</strong> or <strong>classical analysis</strong> — used when the implementation will be in a procedural language such as C. The slide shows two of its modelling tools side by side: a <strong>data flow diagram</strong> and a <strong>state diagram</strong>.</p>
<ul>
<li><strong>Why "procedure-oriented" analysis exists at all</strong> — because the analysis model has to hand the design phase something it can turn into <em>procedures</em>. If your program will be a set of functions calling each other, then modelling the system as <em>data moving between processes</em> is the natural fit.</li>
<li><strong>Read Figure 8.5 (the DFD) element by element</strong> — the legend defines three shapes plus one arrow: a pink rectangle = <em>source or destination</em> (an external entity), a black rounded box = <em>process</em>, a grey open box = <em>data storage</em>, an arrow = <em>data flow</em>. The example is a hotel reservation: <code>Potential guest</code> sends <em>Request</em> to the process <code>Handle reservation</code>, which sends back a <em>Response</em>; it reads <em>Available rooms</em> from and writes <em>Reserved record</em> to the store <code>List of rooms</code>; the process <code>Generate list</code> reads <em>Reserved rooms</em> and sends a <em>Daily report</em> to the <code>Reception desk</code>.</li>
<li><strong>The rule that makes a DFD a DFD</strong> — every arrow is labelled with <em>data</em>, never with control. There is no "then", no "if", no order. A DFD says what flows where, not when. That is why you need a second diagram for behaviour.</li>
<li><strong>Read Figure 8.6 (the state diagram)</strong> — an old-style elevator, with the legend "RF: Requested floor, CF: Current floor". Three states: <code>Moving up</code>, <code>Moving down</code>, <code>Parked</code>. Transitions: from Parked, a request with <code>[RF &gt; CF]</code> goes to <em>Moving up</em>, with <code>[RF &lt; CF]</code> to <em>Moving down</em>, with <code>[RF = CF]</code> it stays; from either moving state, <em>Reached the floor</em> returns to <code>Parked</code>. The filled circle is <em>Turned on</em> (initial state) and the circled dot is <em>Turned off</em> (final state).</li>
<li><strong>Why two diagrams and not one</strong> — they answer different questions. DFD = <em>where does data go?</em> State diagram = <em>how does the system's behaviour change over time?</em> An exam question that describes "the system reacts differently depending on what happened before" is asking for a state diagram.</li>
<li><strong>LibTrack's DFD, level 0</strong> — external entities: <code>Reader</code> and <code>Librarian</code>. Processes: <code>Check membership</code>, <code>Issue loan</code>, <code>Accept return</code>, <code>Compute fine</code>. Stores: <code>Catalogue</code>, <code>Loans</code>, <code>Readers</code>. Flows: Reader → <em>book barcode + card</em> → Issue loan; Issue loan → <em>loan record</em> → Loans; Accept return → <em>days late</em> → Compute fine; Compute fine → <em>fine amount</em> → Reader.</li>
<li><strong>LibTrack's state diagram for one copy of a book</strong> — states <code>On shelf</code> → (borrowed) → <code>On loan</code> → (returned) → <code>On shelf</code>; <code>On loan</code> → (due date passed) → <code>Overdue</code> → (returned, fine paid) → <code>On shelf</code>; <code>Overdue</code> → (90 days) → <code>Lost</code>; <code>On shelf</code> → (reserved) → <code>Reserved</code>. Five states, and every rule in the specification becomes an arrow.</li>
</ul>
<p class="pitfall">⚠️ Two numbering faults on this pair of slides. First, the state diagram here is captioned <strong>Figure 8.6</strong> — and the use case diagram on the <em>next</em> slide is <strong>also</strong> captioned Figure 8.6. Two different pictures, one number. Second, this deck teaches only DFD and state diagram, but Forouzan's structured analysis also includes the <em>entity-relationship diagram (ERD)</em>; you will meet the ERD properly in Chapter 11 (Databases), and for LibTrack it would hold the entities Reader, Copy, Title, Loan, Reservation.</p>`,
        `<p class="y-chinh">🎯 Con đường thủ tục, còn gọi là <strong>phân tích có cấu trúc</strong> hay <strong>phân tích cổ điển</strong> — dùng khi pha hiện thực sẽ viết bằng ngôn ngữ thủ tục như C. Slide bày hai công cụ mô hình hoá của nó cạnh nhau: <strong>sơ đồ luồng dữ liệu (DFD)</strong> và <strong>sơ đồ trạng thái</strong>.</p>
<ul>
<li><strong>Vì sao lại có riêng "phân tích hướng thủ tục"</strong> — vì mô hình phân tích phải giao cho pha thiết kế một thứ mà nó biến thành <em>các thủ tục</em> được. Nếu chương trình của bạn sẽ là một tập hàm gọi lẫn nhau, thì mô hình hoá hệ thống thành <em>dữ liệu di chuyển giữa các tiến trình</em> là chỗ khớp tự nhiên.</li>
<li><strong>Đọc Hình 8.5 (DFD) từng thành phần một</strong> — phần chú giải định nghĩa ba hình khối cộng một mũi tên: chữ nhật hồng = <em>nguồn hoặc đích</em> (thực thể ngoài), hộp bo tròn màu đen = <em>tiến trình</em>, hộp xám hở = <em>kho dữ liệu</em>, mũi tên = <em>luồng dữ liệu</em>. Ví dụ là đặt phòng khách sạn: <code>Potential guest</code> gửi <em>Request</em> tới tiến trình <code>Handle reservation</code>, tiến trình này gửi lại <em>Response</em>; nó đọc <em>Available rooms</em> từ và ghi <em>Reserved record</em> vào kho <code>List of rooms</code>; tiến trình <code>Generate list</code> đọc <em>Reserved rooms</em> rồi gửi <em>Daily report</em> tới <code>Reception desk</code>.</li>
<li><strong>Cái luật làm cho DFD là DFD</strong> — mọi mũi tên đều được gán nhãn bằng <em>DỮ LIỆU</em>, không bao giờ bằng điều khiển. Không có "rồi thì", không có "nếu", không có thứ tự. DFD nói cái gì chảy đi đâu, chứ không nói KHI NÀO. Đó là lý do bạn cần thêm một sơ đồ thứ hai cho hành vi.</li>
<li><strong>Đọc Hình 8.6 (sơ đồ trạng thái)</strong> — một cái thang máy kiểu cũ, có chú giải "RF: tầng được yêu cầu, CF: tầng hiện tại". Ba trạng thái: <code>Moving up</code> (đang lên), <code>Moving down</code> (đang xuống), <code>Parked</code> (đang đỗ). Chuyển: từ Parked, một yêu cầu với <code>[RF &gt; CF]</code> sang <em>Moving up</em>, với <code>[RF &lt; CF]</code> sang <em>Moving down</em>, với <code>[RF = CF]</code> thì đứng yên; từ hai trạng thái đang chạy, <em>Reached the floor</em> đưa về <code>Parked</code>. Chấm tròn đặc là <em>Turned on</em> (trạng thái đầu) và chấm tròn có vòng là <em>Turned off</em> (trạng thái cuối).</li>
<li><strong>Vì sao hai sơ đồ chứ không một</strong> — chúng trả lời hai câu hỏi khác nhau. DFD = <em>dữ liệu đi đâu?</em> Sơ đồ trạng thái = <em>hành vi của hệ thống đổi thế nào theo thời gian?</em> Câu hỏi thi nào mô tả "hệ thống phản ứng khác nhau tuỳ vào chuyện gì đã xảy ra trước đó" là đang đòi một sơ đồ trạng thái.</li>
<li><strong>DFD mức 0 của LibTrack</strong> — thực thể ngoài: <code>Bạn đọc</code> và <code>Thủ thư</code>. Tiến trình: <code>Kiểm tra thẻ</code>, <code>Cho mượn</code>, <code>Nhận trả</code>, <code>Tính phí trễ</code>. Kho: <code>Danh mục</code>, <code>Lượt mượn</code>, <code>Bạn đọc</code>. Luồng: Bạn đọc → <em>mã vạch sách + thẻ</em> → Cho mượn; Cho mượn → <em>bản ghi mượn</em> → kho Lượt mượn; Nhận trả → <em>số ngày trễ</em> → Tính phí trễ; Tính phí trễ → <em>số tiền phạt</em> → Bạn đọc.</li>
<li><strong>Sơ đồ trạng thái của MỘT bản sách trong LibTrack</strong> — <code>Trên kệ</code> → (được mượn) → <code>Đang mượn</code> → (được trả) → <code>Trên kệ</code>; <code>Đang mượn</code> → (quá hạn) → <code>Quá hạn</code> → (trả và nộp phạt) → <code>Trên kệ</code>; <code>Quá hạn</code> → (90 ngày) → <code>Mất</code>; <code>Trên kệ</code> → (được đặt trước) → <code>Đã đặt</code>. Năm trạng thái, và mỗi luật trong bản đặc tả trở thành một mũi tên.</li>
</ul>
<p class="pitfall">⚠️ Hai lỗi đánh số trên cặp slide này. Thứ nhất, sơ đồ trạng thái ở đây mang caption <strong>Figure 8.6</strong> — mà sơ đồ use case ở slide <em>KẾ TIẾP</em> <strong>CŨNG</strong> mang caption Figure 8.6. Hai bức hình khác nhau, một con số. Thứ hai, bộ slide này chỉ dạy DFD và sơ đồ trạng thái, nhưng phân tích có cấu trúc của Forouzan còn có <em>sơ đồ thực thể–liên kết (ERD)</em>; bạn sẽ gặp ERD tử tế ở Chương 11 (Cơ sở dữ liệu), và với LibTrack nó sẽ chứa các thực thể Bạn đọc, Bản sách, Đầu sách, Lượt mượn, Đặt trước.</p>`],

      [12, '3. Object-oriented analysis',
        `<p class="y-chinh">🎯 The object-oriented road — used when the implementation will be in an object-oriented language. Same job as slide 11 (produce a specification), different tools: the slide shows a <strong>use case diagram</strong> and a <strong>class diagram</strong>.</p>
<ul>
<li><strong>Read Figure 8.6 (the use case diagram)</strong> — a restaurant, drawn inside a box labelled "System Boundary". Four stick-figure <em>actors</em> outside the box: <code>Waiter</code>, <code>Chef</code>, <code>Client</code>, <code>Cashier</code>. Ovals inside are <em>use cases</em>: <code>Order Food</code>, <code>Order Wine</code>, <code>Serve Food</code>, <code>Cook Food</code>, <code>Serve Wine</code>, <code>Eat Food</code>, <code>Drink Wine</code>, <code>Pay for Food</code>, <code>Pay for Wine</code>. Lines join actors to the use cases they take part in; the dashed arrows marked <code>&lt;&lt;extend&gt;&gt;</code> with guards like <em>{if wine was ordered}</em> mean "this use case is added <em>only under that condition</em>".</li>
<li><strong>The three things to notice in any use case diagram</strong> — (1) the <em>boundary</em>: what is inside the system and what is outside it; (2) the <em>actors</em>: they are roles, not people — one person can be two actors; (3) the <em>use cases</em>: each is a complete goal of an actor, phrased as a verb.</li>
<li><strong>Read Figure 8.7 (the class diagram)</strong> — four classes. <code>Order</code> has attributes <em>Date Received, isPrepaid, Number, Price</em> and operations <em>Dispatch, Close</em>; it links <code>n</code>-to-<code>1</code> with <code>Customer</code> (<em>Name, Address, Credit Rating</em>) and <code>1</code>-to-<code>n</code> with <code>Order Line</code> (<em>Product, Quantity, Price</em>). The hollow triangle from <code>Customer</code> down to <code>Corporate Customer</code> and <code>Personal Customer</code> is <strong>inheritance</strong> — those are two kinds of Customer.</li>
<li><strong>The three compartments of a UML class box</strong> — name on top, attributes in the middle, operations at the bottom. That layout alone is worth exam marks; students who draw one compartment lose them.</li>
<li><strong>The multiplicities are the specification in miniature</strong> — <code>1</code>-to-<code>n</code> between Order and Order Line says "an order has many lines, a line belongs to one order". That single annotation encodes a business rule.</li>
<li><strong>LibTrack, object-oriented</strong> — actors: <code>Reader</code>, <code>Librarian</code>, <code>Administrator</code>. Use cases: <em>Borrow a copy</em>, <em>Return a copy</em>, <em>Reserve a title</em>, <em>Renew a loan</em>, <em>Pay a fine</em>, <em>Add a title to the catalogue</em>. Classes: <code>Reader</code> (name, cardNo, type, maxLoans), <code>Title</code> (isbn, title, author), <code>Copy</code> (barcode, state), <code>Loan</code> (dateOut, dueDate, dateBack, renewals), <code>Fine</code> (amount, paid). Relationships: Title <code>1</code>–<code>n</code> Copy; Reader <code>1</code>–<code>n</code> Loan; Copy <code>1</code>–<code>n</code> Loan. And <code>Student</code> and <code>Staff</code> inherit from <code>Reader</code>, differing only in maxLoans and loan period — exactly the hollow triangle in Figure 8.7.</li>
</ul>
<table>
<tr><th></th><th>Procedure-oriented analysis</th><th>Object-oriented analysis</th></tr>
<tr><td>Triggered by</td><td>Implementation in C, Pascal, COBOL…</td><td>Implementation in Java, C++, C#…</td></tr>
<tr><td>Other names</td><td>Structured / classical analysis</td><td>OOA</td></tr>
<tr><td>Unit of thought</td><td>A process that transforms data</td><td>An object that owns data and behaviour</td></tr>
<tr><td>Tools on the slides</td><td>Data flow diagram, state diagram</td><td>Use case diagram, class diagram</td></tr>
<tr><td>Question it answers best</td><td>Where does the data go?</td><td>Who does what, and with which things?</td></tr>
<tr><td>Leads into</td><td>Structure chart (slide 15)</td><td>Detailed classes (slide 16)</td></tr>
</table>
<p class="pitfall">⚠️ Two flaws on this slide. The bullet text is broken across two bullets in the middle of a sentence ("…the implementation uses an" / "object-oriented language. The specification…") — a line-wrap accident, not two ideas. And the use case diagram is captioned <strong>Figure 8.6</strong>, the same number the state diagram carried on slide 11.</p>`,
        `<p class="y-chinh">🎯 Con đường hướng đối tượng — dùng khi pha hiện thực sẽ viết bằng ngôn ngữ hướng đối tượng. Cùng nhiệm vụ với slide 11 (đẻ ra bản đặc tả), khác công cụ: slide bày một <strong>sơ đồ use case</strong> và một <strong>sơ đồ lớp</strong>.</p>
<ul>
<li><strong>Đọc Hình 8.6 (sơ đồ use case)</strong> — một nhà hàng, vẽ trong khung có nhãn "System Boundary" (biên hệ thống). Bốn <em>tác nhân</em> hình que đứng NGOÀI khung: <code>Waiter</code> (phục vụ), <code>Chef</code> (đầu bếp), <code>Client</code> (khách), <code>Cashier</code> (thu ngân). Các hình bầu dục bên trong là <em>use case</em>: <code>Order Food</code>, <code>Order Wine</code>, <code>Serve Food</code>, <code>Cook Food</code>, <code>Serve Wine</code>, <code>Eat Food</code>, <code>Drink Wine</code>, <code>Pay for Food</code>, <code>Pay for Wine</code>. Các đường nối tác nhân với use case mà họ tham gia; mũi tên nét đứt ghi <code>&lt;&lt;extend&gt;&gt;</code> kèm điều kiện kiểu <em>{if wine was ordered}</em> nghĩa là "use case này chỉ được thêm vào <em>KHI</em> điều kiện đó đúng".</li>
<li><strong>Ba thứ phải để ý ở mọi sơ đồ use case</strong> — (1) <em>biên</em>: cái gì trong hệ thống, cái gì ngoài; (2) <em>tác nhân</em>: chúng là VAI, không phải người — một người có thể là hai tác nhân; (3) <em>use case</em>: mỗi cái là một MỤC TIÊU trọn vẹn của tác nhân, diễn đạt bằng động từ.</li>
<li><strong>Đọc Hình 8.7 (sơ đồ lớp)</strong> — bốn lớp. <code>Order</code> có thuộc tính <em>Date Received, isPrepaid, Number, Price</em> và phương thức <em>Dispatch, Close</em>; nó nối <code>n</code>–<code>1</code> với <code>Customer</code> (<em>Name, Address, Credit Rating</em>) và <code>1</code>–<code>n</code> với <code>Order Line</code> (<em>Product, Quantity, Price</em>). Cái tam giác rỗng đi từ <code>Customer</code> xuống <code>Corporate Customer</code> và <code>Personal Customer</code> chính là <strong>kế thừa</strong> — đó là hai loại Customer.</li>
<li><strong>Ba ngăn của một hộp lớp UML</strong> — tên ở trên, thuộc tính ở giữa, phương thức ở dưới. Riêng cái bố cục ấy đã có điểm thi; sinh viên vẽ một ngăn là mất.</li>
<li><strong>Các con số bội (multiplicity) chính là bản đặc tả thu nhỏ</strong> — <code>1</code>–<code>n</code> giữa Order và Order Line nói "một đơn hàng có nhiều dòng, một dòng thuộc đúng một đơn". Chỉ một ghi chú ấy đã mã hoá một luật nghiệp vụ.</li>
<li><strong>LibTrack, nhìn theo hướng đối tượng</strong> — tác nhân: <code>Bạn đọc</code>, <code>Thủ thư</code>, <code>Quản trị</code>. Use case: <em>Mượn một bản</em>, <em>Trả một bản</em>, <em>Đặt trước một đầu sách</em>, <em>Gia hạn</em>, <em>Nộp phí trễ</em>, <em>Thêm đầu sách vào danh mục</em>. Lớp: <code>Reader</code> (name, cardNo, type, maxLoans), <code>Title</code> (isbn, title, author), <code>Copy</code> (barcode, state), <code>Loan</code> (dateOut, dueDate, dateBack, renewals), <code>Fine</code> (amount, paid). Liên kết: Title <code>1</code>–<code>n</code> Copy; Reader <code>1</code>–<code>n</code> Loan; Copy <code>1</code>–<code>n</code> Loan. Và <code>Student</code> với <code>Staff</code> kế thừa từ <code>Reader</code>, chỉ khác nhau ở maxLoans và hạn mượn — đúng cái tam giác rỗng trong Hình 8.7.</li>
</ul>
<table>
<tr><th></th><th>Phân tích hướng thủ tục</th><th>Phân tích hướng đối tượng</th></tr>
<tr><td>Bị kích hoạt bởi</td><td>Hiện thực bằng C, Pascal, COBOL…</td><td>Hiện thực bằng Java, C++, C#…</td></tr>
<tr><td>Tên gọi khác</td><td>Phân tích có cấu trúc / cổ điển</td><td>OOA</td></tr>
<tr><td>Đơn vị tư duy</td><td>Một tiến trình biến đổi dữ liệu</td><td>Một đối tượng sở hữu cả dữ liệu lẫn hành vi</td></tr>
<tr><td>Công cụ trên slide</td><td>Sơ đồ luồng dữ liệu, sơ đồ trạng thái</td><td>Sơ đồ use case, sơ đồ lớp</td></tr>
<tr><td>Trả lời tốt nhất câu hỏi</td><td>Dữ liệu chảy đi đâu?</td><td>Ai làm gì, với những thứ nào?</td></tr>
<tr><td>Dẫn tiếp sang</td><td>Sơ đồ cấu trúc (slide 15)</td><td>Lớp chi tiết (slide 16)</td></tr>
</table>
<p class="pitfall">⚠️ Hai lỗi trên slide này. Chữ ở gạch đầu dòng bị cắt làm đôi ngay giữa câu ("…the implementation uses an" / "object-oriented language. The specification…") — tai nạn xuống dòng, không phải hai ý. Và sơ đồ use case mang caption <strong>Figure 8.6</strong>, đúng con số mà sơ đồ trạng thái đã mang ở slide 11.</p>`],

      [13, '3 - Design phase',
        `<p class="y-chinh">🎯 Section divider for 8.3. The question changes from <em>what?</em> to <strong>HOW?</strong> — and everything you were forbidden to say in the analysis phase is now exactly what you must say.</p>
<ul>
<li><strong>The handover is the point</strong> — design takes the specification document as its <em>input</em> and produces a design document as its <em>output</em>. If the specification is vague, the design phase inherits the vagueness and quietly invents the missing rules. That is how systems end up doing something nobody asked for.</li>
<li><strong>Design is where the parallel structure repeats</strong> — procedure-oriented design (slide 15) and object-oriented design (slide 16), matching the two roads of the analysis phase exactly. Same fork, one level lower.</li>
<li><strong>Two levels every course after this one will distinguish</strong> — <em>architectural (high-level) design</em>: which modules exist and how they talk; <em>detailed (low-level) design</em>: what happens inside each module. Forouzan's structure chart is architectural; his class details are detailed design.</li>
<li><strong>For LibTrack</strong> — analysis said "an overdue book is fined 2,000 VND per day, capped at 200,000". Design says: a module <code>fines</code> exposing <code>int phi_tre(int so_ngay, int uu_tien)</code>, called by <code>accept_return()</code>, with the rate and cap held in a configuration table so a change of policy does not require recompiling. Notice the design added something analysis never mentioned — <em>where the numbers live</em> — and that is legitimate: it is a "how".</li>
<li><strong>What the next three slides do</strong> — 14 defines the phase and lists its activities; 15 is the procedural road; 16 is the object-oriented road.</li>
</ul>
<p class="meo">💡 One sentence to separate the two phases forever: <strong>analysis describes the problem in the customer's vocabulary; design describes the solution in the programmer's vocabulary.</strong> If a document uses the word "table", "class", "function" or "screen", it is design.</p>`,
        `<p class="y-chinh">🎯 Slide phân mục cho 8.3. Câu hỏi đổi từ <em>cái gì?</em> sang <strong>BẰNG CÁCH NÀO?</strong> — và mọi thứ bạn bị CẤM nói ở pha phân tích thì giờ đúng là thứ bạn PHẢI nói.</p>
<ul>
<li><strong>Chỗ bàn giao mới là điểm mấu chốt</strong> — thiết kế lấy tài liệu đặc tả làm <em>đầu vào</em> và đẻ ra tài liệu thiết kế làm <em>đầu ra</em>. Nếu bản đặc tả mơ hồ thì pha thiết kế thừa kế luôn sự mơ hồ ấy rồi lặng lẽ TỰ BỊA ra những luật còn thiếu. Đó là cách các hệ thống rốt cuộc làm những việc chẳng ai yêu cầu.</li>
<li><strong>Thiết kế là chỗ cấu trúc song song lặp lại</strong> — thiết kế hướng thủ tục (slide 15) và thiết kế hướng đối tượng (slide 16), khớp chính xác với hai con đường của pha phân tích. Cùng một ngã rẽ, thấp hơn một tầng.</li>
<li><strong>Hai mức mà mọi môn sau này sẽ phân biệt</strong> — <em>thiết kế kiến trúc (mức cao)</em>: có những module nào và chúng nói chuyện ra sao; <em>thiết kế chi tiết (mức thấp)</em>: bên trong mỗi module xảy ra gì. Sơ đồ cấu trúc của Forouzan thuộc mức kiến trúc; phần chi tiết lớp của ông thuộc thiết kế chi tiết.</li>
<li><strong>Với LibTrack</strong> — phân tích nói "sách quá hạn bị phạt 2.000 đ mỗi ngày, trần 200.000 đ". Thiết kế nói: một module <code>fines</code> phơi ra hàm <code>int phi_tre(int so_ngay, int uu_tien)</code>, được <code>accept_return()</code> gọi, với mức phí và trần nằm trong một bảng cấu hình để đổi chính sách không phải biên dịch lại. Chú ý thiết kế đã thêm một thứ mà phân tích chưa hề nói — <em>mấy con số ấy sống ở đâu</em> — và đó là hợp lệ: nó là một "bằng cách nào".</li>
<li><strong>Ba slide kế tiếp làm gì</strong> — 14 định nghĩa pha và liệt kê các hoạt động; 15 là con đường thủ tục; 16 là con đường hướng đối tượng.</li>
</ul>
<p class="meo">💡 Một câu để tách hai pha này vĩnh viễn: <strong>phân tích mô tả BÀI TOÁN bằng từ vựng của khách hàng; thiết kế mô tả LỜI GIẢI bằng từ vựng của lập trình viên.</strong> Tài liệu nào dùng chữ "bảng", "lớp", "hàm" hay "màn hình" thì đó là thiết kế.</p>`],

      [14, '1. Introduction (design phase)',
        `<p class="y-chinh">🎯 One sentence, and it is the definition to memorise: the design phase <strong>defines HOW the system will accomplish what was defined in the analysis phase</strong>, and in it <strong>all components of the system are defined</strong>.</p>
<ul>
<li><strong>Read Figure 8.8 carefully — it is a checklist, not decoration</strong> — the banner reads "SDLC Phases with Design Phase Activities" and shows five ribbons (Project planning → Analysis → <strong>Design</strong> → Implementation → Support). The Design ribbon lists seven activities: <em>design and integrate the network · design the application architecture · design the user interfaces · design the system interfaces · design and integrate the database · prototype for design details · design and integrate the system controls</em>.</li>
<li><strong>Those seven are the real answer to "what happens in the design phase?"</strong> — most students answer "you draw diagrams". The figure says: network, architecture, UI, interfaces to other systems, database, prototypes, and controls (security, audit, validation). Learn the seven; they are more marks than the one-sentence definition.</li>
<li><strong>"System interfaces" is the one people forget</strong> — it means the connections to <em>other software</em>, not to users. For LibTrack: reading the university's student database to check a card is valid, and sending email through the campus mail server.</li>
<li><strong>"System controls" is the other one</strong> — who is allowed to waive a fine? Is that action logged? What stops a librarian marking a book returned that never came back? Controls are design decisions, and they are where real systems are attacked.</li>
<li><strong>LibTrack's design phase, activity by activity</strong> — <em>network</em>: three desk PCs and a server on the library LAN; <em>architecture</em>: three layers (UI, service, data); <em>user interfaces</em>: a borrow screen driven entirely by a barcode scanner so staff never touch the keyboard; <em>system interfaces</em>: a read-only query into the student database; <em>database</em>: the five tables from the class diagram; <em>prototype</em>: a paper mock-up of the borrow screen shown to librarians before coding; <em>controls</em>: only a senior librarian may waive a fine, and every waiver is logged with a reason.</li>
<li><strong>Notice the ribbon before Design is "Analysis" and the one after is "Implementation"</strong> — the figure is quietly showing you the waterfall again. Design cannot start before the specification exists, and coding should not start before the design does.</li>
</ul>
<p class="pitfall">⚠️ A common exam error is answering a "which phase?" question with "design" whenever the word <em>database</em> appears. Deciding that the system must remember who borrowed what is <strong>analysis</strong>; deciding there will be a table <code>loans</code> with a foreign key to <code>readers</code> is <strong>design</strong>. The figure's phrase "design and integrate the database" refers to the second one.</p>`,
        `<p class="y-chinh">🎯 Một câu, và đó là định nghĩa phải thuộc: pha thiết kế <strong>xác định hệ thống sẽ hoàn thành BẰNG CÁCH NÀO cái đã được định ra ở pha phân tích</strong>, và trong pha này <strong>mọi thành phần của hệ thống được định nghĩa</strong>.</p>
<ul>
<li><strong>Đọc kỹ Hình 8.8 — nó là một bảng kiểm, không phải đồ trang trí</strong> — dòng tiêu đề ghi "SDLC Phases with Design Phase Activities" và vẽ năm dải băng (Project planning → Analysis → <strong>Design</strong> → Implementation → Support). Dải Design liệt kê bảy hoạt động: <em>thiết kế và tích hợp mạng · thiết kế kiến trúc ứng dụng · thiết kế giao diện người dùng · thiết kế giao diện hệ thống · thiết kế và tích hợp cơ sở dữ liệu · làm bản mẫu cho các chi tiết thiết kế · thiết kế và tích hợp các chốt kiểm soát hệ thống</em>.</li>
<li><strong>Bảy cái đó mới là câu trả lời thật cho "pha thiết kế làm những gì?"</strong> — phần lớn sinh viên trả lời "thì vẽ sơ đồ". Bức hình nói: mạng, kiến trúc, giao diện người dùng, giao diện với hệ khác, cơ sở dữ liệu, bản mẫu, và các chốt kiểm soát (bảo mật, ghi vết, kiểm tra hợp lệ). Học bảy cái ấy; chúng đáng điểm hơn câu định nghĩa một dòng.</li>
<li><strong>"System interfaces" là cái người ta hay quên</strong> — nó nghĩa là các mối nối sang <em>PHẦN MỀM KHÁC</em>, không phải sang người dùng. Với LibTrack: đọc cơ sở dữ liệu sinh viên của trường để kiểm thẻ còn hiệu lực, và gửi email qua máy chủ thư của trường.</li>
<li><strong>"System controls" là cái thứ hai hay quên</strong> — ai được quyền xoá phí phạt? Hành động đó có được ghi vết không? Cái gì ngăn một thủ thư đánh dấu đã trả cho cuốn sách chưa từng quay về? Kiểm soát là quyết định THIẾT KẾ, và chúng là chỗ các hệ thống thật bị tấn công.</li>
<li><strong>Pha thiết kế của LibTrack, theo từng hoạt động</strong> — <em>mạng</em>: ba máy ở quầy và một máy chủ trong mạng LAN thư viện; <em>kiến trúc</em>: ba tầng (giao diện, dịch vụ, dữ liệu); <em>giao diện người dùng</em>: màn hình cho mượn điều khiển hoàn toàn bằng máy quét mã vạch để nhân viên không phải đụng bàn phím; <em>giao diện hệ thống</em>: một truy vấn chỉ-đọc vào cơ sở dữ liệu sinh viên; <em>cơ sở dữ liệu</em>: năm bảng lấy từ sơ đồ lớp; <em>bản mẫu</em>: một bản vẽ tay màn hình cho mượn đưa thủ thư xem trước khi viết mã; <em>kiểm soát</em>: chỉ thủ thư trưởng được xoá phí phạt, và mọi lần xoá đều ghi vết kèm lý do.</li>
<li><strong>Để ý dải trước Design là "Analysis" và dải sau là "Implementation"</strong> — bức hình đang lặng lẽ vẽ lại mô hình thác nước cho bạn. Thiết kế không thể bắt đầu khi chưa có đặc tả, và viết mã không nên bắt đầu khi chưa có thiết kế.</li>
</ul>
<p class="pitfall">⚠️ Lỗi thi hay gặp là cứ thấy chữ <em>cơ sở dữ liệu</em> là trả lời "pha thiết kế". Quyết định rằng hệ thống phải nhớ ai mượn cuốn nào là <strong>PHÂN TÍCH</strong>; quyết định rằng sẽ có bảng <code>loans</code> với khoá ngoại trỏ tới <code>readers</code> là <strong>THIẾT KẾ</strong>. Cụm "design and integrate the database" trong hình nói về cái thứ hai.</p>`],

      [15, '2. Procedure-oriented design',
        `<p class="y-chinh">🎯 In procedure-oriented design there are <strong>both procedures and data to design</strong>, and the whole system is <strong>divided into a set of procedures or modules</strong>. Figure 8.9 shows the tool for that: a <strong>structure chart</strong>.</p>
<ul>
<li><strong>Read Figure 8.9 exactly</strong> — the top box is <code>PRODUCE PAYROLL</code>. Beneath it, five boxes: <code>GET VALID TIMESHEET</code>, <code>CALCULATE GROSS-PAY</code>, <code>CALCULATE DEDUCTIONS</code>, <code>CREATE PAY CHEQUE</code>, <code>CREATE PAY SLIP</code>. The small circle-and-arrow symbols on the connecting lines are <em>data couples</em> — the parameters passed up and down: <em>Valid Time</em>, <em>Emp ID</em>, <em>Grosspay</em>, <em>Deduction</em>, <em>NetPay</em>, <em>Payslip Details</em>. An arrow pointing up means data returned to the caller; pointing down, data passed to the callee.</li>
<li><strong>A structure chart is not a flowchart</strong> — this is the mistake that costs marks. A flowchart shows the <em>order</em> of steps and has decisions and loops. A structure chart shows <em>which module calls which</em> and <em>what data crosses the call</em>. No time, no order, no diamonds.</li>
<li><strong>The two quality words this slide is silently teaching</strong> — you already met them in PRF192: <strong>cohesion</strong> (one module should do one thing; <code>CALCULATE GROSS-PAY</code> is high cohesion, a module called <code>DO STUFF</code> is not) and <strong>coupling</strong> (modules should exchange as little as possible; the few labelled arrows here are the whole conversation between these modules). <em>High cohesion, low coupling</em> is the design rule the chart exists to check.</li>
<li><strong>How you get from a DFD to a structure chart</strong> — the processes in the DFD become modules, the data flows become the parameters on the lines, and you add a top-level module to call them in order. That is why the analysis road and the design road had to match.</li>
<li><strong>LibTrack's structure chart</strong> — top: <code>CHO_MUON_SACH</code>. Children: <code>DOC_MA_VACH</code> (returns <em>barcode</em>), <code>KIEM_TRA_THE</code> (takes <em>cardNo</em>, returns <em>reader type, isValid</em>), <code>DEM_SO_SACH_DANG_MUON</code> (takes <em>cardNo</em>, returns <em>count</em>), <code>TAO_LUOT_MUON</code> (takes <em>barcode, cardNo, dueDate</em>), <code>IN_PHIEU</code> (takes <em>loan details</em>). Five modules, each doing one thing, each with two or three parameters. If any box needed eight parameters, the design would be telling you the split is wrong.</li>
</ul>
<p class="meo">💡 A practical test for a structure chart: read any box's name out loud as a sentence — "<em>calculate gross pay</em>". If it needs the word "and" to describe what it does, it is doing two things and should be two boxes. That is cohesion, checked in three seconds.</p>`,
        `<p class="y-chinh">🎯 Trong thiết kế hướng thủ tục ta có <strong>cả thủ tục lẫn dữ liệu để thiết kế</strong>, và toàn hệ thống được <strong>chia thành một tập thủ tục hay module</strong>. Hình 8.9 bày công cụ cho việc đó: một <strong>sơ đồ cấu trúc (structure chart)</strong>.</p>
<ul>
<li><strong>Đọc Hình 8.9 cho chính xác</strong> — hộp trên cùng là <code>PRODUCE PAYROLL</code> (lập bảng lương). Dưới nó là năm hộp: <code>GET VALID TIMESHEET</code>, <code>CALCULATE GROSS-PAY</code>, <code>CALCULATE DEDUCTIONS</code>, <code>CREATE PAY CHEQUE</code>, <code>CREATE PAY SLIP</code>. Mấy ký hiệu vòng tròn kèm mũi tên trên các đường nối là <em>cặp dữ liệu</em> (data couple) — tham số truyền lên và truyền xuống: <em>Valid Time</em>, <em>Emp ID</em>, <em>Grosspay</em>, <em>Deduction</em>, <em>NetPay</em>, <em>Payslip Details</em>. Mũi tên chỉ lên nghĩa là dữ liệu trả về cho hàm gọi; chỉ xuống là dữ liệu truyền cho hàm được gọi.</li>
<li><strong>Sơ đồ cấu trúc KHÔNG phải lưu đồ (flowchart)</strong> — đây là chỗ nhầm làm mất điểm. Lưu đồ vẽ <em>THỨ TỰ</em> các bước, có rẽ nhánh và vòng lặp. Sơ đồ cấu trúc vẽ <em>module nào GỌI module nào</em> và <em>dữ liệu gì đi qua lời gọi</em>. Không thời gian, không thứ tự, không hình thoi.</li>
<li><strong>Hai chữ chất lượng mà slide này đang lặng lẽ dạy</strong> — bạn đã gặp ở PRF192: <strong>cohesion</strong> (tính kết dính — một module chỉ nên làm MỘT việc; <code>CALCULATE GROSS-PAY</code> là kết dính cao, một module tên <code>DO STUFF</code> thì không) và <strong>coupling</strong> (tính ghép nối — các module nên trao đổi càng ít càng tốt; mấy mũi tên có nhãn ở đây là TOÀN BỘ cuộc trò chuyện giữa chúng). <em>Kết dính cao, ghép nối thấp</em> là luật thiết kế mà cái sơ đồ này sinh ra để kiểm.</li>
<li><strong>Đi từ DFD sang sơ đồ cấu trúc như thế nào</strong> — các tiến trình trong DFD trở thành module, các luồng dữ liệu trở thành tham số trên các đường nối, và bạn thêm một module cấp cao để gọi chúng theo thứ tự. Đó chính là lý do con đường phân tích và con đường thiết kế buộc phải khớp nhau.</li>
<li><strong>Sơ đồ cấu trúc của LibTrack</strong> — trên cùng: <code>CHO_MUON_SACH</code>. Các con: <code>DOC_MA_VACH</code> (trả về <em>barcode</em>), <code>KIEM_TRA_THE</code> (nhận <em>cardNo</em>, trả về <em>loại bạn đọc, hợp lệ?</em>), <code>DEM_SO_SACH_DANG_MUON</code> (nhận <em>cardNo</em>, trả về <em>số lượng</em>), <code>TAO_LUOT_MUON</code> (nhận <em>barcode, cardNo, dueDate</em>), <code>IN_PHIEU</code> (nhận <em>thông tin lượt mượn</em>). Năm module, mỗi cái làm một việc, mỗi cái hai ba tham số. Nếu có hộp nào cần tới tám tham số thì chính bản thiết kế đang báo cho bạn biết là chia sai chỗ.</li>
</ul>
<p class="meo">💡 Một phép thử thực dụng cho sơ đồ cấu trúc: đọc to tên của bất kỳ hộp nào thành một câu — "<em>tính lương gộp</em>". Nếu phải dùng chữ "và" mới mô tả nổi việc nó làm thì nó đang làm hai việc và phải tách thành hai hộp. Đó là kiểm cohesion, mất ba giây.</p>`],

      [16, '3. Object-oriented design',
        `<p class="y-chinh">🎯 On the object-oriented road, the design phase <strong>continues by elaborating the details of classes</strong>. A class is a set of variables (<em>attributes</em>) plus a set of methods (<em>functions</em>), and design is where those get their types, their parameters and their visibility.</p>
<ul>
<li><strong>The key word is "continues"</strong> — analysis already produced a class diagram (slide 12). Design does not start a new picture; it <em>fills in</em> the same one. That is the cleanest difference between OO analysis and OO design: analysis names the classes, design specifies them.</li>
<li><strong>Read Figure 8.10</strong> — a Book Trading system with three classes. <code>Book Catalogue</code>: <em>+include(), +remove(), +update()</em>. <code>BookSellerService</code>: <em>+registerBook(), +removeFromCatalogue(infor), +search(), +run(), +wait()</em>. <code>BookBuyerService</code>: <em>+lookForBook(), +chooseTheBestOffer(), +informBestOffer(), +buyBook(), +informSellingCode(), +run(), +wait()</em>. Arrows point from BookBuyerService → BookSellerService → Book Catalogue. A dashed line pulls out the body of one method as real Java code (<code>public void search(String title){ long start = getTime(); … }</code>), and the legend explains the shading: hatched = "method with some monitoring code to track threads status", grey = "method with some monitoring to track operations Performance".</li>
<li><strong>What the <code>+</code> signs mean</strong> — UML visibility: <code>+</code> public, <code>-</code> private, <code>#</code> protected. Deciding which attribute is private is a design decision, and it is the practical face of <em>encapsulation</em>.</li>
<li><strong>Why the figure shows a code fragment</strong> — it is making the point that object-oriented design goes all the way down to method bodies when the method is non-obvious. The boundary between "detailed design" and "implementation" is genuinely blurry here.</li>
<li><strong>LibTrack's class <code>Loan</code>, at design detail</strong> — attributes: <code>-int id</code>, <code>-String barcode</code>, <code>-String cardNo</code>, <code>-Date dateOut</code>, <code>-Date dueDate</code>, <code>-Date dateBack</code>, <code>-int renewals</code>. Methods: <code>+boolean isOverdue(Date today)</code>, <code>+int daysLate(Date today)</code>, <code>+boolean renew()</code> — returns false if <code>renewals &gt;= 2</code> or the title is reserved, <code>+int fine(Date today)</code> — calls the fine policy. Every one of those signatures is a decision that analysis did not make.</li>
<li><strong>Where the two roads end up</strong> — procedural design gives you a tree of modules with parameters; object-oriented design gives you a graph of classes with methods. Both are then handed to the same implementation phase.</li>
</ul>
<p class="pitfall">⚠️ The slide's text is inherited wholesale from Forouzan and does not match its own picture: it says "<em>Figure 10.9 shows an example of the details of our four classes used in the design of the old-style elevator</em>", but the figure on screen is a three-class Book Trading system with no elevator anywhere. If an exam question mentions the elevator classes, it means Forouzan's original figure (<code>Button</code>, <code>Door</code>, <code>Elevator</code>, <code>Controller</code>), not this one.</p>`,
        `<p class="y-chinh">🎯 Trên con đường hướng đối tượng, pha thiết kế <strong>tiếp tục bằng cách làm rõ chi tiết của các lớp</strong>. Một lớp gồm một tập biến (<em>thuộc tính</em>) cộng một tập phương thức (<em>hàm</em>), và thiết kế là chỗ chúng được gán kiểu, gán tham số và gán mức truy cập.</p>
<ul>
<li><strong>Chữ mấu chốt là "TIẾP TỤC"</strong> — pha phân tích đã đẻ ra sơ đồ lớp rồi (slide 12). Thiết kế không vẽ bức tranh mới; nó <em>ĐIỀN VÀO</em> đúng bức tranh ấy. Đó là khác biệt sạch sẽ nhất giữa phân tích và thiết kế hướng đối tượng: phân tích ĐẶT TÊN các lớp, thiết kế ĐẶC TẢ chúng.</li>
<li><strong>Đọc Hình 8.10</strong> — một hệ Book Trading với ba lớp. <code>Book Catalogue</code>: <em>+include(), +remove(), +update()</em>. <code>BookSellerService</code>: <em>+registerBook(), +removeFromCatalogue(infor), +search(), +run(), +wait()</em>. <code>BookBuyerService</code>: <em>+lookForBook(), +chooseTheBestOffer(), +informBestOffer(), +buyBook(), +informSellingCode(), +run(), +wait()</em>. Mũi tên đi từ BookBuyerService → BookSellerService → Book Catalogue. Một đường nét đứt kéo thân của một phương thức ra thành mã Java thật (<code>public void search(String title){ long start = getTime(); … }</code>), và chú giải giải thích phần tô: gạch chéo = "phương thức có mã theo dõi trạng thái luồng", tô xám = "phương thức có mã theo dõi hiệu năng thao tác".</li>
<li><strong>Dấu <code>+</code> nghĩa là gì</strong> — mức truy cập trong UML: <code>+</code> công khai, <code>-</code> riêng tư, <code>#</code> được bảo vệ. Quyết định thuộc tính nào là private là một quyết định THIẾT KẾ, và nó là bộ mặt thực dụng của <em>đóng gói (encapsulation)</em>.</li>
<li><strong>Vì sao bức hình lại chèn cả một mẩu mã</strong> — nó muốn nói rằng thiết kế hướng đối tượng đi xuống tận thân phương thức khi phương thức ấy không hiển nhiên. Ranh giới giữa "thiết kế chi tiết" và "hiện thực" ở đây thật sự nhoè.</li>
<li><strong>Lớp <code>Loan</code> của LibTrack, ở mức chi tiết thiết kế</strong> — thuộc tính: <code>-int id</code>, <code>-String barcode</code>, <code>-String cardNo</code>, <code>-Date dateOut</code>, <code>-Date dueDate</code>, <code>-Date dateBack</code>, <code>-int renewals</code>. Phương thức: <code>+boolean isOverdue(Date today)</code>, <code>+int daysLate(Date today)</code>, <code>+boolean renew()</code> — trả về false nếu <code>renewals &gt;= 2</code> hoặc đầu sách đã bị đặt trước, <code>+int fine(Date today)</code> — gọi tới chính sách phí. Từng chữ ký hàm ấy đều là một quyết định mà pha phân tích chưa hề đưa ra.</li>
<li><strong>Hai con đường rốt cuộc dẫn tới đâu</strong> — thiết kế thủ tục cho bạn một CÂY module kèm tham số; thiết kế hướng đối tượng cho bạn một ĐỒ THỊ lớp kèm phương thức. Rồi cả hai được giao cho cùng một pha hiện thực.</li>
</ul>
<p class="pitfall">⚠️ Chữ trên slide bê nguyên từ Forouzan và KHÔNG khớp với chính bức hình của nó: chữ ghi "<em>Figure 10.9 shows an example of the details of our four classes used in the design of the old-style elevator</em>" (bốn lớp dùng để thiết kế thang máy kiểu cũ), nhưng hình trên màn hình là hệ Book Trading ba lớp, chẳng có cái thang máy nào. Nếu đề thi nhắc tới mấy lớp thang máy thì nó đang nói tới hình gốc của Forouzan (<code>Button</code>, <code>Door</code>, <code>Elevator</code>, <code>Controller</code>), không phải hình này.</p>`],

      [17, '4 - Implementation phase',
        `<p class="y-chinh">🎯 Section divider for 8.4 — and the surprise of the chapter: of the seven slides in this section, only <strong>one</strong> is about writing code. The other five are about <strong>quality</strong>.</p>
<ul>
<li><strong>Why the ratio is like that</strong> — because at this level of abstraction, "write the code" really is the mechanical part. Two teams given the same design will produce working code; the difference between them shows up two years later, when one system can be changed in an afternoon and the other cannot be changed at all.</li>
<li><strong>What the section covers</strong> — slide 18: what the phase is. Slide 19: choice of language. Slide 20: the three-branch quality tree. Slides 21–23: one slide per branch (operability, maintainability, transferability).</li>
<li><strong>The quality tree is the exam target</strong> — if you memorise exactly one figure from this whole chapter, make it Figure 8.13 on slide 20. Three branches, thirteen leaves, and it answers objective 5 by itself.</li>
<li><strong>For LibTrack this is the eight-week block</strong> — five modules from the structure chart, or five classes from the class diagram, written, reviewed, and unit-tested as they go. Notice from Figure 8.11 on the next slide that testing is <em>inside</em> this phase too, not only in phase 5.</li>
<li><strong>A word this section never uses but should</strong> — <em>coding standards</em>. Real teams agree on naming, formatting, comment style and error handling before typing, precisely so the result is maintainable. It is the cheapest quality measure that exists.</li>
</ul>
<p class="meo">💡 Frame the whole section with one question: <strong>"the code works — so what could still be wrong with it?"</strong> The three answers are the three branches: it might not run well for the user (operability), it might be impossible to change (maintainability), it might be stuck on one machine (transferability).</p>`,
        `<p class="y-chinh">🎯 Slide phân mục cho 8.4 — và là bất ngờ của cả chương: trong bảy slide của mục này, chỉ <strong>MỘT</strong> slide nói về viết mã. Năm slide còn lại nói về <strong>chất lượng</strong>.</p>
<ul>
<li><strong>Vì sao tỷ lệ lại như thế</strong> — vì ở mức trừu tượng này, "viết mã" đúng là phần cơ học. Hai đội nhận cùng một bản thiết kế thì đều cho ra mã chạy được; khác biệt giữa họ lộ ra sau hai năm, khi một hệ sửa trong một buổi chiều còn hệ kia thì không sửa nổi nữa.</li>
<li><strong>Mục này phủ những gì</strong> — slide 18: pha này là gì. Slide 19: chọn ngôn ngữ. Slide 20: cây chất lượng ba nhánh. Slide 21–23: mỗi nhánh một slide (operability, maintainability, transferability).</li>
<li><strong>Cây chất lượng là đích ra đề</strong> — nếu cả chương này bạn chỉ thuộc đúng MỘT bức hình, hãy chọn Hình 8.13 ở slide 20. Ba nhánh, mười ba lá, và tự nó trả lời trọn mục tiêu số 5.</li>
<li><strong>Với LibTrack đây là khối tám tuần</strong> — năm module từ sơ đồ cấu trúc, hoặc năm lớp từ sơ đồ lớp, được viết, được duyệt mã, và được kiểm thử đơn vị ngay trong lúc viết. Để ý Hình 8.11 ở slide sau: kiểm thử nằm NGAY TRONG pha này nữa, không phải chỉ ở pha 5.</li>
<li><strong>Một chữ mà mục này không hề dùng nhưng đáng lẽ phải có</strong> — <em>chuẩn viết mã (coding standards)</em>. Đội thật thống nhất cách đặt tên, cách định dạng, kiểu chú thích và cách xử lý lỗi TRƯỚC khi gõ, chính là để kết quả bảo trì được. Đó là biện pháp chất lượng rẻ nhất tồn tại.</li>
</ul>
<p class="meo">💡 Hãy đóng khung cả mục này bằng một câu hỏi: <strong>"mã chạy đúng rồi — vậy nó còn có thể sai ở chỗ nào nữa?"</strong> Ba câu trả lời chính là ba nhánh: nó có thể chạy không tốt với người dùng (operability), nó có thể không sửa nổi (maintainability), nó có thể bị kẹt cứng trên một cỗ máy (transferability).</p>`],

      [18, '1. Introduction (implementation phase)',
        `<p class="y-chinh">🎯 In the waterfall model, implementation starts <strong>after the design phase is completed</strong>. Programmers then write <em>the code for the modules</em> (procedure-oriented design) or <em>the program units that implement classes</em> (object-oriented design).</p>
<ul>
<li><strong>Read Figure 8.11 — it contradicts the simple picture of waterfall, usefully</strong> — the figure splits the implementation phase into three coloured bands, each a loop with a decision that can send you backwards. <em>Coding Phase</em> (blue): Code and Unit Tests → Code Review → "Unit Tests &amp; Code Review Passed?" — <strong>No</strong> loops back to coding. <em>Testing Phase</em> (yellow): Update Test Environment → Update &amp; Review Tests → "System Tests Passed?" — <strong>No</strong> loops all the way back. <em>Regression Phase</em> (green): Update Regression Tests → Run Regression Tests → "Regression Tests Passed?" — <strong>No</strong> loops back again; <strong>Yes</strong> reaches <code>Release</code>.</li>
<li><strong>The three terms in that figure are worth owning</strong> — <em>unit test</em>: does this one function do what it should? <em>System test</em>: does the assembled system meet the specification? <em>Regression test</em>: did today's change break something that worked yesterday? The third is the one beginners have never heard of and the one that saves real projects.</li>
<li><strong>Code review is in the picture for a reason</strong> — having another human read your code is, measured repeatedly since the 1970s, one of the most effective defect-removal techniques there is, and it finds a class of problem tests never will (unclear names, missing cases, wrong assumptions).</li>
<li><strong>Note the honest tension</strong> — the bullet says waterfall means "implementation starts after design is completed", but the figure is full of backward arrows. Both are true at different scales: the <em>phases</em> do not loop, but the work <em>inside</em> a phase always does.</li>
<li><strong>LibTrack's implementation</strong> — write <code>KIEM_TRA_THE</code>, unit-test it, get it reviewed; write <code>TAO_LUOT_MUON</code>, same; then the fine module, same. Then system-test "borrow a book, wait 40 simulated days, return it, check the fine". Then, after every later change, re-run all of it — that is the green band.</li>
</ul>
<p class="meo">💡 Remember the three test levels by who they protect: <strong>unit test protects the programmer, system test protects the customer, regression test protects the past.</strong></p>`,
        `<p class="y-chinh">🎯 Trong mô hình thác nước, pha hiện thực bắt đầu <strong>sau khi pha thiết kế hoàn tất</strong>. Lập trình viên khi ấy viết <em>mã cho các module</em> (thiết kế hướng thủ tục) hoặc <em>các đơn vị chương trình hiện thực hoá các lớp</em> (thiết kế hướng đối tượng).</p>
<ul>
<li><strong>Đọc Hình 8.11 — nó mâu thuẫn với bức tranh thác nước đơn giản, một cách có ích</strong> — bức hình chẻ pha hiện thực thành ba dải màu, mỗi dải là một vòng lặp có điểm quyết định đẩy bạn quay ngược. <em>Coding Phase</em> (xanh lam): Code and Unit Tests → Code Review → "Unit Tests &amp; Code Review Passed?" — <strong>No</strong> quay về viết mã. <em>Testing Phase</em> (vàng): Update Test Environment → Update &amp; Review Tests → "System Tests Passed?" — <strong>No</strong> quay ngược về tận đầu. <em>Regression Phase</em> (xanh lá): Update Regression Tests → Run Regression Tests → "Regression Tests Passed?" — <strong>No</strong> lại quay ngược; <strong>Yes</strong> mới tới ô <code>Release</code>.</li>
<li><strong>Ba thuật ngữ trong hình này đáng sở hữu</strong> — <em>kiểm thử đơn vị</em>: đúng một hàm này có làm đúng việc của nó không? <em>kiểm thử hệ thống</em>: hệ đã ráp xong có đáp ứng bản đặc tả không? <em>kiểm thử hồi quy</em>: thay đổi hôm nay có làm hỏng thứ hôm qua vẫn chạy không? Cái thứ ba là cái người mới chưa từng nghe, và là cái cứu các dự án thật.</li>
<li><strong>Code review có mặt trong hình là có lý do</strong> — để một người khác ĐỌC mã của bạn, theo các phép đo lặp đi lặp lại từ thập niên 1970, là một trong những kỹ thuật loại bỏ lỗi hiệu quả nhất, và nó bắt được loại vấn đề mà kiểm thử không bao giờ bắt được (tên biến tối nghĩa, trường hợp bỏ sót, giả định sai).</li>
<li><strong>Chú ý mâu thuẫn thành thật ở đây</strong> — gạch đầu dòng nói thác nước nghĩa là "hiện thực bắt đầu sau khi thiết kế xong", nhưng bức hình thì đầy mũi tên đi ngược. Cả hai đều đúng, ở hai thang khác nhau: các <em>PHA</em> không lặp, nhưng công việc <em>BÊN TRONG</em> một pha thì luôn lặp.</li>
<li><strong>Pha hiện thực của LibTrack</strong> — viết <code>KIEM_TRA_THE</code>, kiểm thử đơn vị nó, cho người duyệt; viết <code>TAO_LUOT_MUON</code>, y vậy; rồi module tính phí, y vậy. Sau đó kiểm thử hệ thống "mượn một cuốn, cho trôi 40 ngày giả lập, trả lại, kiểm số tiền phạt". Rồi sau mỗi lần sửa về sau, chạy lại toàn bộ — đó chính là dải xanh lá.</li>
</ul>
<p class="meo">💡 Nhớ ba mức kiểm thử bằng cách hỏi chúng bảo vệ AI: <strong>kiểm thử đơn vị bảo vệ lập trình viên, kiểm thử hệ thống bảo vệ khách hàng, kiểm thử hồi quy bảo vệ QUÁ KHỨ.</strong></p>`],

      [19, '2. Choice of language',
        `<p class="y-chinh">🎯 A short, practical slide: in a procedure-oriented development the team picks from the <strong>procedural</strong> languages; in an object-oriented one, <strong>C++, Java or C#</strong> are the common choices. And it notes that C++ straddles both, though a purely procedural implementation normally uses <strong>C</strong>.</p>
<ul>
<li><strong>Read Figure 8.12</strong> — two editor screenshots side by side. On the left, C/C++ Windows API code: <code>DWORD dataSize = 0;</code>, <code>const DWORD flags = RRF_RT_REG_SZ;</code>, <code>LONG retCode = ::RegGetValueW(hKey, nullptr, L"Connie", flags, nullptr, nullptr, &amp;dataSize);</code>, then <code>if (retCode != ERROR_SUCCESS) { // Handle error… }</code>. On the right, an Eclipse tab <code>Java8Example.java</code> with a class that creates a <code>Runnable</code>, overrides <code>run()</code> to print "Hello Lambdas", and starts <code>new Thread(runnable)</code>.</li>
<li><strong>"A language or a SET of languages"</strong> — the plural is deliberate and real. LibTrack would plausibly be C# for the desktop client, SQL for the database, and a shell script for the nightly backup. Three languages, one system, one project.</li>
<li><strong>What actually decides the choice</strong> — the slide only gives the paradigm, but in practice: what the team already knows (the biggest factor by far), what runs on the target platform, what libraries exist for the job, how long the system must be maintained, and whether the customer has a standard. "Which language is best" is almost never the real question.</li>
<li><strong>Why C is the example of "purely procedural"</strong> — C has functions and structs but no classes, no inheritance, no methods. You can imitate objects in C with structs of function pointers, and that is exactly what large C systems do, but the language does not help you.</li>
<li><strong>The consistency rule the slide is enforcing</strong> — procedural analysis → procedural design → procedural language. Object-oriented analysis → object-oriented design → object-oriented language. Mixing the roads (an object-oriented design implemented in C) is possible but costs you the benefit of the design.</li>
</ul>
<p class="pitfall">⚠️ Careful with the slide's own examples. Calling C++ "both procedural and object-oriented" is fine. But the code on the left of Figure 8.12 uses <code>nullptr</code> and <code>L"…"</code>, which are C++, not C — so the caption "(C++ &amp; Java)" is right while the sentence about "a purely procedural language such as C" is illustrated by something that is not C. And the deck's list is dated: C# and Java are still everywhere, but Python, JavaScript/TypeScript, Go, Rust and Kotlin now carry an enormous share of new development, and several of them (Python, JavaScript) are multi-paradigm, which quietly weakens the clean procedural/object-oriented fork the whole chapter is built on. Answer the exam with the slide; know the world is wider.</p>`,
        `<p class="y-chinh">🎯 Một slide ngắn và thực dụng: trong phát triển hướng thủ tục, nhóm chọn trong các ngôn ngữ <strong>thủ tục</strong>; trong phát triển hướng đối tượng thì <strong>C++, Java hoặc C#</strong> là các lựa chọn phổ biến. Và nó lưu ý C++ đứng chân cả hai bên, dù một hiện thực thuần thủ tục thường dùng <strong>C</strong>.</p>
<ul>
<li><strong>Đọc Hình 8.12</strong> — hai ảnh chụp trình soạn thảo đặt cạnh nhau. Bên trái là mã C/C++ gọi Windows API: <code>DWORD dataSize = 0;</code>, <code>const DWORD flags = RRF_RT_REG_SZ;</code>, <code>LONG retCode = ::RegGetValueW(hKey, nullptr, L"Connie", flags, nullptr, nullptr, &amp;dataSize);</code>, rồi <code>if (retCode != ERROR_SUCCESS) { // Handle error… }</code>. Bên phải là tab Eclipse <code>Java8Example.java</code> với một lớp tạo một <code>Runnable</code>, ghi đè <code>run()</code> để in "Hello Lambdas", rồi khởi động <code>new Thread(runnable)</code>.</li>
<li><strong>"MỘT ngôn ngữ hoặc MỘT TẬP ngôn ngữ"</strong> — số nhiều ở đây là cố ý và là sự thật. LibTrack rất có thể dùng C# cho ứng dụng máy bàn, SQL cho cơ sở dữ liệu, và một shell script cho việc sao lưu ban đêm. Ba ngôn ngữ, một hệ thống, một dự án.</li>
<li><strong>Cái gì thật sự quyết định lựa chọn</strong> — slide chỉ nêu hệ hình, nhưng thực tế là: đội đã biết sẵn cái gì (yếu tố lớn nhất, bỏ xa các yếu tố khác), cái gì chạy được trên nền tảng đích, có thư viện nào sẵn cho công việc này, hệ thống phải bảo trì bao lâu, và khách hàng có chuẩn riêng không. "Ngôn ngữ nào tốt nhất" hầu như không bao giờ là câu hỏi thật.</li>
<li><strong>Vì sao C là ví dụ cho "thuần thủ tục"</strong> — C có hàm và struct nhưng không có lớp, không kế thừa, không phương thức. Bạn bắt chước đối tượng trong C bằng struct chứa con trỏ hàm, và các hệ C lớn làm đúng như vậy, nhưng bản thân ngôn ngữ không đỡ bạn.</li>
<li><strong>Luật nhất quán mà slide đang siết</strong> — phân tích thủ tục → thiết kế thủ tục → ngôn ngữ thủ tục. Phân tích hướng đối tượng → thiết kế hướng đối tượng → ngôn ngữ hướng đối tượng. Trộn hai con đường (thiết kế hướng đối tượng rồi hiện thực bằng C) là làm được, nhưng bạn mất luôn cái lợi của bản thiết kế.</li>
</ul>
<p class="pitfall">⚠️ Cẩn thận với chính ví dụ của slide. Gọi C++ là "vừa thủ tục vừa hướng đối tượng" thì ổn. Nhưng đoạn mã bên trái Hình 8.12 dùng <code>nullptr</code> và <code>L"…"</code> — đó là C++, không phải C — nên caption "(C++ &amp; Java)" thì đúng, còn câu văn nói về "ngôn ngữ thuần thủ tục như C" lại được minh hoạ bằng một thứ KHÔNG phải C. Và danh sách của bộ slide đã cũ: C# với Java vẫn ở khắp nơi, nhưng Python, JavaScript/TypeScript, Go, Rust và Kotlin nay gánh một phần rất lớn công việc phát triển mới, mà vài trong số đó (Python, JavaScript) là ĐA HỆ HÌNH, điều này âm thầm làm yếu đi cái ngã rẽ thủ tục/hướng-đối-tượng sạch sẽ mà cả chương dựng lên. Đi thi thì trả lời theo slide; nhưng biết rằng thế giới rộng hơn.</p>`],

      [20, '3. Software quality',
        `<p class="y-chinh">🎯 The definition and the tree. High-quality software is software that <strong>satisfies the user's requirements, meets the operating standards of the organization, and runs efficiently on the hardware it was developed for</strong>. Figure 8.13 then splits quality into three branches with thirteen leaves.</p>
<ul>
<li><strong>The definition has three clauses and each one can fail independently</strong> — a system can do exactly what the user asked (clause 1) while ignoring the university's password policy (clause 2) and needing 8 GB of RAM the library's PCs do not have (clause 3). All three must hold.</li>
<li><strong>Read Figure 8.13 as the tree it is</strong> — root <strong>Software quality</strong>, three children: <strong>Operability</strong>, <strong>Maintainability</strong>, <strong>Transferability</strong>. Leaves: Operability → <em>Accuracy, Efficiency, Reliability, Security, Timeliness, Usability</em> (6). Maintainability → <em>Changeability, Correctability, Flexibility, Testability</em> (4). Transferability → <em>Reusability, Interoperability, Portability</em> (3).</li>
<li><strong>6 + 4 + 3 = 13</strong> — that arithmetic is the cheapest way to check you have not dropped a leaf under exam pressure.</li>
<li><strong>What each branch is really asking</strong> — Operability: <em>is it good to use right now?</em> Maintainability: <em>will it still be good in three years?</em> Transferability: <em>can it go somewhere else, or can parts of it be reused?</em> Present tense, future tense, elsewhere.</li>
<li><strong>Why "quality" needs attributes at all</strong> — because "make it high quality" is not an instruction anyone can act on or test. Break it into thirteen named attributes and each one becomes measurable: response time for efficiency, mean time between failures for reliability, number of modules touched by a typical change for changeability.</li>
<li><strong>LibTrack, judged on all three</strong> — <em>Operability</em>: the borrow screen completes in under a second and the fine is always right to the đồng. <em>Maintainability</em>: when the fine goes from 2,000 to 3,000 VND per day, exactly one configuration row changes and no code is recompiled. <em>Transferability</em>: the fine module is a pure function with no database calls, so the mobile app can reuse it unchanged.</li>
</ul>
<p class="meo">💡 If you memorise one figure from Chapter 8, memorise this one. Mnemonic for the branches: <strong>O-M-T</strong> = <em>Operate it, Maintain it, Transfer it</em> — use, keep, move.</p>`,
        `<p class="y-chinh">🎯 Định nghĩa và cái cây. Phần mềm chất lượng cao là phần mềm <strong>thoả mãn yêu cầu người dùng, đáp ứng các chuẩn vận hành của tổ chức, và chạy hiệu quả trên phần cứng mà nó được phát triển cho</strong>. Rồi Hình 8.13 chẻ chất lượng thành ba nhánh với mười ba lá.</p>
<ul>
<li><strong>Định nghĩa có ba vế và mỗi vế có thể hỏng độc lập</strong> — một hệ thống có thể làm đúng y điều người dùng yêu cầu (vế 1) mà vẫn phớt lờ chính sách mật khẩu của trường (vế 2) và đòi 8 GB RAM mà máy thư viện không có (vế 3). Cả ba phải đúng.</li>
<li><strong>Đọc Hình 8.13 đúng như cái cây của nó</strong> — gốc <strong>Software quality</strong>, ba con: <strong>Operability</strong> (khả năng vận hành), <strong>Maintainability</strong> (khả năng bảo trì), <strong>Transferability</strong> (khả năng chuyển đổi). Các lá: Operability → <em>Accuracy, Efficiency, Reliability, Security, Timeliness, Usability</em> (6). Maintainability → <em>Changeability, Correctability, Flexibility, Testability</em> (4). Transferability → <em>Reusability, Interoperability, Portability</em> (3).</li>
<li><strong>6 + 4 + 3 = 13</strong> — phép tính ấy là cách rẻ nhất để kiểm xem bạn có rụng lá nào không khi đang căng thẳng trong phòng thi.</li>
<li><strong>Mỗi nhánh thật ra đang hỏi gì</strong> — Operability: <em>dùng bây giờ có tốt không?</em> Maintainability: <em>ba năm nữa còn tốt không?</em> Transferability: <em>đem đi chỗ khác được không, hoặc dùng lại từng phần được không?</em> Thì hiện tại, thì tương lai, và chỗ khác.</li>
<li><strong>Vì sao "chất lượng" cần được chẻ ra thành thuộc tính</strong> — vì "hãy làm cho chất lượng cao" không phải một chỉ thị ai làm được hay kiểm được. Chẻ thành mười ba thuộc tính có tên thì từng cái đo được: thời gian đáp ứng cho efficiency, thời gian trung bình giữa hai lần hỏng cho reliability, số module phải động vào cho một thay đổi điển hình cho changeability.</li>
<li><strong>LibTrack, chấm theo cả ba nhánh</strong> — <em>Operability</em>: màn hình cho mượn xong dưới một giây và tiền phạt luôn đúng tới từng đồng. <em>Maintainability</em>: khi phí trễ đổi từ 2.000 lên 3.000 đ/ngày, đúng MỘT dòng cấu hình đổi và không biên dịch lại dòng mã nào. <em>Transferability</em>: module tính phí là hàm thuần, không gọi cơ sở dữ liệu, nên app di động dùng lại nguyên xi.</li>
</ul>
<p class="meo">💡 Nếu cả Chương 8 bạn chỉ thuộc một bức hình, hãy thuộc bức này. Mẹo nhớ ba nhánh: <strong>O-M-T</strong> = <em>Operate it, Maintain it, Transfer it</em> — dùng nó, giữ nó, chuyển nó.</p>`],

      [21, '4. Operability',
        `<p class="y-chinh">🎯 Branch one of the quality tree. <strong>Operability refers to the basic operation of a system</strong>, and the slide text names six measures: <em>accuracy, efficiency, reliability, security, timeliness, usability</em>.</p>
<ul>
<li><strong>The six, each with the question it answers and how you would measure it</strong> — <em>Accuracy</em>: are the answers right? (compare against hand-computed cases). <em>Efficiency</em>: does it use resources well? (CPU, memory, disk per transaction). <em>Reliability</em>: how often does it fail? (mean time between failures). <em>Security</em>: can the wrong person do the wrong thing? (access-control tests, audit log). <em>Timeliness</em>: are results delivered when needed? (response time, and "the overdue report must be ready before the library opens"). <em>Usability</em>: can a real user actually do the job? (time for a new librarian to complete a loan unaided).</li>
<li><strong>Accuracy and reliability are not the same thing</strong> — a program that computes the fine perfectly but crashes twice a day is accurate and unreliable. A program that never crashes but rounds the fine wrong is reliable and inaccurate. Exams like this pair.</li>
<li><strong>Efficiency and timeliness are not the same thing either</strong> — efficiency is about <em>resources consumed</em>, timeliness is about <em>deadlines met</em>. A nightly batch that burns 100% CPU for four hours is inefficient but perfectly timely if it finishes by 6 a.m.</li>
<li><strong>LibTrack's operability, made measurable</strong> — accuracy: the fine for 40 days late is exactly 80,000 VND, checked against the specification; efficiency: a loan transaction touches at most three database rows; reliability: no unplanned restart in 30 days of use; security: only a senior librarian can waive a fine, and every waiver is logged; timeliness: the borrow screen responds in under 1 second; usability: a new student assistant completes a loan without help after a 10-minute briefing.</li>
<li><strong>Why this branch is the one users notice</strong> — the other two branches (maintainability, transferability) matter to <em>you</em>. Operability is the only branch the librarian at the desk can feel.</li>
</ul>
<p class="pitfall">⚠️ <strong>The text and the figure on this slide describe two different things, and it is not a small mismatch.</strong> The bullet promises "as shown in Figure 8.14: accuracy, efficiency, reliability, security, timeliness, and usability" — six flat measures. Figure 8.14 actually draws a completely different two-level tree: Operability → <em>Switchability</em> and <em>Availability</em>; Switchability → <em>Controllability</em> and <em>Flexibility</em>; Controllability → <em>Stability</em>; Flexibility → <em>Optimality</em>; Availability → <em>Reliability</em> and <em>Maintainability</em>. Only "Reliability" appears in both. That figure is a stock image from some other quality model, not Forouzan's. <strong>Answer with the six words in the text</strong>, which do match Figure 8.13 on slide 20; and note the figure even puts <em>Maintainability</em> underneath Operability, which contradicts slide 20 where they are siblings.</p>`,
        `<p class="y-chinh">🎯 Nhánh thứ nhất của cây chất lượng. <strong>Operability nói về hoạt động cơ bản của hệ thống</strong>, và chữ trên slide gọi tên sáu đo lường: <em>accuracy (chính xác), efficiency (hiệu quả), reliability (tin cậy), security (an toàn), timeliness (kịp thời), usability (dễ dùng)</em>.</p>
<ul>
<li><strong>Sáu cái, mỗi cái kèm câu hỏi nó trả lời và cách bạn sẽ đo</strong> — <em>Accuracy</em>: kết quả có đúng không? (đối chiếu với các ca tính tay). <em>Efficiency</em>: dùng tài nguyên có khéo không? (CPU, bộ nhớ, đĩa cho mỗi giao dịch). <em>Reliability</em>: bao lâu thì hỏng một lần? (thời gian trung bình giữa hai lần hỏng). <em>Security</em>: người sai có làm được việc sai không? (kiểm thử phân quyền, nhật ký kiểm toán). <em>Timeliness</em>: kết quả có tới ĐÚNG LÚC cần không? (thời gian đáp ứng, và "báo cáo quá hạn phải xong trước giờ mở cửa"). <em>Usability</em>: người dùng thật có làm nổi việc không? (thời gian để một thủ thư mới hoàn tất một lượt mượn mà không cần ai giúp).</li>
<li><strong>Chính xác và tin cậy KHÔNG phải một thứ</strong> — một chương trình tính phí trễ đúng tuyệt đối nhưng ngày sập hai lần thì CHÍNH XÁC mà KHÔNG tin cậy. Một chương trình không bao giờ sập nhưng làm tròn tiền phạt sai thì TIN CẬY mà KHÔNG chính xác. Đề thi rất thích cặp này.</li>
<li><strong>Hiệu quả và kịp thời cũng không phải một thứ</strong> — hiệu quả nói về <em>tài nguyên tiêu tốn</em>, kịp thời nói về <em>hạn chót có đạt không</em>. Một mẻ chạy đêm ngốn 100% CPU suốt bốn tiếng là KHÔNG hiệu quả nhưng hoàn toàn KỊP THỜI nếu nó xong trước 6 giờ sáng.</li>
<li><strong>Operability của LibTrack, viết thành đo được</strong> — chính xác: phí cho 40 ngày trễ đúng bằng 80.000 đ, đối chiếu với bản đặc tả; hiệu quả: một giao dịch mượn chạm tối đa ba dòng dữ liệu; tin cậy: không lần khởi động lại ngoài kế hoạch nào trong 30 ngày dùng; an toàn: chỉ thủ thư trưởng được xoá phí, và mọi lần xoá đều ghi vết; kịp thời: màn hình cho mượn đáp dưới 1 giây; dễ dùng: một sinh viên trợ lý mới hoàn tất một lượt mượn không cần ai giúp sau 10 phút hướng dẫn.</li>
<li><strong>Vì sao đây là nhánh người dùng CẢM được</strong> — hai nhánh kia (bảo trì, chuyển đổi) quan trọng với <em>BẠN</em>. Operability là nhánh duy nhất mà cô thủ thư ngồi ở quầy cảm nhận được.</li>
</ul>
<p class="pitfall">⚠️ <strong>Chữ và hình trên slide này mô tả hai thứ khác nhau, và đây không phải lệch nhỏ.</strong> Gạch đầu dòng hứa "as shown in Figure 8.14: accuracy, efficiency, reliability, security, timeliness, and usability" — sáu đo lường phẳng. Còn Hình 8.14 thật sự vẽ một cái cây hai tầng HOÀN TOÀN KHÁC: Operability → <em>Switchability</em> và <em>Availability</em>; Switchability → <em>Controllability</em> và <em>Flexibility</em>; Controllability → <em>Stability</em>; Flexibility → <em>Optimality</em>; Availability → <em>Reliability</em> và <em>Maintainability</em>. Chỉ mỗi "Reliability" xuất hiện ở cả hai. Bức hình đó là ảnh nhặt từ một mô hình chất lượng khác, không phải của Forouzan. <strong>Hãy trả lời theo sáu chữ trong phần CHỮ</strong>, vì chúng khớp với Hình 8.13 ở slide 20; và để ý bức hình thậm chí đặt <em>Maintainability</em> NẰM DƯỚI Operability, trái ngược với slide 20 nơi hai cái là anh em ngang hàng.</p>`],

      [22, '5. Maintainability',
        `<p class="y-chinh">🎯 Branch two. <strong>Maintainability is the ease with which a system can be kept up to date and running correctly</strong> — and the slide makes the crucial point that most changes are needed <em>not because the software was badly written</em>, but because <strong>external factors changed</strong>.</p>
<ul>
<li><strong>The slide's own example is perfect</strong> — a company payroll system must change often to track changes in government laws and regulations. Nobody made a mistake; the world moved. The software has to move with it.</li>
<li><strong>The four kinds of maintenance (standard vocabulary, not on the slide but often examined)</strong> — <em>corrective</em>: fix a defect. <em>Adaptive</em>: follow a change in the environment (new OS, new tax law). <em>Perfective</em>: add or improve a function the users asked for. <em>Preventive</em>: restructure the code so future changes are cheaper. Industry surveys usually find corrective maintenance is the <em>smallest</em> slice — most maintenance is adaptive and perfective, which is exactly the slide's point.</li>
<li><strong>Forouzan's four leaves, from Figure 8.13 on slide 20</strong> — <em>Changeability</em>: how easily can a requested change be made? <em>Correctability</em>: how easily can a defect be found and fixed? <em>Flexibility</em>: how easily can the system take on something it was not built for? <em>Testability</em>: how easily can you prove a change did not break anything?</li>
<li><strong>Testability is the leaf students under-rate</strong> — and it is the bridge to section 8.5. Code with a clean function like <code>phi_tre(so_ngay, uu_tien)</code> can be tested by calling it with numbers. Code where the same arithmetic is buried inside a button-click handler that also reads the database and paints a screen cannot be tested at all without running the whole system. Same behaviour, wildly different testability — a <em>design</em> difference showing up as a <em>quality</em> difference.</li>
<li><strong>LibTrack's maintenance, six real years</strong> — year 1: fine rate changes (adaptive, one config row). Year 2: add e-books (perfective, a new subclass of Copy). Year 3: student ID format grows from 8 to 10 characters (adaptive, and painful if the length was hard-coded in twelve places). Year 4: a defect where a leap-year due date was computed wrong (corrective). Year 5: split the 900-line loan module into four (preventive). Each one tests a different leaf.</li>
</ul>
<p class="pitfall">⚠️ Figure 8.15 does <strong>not</strong> show Forouzan's four factors. It shows <em>Analyzability, Modularity, Modifiability, Reusability, Testability</em> — those are the maintainability sub-characteristics of the <strong>ISO/IEC 25010</strong> standard, a different (and more modern) model. Two consequences: only <em>Testability</em> appears in both lists, and <em>Reusability</em> sits under Maintainability here while slide 20 puts it under Transferability. If an exam asks "list the maintainability factors", answer with Figure 8.13's four — changeability, correctability, flexibility, testability — because that is the figure the chapter actually teaches.</p>`,
        `<p class="y-chinh">🎯 Nhánh thứ hai. <strong>Maintainability là mức độ DỄ giữ cho hệ thống luôn cập nhật và chạy đúng</strong> — và slide nêu một điểm mấu chốt: phần lớn thay đổi là cần thiết <em>KHÔNG PHẢI vì phần mềm viết dở</em>, mà vì <strong>các yếu tố bên ngoài đã đổi</strong>.</p>
<ul>
<li><strong>Ví dụ của chính slide là hoàn hảo</strong> — hệ tính lương của một công ty phải đổi liên tục để bám theo thay đổi của luật và quy định nhà nước. Không ai làm sai cả; là thế giới dịch chuyển. Phần mềm buộc phải dịch theo.</li>
<li><strong>Bốn loại bảo trì (từ vựng chuẩn, không có trên slide nhưng hay bị thi)</strong> — <em>sửa lỗi (corrective)</em>: vá một khiếm khuyết. <em>thích nghi (adaptive)</em>: chạy theo một thay đổi của môi trường (hệ điều hành mới, luật thuế mới). <em>hoàn thiện (perfective)</em>: thêm hoặc cải tiến một chức năng người dùng yêu cầu. <em>phòng ngừa (preventive)</em>: tái cấu trúc mã để những lần sửa sau rẻ hơn. Các khảo sát công nghiệp thường thấy bảo trì SỬA LỖI là phần NHỎ NHẤT — phần lớn bảo trì là thích nghi và hoàn thiện, đúng như điều slide đang nói.</li>
<li><strong>Bốn lá của Forouzan, lấy từ Hình 8.13 ở slide 20</strong> — <em>Changeability</em>: một thay đổi được yêu cầu thì làm dễ tới đâu? <em>Correctability</em>: một lỗi thì tìm và vá dễ tới đâu? <em>Flexibility</em>: hệ thống gánh thêm một việc mà nó không được dựng cho, dễ tới đâu? <em>Testability</em>: chứng minh một thay đổi không làm hỏng thứ khác, dễ tới đâu?</li>
<li><strong>Testability là cái lá sinh viên đánh giá thấp nhất</strong> — và nó là cây cầu sang mục 8.5. Mã có một hàm sạch như <code>phi_tre(so_ngay, uu_tien)</code> thì kiểm thử bằng cách gọi nó với vài con số. Mã mà cũng phép tính ấy bị chôn trong một hàm xử lý sự kiện bấm nút, hàm đó lại vừa đọc cơ sở dữ liệu vừa vẽ màn hình, thì không cách nào kiểm thử được nếu không chạy cả hệ thống. Cùng một hành vi, khả năng kiểm thử khác nhau một trời một vực — một khác biệt về <em>THIẾT KẾ</em> hiện ra thành khác biệt về <em>CHẤT LƯỢNG</em>.</li>
<li><strong>Sáu năm bảo trì thật của LibTrack</strong> — năm 1: đổi mức phí trễ (thích nghi, một dòng cấu hình). Năm 2: thêm sách điện tử (hoàn thiện, một lớp con mới của Copy). Năm 3: mã sinh viên dài từ 8 lên 10 ký tự (thích nghi, và đau đớn nếu độ dài ấy bị gắn cứng ở mười hai chỗ). Năm 4: vá lỗi tính sai hạn trả vào năm nhuận (sửa lỗi). Năm 5: chẻ module cho mượn 900 dòng thành bốn (phòng ngừa). Mỗi việc kiểm một cái lá khác nhau.</li>
</ul>
<p class="pitfall">⚠️ Hình 8.15 <strong>KHÔNG</strong> vẽ bốn yếu tố của Forouzan. Nó vẽ <em>Analyzability, Modularity, Modifiability, Reusability, Testability</em> — đó là các đặc tính con về bảo trì của chuẩn <strong>ISO/IEC 25010</strong>, một mô hình khác (và hiện đại hơn). Hai hệ quả: chỉ mỗi <em>Testability</em> có mặt ở cả hai danh sách, và <em>Reusability</em> ở đây nằm dưới Maintainability trong khi slide 20 xếp nó dưới Transferability. Nếu đề thi hỏi "liệt kê các yếu tố maintainability", hãy trả lời theo bốn cái của Hình 8.13 — changeability, correctability, flexibility, testability — vì đó mới là bức hình mà chương này thật sự dạy.</p>`],

      [23, '6. Transferability',
        `<p class="y-chinh">🎯 Branch three. <strong>Transferability is the ability to move data and/or a system from one platform to another, and to reuse code.</strong> The slide is honest about it: in many situations this does not matter — but for generalised software it can be critical.</p>
<ul>
<li><strong>Read Figure 8.16</strong> — three applications on the left (<code>Accounting Application</code>, <code>Shopping Cart</code>, <code>Content Management</code>) all wired into a middle box <strong>Shared Code Library</strong> containing three components (<code>Database Access</code>, <code>Login / Authentication</code>, <code>Error Logging</code>), which in turn connect to a <code>Database</code>, an <code>Auth Server</code> and a <code>Log Server</code>. That is reuse drawn as a picture: write the login code once, three products use it.</li>
<li><strong>The three leaves from Figure 8.13</strong> — <em>Reusability</em>: can pieces of this system be used in the next system? <em>Interoperability</em>: can this system work together with other systems (shared file formats, APIs, protocols)? <em>Portability</em>: can the same system run on a different platform (another OS, another database, another CPU)?</li>
<li><strong>Reuse pays twice and costs once</strong> — the login component is written once, tested once, and fixed once for all three applications. The cost is that it must be written to be <em>general</em>, which is harder and slower than writing it for one caller. That is why the slide says it only matters "if we are writing generalized software".</li>
<li><strong>The risk the figure does not draw</strong> — a shared library is also a shared point of failure. A bug in <code>Login / Authentication</code> is now a bug in three products at once, and a change to it must not break any of the three. Reuse trades duplication for coupling.</li>
<li><strong>What kills portability in practice</strong> — hard-coded file paths (<code>C:\\libtrack\\data</code>), assuming a particular byte order or integer size, calling OS-specific APIs directly instead of behind a thin wrapper, and SQL that only one database engine understands.</li>
<li><strong>LibTrack's transferability</strong> — <em>Reusability</em>: <code>phi_tre()</code> is a pure function of two integers, so the desk client, the mobile app and the nightly report all call the same code. <em>Interoperability</em>: the catalogue exports MARC records so it can exchange data with other libraries. <em>Portability</em>: all dates go through one helper and all paths through one configuration entry, so moving from Windows to Linux is a configuration change, not a rewrite.</li>
</ul>
<p class="meo">💡 Distinguish the three leaves with three prepositions: <strong>reusability = IN another system, interoperability = WITH another system, portability = ON another platform.</strong></p>`,
        `<p class="y-chinh">🎯 Nhánh thứ ba. <strong>Transferability là khả năng CHUYỂN dữ liệu và/hoặc hệ thống từ nền tảng này sang nền tảng khác, và khả năng DÙNG LẠI mã.</strong> Slide nói thẳng thắn: trong nhiều tình huống điều này không quan trọng — nhưng với phần mềm tổng quát hoá thì nó có thể sống còn.</p>
<ul>
<li><strong>Đọc Hình 8.16</strong> — ba ứng dụng bên trái (<code>Accounting Application</code>, <code>Shopping Cart</code>, <code>Content Management</code>) đều nối vào hộp giữa <strong>Shared Code Library</strong> chứa ba thành phần (<code>Database Access</code>, <code>Login / Authentication</code>, <code>Error Logging</code>), rồi ba thành phần này nối ra <code>Database</code>, <code>Auth Server</code> và <code>Log Server</code>. Đó là tái sử dụng được vẽ thành tranh: viết phần đăng nhập MỘT lần, ba sản phẩm cùng dùng.</li>
<li><strong>Ba cái lá từ Hình 8.13</strong> — <em>Reusability</em>: các mảnh của hệ này có dùng được cho hệ sau không? <em>Interoperability</em>: hệ này có làm việc chung được với các hệ khác không (định dạng file chung, API, giao thức)? <em>Portability</em>: chính hệ này có chạy được trên nền tảng khác không (hệ điều hành khác, cơ sở dữ liệu khác, CPU khác)?</li>
<li><strong>Tái sử dụng trả lãi hai lần và tính phí một lần</strong> — thành phần đăng nhập được viết một lần, kiểm thử một lần, và vá một lần cho cả ba ứng dụng. Cái giá là nó phải được viết cho <em>TỔNG QUÁT</em>, mà việc đó khó hơn và chậm hơn viết cho đúng một người gọi. Vì thế slide mới nói nó chỉ quan trọng "nếu ta đang viết phần mềm tổng quát hoá".</li>
<li><strong>Rủi ro mà bức hình không vẽ</strong> — thư viện dùng chung cũng là điểm hỏng dùng chung. Một lỗi trong <code>Login / Authentication</code> bây giờ là lỗi của ba sản phẩm cùng lúc, và một thay đổi ở đó không được làm gãy bất kỳ cái nào trong ba. Tái sử dụng đánh đổi sự trùng lặp lấy sự ghép nối.</li>
<li><strong>Cái gì giết portability trong thực tế</strong> — đường dẫn gắn cứng (<code>C:\\libtrack\\data</code>), giả định một thứ tự byte hay một kích thước số nguyên nhất định, gọi thẳng API riêng của hệ điều hành thay vì bọc sau một lớp mỏng, và SQL chỉ một loại cơ sở dữ liệu hiểu.</li>
<li><strong>Transferability của LibTrack</strong> — <em>Reusability</em>: <code>phi_tre()</code> là hàm thuần của hai số nguyên, nên máy ở quầy, app di động và báo cáo chạy đêm đều gọi đúng một đoạn mã. <em>Interoperability</em>: danh mục xuất được bản ghi MARC nên trao đổi dữ liệu được với thư viện khác. <em>Portability</em>: mọi ngày tháng đi qua một hàm phụ trợ và mọi đường dẫn đi qua một mục cấu hình, nên chuyển từ Windows sang Linux là đổi cấu hình, không phải viết lại.</li>
</ul>
<p class="meo">💡 Phân biệt ba cái lá bằng ba giới từ: <strong>reusability = TRONG một hệ khác, interoperability = VỚI một hệ khác, portability = TRÊN một nền tảng khác.</strong></p>`],

      [24, '5 - Testing phase',
        `<p class="y-chinh">🎯 Section divider for 8.5, the last section and the one that actually asks you to <em>do</em> something. Everything in it follows from one uncomfortable sentence on the next slide: <strong>the goal of testing is to FIND errors.</strong></p>
<ul>
<li><strong>Why that sentence is uncomfortable</strong> — it means a test run that finds nothing is a <em>failed</em> test run, not a successful one. A tester who reports "everything passed" has either written excellent software or useless tests, and you cannot tell which from that sentence alone.</li>
<li><strong>Dijkstra's line, which every testing course quotes</strong> — "Program testing can be used to show the presence of bugs, but never to show their absence." Testing gives you evidence, never proof. Slide 28 will show you exactly why: the input space is too big.</li>
<li><strong>The two words to keep straight</strong> — a <em>defect</em> (or fault, or bug) is the mistake in the code; a <em>failure</em> is what the user sees when that defect is executed. A defect on a line that never runs causes no failure — and is still a defect, waiting.</li>
<li><strong>What the section covers</strong> — slide 25: the two families. Slide 26: glass-box (white-box), where you can see the code. Slide 27: black-box, where you cannot. Slides 28–30: three black-box methods — exhaustive, random, boundary-value.</li>
<li><strong>The one thing you may be asked to produce</strong> — a set of test cases. Not a definition: an actual table of inputs and expected outputs. Slides 26, 27 and 30 each build one for LibTrack, and those tables are the model answer format.</li>
</ul>
<p class="meo">💡 Anchor the whole section on this: <strong>glass-box asks "have I exercised every part of the code?"; black-box asks "have I exercised every part of the specification?"</strong> Two different maps of the same territory, and you need both because each one misses what the other catches.</p>`,
        `<p class="y-chinh">🎯 Slide phân mục cho 8.5, mục cuối và là mục thật sự bắt bạn <em>LÀM</em> một việc gì đó. Mọi thứ trong nó đều chảy ra từ một câu khó chịu ở slide kế: <strong>mục tiêu của kiểm thử là TÌM RA lỗi.</strong></p>
<ul>
<li><strong>Vì sao câu ấy khó chịu</strong> — nó nghĩa là một lượt chạy kiểm thử không tìm thấy gì là một lượt chạy <em>THẤT BẠI</em>, không phải thành công. Một người kiểm thử báo "mọi thứ đều đạt" thì hoặc là phần mềm xuất sắc, hoặc là bộ kiểm thử vô dụng — và chỉ từ câu ấy bạn không phân biệt được hai khả năng.</li>
<li><strong>Câu của Dijkstra mà mọi môn kiểm thử đều trích</strong> — "Kiểm thử chương trình dùng để chỉ ra SỰ CÓ MẶT của lỗi, chứ không bao giờ chỉ ra được sự VẮNG MẶT của chúng." Kiểm thử cho bạn bằng chứng, không bao giờ cho bạn chứng minh. Slide 28 sẽ cho bạn thấy chính xác vì sao: không gian đầu vào quá lớn.</li>
<li><strong>Hai chữ phải phân biệt</strong> — <em>khiếm khuyết</em> (defect/fault/bug) là chỗ sai nằm trong mã; <em>sự cố</em> (failure) là thứ người dùng nhìn thấy khi chỗ sai ấy được thi hành. Một khiếm khuyết nằm trên dòng lệnh không bao giờ chạy tới thì không gây sự cố nào — và nó vẫn là khiếm khuyết, đang chờ.</li>
<li><strong>Mục này phủ những gì</strong> — slide 25: hai họ kiểm thử. Slide 26: hộp kính (hộp trắng), nơi bạn nhìn thấy mã. Slide 27: hộp đen, nơi bạn không nhìn thấy. Slide 28–30: ba phương pháp hộp đen — vét cạn, ngẫu nhiên, giá trị biên.</li>
<li><strong>Thứ duy nhất bạn có thể bị bắt LÀM RA</strong> — một bộ test case. Không phải một định nghĩa: một cái BẢNG thật gồm đầu vào và đầu ra mong đợi. Slide 26, 27 và 30 mỗi slide dựng một bảng như vậy cho LibTrack, và mấy cái bảng ấy chính là khuôn mẫu câu trả lời.</li>
</ul>
<p class="meo">💡 Neo cả mục này vào câu sau: <strong>hộp kính hỏi "tôi đã chạy qua mọi phần của MÃ chưa?"; hộp đen hỏi "tôi đã chạy qua mọi phần của ĐẶC TẢ chưa?"</strong> Hai tấm bản đồ khác nhau của cùng một vùng đất, và bạn cần cả hai vì mỗi cái bỏ sót đúng thứ cái kia bắt được.</p>`],

      [25, '1. Introduction (testing phase)',
        `<p class="y-chinh">🎯 Two sentences, and both are exam material: <strong>the goal of the testing phase is to find errors</strong>, so <strong>a good testing strategy is the one that finds most errors</strong>; and there are <strong>two types of testing: white-box and black-box</strong>.</p>
<ul>
<li><strong>Read Figure 8.17</strong> — two boxes side by side. <em>White-box</em>: a transparent box with an "interface" on the left, arrows for input, and inside it a visible flowchart labelled "Internal logic, data, structure", with arrows leaving as output. <em>Black-box</em>: a solid grey box with the same input arrows on the left and output arrows on the right, labelled only "Operations and Functions with I/O", plus a small "operation" foot at the bottom. The picture <em>is</em> the definition: in one you see the machinery, in the other you see only the doors.</li>
<li><strong>"White-box" and "glass-box" are the same thing</strong> — the next slide uses "glass-box (or white-box)". Forouzan prefers glass-box because the point is that the box is <em>transparent</em>, and "white" is a strange word for transparent. Both names are accepted; some books also say <em>clear-box</em>, <em>open-box</em> or <em>structural</em> testing, and black-box is also called <em>functional</em> or <em>behavioural</em> testing.</li>
<li><strong>The real basis of the split is one question: what do you use to design the test?</strong> Glass-box tests are designed from the <em>code</em>. Black-box tests are designed from the <em>specification</em>. That, not who performs them, is the definition — though who performs them follows from it.</li>
<li><strong>Why you need both, in one example</strong> — the specification says the fine is capped at 200,000 VND. A black-box tester reads that and tests 200,000. A glass-box tester opens the code, sees a fifth <code>if</code> nobody mentioned, and tests that too. Now reverse it: the code has no line at all for "reader is deceased", so glass-box can never test it; only someone reading the specification would notice it is missing. <strong>Glass-box cannot find missing code; black-box cannot find extra code.</strong> That sentence is worth a full mark on its own.</li>
<li><strong>A third family the deck omits</strong> — <em>grey-box</em> testing, where the tester knows the internal structure partially (for example the database schema) and designs tests from the specification with that knowledge. Not examinable here, but it is what most professional testing actually is.</li>
</ul>
<p class="pitfall">⚠️ The bullet ends with "(Figure 10.11)" while the caption under the picture says "Figure 8.17" — another leftover from Forouzan's Chapter 10. Harmless, but it confirms that figure numbers in this deck do not cross-reference reliably.</p>`,
        `<p class="y-chinh">🎯 Hai câu, và cả hai đều là vật liệu ra đề: <strong>mục tiêu của pha kiểm thử là TÌM LỖI</strong>, nên <strong>chiến lược kiểm thử tốt là chiến lược tìm được NHIỀU lỗi nhất</strong>; và có <strong>hai loại kiểm thử: hộp trắng và hộp đen</strong>.</p>
<ul>
<li><strong>Đọc Hình 8.17</strong> — hai cái hộp cạnh nhau. <em>White-box</em>: một hộp trong suốt có "interface" bên trái, các mũi tên đầu vào, và bên trong nhìn thấy rõ một lưu đồ ghi "Internal logic, data, structure", các mũi tên đi ra làm đầu ra. <em>Black-box</em>: một hộp xám đặc với đúng mấy mũi tên vào bên trái và ra bên phải, chỉ ghi "Operations and Functions with I/O", cộng một cái chân nhỏ ghi "operation" ở dưới. Bức hình CHÍNH LÀ định nghĩa: một bên bạn thấy bộ máy, một bên bạn chỉ thấy mấy cái cửa.</li>
<li><strong>"White-box" và "glass-box" là cùng một thứ</strong> — slide kế dùng "glass-box (or white-box)". Forouzan chuộng glass-box vì trọng tâm là cái hộp <em>TRONG SUỐT</em>, mà "trắng" thì là một chữ kỳ cục để nói trong suốt. Cả hai tên đều được chấp nhận; vài sách còn gọi là <em>clear-box</em>, <em>open-box</em> hay kiểm thử <em>cấu trúc</em>, còn hộp đen cũng được gọi là kiểm thử <em>chức năng</em> hay <em>hành vi</em>.</li>
<li><strong>Cơ sở thật của phép chia nằm ở một câu hỏi: bạn dùng CÁI GÌ để thiết kế bài kiểm thử?</strong> Test hộp kính được thiết kế từ <em>MÃ NGUỒN</em>. Test hộp đen được thiết kế từ <em>BẢN ĐẶC TẢ</em>. Đó mới là định nghĩa, chứ không phải "ai làm" — dù chuyện ai làm là hệ quả của nó.</li>
<li><strong>Vì sao cần cả hai, gói trong một ví dụ</strong> — bản đặc tả nói phí trễ có trần 200.000 đ. Người kiểm thử hộp đen đọc câu ấy và thử 200.000. Người kiểm thử hộp kính mở mã ra, thấy một câu <code>if</code> thứ năm chẳng ai nhắc tới, và thử luôn cả nó. Giờ lật ngược lại: trong mã KHÔNG hề có dòng nào cho "bạn đọc đã qua đời", nên hộp kính không bao giờ kiểm được; chỉ người đọc bản đặc tả mới nhận ra thứ ấy đang THIẾU. <strong>Hộp kính không tìm được mã BỊ THIẾU; hộp đen không tìm được mã BỊ THỪA.</strong> Riêng câu đó đã đáng trọn một ý điểm.</li>
<li><strong>Một họ thứ ba mà bộ slide bỏ qua</strong> — kiểm thử <em>hộp xám (grey-box)</em>, khi người kiểm biết một phần cấu trúc bên trong (ví dụ lược đồ cơ sở dữ liệu) và thiết kế test từ bản đặc tả với kiến thức ấy. Không bị thi ở đây, nhưng phần lớn kiểm thử chuyên nghiệp ngoài đời chính là loại này.</li>
</ul>
<p class="pitfall">⚠️ Gạch đầu dòng kết thúc bằng "(Figure 10.11)" trong khi caption dưới hình ghi "Figure 8.17" — lại một mẩu sót từ Chương 10 của Forouzan. Vô hại, nhưng nó xác nhận rằng số hình trong bộ slide này không dùng để tham chiếu chéo được.</p>`],

      [26, '2. Glass-box testing (or white-box testing)',
        `<p class="y-chinh">🎯 <strong>Glass-box testing is based on knowing the internal structure of the software</strong>; the goal is to check whether <em>all components do what they are designed to do</em>; the tester is assumed to know everything about the software; and it is <strong>done by the software engineer or a dedicated team</strong> — not by the customer, who cannot see inside.</p>
<ul>
<li><strong>Read Figure 8.18</strong> — a control-flow graph with nine numbered nodes: Start ① → ② → decision ③ → decision ④, branching to ⑦ on one side and ⑤ → ⑥ on the other, both meeting at ⑧, which loops back to ③ and eventually reaches Stop ⑨. The box on the right lists the <em>independent paths</em>: <strong>Path1 (1, 2, 3, 9)</strong>, <strong>Path2 (1, 2, 3, 4, 5, 6, 8, 3, 9)</strong>, <strong>Path3 (1, 2, 3, 4, 7, 8, 3, 9)</strong>. That is <em>basis path testing</em>: two decisions (③ and ④) give a cyclomatic complexity of 2 + 1 = 3, hence exactly three independent paths.</li>
<li><strong>The four coverage criteria — this is CQ13.2, learn the ladder</strong> — <em>(1) statement coverage</em>: every line executed at least once. <em>(2) branch (decision) coverage</em>: every decision taken both true and false. <em>(3) condition coverage</em>: every <em>individual</em> condition inside a compound decision evaluated both true and false. <em>(4) path coverage</em>: every possible route through the code executed. Each level implies more work than the one before, and path coverage is usually impossible (see slide 28).</li>
<li><strong>Now do it on real code.</strong> LibTrack's fine module, compiled with <code>cc -Wall</code> and run for real:</li>
</ul>
<pre>int phi_tre(int so_ngay, int uu_tien) {
    int phi = 0;
    if (so_ngay &gt; 0) {                 /* decision A */
        phi = so_ngay * 2000;
        if (so_ngay &gt; 30)              /* decision B */
            phi = phi + 50000;
    }
    if (uu_tien &amp;&amp; phi &gt; 0)            /* decision C */
        phi = phi / 2;
    if (phi &gt; 200000)                  /* decision D */
        phi = 200000;
    return phi;
}</pre>
<p class="nhan">Four decisions ⇒ 8 branch outcomes to cover. Here is a test set that reaches <strong>100% branch coverage</strong>, with the outputs actually produced by the compiled program:</p>
<table>
<tr><th>Test</th><th>so_ngay</th><th>uu_tien</th><th>A</th><th>B</th><th>C</th><th>D</th><th>Expected = actual output</th></tr>
<tr><td>T1</td><td>0</td><td>0</td><td>F</td><td>—</td><td>F</td><td>F</td><td>0</td></tr>
<tr><td>T2</td><td>5</td><td>0</td><td>T</td><td>F</td><td>F</td><td>F</td><td>10000</td></tr>
<tr><td>T3</td><td>40</td><td>1</td><td>T</td><td>T</td><td>T</td><td>F</td><td>65000</td></tr>
<tr><td>T4</td><td>200</td><td>0</td><td>T</td><td>T</td><td>F</td><td>T</td><td>200000</td></tr>
</table>
<p class="dap-an">✅ Answer: <strong>T1–T4 give 100% branch coverage</strong>. Check it column by column — A is true in T2/T3/T4 and false in T1; B is true in T3/T4 and false in T2; C is true in T3 and false in T1/T2/T4; D is true in T4 and false in T1/T2/T3. All eight outcomes are hit. Measured with <code>clang -fprofile-instr-generate -fcoverage-mapping</code> and <code>llvm-cov</code>, the counters read A [T:3, F:1], B [T:2, F:1], C [T:1, F:3], D [T:1, F:3] — no zero, so nothing is missed.</p>
<p class="dap-an">✅ And two results from the same measurement that are worth more than the table itself. <strong>(a) Statement coverage is weaker than branch coverage.</strong> Running only T3 and T4 executes <em>every line</em> of the function — 100% statement coverage — yet llvm-cov then reports A [T:2, <strong>F:0</strong>] and B [T:2, <strong>F:0</strong>]: only 6 of 8 branch outcomes, 75%. A manager told "we have 100% coverage" has been told almost nothing. <strong>(b) Branch coverage is weaker than condition coverage.</strong> Even with all four tests, the sub-condition <code>phi &gt; 0</code> inside decision C measures [T:1, <strong>F:0</strong>] — it is never false when it is evaluated, because <code>&amp;&amp;</code> short-circuits whenever <code>uu_tien</code> is 0. You need a fifth case, T5 = (so_ngay 0, uu_tien 1) → output 0, to make <code>uu_tien</code> true while <code>phi &gt; 0</code> is false. Only then does the counter become [T:1, F:1].</p>
<p class="meo">💡 Remember the ladder as an inclusion chain: <strong>path ⊃ condition ⊃ branch ⊃ statement.</strong> Every path-covering set covers all branches; every branch-covering set covers all statements; none of the arrows reverses. If an exam asks "does 100% statement coverage imply 100% branch coverage?", the answer is <strong>no</strong>, and T3+T4 above is your counterexample.</p>
<p class="pitfall">⚠️ Two traps. First, glass-box testing can only test code that <em>exists</em> — if the specification's rule "a lost book is charged at replacement value" was never written, no amount of coverage finds it. Second, do not confuse the <em>number of independent paths</em> (3, in Figure 8.18) with the <em>number of test cases</em> (often more, because one path may need several inputs to exercise its boundaries).</p>`,
        `<p class="y-chinh">🎯 <strong>Kiểm thử hộp kính dựa trên việc BIẾT cấu trúc bên trong của phần mềm</strong>; mục tiêu là kiểm xem <em>mọi thành phần có làm đúng việc chúng được thiết kế để làm không</em>; người kiểm được giả định là biết mọi thứ về phần mềm; và nó <strong>do chính kỹ sư phần mềm hoặc một đội chuyên trách thực hiện</strong> — không phải khách hàng, vì khách hàng không nhìn vào trong được.</p>
<ul>
<li><strong>Đọc Hình 8.18</strong> — một đồ thị luồng điều khiển với chín nút đánh số: Start ① → ② → nút quyết định ③ → nút quyết định ④, rẽ sang ⑦ một bên và ⑤ → ⑥ bên kia, cả hai gặp nhau ở ⑧, rồi ⑧ vòng ngược về ③ và cuối cùng tới Stop ⑨. Cái hộp bên phải liệt kê các <em>đường độc lập</em>: <strong>Path1 (1, 2, 3, 9)</strong>, <strong>Path2 (1, 2, 3, 4, 5, 6, 8, 3, 9)</strong>, <strong>Path3 (1, 2, 3, 4, 7, 8, 3, 9)</strong>. Đó là <em>kiểm thử đường cơ sở (basis path testing)</em>: hai nút quyết định (③ và ④) cho độ phức tạp chu trình 2 + 1 = 3, nên đúng ba đường độc lập.</li>
<li><strong>Bốn tiêu chí PHỦ — đây chính là CQ13.2, hãy học thuộc cái thang này</strong> — <em>(1) phủ câu lệnh</em>: mọi dòng đều được chạy ít nhất một lần. <em>(2) phủ nhánh (phủ quyết định)</em>: mọi quyết định đều được lấy cả giá trị đúng lẫn sai. <em>(3) phủ điều kiện</em>: mọi điều kiện <em>CON</em> bên trong một quyết định ghép đều nhận cả đúng lẫn sai. <em>(4) phủ đường đi</em>: mọi lộ trình khả dĩ qua đoạn mã đều được chạy. Mỗi bậc đòi nhiều công hơn bậc trước, và phủ đường đi thường là bất khả (xem slide 28).</li>
<li><strong>Bây giờ làm thật trên mã thật.</strong> Module tính phí của LibTrack, đã biên dịch bằng <code>cc -Wall</code> và chạy thật:</li>
</ul>
<pre>int phi_tre(int so_ngay, int uu_tien) {
    int phi = 0;
    if (so_ngay &gt; 0) {                 /* quyết định A */
        phi = so_ngay * 2000;
        if (so_ngay &gt; 30)              /* quyết định B */
            phi = phi + 50000;
    }
    if (uu_tien &amp;&amp; phi &gt; 0)            /* quyết định C */
        phi = phi / 2;
    if (phi &gt; 200000)                  /* quyết định D */
        phi = 200000;
    return phi;
}</pre>
<p class="nhan">Bốn quyết định ⇒ 8 kết cục nhánh phải phủ. Đây là bộ test đạt <strong>phủ nhánh 100%</strong>, kèm đầu ra do chính chương trình đã biên dịch sinh ra:</p>
<table>
<tr><th>Ca</th><th>so_ngay</th><th>uu_tien</th><th>A</th><th>B</th><th>C</th><th>D</th><th>Kết quả mong đợi = thực tế</th></tr>
<tr><td>T1</td><td>0</td><td>0</td><td>S</td><td>—</td><td>S</td><td>S</td><td>0</td></tr>
<tr><td>T2</td><td>5</td><td>0</td><td>Đ</td><td>S</td><td>S</td><td>S</td><td>10000</td></tr>
<tr><td>T3</td><td>40</td><td>1</td><td>Đ</td><td>Đ</td><td>Đ</td><td>S</td><td>65000</td></tr>
<tr><td>T4</td><td>200</td><td>0</td><td>Đ</td><td>Đ</td><td>S</td><td>Đ</td><td>200000</td></tr>
</table>
<p class="dap-an">✅ Đáp án: <strong>T1–T4 cho phủ nhánh 100%</strong>. Soi từng cột — A đúng ở T2/T3/T4 và sai ở T1; B đúng ở T3/T4 và sai ở T2; C đúng ở T3 và sai ở T1/T2/T4; D đúng ở T4 và sai ở T1/T2/T3. Cả tám kết cục đều được chạm. Đo bằng <code>clang -fprofile-instr-generate -fcoverage-mapping</code> rồi <code>llvm-cov</code>, các bộ đếm đọc ra A [Đ:3, S:1], B [Đ:2, S:1], C [Đ:1, S:3], D [Đ:1, S:3] — không có số 0 nào, nghĩa là không sót cái nào.</p>
<p class="dap-an">✅ Và hai kết quả nữa từ chính phép đo đó, đáng giá hơn cả cái bảng ở trên. <strong>(a) Phủ câu lệnh YẾU HƠN phủ nhánh.</strong> Chỉ chạy T3 và T4 là đã thi hành <em>MỌI DÒNG</em> của hàm — phủ câu lệnh 100% — thế mà llvm-cov khi ấy báo A [Đ:2, <strong>S:0</strong>] và B [Đ:2, <strong>S:0</strong>]: chỉ 6 trên 8 kết cục nhánh, tức 75%. Một người quản lý được báo "chúng ta có 100% coverage" thật ra gần như chưa được báo gì cả. <strong>(b) Phủ nhánh YẾU HƠN phủ điều kiện.</strong> Ngay cả với đủ bốn ca, điều kiện con <code>phi &gt; 0</code> bên trong quyết định C vẫn đo ra [Đ:1, <strong>S:0</strong>] — nó không bao giờ SAI vào lúc nó được tính, vì <code>&amp;&amp;</code> đoản mạch mỗi khi <code>uu_tien</code> bằng 0. Bạn cần ca thứ năm, T5 = (so_ngay 0, uu_tien 1) → kết quả 0, để <code>uu_tien</code> đúng trong khi <code>phi &gt; 0</code> sai. Chỉ khi đó bộ đếm mới thành [Đ:1, S:1].</p>
<p class="meo">💡 Nhớ cái thang này như một chuỗi bao hàm: <strong>đường đi ⊃ điều kiện ⊃ nhánh ⊃ câu lệnh.</strong> Bộ test phủ hết đường đi thì phủ hết nhánh; bộ phủ hết nhánh thì phủ hết câu lệnh; không mũi tên nào đảo ngược được. Nếu đề thi hỏi "phủ câu lệnh 100% có suy ra phủ nhánh 100% không?", đáp án là <strong>KHÔNG</strong>, và T3+T4 ở trên chính là phản ví dụ của bạn.</p>
<p class="pitfall">⚠️ Hai cái bẫy. Thứ nhất, kiểm thử hộp kính chỉ kiểm được đoạn mã ĐANG TỒN TẠI — nếu luật "sách mất thì tính theo giá thay thế" trong bản đặc tả chưa từng được viết ra thì phủ tới bao nhiêu phần trăm cũng không tìm ra. Thứ hai, đừng lẫn <em>số đường độc lập</em> (3, trong Hình 8.18) với <em>số ca kiểm thử</em> (thường nhiều hơn, vì một đường có thể cần vài bộ đầu vào để chạy hết các biên của nó).</p>`],

      [27, '3. Black testing',
        `<p class="y-chinh">🎯 <strong>Black-box testing tests the software without knowing what is inside it or how it works</strong> — only what it is <em>supposed to accomplish</em>, in terms of its inputs and outputs. The slide title drops the word "box", but "black-box testing" is the correct term.</p>
<ul>
<li><strong>Read Figure 8.19</strong> — a table of six test cases for an insurance premium. Columns: <em>Test Case #</em>, <em>Inputs (Causes)</em> = Sex and Age, <em>Expected Output (Effects)</em> = Premium. The rows: 1 Male, &lt;25 → $3000; 2 Male, ≥25 and &lt;65 → $1000; 3 Male, ≥65 → $1500; 4 Female, ≥65 → $1500; 5 Female, &lt;25 → $500; 6 Female, ≥25 and &lt;65 → $500. That is exactly what a black-box test case looks like: inputs in, expected output stated <em>before</em> you run anything.</li>
<li><strong>The figure is an example of equivalence partitioning</strong> — the technique the deck never names but constantly uses. You split the input domain into classes such that <em>every value in a class should be treated the same way by the program</em>, then test one representative per class. Here: Sex has 2 classes, Age has 3, so 2 × 3 = 6 combinations, and the table has exactly 6 rows.</li>
<li><strong>Why partitioning works</strong> — if age 30 and age 40 travel through exactly the same code, testing both is testing once and paying twice. Partitioning is how you get from "infinitely many inputs" to "six tests" without guessing.</li>
<li><strong>What Figure 8.19 forgets, and it matters</strong> — there is no <em>invalid</em> partition. No row for age −5, age 200, a blank sex, or a non-numeric age. A complete partitioning always has invalid classes too, and that is where most real defects hide.</li>
<li><strong>Now LibTrack. The specification says a final mark is an integer 0–10 and maps to a grade.</strong> Partition the input domain into five classes and test one value from each — run against code compiled with <code>cc -Wall</code>:</li>
</ul>
<table>
<tr><th>Partition</th><th>Range</th><th>Representative</th><th>Expected output</th><th>Actual output</th></tr>
<tr><td>Invalid (low)</td><td>diem &lt; 0</td><td>-1</td><td>KHONG HOP LE</td><td>KHONG HOP LE</td></tr>
<tr><td>Fail</td><td>0 – 4</td><td>2</td><td>Truot</td><td>Truot</td></tr>
<tr><td>Average</td><td>5 – 6</td><td>6</td><td>Trung binh</td><td>Trung binh</td></tr>
<tr><td>Good</td><td>7 – 8</td><td>7</td><td>Kha</td><td>Kha</td></tr>
<tr><td>Excellent</td><td>9 – 10</td><td>10</td><td>Gioi</td><td>Gioi</td></tr>
<tr><td>Invalid (high)</td><td>diem &gt; 10</td><td>11</td><td>KHONG HOP LE</td><td>KHONG HOP LE</td></tr>
</table>
<p class="dap-an">✅ Answer: six partitions, six test cases, all passing. Note the economy — the input domain of an <code>int</code> has over four billion values and we have covered the specification's behaviour with six of them. But note the gap too: partitioning alone tests the <em>middle</em> of each class. Slide 30 tests the <em>edges</em>, which is where the defects actually are, and the two techniques are always used together.</p>
<p class="pitfall">⚠️ Who performs black-box testing? The deck contrasts it with glass-box, which is "done by the software engineer or a dedicated team". Black-box can be done by anyone who has the specification — an independent test team, or the customer during acceptance testing — and that independence is the point: a programmer testing their own code tends to test what they <em>meant</em> to write.</p>`,
        `<p class="y-chinh">🎯 <strong>Kiểm thử hộp đen kiểm phần mềm mà KHÔNG biết bên trong nó có gì và nó chạy ra sao</strong> — chỉ biết nó <em>PHẢI làm được gì</em>, xét theo đầu vào và đầu ra. Tiêu đề slide rụng mất chữ "box", nhưng thuật ngữ đúng là "black-box testing".</p>
<ul>
<li><strong>Đọc Hình 8.19</strong> — một bảng sáu ca kiểm thử cho phí bảo hiểm. Các cột: <em>Test Case #</em>, <em>Inputs (Causes)</em> gồm Sex và Age, <em>Expected Output (Effects)</em> là Premium. Các dòng: 1 Nam, &lt;25 → 3000 $; 2 Nam, ≥25 và &lt;65 → 1000 $; 3 Nam, ≥65 → 1500 $; 4 Nữ, ≥65 → 1500 $; 5 Nữ, &lt;25 → 500 $; 6 Nữ, ≥25 và &lt;65 → 500 $. Đó chính xác là hình hài một ca kiểm thử hộp đen: đầu vào đưa vào, đầu ra mong đợi được ghi ra <em>TRƯỚC</em> khi bạn chạy bất cứ thứ gì.</li>
<li><strong>Bức hình là một ví dụ của phân hoạch tương đương</strong> — kỹ thuật mà bộ slide không bao giờ gọi tên nhưng dùng liên tục. Bạn chẻ miền đầu vào thành các lớp sao cho <em>mọi giá trị trong một lớp đều phải được chương trình đối xử như nhau</em>, rồi kiểm một đại diện cho mỗi lớp. Ở đây: Sex có 2 lớp, Age có 3 lớp, nên 2 × 3 = 6 tổ hợp, và cái bảng có đúng 6 dòng.</li>
<li><strong>Vì sao phân hoạch lại hiệu quả</strong> — nếu tuổi 30 và tuổi 40 đi qua đúng cùng một đoạn mã thì kiểm cả hai là kiểm một lần mà trả tiền hai lần. Phân hoạch là cách bạn đi từ "vô hạn đầu vào" xuống "sáu ca kiểm thử" mà không phải đoán mò.</li>
<li><strong>Thứ Hình 8.19 quên, và nó quan trọng</strong> — không có phân hoạch <em>KHÔNG HỢP LỆ</em> nào. Không dòng nào cho tuổi −5, tuổi 200, giới tính để trống, hay tuổi nhập chữ. Một phép phân hoạch đầy đủ luôn có cả các lớp không hợp lệ, và đó mới là chỗ phần lớn lỗi thật ẩn náu.</li>
<li><strong>Giờ tới LibTrack. Bản đặc tả nói điểm tổng kết là số nguyên 0–10 và ánh xạ sang xếp loại.</strong> Chẻ miền đầu vào thành năm lớp rồi kiểm một giá trị mỗi lớp — chạy trên mã đã biên dịch bằng <code>cc -Wall</code>:</li>
</ul>
<table>
<tr><th>Phân hoạch</th><th>Khoảng</th><th>Đại diện</th><th>Đầu ra mong đợi</th><th>Đầu ra thực tế</th></tr>
<tr><td>Không hợp lệ (dưới)</td><td>diem &lt; 0</td><td>-1</td><td>KHONG HOP LE</td><td>KHONG HOP LE</td></tr>
<tr><td>Trượt</td><td>0 – 4</td><td>2</td><td>Truot</td><td>Truot</td></tr>
<tr><td>Trung bình</td><td>5 – 6</td><td>6</td><td>Trung binh</td><td>Trung binh</td></tr>
<tr><td>Khá</td><td>7 – 8</td><td>7</td><td>Kha</td><td>Kha</td></tr>
<tr><td>Giỏi</td><td>9 – 10</td><td>10</td><td>Gioi</td><td>Gioi</td></tr>
<tr><td>Không hợp lệ (trên)</td><td>diem &gt; 10</td><td>11</td><td>KHONG HOP LE</td><td>KHONG HOP LE</td></tr>
</table>
<p class="dap-an">✅ Đáp án: sáu phân hoạch, sáu ca kiểm thử, tất cả đều đạt. Để ý sự tiết kiệm — miền đầu vào của một <code>int</code> có hơn bốn tỷ giá trị mà ta đã phủ hết hành vi trong bản đặc tả bằng sáu giá trị. Nhưng cũng để ý lỗ hổng: phân hoạch một mình chỉ kiểm phần <em>GIỮA</em> mỗi lớp. Slide 30 kiểm phần <em>MÉP</em>, và mép mới là nơi lỗi thật sự nằm; hai kỹ thuật này luôn được dùng cùng nhau.</p>
<p class="pitfall">⚠️ Ai thực hiện kiểm thử hộp đen? Bộ slide đặt nó đối lập với hộp kính, vốn "do chính kỹ sư phần mềm hoặc một đội chuyên trách làm". Hộp đen thì bất kỳ ai có bản đặc tả đều làm được — một đội kiểm thử độc lập, hoặc chính khách hàng trong lúc nghiệm thu — và chính SỰ ĐỘC LẬP ấy là điểm mấu chốt: một lập trình viên tự kiểm mã của mình có xu hướng kiểm đúng cái mà họ ĐỊNH viết.</p>`],

      [28, '4. Exhaustive testing',
        `<p class="y-chinh">🎯 The best black-box method, in principle, is to <strong>test the software for ALL possible values in the input domain</strong>. And the same sentence immediately kills it: in complex software the input domain is so huge that this is <strong>often impractical</strong>.</p>
<ul>
<li><strong>Read Figure 8.20 — the numbers on it are the whole argument</strong> — a small flow graph with a handful of decisions and one loop annotated "loop &lt; 20 times", under the caption: "<em>There are 10^14 possible paths! If we execute one test per millisecond, it would take 3,170 years to test this program!!</em>"</li>
<li><strong>Check the arithmetic, because a number you have verified is a number you will remember</strong> — 10^14 tests × 1 ms = 10^14 ms = 10^11 seconds. One year is about 3.156 × 10^7 seconds. So 10^11 ÷ 3.156 × 10^7 ≈ 3,169 years. <strong>The slide's figure is correct.</strong></li>
<li><strong>Why the number explodes</strong> — it is combinatorics, not code size. A loop that can run 0 to 19 times sitting after a couple of branches multiplies the path count instead of adding to it. This is the same explosion as the travelling-salesman problem you met in Chapter 6: exponential growth beats any computer you can buy.</li>
<li><strong>Do the same sum for our own function</strong> — <code>phi_tre(int so_ngay, int uu_tien)</code> takes two 32-bit integers, so exhaustive testing means 2^32 × 2^32 = 2^64 ≈ 1.84 × 10^19 combinations. At one nanosecond each — a thousand times faster than the slide's millisecond — that is 1.84 × 10^10 seconds ≈ <strong>585 years</strong>, for a twelve-line function. Exhaustive testing is not "hard"; for anything with more than one small input it is arithmetically impossible.</li>
<li><strong>When it IS possible, and you should say so</strong> — if the input domain is genuinely tiny. A function taking one <code>char</code> has 256 inputs; a menu with 7 options has 7. In those cases exhaustive testing is not just possible, it is the correct choice, because it is the only method that gives certainty. Do not answer "exhaustive testing is impossible" — answer "impractical for realistic input domains".</li>
<li><strong>What the rest of the section is for</strong> — since you cannot test everything, you need a principled way to choose a subset. Random testing (slide 29) picks spread-out values; boundary-value testing (slide 30) picks the values most likely to be wrong. Equivalence partitioning (slide 27) picks one per class. All three are answers to the problem this slide poses.</li>
</ul>
<p class="meo">💡 The one-line exam answer: <strong>exhaustive testing is the ideal but is impractical, because the input domain grows combinatorially; so testing is the art of choosing a small subset that finds the most errors.</strong> That sentence links slide 25's "goal is to find errors" to slides 29 and 30, and it is what an examiner wants to see.</p>`,
        `<p class="y-chinh">🎯 Phương pháp hộp đen tốt nhất, về nguyên tắc, là <strong>kiểm phần mềm với TẤT CẢ các giá trị khả dĩ trong miền đầu vào</strong>. Và chính câu ấy giết nó ngay lập tức: với phần mềm phức tạp, miền đầu vào lớn tới mức việc này <strong>thường là bất khả thi trên thực tế</strong>.</p>
<ul>
<li><strong>Đọc Hình 8.20 — mấy con số trên đó CHÍNH LÀ toàn bộ lập luận</strong> — một đồ thị luồng nhỏ với dăm nút quyết định và một vòng lặp ghi chú "loop &lt; 20 times", dưới là dòng chữ: "<em>Có 10^14 đường đi khả dĩ! Nếu chạy một test mỗi mili-giây thì phải mất 3.170 năm mới kiểm xong chương trình này!!</em>"</li>
<li><strong>Kiểm lại phép tính đi, vì một con số bạn đã tự nghiệm là con số bạn sẽ nhớ</strong> — 10^14 test × 1 ms = 10^14 ms = 10^11 giây. Một năm khoảng 3,156 × 10^7 giây. Vậy 10^11 ÷ 3,156 × 10^7 ≈ 3.169 năm. <strong>Con số trên slide là ĐÚNG.</strong></li>
<li><strong>Vì sao con số bùng nổ</strong> — đó là tổ hợp, không phải kích thước mã. Một vòng lặp chạy được 0 tới 19 lần nằm sau vài nhánh rẽ sẽ NHÂN số đường đi lên chứ không phải cộng thêm. Đây đúng là kiểu bùng nổ của bài toán người bán hàng rong bạn gặp ở Chương 6: tăng trưởng hàm mũ thắng mọi cỗ máy bạn mua được.</li>
<li><strong>Làm đúng phép tính đó cho chính hàm của chúng ta</strong> — <code>phi_tre(int so_ngay, int uu_tien)</code> nhận hai số nguyên 32 bit, nên kiểm vét cạn nghĩa là 2^32 × 2^32 = 2^64 ≈ 1,84 × 10^19 tổ hợp. Mỗi ca một nano-giây — nhanh gấp một nghìn lần cái mili-giây của slide — thì cũng mất 1,84 × 10^10 giây ≈ <strong>585 năm</strong>, cho một hàm mười hai dòng. Kiểm vét cạn không phải là "khó"; với bất cứ thứ gì có hơn một đầu vào nhỏ, nó bất khả về mặt SỐ HỌC.</li>
<li><strong>Khi nào nó LÀM ĐƯỢC, và bạn nên nói ra</strong> — khi miền đầu vào thật sự bé xíu. Một hàm nhận một <code>char</code> có 256 đầu vào; một menu 7 lựa chọn có 7. Trong những trường hợp ấy, kiểm vét cạn không chỉ khả thi mà còn là lựa chọn ĐÚNG, vì nó là phương pháp duy nhất cho sự chắc chắn. Đừng trả lời "kiểm vét cạn là không thể" — hãy trả lời "bất khả thi trên thực tế với các miền đầu vào thực tế".</li>
<li><strong>Phần còn lại của mục này để làm gì</strong> — vì không kiểm hết được, bạn cần một cách CÓ NGUYÊN TẮC để chọn một tập con. Kiểm ngẫu nhiên (slide 29) lấy các giá trị rải đều; kiểm giá trị biên (slide 30) lấy đúng những giá trị dễ sai nhất. Phân hoạch tương đương (slide 27) lấy mỗi lớp một giá trị. Cả ba đều là câu trả lời cho bài toán mà slide này đặt ra.</li>
</ul>
<p class="meo">💡 Câu trả lời thi gói trong một dòng: <strong>kiểm vét cạn là lý tưởng nhưng bất khả thi trên thực tế, vì miền đầu vào tăng theo tổ hợp; nên kiểm thử là NGHỆ THUẬT chọn một tập con nhỏ mà tìm được nhiều lỗi nhất.</strong> Câu ấy nối "mục tiêu là tìm lỗi" của slide 25 với slide 29 và 30, và đó đúng là thứ người chấm muốn thấy.</p>`],

      [29, '5. Random testing',
        `<p class="y-chinh">🎯 If you cannot test everything, test a <strong>subset</strong> — and the slide's real warning is about <em>how</em> you pick it: the values must be <strong>distributed over the input domain</strong>. A random number generator is the tool.</p>
<ul>
<li><strong>Read Figure 8.21 (captioned 10.15)</strong> — a loop: a person <em>Define/Refine inform</em> a <code>Formal</code> box; <code>Formal</code> sends <em>Inputs</em> to a <code>Quick check</code> oval; <code>Quick check</code> <em>Runs many cases</em> against the <code>System under test</code>, which <em>Validates states and output</em>; failures come back as a <code>Test report</code> that <em>reports failures with minimized outputs</em>, which the person uses to refine the formal description.</li>
<li><strong>The figure is actually drawing property-based testing</strong> — a modern and powerful form of random testing where you state a <em>property</em> that must always hold, and the tool generates thousands of random inputs trying to break it. "Minimized outputs" is the key phrase: when it finds a failure at <code>so_ngay = 8347291</code>, it automatically shrinks it to the smallest failing case, e.g. <code>so_ngay = 101</code>, which is the one you can actually debug.</li>
<li><strong>Why "distributed" is the word that matters</strong> — "random" done badly is worse than not random. If you draw 1,000 values uniformly from a 32-bit integer range, essentially none of them land near 0, 1, 30 or 200,000 — exactly the values our fine function cares about. Uniform randomness over a huge domain systematically misses the interesting region.</li>
<li><strong>How to do it properly for LibTrack</strong> — do not draw <code>so_ngay</code> uniformly from all <code>int</code>. Draw it from a realistic range (say 0 to 400) and deliberately over-sample near the boundaries 0, 1, 30, 31, and the value where the cap bites. That is random testing combined with partitioning and boundary analysis, which is what real test suites do.</li>
<li><strong>The property you would assert for LibTrack</strong> — "for every <code>so_ngay</code> ≥ 0 and every <code>uu_tien</code> in {0,1}, the result is between 0 and 200,000 inclusive, and it never decreases as <code>so_ngay</code> increases with <code>uu_tien</code> fixed". Run ten thousand random pairs against that and you have tested something no fixed table of six rows could.</li>
<li><strong>What random testing cannot do</strong> — it gives you no guarantee of coverage. You could run a million random cases and still never trigger a branch that needs one exact value. That is why it complements glass-box coverage rather than replacing it.</li>
</ul>
<p class="pitfall">⚠️ Two notes. The caption still reads "Figure 10.15" instead of 8.21 — another leftover number. And be careful with the phrase "random testing" in an exam: the slide presents it as <em>selecting a subset of the input domain</em>, which is the classic textbook meaning. Do not answer "you test by clicking around randomly" — that is called exploratory or monkey testing and it is a different thing.</p>`,
        `<p class="y-chinh">🎯 Nếu không kiểm được hết thì kiểm một <strong>tập con</strong> — và cảnh báo thật của slide nằm ở chỗ bạn chọn tập con ấy <em>NHƯ THẾ NÀO</em>: các giá trị phải <strong>rải đều trên miền đầu vào</strong>. Công cụ là bộ sinh số ngẫu nhiên.</p>
<ul>
<li><strong>Đọc Hình 8.21 (caption ghi 10.15)</strong> — một vòng: một người <em>Define/Refine inform</em> (định nghĩa/tinh chỉnh) cho hộp <code>Formal</code>; <code>Formal</code> gửi <em>Inputs</em> tới hình bầu dục <code>Quick check</code>; <code>Quick check</code> <em>Run many cases</em> (chạy rất nhiều ca) lên <code>System under test</code>, hệ này <em>Validate states and output</em> (nghiệm trạng thái và đầu ra); các ca hỏng quay về thành <code>Test report</code> có ghi <em>report failures with minimized outputs</em> (báo lỗi kèm đầu ra đã rút gọn), rồi người dùng nó để tinh chỉnh bản mô tả hình thức.</li>
<li><strong>Bức hình thật ra đang vẽ kiểm thử theo tính chất (property-based testing)</strong> — một dạng kiểm thử ngẫu nhiên hiện đại và mạnh, trong đó bạn phát biểu một <em>TÍNH CHẤT</em> luôn phải đúng, rồi công cụ sinh hàng nghìn đầu vào ngẫu nhiên để cố phá nó. Cụm "minimized outputs" là mấu chốt: khi nó tìm được ca hỏng ở <code>so_ngay = 8347291</code>, nó tự động co lại thành ca hỏng nhỏ nhất, ví dụ <code>so_ngay = 101</code>, và đó mới là ca bạn gỡ lỗi được.</li>
<li><strong>Vì sao chữ "rải đều" mới là chữ quan trọng</strong> — "ngẫu nhiên" làm dở còn tệ hơn không ngẫu nhiên. Nếu bạn bốc 1.000 giá trị đều từ toàn dải số nguyên 32 bit thì gần như không giá trị nào rơi gần 0, 1, 30 hay 200.000 — đúng những giá trị mà hàm tính phí của ta quan tâm. Ngẫu nhiên đều trên một miền khổng lồ sẽ BỎ SÓT MỘT CÁCH CÓ HỆ THỐNG đúng cái vùng thú vị.</li>
<li><strong>Làm cho đúng với LibTrack</strong> — đừng bốc <code>so_ngay</code> đều trên toàn dải <code>int</code>. Hãy bốc trong một khoảng thực tế (chẳng hạn 0 tới 400) và CỐ Ý lấy mẫu dày quanh các biên 0, 1, 30, 31, và giá trị nơi cái trần bắt đầu cắn. Đó là kiểm ngẫu nhiên kết hợp phân hoạch và phân tích biên, và đó chính là cách các bộ test thật làm.</li>
<li><strong>Tính chất bạn sẽ khẳng định cho LibTrack</strong> — "với mọi <code>so_ngay</code> ≥ 0 và mọi <code>uu_tien</code> thuộc {0,1}, kết quả nằm trong đoạn 0 tới 200.000, và nó không bao giờ GIẢM khi <code>so_ngay</code> tăng trong lúc <code>uu_tien</code> giữ nguyên". Chạy mười nghìn cặp ngẫu nhiên qua khẳng định ấy là bạn đã kiểm được thứ mà một cái bảng sáu dòng cố định không bao giờ kiểm nổi.</li>
<li><strong>Cái mà kiểm ngẫu nhiên KHÔNG làm được</strong> — nó không bảo đảm gì về độ phủ. Bạn có thể chạy một triệu ca ngẫu nhiên mà vẫn không bao giờ chạm được một nhánh cần đúng một giá trị duy nhất. Vì thế nó BỔ SUNG cho phủ hộp kính chứ không thay thế.</li>
</ul>
<p class="pitfall">⚠️ Hai lưu ý. Caption vẫn ghi "Figure 10.15" thay vì 8.21 — lại một con số còn sót. Và cẩn thận với cụm "random testing" trong bài thi: slide trình bày nó là <em>chọn một tập con của miền đầu vào</em>, đó mới là nghĩa kinh điển trong sách. Đừng trả lời "là bấm loạn xạ vào phần mềm" — cái đó gọi là kiểm thử thăm dò hoặc monkey testing, và là một thứ khác.</p>`],

      [30, '6. Boundary-value testing',
        `<p class="y-chinh">🎯 The last method, and the most useful one you will ever learn: <strong>errors often happen when boundary values are encountered.</strong> If a module requires an input greater than or equal to 100, you <em>must</em> test exactly 100.</p>
<ul>
<li><strong>Read Figure 8.22 (captioned 10.16)</strong> — an AGE field annotated "*Accepts value 18 to 56", above a table headed BOUNDARY VALUE ANALYSIS with three columns: <em>Invalid (min −1)</em> = <strong>17</strong>; <em>Valid (min, +min, −max, max)</em> = <strong>18, 19, 55, 56</strong>; <em>Invalid (max +1)</em> = <strong>57</strong>. Six values for a field that accepts thirty-nine. That is the technique in one picture.</li>
<li><strong>The rule to memorise</strong> — for a valid range [min, max], test <strong>min−1, min, min+1, max−1, max, max+1</strong>. Some textbooks use only min−1, min, max, max+1 (four values); Figure 8.22 uses six. Either is defensible; say which convention you are using.</li>
<li><strong>Why boundaries are where bugs live</strong> — because boundaries are where programmers make one-character mistakes: <code>&lt;</code> instead of <code>&lt;=</code>, <code>&gt;</code> instead of <code>&gt;=</code>, a loop running <code>i &lt; n</code> when it should be <code>i &lt;= n</code>. Such a defect is invisible in the middle of a range and fatal at exactly one value. This is the famous <em>off-by-one error</em>.</li>
<li><strong>Apply it to LibTrack's grade function</strong> — the valid range is 0–10 with internal boundaries at 4/5, 6/7 and 8/9. Compiled with <code>cc -Wall</code> and run, every boundary value gives:</li>
</ul>
<table>
<tr><th>Boundary</th><th>Input</th><th>Expected</th><th>Actual (run)</th><th>Verdict</th></tr>
<tr><td>min − 1</td><td>-1</td><td>KHONG HOP LE</td><td>KHONG HOP LE</td><td>pass</td></tr>
<tr><td>min</td><td>0</td><td>Truot</td><td>Truot</td><td>pass</td></tr>
<tr><td>min + 1</td><td>1</td><td>Truot</td><td>Truot</td><td>pass</td></tr>
<tr><td>Fail / Average edge</td><td>4</td><td>Truot</td><td>Truot</td><td>pass</td></tr>
<tr><td>Fail / Average edge</td><td>5</td><td>Trung binh</td><td>Trung binh</td><td>pass</td></tr>
<tr><td>Average / Good edge</td><td>6</td><td>Trung binh</td><td>Trung binh</td><td>pass</td></tr>
<tr><td>Average / Good edge</td><td>7</td><td>Kha</td><td>Kha</td><td>pass</td></tr>
<tr><td>Good / Excellent edge</td><td>8</td><td>Kha</td><td>Kha</td><td>pass</td></tr>
<tr><td>Good / Excellent edge</td><td>9</td><td>Gioi</td><td>Gioi</td><td>pass</td></tr>
<tr><td>max</td><td>10</td><td>Gioi</td><td>Gioi</td><td>pass</td></tr>
<tr><td>max + 1</td><td>11</td><td>KHONG HOP LE</td><td>KHONG HOP LE</td><td>pass</td></tr>
</table>
<p class="dap-an">✅ Answer: eleven boundary values, all eleven correct. Now see what the technique buys you — if the programmer had written <code>if (diem &lt; 4) return "Truot";</code> instead of <code>&lt;= 4</code>, then <em>only</em> the input 4 would expose it. Every value in the "middle" of every partition would still pass. Combine this with slide 27: <strong>equivalence partitioning tells you which classes to test, boundary-value analysis tells you which values inside them to pick.</strong> Together they are the standard black-box method, and that pairing is the answer to CQ13.1-style questions.</p>
<p class="dap-an">✅ And for LibTrack's fine function, the boundaries are 0 (nothing owed), 1 (first day charged), 30 and 31 (the surcharge threshold), and the value where the 200,000 cap engages. Running the compiled code: <code>phi_tre(0,0) = 0</code>, <code>phi_tre(5,0) = 10000</code>, <code>phi_tre(40,1) = 65000</code>, <code>phi_tre(200,0) = 200000</code>, <code>phi_tre(0,1) = 0</code>. Notice this is <em>the same test set as slide 26</em> — a set built for branch coverage turned out to be a set of boundary values, because branches and boundaries are the same thing seen from inside and from outside.</p>
<p class="pitfall">⚠️ This slide's last sentence is corrupted: "<em>it is possible that some condition in the module's code such as x . 100 is written as x . 100</em>" — the two comparison operators were lost when the file was converted, so the sentence says one thing is written as itself. Forouzan's original reads "<strong>x &gt;= 100 is written as x &gt; 100</strong>", which is precisely the off-by-one defect that testing the value 100 catches. Do not quote the slide's broken version; quote the corrected one, and note that the caption should be Figure 8.22, not 10.16.</p>
<p class="meo">💡 Closing the chapter: the five phases produced five artefacts for LibTrack — a specification, a set of diagrams, a module tree, a compiled program, and a test table. If you can name the artefact each phase produces, you can answer almost any question this chapter asks. And remember the one thing the deck never covers but the objectives promise: user, system and technical documentation (see slide 3).</p>`,
        `<p class="y-chinh">🎯 Phương pháp cuối cùng, và là phương pháp hữu dụng nhất bạn từng học: <strong>lỗi thường xảy ra khi gặp các GIÁ TRỊ BIÊN.</strong> Nếu một module đòi đầu vào lớn hơn hoặc bằng 100 thì bạn <em>BẮT BUỘC</em> phải kiểm đúng giá trị 100.</p>
<ul>
<li><strong>Đọc Hình 8.22 (caption ghi 10.16)</strong> — một ô AGE có chú thích "*Accepts value 18 to 56", phía dưới là bảng tiêu đề BOUNDARY VALUE ANALYSIS gồm ba cột: <em>Invalid (min −1)</em> = <strong>17</strong>; <em>Valid (min, +min, −max, max)</em> = <strong>18, 19, 55, 56</strong>; <em>Invalid (max +1)</em> = <strong>57</strong>. Sáu giá trị cho một ô nhận tới ba mươi chín giá trị. Đó là cả kỹ thuật gói trong một bức hình.</li>
<li><strong>Cái luật phải thuộc</strong> — với khoảng hợp lệ [min, max], hãy kiểm <strong>min−1, min, min+1, max−1, max, max+1</strong>. Vài sách chỉ dùng min−1, min, max, max+1 (bốn giá trị); Hình 8.22 dùng sáu. Cách nào cũng biện hộ được; chỉ cần nói rõ bạn đang theo quy ước nào.</li>
<li><strong>Vì sao biên là nơi lỗi sinh sống</strong> — vì biên là chỗ lập trình viên gõ sai đúng một ký tự: <code>&lt;</code> thay vì <code>&lt;=</code>, <code>&gt;</code> thay vì <code>&gt;=</code>, một vòng lặp chạy <code>i &lt; n</code> trong khi đáng lẽ phải là <code>i &lt;= n</code>. Loại lỗi ấy VÔ HÌNH ở giữa khoảng và CHÍ MẠNG ở đúng một giá trị. Đây là lỗi lệch-một (off-by-one) nổi tiếng.</li>
<li><strong>Áp vào hàm xếp loại của LibTrack</strong> — khoảng hợp lệ là 0–10 với các biên nội bộ ở 4/5, 6/7 và 8/9. Biên dịch bằng <code>cc -Wall</code> rồi chạy, mỗi giá trị biên cho ra:</li>
</ul>
<table>
<tr><th>Biên</th><th>Đầu vào</th><th>Mong đợi</th><th>Thực tế (đã chạy)</th><th>Kết luận</th></tr>
<tr><td>min − 1</td><td>-1</td><td>KHONG HOP LE</td><td>KHONG HOP LE</td><td>đạt</td></tr>
<tr><td>min</td><td>0</td><td>Truot</td><td>Truot</td><td>đạt</td></tr>
<tr><td>min + 1</td><td>1</td><td>Truot</td><td>Truot</td><td>đạt</td></tr>
<tr><td>Mép Trượt / Trung bình</td><td>4</td><td>Truot</td><td>Truot</td><td>đạt</td></tr>
<tr><td>Mép Trượt / Trung bình</td><td>5</td><td>Trung binh</td><td>Trung binh</td><td>đạt</td></tr>
<tr><td>Mép Trung bình / Khá</td><td>6</td><td>Trung binh</td><td>Trung binh</td><td>đạt</td></tr>
<tr><td>Mép Trung bình / Khá</td><td>7</td><td>Kha</td><td>Kha</td><td>đạt</td></tr>
<tr><td>Mép Khá / Giỏi</td><td>8</td><td>Kha</td><td>Kha</td><td>đạt</td></tr>
<tr><td>Mép Khá / Giỏi</td><td>9</td><td>Gioi</td><td>Gioi</td><td>đạt</td></tr>
<tr><td>max</td><td>10</td><td>Gioi</td><td>Gioi</td><td>đạt</td></tr>
<tr><td>max + 1</td><td>11</td><td>KHONG HOP LE</td><td>KHONG HOP LE</td><td>đạt</td></tr>
</table>
<p class="dap-an">✅ Đáp án: mười một giá trị biên, cả mười một đều đúng. Giờ hãy thấy kỹ thuật này mua cho bạn cái gì — nếu lập trình viên đã viết <code>if (diem &lt; 4) return "Truot";</code> thay vì <code>&lt;= 4</code>, thì <em>CHỈ</em> đầu vào 4 mới lộ ra lỗi ấy. Mọi giá trị nằm "giữa" mọi phân hoạch đều vẫn đạt. Ghép với slide 27: <strong>phân hoạch tương đương cho bạn biết kiểm những LỚP nào, phân tích giá trị biên cho bạn biết chọn GIÁ TRỊ nào bên trong mỗi lớp.</strong> Hai cái đi cùng nhau là phương pháp hộp đen chuẩn mực, và chính cặp đôi ấy là đáp án cho các câu kiểu CQ13.1.</p>
<p class="dap-an">✅ Còn với hàm tính phí của LibTrack, các biên là 0 (chưa nợ gì), 1 (ngày đầu bị tính), 30 và 31 (ngưỡng phụ thu), và giá trị nơi cái trần 200.000 bắt đầu có hiệu lực. Chạy mã đã biên dịch: <code>phi_tre(0,0) = 0</code>, <code>phi_tre(5,0) = 10000</code>, <code>phi_tre(40,1) = 65000</code>, <code>phi_tre(200,0) = 200000</code>, <code>phi_tre(0,1) = 0</code>. Để ý đây <em>ĐÚNG LÀ bộ test của slide 26</em> — một bộ dựng ra để phủ nhánh hoá ra lại chính là một bộ giá trị biên, vì nhánh và biên là cùng MỘT thứ, nhìn từ bên trong và nhìn từ bên ngoài.</p>
<p class="pitfall">⚠️ Câu cuối của slide này bị hỏng: "<em>it is possible that some condition in the module's code such as x . 100 is written as x . 100</em>" — hai dấu so sánh đã rụng khi chuyển file, nên câu văn thành ra nói một thứ được viết thành chính nó. Nguyên văn Forouzan là "<strong>x &gt;= 100 is written as x &gt; 100</strong>", và đó chính xác là lỗi lệch-một mà việc kiểm giá trị 100 bắt được. Đừng trích bản hỏng của slide; hãy trích bản đã sửa, và lưu ý caption lẽ ra phải là Figure 8.22 chứ không phải 10.16.</p>
<p class="meo">💡 Khép lại chương: năm pha đã đẻ ra năm sản phẩm cho LibTrack — một bản đặc tả, một bộ sơ đồ, một cây module, một chương trình đã biên dịch, và một bảng test case. Nếu bạn gọi được tên sản phẩm mà mỗi pha đẻ ra thì bạn trả lời được gần như mọi câu hỏi của chương này. Và nhớ thứ mà bộ slide không bao giờ dạy nhưng mục tiêu lại hứa: tài liệu người dùng, tài liệu hệ thống và tài liệu kỹ thuật (xem slide 3).</p>`],

    ]),
  ].join('\n'),
};
