/**
 * CSI106 · Chương 9 — Data structures, học theo từng slide: PHẦN A (slide 1–19).
 * Deck 'csi9' (CSI9), 37 slide, ảnh đã render lên CDN images/academy/CSI106/v1/csi9/NNN.webp.
 *
 * Nội dung bám ĐÚNG chữ trích từ CSI_09.pptx của trường (/tmp/csi106-text/csi9.txt, slide 1→19).
 * Các slide nặng sơ đồ (5, 6, 7, 8, 9, 11, 12, 13, 14, 16, 17, 18, 19) đã được đọc thẳng từ
 * ảnh đã render để lấy đúng từng nhãn, từng mũi tên và từng dòng mã giả.
 *
 * MỌI con số địa chỉ / offset / sizeof trong bài đều là SỐ ĐO THẬT, lấy từ ba chương trình C
 * biên dịch bằng `cc -Wall` và chạy thật trên macOS arm64 (clang, LP64). Không có con số nào
 * là suy đoán.
 *
 * Những chỗ SLIDE GỐC SAI — đã nêu rõ trong bài (phần Đáp án), KHÔNG im lặng chép lại và
 * KHÔNG tự ý sửa slide:
 *   · slide 9 (Figure 9.5, mã giả chèn phần tử): sau vòng lặp i = 8 nên dòng cuối
 *     `array[i] ← newValue` ghi vào ô 8, ĐÈ MẤT phần tử cũ và để ô 9 trùng lặp. Chạy thật:
 *     kết quả ra 60 70 999 90 90 100…, đúng phải là 60 70 80 999 90 100… ⇒ dòng cuối phải là
 *     `array[i + 1] ← newValue`.
 *   · slide 19 (Algorithm 9.1): điều kiện `while (target < (*cur).data)` bị NGƯỢC. Chạy thật
 *     trên danh sách 66,72,74,85,96 tìm 85: điều kiện như in dừng ngay ở nút đầu (flag false);
 *     điều kiện đúng `target > (*cur).data` dừng đúng ở 85 (flag true). Ngoài ra vòng lặp
 *     thiếu chốt `cur ≠ null` nên chạy quá đuôi danh sách.
 *   · slide 12 (Figure 9.7) và slide 14 (Figure 9.9) đều mang chú thích "Insert a value to
 *     array" chép nhầm từ Figure 9.5 — nội dung hai hình không hề nói về chèn phần tử.
 *   · slide 13 (Figure 9.8) nói là "Insert a record (name, status, grade)" nhưng mã Java dùng
 *     ArrayList<ArrayList<String>> — không phải record: sinh viên 1 chỉ có 2 phần tử
 *     ("Jane Doe", "50") còn sinh viên 2 có 3 ("John Doe", "Part Time", "70").
 *   · slide 5, 6, 7, 8, 9, 12, 14, 17 nhắc "Figure 11.3 … 11.11" trong thân chữ nhưng chú
 *     thích hình lại đánh số 9.x — dấu vết của việc đánh lại số từ Chương 11 của Forouzan.
 *   · slide 24 (ngoài phần này) lặp cả "Figure 12.2" lẫn "Figure 9.15" cùng một chú thích.
 */
import { walk, walkHead } from './_slides.mjs';

const D = 'csi9';

export default {
  title: '9.0a — Slide by slide: Arrays, records and the start of linked lists (slides 1–19)|||9.0a — Slide bài giảng: Mảng, bản ghi (record) và mở đầu danh sách liên kết (slide 1–19)',
  slug: 'csi106-9-0a-slides-mang-record',
  type: 'DOCUMENT',
  description: 'Nửa đầu Chương 9 của CSI106 (slide 1–19) đi theo đúng bộ slide của trường: mảng một chiều và công thức địa chỉ phần tử, vòng lặp duyệt mảng, mảng hai chiều với hai cách xếp bộ nhớ row-major và column-major, năm thao tác trên mảng, rồi record với tên bản ghi và tên trường, mảng các record, và ba slide mở đầu danh sách liên kết. Mọi con số địa chỉ, offset và sizeof trong bài đều là số đo thật từ ba chương trình C đã biên dịch bằng cc -Wall và chạy thật, gồm cả phép chứng minh C dùng row-major và bảng byte đệm khiến struct { char c; int i; } có sizeof 8 chứ không phải 5. Hai chỗ mã giả sai trong slide gốc (chèn phần tử lệch một ô, điều kiện tìm kiếm ngược dấu) được chạy tay chứng minh và nêu rõ thay vì chép lại.',
  content: [
    walkHead(D, 1, 19),
    walk(D, [

      [1, '9. Data structures',
        `<p class="y-chinh">🎯 The title slide of Chapter 9. The two words that matter are <strong>data structure</strong>: not "how to compute" (that was Chapter 6, Algorithms) but <em>how to arrange data in memory</em> so that an algorithm can reach it quickly.</p>
<ul>
<li><strong>What a data structure is</strong> — a way of grouping several related pieces of data under one name, together with the operations allowed on that group. Forouzan's definition in <em>Foundations of Computer Science</em>, Chapter 11, is exactly that, and the exam expects that sentence back.</li>
<li><strong>Why it comes after algorithms</strong> — an algorithm without a data structure has nothing to work on. Binary search (Chapter 6) only exists because arrays give constant-time access to any element; if the data lived in a linked list, binary search would be impossible. Slide 19 says that in so many words.</li>
<li><strong>The three structures this chapter builds</strong> — array (many elements, <em>same</em> type, reached by index), record (few fields, possibly <em>different</em> types, reached by name), linked list (many elements, reached only by following pointers). Slides 1–19 cover the first two completely and open the third.</li>
<li><strong>Where you have already used all three</strong> — in PRF192: <code>int a[100];</code> is the array, <code>struct student { … };</code> is the record, and any exercise with <code>malloc</code> plus a <code>next</code> pointer was a linked list.</li>
<li><strong>Where it goes next</strong> — Chapter 10 (File structure) writes <em>records</em> to disk, so a record is the unit of a binary file; Chapter 11 (Databases) turns a file of records into a table of rows. Records are the bridge between memory and storage.</li>
</ul>
<p class="meo">💡 One sentence to carry through the whole chapter: <strong>an array combines many things of the same kind; a record combines the parts of one thing.</strong> Almost every "array or record?" exam question falls out of that sentence.</p>`,
        `<p class="y-chinh">🎯 Slide tiêu đề Chương 9. Hai chữ đáng để ý là <strong>cấu trúc dữ liệu</strong>: chương này không nói "tính thế nào" (đó là Chương 6, Thuật toán) mà nói <em>sắp xếp dữ liệu trong bộ nhớ ra sao</em> để thuật toán với tới được nhanh.</p>
<ul>
<li><strong>Cấu trúc dữ liệu là gì</strong> — là cách gom nhiều mẩu dữ liệu có liên quan dưới một cái tên, kèm theo những thao tác được phép làm trên nhóm đó. Định nghĩa của Forouzan trong <em>Foundations of Computer Science</em>, Chương 11, đúng là câu ấy, và bài thi chờ nghe lại đúng câu ấy.</li>
<li><strong>Vì sao nó đứng sau thuật toán</strong> — thuật toán mà không có cấu trúc dữ liệu thì không có gì để làm việc lên. Tìm kiếm nhị phân (Chương 6) tồn tại được chỉ vì mảng cho phép với tới phần tử bất kỳ trong thời gian hằng số; nếu dữ liệu nằm trong danh sách liên kết thì tìm nhị phân là bất khả. Slide 19 nói thẳng điều đó.</li>
<li><strong>Ba cấu trúc mà chương này dựng</strong> — mảng (nhiều phần tử, <em>cùng</em> kiểu, với tới bằng chỉ số), record (vài trường, có thể <em>khác</em> kiểu, với tới bằng tên), danh sách liên kết (nhiều phần tử, chỉ với tới được bằng cách đi theo con trỏ). Slide 1–19 phủ trọn hai cái đầu và mở đầu cái thứ ba.</li>
<li><strong>Bạn đã dùng cả ba ở đâu</strong> — trong PRF192: <code>int a[100];</code> là mảng, <code>struct student { … };</code> là record, và bất kỳ bài tập nào có <code>malloc</code> cộng một con trỏ <code>next</code> đều là danh sách liên kết.</li>
<li><strong>Nó đi tiếp về đâu</strong> — Chương 10 (Cấu trúc tệp) ghi <em>record</em> xuống đĩa, nên record chính là đơn vị của một tệp nhị phân; Chương 11 (Cơ sở dữ liệu) biến một tệp record thành một bảng gồm các dòng. Record là nhịp cầu giữa bộ nhớ và lưu trữ.</li>
</ul>
<p class="meo">💡 Một câu mang theo suốt cả chương: <strong>mảng gom nhiều vật cùng loại; record gom các phần của MỘT vật.</strong> Gần như mọi câu hỏi thi dạng "dùng mảng hay dùng record?" đều rơi ra từ câu ấy.</p>`],

      [2, 'Content',
        `<p class="y-chinh">🎯 Four sections. Read this slide as the map of the chapter — and notice that the last line packs four whole data structures into one bullet, which tells you how shallow that part will be.</p>
<ul>
<li><strong>9.1 Arrays</strong> (slides 4–9) — introduction, loops, multi-dimensional arrays, memory layout, operations. Five slides, and the only part of the chapter where the exam asks you to <em>calculate</em> rather than recall.</li>
<li><strong>9.2 Records</strong> (slides 10–14) — record and field, record name versus field name, records compared with arrays, arrays of records. Four content slides, all conceptual.</li>
<li><strong>9.3 Linked List</strong> (slides 15–20) — data plus link, arrays versus linked lists, list name versus node names, searching, inserting. Slides 1–19 of this walkthrough stop inside this section, at searching.</li>
<li><strong>9.4 Introduction: Stack, Queue, Tree, graph</strong> (slides 21–37) — abstract data types and four of them at a gallop. That is Part B.</li>
<li><strong>What this Part A covers</strong> — sections 9.1 and 9.2 in full, plus the first three slides of 9.3. In the FLM session plan this is <strong>session 39</strong>, aligned with <strong>CLO9</strong> and guiding questions <strong>CQ13.3</strong> and <strong>CQ13.4</strong>.</li>
<li><strong>Proportions to notice</strong> — arrays and records get nine slides between them for two simple ideas, while section 9.4 gets seventeen slides for four hard ones. Budget your revision the other way round: the marks per slide are far higher in 9.1.</li>
</ul>
<p class="pitfall">⚠️ The content slide says "Linked List" (section 9.3) but the chapter in Forouzan is Chapter 11 and its sections are numbered 11.1–11.4. Every figure in this deck says "Figure 9.x" in its caption while the body text still says "Figure 11.3", "Figure 11.5", "Figure 11.11". They are the same pictures under two numbering schemes — do not waste time hunting for a missing Figure 11.3.</p>`,
        `<p class="y-chinh">🎯 Bốn mục. Hãy đọc slide này như bản đồ của cả chương — và để ý dòng cuối nhét trọn bốn cấu trúc dữ liệu vào một gạch đầu dòng, đủ cho bạn biết phần đó sẽ nông tới mức nào.</p>
<ul>
<li><strong>9.1 Arrays</strong> (slide 4–9) — giới thiệu, vòng lặp, mảng nhiều chiều, bố trí bộ nhớ, các thao tác. Năm slide, và là phần DUY NHẤT của chương mà đề thi bắt bạn <em>tính</em> chứ không chỉ nhớ.</li>
<li><strong>9.2 Records</strong> (slide 10–14) — record và trường, tên bản ghi so với tên trường, so sánh record với mảng, mảng các record. Bốn slide nội dung, đều thiên về khái niệm.</li>
<li><strong>9.3 Linked List</strong> (slide 15–20) — dữ liệu cộng liên kết, mảng so với danh sách liên kết, tên danh sách so với tên nút, tìm kiếm, chèn. Loạt bài Phần A này dừng lại giữa mục ấy, ở phần tìm kiếm.</li>
<li><strong>9.4 Introduction: Stack, Queue, Tree, graph</strong> (slide 21–37) — kiểu dữ liệu trừu tượng và bốn cấu trúc lướt qua rất nhanh. Đó là Phần B.</li>
<li><strong>Phần A này phủ tới đâu</strong> — trọn mục 9.1 và 9.2, cộng ba slide đầu của 9.3. Theo lịch buổi của FLM đây là <strong>buổi 39</strong>, ứng với <strong>CLO9</strong> và hai câu hỏi định hướng <strong>CQ13.3</strong>, <strong>CQ13.4</strong>.</li>
<li><strong>Tỷ lệ đáng để ý</strong> — mảng và record chia nhau chín slide cho hai ý đơn giản, trong khi mục 9.4 lấy mười bảy slide cho bốn ý khó. Hãy chia thời gian ôn theo chiều ngược lại: số điểm trên mỗi slide ở mục 9.1 cao hơn hẳn.</li>
</ul>
<p class="pitfall">⚠️ Slide mục lục ghi "Linked List" là mục 9.3, nhưng trong Forouzan đây là Chương 11 và các mục đánh số 11.1–11.4. Mọi hình trong bộ slide này có chú thích "Figure 9.x" trong khi thân chữ vẫn viết "Figure 11.3", "Figure 11.5", "Figure 11.11". Chúng là cùng những bức hình dưới hai hệ đánh số — đừng mất thời gian đi tìm một "Figure 11.3" không tồn tại trong deck.</p>`],

      [3, 'Objectives',
        `<p class="y-chinh">🎯 Ten objectives, and they read as ten exam questions. Count the verbs: <em>define</em> ×4, <em>distinguish</em> ×2, <em>describe</em> ×2, <em>compare</em> ×1, <em>define the applications</em> ×1. Six of the ten are pure recall — and the two "distinguish" lines are the ones students actually lose marks on.</p>
<ul>
<li><strong>"Define a data structure"</strong> — one sentence: a collection of related data items stored under one name, with defined operations. Everything else in the chapter is an instance of it.</li>
<li><strong>"Define an array … how it is used to store a list of data items"</strong> — the key words are <em>list</em> and <em>same data type</em>. Slide 5.</li>
<li><strong>"Distinguish between the name of an array and the names of the elements"</strong> — the array is called <code>scores</code>; the elements are <code>scores[1]</code>, <code>scores[2]</code> … There is exactly <em>one</em> name declared and <em>N</em> derived names. Slides 5 and 12 and 18 all repeat this same distinction for the three structures — the deck clearly considers it the central idea of the chapter.</li>
<li><strong>"Describe operations defined for an array"</strong> — searching, insertion, deletion, retrieval, traversal. Five words, slide 9, guaranteed marks.</li>
<li><strong>"Define a record … attributes belonging to a single data element"</strong> — note the phrase <em>single data element</em>. A record describes one thing; an array holds many. Slide 11.</li>
<li><strong>"Compare and contrast arrays, records, and linked lists"</strong> — this is the one worth preparing as a table, not as prose. Slides 13 and 17 give you two thirds of it.</li>
<li><strong>"Define the applications"</strong> — arrays when you need the <em>i</em>-th item instantly; records when you need the named parts of one item; linked lists when the size changes constantly.</li>
</ul>
<p class="meo">💡 Turn objectives 3, 6 and 9 ("distinguish the name of X from the names of its parts") into one flashcard with three rows. The deck asks that same question three times in three costumes: array name vs <code>scores[i]</code>, record name vs <code>student.id</code>, list name vs <code>(*cur).data</code>.</p>`,
        `<p class="y-chinh">🎯 Mười mục tiêu, và chúng đọc lên như mười câu hỏi thi. Đếm động từ: <em>define</em> ×4, <em>distinguish</em> ×2, <em>describe</em> ×2, <em>compare</em> ×1, <em>define the applications</em> ×1. Sáu trong mười là thuần nhớ lại — còn hai dòng "distinguish" mới là chỗ sinh viên thật sự mất điểm.</p>
<ul>
<li><strong>"Định nghĩa cấu trúc dữ liệu"</strong> — một câu: một tập hợp các mục dữ liệu có liên quan, lưu dưới một tên, kèm các thao tác đã định nghĩa. Mọi thứ còn lại của chương chỉ là một trường hợp cụ thể của nó.</li>
<li><strong>"Định nghĩa mảng … dùng nó để lưu một danh sách mục dữ liệu"</strong> — hai từ khoá là <em>danh sách</em> và <em>cùng kiểu dữ liệu</em>. Slide 5.</li>
<li><strong>"Phân biệt TÊN CỦA MẢNG với TÊN CÁC PHẦN TỬ"</strong> — mảng tên là <code>scores</code>; các phần tử tên là <code>scores[1]</code>, <code>scores[2]</code>… Có đúng <em>một</em> cái tên được khai báo và <em>N</em> cái tên dẫn xuất ra. Slide 5, 12 và 18 lặp lại đúng phép phân biệt này cho cả ba cấu trúc — rõ ràng deck coi đây là ý trung tâm của chương.</li>
<li><strong>"Mô tả các thao tác định nghĩa trên mảng"</strong> — tìm kiếm, chèn, xoá, lấy ra, duyệt. Năm chữ, slide 9, điểm cầm chắc.</li>
<li><strong>"Định nghĩa record … các thuộc tính thuộc về MỘT phần tử dữ liệu"</strong> — để ý cụm <em>một phần tử dữ liệu</em>. Record mô tả một vật; mảng chứa nhiều vật. Slide 11.</li>
<li><strong>"So sánh mảng, record và danh sách liên kết"</strong> — đây là mục đáng chuẩn bị dưới dạng BẢNG chứ không phải văn xuôi. Slide 13 và 17 cho bạn hai phần ba cái bảng ấy.</li>
<li><strong>"Định nghĩa ứng dụng"</strong> — mảng khi cần lấy ngay phần tử thứ <em>i</em>; record khi cần các phần có tên của một vật; danh sách liên kết khi kích thước thay đổi liên tục.</li>
</ul>
<p class="meo">💡 Gộp mục tiêu 3, 6 và 9 ("phân biệt tên của X với tên các phần của nó") thành MỘT thẻ ghi nhớ ba dòng. Deck hỏi đúng một câu hỏi ấy ba lần trong ba bộ áo: tên mảng với <code>scores[i]</code>, tên record với <code>student.id</code>, tên danh sách với <code>(*cur).data</code>.</p>`],

      [4, '1 - Arrays',
        `<p class="y-chinh">🎯 Section divider for 9.1. Five slides follow, and they are the ones with actual arithmetic in them — the address formula on this page is the single most reliable exam calculation in the whole chapter.</p>
<ul>
<li><strong>Why an array at all</strong> — Forouzan opens the chapter with 100 student scores. Without an array you declare <code>score1, score2, … score100</code>: one hundred declarations, one hundred read statements, and no way to write "the <em>i</em>-th score". With an array it is one declaration and a three-line loop. That is the whole argument.</li>
<li><strong>The property that makes an array special</strong> — elements are stored <em>contiguously</em>, all the same size. Therefore the machine can jump straight to element <em>i</em> by arithmetic instead of searching for it. That is called <em>random access</em> and it costs the same for element 0 and element 99.</li>
<li><strong>The formula, once, for the whole section</strong> — <code>address = base + (i − first_index) × element_size</code>. Three symbols to remember: where the array starts, which index counts as the first, and how many bytes one element takes.</li>
<li><strong>Measured proof that it is arithmetic and nothing else</strong> — declare <code>int scores[10];</code> in C and print <code>&amp;scores[i]</code>. Real run, clang on arm64: base <code>0x16b0a2390</code>, then the offsets from base come out <code>0, 4, 8, 12, 16, 20, 24, 28, 32, 36</code>. Exactly <em>i</em> × 4, with <code>sizeof(int)</code> measured as 4.</li>
<li><strong>The price of that speed</strong> — the size must be fixed in advance and insertion in the middle costs a shift of everything after it (slide 9). The linked list of section 9.3 pays the opposite price.</li>
</ul>
<p class="meo">💡 Write the address formula on the top corner of your exam scratch paper before you read the questions. In every CSI106 final there is at least one "what is the address of element X?" and it is four seconds of work <em>if</em> you did not have to reconstruct the formula under pressure.</p>`,
        `<p class="y-chinh">🎯 Slide phân mục cho 9.1. Năm slide tiếp theo là những slide thật sự có phép tính — và công thức địa chỉ trên trang này là phép tính thi đáng tin cậy nhất của cả chương.</p>
<ul>
<li><strong>Vì sao phải có mảng</strong> — Forouzan mở chương bằng 100 điểm thi của sinh viên. Không có mảng thì bạn khai báo <code>score1, score2, … score100</code>: một trăm dòng khai báo, một trăm lệnh đọc, và không cách nào viết được "điểm thứ <em>i</em>". Có mảng thì một dòng khai báo cộng một vòng lặp ba dòng. Toàn bộ lập luận nằm ở đó.</li>
<li><strong>Tính chất làm nên mảng</strong> — các phần tử nằm <em>liền kề</em> nhau, và mọi phần tử CÙNG kích thước. Nhờ vậy máy nhảy thẳng tới phần tử thứ <em>i</em> bằng phép tính chứ không phải đi tìm. Đó gọi là <em>truy cập ngẫu nhiên</em>, và nó tốn y hệt nhau cho phần tử 0 lẫn phần tử 99.</li>
<li><strong>Công thức, ghi một lần cho cả mục</strong> — <code>địa chỉ = base + (i − chỉ_số_đầu) × kích_thước</code>. Ba thứ phải nhớ: mảng bắt đầu ở đâu, chỉ số nào được tính là đầu tiên, và một phần tử chiếm bao nhiêu byte.</li>
<li><strong>Bằng chứng ĐO THẬT rằng nó chỉ là phép tính</strong> — khai báo <code>int scores[10];</code> trong C rồi in <code>&amp;scores[i]</code>. Chạy thật, clang trên arm64: base <code>0x16b0a2390</code>, và khoảng cách tới base lần lượt là <code>0, 4, 8, 12, 16, 20, 24, 28, 32, 36</code>. Đúng bằng <em>i</em> × 4, với <code>sizeof(int)</code> đo được là 4.</li>
<li><strong>Cái giá của tốc độ ấy</strong> — kích thước phải cố định từ trước, và chèn vào giữa thì phải dịch toàn bộ phần đứng sau (slide 9). Danh sách liên kết ở mục 9.3 trả cái giá ngược lại.</li>
</ul>
<p class="meo">💡 Hãy ghi công thức địa chỉ lên góc trên tờ nháp TRƯỚC khi đọc đề. Đề cuối kỳ CSI106 nào cũng có ít nhất một câu "địa chỉ của phần tử X là bao nhiêu?", và nó tốn bốn giây <em>nếu</em> bạn không phải dựng lại công thức trong lúc căng thẳng.</p>`],

      [5, '1. Introduction (arrays)',
        `<p class="y-chinh">🎯 The definition: <strong>an array is a sequenced collection of elements of the same data type</strong>. Figure 9.1 draws it as a vertical stack of boxes labelled <code>scores[1]</code>, <code>scores[2]</code>, …, <code>scores[100]</code>, with the whole stack labelled <code>scores</code> at the bottom.</p>
<ul>
<li><strong>Three words, each doing work</strong> — <em>sequenced</em>: there is a first, a second, a last, and the order is part of the structure. <em>Collection</em>: many items under one name. <em>Same data type</em>: this is what makes every element the same number of bytes, which is what makes the address formula possible.</li>
<li><strong>The name/subscript distinction, which is objective 3</strong> — "The array as a whole has a name, <code>scores</code>, but each score can be accessed individually using its subscript." One declared identifier, one hundred addressable cells.</li>
<li><strong>Forouzan counts from 1, C counts from 0</strong> — the figure says <code>scores[1] … scores[100]</code>. Your C compiler says <code>scores[0] … scores[99]</code>. The textbook uses 1 because it is talking about the <em>abstract</em> structure; C uses 0 because the index is literally an offset from the base address. Both appear in exams, and the formula handles both.</li>
<li><strong>Worked example 1 — C style, first index 0.</strong> <code>int scores[100];</code> at base address 2000, <code>sizeof(int)</code> = 4 (measured). Address of <code>scores[25]</code> = 2000 + (25 − 0) × 4 = 2000 + 100 = <strong>2100</strong>. Address of <code>scores[0]</code> = 2000. Address of the last element <code>scores[99]</code> = 2000 + 99 × 4 = 2396, and the array ends at byte 2399, so it occupies exactly 400 bytes.</li>
<li><strong>Worked example 2 — Forouzan style, first index 1.</strong> Same array, same base 2000, but declared <code>scores[1..100]</code>. Address of <code>scores[25]</code> = 2000 + (25 − 1) × 4 = 2000 + 96 = <strong>2096</strong>. Note it is <em>four bytes lower</em> than in example 1 for the same index number, because index 25 is now the 25th element, not the 26th.</li>
<li><strong>Worked example 3 — a bigger element.</strong> <code>double temp[50];</code> at base 4000, <code>sizeof(double)</code> = 8 (measured: offsets of a real <code>double ds[4]</code> came out 0, 8, 16, 24). First index 0. Address of <code>temp[17]</code> = 4000 + 17 × 8 = <strong>4136</strong>. Change nothing but the type and every answer moves.</li>
</ul>
<p class="dap-an">✅ Đáp án: <code>scores[25]</code> lands at 2100 with 0-based indexing and at 2096 with 1-based indexing; <code>temp[17]</code> lands at 4136. The three numbers differ only in which <em>first_index</em> and which <em>element_size</em> you plugged in — the formula never changed.</p>
<p class="pitfall">⚠️ The commonest exam mistake is forgetting the <code>− first_index</code> term when the question says "an array whose first index is 1". If you compute 2000 + 25 × 4 = 2100 for a 1-based array you have silently answered for element number 26 and lost the mark.</p>`,
        `<p class="y-chinh">🎯 Định nghĩa: <strong>mảng là một tập hợp CÓ THỨ TỰ các phần tử CÙNG kiểu dữ liệu</strong>. Hình 9.1 vẽ nó thành một chồng ô dọc mang nhãn <code>scores[1]</code>, <code>scores[2]</code>, …, <code>scores[100]</code>, cả chồng ấy mang tên <code>scores</code> ghi ở dưới.</p>
<ul>
<li><strong>Ba từ, từ nào cũng gánh việc</strong> — <em>có thứ tự</em>: có phần tử đầu, phần tử thứ hai, phần tử cuối, và thứ tự ấy là một phần của cấu trúc. <em>Tập hợp</em>: nhiều mục dưới một tên. <em>Cùng kiểu dữ liệu</em>: chính chỗ này làm mọi phần tử chiếm đúng bằng nhau số byte, và chính nó làm công thức địa chỉ tồn tại được.</li>
<li><strong>Phân biệt tên mảng với chỉ số, đúng mục tiêu số 3</strong> — "Cả mảng có một cái tên, <code>scores</code>, nhưng từng điểm được truy cập riêng lẻ qua chỉ số của nó." Một định danh được khai báo, một trăm ô có địa chỉ.</li>
<li><strong>Forouzan đếm từ 1, C đếm từ 0</strong> — hình ghi <code>scores[1] … scores[100]</code>. Trình biên dịch C của bạn thì ghi <code>scores[0] … scores[99]</code>. Giáo trình dùng 1 vì nó đang nói về cấu trúc <em>trừu tượng</em>; C dùng 0 vì chỉ số ở đó chính là độ lệch so với địa chỉ nền. Cả hai đều ra thi, và công thức xử được cả hai.</li>
<li><strong>Bài mẫu 1 — kiểu C, chỉ số đầu là 0.</strong> <code>int scores[100];</code> đặt tại địa chỉ nền 2000, <code>sizeof(int)</code> = 4 (đo thật). Địa chỉ <code>scores[25]</code> = 2000 + (25 − 0) × 4 = 2000 + 100 = <strong>2100</strong>. Địa chỉ <code>scores[0]</code> = 2000. Địa chỉ phần tử cuối <code>scores[99]</code> = 2000 + 99 × 4 = 2396, và mảng kết thúc ở byte 2399, tức chiếm đúng 400 byte.</li>
<li><strong>Bài mẫu 2 — kiểu Forouzan, chỉ số đầu là 1.</strong> Cùng mảng ấy, cùng nền 2000, nhưng khai báo <code>scores[1..100]</code>. Địa chỉ <code>scores[25]</code> = 2000 + (25 − 1) × 4 = 2000 + 96 = <strong>2096</strong>. Để ý nó <em>thấp hơn bốn byte</em> so với bài 1 dù cùng con số chỉ số, vì chỉ số 25 bây giờ là phần tử thứ 25 chứ không phải thứ 26.</li>
<li><strong>Bài mẫu 3 — phần tử to hơn.</strong> <code>double temp[50];</code> nền 4000, <code>sizeof(double)</code> = 8 (đo thật: khoảng cách của một mảng <code>double ds[4]</code> chạy thật ra 0, 8, 16, 24). Chỉ số đầu là 0. Địa chỉ <code>temp[17]</code> = 4000 + 17 × 8 = <strong>4136</strong>. Không đổi gì ngoài kiểu dữ liệu mà mọi đáp số đều dịch đi.</li>
</ul>
<p class="dap-an">✅ Đáp án: <code>scores[25]</code> rơi vào 2100 nếu đánh chỉ số từ 0, và rơi vào 2096 nếu đánh từ 1; <code>temp[17]</code> rơi vào 4136. Ba con số chỉ khác nhau ở chỗ bạn thay <em>chỉ_số_đầu</em> nào và <em>kích_thước</em> nào vào — công thức thì chưa hề đổi.</p>
<p class="pitfall">⚠️ Lỗi thi hay gặp nhất là quên số hạng <code>− chỉ_số_đầu</code> khi đề nói "mảng có chỉ số đầu là 1". Nếu bạn tính 2000 + 25 × 4 = 2100 cho một mảng đánh từ 1, bạn đã âm thầm trả lời cho phần tử thứ 26 và mất điểm.</p>`],

      [6, '2. Loops',
        `<p class="y-chinh">🎯 The payoff of having an array: <strong>a loop can touch every element without you writing every element</strong>. "Now it does not matter if there are 100, 1000, or 10,000 elements — loops make it easy to handle them all."</p>
<ul>
<li><strong>What Figure 9.2 actually draws</strong> — a UML activity diagram with <em>three</em> consecutive loops, not one. Each has the identical skeleton: <code>i ← 1</code>, then a red diamond testing <code>[i &gt; 100]</code>, then a body, then <code>i ← i + 1</code> looping back. The three bodies are <code>Read scores[i]</code>, then <code>Process scores[i]</code> (highlighted yellow), then <code>Write scores[i]</code>. Start at the top, Stop at the bottom.</li>
<li><strong>Read → process → write is the shape of nearly every array program</strong> — fill it, work on it, report it. Recognising that skeleton is worth more than memorising the picture.</li>
<li><strong>The exit test is the interesting part</strong> — the slide's words say "remain in the loop as long as the value of this variable is <em>less than</em> the total number of elements", but the diagram tests <code>i &gt; 100</code> to <em>leave</em>. Those two are the same rule stated from opposite sides: stay while <code>i ≤ 100</code>, leave when <code>i &gt; 100</code>.</li>
<li><strong>Translated to C, with the index base changed</strong> — the diagram is 1-based, so it runs <code>for (i = 1; i &lt;= 100; i++)</code>. In C you write <code>for (i = 0; i &lt; 100; i++)</code>. Count the iterations both ways: 100 ← 1 + 1 = 100 and 99 − 0 + 1 = 100. Same number of passes, different numbers written down.</li>
<li><strong>Why three separate loops and not one</strong> — because the read pass must finish before the process pass can compute anything that depends on <em>all</em> the data (an average, a maximum, a sort). If the processing were per-element and independent, one loop would do. Exam questions like to ask exactly this.</li>
<li><strong>Cost, stated honestly</strong> — one loop over <em>n</em> elements is O(<em>n</em>). Three loops in sequence is O(3<em>n</em>) = O(<em>n</em>) — still linear. Three <em>nested</em> loops would be O(<em>n</em><sup>3</sup>). Sequence adds; nesting multiplies.</li>
</ul>
<p class="dap-an">✅ Đáp án: the three loops shown execute the diamond test 101 times each (100 times taking the body, once taking the exit), so 303 tests and 300 body executions for a 100-element array.</p>
<p class="pitfall">⚠️ The classic off-by-one: writing <code>for (i = 1; i &lt;= 100; i++)</code> in C for an array declared <code>int scores[100];</code>. That skips <code>scores[0]</code> and touches <code>scores[100]</code>, which is one element past the end — undefined behaviour, and the bug that produces "it works on my machine". The diagram is 1-based <em>pseudocode</em>; C is not.</p>`,
        `<p class="y-chinh">🎯 Phần thưởng của việc có mảng: <strong>một vòng lặp chạm được tới mọi phần tử mà bạn không phải viết ra từng phần tử</strong>. "Giờ thì 100, 1000 hay 10.000 phần tử cũng không còn quan trọng — vòng lặp làm việc đó dễ dàng."</p>
<ul>
<li><strong>Hình 9.2 thật sự vẽ gì</strong> — một sơ đồ hoạt động UML với <em>BA</em> vòng lặp nối tiếp, không phải một. Cả ba cùng một bộ xương: <code>i ← 1</code>, rồi một hình thoi đỏ kiểm <code>[i &gt; 100]</code>, rồi thân vòng lặp, rồi <code>i ← i + 1</code> quay lại. Ba cái thân lần lượt là <code>Read scores[i]</code>, rồi <code>Process scores[i]</code> (tô vàng), rồi <code>Write scores[i]</code>. Start ở trên cùng, Stop ở dưới cùng.</li>
<li><strong>Đọc → xử lý → ghi là hình dáng của gần như mọi chương trình mảng</strong> — nạp đầy nó, làm việc trên nó, báo kết quả. Nhận ra bộ xương ấy đáng giá hơn học thuộc bức hình.</li>
<li><strong>Điều kiện thoát mới là chỗ thú vị</strong> — chữ trên slide nói "ở lại vòng lặp CHỪNG NÀO giá trị biến còn <em>nhỏ hơn</em> tổng số phần tử", nhưng sơ đồ lại kiểm <code>i &gt; 100</code> để <em>thoát</em>. Hai cái đó là cùng một luật phát biểu từ hai phía: ở lại khi <code>i ≤ 100</code>, thoát khi <code>i &gt; 100</code>.</li>
<li><strong>Dịch sang C, đổi gốc chỉ số</strong> — sơ đồ đánh từ 1 nên nó chạy <code>for (i = 1; i &lt;= 100; i++)</code>. Trong C bạn viết <code>for (i = 0; i &lt; 100; i++)</code>. Đếm số vòng theo cả hai cách: 100 − 1 + 1 = 100 và 99 − 0 + 1 = 100. Cùng số lượt chạy, khác con số viết ra.</li>
<li><strong>Vì sao ba vòng riêng chứ không gộp một</strong> — vì lượt đọc phải xong thì lượt xử lý mới tính được thứ phụ thuộc vào <em>toàn bộ</em> dữ liệu (trung bình, lớn nhất, sắp xếp). Nếu việc xử lý chỉ làm trên từng phần tử độc lập thì một vòng là đủ. Đề thi rất thích hỏi đúng chỗ này.</li>
<li><strong>Chi phí, nói cho thật</strong> — một vòng lặp qua <em>n</em> phần tử là O(<em>n</em>). Ba vòng nối tiếp là O(3<em>n</em>) = O(<em>n</em>) — vẫn tuyến tính. Ba vòng <em>lồng nhau</em> thì mới là O(<em>n</em><sup>3</sup>). Nối tiếp thì CỘNG; lồng nhau thì NHÂN.</li>
</ul>
<p class="dap-an">✅ Đáp án: ba vòng lặp trên chạy phép kiểm ở hình thoi 101 lần mỗi vòng (100 lần đi vào thân, 1 lần đi ra), tức 303 phép kiểm và 300 lượt thân cho mảng 100 phần tử.</p>
<p class="pitfall">⚠️ Lỗi lệch một kinh điển: viết <code>for (i = 1; i &lt;= 100; i++)</code> trong C cho mảng khai báo <code>int scores[100];</code>. Nó bỏ sót <code>scores[0]</code> và chạm vào <code>scores[100]</code> — vượt quá cuối mảng một phần tử, hành vi không xác định, và đúng là loại lỗi sinh ra câu "máy em chạy được mà". Sơ đồ là <em>mã giả</em> đánh từ 1; C thì không.</p>`],

      [7, '3. Multi-dimensional arrays',
        `<p class="y-chinh">🎯 One dimension is a line; two dimensions is a table. Figure 9.3 draws a grid of <strong>5 rows × 4 columns</strong> with rows labelled [1]…[5] down the left ("First dimension (rows)") and columns [1]…[4] across the top ("Second dimension (columns)"), and an arrow pointing at one cell labelled <code>scores[2][3]</code>.</p>
<ul>
<li><strong>Read the notation off the figure carefully</strong> — <code>scores[2][3]</code> means row 2, column 3. Row index always comes first. The arrow in the figure points to the cell in the second row, third column, which confirms it.</li>
<li><strong>What a two-dimensional array is really for</strong> — data that is naturally indexed by two things: student × subject, day × hour, pixel row × pixel column. If your data has two "which one?" questions, it wants two dimensions.</li>
<li><strong>How many elements, and how much memory</strong> — 5 × 4 = 20 elements. If each is an <code>int</code> of 4 bytes that is 80 bytes. Measured, not guessed: a real <code>int m[5][4]</code> compiled with <code>cc -Wall</code> reports <code>sizeof(m)</code> = <strong>80</strong>, and a real <code>int a[2][4]</code> reports <code>sizeof(a)</code> = 32 with <code>sizeof(a[0])</code> = 16 — that is, one <em>row</em> is itself a 4-element array of 16 bytes.</li>
<li><strong>The declaration in C, and where students trip</strong> — <code>int scores[5][4];</code> declares 5 rows of 4. Valid indices are <code>scores[0][0]</code> to <code>scores[4][3]</code>. The figure's <code>scores[2][3]</code> is a valid C index too, but it is a <em>different cell</em> from the figure's — in C it is row 3, column 4 counting humanly.</li>
<li><strong>Worked example — element count from indices.</strong> In a 1-based 5 × 4 table, how many cells come before <code>scores[2][3]</code> when you read row by row? A full first row (4 cells) plus two cells of row 2 = 6 cells. So it is the 7th element. In 0-based C the same "7th element" is <code>a[1][2]</code>. Slide 8 turns that count into a byte address.</li>
<li><strong>Beyond two dimensions</strong> — the idea generalises: <code>int cube[3][4][5]</code> is 60 elements, and the address formula gains one more nested term. CSI106 only examines one and two dimensions.</li>
</ul>
<p class="dap-an">✅ Đáp án: the grid holds 5 × 4 = 20 elements, 80 bytes as <code>int</code> (measured <code>sizeof</code> = 80); <code>scores[2][3]</code> in the figure's 1-based scheme is the 7th cell in row-major reading order.</p>
<p class="meo">💡 Remember the order with the phrase <strong>"row first, like reading an address: street then house number"</strong>. And remember that in C a 2-D array is genuinely an <em>array of arrays</em>: <code>a[1]</code> alone is a whole row, which is why <code>sizeof(a[0])</code> came out 16 and not 4.</p>`,
        `<p class="y-chinh">🎯 Một chiều là một đường thẳng; hai chiều là một cái bảng. Hình 9.3 vẽ lưới <strong>5 hàng × 4 cột</strong>, hàng đánh [1]…[5] dọc bên trái ("First dimension (rows)") và cột đánh [1]…[4] ngang phía trên ("Second dimension (columns)"), kèm một mũi tên chỉ vào một ô mang nhãn <code>scores[2][3]</code>.</p>
<ul>
<li><strong>Đọc kỹ ký hiệu trên hình</strong> — <code>scores[2][3]</code> nghĩa là hàng 2, cột 3. Chỉ số hàng LUÔN đứng trước. Mũi tên trong hình chỉ vào ô hàng hai cột ba, đúng như vậy.</li>
<li><strong>Mảng hai chiều thật ra để làm gì</strong> — cho dữ liệu tự nhiên được đánh chỉ mục bằng HAI thứ: sinh viên × môn học, ngày × giờ, hàng điểm ảnh × cột điểm ảnh. Dữ liệu của bạn có hai câu hỏi "cái nào?" thì nó muốn hai chiều.</li>
<li><strong>Bao nhiêu phần tử, tốn bao nhiêu bộ nhớ</strong> — 5 × 4 = 20 phần tử. Mỗi phần tử là <code>int</code> 4 byte thì thành 80 byte. Đo thật, không đoán: một mảng <code>int m[5][4]</code> biên dịch bằng <code>cc -Wall</code> báo <code>sizeof(m)</code> = <strong>80</strong>, và <code>int a[2][4]</code> báo <code>sizeof(a)</code> = 32 với <code>sizeof(a[0])</code> = 16 — tức MỘT HÀNG tự nó đã là một mảng 4 phần tử chiếm 16 byte.</li>
<li><strong>Khai báo trong C, và chỗ sinh viên hay vấp</strong> — <code>int scores[5][4];</code> khai báo 5 hàng mỗi hàng 4 ô. Chỉ số hợp lệ chạy từ <code>scores[0][0]</code> tới <code>scores[4][3]</code>. Ký hiệu <code>scores[2][3]</code> của hình cũng là chỉ số C hợp lệ, nhưng nó là một <em>ô KHÁC</em> với ô trong hình — trong C nó là hàng thứ 3, cột thứ 4 nếu đếm theo kiểu người.</li>
<li><strong>Bài mẫu — đếm phần tử từ chỉ số.</strong> Trong bảng 5 × 4 đánh từ 1, có bao nhiêu ô đứng trước <code>scores[2][3]</code> nếu đọc lần lượt theo hàng? Trọn hàng một (4 ô) cộng hai ô của hàng hai = 6 ô. Vậy nó là phần tử thứ 7. Trong C đánh từ 0, đúng "phần tử thứ 7" ấy là <code>a[1][2]</code>. Slide 8 biến phép đếm này thành địa chỉ byte.</li>
<li><strong>Nhiều hơn hai chiều</strong> — ý tưởng tổng quát hoá được: <code>int cube[3][4][5]</code> là 60 phần tử, và công thức địa chỉ mọc thêm một số hạng lồng nữa. CSI106 chỉ thi một và hai chiều.</li>
</ul>
<p class="dap-an">✅ Đáp án: lưới chứa 5 × 4 = 20 phần tử, 80 byte nếu là <code>int</code> (đo thật <code>sizeof</code> = 80); <code>scores[2][3]</code> theo cách đánh từ 1 của hình là ô thứ 7 khi đọc theo thứ tự row-major.</p>
<p class="meo">💡 Nhớ thứ tự bằng câu <strong>"hàng trước, như đọc địa chỉ: tên đường rồi mới tới số nhà"</strong>. Và nhớ rằng trong C mảng hai chiều thật sự là <em>mảng của các mảng</em>: riêng <code>a[1]</code> đã là cả một hàng, đó là lý do <code>sizeof(a[0])</code> đo ra 16 chứ không phải 4.</p>`],

      [8, '4. Memory layout',
        `<p class="y-chinh">🎯 The most examinable slide of the chapter. Memory is one-dimensional; a table is two-dimensional; something has to flatten it. Two ways exist: <strong>row-major</strong> (a whole row before the next row) and <strong>column-major</strong> (a whole column before the next column).</p>
<ul>
<li><strong>What Figure 9.4 shows exactly</strong> — in the middle, the "User's View": a 2 × 4 table holding 'A' 'B' 'C' 'D' in row [1] and 'E' 'F' 'G' 'H' in row [2]. On the left, "Row-major storage" as a vertical strip reading <strong>A B C D E F G H</strong> with brackets marking "Row 1" and "Row 2". On the right, "Column-major storage" as a strip reading <strong>A E B F C G D H</strong> with brackets marking Column 1 to Column 4.</li>
<li><strong>The two formulas, for a table of <em>R</em> rows × <em>C</em> columns, first index 0</strong> — row-major: <code>addr = base + (i × C + j) × size</code>; column-major: <code>addr = base + (j × R + i) × size</code>. Note which dimension count appears: row-major multiplies by the number of <em>columns</em>, column-major by the number of <em>rows</em>. That swap is the whole difference.</li>
<li><strong>With 1-based indices</strong> — subtract 1 from each index first: row-major <code>base + ((i−1) × C + (j−1)) × size</code>. Nothing else changes.</li>
<li><strong>Worked example — the same element, two layouts.</strong> Take <code>int m[5][4]</code> (R = 5, C = 4, size = 4, 0-based) and ask for <code>m[3][2]</code>. Row-major: (3 × 4 + 2) × 4 = 14 × 4 = <strong>base + 56</strong>. Column-major: (2 × 5 + 3) × 4 = 13 × 4 = <strong>base + 52</strong>. Same cell, two different addresses, four bytes apart.</li>
<li><strong>Proof that C is row-major, from a real run.</strong> The program declared <code>int m[5][4];</code>, printed <code>(char*)&amp;m[3][2] − (char*)m</code>, and got <strong>56</strong> — the row-major answer, not 52. A second check on <code>m[1][3]</code>: measured <strong>28</strong>; row-major predicts (1 × 4 + 3) × 4 = 28; column-major predicts (3 × 5 + 1) × 4 = 64. Row-major wins again. A third check dumped <code>char t[2][4] = {{'A','B','C','D'},{'E','F','G','H'}}</code> byte by byte through a <code>char*</code> and printed <strong>ABCDEFGH</strong> — exactly the left-hand strip of Figure 9.4.</li>
<li><strong>Which languages do which</strong> — row-major: C, C++, Java, Python (NumPy default), Pascal. Column-major: Fortran, MATLAB, R, Julia. The slide's "depends on the computer" is really "depends on the language"; no modern CPU has an opinion about it.</li>
<li><strong>Why anyone should care</strong> — cache. Walking a big matrix along rows in a row-major language touches consecutive bytes and the cache prefetcher keeps up; walking it along columns jumps by a whole row each step and can be several times slower for the identical arithmetic. Same result, same instruction count, different wall clock.</li>
</ul>
<p class="dap-an">✅ Đáp án: <code>m[3][2]</code> sits at base + 56 under row-major and base + 52 under column-major. The C compiler measured <strong>56</strong>, so <strong>C stores two-dimensional arrays in row-major order</strong>. The slide's claim that "most computers use row-major storage" is confirmed for C but is really a statement about languages, not hardware.</p>
<p class="pitfall">⚠️ Two traps in one slide. (1) Using the wrong multiplier — row-major multiplies the row index by the number of <em>columns</em>; writing <code>i × R</code> is the single most common error here. (2) Forgetting the element size at the end: <code>(i × C + j)</code> counts <em>elements</em>, and you must still multiply by bytes per element. An answer of "base + 14" instead of "base + 56" is that mistake.</p>`,
        `<p class="y-chinh">🎯 Slide ra đề nhiều nhất của cả chương. Bộ nhớ là một chiều; cái bảng là hai chiều; phải có thứ gì đó BẸT nó ra. Có hai cách: <strong>row-major</strong> (trọn một hàng rồi mới tới hàng sau) và <strong>column-major</strong> (trọn một cột rồi mới tới cột sau).</p>
<ul>
<li><strong>Hình 9.4 vẽ chính xác cái gì</strong> — ở giữa là "User's View": bảng 2 × 4 chứa 'A' 'B' 'C' 'D' ở hàng [1] và 'E' 'F' 'G' 'H' ở hàng [2]. Bên trái là "Row-major storage" — một dải dọc đọc ra <strong>A B C D E F G H</strong>, có ngoặc đánh dấu "Row 1" và "Row 2". Bên phải là "Column-major storage" — dải đọc ra <strong>A E B F C G D H</strong>, có ngoặc đánh dấu Column 1 tới Column 4.</li>
<li><strong>Hai công thức, cho bảng <em>R</em> hàng × <em>C</em> cột, chỉ số đầu là 0</strong> — row-major: <code>addr = base + (i × C + j) × size</code>; column-major: <code>addr = base + (j × R + i) × size</code>. Để ý con số chiều nào xuất hiện: row-major nhân với số <em>CỘT</em>, column-major nhân với số <em>HÀNG</em>. Cú hoán vị ấy là toàn bộ khác biệt.</li>
<li><strong>Nếu chỉ số đánh từ 1</strong> — trừ 1 vào từng chỉ số trước đã: row-major thành <code>base + ((i−1) × C + (j−1)) × size</code>. Ngoài ra không đổi gì.</li>
<li><strong>Bài mẫu — CÙNG một phần tử, hai cách xếp.</strong> Lấy <code>int m[5][4]</code> (R = 5, C = 4, size = 4, đánh từ 0) và hỏi <code>m[3][2]</code>. Row-major: (3 × 4 + 2) × 4 = 14 × 4 = <strong>base + 56</strong>. Column-major: (2 × 5 + 3) × 4 = 13 × 4 = <strong>base + 52</strong>. Cùng một ô, hai địa chỉ khác nhau, lệch nhau bốn byte.</li>
<li><strong>Chứng minh C dùng row-major, bằng số đo thật.</strong> Chương trình khai báo <code>int m[5][4];</code>, in <code>(char*)&amp;m[3][2] − (char*)m</code>, và nhận <strong>56</strong> — đáp số row-major, không phải 52. Kiểm lần hai với <code>m[1][3]</code>: đo được <strong>28</strong>; row-major dự đoán (1 × 4 + 3) × 4 = 28; column-major dự đoán (3 × 5 + 1) × 4 = 64. Row-major lại thắng. Kiểm lần ba: đổ <code>char t[2][4] = {{'A','B','C','D'},{'E','F','G','H'}}</code> ra từng byte qua một <code>char*</code> và in ra <strong>ABCDEFGH</strong> — đúng bằng dải bên TRÁI của Hình 9.4.</li>
<li><strong>Ngôn ngữ nào theo cách nào</strong> — row-major: C, C++, Java, Python (NumPy mặc định), Pascal. Column-major: Fortran, MATLAB, R, Julia. Câu "tuỳ vào máy tính" của slide thật ra là "tuỳ vào NGÔN NGỮ"; không CPU hiện đại nào có ý kiến gì về chuyện này.</li>
<li><strong>Vì sao phải quan tâm</strong> — vì cache. Duyệt một ma trận lớn theo hàng trong ngôn ngữ row-major thì chạm vào các byte liền kề và bộ nạp trước của cache theo kịp; duyệt theo cột thì mỗi bước nhảy trọn một hàng và có thể chậm hơn vài lần dù phép tính y hệt. Cùng kết quả, cùng số lệnh, khác thời gian đồng hồ.</li>
</ul>
<p class="dap-an">✅ Đáp án: <code>m[3][2]</code> nằm ở base + 56 theo row-major và base + 52 theo column-major. Trình biên dịch C đo ra <strong>56</strong>, vậy <strong>C lưu mảng hai chiều theo row-major</strong>. Câu "hầu hết máy tính dùng row-major" của slide đúng với C, nhưng thật ra nó là phát biểu về ngôn ngữ chứ không phải về phần cứng.</p>
<p class="pitfall">⚠️ Hai bẫy trong một slide. (1) Nhân nhầm hệ số — row-major nhân chỉ số HÀNG với số <em>CỘT</em>; viết <code>i × R</code> là lỗi phổ biến nhất ở đây. (2) Quên nhân kích thước phần tử ở cuối: <code>(i × C + j)</code> mới chỉ đếm SỐ PHẦN TỬ, còn phải nhân số byte mỗi phần tử. Đáp án "base + 14" thay vì "base + 56" chính là lỗi này.</p>`],

      [9, '5. Operations on array',
        `<p class="y-chinh">🎯 Five operations define an array <em>as a data structure</em>: <strong>searching, insertion, deletion, retrieval, traversal</strong>. Figure 9.5 shows the pseudocode for the hardest of them — inserting a value into the middle of an array.</p>
<ul>
<li><strong>The distinction the first bullet makes</strong> — you can add two elements (<code>a[3] + a[4]</code>): that is an operation on the <em>elements</em>. Searching the array, or traversing it, is an operation on the <em>structure</em>. Only the second kind belongs to this chapter.</li>
<li><strong>The five, with their real costs on an array of <em>n</em> elements</strong> — retrieval O(1) by index; traversal O(<em>n</em>); searching O(<em>n</em>) sequential or O(log <em>n</em>) binary if sorted; insertion O(<em>n</em>) because everything after the hole must shift right; deletion O(<em>n</em>) for the same reason in reverse.</li>
<li><strong>The exact pseudocode on the slide</strong> — <code>i ← 30</code>; <code>while (i ≥ 9) { array[i + 1] ← array[i]; i ← i − 1 }</code>; then <code>array[i] ← newValue</code>. The intent is: open a hole at position 9 of a 30-element array by sliding elements 9…30 up one place, then drop the new value in.</li>
<li><strong>Hand-trace it.</strong> i = 30: <code>array[31] ← array[30]</code>. i = 29: <code>array[30] ← array[29]</code>. … i = 9: <code>array[10] ← array[9]</code>. The loop test then sees i = 8, which fails <code>i ≥ 9</code>, so the loop exits <strong>with i = 8</strong>. The last line therefore writes <code>array[8] ← newValue</code>.</li>
<li><strong>That is one cell too low, and it is a real bug.</strong> Verified by running it: fill a 1-based array with <code>array[k] = 10k</code> and insert 999. Printing cells 6…13 after the literal slide code gives <strong>60 70 999 90 90 100 110 120</strong> — the old value 80 has been destroyed and 90 now appears twice. The same trace with the last line changed to <code>array[i + 1] ← newValue</code> gives <strong>60 70 80 999 90 100 110 120</strong>, which is what "insert at position 9" is supposed to mean.</li>
<li><strong>Count the work</strong> — the loop body ran for i = 30 down to 9, that is 30 − 9 + 1 = <strong>22 shifts</strong> to insert one element. Insert at the front of the same array and it would be 30 shifts; insert at the very end, 0. That is exactly why insertion is O(<em>n</em>) in the worst case, and exactly why linked lists exist (slide 17).</li>
</ul>
<p class="dap-an">✅ Đáp án: the loop performs 22 shifts and exits with i = 8. The slide's final line <code>array[i] ← newValue</code> is <strong>off by one</strong> — it overwrites the element at index 8 and leaves index 9 duplicated. Measured output with the slide code: <code>… 70 999 90 90 100 …</code>; with <code>array[i + 1] ← newValue</code>: <code>… 70 80 999 90 100 …</code>. The slide is wrong here; say so if the exam quotes it, and write the corrected line.</p>
<p class="pitfall">⚠️ Notice also that the loop writes to <code>array[31]</code> while the array holds 30 elements. In pseudocode that is politely ignored; in C it is a buffer overflow. Any "insert into a full array" answer must first check that there is a spare slot — that check is missing from the slide entirely.</p>`,
        `<p class="y-chinh">🎯 Năm thao tác định nghĩa mảng <em>với tư cách một cấu trúc dữ liệu</em>: <strong>tìm kiếm, chèn, xoá, lấy ra, duyệt</strong>. Hình 9.5 đưa mã giả của thao tác khó nhất — chèn một giá trị vào giữa mảng.</p>
<ul>
<li><strong>Gạch đầu dòng đầu phân biệt điều gì</strong> — bạn cộng hai phần tử được (<code>a[3] + a[4]</code>): đó là thao tác trên <em>PHẦN TỬ</em>. Còn tìm kiếm trong mảng, hay duyệt mảng, là thao tác trên <em>CẤU TRÚC</em>. Chỉ loại thứ hai thuộc về chương này.</li>
<li><strong>Năm thao tác, kèm chi phí thật trên mảng <em>n</em> phần tử</strong> — lấy ra O(1) nhờ chỉ số; duyệt O(<em>n</em>); tìm kiếm O(<em>n</em>) nếu tuần tự, O(log <em>n</em>) nếu nhị phân trên mảng đã sắp; chèn O(<em>n</em>) vì mọi thứ sau chỗ trống phải dịch phải; xoá O(<em>n</em>) vì lý do y hệt nhưng ngược chiều.</li>
<li><strong>Mã giả chính xác trên slide</strong> — <code>i ← 30</code>; <code>while (i ≥ 9) { array[i + 1] ← array[i]; i ← i − 1 }</code>; rồi <code>array[i] ← newValue</code>. Ý đồ là: mở một chỗ trống tại vị trí 9 của mảng 30 phần tử bằng cách đẩy các phần tử 9…30 lên một ô, rồi thả giá trị mới vào.</li>
<li><strong>Chạy tay đi.</strong> i = 30: <code>array[31] ← array[30]</code>. i = 29: <code>array[30] ← array[29]</code>. … i = 9: <code>array[10] ← array[9]</code>. Rồi phép kiểm thấy i = 8, không thoả <code>i ≥ 9</code>, nên vòng lặp thoát <strong>với i = 8</strong>. Vậy dòng cuối ghi <code>array[8] ← newValue</code>.</li>
<li><strong>Thấp hơn đúng MỘT ô, và đây là lỗi thật.</strong> Đã kiểm bằng cách chạy: nạp mảng đánh từ 1 với <code>array[k] = 10k</code> rồi chèn 999. In các ô 6…13 sau khi chạy đúng nguyên văn mã của slide ra <strong>60 70 999 90 90 100 110 120</strong> — giá trị cũ 80 đã bị xoá sổ và 90 giờ xuất hiện hai lần. Cũng phép chạy ấy nhưng đổi dòng cuối thành <code>array[i + 1] ← newValue</code> cho <strong>60 70 80 999 90 100 110 120</strong>, đúng nghĩa "chèn vào vị trí 9".</li>
<li><strong>Đếm khối lượng việc</strong> — thân vòng lặp chạy từ i = 30 xuống 9, tức 30 − 9 + 1 = <strong>22 lần dịch</strong> chỉ để chèn một phần tử. Chèn vào đầu mảng ấy thì phải 30 lần dịch; chèn vào sát cuối thì 0 lần. Đó đúng là lý do chèn có chi phí O(<em>n</em>) ở trường hợp xấu nhất, và đúng là lý do danh sách liên kết tồn tại (slide 17).</li>
</ul>
<p class="dap-an">✅ Đáp án: vòng lặp thực hiện 22 lần dịch và thoát ra với i = 8. Dòng cuối <code>array[i] ← newValue</code> của slide bị <strong>lệch một ô</strong> — nó đè mất phần tử ở chỉ số 8 và để chỉ số 9 bị trùng lặp. Kết quả đo thật với mã của slide: <code>… 70 999 90 90 100 …</code>; với <code>array[i + 1] ← newValue</code>: <code>… 70 80 999 90 100 …</code>. Chỗ này SLIDE SAI; nếu đề thi trích nguyên văn thì hãy nói rõ và viết lại dòng đã sửa.</p>
<p class="pitfall">⚠️ Cũng để ý vòng lặp ghi vào <code>array[31]</code> trong khi mảng chỉ có 30 phần tử. Trong mã giả thì người ta lịch sự bỏ qua; trong C đó là tràn bộ đệm. Mọi lời giải "chèn vào mảng đã đầy" đều phải kiểm trước xem còn ô trống hay không — phép kiểm ấy hoàn toàn vắng mặt trên slide.</p>`],

      [10, '2 - Records',
        `<p class="y-chinh">🎯 Section divider for 9.2. The pivot of the whole chapter: an array holds <em>many things of one kind</em>; a record holds <em>the parts of one thing</em>, and those parts may be of different types.</p>
<ul>
<li><strong>The one-line contrast to memorise</strong> — array: many elements, same type, reached by <em>index</em>. Record: several fields, possibly different types, reached by <em>name</em>. Slide 13 states it almost in those words.</li>
<li><strong>The names it goes by</strong> — "record" in Forouzan and in COBOL/Pascal; <code>struct</code> in C, C++ and Go; "class with fields" in Java and C#; "object" informally. Databases call a record a <em>row</em> and a field a <em>column</em>, and that is the exact bridge into Chapter 11.</li>
<li><strong>Why index access does not work here</strong> — the fields have different sizes, so "the third field" is not at a fixed multiple of anything. The compiler must remember a separate <em>offset</em> for each field name. Slide 12 shows those offsets measured.</li>
<li><strong>Four slides follow</strong> — 11 defines record and field, 12 separates the record name from the field names, 13 compares records with arrays, 14 combines the two into an array of records.</li>
<li><strong>Where it lands later</strong> — a record is the unit written to a binary file in Chapter 10 and the unit stored as a row in Chapter 11. When you eventually call <code>fwrite(&amp;student, sizeof(struct student), 1, fp)</code> in PRF192 you are writing exactly one record, padding bytes and all — which is why slide 11's <code>sizeof</code> measurement matters in practice and not only in theory.</li>
</ul>
<p class="meo">💡 Test for "record or array?" in one question: <em>are these things the same kind of thing, or are they different aspects of one thing?</em> Thirty students → array. A student's id, name and grade → record. Thirty students each with id, name and grade → array of records (slide 14).</p>`,
        `<p class="y-chinh">🎯 Slide phân mục cho 9.2. Đây là bản lề của cả chương: mảng chứa <em>nhiều vật cùng loại</em>; record chứa <em>các phần của MỘT vật</em>, và các phần ấy có thể khác kiểu nhau.</p>
<ul>
<li><strong>Câu tương phản một dòng phải thuộc</strong> — mảng: nhiều phần tử, cùng kiểu, với tới bằng <em>chỉ số</em>. Record: vài trường, có thể khác kiểu, với tới bằng <em>tên</em>. Slide 13 phát biểu gần như đúng chừng ấy chữ.</li>
<li><strong>Nó mang những cái tên nào</strong> — "record" trong Forouzan và trong COBOL/Pascal; <code>struct</code> trong C, C++ và Go; "lớp có các trường" trong Java và C#; nói nôm na là "đối tượng". Cơ sở dữ liệu gọi record là một <em>dòng</em> và trường là một <em>cột</em>, và đó đúng là nhịp cầu sang Chương 11.</li>
<li><strong>Vì sao không truy cập bằng chỉ số được ở đây</strong> — các trường có kích thước khác nhau, nên "trường thứ ba" không nằm ở bội số cố định của cái gì cả. Trình biên dịch buộc phải nhớ một <em>offset</em> riêng cho từng tên trường. Slide 12 cho bạn xem các offset ấy đo thật.</li>
<li><strong>Bốn slide tiếp theo</strong> — 11 định nghĩa record và trường, 12 tách tên bản ghi khỏi tên các trường, 13 so sánh record với mảng, 14 gộp cả hai thành mảng các record.</li>
<li><strong>Về sau nó rơi vào đâu</strong> — record là đơn vị được ghi xuống tệp nhị phân ở Chương 10 và là đơn vị lưu thành một dòng ở Chương 11. Khi sau này bạn gọi <code>fwrite(&amp;student, sizeof(struct student), 1, fp)</code> trong PRF192 là bạn đang ghi đúng MỘT record, kèm cả byte đệm — và đó là lý do phép đo <code>sizeof</code> ở slide 11 quan trọng trong thực tế chứ không chỉ trên lý thuyết.</li>
</ul>
<p class="meo">💡 Phép thử "record hay mảng?" gọn trong một câu hỏi: <em>đây là nhiều vật cùng loại, hay là nhiều khía cạnh của MỘT vật?</em> Ba mươi sinh viên → mảng. Mã số, tên, điểm của một sinh viên → record. Ba mươi sinh viên mỗi người có mã số, tên, điểm → mảng các record (slide 14).</p>`],

      [11, '1. Introduction (records)',
        `<p class="y-chinh">🎯 Two definitions. <strong>A record is a collection of related elements, possibly of different types, having a single name.</strong> <strong>Each element in a record is called a field.</strong> Figure 9.6 draws two: a <em>fraction</em> with fields numerator = 13 and denominator = 17, and a <em>student</em> with id = 2005, name = "George Boole", grade = 'A'.</p>
<ul>
<li><strong>The red banner at the bottom is the examinable sentence</strong> — "The elements in a record can be of the same or different types, but all elements in the record must be <em>related</em>." Both halves matter: <em>fraction</em> has two fields of the <em>same</em> type and is still a record; <em>student</em> mixes an integer, a string and a character.</li>
<li><strong>"A field differs from a variable primarily in that it is part of a record"</strong> — the slide's own sentence, and it is the whole of the difference. A field has a type, occupies memory, and can be assigned to, exactly like a variable. It simply does not stand alone.</li>
<li><strong>The same two records in C</strong> — <code>struct fraction { int numerator; int denominator; };</code> and <code>struct student { int id; char name[20]; char grade; };</code>. Note <code>name</code> is itself an array living inside a record: that is legal and common, and slide 14 contrasts it with the opposite arrangement.</li>
<li><strong>Measured layout of <code>struct fraction</code></strong> — <code>sizeof</code> = <strong>8</strong>, numerator at offset 0, denominator at offset 4. Two 4-byte integers, no surprises, nothing wasted.</li>
<li><strong>Measured layout of <code>struct student</code>, where there <em>is</em> a surprise</strong> — <code>offsetof</code> gives id at 0, name at 4, grade at 24; the fields add up to 4 + 20 + 1 = 25 bytes, but <code>sizeof(struct student)</code> measures <strong>28</strong>.</li>
<li><strong>Why 28 and not 25 — padding.</strong> The compiler must be able to place these records back to back in an array and still have every <code>int id</code> land on an address divisible by 4. So it adds 3 unused bytes at the end of the record. Those bytes are called <em>padding</em>, they hold nothing, and they are still written to disk when you <code>fwrite</code> the record.</li>
<li><strong>The sharpest padding example, measured.</strong> <code>struct { char c; int i; }</code> — the fields total 1 + 4 = 5 bytes, but <code>sizeof</code> measures <strong>8</strong>, with c at offset 0 and i at offset <strong>4</strong>. Three bytes of nothing sit between them so that <code>i</code> is 4-byte aligned. Reversing the declaration to <code>{ int i; char c; }</code> does not help: still <strong>8</strong>, i at 0 and c at 4, with 3 bytes of tail padding instead.</li>
</ul>
<table>
<tr><th>Record</th><th>Field</th><th>Offset (đo thật)</th><th>Kích thước</th></tr>
<tr><td rowspan="2">fraction</td><td>numerator</td><td>0</td><td>4</td></tr>
<tr><td>denominator</td><td>4</td><td>4</td></tr>
<tr><td rowspan="3">student</td><td>id</td><td>0</td><td>4</td></tr>
<tr><td>name[20]</td><td>4</td><td>20</td></tr>
<tr><td>grade</td><td>24</td><td>1 (+3 padding)</td></tr>
</table>
<p class="dap-an">✅ Đáp án: <code>sizeof(struct fraction)</code> = 8 = 4 + 4, no padding. <code>sizeof(struct student)</code> = <strong>28</strong> although its fields total only 25 — three padding bytes at the end. <code>sizeof(struct { char c; int i; })</code> = <strong>8</strong>, not 5. All three numbers come from <code>offsetof</code> and <code>sizeof</code> in a program compiled with <code>cc -Wall</code> and run, not from arithmetic on paper.</p>
<p class="pitfall">⚠️ "The size of a record is the sum of the sizes of its fields" is false, and it is a favourite trick question. It is true only when every field is already aligned — like <code>fraction</code>. Never compute a record's size by addition; measure it with <code>sizeof</code>.</p>`,
        `<p class="y-chinh">🎯 Hai định nghĩa. <strong>Record là một tập hợp các phần tử có liên quan, có thể KHÁC kiểu nhau, mang chung một cái tên.</strong> <strong>Mỗi phần tử trong record gọi là một TRƯỜNG (field).</strong> Hình 9.6 vẽ hai cái: một <em>fraction</em> có trường numerator = 13 và denominator = 17, và một <em>student</em> có id = 2005, name = "George Boole", grade = 'A'.</p>
<ul>
<li><strong>Dải băng đỏ dưới cùng mới là câu ra thi</strong> — "Các phần tử trong một record có thể cùng kiểu hoặc khác kiểu, nhưng mọi phần tử trong record đều phải CÓ LIÊN QUAN với nhau." Cả hai vế đều quan trọng: <em>fraction</em> có hai trường <em>cùng</em> kiểu mà vẫn là record; <em>student</em> thì trộn một số nguyên, một chuỗi và một ký tự.</li>
<li><strong>"Trường khác biến chủ yếu ở chỗ nó là một phần của record"</strong> — câu của chính slide, và toàn bộ khác biệt nằm ở đó. Trường có kiểu, chiếm bộ nhớ, gán giá trị được, y hệt một biến. Nó chỉ không đứng một mình được thôi.</li>
<li><strong>Đúng hai record ấy trong C</strong> — <code>struct fraction { int numerator; int denominator; };</code> và <code>struct student { int id; char name[20]; char grade; };</code>. Để ý <code>name</code> tự nó là một MẢNG nằm bên trong một record: hợp lệ và rất thường gặp, và slide 14 sẽ đặt nó cạnh cách sắp xếp ngược lại.</li>
<li><strong>Bố trí ĐO THẬT của <code>struct fraction</code></strong> — <code>sizeof</code> = <strong>8</strong>, numerator ở offset 0, denominator ở offset 4. Hai số nguyên 4 byte, không có gì bất ngờ, không phí byte nào.</li>
<li><strong>Bố trí ĐO THẬT của <code>struct student</code>, chỗ này thì CÓ bất ngờ</strong> — <code>offsetof</code> cho id ở 0, name ở 4, grade ở 24; cộng các trường lại được 4 + 20 + 1 = 25 byte, nhưng <code>sizeof(struct student)</code> đo ra <strong>28</strong>.</li>
<li><strong>Vì sao là 28 chứ không phải 25 — BYTE ĐỆM.</strong> Trình biên dịch phải xếp được các record này sát nhau trong một mảng mà mọi <code>int id</code> vẫn rơi vào địa chỉ chia hết cho 4. Nên nó thêm 3 byte vô dụng vào cuối record. Những byte ấy gọi là <em>padding</em>, chúng không chứa gì, và chúng vẫn bị ghi xuống đĩa khi bạn <code>fwrite</code> cái record.</li>
<li><strong>Ví dụ padding sắc nhất, đo thật.</strong> <code>struct { char c; int i; }</code> — các trường cộng lại là 1 + 4 = 5 byte, nhưng <code>sizeof</code> đo ra <strong>8</strong>, với c ở offset 0 và i ở offset <strong>4</strong>. Ba byte trống nằm chen giữa để <code>i</code> thẳng hàng 4 byte. Đảo thứ tự khai báo thành <code>{ int i; char c; }</code> cũng không cứu được: vẫn <strong>8</strong>, i ở 0 và c ở 4, chỉ là 3 byte đệm chuyển ra đuôi.</li>
</ul>
<table>
<tr><th>Record</th><th>Trường</th><th>Offset (đo thật)</th><th>Kích thước</th></tr>
<tr><td rowspan="2">fraction</td><td>numerator</td><td>0</td><td>4</td></tr>
<tr><td>denominator</td><td>4</td><td>4</td></tr>
<tr><td rowspan="3">student</td><td>id</td><td>0</td><td>4</td></tr>
<tr><td>name[20]</td><td>4</td><td>20</td></tr>
<tr><td>grade</td><td>24</td><td>1 (+3 byte đệm)</td></tr>
</table>
<p class="dap-an">✅ Đáp án: <code>sizeof(struct fraction)</code> = 8 = 4 + 4, không có byte đệm. <code>sizeof(struct student)</code> = <strong>28</strong> dù các trường chỉ cộng được 25 — ba byte đệm ở cuối. <code>sizeof(struct { char c; int i; })</code> = <strong>8</strong>, không phải 5. Cả ba con số lấy từ <code>offsetof</code> và <code>sizeof</code> trong một chương trình đã biên dịch bằng <code>cc -Wall</code> và chạy thật, không phải tính nhẩm trên giấy.</p>
<p class="pitfall">⚠️ "Kích thước record bằng tổng kích thước các trường" là SAI, và đó là câu hỏi mẹo ưa thích. Nó chỉ đúng khi mọi trường vốn đã thẳng hàng sẵn — như <code>fraction</code>. Đừng bao giờ tính kích thước record bằng phép cộng; hãy đo bằng <code>sizeof</code>.</p>`],

      [12, '2. Record name versus field name',
        `<p class="y-chinh">🎯 The same distinction as slide 5, now for records: <strong>the record has a name, and each field has its own name</strong>. For the student record the name is <code>student</code> and the fields are <code>student.id</code>, <code>student.name</code>, <code>student.grade</code>.</p>
<ul>
<li><strong>The syntax is the dot</strong> — <em>recordName</em> <code>.</code> <em>fieldName</em>. Universal: C, C++, Java, C#, Python, JavaScript, SQL all use the same dot for "the named part of". It is one of the very few notations that survived every language war.</li>
<li><strong>Read the figure literally</strong> — the two records from slide 11 are redrawn in yellow, this time with the point being the labels, not the values. <code>fraction.numerator</code> is 13, <code>fraction.denominator</code> is 17, <code>student.id</code> is 2005, <code>student.name</code> is "George Boole", <code>student.grade</code> is 'A'.</li>
<li><strong>Index versus name, and why the difference is physical</strong> — <code>scores[i]</code> computes an address at <em>run time</em> from the value of <code>i</code>, so the index can be a variable. <code>student.grade</code> is resolved at <em>compile time</em> to a fixed offset; there is no such thing as "the i-th field". That is the deepest difference between an array and a record, and it follows directly from the fields having different sizes.</li>
<li><strong>Measured proof that a field name is just a constant offset</strong> — <code>offsetof(struct student, id)</code> = 0, <code>offsetof(struct student, name)</code> = 4, <code>offsetof(struct student, grade)</code> = 24. When you write <code>s.grade</code>, the compiler emits "the byte at the address of <code>s</code> plus 24". Nothing is searched for at run time.</li>
<li><strong>Worked example — the address of a field.</strong> A <code>struct student s;</code> lives at address 5000. Then <code>&amp;s.id</code> = 5000, <code>&amp;s.name</code> = 5000 + 4 = 5004, <code>&amp;s.name[3]</code> = 5004 + 3 = 5007 (a <code>char</code> is 1 byte), and <code>&amp;s.grade</code> = 5000 + 24 = <strong>5024</strong>. The record ends at 5027 because <code>sizeof</code> is 28.</li>
<li><strong>Through a pointer, the arrow</strong> — <code>struct student *p = &amp;s;</code> then <code>p-&gt;grade</code> means exactly <code>(*p).grade</code>. The deck itself uses the <code>(*ptr).field</code> form on slide 18 for linked lists; C programmers write <code>p-&gt;field</code>. Measured on a real run: <code>p-&gt;x</code> and <code>(*p).x</code> both printed 7.</li>
</ul>
<p class="dap-an">✅ Đáp án: with the record at 5000, <code>&amp;s.grade</code> = 5024 (5000 + offset 24), and the record occupies bytes 5000–5027. The three field offsets 0, 4, 24 are measured values, not assumptions — and note 4 + 20 = 24 exactly, so there is <em>no</em> padding before <code>grade</code>; the 3 wasted bytes are all at the end.</p>
<p class="pitfall">⚠️ The deck labels this figure "Figure 9.7 <em>Insert a value to array</em>". That caption is copied by mistake from Figure 9.5 — this slide has nothing to do with inserting into an array. The same wrong caption reappears on slide 14 as "Figure 9.9". Do not try to find an insertion algorithm in these pictures; there is none.</p>`,
        `<p class="y-chinh">🎯 Vẫn phép phân biệt như slide 5, giờ áp cho record: <strong>bản ghi có một cái tên, và mỗi trường có tên riêng của nó</strong>. Với record sinh viên thì tên bản ghi là <code>student</code> còn các trường là <code>student.id</code>, <code>student.name</code>, <code>student.grade</code>.</p>
<ul>
<li><strong>Cú pháp là DẤU CHẤM</strong> — <em>tênBảnGhi</em> <code>.</code> <em>tênTrường</em>. Phổ quát: C, C++, Java, C#, Python, JavaScript, SQL đều dùng đúng dấu chấm ấy cho nghĩa "phần mang tên … của". Đây là một trong rất ít ký hiệu sống sót qua mọi cuộc chiến ngôn ngữ.</li>
<li><strong>Đọc hình đúng như nó vẽ</strong> — hai record của slide 11 được vẽ lại trên nền vàng, lần này trọng tâm là các NHÃN chứ không phải các giá trị. <code>fraction.numerator</code> là 13, <code>fraction.denominator</code> là 17, <code>student.id</code> là 2005, <code>student.name</code> là "George Boole", <code>student.grade</code> là 'A'.</li>
<li><strong>Chỉ số so với tên, và vì sao khác biệt ấy là khác biệt VẬT LÝ</strong> — <code>scores[i]</code> tính ra địa chỉ <em>lúc chạy</em> từ giá trị của <code>i</code>, nên chỉ số được phép là một biến. Còn <code>student.grade</code> được giải quyết <em>lúc biên dịch</em> thành một offset cố định; không hề có thứ gọi là "trường thứ i". Đó là khác biệt sâu nhất giữa mảng và record, và nó suy ra thẳng từ việc các trường có kích thước khác nhau.</li>
<li><strong>Bằng chứng đo thật rằng tên trường chỉ là một offset hằng số</strong> — <code>offsetof(struct student, id)</code> = 0, <code>offsetof(struct student, name)</code> = 4, <code>offsetof(struct student, grade)</code> = 24. Khi bạn viết <code>s.grade</code>, trình biên dịch phát ra lệnh "byte tại địa chỉ của <code>s</code> cộng 24". Không có gì phải đi tìm lúc chạy cả.</li>
<li><strong>Bài mẫu — địa chỉ của một trường.</strong> Một <code>struct student s;</code> nằm ở địa chỉ 5000. Khi ấy <code>&amp;s.id</code> = 5000, <code>&amp;s.name</code> = 5000 + 4 = 5004, <code>&amp;s.name[3]</code> = 5004 + 3 = 5007 (một <code>char</code> là 1 byte), và <code>&amp;s.grade</code> = 5000 + 24 = <strong>5024</strong>. Bản ghi kết thúc ở 5027 vì <code>sizeof</code> bằng 28.</li>
<li><strong>Qua con trỏ thì dùng MŨI TÊN</strong> — <code>struct student *p = &amp;s;</code> rồi <code>p-&gt;grade</code> có nghĩa đúng bằng <code>(*p).grade</code>. Chính bộ slide này dùng dạng <code>(*ptr).field</code> ở slide 18 cho danh sách liên kết; dân lập trình C thì viết <code>p-&gt;field</code>. Đo thật khi chạy: <code>p-&gt;x</code> và <code>(*p).x</code> đều in ra 7.</li>
</ul>
<p class="dap-an">✅ Đáp án: với bản ghi đặt ở 5000, <code>&amp;s.grade</code> = 5024 (5000 + offset 24), và bản ghi chiếm các byte 5000–5027. Ba offset 0, 4, 24 là giá trị ĐO ĐƯỢC chứ không phải giả định — và để ý 4 + 20 = 24 chằn chặn, nên KHÔNG có byte đệm nào trước <code>grade</code>; cả 3 byte phí đều nằm ở cuối.</p>
<p class="pitfall">⚠️ Bộ slide ghi chú thích hình này là "Figure 9.7 <em>Insert a value to array</em>". Chú thích ấy bị chép nhầm từ Figure 9.5 — slide này chẳng liên quan gì tới việc chèn vào mảng. Đúng chú thích sai ấy lại xuất hiện lần nữa ở slide 14 dưới tên "Figure 9.9". Đừng cố đi tìm thuật toán chèn trong mấy bức hình này; không có đâu.</p>`],

      [13, '3. Comparison of records and arrays',
        `<p class="y-chinh">🎯 The sentence the whole section exists for: <strong>"An array defines a combination of elements, while a record defines the identifiable parts of an element."</strong> The example: an array can define a class of 40 students; a record defines one student's id, name and grade.</p>
<ul>
<li><strong>Build the comparison table now</strong> — it is objective 9 of slide 3, and writing it out once is worth more than re-reading the slide three times.</li>
<li><strong>The type rule is the hard boundary</strong> — an array <em>must</em> be homogeneous; a record <em>may</em> be heterogeneous. Note "may": <code>struct fraction</code> has two <code>int</code> fields and is still a record, because the deciding factor is <em>how you name the parts</em>, not what types they happen to be.</li>
<li><strong>The access rule follows from it</strong> — homogeneous and equal-sized means index arithmetic works, so arrays support <code>a[i]</code> with <em>i</em> computed at run time. Heterogeneous means fixed offsets per name, so records support <code>r.field</code> only, resolved at compile time.</li>
<li><strong>What Figure 9.8 actually shows, and its flaw</strong> — the caption promises "Insert a record (name, status, grade) to array of students in Java", but the code is <code>ArrayList&lt;ArrayList&lt;String&gt;&gt; details</code>: a list of lists of <em>strings</em>. Student 1 gets two items (<code>"Jane Doe"</code>, <code>"50"</code>) while student 2 gets three (<code>"John Doe"</code>, <code>"Part Time"</code>, <code>"70"</code>).</li>
<li><strong>That mismatch is itself the lesson.</strong> A list of strings is <em>not</em> a record: it does not enforce how many fields there are, it does not name them, and it turns the grade 50 into the text <code>"50"</code>. The compiler cannot catch the missing "status" field of student 1, because to the compiler both are just lists. A real record — <code>class Student { int id; String name; String status; int grade; }</code> — would have refused to compile.</li>
<li><strong>The honest way to write the slide's intent</strong> — in Java: <code>record Student(String name, String status, int grade) {}</code> then <code>List&lt;Student&gt; details</code>. In C: <code>struct student details[40];</code>. Both make "every entry has exactly these fields" a rule the machine checks, which is the entire point of having records.</li>
</ul>
<table>
<tr><th>Tiêu chí</th><th>Array (mảng)</th><th>Record (bản ghi)</th></tr>
<tr><td>Chứa cái gì</td><td>Nhiều phần tử cùng loại</td><td>Các phần của MỘT phần tử</td></tr>
<tr><td>Kiểu dữ liệu</td><td>Bắt buộc giống nhau</td><td>Được phép khác nhau</td></tr>
<tr><td>Truy cập bằng</td><td>Chỉ số: <code>scores[i]</code></td><td>Tên trường: <code>student.id</code></td></tr>
<tr><td>Chỉ số tính lúc nào</td><td>Lúc chạy (i là biến)</td><td>Lúc biên dịch (offset cố định)</td></tr>
<tr><td>Số phần tử</td><td>Thường nhiều, cố định</td><td>Thường ít, cố định</td></tr>
<tr><td>Kích thước</td><td>n × kích_thước_phần_tử</td><td>Tổng các trường <em>+ byte đệm</em></td></tr>
<tr><td>Ví dụ</td><td>40 sinh viên một lớp</td><td>id, name, grade của một sinh viên</td></tr>
</table>
<p class="dap-an">✅ Đáp án: the Java code on the slide does <em>not</em> implement a record. It builds <code>ArrayList&lt;ArrayList&lt;String&gt;&gt;</code>, whose entries have 2 and 3 items respectively — proof that nothing enforces a field list. A record is exactly the construct that would have made that inconsistency impossible.</p>
<p class="meo">💡 Remember the pair with a picture: an array is a <em>column</em> of identical boxes; a record is a <em>row</em> of differently-shaped boxes. Slide 14 puts a row inside every box of the column, and that is a table — which is to say, a database.</p>`,
        `<p class="y-chinh">🎯 Câu văn mà cả mục này tồn tại vì nó: <strong>"Mảng định nghĩa một TỔ HỢP CÁC PHẦN TỬ, còn record định nghĩa CÁC PHẦN NHẬN DẠNG ĐƯỢC của MỘT phần tử."</strong> Ví dụ đi kèm: mảng định nghĩa được một lớp 40 sinh viên; record định nghĩa mã số, tên và điểm của MỘT sinh viên.</p>
<ul>
<li><strong>Dựng bảng so sánh ngay bây giờ</strong> — đó chính là mục tiêu số 9 ở slide 3, và viết nó ra một lần đáng giá hơn đọc lại slide ba lần.</li>
<li><strong>Luật về kiểu là ranh giới cứng</strong> — mảng <em>BẮT BUỘC</em> đồng nhất; record thì <em>ĐƯỢC PHÉP</em> hỗn tạp. Để ý chữ "được phép": <code>struct fraction</code> có hai trường <code>int</code> mà vẫn là record, vì thứ quyết định là <em>bạn ĐẶT TÊN cho các phần ra sao</em>, chứ không phải chúng tình cờ mang kiểu gì.</li>
<li><strong>Luật truy cập suy ra từ đó</strong> — đồng nhất và bằng kích thước thì số học chỉ số chạy được, nên mảng hỗ trợ <code>a[i]</code> với <em>i</em> tính lúc chạy. Hỗn tạp thì mỗi tên có một offset cố định, nên record chỉ hỗ trợ <code>r.field</code>, giải quyết lúc biên dịch.</li>
<li><strong>Figure 9.8 thật sự cho xem gì, và chỗ hỏng của nó</strong> — chú thích hứa "Insert a record (name, status, grade) to array of students in Java", nhưng mã lại là <code>ArrayList&lt;ArrayList&lt;String&gt;&gt; details</code>: một danh sách chứa các danh sách <em>chuỗi</em>. Sinh viên 1 được hai mục (<code>"Jane Doe"</code>, <code>"50"</code>) trong khi sinh viên 2 được ba mục (<code>"John Doe"</code>, <code>"Part Time"</code>, <code>"70"</code>).</li>
<li><strong>Chính chỗ vênh ấy MỚI là bài học.</strong> Một danh sách chuỗi <em>KHÔNG</em> phải là record: nó không ép có bao nhiêu trường, không đặt tên cho trường nào, và biến điểm 50 thành chữ <code>"50"</code>. Trình biên dịch không thể bắt được việc sinh viên 1 thiếu trường "status", vì với nó cả hai chỉ là danh sách. Một record thật — <code>class Student { int id; String name; String status; int grade; }</code> — thì đã từ chối biên dịch.</li>
<li><strong>Cách viết trung thực với ý đồ của slide</strong> — trong Java: <code>record Student(String name, String status, int grade) {}</code> rồi <code>List&lt;Student&gt; details</code>. Trong C: <code>struct student details[40];</code>. Cả hai biến "mọi mục đều có đúng những trường này" thành một luật mà MÁY kiểm, và đó là toàn bộ lý do người ta cần record.</li>
</ul>
<table>
<tr><th>Tiêu chí</th><th>Array (mảng)</th><th>Record (bản ghi)</th></tr>
<tr><td>Chứa cái gì</td><td>Nhiều phần tử cùng loại</td><td>Các phần của MỘT phần tử</td></tr>
<tr><td>Kiểu dữ liệu</td><td>Bắt buộc giống nhau</td><td>Được phép khác nhau</td></tr>
<tr><td>Truy cập bằng</td><td>Chỉ số: <code>scores[i]</code></td><td>Tên trường: <code>student.id</code></td></tr>
<tr><td>Chỉ số tính lúc nào</td><td>Lúc chạy (i là biến)</td><td>Lúc biên dịch (offset cố định)</td></tr>
<tr><td>Số phần tử</td><td>Thường nhiều, cố định</td><td>Thường ít, cố định</td></tr>
<tr><td>Kích thước</td><td>n × kích_thước_phần_tử</td><td>Tổng các trường <em>+ byte đệm</em></td></tr>
<tr><td>Ví dụ</td><td>40 sinh viên một lớp</td><td>id, name, grade của một sinh viên</td></tr>
</table>
<p class="dap-an">✅ Đáp án: đoạn mã Java trên slide <em>KHÔNG</em> hiện thực một record. Nó dựng <code>ArrayList&lt;ArrayList&lt;String&gt;&gt;</code>, mà hai mục trong đó lần lượt có 2 và 3 phần tử — bằng chứng rằng chẳng có gì ép buộc danh sách trường. Record chính là cấu trúc đã khiến sự vênh ấy thành bất khả.</p>
<p class="meo">💡 Nhớ cặp này bằng một hình ảnh: mảng là một <em>CỘT</em> các ô giống hệt nhau; record là một <em>HÀNG</em> các ô hình thù khác nhau. Slide 14 nhét một hàng vào trong từng ô của cột, và thế là thành một cái bảng — nói cách khác, thành cơ sở dữ liệu.</p>`],

      [14, '4. Array of records',
        `<p class="y-chinh">🎯 Combine the two: <strong>an array of records</strong>. Figure 9.9 draws <code>students[1]</code>, <code>students[2]</code>, …, <code>students[30]</code>, each one a three-box row labelled id · name · grade, with the whole stack named <code>students</code>.</p>
<ul>
<li><strong>Read the two names off the figure</strong> — <code>students</code> is the array; <code>students[2]</code> is one record; <code>students[2].name</code> is one field of one record. Three levels of naming, and exam questions love asking you to write the third one.</li>
<li><strong>In C it is one line</strong> — <code>struct student students[30];</code>. Every element is a whole record, so the element size is <code>sizeof(struct student)</code>, not the size of any field.</li>
<li><strong>Measured stride, which is where padding suddenly matters.</strong> For <code>struct student arr[3]</code> the measured offsets of the elements from the base are <strong>0, 28, 56</strong> — a stride of 28, the padded size, not 25. And <code>&amp;arr[1].grade</code> measured at offset <strong>52</strong> = 1 × 28 + 24. The address formula from slide 5 still works; you just feed it the record's <code>sizeof</code>.</li>
<li><strong>Worked example.</strong> <code>students</code> starts at address 8000, <code>sizeof(struct student)</code> = 28 (measured), 0-based. Address of <code>students[12]</code> = 8000 + 12 × 28 = 8000 + 336 = <strong>8336</strong>. Address of <code>students[12].grade</code> = 8336 + 24 = <strong>8360</strong>. Total bytes for 30 students = 30 × 28 = <strong>840</strong> — and 90 of those bytes are padding that holds nothing.</li>
<li><strong>Array of records is NOT the same as a record containing an array.</strong> Both exist, both are legal, and they store the same values in a completely different order. Measured side by side: <code>struct Point { int x; int y; }; struct Point aos[4];</code> lays out as <strong>x y x y x y x y</strong> (offsets of <code>x</code>: 0, 8, 16, 24 — stride 8). The opposite arrangement <code>struct Poly { int xs[4]; int ys[4]; };</code> lays out as <strong>x x x x y y y y</strong> (<code>xs</code> at offset 0, <code>ys</code> at offset 16). Both measure <code>sizeof</code> = 32; nothing else about them is alike.</li>
<li><strong>You have already met a record containing an array</strong> — <code>struct student</code> itself, because <code>char name[20]</code> is an array field. So <code>students[2].name[0]</code> is "the first character of the name of the third student": array of records, then field, then array inside the field.</li>
<li><strong>This is the shape of a database table, and of a binary file</strong> — 30 rows × 3 columns. In Chapter 10 you write this array to disk with one <code>fwrite</code> of 840 bytes; in Chapter 11 the same thing is a table with 30 rows. An array of records is the single most reused structure in the rest of the course.</li>
</ul>
<p class="dap-an">✅ Đáp án: <code>students[12]</code> at base 8000 sits at 8336, its <code>grade</code> field at 8360, and 30 records occupy 840 bytes with 90 bytes of padding. The stride is the <em>measured</em> 28, not the 25 you get by adding field sizes — the exact mistake that makes a hand-computed record address wrong by 3 bytes per element.</p>
<p class="pitfall">⚠️ The deck labels this "Figure 9.9 <em>Insert a value to array</em>" — the same wrong caption already used on slide 12. The figure shows an array of records and says nothing about insertion. And do not confuse <code>students[2].name</code> (field of a record inside an array) with <code>students.name[2]</code>, which does not compile at all.</p>`,
        `<p class="y-chinh">🎯 Gộp cả hai lại: <strong>mảng các record</strong>. Hình 9.9 vẽ <code>students[1]</code>, <code>students[2]</code>, …, <code>students[30]</code>, mỗi cái là một hàng ba ô mang nhãn id · name · grade, cả chồng ấy tên là <code>students</code>.</p>
<ul>
<li><strong>Đọc hai cái tên ra khỏi hình</strong> — <code>students</code> là mảng; <code>students[2]</code> là MỘT record; <code>students[2].name</code> là MỘT trường của MỘT record. Ba tầng đặt tên, và đề thi rất thích bắt bạn viết ra cái tầng thứ ba.</li>
<li><strong>Trong C nó gọn một dòng</strong> — <code>struct student students[30];</code>. Mỗi phần tử là trọn một record, nên kích thước phần tử là <code>sizeof(struct student)</code>, không phải kích thước của một trường nào.</li>
<li><strong>Bước nhảy ĐO THẬT, và đây là chỗ byte đệm đột nhiên quan trọng.</strong> Với <code>struct student arr[3]</code>, khoảng cách đo được của các phần tử so với nền là <strong>0, 28, 56</strong> — bước nhảy 28, tức kích thước ĐÃ ĐỆM, không phải 25. Và <code>&amp;arr[1].grade</code> đo được ở offset <strong>52</strong> = 1 × 28 + 24. Công thức địa chỉ của slide 5 vẫn chạy y nguyên; bạn chỉ việc thay <code>sizeof</code> của record vào.</li>
<li><strong>Bài mẫu.</strong> <code>students</code> bắt đầu ở địa chỉ 8000, <code>sizeof(struct student)</code> = 28 (đo thật), đánh chỉ số từ 0. Địa chỉ <code>students[12]</code> = 8000 + 12 × 28 = 8000 + 336 = <strong>8336</strong>. Địa chỉ <code>students[12].grade</code> = 8336 + 24 = <strong>8360</strong>. Tổng số byte cho 30 sinh viên = 30 × 28 = <strong>840</strong> — và 90 byte trong đó là byte đệm chẳng chứa gì.</li>
<li><strong>Mảng các record KHÁC HẲN record chứa mảng.</strong> Cả hai đều tồn tại, đều hợp lệ, và chúng cất CÙNG những giá trị ấy theo thứ tự hoàn toàn khác nhau. Đo thật đặt cạnh nhau: <code>struct Point { int x; int y; }; struct Point aos[4];</code> xếp ra <strong>x y x y x y x y</strong> (offset của <code>x</code>: 0, 8, 16, 24 — bước nhảy 8). Cách sắp ngược lại <code>struct Poly { int xs[4]; int ys[4]; };</code> xếp ra <strong>x x x x y y y y</strong> (<code>xs</code> ở offset 0, <code>ys</code> ở offset 16). Cả hai đo <code>sizeof</code> = 32; ngoài con số đó ra thì chẳng có gì giống nhau.</li>
<li><strong>Bạn đã gặp "record chứa mảng" rồi</strong> — chính <code>struct student</code>, vì <code>char name[20]</code> là một trường kiểu mảng. Nên <code>students[2].name[0]</code> nghĩa là "ký tự đầu trong tên của sinh viên thứ ba": mảng các record, rồi tới trường, rồi tới mảng bên trong trường.</li>
<li><strong>Đây chính là hình dáng của một bảng CSDL, và của một tệp nhị phân</strong> — 30 dòng × 3 cột. Ở Chương 10 bạn ghi mảng này xuống đĩa bằng một lệnh <code>fwrite</code> 840 byte; ở Chương 11 đúng thứ ấy là một bảng 30 dòng. Mảng các record là cấu trúc được dùng lại nhiều nhất trong phần còn lại của môn.</li>
</ul>
<p class="dap-an">✅ Đáp án: <code>students[12]</code> với nền 8000 nằm ở 8336, trường <code>grade</code> của nó ở 8360, và 30 record chiếm 840 byte trong đó 90 byte là đệm. Bước nhảy là con số ĐO ĐƯỢC 28, không phải 25 mà bạn có được khi cộng kích thước các trường — đúng cái lỗi làm một phép tính địa chỉ record trên giấy sai 3 byte mỗi phần tử.</p>
<p class="pitfall">⚠️ Bộ slide ghi chú thích "Figure 9.9 <em>Insert a value to array</em>" — đúng cái chú thích sai đã dùng ở slide 12. Hình này vẽ mảng các record và không nói gì về chèn cả. Và đừng lẫn <code>students[2].name</code> (trường của một record nằm trong mảng) với <code>students.name[2]</code>, thứ hoàn toàn không biên dịch được.</p>`],

      [15, '3 - Linked List',
        `<p class="y-chinh">🎯 Section divider for 9.3. The third structure, and it answers the complaint left open by slide 9: insertion into an array costs 22 shifts. A linked list makes it cost <em>one pointer assignment</em> — by giving up random access in exchange.</p>
<ul>
<li><strong>The trade, stated once</strong> — an array pays for cheap access with expensive insertion; a linked list pays for cheap insertion with expensive access. There is no structure that is cheap at both, and that sentence answers half the "compare and contrast" exam questions on its own.</li>
<li><strong>The idea in one line</strong> — each element carries the <em>address</em> of the next element, so elements no longer need to sit next to each other in memory. That frees the structure from needing a contiguous block, which is why it can grow one node at a time.</li>
<li><strong>Which slides follow</strong> — 16 defines node, data, link and the null pointer; 17 sets array against linked list with a picture; 18 repeats the name-versus-parts distinction a third time; 19 gives the search algorithm and explains why binary search is impossible here. Slide 20 (insertion, four cases) belongs to Part B.</li>
<li><strong>The prerequisite from earlier chapters</strong> — a pointer is just an address, and an address is just an integer that names a memory cell (Chapter 1's memory subsystem). Nothing mystical: a link field holds a number, and the number is where to look next.</li>
<li><strong>Where you meet it in real code</strong> — every dynamic list you have written with <code>malloc</code>, plus, underneath, the free list of the memory allocator itself, the queue of processes waiting in an operating system (Chapter 5), and the chain of items sharing a hash bucket.</li>
</ul>
<p class="meo">💡 Picture the contrast physically: an array is a row of seats bolted to the floor — seat 17 is always in the same place, but squeezing a new seat into the middle means moving every seat behind it. A linked list is a line of people each holding the hand of the next — to insert someone you only change two handholds, but to find the seventeenth person you must count from the front.</p>`,
        `<p class="y-chinh">🎯 Slide phân mục cho 9.3. Cấu trúc thứ ba, và nó trả lời đúng lời than còn bỏ ngỏ ở slide 9: chèn vào mảng tốn 22 lần dịch. Danh sách liên kết làm việc ấy chỉ tốn <em>một phép gán con trỏ</em> — đổi lại phải từ bỏ truy cập ngẫu nhiên.</p>
<ul>
<li><strong>Cuộc đánh đổi, nói một lần cho xong</strong> — mảng trả giá cho việc truy cập rẻ bằng việc chèn đắt; danh sách liên kết trả giá cho việc chèn rẻ bằng việc truy cập đắt. Không có cấu trúc nào rẻ cả hai đầu, và riêng câu ấy đã trả lời được một nửa số câu hỏi thi dạng "so sánh".</li>
<li><strong>Ý tưởng gói trong một dòng</strong> — mỗi phần tử mang theo <em>ĐỊA CHỈ</em> của phần tử kế tiếp, nên các phần tử không còn cần nằm cạnh nhau trong bộ nhớ. Điều đó cởi trói cho cấu trúc khỏi việc phải xin một khối liền mạch, và đó là lý do nó lớn lên được từng nút một.</li>
<li><strong>Những slide tiếp theo</strong> — 16 định nghĩa nút, dữ liệu, liên kết và con trỏ null; 17 đặt mảng cạnh danh sách liên kết bằng một bức hình; 18 lặp lại phép phân biệt tên-với-các-phần lần thứ ba; 19 đưa thuật toán tìm kiếm và giải thích vì sao tìm nhị phân bất khả ở đây. Slide 20 (chèn, bốn trường hợp) thuộc Phần B.</li>
<li><strong>Kiến thức nền từ các chương trước</strong> — con trỏ chỉ là một địa chỉ, và địa chỉ chỉ là một số nguyên gọi tên một ô nhớ (hệ con bộ nhớ ở Chương 1). Chẳng có gì huyền bí: trường liên kết chứa một con số, và con số ấy nói chỗ tiếp theo phải nhìn vào đâu.</li>
<li><strong>Bạn gặp nó trong mã thật ở đâu</strong> — mọi danh sách động bạn từng viết bằng <code>malloc</code>, và bên dưới nữa là danh sách vùng trống của chính bộ cấp phát bộ nhớ, hàng đợi tiến trình chờ trong hệ điều hành (Chương 5), và chuỗi các mục cùng rơi vào một ô băm.</li>
</ul>
<p class="meo">💡 Hình dung tương phản ấy một cách vật lý: mảng là một dãy ghế bắt vít xuống sàn — ghế số 17 luôn ở đúng chỗ ấy, nhưng nhét thêm một ghế vào giữa thì phải dời mọi ghế phía sau. Danh sách liên kết là một hàng người mỗi người nắm tay người kế tiếp — chèn thêm một người thì chỉ đổi hai cái nắm tay, nhưng muốn tìm người thứ mười bảy thì phải đếm từ đầu hàng.</p>`],

      [16, '1. Introduction (linked lists)',
        `<p class="y-chinh">🎯 The definition: <strong>each element contains two parts, data and link</strong>. The data part holds the value; the link holds a <em>pointer</em> — the address of the next element. A separate pointer variable identifies the first element, and <strong>the name of the list is the name of that pointer variable</strong>.</p>
<ul>
<li><strong>What Figure 9.10 draws, in three pieces</strong> — top row, "A linked list": a small box named <code>scores</code> with a dot, an arrow from it to the first node, then four nodes each split into a grey <code>data</code> half and a red <code>link</code> half, arrows chaining them, and the last link marked with a cross labelled "null pointer". Bottom left, "An empty linked list": just the <code>scores</code> box with a cross in it. Bottom right, "A node": one data + link pair on its own.</li>
<li><strong>The cross means null, and null is not zero data</strong> — it means "there is no next element". An empty list is the pointer itself holding null. Distinguish carefully: a list of one node is a pointer to a node whose link is null; an empty list is a pointer that <em>is</em> null and points to no node at all.</li>
<li><strong>"The name of the list is the same as the name of this pointer variable"</strong> — this is a subtle and heavily examined sentence. <code>scores</code> is <em>not</em> the collection of nodes; <code>scores</code> is a single pointer variable. Lose that pointer and every node becomes unreachable, even though all of them still exist in memory. That is exactly what a memory leak is.</li>
<li><strong>In C, the node</strong> — <code>struct node { int data; struct node *link; };</code>. Note the self-reference: the record contains a pointer to its own type. That is legal precisely because a pointer has a fixed size whatever it points to, so the compiler can compute <code>sizeof</code> without infinite regress. A field of type <code>struct node</code> (not a pointer) would be impossible.</li>
<li><strong>The cost of the link, measured in the same units as the rest of the chapter</strong> — on a 64-bit machine a pointer is 8 bytes. A list of <code>int</code> values therefore spends 4 bytes on data and 8 on overhead per node, plus 4 of padding to align the pointer: 16 bytes to store one 4-byte number. The equivalent array spends 4. Linked lists buy flexibility with memory, not just with access time.</li>
<li><strong>Why nodes need not be adjacent</strong> — each one is usually a separate <code>malloc</code>, so they may be scattered anywhere in the heap. The chain of links is the <em>only</em> thing that makes them a list. That is also why they are slower in practice than the O-notation suggests: every hop is a potential cache miss.</li>
</ul>
<p class="dap-an">✅ Đáp án: a node is <code>{ data, link }</code>; an empty list is a null pointer, drawn as the crossed box; the last node's link is null; and the list's name is the name of the head pointer, not of any node.</p>
<p class="pitfall">⚠️ Two confusions the figure is designed to prevent. (1) The <code>scores</code> box on the left is <em>not</em> a node — it has no data part, it is only a pointer. (2) The crossed link in the last node and the crossed box of the empty list mean the same thing (null) in two different places; an exam may show either and ask "what does the cross mean?"</p>`,
        `<p class="y-chinh">🎯 Định nghĩa: <strong>mỗi phần tử gồm HAI phần, dữ liệu và liên kết</strong>. Phần dữ liệu giữ giá trị; phần liên kết giữ một <em>con trỏ</em> — địa chỉ của phần tử kế tiếp. Một biến con trỏ riêng chỉ ra phần tử đầu tiên, và <strong>tên của danh sách chính là tên của biến con trỏ ấy</strong>.</p>
<ul>
<li><strong>Hình 9.10 vẽ gì, chia làm ba mảnh</strong> — hàng trên, "A linked list": một ô nhỏ tên <code>scores</code> có chấm tròn, một mũi tên từ đó tới nút đầu tiên, rồi bốn nút mỗi nút chẻ đôi thành nửa xám <code>data</code> và nửa đỏ <code>link</code>, các mũi tên nối chúng thành chuỗi, và liên kết cuối cùng bị đánh dấu chéo kèm nhãn "null pointer". Góc dưới trái, "An empty linked list": chỉ có ô <code>scores</code> với một dấu chéo bên trong. Góc dưới phải, "A node": một cặp data + link đứng riêng.</li>
<li><strong>Dấu chéo nghĩa là null, và null KHÔNG phải là dữ liệu bằng 0</strong> — nó nghĩa là "không có phần tử kế tiếp". Danh sách rỗng là chính cái con trỏ đang giữ giá trị null. Phân biệt cho kỹ: danh sách một nút là con trỏ trỏ tới một nút mà liên kết của nút đó là null; danh sách rỗng là con trỏ <em>TỰ NÓ</em> null và không trỏ tới nút nào cả.</li>
<li><strong>"Tên của danh sách trùng với tên của biến con trỏ này"</strong> — câu này tinh tế và bị ra thi rất nhiều. <code>scores</code> <em>KHÔNG</em> phải là tập hợp các nút; <code>scores</code> là MỘT biến con trỏ. Mất cái con trỏ ấy là mọi nút thành không với tới được, dù tất cả chúng vẫn còn nguyên trong bộ nhớ. Đó đúng là cái gọi là rò rỉ bộ nhớ.</li>
<li><strong>Nút ấy trong C</strong> — <code>struct node { int data; struct node *link; };</code>. Để ý chỗ tự tham chiếu: record chứa một con trỏ tới chính kiểu của nó. Hợp lệ được chính vì con trỏ có kích thước cố định bất kể nó trỏ tới cái gì, nên trình biên dịch tính được <code>sizeof</code> mà không rơi vào đệ quy vô tận. Một trường kiểu <code>struct node</code> (không phải con trỏ) thì bất khả.</li>
<li><strong>Cái giá của liên kết, đo bằng đúng đơn vị của cả chương</strong> — trên máy 64 bit một con trỏ là 8 byte. Vậy một danh sách chứa <code>int</code> tốn 4 byte cho dữ liệu và 8 byte cho phần phụ trội mỗi nút, cộng 4 byte đệm để căn thẳng con trỏ: 16 byte để cất một con số 4 byte. Mảng tương đương tốn 4. Danh sách liên kết mua sự linh hoạt bằng BỘ NHỚ, chứ không chỉ bằng thời gian truy cập.</li>
<li><strong>Vì sao các nút không cần nằm sát nhau</strong> — mỗi nút thường là một lần <code>malloc</code> riêng, nên chúng có thể nằm rải rác bất cứ đâu trong vùng heap. Chuỗi liên kết là thứ <em>DUY NHẤT</em> làm chúng thành một danh sách. Đó cũng là lý do trong thực tế nó chậm hơn những gì ký hiệu O gợi ý: mỗi bước nhảy là một lần có thể trượt cache.</li>
</ul>
<p class="dap-an">✅ Đáp án: một nút là <code>{ data, link }</code>; danh sách rỗng là một con trỏ null, vẽ thành ô có dấu chéo; liên kết của nút cuối là null; và tên danh sách là tên của con trỏ đầu, không phải tên của nút nào.</p>
<p class="pitfall">⚠️ Hai chỗ nhầm mà bức hình được vẽ ra để ngăn. (1) Ô <code>scores</code> bên trái <em>KHÔNG</em> phải là một nút — nó không có phần dữ liệu, nó chỉ là con trỏ. (2) Dấu chéo ở liên kết nút cuối và dấu chéo trong ô của danh sách rỗng mang cùng một nghĩa (null) nhưng ở hai chỗ khác nhau; đề thi có thể đưa ra cái nào cũng được rồi hỏi "dấu chéo nghĩa là gì?"</p>`],

      [17, '2. Arrays versus linked lists',
        `<p class="y-chinh">🎯 "Both an array and a linked list are representations of a list of items in memory. <strong>The only difference is the way in which the items are linked together.</strong>" In an array the <em>linking tool</em> is the index; in a linked list it is the pointer.</p>
<ul>
<li><strong>The slide's own argument, which is genuinely elegant</strong> — <code>scores[3]</code> is connected to <code>scores[4]</code> for one reason only: the integer 4 comes after the integer 3. The connection is implicit, made by arithmetic. In a linked list the connection is explicit, made by a stored address.</li>
<li><strong>Read Figure 9.11 very carefully — it is the whole point.</strong> Panel (a), array representation: <code>scores[1]</code>=66, <code>[2]</code>=72, <code>[3]</code>=74, <code>[4]</code>=85, <code>[5]</code>=96, stacked neatly in ascending order. Panel (b), linked list of the same five integers: the boxes are <em>drawn</em> top to bottom as 66, 72, 96, 85, 74, but the arrows weave between them so that following the links gives 66 → 72 → 74 → 85 → 96, and the node holding 96 carries the crossed null link.</li>
<li><strong>That mismatch is deliberate and is the exam point</strong> — in a linked list the <em>physical</em> order in memory has nothing to do with the <em>logical</em> order of the list. In an array the two are the same thing. Any exam question that draws a linked list with scrambled boxes is testing exactly this.</li>
<li><strong>Which operations change cost, concretely on these five numbers</strong> — "give me the 4th item": array does one multiplication (base + 3 × 4 bytes); the list must walk 66 → 72 → 74 → 85, four hops. "Insert 80 after 74": the array must shift 85 and 96 up one slot; the list changes two pointers and touches nothing else.</li>
<li><strong>Memory, measured honestly</strong> — five <code>int</code> values in an array: 20 bytes, contiguous. Five nodes of <code>{ int data; struct node *link; }</code> on a 64-bit machine: 16 bytes each after alignment padding, 80 bytes, scattered, plus one 8-byte head pointer. The list costs about four times the memory for the same five numbers.</li>
<li><strong>When each one wins</strong> — array when the size is known and you index a lot (a matrix, a lookup table, a sorted set you binary-search). Linked list when the size is unknown and changes constantly and you mostly traverse (a queue, an undo history, a chain of hash collisions).</li>
</ul>
<table>
<tr><th>Tiêu chí</th><th>Mảng</th><th>Danh sách liên kết</th></tr>
<tr><td>Công cụ nối (linking tool)</td><td>Chỉ số — ngầm định</td><td>Con trỏ — tường minh</td></tr>
<tr><td>Vị trí trong bộ nhớ</td><td>Liền kề bắt buộc</td><td>Rải rác tuỳ ý</td></tr>
<tr><td>Thứ tự vật lý so với logic</td><td>Trùng nhau</td><td>Có thể khác hẳn</td></tr>
<tr><td>Lấy phần tử thứ i</td><td>O(1) — một phép nhân</td><td>O(n) — phải đi từ đầu</td></tr>
<tr><td>Chèn / xoá ở giữa</td><td>O(n) — dịch phần đuôi</td><td>O(1) nếu đã ở đúng chỗ</td></tr>
<tr><td>Tìm nhị phân</td><td>Được (nếu đã sắp)</td><td>Không thể (slide 19)</td></tr>
<tr><td>Bộ nhớ cho 5 số int</td><td>20 byte</td><td>~80 byte + 8 byte con trỏ đầu</td></tr>
<tr><td>Kích thước</td><td>Cố định khi khai báo</td><td>Lớn/nhỏ tuỳ ý lúc chạy</td></tr>
</table>
<p class="dap-an">✅ Đáp án: the two panels hold the same five values 66, 72, 74, 85, 96, but only panel (a) stores them in that physical order. In panel (b) the boxes sit as 66, 72, 96, 85, 74 on the page while the links impose the ascending order — the figure is showing you that in a linked list, position on the page and position in the list are unrelated.</p>
<p class="meo">💡 The slide's phrase <em>linking tool</em> is the best single-word summary in the chapter. Array = linked by counting. Linked list = linked by pointing. If you can say which linking tool a structure uses, you can derive every cost in the table above without memorising it.</p>`,
        `<p class="y-chinh">🎯 "Cả mảng lẫn danh sách liên kết đều là cách biểu diễn một danh sách các mục trong bộ nhớ. <strong>Khác biệt DUY NHẤT nằm ở cách các mục được nối với nhau.</strong>" Trong mảng, <em>công cụ nối</em> là chỉ số; trong danh sách liên kết, đó là con trỏ.</p>
<ul>
<li><strong>Lập luận của chính slide, thật sự đẹp</strong> — <code>scores[3]</code> nối với <code>scores[4]</code> chỉ vì đúng một lý do: số nguyên 4 đứng sau số nguyên 3. Mối nối ấy là NGẦM ĐỊNH, do phép tính tạo ra. Trong danh sách liên kết, mối nối là TƯỜNG MINH, do một địa chỉ được cất sẵn tạo ra.</li>
<li><strong>Hãy đọc Hình 9.11 thật kỹ — cả điểm mấu chốt nằm ở đó.</strong> Khung (a), biểu diễn bằng mảng: <code>scores[1]</code>=66, <code>[2]</code>=72, <code>[3]</code>=74, <code>[4]</code>=85, <code>[5]</code>=96, xếp ngay ngắn theo thứ tự tăng dần. Khung (b), danh sách liên kết cho đúng năm số đó: các ô được <em>VẼ</em> từ trên xuống là 66, 72, 96, 85, 74, nhưng những mũi tên luồn lách giữa chúng sao cho đi theo liên kết sẽ ra 66 → 72 → 74 → 85 → 96, và nút chứa 96 mang liên kết null đánh chéo.</li>
<li><strong>Chỗ vênh ấy là CỐ Ý và là điểm ra thi</strong> — trong danh sách liên kết, thứ tự <em>VẬT LÝ</em> trong bộ nhớ không liên quan gì tới thứ tự <em>LOGIC</em> của danh sách. Trong mảng thì hai thứ ấy là một. Bất kỳ câu thi nào vẽ danh sách liên kết với các ô xáo trộn đều đang kiểm đúng chỗ này.</li>
<li><strong>Thao tác nào đổi giá, tính cụ thể trên năm con số ấy</strong> — "cho tôi phần tử thứ 4": mảng làm một phép nhân (base + 3 × 4 byte); danh sách phải đi 66 → 72 → 74 → 85, bốn bước nhảy. "Chèn 80 sau 74": mảng phải đẩy 85 và 96 lên một ô; danh sách đổi hai con trỏ và không đụng tới thứ gì khác.</li>
<li><strong>Bộ nhớ, nói cho sòng phẳng</strong> — năm giá trị <code>int</code> trong mảng: 20 byte, liền kề. Năm nút kiểu <code>{ int data; struct node *link; }</code> trên máy 64 bit: mỗi nút 16 byte sau khi đệm căn hàng, tổng 80 byte, nằm rải rác, cộng thêm một con trỏ đầu 8 byte. Danh sách tốn khoảng gấp bốn lần bộ nhớ cho cùng năm con số.</li>
<li><strong>Bên nào thắng lúc nào</strong> — mảng khi biết trước kích thước và hay truy cập theo chỉ số (ma trận, bảng tra, tập đã sắp để tìm nhị phân). Danh sách liên kết khi không biết kích thước, kích thước đổi liên tục, và chủ yếu là duyệt (hàng đợi, lịch sử hoàn tác, chuỗi các mục đụng độ băm).</li>
</ul>
<table>
<tr><th>Tiêu chí</th><th>Mảng</th><th>Danh sách liên kết</th></tr>
<tr><td>Công cụ nối (linking tool)</td><td>Chỉ số — ngầm định</td><td>Con trỏ — tường minh</td></tr>
<tr><td>Vị trí trong bộ nhớ</td><td>Liền kề bắt buộc</td><td>Rải rác tuỳ ý</td></tr>
<tr><td>Thứ tự vật lý so với logic</td><td>Trùng nhau</td><td>Có thể khác hẳn</td></tr>
<tr><td>Lấy phần tử thứ i</td><td>O(1) — một phép nhân</td><td>O(n) — phải đi từ đầu</td></tr>
<tr><td>Chèn / xoá ở giữa</td><td>O(n) — dịch phần đuôi</td><td>O(1) nếu đã ở đúng chỗ</td></tr>
<tr><td>Tìm nhị phân</td><td>Được (nếu đã sắp)</td><td>Không thể (slide 19)</td></tr>
<tr><td>Bộ nhớ cho 5 số int</td><td>20 byte</td><td>~80 byte + 8 byte con trỏ đầu</td></tr>
<tr><td>Kích thước</td><td>Cố định khi khai báo</td><td>Lớn/nhỏ tuỳ ý lúc chạy</td></tr>
</table>
<p class="dap-an">✅ Đáp án: hai khung chứa cùng năm giá trị 66, 72, 74, 85, 96, nhưng chỉ khung (a) cất chúng theo đúng thứ tự vật lý ấy. Ở khung (b), các ô nằm trên giấy theo thứ tự 66, 72, 96, 85, 74 trong khi các liên kết áp đặt thứ tự tăng dần — bức hình đang cho bạn thấy rằng trong danh sách liên kết, vị trí trên giấy và vị trí trong danh sách chẳng liên quan gì tới nhau.</p>
<p class="meo">💡 Cụm <em>linking tool</em> của slide là bản tóm tắt một-từ hay nhất cả chương. Mảng = nối bằng cách ĐẾM. Danh sách liên kết = nối bằng cách TRỎ. Nếu nói được một cấu trúc dùng công cụ nối nào thì bạn suy ra được mọi chi phí trong bảng trên mà không cần học thuộc.</p>`],

      [18, '3. Linked list names versus nodes names',
        `<p class="y-chinh">🎯 The third appearance of the same distinction (after arrays on slide 5 and records on slide 12): <strong>the list has a name, the nodes do not</strong>. Figure 9.12 shows why that matters — the only way to name a node is by the pointer currently aimed at it.</p>
<ul>
<li><strong>Every label in Figure 9.12, read off the picture</strong> — a box <code>scores</code> on the far left with an arrow to the first node. Above the first node, <code>*scores</code>. Below it, two labels: <code>(*scores).data</code> pointing at the grey half, and <code>(*scores).link</code> pointing at the red half. Further right, a second pointer variable <code>cur</code> sitting below the third node with an arrow up into it, labelled <code>*cur</code>, and a dashed label <code>(*cur).link</code> for its red half. The last node's link carries the null cross.</li>
<li><strong>Decode the notation in two steps</strong> — <code>scores</code> is a pointer, so <code>*scores</code> is <em>the node it points to</em>, and <code>(*scores).data</code> is the data field of that node. Exactly the record-field dot from slide 12, applied to a node you reached through a pointer.</li>
<li><strong>Why the parentheses are not optional</strong> — in C, <code>.</code> binds tighter than <code>*</code>, so <code>*scores.data</code> would parse as <code>*(scores.data)</code> and fail to compile. That is precisely why C invented the arrow: <code>scores-&gt;data</code> means <code>(*scores).data</code> with no parentheses to get wrong. Measured on a real run: <code>p-&gt;x</code> and <code>(*p).x</code> both printed 7 for the same pointer.</li>
<li><strong>The deep point: nodes have no permanent names</strong> — <code>scores[3]</code> is a name that exists whether or not any code is currently looking at it. The third node of a linked list has <em>no</em> name until you place a pointer on it. <code>*cur</code> is not the name of a node; it is the name of "whatever <code>cur</code> points at right now", and it means a different node after every <code>cur ← (*cur).link</code>.</li>
<li><strong>Which is why the search of slide 19 needs two pointers</strong> — <code>cur</code> to name the node you are inspecting and <code>pre</code> to name the one before it, because there is no way to ask a node "who points at you?". A singly linked list can only be walked forwards, so you must carry the previous position with you or lose it.</li>
<li><strong>Three structures, one lesson, three notations</strong> — <code>scores[i]</code> for the array, <code>student.id</code> for the record, <code>(*cur).data</code> for the list. Objectives 3, 6 and 9 of slide 3 are this single idea asked three times.</li>
</ul>
<p class="dap-an">✅ Đáp án: <code>(*scores).data</code> is the data field of the first node; <code>(*cur).link</code> is the link field of whichever node <code>cur</code> currently points to. In C these are written <code>scores-&gt;data</code> and <code>cur-&gt;link</code>, and the parentheses in the deck's form are mandatory because <code>.</code> binds tighter than <code>*</code>.</p>
<p class="pitfall">⚠️ Losing the head pointer loses the whole list. If you write <code>scores = (*scores).link</code> while walking, every node before the new position becomes unreachable — the data is still in memory and no longer has any name at all. Always walk with a <em>temporary</em> pointer (<code>cur</code>), never with the list's own name. This is the single most common bug in first linked-list assignments.</p>`,
        `<p class="y-chinh">🎯 Lần thứ ba của cùng phép phân biệt ấy (sau mảng ở slide 5 và record ở slide 12): <strong>danh sách có tên, các nút thì KHÔNG</strong>. Hình 9.12 cho thấy vì sao điều đó quan trọng — cách duy nhất để gọi tên một nút là qua cái con trỏ đang nhắm vào nó.</p>
<ul>
<li><strong>Từng nhãn trong Hình 9.12, đọc thẳng từ hình</strong> — một ô <code>scores</code> ở tận trái với mũi tên tới nút đầu. Phía trên nút đầu ghi <code>*scores</code>. Phía dưới nó là hai nhãn: <code>(*scores).data</code> chỉ vào nửa xám, và <code>(*scores).link</code> chỉ vào nửa đỏ. Xa hơn về bên phải, một biến con trỏ thứ hai tên <code>cur</code> nằm dưới nút thứ ba với mũi tên chỉ ngược lên, mang nhãn <code>*cur</code>, cùng một nhãn nét đứt <code>(*cur).link</code> cho nửa đỏ của nó. Liên kết của nút cuối mang dấu chéo null.</li>
<li><strong>Giải mã ký hiệu theo hai bước</strong> — <code>scores</code> là con trỏ, nên <code>*scores</code> là <em>cái nút mà nó trỏ tới</em>, và <code>(*scores).data</code> là trường dữ liệu của nút ấy. Đúng bằng dấu chấm truy cập trường của slide 12, áp lên một nút mà bạn tới được qua con trỏ.</li>
<li><strong>Vì sao cặp ngoặc KHÔNG được bỏ</strong> — trong C, dấu <code>.</code> có độ ưu tiên cao hơn dấu <code>*</code>, nên <code>*scores.data</code> sẽ được hiểu thành <code>*(scores.data)</code> và không biên dịch nổi. Đó chính là lý do C nghĩ ra dấu mũi tên: <code>scores-&gt;data</code> nghĩa là <code>(*scores).data</code> mà không có cặp ngoặc nào để viết sai. Đo thật khi chạy: <code>p-&gt;x</code> và <code>(*p).x</code> đều in ra 7 cho cùng một con trỏ.</li>
<li><strong>Ý sâu: các nút KHÔNG có tên cố định</strong> — <code>scores[3]</code> là một cái tên tồn tại bất kể lúc đó có đoạn mã nào đang nhìn vào nó hay không. Còn nút thứ ba của một danh sách liên kết thì <em>KHÔNG</em> có tên nào cả cho tới khi bạn đặt một con trỏ lên nó. <code>*cur</code> không phải tên của một nút; nó là tên của "cái mà <code>cur</code> đang trỏ vào lúc này", và nó chỉ sang một nút khác sau mỗi lệnh <code>cur ← (*cur).link</code>.</li>
<li><strong>Và đó là lý do phép tìm kiếm ở slide 19 cần HAI con trỏ</strong> — <code>cur</code> để gọi tên nút đang xét và <code>pre</code> để gọi tên nút đứng trước nó, bởi không có cách nào hỏi một nút rằng "ai đang trỏ vào anh?". Danh sách liên kết đơn chỉ đi tới được, nên bạn phải mang theo vị trí trước đó, không thì mất luôn.</li>
<li><strong>Ba cấu trúc, một bài học, ba ký hiệu</strong> — <code>scores[i]</code> cho mảng, <code>student.id</code> cho record, <code>(*cur).data</code> cho danh sách. Mục tiêu 3, 6 và 9 ở slide 3 chính là một ý duy nhất được hỏi ba lần.</li>
</ul>
<p class="dap-an">✅ Đáp án: <code>(*scores).data</code> là trường dữ liệu của nút đầu tiên; <code>(*cur).link</code> là trường liên kết của bất cứ nút nào mà <code>cur</code> đang trỏ tới. Trong C người ta viết là <code>scores-&gt;data</code> và <code>cur-&gt;link</code>, và cặp ngoặc trong dạng viết của bộ slide là BẮT BUỘC vì <code>.</code> ưu tiên cao hơn <code>*</code>.</p>
<p class="pitfall">⚠️ Mất con trỏ đầu là mất cả danh sách. Nếu bạn viết <code>scores = (*scores).link</code> trong lúc duyệt, mọi nút đứng trước vị trí mới đều thành không với tới được — dữ liệu vẫn nằm trong bộ nhớ và không còn cái tên nào nữa. Luôn duyệt bằng một con trỏ <em>TẠM</em> (<code>cur</code>), đừng bao giờ duyệt bằng chính cái tên của danh sách. Đây là lỗi phổ biến nhất trong bài tập danh sách liên kết đầu tiên.</p>`],

      [19, '4. Operations on linked lists (searching)',
        `<p class="y-chinh">🎯 <strong>Searching a linked list can only be sequential.</strong> Not "is usually", but "can only be" — because the nodes have no names to compute with, so binary search has nothing to halve. Algorithm 9.1 walks the list with the two pointers <code>pre</code> and <code>cur</code>.</p>
<ul>
<li><strong>Why binary search is impossible, spelled out</strong> — binary search needs the middle element in O(1). In an array that is <code>a[(low+high)/2]</code>, one multiplication. In a linked list, reaching the middle means walking half the list, so each "halving" costs O(<em>n</em>) and the whole thing degrades to worse than sequential. Sorting the list does not help. This is the clearest example in the course of a data structure <em>forbidding</em> an algorithm.</li>
<li><strong>The algorithm header, read off the slide</strong> — Purpose: "Search the list using two pointers: pre and cur". Pre-condition: the linked list (head pointer) and the target value. Post-condition: None. Return: "The position of pre and cur pointers and the value of the flag".</li>
<li><strong>The body, exactly as printed</strong> — <code>pre ← null</code>; <code>cur ← list</code>; <code>while (target &lt; (*cur).data) { pre ← cur; cur ← (*cur).link }</code>; <code>if ((*cur).data = target) flag ← true else flag ← false</code>.</li>
<li><strong>What it returns and why that is clever</strong> — not just true/false, but <em>where it stopped</em>. On a sorted list, if the target is absent, <code>pre</code> and <code>cur</code> now straddle the gap where it should go. That is why slide 20's insertion algorithm starts by calling this search: one walk does the finding and the positioning at once.</li>
<li><strong>Hand-trace on the list from slide 17</strong> — 66 → 72 → 74 → 85 → 96, target 85. The intent is: advance while the current value is still <em>smaller</em> than the target, then stop. That means the loop test should be <code>target &gt; (*cur).data</code>. Trace it: 85 &gt; 66 advance, 85 &gt; 72 advance, 85 &gt; 74 advance, 85 &gt; 85 false → stop at the node holding 85, <code>pre</code> on 74, flag true. Three hops.</li>
<li><strong>The printed condition is reversed, and it was run to be sure.</strong> With <code>target &lt; (*cur).data</code> as printed, the first test is 85 &lt; 66, which is false immediately, so the loop never runs, <code>cur</code> stays on 66, and the flag comes out <strong>false</strong> for a value that <em>is</em> in the list. Executed on the same five numbers: the printed condition stops at index 0 (value 66, flag false); the corrected condition stops at index 3 (value 85, flag true).</li>
<li><strong>A second defect in the same three lines</strong> — there is no <code>cur ≠ null</code> guard. Search for 200 in that list and the loop advances past 96, sets <code>cur</code> to null, and then evaluates <code>(*cur).data</code> — dereferencing null, a crash. The correct loop is <code>while (cur ≠ null AND target &gt; (*cur).data)</code>, and the final test must also check <code>cur ≠ null</code> before touching <code>(*cur).data</code>.</li>
<li><strong>Cost</strong> — O(<em>n</em>) in the worst case, <em>n</em>/2 on average for a present value, <em>n</em> for an absent one in an unsorted list. On a <em>sorted</em> list the search may stop early, which is the only speed benefit sorting gives a linked list.</li>
</ul>
<p class="dap-an">✅ Đáp án: searching 85 in 66,72,74,85,96 should take 3 hops and return flag = true with <code>cur</code> on 85 and <code>pre</code> on 74. The slide's condition <code>while (target &lt; (*cur).data)</code> is <strong>backwards</strong> — run on those five numbers it exits at the very first node and returns flag = false. The correct condition is <code>while (cur ≠ null AND target &gt; (*cur).data)</code>. The slide is wrong on both counts (direction and the missing null guard); state that rather than copying it.</p>
<p class="pitfall">⚠️ Do not conclude from this that "linked lists are slow at searching and arrays are fast". An <em>unsorted</em> array is also O(<em>n</em>) to search. The real statement is narrower and is the one the slide makes: a linked list cannot use binary search <em>even when sorted</em>, so sorting buys it an early exit and nothing more.</p>`,
        `<p class="y-chinh">🎯 <strong>Tìm kiếm trên danh sách liên kết CHỈ có thể tuần tự.</strong> Không phải "thường là", mà là "chỉ có thể" — vì các nút không có tên để mà tính toán, nên tìm nhị phân chẳng có gì để chia đôi. Thuật toán 9.1 duyệt danh sách bằng hai con trỏ <code>pre</code> và <code>cur</code>.</p>
<ul>
<li><strong>Vì sao tìm nhị phân là bất khả, nói cho rõ</strong> — tìm nhị phân cần lấy phần tử GIỮA trong O(1). Với mảng đó là <code>a[(low+high)/2]</code>, một phép nhân. Với danh sách liên kết, tới được giữa nghĩa là phải đi hết nửa danh sách, nên mỗi lần "chia đôi" tốn O(<em>n</em>) và cả thuật toán tụt xuống còn tệ hơn tìm tuần tự. Sắp xếp danh sách cũng không cứu được. Đây là ví dụ rõ nhất cả môn về việc một cấu trúc dữ liệu <em>CẤM</em> một thuật toán.</li>
<li><strong>Phần đầu thuật toán, đọc thẳng từ slide</strong> — Purpose: "Search the list using two pointers: pre and cur". Điều kiện trước: danh sách liên kết (con trỏ đầu) và giá trị cần tìm. Điều kiện sau: None. Trả về: "Vị trí của hai con trỏ pre, cur và giá trị của cờ flag".</li>
<li><strong>Phần thân, đúng nguyên văn như in</strong> — <code>pre ← null</code>; <code>cur ← list</code>; <code>while (target &lt; (*cur).data) { pre ← cur; cur ← (*cur).link }</code>; <code>if ((*cur).data = target) flag ← true else flag ← false</code>.</li>
<li><strong>Nó trả về gì và vì sao chỗ đó khôn</strong> — không chỉ đúng/sai, mà cả <em>chỗ nó dừng lại</em>. Trên danh sách đã sắp, nếu giá trị không có thì <code>pre</code> và <code>cur</code> lúc này kẹp đúng cái khe mà nó đáng lẽ phải nằm. Đó là lý do thuật toán chèn ở slide 20 bắt đầu bằng cách gọi chính phép tìm này: một lượt đi vừa tìm vừa định vị luôn.</li>
<li><strong>Chạy tay trên danh sách của slide 17</strong> — 66 → 72 → 74 → 85 → 96, cần tìm 85. Ý đồ là: đi tiếp CHỪNG NÀO giá trị hiện tại còn <em>NHỎ HƠN</em> giá trị cần tìm, rồi dừng. Tức điều kiện vòng lặp phải là <code>target &gt; (*cur).data</code>. Chạy thử: 85 &gt; 66 đi tiếp, 85 &gt; 72 đi tiếp, 85 &gt; 74 đi tiếp, 85 &gt; 85 sai → dừng tại nút chứa 85, <code>pre</code> ở 74, cờ true. Ba bước nhảy.</li>
<li><strong>Điều kiện in trên slide bị NGƯỢC, và đã chạy thật để chắc chắn.</strong> Với <code>target &lt; (*cur).data</code> đúng như in, phép kiểm đầu tiên là 85 &lt; 66, sai ngay lập tức, nên vòng lặp không chạy lần nào, <code>cur</code> đứng yên ở 66, và cờ trả về <strong>false</strong> cho một giá trị vốn CÓ trong danh sách. Chạy thật trên đúng năm con số ấy: điều kiện như in dừng ở chỉ số 0 (giá trị 66, cờ false); điều kiện đã sửa dừng ở chỉ số 3 (giá trị 85, cờ true).</li>
<li><strong>Một khiếm khuyết thứ hai trong cùng ba dòng</strong> — không có chốt <code>cur ≠ null</code>. Đi tìm 200 trong danh sách ấy thì vòng lặp chạy qua khỏi 96, đặt <code>cur</code> thành null, rồi vẫn tính <code>(*cur).data</code> — giải tham chiếu null, sập chương trình. Vòng lặp đúng phải là <code>while (cur ≠ null AND target &gt; (*cur).data)</code>, và phép kiểm cuối cũng phải xét <code>cur ≠ null</code> trước khi chạm vào <code>(*cur).data</code>.</li>
<li><strong>Chi phí</strong> — O(<em>n</em>) ở trường hợp xấu nhất, trung bình <em>n</em>/2 nếu giá trị có mặt, và <em>n</em> nếu vắng mặt trên danh sách chưa sắp. Trên danh sách ĐÃ SẮP thì phép tìm có thể dừng sớm, và đó là lợi ích tốc độ DUY NHẤT mà việc sắp xếp mang lại cho danh sách liên kết.</li>
</ul>
<p class="dap-an">✅ Đáp án: tìm 85 trong 66,72,74,85,96 phải mất 3 bước nhảy và trả về cờ = true với <code>cur</code> ở 85 và <code>pre</code> ở 74. Điều kiện <code>while (target &lt; (*cur).data)</code> của slide bị <strong>NGƯỢC</strong> — chạy thật trên năm con số ấy thì nó thoát ngay ở nút đầu tiên và trả về cờ = false. Điều kiện đúng là <code>while (cur ≠ null AND target &gt; (*cur).data)</code>. Slide sai cả hai chỗ (chiều so sánh và thiếu chốt null); hãy nói rõ ra thay vì chép lại.</p>
<p class="pitfall">⚠️ Đừng từ đây mà kết luận "danh sách liên kết tìm chậm còn mảng tìm nhanh". Một mảng <em>CHƯA SẮP</em> cũng tốn O(<em>n</em>) để tìm. Phát biểu đúng thì hẹp hơn, và đúng là điều slide nói: danh sách liên kết không dùng được tìm nhị phân <em>ngay cả khi đã sắp</em>, nên việc sắp xếp chỉ mua cho nó quyền dừng sớm, không hơn.</p>`],
    ]),
  ].join('\n'),
};
