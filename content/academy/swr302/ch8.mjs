/**
 * SWR302 · Academy Chapter 4 (Analysis) additions — book chapter Ch.8
 * "Understanding user requirements" (36 slides: use cases & user stories),
 * split into FOUR focused lessons:
 *   4.3 usage-centric: use cases vs user stories   (1–11)
 *   4.4 use case anatomy: actors, flows, relations (12–24)
 *   4.5 working with use cases → functional reqs   (25–33)
 *   4.6 the use case specification template        (34–36)
 * Exported as an array spliced into academy Chapter 4 before its quiz; existing
 * 4.1 / 4.2 lessons untouched. Grounded slide-by-slide in
 * CHAPTER_8_Understanding user requirements.pdf.
 */
import { walk, walkHead, bi, books } from './_slides.mjs';

/* 4.3 — usage-centric: use cases vs user stories */
const C1 = {
  title: '4.3 — Slides: use cases vs user stories (Ch.8 · 1–11)|||4.3 — Slide: use case vs user story (Ch.8 · 1–11)',
  slug: 'swr302-4-3-ch8-usage-centric',
  type: 'DOCUMENT',
  description: 'Slide 1–11: cách tiếp cận usage-centric vs product-centric; use case (chuỗi tương tác actor–hệ thống đạt kết quả có giá trị, tên = động từ + tân ngữ) và user story (As a…, I want…, so that…); cách hai thứ dẫn tới functional requirement & test; và use case diagram.',
  content: [
    bi(
      `<span class="eyebrow">Chapter 4 · Lesson 4.3 · Book Ch.8 slides 1–11</span>
<h2>Understanding user requirements: use cases vs user stories</h2>
<p class="lead">The best way to find the right functionality is to understand what users <em>do</em>. This lesson contrasts the <strong>usage-centric</strong> and product-centric approaches, and introduces the two workhorses of user requirements: <strong>use cases</strong> and <strong>user stories</strong>.</p>`,
      `<span class="eyebrow">Chương 4 · Bài 4.3 · Slide 1–11 sách Ch.8</span>
<h2>Hiểu yêu cầu người dùng: use case vs user story</h2>
<p class="lead">Cách tốt nhất để tìm đúng chức năng là hiểu người dùng <em>làm gì</em>. Bài này đối chiếu cách <strong>usage-centric</strong> với product-centric, và giới thiệu hai công cụ chủ lực của user requirement: <strong>use case</strong> và <strong>user story</strong>.</p>`,
    ),
    walkHead('ch8', 1, 11),
    walk('ch8', [
      [1, 'Understanding user requirements', `<p>Title slide — the chapter on capturing what users need to accomplish.</p>`, `<p>Slide bìa — chương về nắm bắt điều người dùng cần hoàn thành.</p>`],
      [2, 'Objectives', `<p>Learn the usage-centric approach, how to write and structure use cases (and user stories), how they lead to functional requirements and tests, and the traps to avoid.</p>`, `<p>Học cách tiếp cận usage-centric, cách viết và cấu trúc use case (và user story), cách chúng dẫn tới functional requirement và test, và các bẫy cần tránh.</p>`],
      [3, 'Contents', `<p>Road map: usage vs product-centric, use cases &amp; user stories, the use case approach &amp; anatomy, working with use cases, and the specification template. Lessons 4.3–4.6 follow.</p>`, `<p>Bản đồ: usage vs product-centric, use case &amp; user story, cách tiếp cận & giải phẫu use case, làm việc với use case, và template đặc tả. Bài 4.3–4.6 theo đây.</p>`],
      [4, 'Usage-centric or product-centric?', `<p>Two strategies. <strong>Usage-centric</strong>: understand user goals to <em>derive</em> the needed functionality. <strong>Product-centric</strong>: define features you expect will sell — riskier, since you may build features no one uses. In most cases, a <strong>user- and usage-centric</strong> approach is better: it reveals the necessary functionality, avoids unused features, and helps prioritisation. First understand what users intend to do with the system.</p>`, `<p>Hai chiến lược. <strong>Usage-centric</strong>: hiểu mục tiêu người dùng để <em>suy ra</em> chức năng cần. <strong>Product-centric</strong>: định feature bạn kỳ vọng sẽ bán được — rủi ro hơn, vì có thể xây feature không ai dùng. Đa số trường hợp, cách <strong>user- và usage-centric</strong> tốt hơn: nó lộ chức năng cần, tránh feature thừa, và giúp ưu tiên. Trước hết hiểu người dùng định làm gì với hệ thống.</p>`],
      [5, 'Use cases', `<p>A <strong>use case</strong> describes a sequence of interactions between the system and an <strong>external actor</strong> that results in the actor achieving an outcome of <strong>value</strong>. Name it as a strong, descriptive <strong>verb + object</strong> (e.g. "Request a Chemical") so its value is evident from the name.</p>`, `<p>Một <strong>use case</strong> mô tả chuỗi tương tác giữa hệ thống và một <strong>actor bên ngoài</strong> dẫn tới actor đạt một kết quả <strong>có giá trị</strong>. Đặt tên là <strong>động từ + tân ngữ</strong> mạnh, mô tả (vd "Request a Chemical") để thấy giá trị ngay từ tên.</p>`],
      [6, '(diagram)', `<p>A figure illustrating the actor–system interaction of a use case.</p>`, `<p>Một hình minh hoạ tương tác actor–hệ thống của một use case.</p>`],
      [7, 'User stories', `<p>On agile, a <strong>user story</strong> is a "short, simple description of a feature from the perspective of the person who wants it." Common template: <em>"As a <strong>[type of user]</strong>, I want <strong>[goal]</strong> so that <strong>[reason]</strong>."</em> Its advantage over a bare use case name: it also identifies the <strong>user class</strong> (= the primary actor) and the <strong>rationale</strong> (which a use case puts in its brief description).</p>`, `<p>Trên agile, một <strong>user story</strong> là "mô tả ngắn gọn về một feature từ góc của người muốn nó." Template phổ biến: <em>"Là <strong>[loại người dùng]</strong>, tôi muốn <strong>[mục tiêu]</strong> để <strong>[lý do]</strong>."</em> Lợi thế so với chỉ tên use case: nó còn nêu <strong>user class</strong> (= primary actor) và <strong>lý do</strong> (mà use case đặt trong brief description).</p>`],
      [8, '(diagram)', `<p>A figure comparing the use case and user story formats.</p>`, `<p>Một hình so sánh định dạng use case và user story.</p>`],
      [9, 'From user requirements to functional requirements (Figure 8-1)', `<p>With <strong>use cases</strong>: the BA works with user reps to understand the actor–system dialog, structures it in a use case template, then <strong>derives functional requirements</strong> (for developers) and <strong>tests</strong> (for testers). With <strong>user stories</strong>: the story is a <em>placeholder for a just-in-time conversation</em>; big stories (<strong>epics</strong>) are split to fit one iteration, and agile teams typically elaborate a refined story into <strong>acceptance tests</strong> ("conditions of satisfaction") rather than written functional requirements.</p>`, `<p>Với <strong>use case</strong>: BA làm việc với đại diện người dùng để hiểu đối thoại actor–hệ thống, cấu trúc vào template, rồi <strong>suy ra functional requirement</strong> (cho developer) và <strong>test</strong> (cho tester). Với <strong>user story</strong>: story là <em>chỗ giữ chỗ cho một cuộc trò chuyện đúng lúc</em>; story lớn (<strong>epic</strong>) tách để vừa một iteration, và nhóm agile thường triển khai một story tinh gọn thành <strong>acceptance test</strong> ("điều kiện thoả mãn") thay vì functional requirement viết ra.</p>`],
      [10, 'The use case approach', `<p>Questions to explore each use case: what will the actor use the system for? Will they create/store/change/remove/read data? Inform the system of external events? Need to be informed of occurrences? Provide a brief <strong>description</strong> elaborating intent (e.g. "Program Vacation Setting — Homeowner sets lighting and alarm options for an extended stay away").</p>`, `<p>Câu hỏi để khám phá mỗi use case: actor dùng hệ thống để làm gì? Họ tạo/lưu/đổi/xoá/đọc dữ liệu? Báo hệ thống về sự kiện ngoài? Cần được báo về sự việc? Cho một <strong>mô tả</strong> ngắn nêu ý định (vd "Program Vacation Setting — Chủ nhà đặt tuỳ chọn đèn và báo động cho chuyến đi dài").</p>`],
      [11, 'Use case diagram (Figure 8-2)', `<p>A <strong>use case diagram</strong> (UML) gives the bird's-eye view: <strong>actors</strong> (stick figures) connected to the <strong>use cases</strong> (ovals) they participate in, inside the system boundary. The partial Chemical Tracking System diagram shows who does what at a glance — a great scope and completeness check, though the detail lives in the specifications.</p>`, `<p>Một <strong>use case diagram</strong> (UML) cho cái nhìn tổng: <strong>actor</strong> (hình que) nối tới các <strong>use case</strong> (hình oval) họ tham gia, trong ranh giới hệ thống. Sơ đồ một phần của Chemical Tracking System cho thấy ai làm gì trong nháy mắt — một cách kiểm phạm vi và tính đầy đủ tốt, dù chi tiết nằm trong các đặc tả.</p>`],
    ]),
    books([
      ['wiegers', 'Chapter 8, "Usage-centric or product-centric?", "Use cases and user stories"', 'Chương 8, "Usage-centric or product-centric?", "Use cases and user stories"'],
      ['cos', 'the COS Use Cases document is a full worked example', 'tài liệu COS Use Cases là một ví dụ giải đầy đủ'],
    ]),
  ].join('\n'),
};

/* 4.4 — use case anatomy */
const C2 = {
  title: '4.4 — Slides: use case anatomy — actors, flows, relationships (Ch.8 · 12–24)|||4.4 — Slide: giải phẫu use case — actor, luồng, quan hệ (Ch.8 · 12–24)',
  slug: 'swr302-4-4-ch8-anatomy',
  type: 'DOCUMENT',
  description: 'Slide 12–24: dàn ý use case, giải phẫu (name/description/actors/flows bắt buộc; pre/postconditions… tuỳ chọn), actor là gì, use case vs scenario, quy ước đánh nhãn, pre/postconditions, normal/alternative flows & exceptions, mức độ "dressed", quan hệ extend & include, ghép chuỗi use case, và use case với business rule.',
  content: [
    bi(
      `<span class="eyebrow">Chapter 4 · Lesson 4.4 · Book Ch.8 slides 12–24</span>
<h2>Use case anatomy: actors, flows &amp; relationships</h2>
<p class="lead">The inside of a use case: its mandatory and optional parts, the normal / alternative / exception flows, the <strong>extend</strong> and <strong>include</strong> relationships, and how preconditions and postconditions chain use cases together.</p>`,
      `<span class="eyebrow">Chương 4 · Bài 4.4 · Slide 12–24 sách Ch.8</span>
<h2>Giải phẫu use case: actor, luồng &amp; quan hệ</h2>
<p class="lead">Bên trong một use case: các phần bắt buộc và tuỳ chọn, các luồng normal / alternative / exception, quan hệ <strong>extend</strong> và <strong>include</strong>, và cách pre/postcondition ghép các use case thành chuỗi.</p>`,
    ),
    walkHead('ch8', 12, 24),
    walk('ch8', [
      [12, 'Outline the use cases', `<p>First outline the <strong>basic flow</strong> (the most common path): what event starts it? how does it end? does it repeat behaviour? Then outline <strong>alternative flows</strong> by asking: what optional situations, odd cases, variants; what may go wrong, may not happen, or may block resources?</p>`, `<p>Trước tiên phác <strong>basic flow</strong> (đường phổ biến nhất): sự kiện nào bắt đầu? kết thúc ra sao? có lặp hành vi không? Rồi phác <strong>alternative flow</strong> bằng cách hỏi: tình huống tuỳ chọn, ca lạ, biến thể nào; cái gì có thể sai, không xảy ra, hoặc chặn tài nguyên?</p>`],
      [13, 'Use case anatomy', `<p><strong>Mandatory</strong> elements: name (unique, descriptive), brief description, actors, flow of events (basic + alternate). <strong>Optional</strong>: preconditions, postconditions, system/subsystem level, other stakeholders, special requirements (e.g. performance). Fill only what adds value.</p>`, `<p>Phần <strong>bắt buộc</strong>: name (duy nhất, mô tả), brief description, actors, flow of events (basic + alternate). <strong>Tuỳ chọn</strong>: preconditions, postconditions, mức system/subsystem, stakeholder khác, yêu cầu đặc biệt (vd hiệu năng). Chỉ điền cái thêm giá trị.</p>`],
      [14, 'Actors', `<p>An <strong>actor</strong> is someone or something that interacts with the system: a <strong>user</strong> (Homeowner, Author), <strong>another system/application</strong> (a control switch), or a <strong>device</strong> (lights, printer). An actor need not be human. In UML it's the stick figure at the edge of a use case diagram.</p>`, `<p>Một <strong>actor</strong> là ai hoặc cái gì tương tác với hệ thống: một <strong>người dùng</strong> (Chủ nhà, Tác giả), <strong>hệ/ứng dụng khác</strong> (một control switch), hoặc một <strong>thiết bị</strong> (đèn, máy in). Actor không nhất thiết là người. Trong UML là hình que ở rìa use case diagram.</p>`],
      [15, 'Use cases and usage scenarios', `<p>A <strong>use case</strong> is a discrete standalone activity yielding value (it may bundle related activities with a common goal). A <strong>scenario</strong> is a single specific instance of usage. So a use case is a <strong>collection of related scenarios</strong>; a scenario is one instance of a use case. You can start general (use case → specific scenarios) or generalise from a concrete scenario up to the use case.</p>`, `<p>Một <strong>use case</strong> là một hoạt động đứng riêng, rời rạc, sinh giá trị (có thể gộp các hoạt động liên quan cùng mục tiêu). Một <strong>scenario</strong> là một trường hợp sử dụng cụ thể đơn lẻ. Vậy use case là một <strong>tập scenario liên quan</strong>; scenario là một instance của use case. Bạn có thể bắt đầu tổng quát (use case → scenario cụ thể) hoặc khái quát từ một scenario cụ thể lên use case.</p>`],
      [16, 'Detailing use cases — the topics', `<p>Seven detailing topics next: labeling convention, pre/postconditions, normal/alternative/exception flows, "dressing" (level of detail), extend &amp; include, aligning pre/postconditions, and use cases &amp; business rules.</p>`, `<p>Bảy chủ đề chi tiết hoá ngay sau: quy ước đánh nhãn, pre/postcondition, luồng normal/alternative/exception, "dressing" (mức chi tiết), extend &amp; include, căn pre/postcondition, và use case &amp; business rule.</p>`],
      [17, '1. Use case labeling convention', `<p>A use case has many small packets, so label them: a use case gets a sequence number + goal name (<strong>UC-4 Request a Chemical</strong>); its normal flow is <strong>4.0</strong>; alternative flows increment the decimal (<strong>4.1, 4.2</strong>); exceptions append <strong>.E<em>n</em></strong> (the first exception on the normal flow is <strong>4.0.E1</strong>). This keeps everything traceable.</p>`, `<p>Một use case có nhiều gói nhỏ, nên đánh nhãn: use case được số thứ tự + tên mục tiêu (<strong>UC-4 Request a Chemical</strong>); normal flow là <strong>4.0</strong>; alternative flow tăng phần thập phân (<strong>4.1, 4.2</strong>); exception thêm <strong>.E<em>n</em></strong> (exception đầu của normal flow là <strong>4.0.E1</strong>). Giúp mọi thứ truy vết được.</p>`],
      [18, '2. Preconditions & postconditions', `<p><strong>Preconditions</strong> = prerequisites that must be true before the use case can begin; the system tests them when it detects the trigger (the trigger itself is <em>not</em> a precondition). <strong>Postconditions</strong> = the state after successful execution — observable to the user (balance shown), physical (ATM dispensed cash + receipt), or internal (account debited by the amount plus fees).</p>`, `<p><strong>Precondition</strong> = điều kiện phải đúng trước khi use case bắt đầu; hệ thống kiểm chúng khi phát hiện trigger (bản thân trigger <em>không</em> phải precondition). <strong>Postcondition</strong> = trạng thái sau khi chạy thành công — quan sát được (hiện số dư), vật lý (ATM nhả tiền + biên lai), hoặc nội bộ (tài khoản bị trừ số tiền cộng phí).</p>`],
      [19, '3. Normal, alternative flows & exceptions', `<p>One scenario is the <strong>normal flow</strong> (a.k.a. main/basic flow, happy path) — a numbered list of steps naming who does each. <strong>Alternative flows</strong> deliver the same outcome via less-common variations, branching at a decision point and maybe rejoining. <strong>Exceptions</strong> are anticipated error conditions and how they're handled — the user may recover, or the use case may terminate without success. ("The default should be…" = normal flow; "The user should also be able to…" = alternative flow.)</p>`, `<p>Một scenario là <strong>normal flow</strong> (main/basic flow, happy path) — danh sách bước đánh số nêu ai làm gì. <strong>Alternative flow</strong> cho cùng kết quả qua biến thể ít gặp hơn, rẽ nhánh tại một điểm quyết định và có thể nhập lại. <strong>Exception</strong> là điều kiện lỗi lường trước và cách xử lý — người dùng có thể khôi phục, hoặc use case kết thúc không thành công. ("Mặc định nên là…" = normal flow; "Người dùng cũng nên…" = alternative flow.)</p>`],
      [20, 'Activity diagram (Figure 8-4)', `<p>An <strong>activity diagram</strong> visualises the step sequence of the normal and alternative flows — a flowchart of the actor–system dialog. Useful for spotting missing branches or unreachable steps that plain text hides.</p>`, `<p>Một <strong>activity diagram</strong> trực quan hoá trình tự bước của normal và alternative flow — một lưu đồ của đối thoại actor–hệ thống. Hữu ích để phát hiện nhánh thiếu hoặc bước không tới được mà văn bản thuần giấu đi.</p>`],
      [21, '4. Dressing the use cases', `<p>Match detail to need. A <strong>casual</strong> use case is just a narrative (maybe only the Description); a <strong>fully dressed</strong> one fills the whole template; anything in between is fine, and not all use cases need the same detail. <strong>Fully dress</strong> when: users aren't closely engaged; the app is complex/high-risk; requirements are novel; use cases are the most detailed spec developers get; you'll build comprehensive tests from them; or remote teams need shared memory. The goal — not dogma — is enough understanding to build at low rework risk.</p>`, `<p>Khớp mức chi tiết với nhu cầu. Use case <strong>casual</strong> chỉ là một đoạn kể (có thể chỉ Description); <strong>fully dressed</strong> điền cả template; ở giữa cũng được, và không phải use case nào cũng cần cùng mức. <strong>Dress đầy đủ</strong> khi: người dùng không sát nhóm; app phức tạp/rủi ro cao; yêu cầu mới lạ; use case là spec chi tiết nhất developer nhận; bạn sẽ xây test toàn diện từ chúng; hoặc nhóm ở xa cần bộ nhớ chung. Mục tiêu — không giáo điều — là đủ hiểu để xây với ít rủi ro làm lại.</p>`],
      [22, '5. Extend & include', `<p>Two use case relationships. <strong>Extend</strong>: an optional/standalone use case adds behaviour to another at a point in its flow (e.g. "Search Vendor Catalog" extends "Request a Chemical" during an alternative flow). <strong>Include</strong>: several use cases share common steps, so factor them into a separate use case that the others <em>include</em> (e.g. an "Authenticate User" included by many). Include = reuse; extend = optional addition.</p>`, `<p>Hai quan hệ use case. <strong>Extend</strong>: một use case tuỳ chọn/đứng riêng thêm hành vi vào cái khác tại một điểm trong luồng (vd "Search Vendor Catalog" extend "Request a Chemical" trong một alternative flow). <strong>Include</strong>: nhiều use case chung các bước, nên tách vào một use case riêng mà các cái khác <em>include</em> (vd "Authenticate User" được nhiều cái include). Include = tái dùng; extend = phần thêm tuỳ chọn.</p>`],
      [23, '6. Aligning preconditions & postconditions', `<p>Users often chain use cases into a "macro" task: "Buy Product" = "Search Catalog" → "Add to Cart" → "Pay for Items". For chaining to work, each use case must leave the system in a state that lets the next begin — the <strong>postconditions of one must satisfy the preconditions of the next</strong>. Pre/postconditions are the boundaries that let use cases compose.</p>`, `<p>Người dùng thường ghép chuỗi use case thành một tác vụ "macro": "Buy Product" = "Search Catalog" → "Add to Cart" → "Pay". Để ghép được, mỗi use case phải để hệ ở trạng thái cho cái kế bắt đầu — <strong>postcondition của cái này phải thoả precondition của cái kế</strong>. Pre/postcondition là ranh giới cho phép use case ghép lại.</p>`],
      [24, '7. Use cases & business rules', `<p>They're intertwined. A business rule may constrain <strong>which roles</strong> can perform a use case or an alternative flow (imposing preconditions to test), and may govern <strong>steps</strong> by defining valid inputs or computations (e.g. an airline seat-selection rule that changes the fare for premium seats). When specifying a use case, <strong>record the identifiers of the business rules that affect it</strong> and which part each affects.</p>`, `<p>Chúng đan xen. Một business rule có thể ràng buộc <strong>vai nào</strong> được thực hiện use case hay một alternative flow (áp precondition cần kiểm), và có thể chi phối <strong>các bước</strong> bằng cách định input hợp lệ hoặc phép tính (vd rule chọn ghế hãng bay đổi giá vé cho ghế cao cấp). Khi đặc tả một use case, <strong>ghi mã các business rule ảnh hưởng nó</strong> và mỗi rule ảnh hưởng phần nào.</p>`],
    ]),
    books([['wiegers', 'Chapter 8, "Use case anatomy", "Use cases and usage scenarios" (labeling → business rules)', 'Chương 8, "Use case anatomy", "Use cases and usage scenarios" (labeling → business rule)']]),
  ].join('\n'),
};

/* 4.5 — working with use cases → functional requirements */
const C3 = {
  title: '4.5 — Slides: working with use cases → functional requirements (Ch.8 · 25–33)|||4.5 — Slide: làm việc với use case → functional requirement (Ch.8 · 25–33)',
  slug: 'swr302-4-5-ch8-working',
  type: 'DOCUMENT',
  description: 'Slide 25–33: cách xác định use case (từ actor/scenario/event/CRUD/context diagram); khám phá & validate use case theo lớp; bốn cách gắn use case với functional requirement (use cases only / +functional req / functional req only / +tests); các bẫy use case; và lợi ích của yêu cầu usage-centric.',
  content: [
    bi(
      `<span class="eyebrow">Chapter 4 · Lesson 4.5 · Book Ch.8 slides 25–33</span>
<h2>Working with use cases → functional requirements</h2>
<p class="lead">How to <strong>identify</strong> use cases, explore and validate them iteratively, turn them into the <strong>functional requirements developers actually build</strong>, avoid the classic traps, and reap the benefits of a usage-centric approach.</p>`,
      `<span class="eyebrow">Chương 4 · Bài 4.5 · Slide 25–33 sách Ch.8</span>
<h2>Làm việc với use case → functional requirement</h2>
<p class="lead">Cách <strong>xác định</strong> use case, khám phá và validate chúng theo vòng lặp, biến chúng thành <strong>functional requirement mà developer thực sự xây</strong>, tránh các bẫy kinh điển, và gặt lợi ích của cách usage-centric.</p>`,
    ),
    walkHead('ch8', 25, 33),
    walk('ch8', [
      [25, 'Identifying use cases', `<p>Several ways: identify the <strong>actors</strong> first, then the business processes and the actor–system interactions; write <strong>scenarios</strong> then generalise; ask "what tasks convert inputs to outputs?"; relate <strong>external events</strong> to actors and use cases; use <strong>CRUD</strong> to find entities needing use cases; or examine the <strong>context diagram</strong> and ask what each external entity wants to achieve.</p>`, `<p>Vài cách: xác định <strong>actor</strong> trước, rồi quy trình nghiệp vụ và tương tác actor–hệ thống; viết <strong>scenario</strong> rồi khái quát; hỏi "tác vụ nào biến input thành output?"; liên hệ <strong>sự kiện ngoài</strong> với actor và use case; dùng <strong>CRUD</strong> tìm entity cần use case; hoặc xét <strong>context diagram</strong> và hỏi mỗi thực thể ngoài muốn đạt gì.</p>`],
      [26, 'Working with use cases — the topics', `<p>Six topics next: exploring use cases, validating them, use cases &amp; functional requirements (four documentation options), traps to avoid, and the benefits of usage-centric requirements.</p>`, `<p>Sáu chủ đề ngay sau: khám phá use case, validate, use case &amp; functional requirement (bốn cách ghi), các bẫy cần tránh, và lợi ích của yêu cầu usage-centric.</p>`],
      [27, '1. Exploring use cases', `<p>The CTS workshops explored each use case in <strong>layers</strong>: identify the actor + write the short description; estimate frequency (an early capacity indicator); define pre/postconditions (the boundaries); then derive the <strong>normal flow</strong> from how participants envision the dialog. They didn't cover every use case or every detail in one marathon — they took broad strokes on top-priority use cases and refined iteratively <strong>just before implementation</strong>.</p>`, `<p>Workshop CTS khám phá mỗi use case theo <strong>lớp</strong>: xác định actor + viết mô tả ngắn; ước tần suất (chỉ báo dung lượng sớm); định pre/postcondition (ranh giới); rồi suy ra <strong>normal flow</strong> từ cách người dự hình dung đối thoại. Họ không phủ mọi use case hay mọi chi tiết trong một buổi marathon — họ vẽ nét lớn cho use case ưu tiên cao và tinh chỉnh lặp <strong>ngay trước khi hiện thực</strong>.</p>`],
      [28, '2. Validating use cases', `<p>After each workshop the BAs <strong>derived functional requirements</strong> and drew analysis models (e.g. a state-transition diagram of chemical-request statuses spanning several use cases). A day or two later, participants <strong>reviewed</strong> the use cases + requirements — informal reviews found undiscovered alternative flows, new exceptions, wrong requirements and missing steps. Lesson: leave <strong>a day between workshops</strong> — fresh eyes catch far more.</p>`, `<p>Sau mỗi workshop, BA <strong>suy ra functional requirement</strong> và vẽ analysis model (vd state-transition của trạng thái yêu cầu hoá chất trải nhiều use case). Một hai ngày sau, người dự <strong>review</strong> use case + yêu cầu — review không chính thức tìm ra alternative flow chưa biết, exception mới, yêu cầu sai và bước thiếu. Bài học: chừa <strong>một ngày giữa các workshop</strong> — mắt tươi bắt được nhiều hơn hẳn.</p>`],
      [29, '3. Use cases → functional requirements', `<p>Developers implement <strong>functional requirements</strong>, not business/user requirements. Use cases show the <em>external</em> behaviour from the user's view but <strong>don't contain everything a developer needs</strong> — even fully dressed ones raise questions. The BA must <strong>derive the missing functional requirements</strong> and communicate them; this analysis (user's view → developer's view) is a key way the BA adds value. Four ways to document the link, next.</p>`, `<p>Developer hiện thực <strong>functional requirement</strong>, không phải business/user requirement. Use case cho thấy hành vi <em>bên ngoài</em> theo góc người dùng nhưng <strong>không chứa mọi thứ developer cần</strong> — kể cả bản fully dressed cũng gây thắc mắc. BA phải <strong>suy ra các functional requirement còn thiếu</strong> và truyền đạt; phân tích này (góc người dùng → góc developer) là cách chính BA thêm giá trị. Bốn cách ghi liên kết, ngay sau.</p>`],
      [30, 'Documentation options (1): use cases only / + functional requirements', `<p><strong>Use cases only</strong>: put functional requirements inside each use case (still document nonfunctional requirements and non-use-case functionality; <strong>cross-reference</strong> shared functionality like authentication instead of duplicating). <strong>Use cases + functional requirements</strong>: write simpler use cases and record derived functional requirements in the SRS/repository, with <strong>traceability</strong> between them (best managed by an RM tool) so a use case change quickly finds affected requirements.</p>`, `<p><strong>Chỉ use case</strong>: đặt functional requirement trong mỗi use case (vẫn ghi yêu cầu phi chức năng và chức năng không thuộc use case; <strong>tham chiếu chéo</strong> chức năng dùng chung như xác thực thay vì lặp). <strong>Use case + functional requirement</strong>: viết use case đơn giản hơn và ghi functional requirement suy ra vào SRS/repository, có <strong>truy vết</strong> giữa chúng (quản tốt nhất bằng công cụ RM) để đổi một use case là tìm nhanh yêu cầu bị ảnh hưởng.</p>`],
      [31, 'Documentation options (2): functional requirements only / + tests', `<p><strong>Functional requirements only</strong>: organise functional requirements by use case or feature in the SRS; use cases stay very concise (no full template), details live in the functional requirements — no separate user-requirements document. <strong>Use cases + tests</strong>: to avoid duplicating the normal flow in both a detailed use case <em>and</em> functional requirements, write fairly complete use cases and then <strong>acceptance tests</strong> covering the basic behaviour, alternative paths and failure cases.</p>`, `<p><strong>Chỉ functional requirement</strong>: tổ chức functional requirement theo use case hoặc feature trong SRS; use case rất súc tích (không template đầy đủ), chi tiết nằm ở functional requirement — không có tài liệu user-requirement riêng. <strong>Use case + test</strong>: để khỏi lặp normal flow ở cả use case chi tiết <em>và</em> functional requirement, viết use case khá đầy đủ rồi <strong>acceptance test</strong> phủ hành vi cơ bản, đường thay thế và ca lỗi.</p>`],
      [32, '4. Use case traps to avoid', `<p><strong>Too many use cases</strong> → wrong abstraction level (don't make one per scenario). <strong>Highly complex use cases</strong> → pick one success path as the normal flow, put other success branches in alternative flows and failures in exceptions; if a flow exceeds 10–15 steps, check it's really one scenario. <strong>Including design</strong> (screen looks) → focus on <em>what</em>, keep interactions conceptual. <strong>Including data definitions</strong> → store them in a project-wide data dictionary/model instead. <strong>Use cases users don't understand</strong> → write from the user's perspective and have them review.</p>`, `<p><strong>Quá nhiều use case</strong> → sai mức trừu tượng (đừng một cái mỗi scenario). <strong>Use case quá phức tạp</strong> → chọn một đường thành công làm normal flow, đặt nhánh thành công khác vào alternative flow và lỗi vào exception; nếu một flow vượt 10–15 bước, kiểm xem có thật là một scenario. <strong>Nhét thiết kế</strong> (màn hình trông ra sao) → tập trung cái <em>what</em>, giữ tương tác ở mức khái niệm. <strong>Nhét định nghĩa dữ liệu</strong> → lưu vào data dictionary/model toàn dự án. <strong>Use case người dùng không hiểu</strong> → viết từ góc người dùng và nhờ họ review.</p>`],
      [33, '5. Benefits of usage-centric requirements', `<p>Both use cases and user stories are powerful because they're <strong>user- and usage-centric</strong>: clearer user expectations; they help BAs/developers understand the business and reveal ambiguity early (as does generating tests); they avoid <strong>orphan functionality</strong> (features no one uses); and they aid <strong>prioritisation</strong> — the highest-priority functional requirements come from the top-priority user requirements (a use case/story is high-priority if it's a core process, used often, requested by a favored user class, needed for compliance, or depended on by other functions).</p>`, `<p>Cả use case và user story mạnh vì chúng <strong>user- và usage-centric</strong>: kỳ vọng người dùng rõ hơn; giúp BA/developer hiểu nghiệp vụ và lộ mơ hồ sớm (sinh test cũng vậy); tránh <strong>chức năng mồ côi</strong> (feature không ai dùng); và hỗ trợ <strong>ưu tiên</strong> — functional requirement ưu tiên cao nhất đến từ user requirement ưu tiên cao nhất (một use case/story ưu tiên cao nếu là quy trình lõi, dùng thường xuyên, do lớp người dùng favored yêu cầu, cần cho tuân thủ, hoặc chức năng khác phụ thuộc).</p>`],
    ]),
    books([['wiegers', 'Chapter 8, "Identifying use cases", "Use cases and functional requirements", "Use case traps", "Benefits"', 'Chương 8, "Identifying use cases", "Use cases and functional requirements", "Use case traps", "Benefits"']]),
  ].join('\n'),
};

/* 4.6 — the template */
const C4 = {
  title: '4.6 — Slides: the use case specification template + ATM example (Ch.8 · 34–36)|||4.6 — Slide: template đặc tả use case + ví dụ ATM (Ch.8 · 34–36)',
  slug: 'swr302-4-6-ch8-template',
  type: 'DOCUMENT',
  description: 'Slide 34–36: template đặc tả use case (number, name, summary, priority, pre/postconditions, primary/secondary actors, trigger, main scenario, extensions, open issues) — theo Cockburn — và ví dụ điền đầy đủ "Withdraw Money" trên ATM.',
  content: [
    bi(
      `<span class="eyebrow">Chapter 4 · Lesson 4.6 · Book Ch.8 slides 34–36</span>
<h2>The use case specification template — with a worked ATM example</h2>
<p class="lead">A ready-to-use template (Cockburn's) that pulls every part of the previous lessons into one form — and a fully filled "Withdraw Money" example you can copy for your own use cases.</p>`,
      `<span class="eyebrow">Chương 4 · Bài 4.6 · Slide 34–36 sách Ch.8</span>
<h2>Template đặc tả use case — kèm ví dụ ATM đã điền</h2>
<p class="lead">Một template dùng ngay (của Cockburn) gom mọi phần của các bài trước vào một biểu mẫu — và một ví dụ "Withdraw Money" điền đầy đủ để bạn chép cho use case của mình.</p>`,
    ),
    walkHead('ch8', 34, 36),
    walk('ch8', [
      [34, 'Use case specification template', `<p>The fields (adapted from Cockburn's "Basic Use Case Template"): <strong>Number</strong>, <strong>Name</strong> (noun-verb phrase), <strong>Summary</strong>, <strong>Priority</strong> (1–5), <strong>Preconditions</strong>, <strong>Postconditions</strong>, <strong>Primary/Secondary Actors</strong>, <strong>Trigger</strong>, <strong>Main Scenario</strong> (numbered system-user-system steps), <strong>Extensions</strong> (branching alternatives per step), and <strong>Open Issues</strong>. Adopt one template org-wide.</p>`, `<p>Các trường (phỏng theo "Basic Use Case Template" của Cockburn): <strong>Number</strong>, <strong>Name</strong> (cụm danh-động từ), <strong>Summary</strong>, <strong>Priority</strong> (1–5), <strong>Preconditions</strong>, <strong>Postconditions</strong>, <strong>Primary/Secondary Actor</strong>, <strong>Trigger</strong>, <strong>Main Scenario</strong> (các bước system-user-system đánh số), <strong>Extensions</strong> (nhánh thay thế theo từng bước), và <strong>Open Issues</strong>. Áp dụng một template chung toàn tổ chức.</p>`],
      [35, 'Example — "Withdraw Money" (header)', `<p>The filled ATM example: <strong>1 · Withdraw Money · Priority 5</strong>; <strong>Precondition:</strong> user has logged into the ATM; <strong>Postcondition:</strong> user has withdrawn money and received a receipt; <strong>Primary actor:</strong> Bank Customer; <strong>Secondary actor:</strong> Customer Accounts Database. This header alone already frames the whole interaction.</p>`, `<p>Ví dụ ATM đã điền: <strong>1 · Withdraw Money · Priority 5</strong>; <strong>Precondition:</strong> người dùng đã đăng nhập ATM; <strong>Postcondition:</strong> người dùng đã rút tiền và nhận biên lai; <strong>Primary actor:</strong> Bank Customer; <strong>Secondary actor:</strong> Customer Accounts Database. Riêng phần đầu này đã khung được cả tương tác.</p>`],
      [36, 'Example — "Withdraw Money" (scenario & extensions)', `<p><strong>Trigger:</strong> user chose to withdraw. <strong>Main scenario:</strong> system shows account types → user chooses → system asks amount → user enters → system debits and dispenses money → user takes it → system prints/dispenses receipt → returns card → user takes card → welcome message. <strong>Extensions</strong> at step 5: <em>5a</em> insufficient funds, <em>5b</em> show balance, <em>5c</em> exit. <strong>Open issue:</strong> should the system offer to show the balance? Note how the extensions capture the "what could go wrong" branches cleanly.</p>`, `<p><strong>Trigger:</strong> người dùng chọn rút tiền. <strong>Main scenario:</strong> hệ hiện loại tài khoản → người dùng chọn → hệ hỏi số tiền → người dùng nhập → hệ trừ và nhả tiền → người dùng lấy → hệ in/nhả biên lai → trả thẻ → người dùng lấy thẻ → lời chào. <strong>Extension</strong> ở bước 5: <em>5a</em> không đủ tiền, <em>5b</em> hiện số dư, <em>5c</em> thoát. <strong>Open issue:</strong> hệ có nên hỏi hiện số dư không? Chú ý cách extension nắm các nhánh "cái gì có thể sai" gọn gàng.</p>`],
    ]),
    books([
      ['wiegers', 'Chapter 8, "Use Case Specification Template" and example; Cockburn, "Writing Effective Use Cases"', 'Chương 8, "Use Case Specification Template" và ví dụ; Cockburn, "Writing Effective Use Cases"'],
      ['cos', 'compare the template with the COS Use Cases document', 'so template với tài liệu COS Use Cases'],
    ]),
  ].join('\n'),
};

export default [C1, C2, C3, C4];
