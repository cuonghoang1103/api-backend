/**
 * SWR302 · NEW section for book chapter Ch.9 "Playing by the rules"
 * (business rules, 21 slides). Own section, split into THREE focused lessons:
 * the 5-type taxonomy (1–14), documenting & discovering rules (15–18), and
 * rules → requirements + traceability (19–21). Grounded slide-by-slide in
 * CHAPTER_9_Playing by the rules.pdf.
 */
import { walk, walkHead, bi, books } from './_slides.mjs';

/* 9R.1 — the taxonomy */
const R1 = {
  title: '9R.1 — Slides: the business-rules taxonomy — 5 types (Ch.9 · 1–14)|||9R.1 — Slide: phân loại business rule — 5 loại (Ch.9 · 1–14)',
  slug: 'swr302-9r-1-ch9-taxonomy',
  type: 'DOCUMENT',
  description: 'Slide 1–14: business rule là gì, viết ở mức "atomic", và năm loại theo Figure 9-1 — facts, constraints, action enablers, inferences, computations — kèm ví dụ và cách biểu diễn (roles & permissions matrix, decision table, bảng công thức).',
  content: [
    bi(
      `<span class="eyebrow">Business Rules · Lesson 9R.1 · Book Ch.9 slides 1–14</span>
<h2>The business-rules taxonomy — five types</h2>
<p class="lead">Business rules are policies, regulations, standards and algorithms that exist beyond any one application but often <em>dictate</em> what the software must do. Wiegers's taxonomy sorts them into five types — <strong>facts, constraints, action enablers, inferences, computations</strong> — which is exactly how the exam and real projects classify them.</p>`,
      `<span class="eyebrow">Business Rules · Bài 9R.1 · Slide 1–14 sách Ch.9</span>
<h2>Phân loại business rule — năm loại</h2>
<p class="lead">Business rule là chính sách, quy định, chuẩn và thuật toán tồn tại ngoài bất kỳ ứng dụng nào nhưng thường <em>bắt buộc</em> phần mềm phải làm gì. Phân loại của Wiegers chia chúng thành năm loại — <strong>facts, constraints, action enablers, inferences, computations</strong> — đúng cách đề thi và dự án thật phân loại.</p>`,
    ),
    walkHead('ch9', 1, 14),
    walk('ch9', [
      [1, 'Playing by the rules', `<p>Title slide — the chapter on business rules.</p>`, `<p>Slide bìa — chương về business rule.</p>`],
      [2, 'Objectives', `<p>Understand how business rules influence many requirement types; identify at least one of each rule type (Figure 9-1) for your project; set up a <strong>traceability matrix</strong> showing which functional requirements enforce each rule; and use the rationale behind functional requirements to discover implicit rules.</p>`, `<p>Hiểu business rule ảnh hưởng nhiều loại yêu cầu; xác định ít nhất một cái mỗi loại (Hình 9-1) cho dự án; lập <strong>ma trận truy vết</strong> cho thấy functional requirement nào thực thi mỗi rule; và dùng lý do sau functional requirement để khám phá rule ngầm.</p>`],
      [3, 'Contents', `<p>Road map: a business-rules taxonomy, documenting them, discovering them, rules &amp; requirements, and tying everything together. Lessons 9R.1–9R.3 follow.</p>`, `<p>Bản đồ: phân loại business rule, ghi tài liệu, khám phá, rule &amp; requirement, và gắn tất cả lại. Bài 9R.1–9R.3 theo đây.</p>`],
      [4, 'Business rules overview', `<p>Section divider — the definition and the five-type taxonomy.</p>`, `<p>Slide phân mục — định nghĩa và phân loại năm loại.</p>`],
      [5, 'A business rules taxonomy — definition', `<p>Two views. <em>Business:</em> a rule is <strong>guidance about an obligation</strong> concerning conduct, action, practice or procedure. <em>Information-system:</em> a rule is a statement that <strong>defines or constrains some aspect of the business</strong>, asserting structure or controlling behaviour. The chapter also stresses writing rules <strong>atomically</strong> and gives a simple taxonomy.</p>`, `<p>Hai góc nhìn. <em>Nghiệp vụ:</em> rule là <strong>chỉ dẫn về một nghĩa vụ</strong> liên quan hành vi, hành động, thực hành hay thủ tục. <em>Hệ thông tin:</em> rule là một phát biểu <strong>định nghĩa hoặc ràng buộc một khía cạnh của nghiệp vụ</strong>, khẳng định cấu trúc hoặc kiểm soát hành vi. Chương cũng nhấn mạnh viết rule <strong>atomic</strong> và cho một phân loại đơn giản.</p>`],
      [6, 'Atomic business rules', `<p>A librarian's answer — "check out a DVD one week, renew twice for three days each, unless another patron holds it" — combines several rules. <strong>Composite</strong> rules are hard to understand and maintain; write rules at the <strong>atomic</strong> level (one rule = one indivisible statement). Guidelines: no "or" on the left of an if/then, no "and" on the right. Atomic rules are short, reusable, and your functional requirements depend on combinations of them.</p>`, `<p>Câu trả lời của thủ thư — "mượn DVD một tuần, gia hạn hai lần mỗi lần ba ngày, trừ khi có người khác giữ chỗ" — gộp nhiều rule. Rule <strong>composite</strong> khó hiểu và bảo trì; viết rule ở mức <strong>atomic</strong> (một rule = một phát biểu không chia được). Hướng dẫn: không "or" bên trái if/then, không "and" bên phải. Rule atomic ngắn, tái dùng được, và functional requirement phụ thuộc tổ hợp của chúng.</p>`],
      [7, '1. Facts', `<p>A <strong>fact</strong> is a statement that's true about the business at a point in time — an association/relationship between important business terms ("every chemical container has a unique barcode", "every order has a shipping charge"). Facts about data entities appear in <strong>data models</strong>. Collect only <em>in-scope</em> facts (don't amass all business knowledge), and connect each to context-diagram inputs/outputs, events, data objects, or user requirements.</p>`, `<p>Một <strong>fact</strong> là phát biểu đúng về nghiệp vụ tại một thời điểm — một liên kết/quan hệ giữa các thuật ngữ nghiệp vụ quan trọng ("mỗi container hoá chất có barcode duy nhất", "mỗi đơn có phí giao"). Fact về entity dữ liệu xuất hiện trong <strong>data model</strong>. Chỉ thu fact <em>trong phạm vi</em> (đừng gom hết kiến thức nghiệp vụ), và nối mỗi cái với input/output context-diagram, sự kiện, data object, hoặc user requirement.</p>`],
      [8, '2. Constraints', `<p>A <strong>constraint</strong> restricts what the system or users may do — "must / must not / may not", or "only certain roles may…". Origins: <strong>organizational policies</strong> ("a loan applicant under 18 must have a cosigner"), <strong>government regulations</strong> ("pilots must get 8 continuous hours rest per 24h"), and <strong>industry standards</strong> ("mortgage applicants must meet FHA standards"). These drive access rules, validations and compliance functionality.</p>`, `<p>Một <strong>constraint</strong> hạn chế điều hệ thống hoặc người dùng được làm — "phải / không được / không được phép", hoặc "chỉ vai nào đó mới…". Nguồn gốc: <strong>chính sách tổ chức</strong> ("người vay dưới 18 phải có người đồng ký"), <strong>quy định nhà nước</strong> ("phi công phải nghỉ 8 giờ liên tục mỗi 24h"), và <strong>chuẩn ngành</strong> ("người vay thế chấp phải đạt chuẩn FHA"). Chúng dẫn tới quy tắc truy cập, validation và chức năng tuân thủ.</p>`],
      [9, 'Constraints — roles & permissions matrix', `<p>Because many constraints govern <strong>which user types can perform which functions</strong>, a concise way to document them is a <strong>roles and permissions matrix</strong> (Figure 9-2): rows = roles, columns = functions, cells = allowed/denied. It's a compact, reviewable form of a whole family of access-control rules.</p>`, `<p>Vì nhiều constraint chi phối <strong>loại người dùng nào được thực hiện chức năng nào</strong>, cách ghi gọn là một <strong>ma trận vai & quyền (roles and permissions matrix)</strong> (Hình 9-2): hàng = vai, cột = chức năng, ô = cho phép/từ chối. Đó là dạng gọn, review được của cả một họ rule kiểm soát truy cập.</p>`],
      [10, '3. Action enablers', `<p>An <strong>action enabler</strong> triggers activity when conditions are true — "If <em>&lt;condition/event&gt;</em>, then <em>&lt;something happens&gt;</em>". Examples (CTS): "if the stockroom has the requested chemical, offer existing containers"; "on the last day of a quarter, generate OSHA/EPA reports"; "if a container's expiration date is reached, notify its holder". Complex condition logic is documented concisely with a <strong>decision table</strong>.</p>`, `<p>Một <strong>action enabler</strong> kích hoạt hoạt động khi điều kiện đúng — "Nếu <em>&lt;điều kiện/sự kiện&gt;</em> thì <em>&lt;việc gì đó xảy ra&gt;</em>". Ví dụ (CTS): "nếu kho có hoá chất được yêu cầu, mời các container sẵn có"; "ngày cuối quý, sinh báo cáo OSHA/EPA"; "nếu tới hạn của một container, báo người giữ". Logic điều kiện phức tạp ghi gọn bằng <strong>decision table</strong>.</p>`],
      [11, 'Action enablers — decision table (Figure 12-6)', `<p>A <strong>decision table</strong> lays out every combination of conditions (rows) against the resulting actions (columns), so no combination is missed. It's the go-to representation when an action enabler depends on several true/false conditions — and it doubles as a test-design tool (Chapter 12).</p>`, `<p>Một <strong>decision table</strong> bày mọi tổ hợp điều kiện (hàng) đối chiếu hành động kết quả (cột), để không sót tổ hợp nào. Đó là cách biểu diễn ưu tiên khi một action enabler phụ thuộc nhiều điều kiện đúng/sai — và cũng là công cụ thiết kế test (Chương 12).</p>`],
      [12, '4. Inferences', `<p>An <strong>inference</strong> (inferred knowledge / derived fact) creates a <em>new fact</em> from other facts — written in if/then form like an action enabler, but the "then" yields <strong>knowledge, not an action</strong>. Examples: "if a payment is 30 days overdue, then the account is delinquent"; "if a vendor can't ship within 5 days, the item is back-ordered"; "chemicals with LD50 &lt; 5 mg/kg are hazardous".</p>`, `<p>Một <strong>inference</strong> (kiến thức suy ra / fact dẫn xuất) tạo <em>một fact mới</em> từ các fact khác — viết dạng if/then như action enabler, nhưng "then" cho ra <strong>kiến thức, không phải hành động</strong>. Ví dụ: "nếu thanh toán quá hạn 30 ngày thì tài khoản quá hạn"; "nếu nhà cung cấp không giao trong 5 ngày, món hàng bị back-order"; "hoá chất có LD50 &lt; 5 mg/kg là nguy hiểm".</p>`],
      [13, '5. Computations', `<p>A <strong>computation</strong> transforms existing data into new data via a formula/algorithm — often externally mandated (e.g. income-tax withholding). Examples: "domestic ground shipping for &gt;2 lb = $4.75 + 12¢/oz"; "order total = item prices − volume discounts + taxes + shipping + optional insurance"; "unit price −10% for 6–10 units, −20% for 11–20, −30% for &gt;20".</p>`, `<p>Một <strong>computation</strong> biến dữ liệu sẵn có thành dữ liệu mới qua công thức/thuật toán — thường do bên ngoài áp (vd khấu trừ thuế thu nhập). Ví dụ: "phí giao đường bộ nội địa cho &gt;2 lb = $4.75 + 12¢/oz"; "tổng đơn = giá món − chiết khấu số lượng + thuế + phí giao + bảo hiểm tuỳ chọn"; "đơn giá −10% cho 6–10 đơn vị, −20% cho 11–20, −30% cho &gt;20".</p>`],
      [14, 'Computations — represent clearly & the boundary trap', `<p>Natural-language computations are wordy and confusing — represent them as a <strong>math expression or a table of rules</strong> (Table 9-2) that's clearer and easier to maintain. <strong>Trap:</strong> watch for <strong>boundary-value overlaps</strong> — writing ranges as 1–5, 5–10, 10–20 leaves it ambiguous which range exactly 5 and 10 fall into. Define ranges with no overlaps.</p>`, `<p>Computation bằng ngôn ngữ tự nhiên thì dài dòng, khó hiểu — biểu diễn dạng <strong>biểu thức toán hoặc bảng rule</strong> (Bảng 9-2) rõ và dễ bảo trì hơn. <strong>Bẫy:</strong> coi chừng <strong>chồng lấn giá trị biên</strong> — viết dải 1–5, 5–10, 10–20 để lại mơ hồ giá trị đúng 5 và 10 thuộc dải nào. Định dải không chồng lấn.</p>`],
    ]),
    books([['wiegers', 'Chapter 9, "A business rules taxonomy" (Figure 9-1: facts, constraints, action enablers, inferences, computations)', 'Chương 9, "A business rules taxonomy" (Hình 9-1: facts, constraints, action enablers, inferences, computations)']]),
  ].join('\n'),
};

/* 9R.2 — documenting & discovering */
const R2 = {
  title: '9R.2 — Slides: documenting & discovering business rules (Ch.9 · 15–18)|||9R.2 — Slide: ghi tài liệu & khám phá business rule (Ch.9 · 15–18)',
  slug: 'swr302-9r-2-ch9-document-discover',
  type: 'DOCUMENT',
  description: 'Slide 15–18: ghi business rule như tài sản cấp doanh nghiệp (catalog, RM tool, rule engine), các thuộc tính (ID, type, static/dynamic, source); và cách khám phá business rule (common knowledge, hệ legacy, mô hình quy trình, tài liệu, phân tích dữ liệu, phòng compliance).',
  content: [
    bi(
      `<span class="eyebrow">Business Rules · Lesson 9R.2 · Book Ch.9 slides 15–18</span>
<h2>Documenting &amp; discovering business rules</h2>
<p class="lead">Because a rule can affect many applications, treat rules as <strong>enterprise-level assets</strong>. This lesson covers how to catalog them (and the attributes to record) and the six sources where rules hide.</p>`,
      `<span class="eyebrow">Business Rules · Bài 9R.2 · Slide 15–18 sách Ch.9</span>
<h2>Ghi tài liệu &amp; khám phá business rule</h2>
<p class="lead">Vì một rule có thể ảnh hưởng nhiều ứng dụng, coi rule là <strong>tài sản cấp doanh nghiệp</strong>. Bài này bàn cách lập catalog (và các thuộc tính cần ghi) và sáu nguồn nơi rule ẩn nấp.</p>`,
    ),
    walkHead('ch9', 15, 18),
    walk('ch9', [
      [15, 'Documenting business rules — where to store them', `<p>Manage rules as <strong>enterprise assets</strong>: a simple <strong>catalog</strong> suffices at first; a requirements tool can store rules as a requirement type accessible to all projects; rule-heavy or large organisations should build a <strong>rules database</strong>, possibly with a <strong>rules engine</strong> that automates enforcement. Rules touching <strong>safety, security, finance or compliance</strong> carry the greatest risk if unmanaged. Use structured templates as you mature (start with the simple format of Table 9-4).</p>`, `<p>Quản rule như <strong>tài sản doanh nghiệp</strong>: một <strong>catalog</strong> đơn giản đủ lúc đầu; công cụ yêu cầu có thể lưu rule như một loại requirement truy cập được cho mọi dự án; tổ chức nhiều rule hoặc lớn nên dựng một <strong>rules database</strong>, có thể kèm <strong>rules engine</strong> tự động thực thi. Rule chạm <strong>an toàn, bảo mật, tài chính hoặc tuân thủ</strong> rủi ro nhất nếu không quản. Dùng template có cấu trúc khi trưởng thành (bắt đầu với định dạng đơn giản Bảng 9-4).</p>`],
      [16, 'Documenting — the rule attributes', `<p>Give each rule a <strong>unique ID</strong> (so requirements can link back — use-case templates often have a "business rules" field). Record: <strong>Type</strong> (fact/constraint/action-enabler/inference/computation); <strong>Static or dynamic</strong> (how likely to change — dynamic rules tell developers to make that functionality/data easy to update); and <strong>Source</strong> (corporate/management policy, SME, or documents like laws and regulations).</p>`, `<p>Cho mỗi rule một <strong>ID duy nhất</strong> (để requirement liên kết ngược — template use case thường có trường "business rules"). Ghi: <strong>Type</strong> (fact/constraint/action-enabler/inference/computation); <strong>Static hay dynamic</strong> (khả năng đổi — rule dynamic báo developer làm chức năng/dữ liệu đó dễ cập nhật); và <strong>Source</strong> (chính sách công ty/quản lý, SME, hoặc tài liệu như luật và quy định).</p>`],
      [17, 'Discovering business rules — the sources', `<p>Rules hide in: <strong>"common knowledge"</strong> (long-time staff know the details); <strong>legacy systems</strong> (rules embedded in code/requirements — reverse-engineer the rationale, though this can be incomplete); <strong>business-process modeling</strong> (look for constraints, triggers, computations, facts at each step); <strong>document analysis</strong> (old specs, regulations, standards, policies, contracts, business plans); <strong>data analysis</strong> (object states and what changes them); and <strong>compliance departments</strong> for regulated systems.</p>`, `<p>Rule ẩn trong: <strong>"kiến thức chung"</strong> (nhân viên lâu năm biết chi tiết); <strong>hệ legacy</strong> (rule nhúng trong code/yêu cầu — reverse-engineer lý do, dù có thể không đủ); <strong>mô hình quy trình nghiệp vụ</strong> (tìm constraint, trigger, computation, fact ở mỗi bước); <strong>phân tích tài liệu</strong> (spec cũ, quy định, chuẩn, chính sách, hợp đồng, kế hoạch kinh doanh); <strong>phân tích dữ liệu</strong> (trạng thái object và cái gì đổi chúng); và <strong>phòng compliance</strong> cho hệ chịu quy định.</p>`],
      [18, 'Discovering — origins & questions (Figure 9-3)', `<p>Figure 9-3 maps several <strong>origins of rules</strong> and suggests <strong>questions the BA can ask</strong> while discussing requirements — e.g. "Are there conditions under which this isn't allowed?", "Who is permitted to do this?", "How is this value calculated?". Asking rule-hunting questions during ordinary elicitation surfaces rules that no one would state unprompted.</p>`, `<p>Hình 9-3 vẽ vài <strong>nguồn gốc của rule</strong> và gợi ý <strong>câu hỏi BA có thể hỏi</strong> khi bàn yêu cầu — vd "Có điều kiện nào việc này không được phép?", "Ai được làm việc này?", "Giá trị này tính thế nào?". Hỏi các câu săn-rule trong lúc elicitation thường lộ ra rule mà không ai tự nêu.</p>`],
    ]),
    books([['wiegers', 'Chapter 9, "Documenting business rules" and "Discovering business rules"', 'Chương 9, "Documenting business rules" và "Discovering business rules"']]),
  ].join('\n'),
};

/* 9R.3 — rules → requirements & traceability */
const R3 = {
  title: '9R.3 — Slides: business rules → requirements & traceability (Ch.9 · 19–21)|||9R.3 — Slide: business rule → requirement & truy vết (Ch.9 · 19–21)',
  slug: 'swr302-9r-3-ch9-rules-to-requirements',
  type: 'DOCUMENT',
  description: 'Slide 19–21: business rule khác functional requirement thế nào; ví dụ suy functional requirement từ rule (CTS "Notify chemical owner of expiration"); và cách nối rule với requirement bằng truy vết (attribute Origin, traceability matrix, hyperlink) mà không lặp rule.',
  content: [
    bi(
      `<span class="eyebrow">Business Rules · Lesson 9R.3 · Book Ch.9 slides 19–21</span>
<h2>Business rules → requirements &amp; traceability</h2>
<p class="lead">Rules and requirements look alike but aren't the same. This lesson shows how a BA turns rules into functional requirements and — crucially — how to <strong>link them by traceability instead of duplicating them</strong>.</p>`,
      `<span class="eyebrow">Business Rules · Bài 9R.3 · Slide 19–21 sách Ch.9</span>
<h2>Business rule → requirement &amp; truy vết</h2>
<p class="lead">Rule và requirement trông giống nhưng không giống. Bài này cho thấy BA biến rule thành functional requirement ra sao và — quan trọng — cách <strong>nối chúng bằng truy vết thay vì lặp lại</strong>.</p>`,
    ),
    walkHead('ch9', 19, 21),
    walk('ch9', [
      [19, 'Business rules & requirements — the distinction', `<p>After identifying rules, decide which must be <strong>enforced in software</strong>. Rules and functional requirements can look alike, but a rule is an <strong>external statement of policy</strong> that <em>drives</em> system functionality, whereas a functional requirement is the system behaviour that enforces it. Every BA must decide which rules pertain to the app, which must be enforced in software, and <strong>how</strong> to enforce them.</p>`, `<p>Sau khi xác định rule, quyết cái nào phải được <strong>thực thi trong phần mềm</strong>. Rule và functional requirement có thể trông giống, nhưng rule là <strong>phát biểu chính sách bên ngoài</strong> <em>dẫn dắt</em> chức năng hệ thống, còn functional requirement là hành vi hệ thống thực thi nó. Mỗi BA phải quyết rule nào liên quan app, cái nào phải thực thi trong phần mềm, và <strong>thực thi thế nào</strong>.</p>`],
      [20, 'Deriving functional requirements from rules — example', `<p>Two CTS rules: #1 (action enabler) "if a container's expiration date is reached, notify its holder"; #2 (fact) "a container that can form explosive decomposition products expires one year after manufacture". #1 is the <strong>origin of a feature</strong> "Notify chemical owner of expiration"; #2 helps determine which containers have expiry dates. From them the BA derives functional requirements like <em>"Expired.Notify — if a container with an expiration date isn't Disposed, the system shall notify the listed individuals at the indicated times."</em></p>`, `<p>Hai rule CTS: #1 (action enabler) "nếu tới hạn của một container, báo người giữ"; #2 (fact) "container có thể tạo sản phẩm phân huỷ nổ hết hạn một năm sau sản xuất". #1 là <strong>nguồn gốc của một feature</strong> "Notify chemical owner of expiration"; #2 giúp xác định container nào có hạn. Từ chúng BA suy ra functional requirement kiểu <em>"Expired.Notify — nếu một container có hạn chưa Disposed, hệ thống <b>shall</b> báo những người trong bảng vào các thời điểm chỉ định."</em></p>`],
      [21, 'Tying everything together — traceability', `<p>To avoid redundancy, <strong>don't duplicate catalog rules in the requirements</strong> — refer back to the rule as the <strong>source</strong> of a function or algorithm. Link a functional requirement to its parent rule(s) by: an <strong>"Origin" attribute</strong> in an RM tool; <strong>traceability links</strong> in a traceability/mapping matrix; or <strong>hyperlinks</strong> from rule-ID references (fragile if the rules move). This is how a change to one rule instantly reveals every requirement that depends on it.</p>`, `<p>Để tránh trùng lặp, <strong>đừng lặp rule của catalog trong yêu cầu</strong> — trỏ ngược về rule như <strong>nguồn</strong> của một chức năng hoặc thuật toán. Nối một functional requirement với rule cha bằng: một <strong>thuộc tính "Origin"</strong> trong công cụ RM; <strong>liên kết truy vết</strong> trong traceability/mapping matrix; hoặc <strong>hyperlink</strong> từ tham chiếu rule-ID (dễ gãy nếu rule bị di chuyển). Nhờ vậy đổi một rule là lập tức lộ mọi yêu cầu phụ thuộc nó.</p>`],
    ]),
    books([
      ['wiegers', 'Chapter 9, "Business rules and requirements" and "Tying everything together"', 'Chương 9, "Business rules and requirements" và "Tying everything together"'],
      ['cos', 'the COS Business Rules document classifies each rule by type — map them to functional requirements', 'tài liệu COS Business Rules phân loại từng rule theo type — ánh xạ chúng tới functional requirement'],
    ]),
  ].join('\n'),
};

export default {
  title: 'Playing by the rules — business rules (book Ch.9)|||Chơi theo luật — business rules (Ch.9 sách)',
  description: 'Chương 9 sách Wiegers: phân loại 5 loại business rule (facts, constraints, action enablers, inferences, computations), atomic rules, ghi & khám phá rule, và truy vết rule → functional requirement. Tách 3 bài. Gắn với tài liệu mẫu COS Business Rules.',
  lessons: [R1, R2, R3],
};
