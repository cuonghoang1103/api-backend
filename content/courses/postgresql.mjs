/**
 * PostgreSQL — khoá học CuongThai (Courses, academyType=GENERAL, KHÔNG thuộc kỳ Academy).
 * Giáo trình tự soạn: 16 chương, zero → vận hành production, song ngữ EN/VI.
 * Mọi output SQL/psql trong khoá đều CHẠY THẬT trên PostgreSQL 15.4 rồi chép đúng.
 * Sections tách theo file trong ./postgresql/ cho dễ soạn; seeder chỉ đọc file này.
 *
 * Seed: node scripts/course-seed.mjs --file ./content/courses/postgresql.mjs --apply
 */
import s00 from './postgresql/s00-intro.mjs';
import s01 from './postgresql/s01-vi-sao.mjs';
import s02 from './postgresql/s02-kieu-du-lieu.mjs';
import s03 from './postgresql/s03-bang-rang-buoc.mjs';
import s04 from './postgresql/s04-chen-truy-van.mjs';
import s05 from './postgresql/s05-join.mjs';

export default {
  category: { slug: 'databases', name: 'Cơ sở dữ liệu', icon: 'Database', sortOrder: 3 },
  course: {
    slug: 'postgresql',
    title: 'PostgreSQL',
    level: 'BEGINNER',
    language: 'Vietnamese',
    status: 'PUBLISHED',
    isFeatured: true,
    // Generated by scripts/course-cover.mjs (official Simple Icons logo on the
    // shared CuongThai cover template). Regenerate, don't hand-edit.
    thumbnailUrl: 'https://media.cuongthai.com/images/course-covers/postgresql.png',
    shortDescription: "Master PostgreSQL from zero to production: data types, schema design, joins, aggregation, CTEs, window functions, indexes, EXPLAIN ANALYZE, transactions & MVCC, JSONB and full-text search — every query proven with real output.|||Làm chủ PostgreSQL từ số 0 tới production: kiểu dữ liệu, lược đồ, JOIN, tổng hợp, CTE, hàm cửa sổ, chỉ mục, EXPLAIN ANALYZE, giao dịch & MVCC, JSONB, full-text search — mọi truy vấn đều có output thật.",
    description: 'Khoá PostgreSQL từ số 0 tới vận hành production, do CuongThai tự biên soạn. 16 chương đi từ vì sao PostgreSQL tồn tại và mô hình client-server, hệ thống kiểu dữ liệu, thiết kế lược đồ và ràng buộc, truy vấn (SELECT/JOIN/tổng hợp/CTE/hàm cửa sổ), tới hiệu năng (chỉ mục, EXPLAIN ANALYZE, bộ lập kế hoạch), giao dịch và đồng thời (MVCC, isolation, lock, deadlock), hàm/trigger/PL-pgSQL, JSONB & full-text search, kết nối/pool/VACUUM, sao lưu/nhân bản/phân mảnh, cho tới PostgreSQL trong production với Prisma. Mọi truy vấn đều chạy thật trên một Postgres thật — không có dòng nào "chắc là đúng".',
    whatYouLearn: 'Hiểu mô hình client-server và vì sao PostgreSQL là lựa chọn mặc định cho ứng dụng nghiêm túc; chọn đúng kiểu dữ liệu (numeric, text, timestamptz, uuid, enum, array, jsonb); thiết kế lược đồ chuẩn với khoá chính/ngoại, unique, check, generated columns; viết mọi truy vấn từ SELECT cơ bản tới JOIN nhiều bảng, tổng hợp với GROUP BY/HAVING/FILTER, CTE đệ quy và hàm cửa sổ; đọc EXPLAIN ANALYZE và biết vì sao truy vấn chậm; chọn đúng loại chỉ mục (B-tree, partial, GIN, BRIN) và đo tác động thật; hiểu MVCC, các mức cô lập, khoá và deadlock; viết function/trigger bằng PL/pgSQL; dùng JSONB và full-text search; và vận hành Postgres thật (pool, VACUUM, sao lưu, nhân bản).',
    requirements: 'Không cần kinh nghiệm cơ sở dữ liệu trước — chương 1 bắt đầu từ vì sao PostgreSQL tồn tại. Biết dùng dòng lệnh (terminal) cơ bản là đủ. Máy cài được PostgreSQL 15+ hoặc chạy được Docker. Nếu bạn đã học khoá Node.js (chương 7 Prisma/PostgreSQL) thì đây là phần đào sâu về chính cơ sở dữ liệu đó.',
    documentsNote: 'Tài liệu tham chiếu chính: postgresql.org/docs (tài liệu chính thức, đầy đủ và chính xác nhất) • use-the-index-luke.com (giải thích chỉ mục và EXPLAIN cực rõ) • pgexercises.com (bài tập SQL tương tác). Phần thực hành đi kèm: track "PostgreSQL" trên Code Lab. Loạt bài "100 Ngày Database" trên trang chủ /feed bổ trợ phần lý thuyết quan hệ và chuẩn hoá.',
  },
  sections: [
    s00,
    s01,
    s02,
    s03,
    s04,
    s05,
  ],
};
