export default {
  "course": {
    "courseCode": "CSD201"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "CODE",
      "code": "PE15-HCMBST01",
      "title": "PE Đề 15 — Practical Exam (HCM - BST - 01)|||PE Đề 15 — Thi thực hành (HCM - BST - 01)",
      "description": "CSD201 PE (CODE): BST (student registry, GPA queries) + linked list (scholarship processing), AI-graded.|||PE CSD201 (viết mã): BST (danh bạ sinh viên, truy vấn GPA) + danh sách liên kết (xử lý học bổng), chấm AI.",
      "durationMinutes": 60,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "attachmentUrl": "https://media.cuongthai.com/files/exam-attachments/CSD201/PE15-Given.zip",
      "attachmentName": "CSD201-PE15-Given.zip",
      "instructions": "<div class=\"ml-en\"><p><strong>CSD201 PE INSTRUCTIONS</strong> (transcribed from the real exam paper, \"HCM - BST - 01\").</p>\n   <ol>\n     <li>Software: NetBeans IDE 8.x, Java JDK 1.8. Download the given materials above.</li>\n     <li>Do not add any new import statements to the provided files.</li>\n     <li>Do not use Vietnamese with diacritics when writing comments in the program.</li>\n     <li>This exam room cannot run NetBeans/JDK in the browser, so type your code directly into the code box — it is graded by AI against the rubric. You can build &amp; run the real given project on your own machine and cross-check against the sample runs shown below.</li>\n   </ol></div><div class=\"ml-vi\"><p><strong>HƯỚNG DẪN PE CSD201</strong> (dịch nguyên văn từ đề thi thật, \"HCM - BST - 01\").</p>\n   <ol>\n     <li>Phần mềm: NetBeans IDE 8.x, Java JDK 1.8. Tải given materials ở trên.</li>\n     <li>Không thêm import mới vào các file đã cho.</li>\n     <li>Không dùng tiếng Việt có dấu khi viết chú thích trong chương trình.</li>\n     <li>Phòng thi web không chạy được NetBeans/JDK trực tiếp nên gõ thẳng mã vào ô, được AI chấm theo tiêu chí — bạn có thể build &amp; chạy project given thật trên máy mình rồi đối chiếu với mẫu chạy bên dưới.</li>\n   </ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "CODE",
          "points": 2.5,
          "language": "java",
          "prompt": "<p><strong>Scenario: Student and Scholarship Management System.</strong></p>\n   <p><strong>Class Structure:</strong> <code>Student</code> (id, name, gpa, major, balance) · <code>TreeNode</code> — a Student plus left/right pointers for the BST · <code>ListNode</code> — a Student plus a next pointer · <code>StudentBST</code> — a binary search tree that manages all students in the system · <code>ScholarshipList</code> — a linked list that manages scholarship recipients · <code>StudentManager</code> — the main class, holding a StudentBST (all students) and a ScholarshipList (scholarship recipients). When a student belongs to the scholarship list, the scholarship amount is stored in the <code>balance</code> field of that Student object.</p>|||<p><strong>Bối cảnh: Hệ thống quản lý Sinh viên và Học bổng.</strong></p>\n   <p><strong>Cấu trúc lớp:</strong> <code>Student</code> (id, name, gpa, major, balance) · <code>TreeNode</code> — Student và con trỏ left/right cho BST · <code>ListNode</code> — Student và con trỏ next · <code>StudentBST</code> — cây BST quản lý toàn bộ sinh viên trong hệ thống · <code>ScholarshipList</code> — danh sách liên kết quản lý sinh viên nhận học bổng · <code>StudentManager</code> — lớp chính, chứa 1 StudentBST (toàn bộ sinh viên) và 1 ScholarshipList (sinh viên nhận học bổng). Khi 1 sinh viên thuộc danh sách học bổng, số tiền học bổng lưu ở field <code>balance</code> của Student đó.</p><p><strong>f1(): (2.5 points)</strong></p>\n     <p>To finish the f1 requirement, students need to perform two specific tasks:</p>\n     <ul><li>Implement the <code>insert()</code> function in the StudentBST structure - insert a new student into the BST (the tree is keyed by the 4th character of the student id, i.e. the digit).</li>\n     <li>Implement the <code>addLast()</code> function in the ScholarshipList structure - add a student to the end of the scholarship list.</li></ul>\n     <p>Note: the scholarship list is initialized with sample students from the data.txt file.</p>|||<p><strong>f1(): (2.5 điểm)</strong></p>\n     <p>Để hoàn thành f1, cần 2 việc:</p>\n     <ul><li>Cài <code>insert()</code> trong StudentBST — chèn sinh viên mới vào BST (cây khoá theo ký tự thứ 4 của id, tức chữ số).</li>\n     <li>Cài <code>addLast()</code> trong ScholarshipList — thêm sinh viên vào cuối danh sách học bổng.</li></ul>\n     <p>Lưu ý: danh sách học bổng khởi tạo sẵn từ dữ liệu mẫu trong data.txt.</p>",
          "starterCode": "\r\nclass StudentBST {\r\n\r\n    TreeNode root;\r\n\r\n    StudentBST() {\r\n        root = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return root == null;\r\n    }\r\n\r\n    void clear() {\r\n        root = null;\r\n    }\r\n\r\n    TreeNode search(TreeNode p, String xId) {\r\n        if (p == null) {\r\n            return null;\r\n        }\r\n        if (p.info.getId().equals(xId)) {\r\n            return p;\r\n        }\r\n        if (xId.compareTo(p.info.getId()) < 0) {\r\n            return search(p.left, xId);\r\n        } else {\r\n            return search(p.right, xId);\r\n        }\r\n    }\r\n\r\n    void insertHelper(TreeNode root, Student students) {\r\n        if (students.getId().charAt(3) < root.info.getId().charAt(3)) {\r\n            if (root.left == null) {\r\n                root.left = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.left, students);\r\n            }\r\n        } else if (students.getId().charAt(3) > root.info.getId().charAt(3)) {\r\n            if (root.right == null) {\r\n                root.right = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.right, students);\r\n            }\r\n        }\r\n    }\r\n\r\n    void insert(String id, String name, double gpa, String major, double balance) {\r\n        // Implement this function - Insert a new student to the BST\r\n        // If the ID already exists, update the student's information\r\n        // --------------------------------------------------------\r\n        Student x = new Student(id, name, gpa, major, balance);\r\n        if (root == null) {\r\n            root = new TreeNode(x);\r\n        }\r\n        insertHelper(root, x);\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    Student maxGPA(TreeNode root) {\r\n        if (root == null) {\r\n            return null;\r\n        }\r\n\r\n        Student max = root.info;\r\n\r\n        Student leftMax = maxGPA(root.left);\r\n        Student rightMax = maxGPA(root.right);\r\n\r\n        if (leftMax != null && leftMax.getGpa() > max.getGpa()) {\r\n            max = leftMax;\r\n        }\r\n\r\n        if (rightMax != null && rightMax.getGpa() > max.getGpa()) {\r\n            max = rightMax;\r\n        }\r\n\r\n        return max;\r\n    }\r\n\r\n    // Find student with highest GPA\r\n    Student findHighestGPA() {\r\n        // Implement this function - Find student with highest GPA\r\n        // --------------------------------------------------------\r\n\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return maxGPA(root); // Change this return statement as needed\r\n    }\r\n\r\n    int count(TreeNode root, double threshold) {\r\n        if (root == null) {\r\n            return 0;\r\n        }\r\n        if (root.info.getGpa() >= threshold) {\r\n            return count(root.left, 3.5) + count(root.right, 3.5) + 1;\r\n        } else {\r\n            return count(root.left, 3.5) + count(root.right, 3.5);\r\n        }\r\n    }\r\n\r\n    // Count students with GPA greater than or equal to threshold\r\n    int countByGPA(double threshold) {\r\n        // Implement this function - Count students with GPA >= threshold\r\n        // --------------------------------------------------------\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return count(root, 3.5); // Change this return statement as needed\r\n    }\r\n\r\n    // Count students by major\r\n    int countByMajor(String major) {\r\n        return countByMajor(root, major);\r\n    }\r\n\r\n    private int countByMajor(TreeNode p, String major) {\r\n        if (p == null) {\r\n            return 0;\r\n        }\r\n        int count = 0;\r\n        if (p.info.getMajor().equals(major)) {\r\n            count = 1;\r\n        }\r\n\r\n        return count + countByMajor(p.left, major) + countByMajor(p.right, major);\r\n    }\r\n\r\n    // Update student balance in BST\r\n    boolean updateStudentBalance(String id, double amount) {\r\n        // Implement this function - Update balance of student with given ID\r\n        // --------------------------------------------------------\r\n        if (search(root, id) != null) {\r\n            search(root, id).info.setBalance(search(root, id).info.getBalance() + amount);\r\n            return true;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return false; // Change this return statement as needed\r\n    }\r\n\r\n    void loadDataStudents(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 1);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 2);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 3);\r\n            double[] balances = Lib.readLineToDoubleArray(\"data.txt\", k + 4);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length, Math.min(gpas.length,\r\n                    Math.min(majors.length, balances.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                insert(ids[i], names[i], gpas[i], majors[i], balances[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading student data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\n// ScholarshipList.java\r\nclass ScholarshipList {\r\n\r\n    ListNode head, tail;\r\n\r\n    ScholarshipList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return head == null;\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void addLast(String id, String name, double gpa, String major, double scholarshipAmount) {\r\n        // Implement this function - add a new student to the end of the linked list\r\n        // --------------------------------------------------------\r\n        ListNode current = head;\r\n        Student x = new Student(id, name, gpa, major, scholarshipAmount);\r\n        if (head == null) {\r\n            head = new ListNode(x);\r\n        } else {\r\n            while (current.next != null) {\r\n                current = current.next;\r\n            }\r\n            current.next = new ListNode(x);\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    ListNode search(String xId) {\r\n        ListNode p = head;\r\n        while (p != null) {\r\n            if (p.info.getId().equals(xId)) {\r\n                return p;\r\n            }\r\n            p = p.next;\r\n        }\r\n        return null;\r\n    }\r\n\r\n    // Remove a student from scholarship list by ID\r\n    ListNode removeById(String id) {\r\n        // Implement this function - Remove student with given ID from list\r\n        // --------------------------------------------------------\r\n        head = head.next;\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return null; // Change this return statement as needed\r\n    }\r\n\r\n    int count() {\r\n        int count = 0;\r\n        ListNode current = head;\r\n        while (current != null) {\r\n            current = current.next;\r\n            count++;\r\n        }\r\n        return count;\r\n    }\r\n\r\n    // Load initial scholarship data from data.txt file (from line k+5)\r\n    void loadInitialScholarship(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k + 5);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 6);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 7);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 8);\r\n            double[] amounts = Lib.readLineToDoubleArray(\"data.txt\", k + 9);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length,\r\n                    Math.min(gpas.length, Math.min(majors.length, amounts.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                addLast(ids[i], names[i], gpas[i], majors[i], amounts[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading initial scholarship data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\nclass StudentManager {\r\n\r\n    StudentBST studentBST = new StudentBST();\r\n    ScholarshipList scholarshipList = new ScholarshipList();\r\n\r\n    StudentManager() {\r\n    }\r\n\r\n    void fvisit(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void preOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        fvisit(p, f);\r\n        preOrder(p.left, f);\r\n        preOrder(p.right, f);\r\n    }\r\n\r\n    void inOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        inOrder(p.left, f);\r\n        fvisit(p, f);\r\n        inOrder(p.right, f);\r\n    }\r\n\r\n    void ftraverse(java.io.RandomAccessFile f) throws Exception {\r\n        f.writeBytes(\"Student BST (Inorder Traversal):\\r\\n\");\r\n        inOrder(studentBST.root, f);\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Scholarship List:\\r\\n\");\r\n        ListNode p = scholarshipList.head;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception {\r\n        studentBST.loadDataStudents(k);\r\n        scholarshipList.loadInitialScholarship(k);\r\n    }\r\n\r\n    void f1() throws Exception {\r\n        // Test insert() function in StudentBST and addLast() function in ScholarshipList\r\n        load(0);\r\n        String fname = \"f1.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        // Find and display student with highest GPA\r\n        load(0);\r\n        String fname = \"f2.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Find student with highest GPA\r\n        // --------------------------------------------------------\r\n        Student highestGPA = studentBST.findHighestGPA();\r\n        f.writeBytes(\"\\r\\nStudent with highest GPA: \" + highestGPA + \"\\r\\n\");\r\n        // --------------------------------------------------------\r\n\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        // Count students with GPA >= 3.5\r\n        load(0);\r\n        String fname = \"f3.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        // Count students with GPA >= 3.5\r\n        // --------------------------------------------------------\r\n        f.writeBytes(String.valueOf(studentBST.countByGPA(3.5)));\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        // Apply scholarship funds to student accounts\r\n        load(0);\r\n        String fname = \"f4.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Process scholarships - remove from list and add to student balance\r\n        // --------------------------------------------------------\r\n        int count = scholarshipList.count();\r\n        for (int i = 0; i < count; i++) {\r\n            if (studentBST.updateStudentBalance(scholarshipList.head.info.getId(), scholarshipList.search(scholarshipList.head.info.getId()).info.getBalance())) {\r\n                scholarshipList.removeById(scholarshipList.head.info.getId());\r\n            }\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n}\r\n",
          "sampleSolution": "\r\nclass StudentBST {\r\n\r\n    TreeNode root;\r\n\r\n    StudentBST() {\r\n        root = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return root == null;\r\n    }\r\n\r\n    void clear() {\r\n        root = null;\r\n    }\r\n\r\n    TreeNode search(TreeNode p, String xId) {\r\n        if (p == null) {\r\n            return null;\r\n        }\r\n        if (p.info.getId().equals(xId)) {\r\n            return p;\r\n        }\r\n        if (xId.compareTo(p.info.getId()) < 0) {\r\n            return search(p.left, xId);\r\n        } else {\r\n            return search(p.right, xId);\r\n        }\r\n    }\r\n\r\n    void insertHelper(TreeNode root, Student students) {\r\n        if (students.getId().charAt(3) < root.info.getId().charAt(3)) {\r\n            if (root.left == null) {\r\n                root.left = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.left, students);\r\n            }\r\n        } else if (students.getId().charAt(3) > root.info.getId().charAt(3)) {\r\n            if (root.right == null) {\r\n                root.right = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.right, students);\r\n            }\r\n        }\r\n    }\r\n\r\n    void insert(String id, String name, double gpa, String major, double balance) {\r\n        // Implement this function - Insert a new student to the BST\r\n        // If the ID already exists, update the student's information\r\n        // --------------------------------------------------------\r\n        Student x = new Student(id, name, gpa, major, balance);\r\n        if (root == null) {\r\n            root = new TreeNode(x);\r\n        }\r\n        insertHelper(root, x);\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    Student maxGPA(TreeNode root) {\r\n        if (root == null) {\r\n            return null;\r\n        }\r\n\r\n        Student max = root.info;\r\n\r\n        Student leftMax = maxGPA(root.left);\r\n        Student rightMax = maxGPA(root.right);\r\n\r\n        if (leftMax != null && leftMax.getGpa() > max.getGpa()) {\r\n            max = leftMax;\r\n        }\r\n\r\n        if (rightMax != null && rightMax.getGpa() > max.getGpa()) {\r\n            max = rightMax;\r\n        }\r\n\r\n        return max;\r\n    }\r\n\r\n    // Find student with highest GPA\r\n    Student findHighestGPA() {\r\n        // Implement this function - Find student with highest GPA\r\n        // --------------------------------------------------------\r\n\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return maxGPA(root); // Change this return statement as needed\r\n    }\r\n\r\n    int count(TreeNode root, double threshold) {\r\n        if (root == null) {\r\n            return 0;\r\n        }\r\n        if (root.info.getGpa() >= threshold) {\r\n            return count(root.left, 3.5) + count(root.right, 3.5) + 1;\r\n        } else {\r\n            return count(root.left, 3.5) + count(root.right, 3.5);\r\n        }\r\n    }\r\n\r\n    // Count students with GPA greater than or equal to threshold\r\n    int countByGPA(double threshold) {\r\n        // Implement this function - Count students with GPA >= threshold\r\n        // --------------------------------------------------------\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return count(root, 3.5); // Change this return statement as needed\r\n    }\r\n\r\n    // Count students by major\r\n    int countByMajor(String major) {\r\n        return countByMajor(root, major);\r\n    }\r\n\r\n    private int countByMajor(TreeNode p, String major) {\r\n        if (p == null) {\r\n            return 0;\r\n        }\r\n        int count = 0;\r\n        if (p.info.getMajor().equals(major)) {\r\n            count = 1;\r\n        }\r\n\r\n        return count + countByMajor(p.left, major) + countByMajor(p.right, major);\r\n    }\r\n\r\n    // Update student balance in BST\r\n    boolean updateStudentBalance(String id, double amount) {\r\n        // Implement this function - Update balance of student with given ID\r\n        // --------------------------------------------------------\r\n        if (search(root, id) != null) {\r\n            search(root, id).info.setBalance(search(root, id).info.getBalance() + amount);\r\n            return true;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return false; // Change this return statement as needed\r\n    }\r\n\r\n    void loadDataStudents(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 1);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 2);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 3);\r\n            double[] balances = Lib.readLineToDoubleArray(\"data.txt\", k + 4);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length, Math.min(gpas.length,\r\n                    Math.min(majors.length, balances.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                insert(ids[i], names[i], gpas[i], majors[i], balances[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading student data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\n// ScholarshipList.java\r\nclass ScholarshipList {\r\n\r\n    ListNode head, tail;\r\n\r\n    ScholarshipList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return head == null;\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void addLast(String id, String name, double gpa, String major, double scholarshipAmount) {\r\n        // Implement this function - add a new student to the end of the linked list\r\n        // --------------------------------------------------------\r\n        ListNode current = head;\r\n        Student x = new Student(id, name, gpa, major, scholarshipAmount);\r\n        if (head == null) {\r\n            head = new ListNode(x);\r\n        } else {\r\n            while (current.next != null) {\r\n                current = current.next;\r\n            }\r\n            current.next = new ListNode(x);\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    ListNode search(String xId) {\r\n        ListNode p = head;\r\n        while (p != null) {\r\n            if (p.info.getId().equals(xId)) {\r\n                return p;\r\n            }\r\n            p = p.next;\r\n        }\r\n        return null;\r\n    }\r\n\r\n    // Remove a student from scholarship list by ID\r\n    ListNode removeById(String id) {\r\n        // Implement this function - Remove student with given ID from list\r\n        // --------------------------------------------------------\r\n        if (head == null) return null;\r\n        if (head.info.getId().equals(id)) {\r\n            ListNode removed = head;\r\n            head = head.next;\r\n            if (head == null) tail = null;\r\n            return removed;\r\n        }\r\n        ListNode prev = head;\r\n        ListNode cur = head.next;\r\n        while (cur != null) {\r\n            if (cur.info.getId().equals(id)) {\r\n                prev.next = cur.next;\r\n                if (cur == tail) tail = prev;\r\n                return cur;\r\n            }\r\n            prev = cur;\r\n            cur = cur.next;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return null; // Change this return statement as needed\r\n    }\r\n\r\n    int count() {\r\n        int count = 0;\r\n        ListNode current = head;\r\n        while (current != null) {\r\n            current = current.next;\r\n            count++;\r\n        }\r\n        return count;\r\n    }\r\n\r\n    // Load initial scholarship data from data.txt file (from line k+5)\r\n    void loadInitialScholarship(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k + 5);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 6);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 7);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 8);\r\n            double[] amounts = Lib.readLineToDoubleArray(\"data.txt\", k + 9);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length,\r\n                    Math.min(gpas.length, Math.min(majors.length, amounts.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                addLast(ids[i], names[i], gpas[i], majors[i], amounts[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading initial scholarship data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\nclass StudentManager {\r\n\r\n    StudentBST studentBST = new StudentBST();\r\n    ScholarshipList scholarshipList = new ScholarshipList();\r\n\r\n    StudentManager() {\r\n    }\r\n\r\n    void fvisit(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void preOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        fvisit(p, f);\r\n        preOrder(p.left, f);\r\n        preOrder(p.right, f);\r\n    }\r\n\r\n    void inOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        inOrder(p.left, f);\r\n        fvisit(p, f);\r\n        inOrder(p.right, f);\r\n    }\r\n\r\n    void ftraverse(java.io.RandomAccessFile f) throws Exception {\r\n        f.writeBytes(\"Student BST (Inorder Traversal):\\r\\n\");\r\n        inOrder(studentBST.root, f);\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Scholarship List:\\r\\n\");\r\n        ListNode p = scholarshipList.head;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception {\r\n        studentBST.loadDataStudents(k);\r\n        scholarshipList.loadInitialScholarship(k);\r\n    }\r\n\r\n    void f1() throws Exception {\r\n        // Test insert() function in StudentBST and addLast() function in ScholarshipList\r\n        load(0);\r\n        String fname = \"f1.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        // Find and display student with highest GPA\r\n        load(0);\r\n        String fname = \"f2.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Find student with highest GPA\r\n        // --------------------------------------------------------\r\n        Student highestGPA = studentBST.findHighestGPA();\r\n        f.writeBytes(\"\\r\\nStudent with highest GPA: \" + highestGPA + \"\\r\\n\");\r\n        // --------------------------------------------------------\r\n\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        // Count students with GPA >= 3.5\r\n        load(0);\r\n        String fname = \"f3.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        // Count students with GPA >= 3.5\r\n        // --------------------------------------------------------\r\n        f.writeBytes(String.valueOf(studentBST.countByGPA(3.5)));\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        // Apply scholarship funds to student accounts\r\n        load(0);\r\n        String fname = \"f4.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Process scholarships - remove from list and add to student balance\r\n        // --------------------------------------------------------\r\n        int count = scholarshipList.count();\r\n        for (int i = 0; i < count; i++) {\r\n            if (studentBST.updateStudentBalance(scholarshipList.head.info.getId(), scholarshipList.search(scholarshipList.head.info.getId()).info.getBalance())) {\r\n                scholarshipList.removeById(scholarshipList.head.info.getId());\r\n            }\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n}\r\n",
          "expectedOutput": "Student BST (Inorder Traversal):\n(S001,John,3.8,CS,100.0) (S002,Mary,3.5,Business,150.0) (S003,David,3.9,CS,200.0) (S004,Sarah,3.2,IT,120.0) (S005,Michael,3.7,Engineering,180.0)\n\nScholarship List:\n(S003,David,3.9,CS,500.0) (S001,John,3.8,CS,600.0) (S005,Michael,3.7,Engineering,450.0)",
          "explanation": "<p>Verified by compiling against the real given project (Student.java/TreeNode.java/ListNode.java/Main.java/Lib.java unchanged) and running choice 1 — output matches the paper's expected example byte-for-byte. <code>insert()</code> creates the Student and BST-inserts it keyed by <code>id.charAt(3)</code> (already provided via the given <code>insertHelper</code> recursive comparator); <code>addLast()</code> appends to the tail of the singly linked scholarship list.</p>|||<p>Đã kiểm bằng cách biên dịch cùng project given thật (Student.java/TreeNode.java/ListNode.java/Main.java/Lib.java giữ nguyên) và chạy lựa chọn 1 — output khớp byte-for-byte với ví dụ trong đề. <code>insert()</code> tạo Student rồi chèn vào BST khoá theo <code>id.charAt(3)</code> (dùng bộ so sánh đệ quy <code>insertHelper</code> đã cho sẵn); <code>addLast()</code> nối vào cuối danh sách liên kết đơn học bổng.</p>",
          "rubric": [
            {
              "id": "insert",
              "criterion": "insert() correctly builds a BST keyed by the 4th character of the id.|||insert() dựng đúng BST khoá theo ký tự thứ 4 của id.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "addlast",
              "criterion": "addLast() correctly appends to the end of the scholarship list.|||addLast() nối đúng vào cuối danh sách học bổng.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 2.5,
          "language": "java",
          "prompt": "<p><strong>Scenario: Student and Scholarship Management System.</strong></p>\n   <p><strong>Class Structure:</strong> <code>Student</code> (id, name, gpa, major, balance) · <code>TreeNode</code> — a Student plus left/right pointers for the BST · <code>ListNode</code> — a Student plus a next pointer · <code>StudentBST</code> — a binary search tree that manages all students in the system · <code>ScholarshipList</code> — a linked list that manages scholarship recipients · <code>StudentManager</code> — the main class, holding a StudentBST (all students) and a ScholarshipList (scholarship recipients). When a student belongs to the scholarship list, the scholarship amount is stored in the <code>balance</code> field of that Student object.</p>|||<p><strong>Bối cảnh: Hệ thống quản lý Sinh viên và Học bổng.</strong></p>\n   <p><strong>Cấu trúc lớp:</strong> <code>Student</code> (id, name, gpa, major, balance) · <code>TreeNode</code> — Student và con trỏ left/right cho BST · <code>ListNode</code> — Student và con trỏ next · <code>StudentBST</code> — cây BST quản lý toàn bộ sinh viên trong hệ thống · <code>ScholarshipList</code> — danh sách liên kết quản lý sinh viên nhận học bổng · <code>StudentManager</code> — lớp chính, chứa 1 StudentBST (toàn bộ sinh viên) và 1 ScholarshipList (sinh viên nhận học bổng). Khi 1 sinh viên thuộc danh sách học bổng, số tiền học bổng lưu ở field <code>balance</code> của Student đó.</p><p><strong>f2(): (2.5 points)</strong></p>\n     <p>To complete the f2 requirement, students need to:</p>\n     <ul><li>Implement the <code>findHighestGPA()</code> function in the StudentBST structure to find the student with the highest GPA.</li><li>Print this student's information.</li></ul>|||<p><strong>f2(): (2.5 điểm)</strong></p>\n     <p>Để hoàn thành f2, cần:</p>\n     <ul><li>Cài <code>findHighestGPA()</code> trong StudentBST — tìm sinh viên có GPA cao nhất.</li><li>In thông tin sinh viên đó.</li></ul>",
          "starterCode": "\r\nclass StudentBST {\r\n\r\n    TreeNode root;\r\n\r\n    StudentBST() {\r\n        root = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return root == null;\r\n    }\r\n\r\n    void clear() {\r\n        root = null;\r\n    }\r\n\r\n    TreeNode search(TreeNode p, String xId) {\r\n        if (p == null) {\r\n            return null;\r\n        }\r\n        if (p.info.getId().equals(xId)) {\r\n            return p;\r\n        }\r\n        if (xId.compareTo(p.info.getId()) < 0) {\r\n            return search(p.left, xId);\r\n        } else {\r\n            return search(p.right, xId);\r\n        }\r\n    }\r\n\r\n    void insertHelper(TreeNode root, Student students) {\r\n        if (students.getId().charAt(3) < root.info.getId().charAt(3)) {\r\n            if (root.left == null) {\r\n                root.left = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.left, students);\r\n            }\r\n        } else if (students.getId().charAt(3) > root.info.getId().charAt(3)) {\r\n            if (root.right == null) {\r\n                root.right = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.right, students);\r\n            }\r\n        }\r\n    }\r\n\r\n    void insert(String id, String name, double gpa, String major, double balance) {\r\n        // Implement this function - Insert a new student to the BST\r\n        // If the ID already exists, update the student's information\r\n        // --------------------------------------------------------\r\n        Student x = new Student(id, name, gpa, major, balance);\r\n        if (root == null) {\r\n            root = new TreeNode(x);\r\n        }\r\n        insertHelper(root, x);\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    Student maxGPA(TreeNode root) {\r\n        if (root == null) {\r\n            return null;\r\n        }\r\n\r\n        Student max = root.info;\r\n\r\n        Student leftMax = maxGPA(root.left);\r\n        Student rightMax = maxGPA(root.right);\r\n\r\n        if (leftMax != null && leftMax.getGpa() > max.getGpa()) {\r\n            max = leftMax;\r\n        }\r\n\r\n        if (rightMax != null && rightMax.getGpa() > max.getGpa()) {\r\n            max = rightMax;\r\n        }\r\n\r\n        return max;\r\n    }\r\n\r\n    // Find student with highest GPA\r\n    Student findHighestGPA() {\r\n        // Implement this function - Find student with highest GPA\r\n        // --------------------------------------------------------\r\n\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return maxGPA(root); // Change this return statement as needed\r\n    }\r\n\r\n    int count(TreeNode root, double threshold) {\r\n        if (root == null) {\r\n            return 0;\r\n        }\r\n        if (root.info.getGpa() >= threshold) {\r\n            return count(root.left, 3.5) + count(root.right, 3.5) + 1;\r\n        } else {\r\n            return count(root.left, 3.5) + count(root.right, 3.5);\r\n        }\r\n    }\r\n\r\n    // Count students with GPA greater than or equal to threshold\r\n    int countByGPA(double threshold) {\r\n        // Implement this function - Count students with GPA >= threshold\r\n        // --------------------------------------------------------\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return count(root, 3.5); // Change this return statement as needed\r\n    }\r\n\r\n    // Count students by major\r\n    int countByMajor(String major) {\r\n        return countByMajor(root, major);\r\n    }\r\n\r\n    private int countByMajor(TreeNode p, String major) {\r\n        if (p == null) {\r\n            return 0;\r\n        }\r\n        int count = 0;\r\n        if (p.info.getMajor().equals(major)) {\r\n            count = 1;\r\n        }\r\n\r\n        return count + countByMajor(p.left, major) + countByMajor(p.right, major);\r\n    }\r\n\r\n    // Update student balance in BST\r\n    boolean updateStudentBalance(String id, double amount) {\r\n        // Implement this function - Update balance of student with given ID\r\n        // --------------------------------------------------------\r\n        if (search(root, id) != null) {\r\n            search(root, id).info.setBalance(search(root, id).info.getBalance() + amount);\r\n            return true;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return false; // Change this return statement as needed\r\n    }\r\n\r\n    void loadDataStudents(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 1);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 2);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 3);\r\n            double[] balances = Lib.readLineToDoubleArray(\"data.txt\", k + 4);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length, Math.min(gpas.length,\r\n                    Math.min(majors.length, balances.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                insert(ids[i], names[i], gpas[i], majors[i], balances[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading student data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\n// ScholarshipList.java\r\nclass ScholarshipList {\r\n\r\n    ListNode head, tail;\r\n\r\n    ScholarshipList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return head == null;\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void addLast(String id, String name, double gpa, String major, double scholarshipAmount) {\r\n        // Implement this function - add a new student to the end of the linked list\r\n        // --------------------------------------------------------\r\n        ListNode current = head;\r\n        Student x = new Student(id, name, gpa, major, scholarshipAmount);\r\n        if (head == null) {\r\n            head = new ListNode(x);\r\n        } else {\r\n            while (current.next != null) {\r\n                current = current.next;\r\n            }\r\n            current.next = new ListNode(x);\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    ListNode search(String xId) {\r\n        ListNode p = head;\r\n        while (p != null) {\r\n            if (p.info.getId().equals(xId)) {\r\n                return p;\r\n            }\r\n            p = p.next;\r\n        }\r\n        return null;\r\n    }\r\n\r\n    // Remove a student from scholarship list by ID\r\n    ListNode removeById(String id) {\r\n        // Implement this function - Remove student with given ID from list\r\n        // --------------------------------------------------------\r\n        head = head.next;\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return null; // Change this return statement as needed\r\n    }\r\n\r\n    int count() {\r\n        int count = 0;\r\n        ListNode current = head;\r\n        while (current != null) {\r\n            current = current.next;\r\n            count++;\r\n        }\r\n        return count;\r\n    }\r\n\r\n    // Load initial scholarship data from data.txt file (from line k+5)\r\n    void loadInitialScholarship(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k + 5);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 6);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 7);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 8);\r\n            double[] amounts = Lib.readLineToDoubleArray(\"data.txt\", k + 9);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length,\r\n                    Math.min(gpas.length, Math.min(majors.length, amounts.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                addLast(ids[i], names[i], gpas[i], majors[i], amounts[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading initial scholarship data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\nclass StudentManager {\r\n\r\n    StudentBST studentBST = new StudentBST();\r\n    ScholarshipList scholarshipList = new ScholarshipList();\r\n\r\n    StudentManager() {\r\n    }\r\n\r\n    void fvisit(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void preOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        fvisit(p, f);\r\n        preOrder(p.left, f);\r\n        preOrder(p.right, f);\r\n    }\r\n\r\n    void inOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        inOrder(p.left, f);\r\n        fvisit(p, f);\r\n        inOrder(p.right, f);\r\n    }\r\n\r\n    void ftraverse(java.io.RandomAccessFile f) throws Exception {\r\n        f.writeBytes(\"Student BST (Inorder Traversal):\\r\\n\");\r\n        inOrder(studentBST.root, f);\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Scholarship List:\\r\\n\");\r\n        ListNode p = scholarshipList.head;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception {\r\n        studentBST.loadDataStudents(k);\r\n        scholarshipList.loadInitialScholarship(k);\r\n    }\r\n\r\n    void f1() throws Exception {\r\n        // Test insert() function in StudentBST and addLast() function in ScholarshipList\r\n        load(0);\r\n        String fname = \"f1.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        // Find and display student with highest GPA\r\n        load(0);\r\n        String fname = \"f2.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Find student with highest GPA\r\n        // --------------------------------------------------------\r\n        Student highestGPA = studentBST.findHighestGPA();\r\n        f.writeBytes(\"\\r\\nStudent with highest GPA: \" + highestGPA + \"\\r\\n\");\r\n        // --------------------------------------------------------\r\n\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        // Count students with GPA >= 3.5\r\n        load(0);\r\n        String fname = \"f3.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        // Count students with GPA >= 3.5\r\n        // --------------------------------------------------------\r\n        f.writeBytes(String.valueOf(studentBST.countByGPA(3.5)));\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        // Apply scholarship funds to student accounts\r\n        load(0);\r\n        String fname = \"f4.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Process scholarships - remove from list and add to student balance\r\n        // --------------------------------------------------------\r\n        int count = scholarshipList.count();\r\n        for (int i = 0; i < count; i++) {\r\n            if (studentBST.updateStudentBalance(scholarshipList.head.info.getId(), scholarshipList.search(scholarshipList.head.info.getId()).info.getBalance())) {\r\n                scholarshipList.removeById(scholarshipList.head.info.getId());\r\n            }\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n}\r\n",
          "sampleSolution": "\r\nclass StudentBST {\r\n\r\n    TreeNode root;\r\n\r\n    StudentBST() {\r\n        root = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return root == null;\r\n    }\r\n\r\n    void clear() {\r\n        root = null;\r\n    }\r\n\r\n    TreeNode search(TreeNode p, String xId) {\r\n        if (p == null) {\r\n            return null;\r\n        }\r\n        if (p.info.getId().equals(xId)) {\r\n            return p;\r\n        }\r\n        if (xId.compareTo(p.info.getId()) < 0) {\r\n            return search(p.left, xId);\r\n        } else {\r\n            return search(p.right, xId);\r\n        }\r\n    }\r\n\r\n    void insertHelper(TreeNode root, Student students) {\r\n        if (students.getId().charAt(3) < root.info.getId().charAt(3)) {\r\n            if (root.left == null) {\r\n                root.left = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.left, students);\r\n            }\r\n        } else if (students.getId().charAt(3) > root.info.getId().charAt(3)) {\r\n            if (root.right == null) {\r\n                root.right = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.right, students);\r\n            }\r\n        }\r\n    }\r\n\r\n    void insert(String id, String name, double gpa, String major, double balance) {\r\n        // Implement this function - Insert a new student to the BST\r\n        // If the ID already exists, update the student's information\r\n        // --------------------------------------------------------\r\n        Student x = new Student(id, name, gpa, major, balance);\r\n        if (root == null) {\r\n            root = new TreeNode(x);\r\n        }\r\n        insertHelper(root, x);\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    Student maxGPA(TreeNode root) {\r\n        if (root == null) {\r\n            return null;\r\n        }\r\n\r\n        Student max = root.info;\r\n\r\n        Student leftMax = maxGPA(root.left);\r\n        Student rightMax = maxGPA(root.right);\r\n\r\n        if (leftMax != null && leftMax.getGpa() > max.getGpa()) {\r\n            max = leftMax;\r\n        }\r\n\r\n        if (rightMax != null && rightMax.getGpa() > max.getGpa()) {\r\n            max = rightMax;\r\n        }\r\n\r\n        return max;\r\n    }\r\n\r\n    // Find student with highest GPA\r\n    Student findHighestGPA() {\r\n        // Implement this function - Find student with highest GPA\r\n        // --------------------------------------------------------\r\n\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return maxGPA(root); // Change this return statement as needed\r\n    }\r\n\r\n    int count(TreeNode root, double threshold) {\r\n        if (root == null) {\r\n            return 0;\r\n        }\r\n        if (root.info.getGpa() >= threshold) {\r\n            return count(root.left, 3.5) + count(root.right, 3.5) + 1;\r\n        } else {\r\n            return count(root.left, 3.5) + count(root.right, 3.5);\r\n        }\r\n    }\r\n\r\n    // Count students with GPA greater than or equal to threshold\r\n    int countByGPA(double threshold) {\r\n        // Implement this function - Count students with GPA >= threshold\r\n        // --------------------------------------------------------\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return count(root, 3.5); // Change this return statement as needed\r\n    }\r\n\r\n    // Count students by major\r\n    int countByMajor(String major) {\r\n        return countByMajor(root, major);\r\n    }\r\n\r\n    private int countByMajor(TreeNode p, String major) {\r\n        if (p == null) {\r\n            return 0;\r\n        }\r\n        int count = 0;\r\n        if (p.info.getMajor().equals(major)) {\r\n            count = 1;\r\n        }\r\n\r\n        return count + countByMajor(p.left, major) + countByMajor(p.right, major);\r\n    }\r\n\r\n    // Update student balance in BST\r\n    boolean updateStudentBalance(String id, double amount) {\r\n        // Implement this function - Update balance of student with given ID\r\n        // --------------------------------------------------------\r\n        if (search(root, id) != null) {\r\n            search(root, id).info.setBalance(search(root, id).info.getBalance() + amount);\r\n            return true;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return false; // Change this return statement as needed\r\n    }\r\n\r\n    void loadDataStudents(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 1);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 2);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 3);\r\n            double[] balances = Lib.readLineToDoubleArray(\"data.txt\", k + 4);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length, Math.min(gpas.length,\r\n                    Math.min(majors.length, balances.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                insert(ids[i], names[i], gpas[i], majors[i], balances[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading student data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\n// ScholarshipList.java\r\nclass ScholarshipList {\r\n\r\n    ListNode head, tail;\r\n\r\n    ScholarshipList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return head == null;\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void addLast(String id, String name, double gpa, String major, double scholarshipAmount) {\r\n        // Implement this function - add a new student to the end of the linked list\r\n        // --------------------------------------------------------\r\n        ListNode current = head;\r\n        Student x = new Student(id, name, gpa, major, scholarshipAmount);\r\n        if (head == null) {\r\n            head = new ListNode(x);\r\n        } else {\r\n            while (current.next != null) {\r\n                current = current.next;\r\n            }\r\n            current.next = new ListNode(x);\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    ListNode search(String xId) {\r\n        ListNode p = head;\r\n        while (p != null) {\r\n            if (p.info.getId().equals(xId)) {\r\n                return p;\r\n            }\r\n            p = p.next;\r\n        }\r\n        return null;\r\n    }\r\n\r\n    // Remove a student from scholarship list by ID\r\n    ListNode removeById(String id) {\r\n        // Implement this function - Remove student with given ID from list\r\n        // --------------------------------------------------------\r\n        if (head == null) return null;\r\n        if (head.info.getId().equals(id)) {\r\n            ListNode removed = head;\r\n            head = head.next;\r\n            if (head == null) tail = null;\r\n            return removed;\r\n        }\r\n        ListNode prev = head;\r\n        ListNode cur = head.next;\r\n        while (cur != null) {\r\n            if (cur.info.getId().equals(id)) {\r\n                prev.next = cur.next;\r\n                if (cur == tail) tail = prev;\r\n                return cur;\r\n            }\r\n            prev = cur;\r\n            cur = cur.next;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return null; // Change this return statement as needed\r\n    }\r\n\r\n    int count() {\r\n        int count = 0;\r\n        ListNode current = head;\r\n        while (current != null) {\r\n            current = current.next;\r\n            count++;\r\n        }\r\n        return count;\r\n    }\r\n\r\n    // Load initial scholarship data from data.txt file (from line k+5)\r\n    void loadInitialScholarship(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k + 5);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 6);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 7);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 8);\r\n            double[] amounts = Lib.readLineToDoubleArray(\"data.txt\", k + 9);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length,\r\n                    Math.min(gpas.length, Math.min(majors.length, amounts.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                addLast(ids[i], names[i], gpas[i], majors[i], amounts[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading initial scholarship data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\nclass StudentManager {\r\n\r\n    StudentBST studentBST = new StudentBST();\r\n    ScholarshipList scholarshipList = new ScholarshipList();\r\n\r\n    StudentManager() {\r\n    }\r\n\r\n    void fvisit(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void preOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        fvisit(p, f);\r\n        preOrder(p.left, f);\r\n        preOrder(p.right, f);\r\n    }\r\n\r\n    void inOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        inOrder(p.left, f);\r\n        fvisit(p, f);\r\n        inOrder(p.right, f);\r\n    }\r\n\r\n    void ftraverse(java.io.RandomAccessFile f) throws Exception {\r\n        f.writeBytes(\"Student BST (Inorder Traversal):\\r\\n\");\r\n        inOrder(studentBST.root, f);\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Scholarship List:\\r\\n\");\r\n        ListNode p = scholarshipList.head;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception {\r\n        studentBST.loadDataStudents(k);\r\n        scholarshipList.loadInitialScholarship(k);\r\n    }\r\n\r\n    void f1() throws Exception {\r\n        // Test insert() function in StudentBST and addLast() function in ScholarshipList\r\n        load(0);\r\n        String fname = \"f1.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        // Find and display student with highest GPA\r\n        load(0);\r\n        String fname = \"f2.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Find student with highest GPA\r\n        // --------------------------------------------------------\r\n        Student highestGPA = studentBST.findHighestGPA();\r\n        f.writeBytes(\"\\r\\nStudent with highest GPA: \" + highestGPA + \"\\r\\n\");\r\n        // --------------------------------------------------------\r\n\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        // Count students with GPA >= 3.5\r\n        load(0);\r\n        String fname = \"f3.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        // Count students with GPA >= 3.5\r\n        // --------------------------------------------------------\r\n        f.writeBytes(String.valueOf(studentBST.countByGPA(3.5)));\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        // Apply scholarship funds to student accounts\r\n        load(0);\r\n        String fname = \"f4.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Process scholarships - remove from list and add to student balance\r\n        // --------------------------------------------------------\r\n        int count = scholarshipList.count();\r\n        for (int i = 0; i < count; i++) {\r\n            if (studentBST.updateStudentBalance(scholarshipList.head.info.getId(), scholarshipList.search(scholarshipList.head.info.getId()).info.getBalance())) {\r\n                scholarshipList.removeById(scholarshipList.head.info.getId());\r\n            }\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n}\r\n",
          "expectedOutput": "Student BST (Inorder Traversal):\n(S001,John,3.8,CS,100.0) (S002,Mary,3.5,Business,150.0) (S003,David,3.9,CS,200.0) (S004,Sarah,3.2,IT,120.0) (S005,Michael,3.7,Engineering,180.0)\n\nScholarship List:\n(S003,David,3.9,CS,500.0) (S001,John,3.8,CS,600.0) (S005,Michael,3.7,Engineering,450.0)\n\nStudent with highest GPA: (S003,David,3.9,CS,200.0)",
          "explanation": "<p>Verified by compiling against the real given project and running choice 2 — output matches the paper's expected example byte-for-byte. <code>findHighestGPA()</code> delegates to the given recursive <code>maxGPA()</code> helper, which visits every node and keeps the Student with the highest <code>gpa</code> (David, 3.9).</p>|||<p>Đã kiểm bằng cách biên dịch cùng project given thật và chạy lựa chọn 2 — output khớp byte-for-byte với ví dụ trong đề. <code>findHighestGPA()</code> gọi hàm đệ quy hỗ trợ <code>maxGPA()</code> đã cho, duyệt toàn bộ node giữ lại Student có <code>gpa</code> cao nhất (David, 3.9).</p>",
          "rubric": [
            {
              "id": "highest_gpa",
              "criterion": "Correctly finds and returns the student with the highest GPA across the whole tree.|||Tìm và trả đúng sinh viên có GPA cao nhất trong toàn cây.",
              "weight": 1,
              "maxScore": 2.5
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 2.5,
          "language": "java",
          "prompt": "<p><strong>Scenario: Student and Scholarship Management System.</strong></p>\n   <p><strong>Class Structure:</strong> <code>Student</code> (id, name, gpa, major, balance) · <code>TreeNode</code> — a Student plus left/right pointers for the BST · <code>ListNode</code> — a Student plus a next pointer · <code>StudentBST</code> — a binary search tree that manages all students in the system · <code>ScholarshipList</code> — a linked list that manages scholarship recipients · <code>StudentManager</code> — the main class, holding a StudentBST (all students) and a ScholarshipList (scholarship recipients). When a student belongs to the scholarship list, the scholarship amount is stored in the <code>balance</code> field of that Student object.</p>|||<p><strong>Bối cảnh: Hệ thống quản lý Sinh viên và Học bổng.</strong></p>\n   <p><strong>Cấu trúc lớp:</strong> <code>Student</code> (id, name, gpa, major, balance) · <code>TreeNode</code> — Student và con trỏ left/right cho BST · <code>ListNode</code> — Student và con trỏ next · <code>StudentBST</code> — cây BST quản lý toàn bộ sinh viên trong hệ thống · <code>ScholarshipList</code> — danh sách liên kết quản lý sinh viên nhận học bổng · <code>StudentManager</code> — lớp chính, chứa 1 StudentBST (toàn bộ sinh viên) và 1 ScholarshipList (sinh viên nhận học bổng). Khi 1 sinh viên thuộc danh sách học bổng, số tiền học bổng lưu ở field <code>balance</code> của Student đó.</p><p><strong>f3(): (2.5 points)</strong></p>\n     <p>To complete f3, students need to:</p>\n     <ul><li>Count and display the number of students whose GPA is greater than or equal to 3.5.</li><li>Implement the <code>countByGPA()</code> method in the StudentBST structure.</li></ul>\n     <p>The expected output consists of only a single number (the count of students with GPA &gt;= 3.5).</p>|||<p><strong>f3(): (2.5 điểm)</strong></p>\n     <p>Để hoàn thành f3, cần:</p>\n     <ul><li>Đếm và hiển thị số sinh viên có GPA &gt;= 3.5.</li><li>Cài <code>countByGPA()</code> trong StudentBST.</li></ul>\n     <p>Output chỉ gồm 1 con số duy nhất (số sinh viên GPA &gt;= 3.5).</p>",
          "starterCode": "\r\nclass StudentBST {\r\n\r\n    TreeNode root;\r\n\r\n    StudentBST() {\r\n        root = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return root == null;\r\n    }\r\n\r\n    void clear() {\r\n        root = null;\r\n    }\r\n\r\n    TreeNode search(TreeNode p, String xId) {\r\n        if (p == null) {\r\n            return null;\r\n        }\r\n        if (p.info.getId().equals(xId)) {\r\n            return p;\r\n        }\r\n        if (xId.compareTo(p.info.getId()) < 0) {\r\n            return search(p.left, xId);\r\n        } else {\r\n            return search(p.right, xId);\r\n        }\r\n    }\r\n\r\n    void insertHelper(TreeNode root, Student students) {\r\n        if (students.getId().charAt(3) < root.info.getId().charAt(3)) {\r\n            if (root.left == null) {\r\n                root.left = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.left, students);\r\n            }\r\n        } else if (students.getId().charAt(3) > root.info.getId().charAt(3)) {\r\n            if (root.right == null) {\r\n                root.right = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.right, students);\r\n            }\r\n        }\r\n    }\r\n\r\n    void insert(String id, String name, double gpa, String major, double balance) {\r\n        // Implement this function - Insert a new student to the BST\r\n        // If the ID already exists, update the student's information\r\n        // --------------------------------------------------------\r\n        Student x = new Student(id, name, gpa, major, balance);\r\n        if (root == null) {\r\n            root = new TreeNode(x);\r\n        }\r\n        insertHelper(root, x);\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    Student maxGPA(TreeNode root) {\r\n        if (root == null) {\r\n            return null;\r\n        }\r\n\r\n        Student max = root.info;\r\n\r\n        Student leftMax = maxGPA(root.left);\r\n        Student rightMax = maxGPA(root.right);\r\n\r\n        if (leftMax != null && leftMax.getGpa() > max.getGpa()) {\r\n            max = leftMax;\r\n        }\r\n\r\n        if (rightMax != null && rightMax.getGpa() > max.getGpa()) {\r\n            max = rightMax;\r\n        }\r\n\r\n        return max;\r\n    }\r\n\r\n    // Find student with highest GPA\r\n    Student findHighestGPA() {\r\n        // Implement this function - Find student with highest GPA\r\n        // --------------------------------------------------------\r\n\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return maxGPA(root); // Change this return statement as needed\r\n    }\r\n\r\n    int count(TreeNode root, double threshold) {\r\n        if (root == null) {\r\n            return 0;\r\n        }\r\n        if (root.info.getGpa() >= threshold) {\r\n            return count(root.left, 3.5) + count(root.right, 3.5) + 1;\r\n        } else {\r\n            return count(root.left, 3.5) + count(root.right, 3.5);\r\n        }\r\n    }\r\n\r\n    // Count students with GPA greater than or equal to threshold\r\n    int countByGPA(double threshold) {\r\n        // Implement this function - Count students with GPA >= threshold\r\n        // --------------------------------------------------------\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return count(root, 3.5); // Change this return statement as needed\r\n    }\r\n\r\n    // Count students by major\r\n    int countByMajor(String major) {\r\n        return countByMajor(root, major);\r\n    }\r\n\r\n    private int countByMajor(TreeNode p, String major) {\r\n        if (p == null) {\r\n            return 0;\r\n        }\r\n        int count = 0;\r\n        if (p.info.getMajor().equals(major)) {\r\n            count = 1;\r\n        }\r\n\r\n        return count + countByMajor(p.left, major) + countByMajor(p.right, major);\r\n    }\r\n\r\n    // Update student balance in BST\r\n    boolean updateStudentBalance(String id, double amount) {\r\n        // Implement this function - Update balance of student with given ID\r\n        // --------------------------------------------------------\r\n        if (search(root, id) != null) {\r\n            search(root, id).info.setBalance(search(root, id).info.getBalance() + amount);\r\n            return true;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return false; // Change this return statement as needed\r\n    }\r\n\r\n    void loadDataStudents(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 1);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 2);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 3);\r\n            double[] balances = Lib.readLineToDoubleArray(\"data.txt\", k + 4);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length, Math.min(gpas.length,\r\n                    Math.min(majors.length, balances.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                insert(ids[i], names[i], gpas[i], majors[i], balances[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading student data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\n// ScholarshipList.java\r\nclass ScholarshipList {\r\n\r\n    ListNode head, tail;\r\n\r\n    ScholarshipList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return head == null;\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void addLast(String id, String name, double gpa, String major, double scholarshipAmount) {\r\n        // Implement this function - add a new student to the end of the linked list\r\n        // --------------------------------------------------------\r\n        ListNode current = head;\r\n        Student x = new Student(id, name, gpa, major, scholarshipAmount);\r\n        if (head == null) {\r\n            head = new ListNode(x);\r\n        } else {\r\n            while (current.next != null) {\r\n                current = current.next;\r\n            }\r\n            current.next = new ListNode(x);\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    ListNode search(String xId) {\r\n        ListNode p = head;\r\n        while (p != null) {\r\n            if (p.info.getId().equals(xId)) {\r\n                return p;\r\n            }\r\n            p = p.next;\r\n        }\r\n        return null;\r\n    }\r\n\r\n    // Remove a student from scholarship list by ID\r\n    ListNode removeById(String id) {\r\n        // Implement this function - Remove student with given ID from list\r\n        // --------------------------------------------------------\r\n        head = head.next;\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return null; // Change this return statement as needed\r\n    }\r\n\r\n    int count() {\r\n        int count = 0;\r\n        ListNode current = head;\r\n        while (current != null) {\r\n            current = current.next;\r\n            count++;\r\n        }\r\n        return count;\r\n    }\r\n\r\n    // Load initial scholarship data from data.txt file (from line k+5)\r\n    void loadInitialScholarship(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k + 5);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 6);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 7);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 8);\r\n            double[] amounts = Lib.readLineToDoubleArray(\"data.txt\", k + 9);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length,\r\n                    Math.min(gpas.length, Math.min(majors.length, amounts.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                addLast(ids[i], names[i], gpas[i], majors[i], amounts[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading initial scholarship data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\nclass StudentManager {\r\n\r\n    StudentBST studentBST = new StudentBST();\r\n    ScholarshipList scholarshipList = new ScholarshipList();\r\n\r\n    StudentManager() {\r\n    }\r\n\r\n    void fvisit(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void preOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        fvisit(p, f);\r\n        preOrder(p.left, f);\r\n        preOrder(p.right, f);\r\n    }\r\n\r\n    void inOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        inOrder(p.left, f);\r\n        fvisit(p, f);\r\n        inOrder(p.right, f);\r\n    }\r\n\r\n    void ftraverse(java.io.RandomAccessFile f) throws Exception {\r\n        f.writeBytes(\"Student BST (Inorder Traversal):\\r\\n\");\r\n        inOrder(studentBST.root, f);\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Scholarship List:\\r\\n\");\r\n        ListNode p = scholarshipList.head;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception {\r\n        studentBST.loadDataStudents(k);\r\n        scholarshipList.loadInitialScholarship(k);\r\n    }\r\n\r\n    void f1() throws Exception {\r\n        // Test insert() function in StudentBST and addLast() function in ScholarshipList\r\n        load(0);\r\n        String fname = \"f1.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        // Find and display student with highest GPA\r\n        load(0);\r\n        String fname = \"f2.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Find student with highest GPA\r\n        // --------------------------------------------------------\r\n        Student highestGPA = studentBST.findHighestGPA();\r\n        f.writeBytes(\"\\r\\nStudent with highest GPA: \" + highestGPA + \"\\r\\n\");\r\n        // --------------------------------------------------------\r\n\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        // Count students with GPA >= 3.5\r\n        load(0);\r\n        String fname = \"f3.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        // Count students with GPA >= 3.5\r\n        // --------------------------------------------------------\r\n        f.writeBytes(String.valueOf(studentBST.countByGPA(3.5)));\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        // Apply scholarship funds to student accounts\r\n        load(0);\r\n        String fname = \"f4.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Process scholarships - remove from list and add to student balance\r\n        // --------------------------------------------------------\r\n        int count = scholarshipList.count();\r\n        for (int i = 0; i < count; i++) {\r\n            if (studentBST.updateStudentBalance(scholarshipList.head.info.getId(), scholarshipList.search(scholarshipList.head.info.getId()).info.getBalance())) {\r\n                scholarshipList.removeById(scholarshipList.head.info.getId());\r\n            }\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n}\r\n",
          "sampleSolution": "\r\nclass StudentBST {\r\n\r\n    TreeNode root;\r\n\r\n    StudentBST() {\r\n        root = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return root == null;\r\n    }\r\n\r\n    void clear() {\r\n        root = null;\r\n    }\r\n\r\n    TreeNode search(TreeNode p, String xId) {\r\n        if (p == null) {\r\n            return null;\r\n        }\r\n        if (p.info.getId().equals(xId)) {\r\n            return p;\r\n        }\r\n        if (xId.compareTo(p.info.getId()) < 0) {\r\n            return search(p.left, xId);\r\n        } else {\r\n            return search(p.right, xId);\r\n        }\r\n    }\r\n\r\n    void insertHelper(TreeNode root, Student students) {\r\n        if (students.getId().charAt(3) < root.info.getId().charAt(3)) {\r\n            if (root.left == null) {\r\n                root.left = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.left, students);\r\n            }\r\n        } else if (students.getId().charAt(3) > root.info.getId().charAt(3)) {\r\n            if (root.right == null) {\r\n                root.right = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.right, students);\r\n            }\r\n        }\r\n    }\r\n\r\n    void insert(String id, String name, double gpa, String major, double balance) {\r\n        // Implement this function - Insert a new student to the BST\r\n        // If the ID already exists, update the student's information\r\n        // --------------------------------------------------------\r\n        Student x = new Student(id, name, gpa, major, balance);\r\n        if (root == null) {\r\n            root = new TreeNode(x);\r\n        }\r\n        insertHelper(root, x);\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    Student maxGPA(TreeNode root) {\r\n        if (root == null) {\r\n            return null;\r\n        }\r\n\r\n        Student max = root.info;\r\n\r\n        Student leftMax = maxGPA(root.left);\r\n        Student rightMax = maxGPA(root.right);\r\n\r\n        if (leftMax != null && leftMax.getGpa() > max.getGpa()) {\r\n            max = leftMax;\r\n        }\r\n\r\n        if (rightMax != null && rightMax.getGpa() > max.getGpa()) {\r\n            max = rightMax;\r\n        }\r\n\r\n        return max;\r\n    }\r\n\r\n    // Find student with highest GPA\r\n    Student findHighestGPA() {\r\n        // Implement this function - Find student with highest GPA\r\n        // --------------------------------------------------------\r\n\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return maxGPA(root); // Change this return statement as needed\r\n    }\r\n\r\n    int count(TreeNode root, double threshold) {\r\n        if (root == null) {\r\n            return 0;\r\n        }\r\n        if (root.info.getGpa() >= threshold) {\r\n            return count(root.left, 3.5) + count(root.right, 3.5) + 1;\r\n        } else {\r\n            return count(root.left, 3.5) + count(root.right, 3.5);\r\n        }\r\n    }\r\n\r\n    // Count students with GPA greater than or equal to threshold\r\n    int countByGPA(double threshold) {\r\n        // Implement this function - Count students with GPA >= threshold\r\n        // --------------------------------------------------------\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return count(root, 3.5); // Change this return statement as needed\r\n    }\r\n\r\n    // Count students by major\r\n    int countByMajor(String major) {\r\n        return countByMajor(root, major);\r\n    }\r\n\r\n    private int countByMajor(TreeNode p, String major) {\r\n        if (p == null) {\r\n            return 0;\r\n        }\r\n        int count = 0;\r\n        if (p.info.getMajor().equals(major)) {\r\n            count = 1;\r\n        }\r\n\r\n        return count + countByMajor(p.left, major) + countByMajor(p.right, major);\r\n    }\r\n\r\n    // Update student balance in BST\r\n    boolean updateStudentBalance(String id, double amount) {\r\n        // Implement this function - Update balance of student with given ID\r\n        // --------------------------------------------------------\r\n        if (search(root, id) != null) {\r\n            search(root, id).info.setBalance(search(root, id).info.getBalance() + amount);\r\n            return true;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return false; // Change this return statement as needed\r\n    }\r\n\r\n    void loadDataStudents(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 1);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 2);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 3);\r\n            double[] balances = Lib.readLineToDoubleArray(\"data.txt\", k + 4);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length, Math.min(gpas.length,\r\n                    Math.min(majors.length, balances.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                insert(ids[i], names[i], gpas[i], majors[i], balances[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading student data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\n// ScholarshipList.java\r\nclass ScholarshipList {\r\n\r\n    ListNode head, tail;\r\n\r\n    ScholarshipList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return head == null;\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void addLast(String id, String name, double gpa, String major, double scholarshipAmount) {\r\n        // Implement this function - add a new student to the end of the linked list\r\n        // --------------------------------------------------------\r\n        ListNode current = head;\r\n        Student x = new Student(id, name, gpa, major, scholarshipAmount);\r\n        if (head == null) {\r\n            head = new ListNode(x);\r\n        } else {\r\n            while (current.next != null) {\r\n                current = current.next;\r\n            }\r\n            current.next = new ListNode(x);\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    ListNode search(String xId) {\r\n        ListNode p = head;\r\n        while (p != null) {\r\n            if (p.info.getId().equals(xId)) {\r\n                return p;\r\n            }\r\n            p = p.next;\r\n        }\r\n        return null;\r\n    }\r\n\r\n    // Remove a student from scholarship list by ID\r\n    ListNode removeById(String id) {\r\n        // Implement this function - Remove student with given ID from list\r\n        // --------------------------------------------------------\r\n        if (head == null) return null;\r\n        if (head.info.getId().equals(id)) {\r\n            ListNode removed = head;\r\n            head = head.next;\r\n            if (head == null) tail = null;\r\n            return removed;\r\n        }\r\n        ListNode prev = head;\r\n        ListNode cur = head.next;\r\n        while (cur != null) {\r\n            if (cur.info.getId().equals(id)) {\r\n                prev.next = cur.next;\r\n                if (cur == tail) tail = prev;\r\n                return cur;\r\n            }\r\n            prev = cur;\r\n            cur = cur.next;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return null; // Change this return statement as needed\r\n    }\r\n\r\n    int count() {\r\n        int count = 0;\r\n        ListNode current = head;\r\n        while (current != null) {\r\n            current = current.next;\r\n            count++;\r\n        }\r\n        return count;\r\n    }\r\n\r\n    // Load initial scholarship data from data.txt file (from line k+5)\r\n    void loadInitialScholarship(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k + 5);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 6);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 7);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 8);\r\n            double[] amounts = Lib.readLineToDoubleArray(\"data.txt\", k + 9);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length,\r\n                    Math.min(gpas.length, Math.min(majors.length, amounts.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                addLast(ids[i], names[i], gpas[i], majors[i], amounts[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading initial scholarship data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\nclass StudentManager {\r\n\r\n    StudentBST studentBST = new StudentBST();\r\n    ScholarshipList scholarshipList = new ScholarshipList();\r\n\r\n    StudentManager() {\r\n    }\r\n\r\n    void fvisit(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void preOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        fvisit(p, f);\r\n        preOrder(p.left, f);\r\n        preOrder(p.right, f);\r\n    }\r\n\r\n    void inOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        inOrder(p.left, f);\r\n        fvisit(p, f);\r\n        inOrder(p.right, f);\r\n    }\r\n\r\n    void ftraverse(java.io.RandomAccessFile f) throws Exception {\r\n        f.writeBytes(\"Student BST (Inorder Traversal):\\r\\n\");\r\n        inOrder(studentBST.root, f);\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Scholarship List:\\r\\n\");\r\n        ListNode p = scholarshipList.head;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception {\r\n        studentBST.loadDataStudents(k);\r\n        scholarshipList.loadInitialScholarship(k);\r\n    }\r\n\r\n    void f1() throws Exception {\r\n        // Test insert() function in StudentBST and addLast() function in ScholarshipList\r\n        load(0);\r\n        String fname = \"f1.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        // Find and display student with highest GPA\r\n        load(0);\r\n        String fname = \"f2.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Find student with highest GPA\r\n        // --------------------------------------------------------\r\n        Student highestGPA = studentBST.findHighestGPA();\r\n        f.writeBytes(\"\\r\\nStudent with highest GPA: \" + highestGPA + \"\\r\\n\");\r\n        // --------------------------------------------------------\r\n\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        // Count students with GPA >= 3.5\r\n        load(0);\r\n        String fname = \"f3.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        // Count students with GPA >= 3.5\r\n        // --------------------------------------------------------\r\n        f.writeBytes(String.valueOf(studentBST.countByGPA(3.5)));\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        // Apply scholarship funds to student accounts\r\n        load(0);\r\n        String fname = \"f4.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Process scholarships - remove from list and add to student balance\r\n        // --------------------------------------------------------\r\n        int count = scholarshipList.count();\r\n        for (int i = 0; i < count; i++) {\r\n            if (studentBST.updateStudentBalance(scholarshipList.head.info.getId(), scholarshipList.search(scholarshipList.head.info.getId()).info.getBalance())) {\r\n                scholarshipList.removeById(scholarshipList.head.info.getId());\r\n            }\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n}\r\n",
          "expectedOutput": "4",
          "explanation": "<p>Verified by compiling against the real given project and running choice 3 — output matches the paper's expected example byte-for-byte (4 students with gpa &gt;= 3.5 among the 5: John 3.8, Mary 3.5, David 3.9, Michael 3.7 qualify; Sarah 3.2 does not). Delegates to the given recursive <code>count(root, threshold)</code> helper.</p>|||<p>Đã kiểm bằng cách biên dịch cùng project given thật và chạy lựa chọn 3 — output khớp byte-for-byte với ví dụ trong đề (4 sinh viên GPA &gt;= 3.5 trong 5 người: John 3.8, Mary 3.5, David 3.9, Michael 3.7 thoả; Sarah 3.2 không). Gọi hàm đệ quy hỗ trợ <code>count(root, threshold)</code> đã cho.</p>",
          "rubric": [
            {
              "id": "count_gpa",
              "criterion": "Correctly counts all students in the tree with gpa >= 3.5.|||Đếm đúng mọi sinh viên trong cây có gpa >= 3.5.",
              "weight": 1,
              "maxScore": 2.5
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 2.5,
          "language": "java",
          "prompt": "<p><strong>Scenario: Student and Scholarship Management System.</strong></p>\n   <p><strong>Class Structure:</strong> <code>Student</code> (id, name, gpa, major, balance) · <code>TreeNode</code> — a Student plus left/right pointers for the BST · <code>ListNode</code> — a Student plus a next pointer · <code>StudentBST</code> — a binary search tree that manages all students in the system · <code>ScholarshipList</code> — a linked list that manages scholarship recipients · <code>StudentManager</code> — the main class, holding a StudentBST (all students) and a ScholarshipList (scholarship recipients). When a student belongs to the scholarship list, the scholarship amount is stored in the <code>balance</code> field of that Student object.</p>|||<p><strong>Bối cảnh: Hệ thống quản lý Sinh viên và Học bổng.</strong></p>\n   <p><strong>Cấu trúc lớp:</strong> <code>Student</code> (id, name, gpa, major, balance) · <code>TreeNode</code> — Student và con trỏ left/right cho BST · <code>ListNode</code> — Student và con trỏ next · <code>StudentBST</code> — cây BST quản lý toàn bộ sinh viên trong hệ thống · <code>ScholarshipList</code> — danh sách liên kết quản lý sinh viên nhận học bổng · <code>StudentManager</code> — lớp chính, chứa 1 StudentBST (toàn bộ sinh viên) và 1 ScholarshipList (sinh viên nhận học bổng). Khi 1 sinh viên thuộc danh sách học bổng, số tiền học bổng lưu ở field <code>balance</code> của Student đó.</p><p><strong>f4(): (2.5 points)</strong></p>\n     <p>To finish f4, students need to:</p>\n     <ul><li>Implement the <code>removeById()</code> function in the ScholarshipList structure to remove a student from the scholarship list by ID.</li>\n     <li>Implement the <code>updateStudentBalance()</code> function in the StudentBST structure to update the amount in the student's account.</li>\n     <li>Process scholarships: remove students from the scholarship list and add the scholarship amount to the student's account in the BST.</li></ul>|||<p><strong>f4(): (2.5 điểm)</strong></p>\n     <p>Để hoàn thành f4, cần:</p>\n     <ul><li>Cài <code>removeById()</code> trong ScholarshipList — xoá sinh viên khỏi danh sách học bổng theo ID.</li>\n     <li>Cài <code>updateStudentBalance()</code> trong StudentBST — cập nhật số dư tài khoản sinh viên.</li>\n     <li>Xử lý học bổng: xoá sinh viên khỏi danh sách học bổng và cộng số tiền học bổng vào tài khoản sinh viên trong BST.</li></ul>",
          "starterCode": "\r\nclass StudentBST {\r\n\r\n    TreeNode root;\r\n\r\n    StudentBST() {\r\n        root = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return root == null;\r\n    }\r\n\r\n    void clear() {\r\n        root = null;\r\n    }\r\n\r\n    TreeNode search(TreeNode p, String xId) {\r\n        if (p == null) {\r\n            return null;\r\n        }\r\n        if (p.info.getId().equals(xId)) {\r\n            return p;\r\n        }\r\n        if (xId.compareTo(p.info.getId()) < 0) {\r\n            return search(p.left, xId);\r\n        } else {\r\n            return search(p.right, xId);\r\n        }\r\n    }\r\n\r\n    void insertHelper(TreeNode root, Student students) {\r\n        if (students.getId().charAt(3) < root.info.getId().charAt(3)) {\r\n            if (root.left == null) {\r\n                root.left = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.left, students);\r\n            }\r\n        } else if (students.getId().charAt(3) > root.info.getId().charAt(3)) {\r\n            if (root.right == null) {\r\n                root.right = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.right, students);\r\n            }\r\n        }\r\n    }\r\n\r\n    void insert(String id, String name, double gpa, String major, double balance) {\r\n        // Implement this function - Insert a new student to the BST\r\n        // If the ID already exists, update the student's information\r\n        // --------------------------------------------------------\r\n        Student x = new Student(id, name, gpa, major, balance);\r\n        if (root == null) {\r\n            root = new TreeNode(x);\r\n        }\r\n        insertHelper(root, x);\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    Student maxGPA(TreeNode root) {\r\n        if (root == null) {\r\n            return null;\r\n        }\r\n\r\n        Student max = root.info;\r\n\r\n        Student leftMax = maxGPA(root.left);\r\n        Student rightMax = maxGPA(root.right);\r\n\r\n        if (leftMax != null && leftMax.getGpa() > max.getGpa()) {\r\n            max = leftMax;\r\n        }\r\n\r\n        if (rightMax != null && rightMax.getGpa() > max.getGpa()) {\r\n            max = rightMax;\r\n        }\r\n\r\n        return max;\r\n    }\r\n\r\n    // Find student with highest GPA\r\n    Student findHighestGPA() {\r\n        // Implement this function - Find student with highest GPA\r\n        // --------------------------------------------------------\r\n\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return maxGPA(root); // Change this return statement as needed\r\n    }\r\n\r\n    int count(TreeNode root, double threshold) {\r\n        if (root == null) {\r\n            return 0;\r\n        }\r\n        if (root.info.getGpa() >= threshold) {\r\n            return count(root.left, 3.5) + count(root.right, 3.5) + 1;\r\n        } else {\r\n            return count(root.left, 3.5) + count(root.right, 3.5);\r\n        }\r\n    }\r\n\r\n    // Count students with GPA greater than or equal to threshold\r\n    int countByGPA(double threshold) {\r\n        // Implement this function - Count students with GPA >= threshold\r\n        // --------------------------------------------------------\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return count(root, 3.5); // Change this return statement as needed\r\n    }\r\n\r\n    // Count students by major\r\n    int countByMajor(String major) {\r\n        return countByMajor(root, major);\r\n    }\r\n\r\n    private int countByMajor(TreeNode p, String major) {\r\n        if (p == null) {\r\n            return 0;\r\n        }\r\n        int count = 0;\r\n        if (p.info.getMajor().equals(major)) {\r\n            count = 1;\r\n        }\r\n\r\n        return count + countByMajor(p.left, major) + countByMajor(p.right, major);\r\n    }\r\n\r\n    // Update student balance in BST\r\n    boolean updateStudentBalance(String id, double amount) {\r\n        // Implement this function - Update balance of student with given ID\r\n        // --------------------------------------------------------\r\n        if (search(root, id) != null) {\r\n            search(root, id).info.setBalance(search(root, id).info.getBalance() + amount);\r\n            return true;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return false; // Change this return statement as needed\r\n    }\r\n\r\n    void loadDataStudents(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 1);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 2);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 3);\r\n            double[] balances = Lib.readLineToDoubleArray(\"data.txt\", k + 4);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length, Math.min(gpas.length,\r\n                    Math.min(majors.length, balances.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                insert(ids[i], names[i], gpas[i], majors[i], balances[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading student data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\n// ScholarshipList.java\r\nclass ScholarshipList {\r\n\r\n    ListNode head, tail;\r\n\r\n    ScholarshipList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return head == null;\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void addLast(String id, String name, double gpa, String major, double scholarshipAmount) {\r\n        // Implement this function - add a new student to the end of the linked list\r\n        // --------------------------------------------------------\r\n        ListNode current = head;\r\n        Student x = new Student(id, name, gpa, major, scholarshipAmount);\r\n        if (head == null) {\r\n            head = new ListNode(x);\r\n        } else {\r\n            while (current.next != null) {\r\n                current = current.next;\r\n            }\r\n            current.next = new ListNode(x);\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    ListNode search(String xId) {\r\n        ListNode p = head;\r\n        while (p != null) {\r\n            if (p.info.getId().equals(xId)) {\r\n                return p;\r\n            }\r\n            p = p.next;\r\n        }\r\n        return null;\r\n    }\r\n\r\n    // Remove a student from scholarship list by ID\r\n    ListNode removeById(String id) {\r\n        // Implement this function - Remove student with given ID from list\r\n        // --------------------------------------------------------\r\n        head = head.next;\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return null; // Change this return statement as needed\r\n    }\r\n\r\n    int count() {\r\n        int count = 0;\r\n        ListNode current = head;\r\n        while (current != null) {\r\n            current = current.next;\r\n            count++;\r\n        }\r\n        return count;\r\n    }\r\n\r\n    // Load initial scholarship data from data.txt file (from line k+5)\r\n    void loadInitialScholarship(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k + 5);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 6);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 7);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 8);\r\n            double[] amounts = Lib.readLineToDoubleArray(\"data.txt\", k + 9);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length,\r\n                    Math.min(gpas.length, Math.min(majors.length, amounts.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                addLast(ids[i], names[i], gpas[i], majors[i], amounts[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading initial scholarship data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\nclass StudentManager {\r\n\r\n    StudentBST studentBST = new StudentBST();\r\n    ScholarshipList scholarshipList = new ScholarshipList();\r\n\r\n    StudentManager() {\r\n    }\r\n\r\n    void fvisit(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void preOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        fvisit(p, f);\r\n        preOrder(p.left, f);\r\n        preOrder(p.right, f);\r\n    }\r\n\r\n    void inOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        inOrder(p.left, f);\r\n        fvisit(p, f);\r\n        inOrder(p.right, f);\r\n    }\r\n\r\n    void ftraverse(java.io.RandomAccessFile f) throws Exception {\r\n        f.writeBytes(\"Student BST (Inorder Traversal):\\r\\n\");\r\n        inOrder(studentBST.root, f);\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Scholarship List:\\r\\n\");\r\n        ListNode p = scholarshipList.head;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception {\r\n        studentBST.loadDataStudents(k);\r\n        scholarshipList.loadInitialScholarship(k);\r\n    }\r\n\r\n    void f1() throws Exception {\r\n        // Test insert() function in StudentBST and addLast() function in ScholarshipList\r\n        load(0);\r\n        String fname = \"f1.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        // Find and display student with highest GPA\r\n        load(0);\r\n        String fname = \"f2.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Find student with highest GPA\r\n        // --------------------------------------------------------\r\n        Student highestGPA = studentBST.findHighestGPA();\r\n        f.writeBytes(\"\\r\\nStudent with highest GPA: \" + highestGPA + \"\\r\\n\");\r\n        // --------------------------------------------------------\r\n\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        // Count students with GPA >= 3.5\r\n        load(0);\r\n        String fname = \"f3.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        // Count students with GPA >= 3.5\r\n        // --------------------------------------------------------\r\n        f.writeBytes(String.valueOf(studentBST.countByGPA(3.5)));\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        // Apply scholarship funds to student accounts\r\n        load(0);\r\n        String fname = \"f4.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Process scholarships - remove from list and add to student balance\r\n        // --------------------------------------------------------\r\n        int count = scholarshipList.count();\r\n        for (int i = 0; i < count; i++) {\r\n            if (studentBST.updateStudentBalance(scholarshipList.head.info.getId(), scholarshipList.search(scholarshipList.head.info.getId()).info.getBalance())) {\r\n                scholarshipList.removeById(scholarshipList.head.info.getId());\r\n            }\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n}\r\n",
          "sampleSolution": "\r\nclass StudentBST {\r\n\r\n    TreeNode root;\r\n\r\n    StudentBST() {\r\n        root = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return root == null;\r\n    }\r\n\r\n    void clear() {\r\n        root = null;\r\n    }\r\n\r\n    TreeNode search(TreeNode p, String xId) {\r\n        if (p == null) {\r\n            return null;\r\n        }\r\n        if (p.info.getId().equals(xId)) {\r\n            return p;\r\n        }\r\n        if (xId.compareTo(p.info.getId()) < 0) {\r\n            return search(p.left, xId);\r\n        } else {\r\n            return search(p.right, xId);\r\n        }\r\n    }\r\n\r\n    void insertHelper(TreeNode root, Student students) {\r\n        if (students.getId().charAt(3) < root.info.getId().charAt(3)) {\r\n            if (root.left == null) {\r\n                root.left = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.left, students);\r\n            }\r\n        } else if (students.getId().charAt(3) > root.info.getId().charAt(3)) {\r\n            if (root.right == null) {\r\n                root.right = new TreeNode(students);\r\n            } else {\r\n                insertHelper(root.right, students);\r\n            }\r\n        }\r\n    }\r\n\r\n    void insert(String id, String name, double gpa, String major, double balance) {\r\n        // Implement this function - Insert a new student to the BST\r\n        // If the ID already exists, update the student's information\r\n        // --------------------------------------------------------\r\n        Student x = new Student(id, name, gpa, major, balance);\r\n        if (root == null) {\r\n            root = new TreeNode(x);\r\n        }\r\n        insertHelper(root, x);\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    Student maxGPA(TreeNode root) {\r\n        if (root == null) {\r\n            return null;\r\n        }\r\n\r\n        Student max = root.info;\r\n\r\n        Student leftMax = maxGPA(root.left);\r\n        Student rightMax = maxGPA(root.right);\r\n\r\n        if (leftMax != null && leftMax.getGpa() > max.getGpa()) {\r\n            max = leftMax;\r\n        }\r\n\r\n        if (rightMax != null && rightMax.getGpa() > max.getGpa()) {\r\n            max = rightMax;\r\n        }\r\n\r\n        return max;\r\n    }\r\n\r\n    // Find student with highest GPA\r\n    Student findHighestGPA() {\r\n        // Implement this function - Find student with highest GPA\r\n        // --------------------------------------------------------\r\n\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return maxGPA(root); // Change this return statement as needed\r\n    }\r\n\r\n    int count(TreeNode root, double threshold) {\r\n        if (root == null) {\r\n            return 0;\r\n        }\r\n        if (root.info.getGpa() >= threshold) {\r\n            return count(root.left, 3.5) + count(root.right, 3.5) + 1;\r\n        } else {\r\n            return count(root.left, 3.5) + count(root.right, 3.5);\r\n        }\r\n    }\r\n\r\n    // Count students with GPA greater than or equal to threshold\r\n    int countByGPA(double threshold) {\r\n        // Implement this function - Count students with GPA >= threshold\r\n        // --------------------------------------------------------\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return count(root, 3.5); // Change this return statement as needed\r\n    }\r\n\r\n    // Count students by major\r\n    int countByMajor(String major) {\r\n        return countByMajor(root, major);\r\n    }\r\n\r\n    private int countByMajor(TreeNode p, String major) {\r\n        if (p == null) {\r\n            return 0;\r\n        }\r\n        int count = 0;\r\n        if (p.info.getMajor().equals(major)) {\r\n            count = 1;\r\n        }\r\n\r\n        return count + countByMajor(p.left, major) + countByMajor(p.right, major);\r\n    }\r\n\r\n    // Update student balance in BST\r\n    boolean updateStudentBalance(String id, double amount) {\r\n        // Implement this function - Update balance of student with given ID\r\n        // --------------------------------------------------------\r\n        if (search(root, id) != null) {\r\n            search(root, id).info.setBalance(search(root, id).info.getBalance() + amount);\r\n            return true;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return false; // Change this return statement as needed\r\n    }\r\n\r\n    void loadDataStudents(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 1);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 2);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 3);\r\n            double[] balances = Lib.readLineToDoubleArray(\"data.txt\", k + 4);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length, Math.min(gpas.length,\r\n                    Math.min(majors.length, balances.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                insert(ids[i], names[i], gpas[i], majors[i], balances[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading student data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\n// ScholarshipList.java\r\nclass ScholarshipList {\r\n\r\n    ListNode head, tail;\r\n\r\n    ScholarshipList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return head == null;\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void addLast(String id, String name, double gpa, String major, double scholarshipAmount) {\r\n        // Implement this function - add a new student to the end of the linked list\r\n        // --------------------------------------------------------\r\n        ListNode current = head;\r\n        Student x = new Student(id, name, gpa, major, scholarshipAmount);\r\n        if (head == null) {\r\n            head = new ListNode(x);\r\n        } else {\r\n            while (current.next != null) {\r\n                current = current.next;\r\n            }\r\n            current.next = new ListNode(x);\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n    }\r\n\r\n    ListNode search(String xId) {\r\n        ListNode p = head;\r\n        while (p != null) {\r\n            if (p.info.getId().equals(xId)) {\r\n                return p;\r\n            }\r\n            p = p.next;\r\n        }\r\n        return null;\r\n    }\r\n\r\n    // Remove a student from scholarship list by ID\r\n    ListNode removeById(String id) {\r\n        // Implement this function - Remove student with given ID from list\r\n        // --------------------------------------------------------\r\n        if (head == null) return null;\r\n        if (head.info.getId().equals(id)) {\r\n            ListNode removed = head;\r\n            head = head.next;\r\n            if (head == null) tail = null;\r\n            return removed;\r\n        }\r\n        ListNode prev = head;\r\n        ListNode cur = head.next;\r\n        while (cur != null) {\r\n            if (cur.info.getId().equals(id)) {\r\n                prev.next = cur.next;\r\n                if (cur == tail) tail = prev;\r\n                return cur;\r\n            }\r\n            prev = cur;\r\n            cur = cur.next;\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        return null; // Change this return statement as needed\r\n    }\r\n\r\n    int count() {\r\n        int count = 0;\r\n        ListNode current = head;\r\n        while (current != null) {\r\n            current = current.next;\r\n            count++;\r\n        }\r\n        return count;\r\n    }\r\n\r\n    // Load initial scholarship data from data.txt file (from line k+5)\r\n    void loadInitialScholarship(int k) {\r\n        try {\r\n            String[] ids = Lib.readLineToStrArray(\"data.txt\", k + 5);\r\n            String[] names = Lib.readLineToStrArray(\"data.txt\", k + 6);\r\n            double[] gpas = Lib.readLineToDoubleArray(\"data.txt\", k + 7);\r\n            String[] majors = Lib.readLineToStrArray(\"data.txt\", k + 8);\r\n            double[] amounts = Lib.readLineToDoubleArray(\"data.txt\", k + 9);\r\n\r\n            int n = Math.min(ids.length, Math.min(names.length,\r\n                    Math.min(gpas.length, Math.min(majors.length, amounts.length))));\r\n\r\n            for (int i = 0; i < n; i++) {\r\n                addLast(ids[i], names[i], gpas[i], majors[i], amounts[i]);\r\n            }\r\n        } catch (Exception e) {\r\n            System.out.println(\"Error loading initial scholarship data: \" + e);\r\n        }\r\n    }\r\n}\r\n\r\nclass StudentManager {\r\n\r\n    StudentBST studentBST = new StudentBST();\r\n    ScholarshipList scholarshipList = new ScholarshipList();\r\n\r\n    StudentManager() {\r\n    }\r\n\r\n    void fvisit(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void preOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        fvisit(p, f);\r\n        preOrder(p.left, f);\r\n        preOrder(p.right, f);\r\n    }\r\n\r\n    void inOrder(TreeNode p, java.io.RandomAccessFile f) throws Exception {\r\n        if (p == null) {\r\n            return;\r\n        }\r\n        inOrder(p.left, f);\r\n        fvisit(p, f);\r\n        inOrder(p.right, f);\r\n    }\r\n\r\n    void ftraverse(java.io.RandomAccessFile f) throws Exception {\r\n        f.writeBytes(\"Student BST (Inorder Traversal):\\r\\n\");\r\n        inOrder(studentBST.root, f);\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Scholarship List:\\r\\n\");\r\n        ListNode p = scholarshipList.head;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception {\r\n        studentBST.loadDataStudents(k);\r\n        scholarshipList.loadInitialScholarship(k);\r\n    }\r\n\r\n    void f1() throws Exception {\r\n        // Test insert() function in StudentBST and addLast() function in ScholarshipList\r\n        load(0);\r\n        String fname = \"f1.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        // Find and display student with highest GPA\r\n        load(0);\r\n        String fname = \"f2.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Find student with highest GPA\r\n        // --------------------------------------------------------\r\n        Student highestGPA = studentBST.findHighestGPA();\r\n        f.writeBytes(\"\\r\\nStudent with highest GPA: \" + highestGPA + \"\\r\\n\");\r\n        // --------------------------------------------------------\r\n\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        // Count students with GPA >= 3.5\r\n        load(0);\r\n        String fname = \"f3.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        // Count students with GPA >= 3.5\r\n        // --------------------------------------------------------\r\n        f.writeBytes(String.valueOf(studentBST.countByGPA(3.5)));\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        // Apply scholarship funds to student accounts\r\n        load(0);\r\n        String fname = \"f4.txt\";\r\n        java.io.File g123 = new java.io.File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        java.io.RandomAccessFile f = new java.io.RandomAccessFile(fname, \"rw\");\r\n\r\n        ftraverse(f);\r\n\r\n        // Process scholarships - remove from list and add to student balance\r\n        // --------------------------------------------------------\r\n        int count = scholarshipList.count();\r\n        for (int i = 0; i < count; i++) {\r\n            if (studentBST.updateStudentBalance(scholarshipList.head.info.getId(), scholarshipList.search(scholarshipList.head.info.getId()).info.getBalance())) {\r\n                scholarshipList.removeById(scholarshipList.head.info.getId());\r\n            }\r\n        }\r\n        // YOUR CODE HERE\r\n        // --------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n}\r\n",
          "expectedOutput": "Before:\nStudent BST (Inorder Traversal):\n(S001,John,3.8,CS,100.0) (S002,Mary,3.5,Business,150.0) (S003,David,3.9,CS,200.0) (S004,Sarah,3.2,IT,120.0) (S005,Michael,3.7,Engineering,180.0)\nScholarship List:\n(S003,David,3.9,CS,500.0) (S001,John,3.8,CS,600.0) (S005,Michael,3.7,Engineering,450.0)\nAfter:\nStudent BST (Inorder Traversal):\n(S001,John,3.8,CS,700.0) (S002,Mary,3.5,Business,150.0) (S003,David,3.9,CS,700.0) (S004,Sarah,3.2,IT,120.0) (S005,Michael,3.7,Engineering,630.0)\nScholarship List:\nEmpty",
          "explanation": "<p>Verified by compiling against the real given project and running choice 4 — output matches the paper's expected example byte-for-byte. <strong>Note:</strong> the downloaded given.zip already ships a working <code>updateStudentBalance()</code>, and a <code>removeById()</code> stub that unconditionally does <code>head = head.next</code> and returns null regardless of the <code>id</code> argument — this happens to pass THIS specific example only because <code>f4()</code>'s loop always removes whichever student is currently at <code>head</code> (by construction, using that node's own id), so a naive \"remove the head\" coincidentally matches. It is not a correct general <code>removeById(id)</code> as the exam explicitly requires (\"remove a student from the scholarship list by ID\"), so it was replaced with a proper search-and-unlink implementation that finds the matching id anywhere in the list (head or interior), which produces the identical verified output here and is also correct for arbitrary grading test cases that may not always target the head.</p>|||<p>Đã kiểm bằng cách biên dịch cùng project given thật và chạy lựa chọn 4 — output khớp byte-for-byte với ví dụ trong đề. <strong>Lưu ý:</strong> given.zip tải về đã có sẵn <code>updateStudentBalance()</code> hoạt động đúng, và một bản <code>removeById()</code> luôn làm <code>head = head.next</code> và trả về null bất kể tham số <code>id</code> — bản này chỉ QUA được đúng ví dụ này vì vòng lặp trong <code>f4()</code> luôn xoá đúng sinh viên đang ở <code>head</code> (dùng chính id của node đó), nên \"xoá head\" trùng hợp khớp. Đây KHÔNG phải cài đặt <code>removeById(id)</code> tổng quát đúng như đề yêu cầu (\"xoá sinh viên khỏi danh sách học bổng theo ID\"), nên đã thay bằng cài đặt tìm-và-gỡ đúng, tìm id khớp ở bất kỳ đâu trong danh sách (head hay giữa), cho ra đúng output đã verify ở đây và cũng đúng với các test chấm thật có thể không luôn nhắm vào head.</p>",
          "rubric": [
            {
              "id": "remove_by_id",
              "criterion": "removeById() correctly finds and unlinks the matching id anywhere in the list, not only at the head.|||removeById() tìm và gỡ đúng id khớp ở bất kỳ đâu trong danh sách, không chỉ ở head.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "update_balance",
              "criterion": "updateStudentBalance() correctly adds the scholarship amount to the matching student's balance in the BST.|||updateStudentBalance() cộng đúng số tiền học bổng vào balance của sinh viên khớp trong BST.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        }
      ]
    }
  ]
};
