export default {
  "course": {
    "courseCode": "CSD201"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "CODE",
      "code": "PE16-FA24B5PE",
      "title": "PE Đề 16 — Practical Exam (FA24 - Block 5)|||PE Đề 16 — Thi thực hành (FA24 - Block 5)",
      "description": "CSD201 PE (CODE): singly linked list + BST (depth-keyed, custom \"rotate\") + Graph (directed, DFS+Dijkstra), AI-graded, 10 câu 1đ.|||PE CSD201 (viết mã): danh sách liên kết đơn + BST (khoá depth, \"rotate\" tự định nghĩa) + Đồ thị (có hướng, DFS+Dijkstra), chấm AI, 10 câu 1đ.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "attachmentUrl": "https://media.cuongthai.com/files/exam-attachments/CSD201/PE16-Given.zip",
      "attachmentName": "CSD201-PE16-Given.zip",
      "instructions": "<div class=\"ml-en\"><p><strong>CSD201 PE INSTRUCTIONS</strong> (transcribed from the real exam paper, \"FA24 - Block 5 - PE\").</p>\n   <ol>\n     <li>Do not pay attention to the real meaning of objects, variables and their values in the questions below.</li>\n     <li>Each of the f1()..f4() methods below is graded independently (1 mark each). Your job is to complete only the method bodies. Do not change method signatures or add new import statements.</li>\n     <li>This exam room cannot run NetBeans/JDK in the browser, so type your code directly into the code box — it is graded by AI against the rubric. You can build &amp; run the real given project on your own machine and cross-check against the sample runs shown below.</li>\n   </ol></div><div class=\"ml-vi\"><p><strong>HƯỚNG DẪN PE CSD201</strong> (dịch nguyên văn từ đề thi thật, \"FA24 - Block 5 - PE\").</p>\n   <ol>\n     <li>Không cần để ý ý nghĩa thực của đối tượng/biến/giá trị trong các câu dưới đây.</li>\n     <li>Mỗi hàm f1()..f4() dưới đây được chấm ĐỘC LẬP (1 điểm mỗi hàm). Chỉ hoàn thiện phần thân method. Không đổi chữ ký method hay thêm import mới.</li>\n     <li>Phòng thi web không chạy được NetBeans/JDK trực tiếp nên gõ thẳng mã vào ô, được AI chấm theo tiêu chí — bạn có thể build &amp; chạy project given thật trên máy mình rồi đối chiếu với mẫu chạy bên dưới.</li>\n   </ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 1 (4 marks, file MyList.java).</strong> The class <code>Pen</code> with 3 data members: owner, price and color is given (do not edit it). <code>MyList</code> is a singly linked list of Pen objects.</p>|||<p><strong>Câu 1 (4 điểm, file MyList.java).</strong> Lớp <code>Pen</code> (owner, price, color) đã có sẵn (không sửa). <code>MyList</code> là danh sách liên kết đơn các Pen.</p><p><strong>void addLast(String xOwner, int xPrice, int xColor)</strong> — if <code>xOwner.charAt(0) == 'A'</code> do nothing, otherwise add a new node with owner=xOwner, price=xPrice, color=xColor to the end of the list (price and color can be arbitrary, even negative values).</p>\n     <p><strong>void f1()</strong> — do not edit. Your task is to complete addLast() above only.</p>|||<p><strong>void addLast(String xOwner, int xPrice, int xColor)</strong> — nếu <code>xOwner.charAt(0) == 'A'</code> thì không làm gì, ngược lại thêm node mới (owner=xOwner, price=xPrice, color=xColor) vào cuối danh sách.</p>\n     <p><strong>void f1()</strong> — không sửa. Chỉ hoàn thiện addLast() ở trên.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xOwner, int xPrice, int xColor) {\r\n    //You should write here appropriate statements to complete this function.\r\n\r\n\r\n\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Pen x, y;\r\n     x = new Pen(\"X\",1,2);\r\n     y = new Pen(\"Y\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xOwner, int xPrice, int xColor) {\r\n    //You should write here appropriate statements to complete this function.\r\n    if (xOwner.charAt(0) == 'A') return;\r\n    Pen p = new Pen(xOwner, xPrice, xColor);\r\n    Node node = new Node(p);\r\n    if (head == null) { head = tail = node; }\r\n    else { tail.next = node; tail = node; }\r\n   }\r\n\r\n  void insertAtPosition(Pen p, int pos) {\r\n    Node node = new Node(p);\r\n    if (pos == 0) {\r\n      node.next = head; head = node;\r\n      if (tail == null) tail = node;\r\n      return;\r\n    }\r\n    Node prev = head;\r\n    for (int i = 0; i < pos - 1; i++) prev = prev.next;\r\n    node.next = prev.next;\r\n    prev.next = node;\r\n    if (node.next == null) tail = node;\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Pen x, y;\r\n     x = new Pen(\"X\",1,2);\r\n     y = new Pen(\"Y\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n     insertAtPosition(y, 0);\r\n     insertAtPosition(x, 1);\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n    Node prev = null, target = null, targetPrev = null;\r\n    for (Node t = head; t != null; t = t.next) {\r\n      if (t.info.color < 6) { target = t; targetPrev = prev; break; }\r\n      prev = t;\r\n    }\r\n    if (target != null) {\r\n      if (targetPrev == null) head = target.next;\r\n      else targetPrev.next = target.next;\r\n      if (target == tail) tail = targetPrev;\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n    java.util.ArrayList<Node> first5 = new java.util.ArrayList<Node>();\r\n    Node t = head;\r\n    for (int i = 0; i < 5 && t != null; i++) { first5.add(t); t = t.next; }\r\n    java.util.ArrayList<Pen> vals = new java.util.ArrayList<Pen>();\r\n    for (Node n : first5) vals.add(n.info);\r\n    vals.sort((a, b) -> a.color - b.color);\r\n    for (int i = 0; i < first5.size(); i++) first5.get(i).info = vals.get(i);\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "expectedOutput": "(B,5,3) (C,6,5) (D,2,4) (E,7,9) (F,4,7)\n[real given data.txt gives F's color=7, not the \"-7\" printed in the paper's own illustration — see explanation]",
          "explanation": "<p>Verified by compiling against the real given project and running choice 1. Skips owner starting with 'A', otherwise appends at <code>tail</code> — this straightforward rule is confirmed self-consistent (every other output byte matches the paper exactly). The paper's own illustrative example prints F's color as -7, but the real data.txt bundled with THIS specific paper variant gives F color=7 (not -7); this exact same \"-7\" text also appears verbatim in a completely different CSD201 paper (Đề 13) whose own bundled data.txt DOES have F=-7 — strong evidence this paper's f1 illustration was copy-pasted from that other paper's template and never updated for this variant's actual data, a known pattern in these crowd-sourced FUOverflow archives. The real given code and data were trusted over the stale illustration.</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 1. Bỏ qua owner bắt đầu bằng 'A', ngược lại thêm vào <code>tail</code> — luật đơn giản này tự nhất quán (mọi byte output khác đều khớp đề chính xác). Minh hoạ của chính đề in màu F là -7, nhưng data.txt thật đi kèm ĐỀ NÀY cho F color=7 (không phải -7); đúng chuỗi \"-7\" này cũng xuất hiện y hệt ở một đề CSD201 hoàn toàn khác (Đề 13) mà data.txt đi kèm đề đó THẬT SỰ có F=-7 — bằng chứng mạnh rằng minh hoạ f1 của đề này bị copy nguyên từ mẫu đề kia và chưa cập nhật theo dữ liệu thật của biến thể này, một kiểu lỗi đã biết trong các đề crowd-source trên FUOverflow. Tin theo mã given và dữ liệu thật hơn minh hoạ cũ.</p>",
          "rubric": [
            {
              "id": "correct",
              "criterion": "addLast() correctly skips owner starting with A and appends at tail otherwise.|||addLast() bỏ qua đúng owner bắt đầu bằng A, ngược lại thêm vào cuối.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 1 (4 marks, file MyList.java).</strong> The class <code>Pen</code> with 3 data members: owner, price and color is given (do not edit it). <code>MyList</code> is a singly linked list of Pen objects.</p>|||<p><strong>Câu 1 (4 điểm, file MyList.java).</strong> Lớp <code>Pen</code> (owner, price, color) đã có sẵn (không sửa). <code>MyList</code> là danh sách liên kết đơn các Pen.</p><p><strong>void f2()</strong> — 2 given Pen objects x and y. Assume the list contains at least 2 elements. Insert x and y to the list so that y will be at position 0 (head) and x will be at position 1.</p>|||<p><strong>void f2()</strong> — cho 2 đối tượng Pen x, y. Giả sử danh sách có ít nhất 2 phần tử. Chèn x, y sao cho y ở vị trí 0 (head) và x ở vị trí 1.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xOwner, int xPrice, int xColor) {\r\n    //You should write here appropriate statements to complete this function.\r\n\r\n\r\n\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Pen x, y;\r\n     x = new Pen(\"X\",1,2);\r\n     y = new Pen(\"Y\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xOwner, int xPrice, int xColor) {\r\n    //You should write here appropriate statements to complete this function.\r\n    if (xOwner.charAt(0) == 'A') return;\r\n    Pen p = new Pen(xOwner, xPrice, xColor);\r\n    Node node = new Node(p);\r\n    if (head == null) { head = tail = node; }\r\n    else { tail.next = node; tail = node; }\r\n   }\r\n\r\n  void insertAtPosition(Pen p, int pos) {\r\n    Node node = new Node(p);\r\n    if (pos == 0) {\r\n      node.next = head; head = node;\r\n      if (tail == null) tail = node;\r\n      return;\r\n    }\r\n    Node prev = head;\r\n    for (int i = 0; i < pos - 1; i++) prev = prev.next;\r\n    node.next = prev.next;\r\n    prev.next = node;\r\n    if (node.next == null) tail = node;\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Pen x, y;\r\n     x = new Pen(\"X\",1,2);\r\n     y = new Pen(\"Y\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n     insertAtPosition(y, 0);\r\n     insertAtPosition(x, 1);\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n    Node prev = null, target = null, targetPrev = null;\r\n    for (Node t = head; t != null; t = t.next) {\r\n      if (t.info.color < 6) { target = t; targetPrev = prev; break; }\r\n      prev = t;\r\n    }\r\n    if (target != null) {\r\n      if (targetPrev == null) head = target.next;\r\n      else targetPrev.next = target.next;\r\n      if (target == tail) tail = targetPrev;\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n    java.util.ArrayList<Node> first5 = new java.util.ArrayList<Node>();\r\n    Node t = head;\r\n    for (int i = 0; i < 5 && t != null; i++) { first5.add(t); t = t.next; }\r\n    java.util.ArrayList<Pen> vals = new java.util.ArrayList<Pen>();\r\n    for (Node n : first5) vals.add(n.info);\r\n    vals.sort((a, b) -> a.color - b.color);\r\n    for (int i = 0; i < first5.size(); i++) first5.get(i).info = vals.get(i);\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "expectedOutput": "(C,9,8) (D,6,3) (E,8,5) (F,5,4) (I,4,9)\n(Y,3,4) (X,1,2) (C,9,8) (D,6,3) (E,8,5) (F,5,4) (I,4,9)",
          "explanation": "<p>Verified by compiling against the real given project and running choice 2 — matches the paper's expected example byte-for-byte. Insert y at position 0 first (becomes new head), then x at position 1.</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 2 — khớp byte-for-byte với ví dụ trong đề. Chèn y ở vị trí 0 trước (thành head mới), rồi x ở vị trí 1.</p>",
          "rubric": [
            {
              "id": "correct",
              "criterion": "Correctly inserts y at head and x at position 1, in that order.|||Chèn đúng y ở head và x ở vị trí 1, theo đúng thứ tự đó.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 1 (4 marks, file MyList.java).</strong> The class <code>Pen</code> with 3 data members: owner, price and color is given (do not edit it). <code>MyList</code> is a singly linked list of Pen objects.</p>|||<p><strong>Câu 1 (4 điểm, file MyList.java).</strong> Lớp <code>Pen</code> (owner, price, color) đã có sẵn (không sửa). <code>MyList</code> là danh sách liên kết đơn các Pen.</p><p><strong>void f3()</strong> — Assume the list is not empty. Remove the first node having color &lt; 6.</p>|||<p><strong>void f3()</strong> — Giả sử danh sách không rỗng. Xoá node đầu tiên có color &lt; 6.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xOwner, int xPrice, int xColor) {\r\n    //You should write here appropriate statements to complete this function.\r\n\r\n\r\n\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Pen x, y;\r\n     x = new Pen(\"X\",1,2);\r\n     y = new Pen(\"Y\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xOwner, int xPrice, int xColor) {\r\n    //You should write here appropriate statements to complete this function.\r\n    if (xOwner.charAt(0) == 'A') return;\r\n    Pen p = new Pen(xOwner, xPrice, xColor);\r\n    Node node = new Node(p);\r\n    if (head == null) { head = tail = node; }\r\n    else { tail.next = node; tail = node; }\r\n   }\r\n\r\n  void insertAtPosition(Pen p, int pos) {\r\n    Node node = new Node(p);\r\n    if (pos == 0) {\r\n      node.next = head; head = node;\r\n      if (tail == null) tail = node;\r\n      return;\r\n    }\r\n    Node prev = head;\r\n    for (int i = 0; i < pos - 1; i++) prev = prev.next;\r\n    node.next = prev.next;\r\n    prev.next = node;\r\n    if (node.next == null) tail = node;\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Pen x, y;\r\n     x = new Pen(\"X\",1,2);\r\n     y = new Pen(\"Y\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n     insertAtPosition(y, 0);\r\n     insertAtPosition(x, 1);\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n    Node prev = null, target = null, targetPrev = null;\r\n    for (Node t = head; t != null; t = t.next) {\r\n      if (t.info.color < 6) { target = t; targetPrev = prev; break; }\r\n      prev = t;\r\n    }\r\n    if (target != null) {\r\n      if (targetPrev == null) head = target.next;\r\n      else targetPrev.next = target.next;\r\n      if (target == tail) tail = targetPrev;\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n    java.util.ArrayList<Node> first5 = new java.util.ArrayList<Node>();\r\n    Node t = head;\r\n    for (int i = 0; i < 5 && t != null; i++) { first5.add(t); t = t.next; }\r\n    java.util.ArrayList<Pen> vals = new java.util.ArrayList<Pen>();\r\n    for (Node n : first5) vals.add(n.info);\r\n    vals.sort((a, b) -> a.color - b.color);\r\n    for (int i = 0; i < first5.size(); i++) first5.get(i).info = vals.get(i);\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "expectedOutput": "(C,8,6) (D,6,7) (E,9,2) (F,5,8) (G,9,7) (H,6,8) (I,7,3)\n(C,8,6) (D,6,7) (F,5,8) (G,9,7) (H,6,8) (I,7,3)",
          "explanation": "<p>Verified by compiling against the real given project and running choice 3 — matches the paper's expected example byte-for-byte. Walks the list once, unlinking the first node with color &lt; 6 (E, color=2).</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 3 — khớp byte-for-byte với ví dụ trong đề. Duyệt 1 lượt, gỡ node đầu tiên có color &lt; 6 (E, color=2).</p>",
          "rubric": [
            {
              "id": "correct",
              "criterion": "Correctly finds and removes the first node with color strictly less than 6.|||Tìm và xoá đúng node đầu tiên có color nhỏ hơn 6.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 1 (4 marks, file MyList.java).</strong> The class <code>Pen</code> with 3 data members: owner, price and color is given (do not edit it). <code>MyList</code> is a singly linked list of Pen objects.</p>|||<p><strong>Câu 1 (4 điểm, file MyList.java).</strong> Lớp <code>Pen</code> (owner, price, color) đã có sẵn (không sửa). <code>MyList</code> là danh sách liên kết đơn các Pen.</p><p><strong>void f4()</strong> — Assume the list contains at least 5 elements. Sort the first 5 elements ascendingly by color.</p>|||<p><strong>void f4()</strong> — Giả sử danh sách có ít nhất 5 phần tử. Sắp 5 phần tử đầu tăng dần theo color.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xOwner, int xPrice, int xColor) {\r\n    //You should write here appropriate statements to complete this function.\r\n\r\n\r\n\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Pen x, y;\r\n     x = new Pen(\"X\",1,2);\r\n     y = new Pen(\"Y\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xOwner, int xPrice, int xColor) {\r\n    //You should write here appropriate statements to complete this function.\r\n    if (xOwner.charAt(0) == 'A') return;\r\n    Pen p = new Pen(xOwner, xPrice, xColor);\r\n    Node node = new Node(p);\r\n    if (head == null) { head = tail = node; }\r\n    else { tail.next = node; tail = node; }\r\n   }\r\n\r\n  void insertAtPosition(Pen p, int pos) {\r\n    Node node = new Node(p);\r\n    if (pos == 0) {\r\n      node.next = head; head = node;\r\n      if (tail == null) tail = node;\r\n      return;\r\n    }\r\n    Node prev = head;\r\n    for (int i = 0; i < pos - 1; i++) prev = prev.next;\r\n    node.next = prev.next;\r\n    prev.next = node;\r\n    if (node.next == null) tail = node;\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Pen x, y;\r\n     x = new Pen(\"X\",1,2);\r\n     y = new Pen(\"Y\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n     insertAtPosition(y, 0);\r\n     insertAtPosition(x, 1);\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n    Node prev = null, target = null, targetPrev = null;\r\n    for (Node t = head; t != null; t = t.next) {\r\n      if (t.info.color < 6) { target = t; targetPrev = prev; break; }\r\n      prev = t;\r\n    }\r\n    if (target != null) {\r\n      if (targetPrev == null) head = target.next;\r\n      else targetPrev.next = target.next;\r\n      if (target == tail) tail = targetPrev;\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n    java.util.ArrayList<Node> first5 = new java.util.ArrayList<Node>();\r\n    Node t = head;\r\n    for (int i = 0; i < 5 && t != null; i++) { first5.add(t); t = t.next; }\r\n    java.util.ArrayList<Pen> vals = new java.util.ArrayList<Pen>();\r\n    for (Node n : first5) vals.add(n.info);\r\n    vals.sort((a, b) -> a.color - b.color);\r\n    for (int i = 0; i < first5.size(); i++) first5.get(i).info = vals.get(i);\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "expectedOutput": "(C,7,9) (D,16,7) (E,6,16) (F,5,6) (I,4,5) (J,3,4) (K,2,3)\n(I,4,5) (F,5,6) (D,16,7) (C,7,9) (E,6,16) (J,3,4) (K,2,3)",
          "explanation": "<p>Verified by compiling against the real given project and running choice 4 — matches the paper's expected example byte-for-byte. Extracts the first 5 nodes' data into a list, sorts by color, writes back in place (node identities/positions unchanged, only their data reassigned).</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 4 — khớp byte-for-byte với ví dụ trong đề. Rút dữ liệu 5 node đầu ra danh sách, sắp theo color, ghi ngược lại tại chỗ (giữ nguyên vị trí/danh tính node, chỉ đổi dữ liệu).</p>",
          "rubric": [
            {
              "id": "correct",
              "criterion": "Correctly sorts exactly the first 5 elements ascending by color, leaving the rest untouched.|||Sắp đúng 5 phần tử đầu tăng dần theo color, giữ nguyên phần còn lại.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 2 (4 marks, file BSTree.java).</strong> The class <code>Beaver</code> with 3 data members: place, depth and type is given (do not edit it). <code>BSTree</code> is a binary search tree of Beaver objects. <strong>The variable depth is the key of the tree, thus it must be unique.</strong></p>|||<p><strong>Câu 2 (4 điểm, file BSTree.java).</strong> Lớp <code>Beaver</code> (place, depth, type) đã có sẵn (không sửa). <code>BSTree</code> là cây BST các Beaver. <strong>Biến depth là khoá của cây, phải duy nhất.</strong></p><p><strong>void insert(String xPlace, int xDepth, int xType)</strong> — if <code>xPlace.charAt(0) == 'B'</code> do nothing, otherwise insert a new Beaver object into the tree (depth and type can be arbitrary, even negative values).</p>\n     <p><strong>void f1()</strong> — do not edit. Your task is to complete insert() above only.</p>|||<p><strong>void insert(String xPlace, int xDepth, int xType)</strong> — nếu <code>xPlace.charAt(0) == 'B'</code> thì không làm gì, ngược lại chèn Beaver mới vào cây.</p>\n     <p><strong>void f1()</strong> — không sửa. Chỉ hoàn thiện insert() ở trên.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xPlace, int xDepth, int xType) {\r\n    //You should insert here statements to complete this function\r\n\r\n\r\n\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n  void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    postOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n \r\n\r\n     \r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n }\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xPlace, int xDepth, int xType) {\r\n    //You should insert here statements to complete this function\r\n    if (xPlace.charAt(0) == 'B') return;\r\n    Beaver b = new Beaver(xPlace, xDepth, xType);\r\n    root = insertRec(root, b);\r\n   }\r\n\r\n  Node insertRec(Node node, Beaver b) {\r\n    if (node == null) return new Node(b);\r\n    if (b.depth < node.info.depth) node.left = insertRec(node.left, b);\r\n    else if (b.depth > node.info.depth) node.right = insertRec(node.right, b);\r\n    return node;\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n  void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    postOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    postOrder2(root, f);\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  void postOrder2(Node p, RandomAccessFile f) throws Exception {\r\n    if (p == null) return;\r\n    postOrder2(p.left, f);\r\n    postOrder2(p.right, f);\r\n    if (p.info.type < 7) fvisit(p, f);\r\n   }\r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    Node p = findNthWithLeftSonBFS(2);\r\n    if (p != null) {\r\n      Node maxNode = findMaxDepthInSubtree(p);\r\n      root = deleteByDepth(root, maxNode.info.depth);\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  Node findNthWithLeftSonBFS(int nth) {\r\n    if (root == null) return null;\r\n    Queue q = new Queue();\r\n    q.enqueue(root);\r\n    int count = 0;\r\n    while (!q.isEmpty()) {\r\n      Node r = (Node) q.dequeue();\r\n      if (r.left != null) {\r\n        count++;\r\n        if (count == nth) return r;\r\n      }\r\n      if (r.left != null) q.enqueue(r.left);\r\n      if (r.right != null) q.enqueue(r.right);\r\n    }\r\n    return null;\r\n   }\r\n\r\n  Node findMaxDepthInSubtree(Node p) {\r\n    if (p == null) return null;\r\n    Node max = p;\r\n    Node leftMax = findMaxDepthInSubtree(p.left);\r\n    Node rightMax = findMaxDepthInSubtree(p.right);\r\n    if (leftMax != null && leftMax.info.depth > max.info.depth) max = leftMax;\r\n    if (rightMax != null && rightMax.info.depth > max.info.depth) max = rightMax;\r\n    return max;\r\n   }\r\n\r\n  Node deleteByDepth(Node node, int depthKey) {\r\n    if (node == null) return null;\r\n    if (depthKey < node.info.depth) {\r\n      node.left = deleteByDepth(node.left, depthKey);\r\n    } else if (depthKey > node.info.depth) {\r\n      node.right = deleteByDepth(node.right, depthKey);\r\n    } else {\r\n      if (node.left == null) return node.right;\r\n      if (node.right == null) return node.left;\r\n      Node pred = node.left;\r\n      while (pred.right != null) pred = pred.right;\r\n      node.info = pred.info;\r\n      node.left = deleteByDepth(node.left, pred.info.depth);\r\n    }\r\n    return node;\r\n   }\r\n\r\n//=============================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    Node p = findNthWithLeftSonBFS(2);\r\n    if (p != null) {\r\n      Beaver tmp = p.info;\r\n      p.info = p.left.info;\r\n      p.left.info = tmp;\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n }\r\n",
          "expectedOutput": "(A,7,9) (C,4,3) (D,8,6) (E,2,5) (F,6,7)\n(E,2,5) (C,4,3) (F,6,7) (A,7,9) (D,8,6)",
          "explanation": "<p>Verified by compiling against the real given project and running choice 1 — matches the paper's expected example byte-for-byte on BOTH lines (breadth-first AND the given, unmodified inOrder — unlike a similar deck this session, this paper's inOrder line is internally consistent/monotonic). Standard recursive BST insert comparing <code>depth</code>, skipping any place starting with 'B'; a duplicate depth (from place \"X\", depth=4, colliding with \"C\"'s depth=4) is naturally excluded.</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 1 — khớp byte-for-byte với ví dụ trong đề ở CẢ 2 dòng (breadth-first VÀ inOrder given không sửa — khác với 1 đề tương tự trong phiên này, dòng inOrder của đề này tự nhất quán/đơn điệu). Chèn BST đệ quy chuẩn theo <code>depth</code>, bỏ qua place bắt đầu bằng 'B'; depth trùng (place \"X\", depth=4, trùng \"C\") tự động bị loại.</p>",
          "rubric": [
            {
              "id": "correct",
              "criterion": "Correctly inserts following BST rules keyed by depth, skipping place B and duplicate depths.|||Chèn đúng theo luật BST khoá theo depth, bỏ qua place B và depth trùng.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 2 (4 marks, file BSTree.java).</strong> The class <code>Beaver</code> with 3 data members: place, depth and type is given (do not edit it). <code>BSTree</code> is a binary search tree of Beaver objects. <strong>The variable depth is the key of the tree, thus it must be unique.</strong></p>|||<p><strong>Câu 2 (4 điểm, file BSTree.java).</strong> Lớp <code>Beaver</code> (place, depth, type) đã có sẵn (không sửa). <code>BSTree</code> là cây BST các Beaver. <strong>Biến depth là khoá của cây, phải duy nhất.</strong></p><p><strong>void f2()</strong> — Perform post-order traversal from the root but display to f2.txt nodes with type &lt; 7 only. Hint: copy postOrder(...) to postOrder2(...) and modify it.</p>|||<p><strong>void f2()</strong> — Duyệt post-order từ root nhưng chỉ hiển thị ra f2.txt các node có type &lt; 7. Gợi ý: sao chép postOrder(...) thành postOrder2(...) rồi sửa.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xPlace, int xDepth, int xType) {\r\n    //You should insert here statements to complete this function\r\n\r\n\r\n\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n  void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    postOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n \r\n\r\n     \r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n }\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xPlace, int xDepth, int xType) {\r\n    //You should insert here statements to complete this function\r\n    if (xPlace.charAt(0) == 'B') return;\r\n    Beaver b = new Beaver(xPlace, xDepth, xType);\r\n    root = insertRec(root, b);\r\n   }\r\n\r\n  Node insertRec(Node node, Beaver b) {\r\n    if (node == null) return new Node(b);\r\n    if (b.depth < node.info.depth) node.left = insertRec(node.left, b);\r\n    else if (b.depth > node.info.depth) node.right = insertRec(node.right, b);\r\n    return node;\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n  void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    postOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    postOrder2(root, f);\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  void postOrder2(Node p, RandomAccessFile f) throws Exception {\r\n    if (p == null) return;\r\n    postOrder2(p.left, f);\r\n    postOrder2(p.right, f);\r\n    if (p.info.type < 7) fvisit(p, f);\r\n   }\r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    Node p = findNthWithLeftSonBFS(2);\r\n    if (p != null) {\r\n      Node maxNode = findMaxDepthInSubtree(p);\r\n      root = deleteByDepth(root, maxNode.info.depth);\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  Node findNthWithLeftSonBFS(int nth) {\r\n    if (root == null) return null;\r\n    Queue q = new Queue();\r\n    q.enqueue(root);\r\n    int count = 0;\r\n    while (!q.isEmpty()) {\r\n      Node r = (Node) q.dequeue();\r\n      if (r.left != null) {\r\n        count++;\r\n        if (count == nth) return r;\r\n      }\r\n      if (r.left != null) q.enqueue(r.left);\r\n      if (r.right != null) q.enqueue(r.right);\r\n    }\r\n    return null;\r\n   }\r\n\r\n  Node findMaxDepthInSubtree(Node p) {\r\n    if (p == null) return null;\r\n    Node max = p;\r\n    Node leftMax = findMaxDepthInSubtree(p.left);\r\n    Node rightMax = findMaxDepthInSubtree(p.right);\r\n    if (leftMax != null && leftMax.info.depth > max.info.depth) max = leftMax;\r\n    if (rightMax != null && rightMax.info.depth > max.info.depth) max = rightMax;\r\n    return max;\r\n   }\r\n\r\n  Node deleteByDepth(Node node, int depthKey) {\r\n    if (node == null) return null;\r\n    if (depthKey < node.info.depth) {\r\n      node.left = deleteByDepth(node.left, depthKey);\r\n    } else if (depthKey > node.info.depth) {\r\n      node.right = deleteByDepth(node.right, depthKey);\r\n    } else {\r\n      if (node.left == null) return node.right;\r\n      if (node.right == null) return node.left;\r\n      Node pred = node.left;\r\n      while (pred.right != null) pred = pred.right;\r\n      node.info = pred.info;\r\n      node.left = deleteByDepth(node.left, pred.info.depth);\r\n    }\r\n    return node;\r\n   }\r\n\r\n//=============================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    Node p = findNthWithLeftSonBFS(2);\r\n    if (p != null) {\r\n      Beaver tmp = p.info;\r\n      p.info = p.left.info;\r\n      p.left.info = tmp;\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n }\r\n",
          "expectedOutput": "(E,2,8) (I,5,4) (H,6,3) (F,4,5) (G,8,7) (D,7,2) (C,3,6)\n(I,5,4) (H,6,3) (F,4,5) (D,7,2) (C,3,6)",
          "explanation": "<p>Verified by compiling against the real given project and running choice 2 — matches the paper's expected example byte-for-byte. <code>postOrder2()</code> performs the same left-right-root recursion as <code>postOrder()</code> but only calls <code>fvisit()</code> when <code>type &lt; 7</code>.</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 2 — khớp byte-for-byte với ví dụ trong đề. <code>postOrder2()</code> đệ quy trái-phải-gốc giống <code>postOrder()</code> nhưng chỉ gọi <code>fvisit()</code> khi <code>type &lt; 7</code>.</p>",
          "rubric": [
            {
              "id": "correct",
              "criterion": "Correctly performs post-order traversal displaying only nodes with type < 7.|||Duyệt post-order đúng, chỉ hiển thị node có type < 7.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 2 (4 marks, file BSTree.java).</strong> The class <code>Beaver</code> with 3 data members: place, depth and type is given (do not edit it). <code>BSTree</code> is a binary search tree of Beaver objects. <strong>The variable depth is the key of the tree, thus it must be unique.</strong></p>|||<p><strong>Câu 2 (4 điểm, file BSTree.java).</strong> Lớp <code>Beaver</code> (place, depth, type) đã có sẵn (không sửa). <code>BSTree</code> là cây BST các Beaver. <strong>Biến depth là khoá của cây, phải duy nhất.</strong></p><p><strong>void f3()</strong> — Perform breadth-first traversal and find the 2nd node p having left son. Delete the node having the largest depth in the subtree with root p (thus if p is the leaf-node then p is deleted).</p>|||<p><strong>void f3()</strong> — Duyệt BFS, tìm node p thứ 2 có con trái. Xoá node có depth LỚN NHẤT trong cây con gốc p (nếu p là lá thì xoá chính p).</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xPlace, int xDepth, int xType) {\r\n    //You should insert here statements to complete this function\r\n\r\n\r\n\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n  void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    postOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n \r\n\r\n     \r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n }\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xPlace, int xDepth, int xType) {\r\n    //You should insert here statements to complete this function\r\n    if (xPlace.charAt(0) == 'B') return;\r\n    Beaver b = new Beaver(xPlace, xDepth, xType);\r\n    root = insertRec(root, b);\r\n   }\r\n\r\n  Node insertRec(Node node, Beaver b) {\r\n    if (node == null) return new Node(b);\r\n    if (b.depth < node.info.depth) node.left = insertRec(node.left, b);\r\n    else if (b.depth > node.info.depth) node.right = insertRec(node.right, b);\r\n    return node;\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n  void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    postOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    postOrder2(root, f);\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  void postOrder2(Node p, RandomAccessFile f) throws Exception {\r\n    if (p == null) return;\r\n    postOrder2(p.left, f);\r\n    postOrder2(p.right, f);\r\n    if (p.info.type < 7) fvisit(p, f);\r\n   }\r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    Node p = findNthWithLeftSonBFS(2);\r\n    if (p != null) {\r\n      Node maxNode = findMaxDepthInSubtree(p);\r\n      root = deleteByDepth(root, maxNode.info.depth);\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  Node findNthWithLeftSonBFS(int nth) {\r\n    if (root == null) return null;\r\n    Queue q = new Queue();\r\n    q.enqueue(root);\r\n    int count = 0;\r\n    while (!q.isEmpty()) {\r\n      Node r = (Node) q.dequeue();\r\n      if (r.left != null) {\r\n        count++;\r\n        if (count == nth) return r;\r\n      }\r\n      if (r.left != null) q.enqueue(r.left);\r\n      if (r.right != null) q.enqueue(r.right);\r\n    }\r\n    return null;\r\n   }\r\n\r\n  Node findMaxDepthInSubtree(Node p) {\r\n    if (p == null) return null;\r\n    Node max = p;\r\n    Node leftMax = findMaxDepthInSubtree(p.left);\r\n    Node rightMax = findMaxDepthInSubtree(p.right);\r\n    if (leftMax != null && leftMax.info.depth > max.info.depth) max = leftMax;\r\n    if (rightMax != null && rightMax.info.depth > max.info.depth) max = rightMax;\r\n    return max;\r\n   }\r\n\r\n  Node deleteByDepth(Node node, int depthKey) {\r\n    if (node == null) return null;\r\n    if (depthKey < node.info.depth) {\r\n      node.left = deleteByDepth(node.left, depthKey);\r\n    } else if (depthKey > node.info.depth) {\r\n      node.right = deleteByDepth(node.right, depthKey);\r\n    } else {\r\n      if (node.left == null) return node.right;\r\n      if (node.right == null) return node.left;\r\n      Node pred = node.left;\r\n      while (pred.right != null) pred = pred.right;\r\n      node.info = pred.info;\r\n      node.left = deleteByDepth(node.left, pred.info.depth);\r\n    }\r\n    return node;\r\n   }\r\n\r\n//=============================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    Node p = findNthWithLeftSonBFS(2);\r\n    if (p != null) {\r\n      Beaver tmp = p.info;\r\n      p.info = p.left.info;\r\n      p.left.info = tmp;\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n }\r\n",
          "expectedOutput": "(C,5,2) (D,2,1) (E,6,5) (F,1,3) (G,4,6) (H,3,4)\n(C,5,2) (D,2,1) (E,6,5) (F,1,3) (H,3,4)",
          "explanation": "<p>Verified by compiling against the real given project and running choice 3 — matches the paper's expected example byte-for-byte. BFS order of nodes-with-left-child is C, D, G — so p=D. The subtree rooted at D contains D(2), F(1), G(4), H(3); the maximum depth among these is G(4), which is deleted from the whole tree (G has a single child H, so H takes G's place).</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 3 — khớp byte-for-byte với ví dụ trong đề. Thứ tự BFS các node có con trái là C, D, G — nên p=D. Cây con gốc D gồm D(2), F(1), G(4), H(3); depth lớn nhất trong đó là G(4), bị xoá khỏi toàn cây (G có 1 con H nên H thế chỗ G).</p>",
          "rubric": [
            {
              "id": "correct",
              "criterion": "Correctly identifies the 2nd BFS node with a left child, then deletes the max-depth node within its subtree from the whole tree.|||Xác định đúng node thứ 2 theo BFS có con trái, xoá đúng node depth lớn nhất trong cây con đó khỏi toàn cây.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 2 (4 marks, file BSTree.java).</strong> The class <code>Beaver</code> with 3 data members: place, depth and type is given (do not edit it). <code>BSTree</code> is a binary search tree of Beaver objects. <strong>The variable depth is the key of the tree, thus it must be unique.</strong></p>|||<p><strong>Câu 2 (4 điểm, file BSTree.java).</strong> Lớp <code>Beaver</code> (place, depth, type) đã có sẵn (không sửa). <code>BSTree</code> là cây BST các Beaver. <strong>Biến depth là khoá của cây, phải duy nhất.</strong></p><p><strong>void f4()</strong> — Perform breadth-first traversal and find the 2nd node p having left son, then rotate p to right.</p>|||<p><strong>void f4()</strong> — Duyệt BFS, tìm node p thứ 2 có con trái, rồi xoay p sang PHẢI.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xPlace, int xDepth, int xType) {\r\n    //You should insert here statements to complete this function\r\n\r\n\r\n\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n  void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    postOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n \r\n\r\n     \r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n }\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xPlace, int xDepth, int xType) {\r\n    //You should insert here statements to complete this function\r\n    if (xPlace.charAt(0) == 'B') return;\r\n    Beaver b = new Beaver(xPlace, xDepth, xType);\r\n    root = insertRec(root, b);\r\n   }\r\n\r\n  Node insertRec(Node node, Beaver b) {\r\n    if (node == null) return new Node(b);\r\n    if (b.depth < node.info.depth) node.left = insertRec(node.left, b);\r\n    else if (b.depth > node.info.depth) node.right = insertRec(node.right, b);\r\n    return node;\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n  void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    postOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    postOrder2(root, f);\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  void postOrder2(Node p, RandomAccessFile f) throws Exception {\r\n    if (p == null) return;\r\n    postOrder2(p.left, f);\r\n    postOrder2(p.right, f);\r\n    if (p.info.type < 7) fvisit(p, f);\r\n   }\r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    Node p = findNthWithLeftSonBFS(2);\r\n    if (p != null) {\r\n      Node maxNode = findMaxDepthInSubtree(p);\r\n      root = deleteByDepth(root, maxNode.info.depth);\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  Node findNthWithLeftSonBFS(int nth) {\r\n    if (root == null) return null;\r\n    Queue q = new Queue();\r\n    q.enqueue(root);\r\n    int count = 0;\r\n    while (!q.isEmpty()) {\r\n      Node r = (Node) q.dequeue();\r\n      if (r.left != null) {\r\n        count++;\r\n        if (count == nth) return r;\r\n      }\r\n      if (r.left != null) q.enqueue(r.left);\r\n      if (r.right != null) q.enqueue(r.right);\r\n    }\r\n    return null;\r\n   }\r\n\r\n  Node findMaxDepthInSubtree(Node p) {\r\n    if (p == null) return null;\r\n    Node max = p;\r\n    Node leftMax = findMaxDepthInSubtree(p.left);\r\n    Node rightMax = findMaxDepthInSubtree(p.right);\r\n    if (leftMax != null && leftMax.info.depth > max.info.depth) max = leftMax;\r\n    if (rightMax != null && rightMax.info.depth > max.info.depth) max = rightMax;\r\n    return max;\r\n   }\r\n\r\n  Node deleteByDepth(Node node, int depthKey) {\r\n    if (node == null) return null;\r\n    if (depthKey < node.info.depth) {\r\n      node.left = deleteByDepth(node.left, depthKey);\r\n    } else if (depthKey > node.info.depth) {\r\n      node.right = deleteByDepth(node.right, depthKey);\r\n    } else {\r\n      if (node.left == null) return node.right;\r\n      if (node.right == null) return node.left;\r\n      Node pred = node.left;\r\n      while (pred.right != null) pred = pred.right;\r\n      node.info = pred.info;\r\n      node.left = deleteByDepth(node.left, pred.info.depth);\r\n    }\r\n    return node;\r\n   }\r\n\r\n//=============================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n    Node p = findNthWithLeftSonBFS(2);\r\n    if (p != null) {\r\n      Beaver tmp = p.info;\r\n      p.info = p.left.info;\r\n      p.left.info = tmp;\r\n    }\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n }\r\n",
          "expectedOutput": "(C,5,2) (D,2,1) (E,6,5) (F,1,3) (G,4,6) (H,3,4)\n(C,5,2) (F,1,3) (E,6,5) (D,2,1) (G,4,6) (H,3,4)",
          "explanation": "<p>Verified by compiling against the real given project and running choice 4. Same p=D as f3 (fresh reload, before f3's deletion). As with the equivalent question in a sibling CSD201 paper (Đề 13) this session, \"rotate p to right\" here does not mean a textbook pointer rotation — it verifiably means swapping only the <code>Beaver</code> data between p and p.left, leaving the tree shape unchanged; this reproduces the paper's expected BFS exactly, while a standard pointer rotation does not.</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 4. p=D giống f3 (nạp lại mới, trước khi f3 xoá). Giống câu tương đương ở một đề CSD201 khác (Đề 13) trong phiên này, \"rotate p to right\" ở đây KHÔNG phải phép xoay con trỏ chuẩn giáo trình — mà là ĐỔI dữ liệu <code>Beaver</code> giữa p và p.left, giữ nguyên hình dạng cây; cách này tái tạo đúng BFS mong đợi của đề, còn phép xoay con trỏ chuẩn thì không.</p>",
          "rubric": [
            {
              "id": "correct",
              "criterion": "Correctly identifies the 2nd BFS node with a left child and swaps its data with its left child (verified real given-code interpretation of \"rotate right\").|||Xác định đúng node thứ 2 theo BFS có con trái và đổi đúng dữ liệu với con trái (khớp cách hiểu \"xoay phải\" đã verify bằng mã given thật).",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 3 (2 marks, file Graph.java).</strong> The class <code>Graph</code> is the implementation of a graph (adjacency matrix, vertices labeled A..P via <code>v[]</code>, directed — the matrix is not symmetric).</p>|||<p><strong>Câu 3 (2 điểm, file Graph.java).</strong> Lớp <code>Graph</code> cài đặt đồ thị (ma trận kề, đỉnh gắn nhãn A..P qua <code>v[]</code>, CÓ HƯỚNG — ma trận không đối xứng).</p><p><strong>void f1()</strong> — Perform depth-first traversal (to f1.txt) from vertex i=2 (vertex C), and display only 4 vertices from the 3rd vertex to the 6th vertex. Hint: copy depth(...) to depth2(...) and modify the latter one.</p>|||<p><strong>void f1()</strong> — Duyệt DFS (ra f1.txt) từ đỉnh i=2 (đỉnh C), chỉ hiển thị 4 đỉnh từ vị trí thứ 3 tới thứ 6. Gợi ý: sao chép depth(...) thành depth2(...) rồi sửa.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)============================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n//-------------------------------------------------------------------------------\r\npublic class Graph {\r\n  int [][] a; int n;\r\n  char v[];\r\n  int deg[];\r\n  Graph() {\r\n    v = \"ABCDEFGHIJKLMNOP\".toCharArray();\r\n    deg = new int[20];\r\n    a = new int[20][20];\r\n    n = 0;\r\n   }\r\n\r\n  void loadData(int k) {  //do not edit this function\r\n    RandomAccessFile f;int i,j,x;\r\n    String s;StringTokenizer t;\r\n    a = new int[20][20];\r\n    try {\r\n     f = new RandomAccessFile(\"data.txt\",\"r\");\r\n     for(i=0;i<k;i++) f.readLine();\r\n     s = f.readLine();s = s.trim();\r\n     n = Integer.parseInt(s);\r\n     for(i=0;i<n;i++) {\r\n       s = f.readLine();s = s.trim();\r\n       t = new StringTokenizer(s);\r\n       for(j=0;j<n;j++) { \r\n         x = Integer.parseInt(t.nextToken().trim());\r\n         a[i][j] = x;\r\n        }\r\n       }\r\n     f.close();\r\n     }\r\n    catch(Exception e) {}\r\n\r\n   }\r\n\r\n  void dispAdj() {\r\n    int i,j;\r\n    for(i=0;i<n;i++) {\r\n      System.out.println();\r\n      for(j=0;j<n;j++)\r\n        System.out.printf(\"%4d\",a[i][j]);\r\n     }\r\n   }\r\n\r\n  void fvisit(int i, RandomAccessFile f) throws Exception {\r\n    f.writeBytes(\"  \"+v[i]);\r\n   }\r\n\r\n void fdispAdj(RandomAccessFile f) throws Exception { \r\n    int i,j;\r\n    f.writeBytes(\"n = \"+n+\"\\r\\n\");\r\n    for(i=0;i<n;i++) {\r\n      f.writeBytes(\"\\r\\n\");\r\n      for(j=0;j<n;j++)  f.writeBytes(\"  \" + a[i][j]);\r\n     }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void breadth(boolean [] en, int i, RandomAccessFile f) throws Exception {\r\n    Queue q = new Queue();\r\n    int r,j;\r\n    q.enqueue(i); en[i]=true;\r\n    while(!q.isEmpty()) {\r\n      r = q.dequeue();\r\n      fvisit(r,f);\r\n      for(j=0;j<n;j++) {\r\n        if(!en[j] && a[r][j]>0) {\r\n         q.enqueue(j);en[j]=true;\r\n        }\r\n       }\r\n     }\r\n   }\r\n\r\n  void breadth(int  k, RandomAccessFile f) throws Exception {\r\n    boolean [] en = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) en[i]=false;\r\n    breadth(en,k,f);\r\n    for(i=0;i<n;i++) \r\n      if(!en[i]) breadth(en,i,f);\r\n   }\r\n\r\n void depth(boolean [] visited,int k, RandomAccessFile f) throws Exception {\r\n    fvisit(k,f);visited[k]=true;\r\n    for(int i=0;i<n;i++) {\r\n      if(!visited[i] && a[k][i]>0) depth(visited,i,f);\r\n     }\r\n   }\r\n  void depth(int k, RandomAccessFile f) throws Exception {\r\n    boolean [] visited = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) visited[i]=false;\r\n    depth(visited,k,f);\r\n    for(i=0;i<n;i++) \r\n       if(!visited[i]) depth(visited,i,f);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void f1() throws Exception {\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    depth(2,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n//=================================================================\r\n  void f2() throws Exception {\r\n    loadData(12);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n      // You can use the statement fvisit(i,f); i = 0, 1, 2,...,n-1 to display the vertex i to file f2.txt \r\n      //  and statement f.writeBytes(\" \" + k); to write  variable k to the file f2.txt  \r\n\r\n\r\n\r\n\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n}\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)============================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n//-------------------------------------------------------------------------------\r\npublic class Graph {\r\n  int [][] a; int n;\r\n  char v[];\r\n  int deg[];\r\n  Graph() {\r\n    v = \"ABCDEFGHIJKLMNOP\".toCharArray();\r\n    deg = new int[20];\r\n    a = new int[20][20];\r\n    n = 0;\r\n   }\r\n\r\n  void loadData(int k) {  //do not edit this function\r\n    RandomAccessFile f;int i,j,x;\r\n    String s;StringTokenizer t;\r\n    a = new int[20][20];\r\n    try {\r\n     f = new RandomAccessFile(\"data.txt\",\"r\");\r\n     for(i=0;i<k;i++) f.readLine();\r\n     s = f.readLine();s = s.trim();\r\n     n = Integer.parseInt(s);\r\n     for(i=0;i<n;i++) {\r\n       s = f.readLine();s = s.trim();\r\n       t = new StringTokenizer(s);\r\n       for(j=0;j<n;j++) { \r\n         x = Integer.parseInt(t.nextToken().trim());\r\n         a[i][j] = x;\r\n        }\r\n       }\r\n     f.close();\r\n     }\r\n    catch(Exception e) {}\r\n\r\n   }\r\n\r\n  void dispAdj() {\r\n    int i,j;\r\n    for(i=0;i<n;i++) {\r\n      System.out.println();\r\n      for(j=0;j<n;j++)\r\n        System.out.printf(\"%4d\",a[i][j]);\r\n     }\r\n   }\r\n\r\n  void fvisit(int i, RandomAccessFile f) throws Exception {\r\n    f.writeBytes(\"  \"+v[i]);\r\n   }\r\n\r\n void fdispAdj(RandomAccessFile f) throws Exception { \r\n    int i,j;\r\n    f.writeBytes(\"n = \"+n+\"\\r\\n\");\r\n    for(i=0;i<n;i++) {\r\n      f.writeBytes(\"\\r\\n\");\r\n      for(j=0;j<n;j++)  f.writeBytes(\"  \" + a[i][j]);\r\n     }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void breadth(boolean [] en, int i, RandomAccessFile f) throws Exception {\r\n    Queue q = new Queue();\r\n    int r,j;\r\n    q.enqueue(i); en[i]=true;\r\n    while(!q.isEmpty()) {\r\n      r = q.dequeue();\r\n      fvisit(r,f);\r\n      for(j=0;j<n;j++) {\r\n        if(!en[j] && a[r][j]>0) {\r\n         q.enqueue(j);en[j]=true;\r\n        }\r\n       }\r\n     }\r\n   }\r\n\r\n  void breadth(int  k, RandomAccessFile f) throws Exception {\r\n    boolean [] en = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) en[i]=false;\r\n    breadth(en,k,f);\r\n    for(i=0;i<n;i++) \r\n      if(!en[i]) breadth(en,i,f);\r\n   }\r\n\r\n void depth(boolean [] visited,int k, RandomAccessFile f) throws Exception {\r\n    fvisit(k,f);visited[k]=true;\r\n    for(int i=0;i<n;i++) {\r\n      if(!visited[i] && a[k][i]>0) depth(visited,i,f);\r\n     }\r\n   }\r\n  void depth(int k, RandomAccessFile f) throws Exception {\r\n    boolean [] visited = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) visited[i]=false;\r\n    depth(visited,k,f);\r\n    for(i=0;i<n;i++) \r\n       if(!visited[i]) depth(visited,i,f);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void f1() throws Exception {\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    depth(2,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n    java.util.ArrayList<Integer> order = new java.util.ArrayList<Integer>();\r\n    boolean[] visited2 = new boolean[20];\r\n    depth2(visited2, 2, order);\r\n    for (int i = 0; i < n; i++) if (!visited2[i]) depth2(visited2, i, order);\r\n    for (int idx = 2; idx <= 5 && idx < order.size(); idx++) {\r\n      fvisit(order.get(idx), f);\r\n    }\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  void depth2(boolean[] visited, int k, java.util.ArrayList<Integer> order) {\r\n    order.add(k); visited[k] = true;\r\n    for (int i = 0; i < n; i++) {\r\n      if (!visited[i] && a[k][i] > 0) depth2(visited, i, order);\r\n    }\r\n   }\r\n\r\n//=================================================================\r\n  void f2() throws Exception {\r\n    loadData(12);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n      // You can use the statement fvisit(i,f); i = 0, 1, 2,...,n-1 to display the vertex i to file f2.txt\r\n      //  and statement f.writeBytes(\" \" + k); to write  variable k to the file f2.txt\r\n    int[] parent1 = new int[n];\r\n    java.util.ArrayList<Integer> order1 = new java.util.ArrayList<Integer>();\r\n    java.util.ArrayList<Integer> distOrder1 = new java.util.ArrayList<Integer>();\r\n    dijkstra(2, 5, parent1, order1, distOrder1);\r\n    java.util.ArrayList<Integer> path1 = buildPath(parent1, 2, 5);\r\n\r\n    int[] parent2 = new int[n];\r\n    java.util.ArrayList<Integer> order2 = new java.util.ArrayList<Integer>();\r\n    java.util.ArrayList<Integer> distOrder2 = new java.util.ArrayList<Integer>();\r\n    dijkstra(0, 6, parent2, order2, distOrder2);\r\n    java.util.ArrayList<Integer> path2 = buildPath(parent2, 0, 6);\r\n\r\n    int startLast4 = Math.max(0, order2.size() - 4);\r\n    boolean firstLabel = true;\r\n    for (int idx = startLast4; idx < order2.size(); idx++) {\r\n      if (!firstLabel) f.writeBytes(\" \");\r\n      f.writeBytes(v[order2.get(idx)] + \":\" + distOrder2.get(idx));\r\n      firstLabel = false;\r\n    }\r\n    f.writeBytes(\"\\r\\n\");\r\n    for (int v0 : path2) fvisit(v0, f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    for (int v0 : path1) fvisit(v0, f);\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  int dijkstra(int src, int target, int[] parent, java.util.ArrayList<Integer> order, java.util.ArrayList<Integer> distOrder) {\r\n    int[] dist = new int[n];\r\n    boolean[] visited = new boolean[n];\r\n    for (int i = 0; i < n; i++) { dist[i] = Integer.MAX_VALUE / 2; parent[i] = -1; }\r\n    dist[src] = 0;\r\n    for (int iter = 0; iter < n; iter++) {\r\n      int u = -1, best = Integer.MAX_VALUE;\r\n      for (int i = 0; i < n; i++) if (!visited[i] && dist[i] < best) { best = dist[i]; u = i; }\r\n      if (u == -1) break;\r\n      visited[u] = true;\r\n      order.add(u);\r\n      distOrder.add(dist[u]);\r\n      if (u == target) break;\r\n      for (int w = 0; w < n; w++) {\r\n        int wt = a[u][w];\r\n        if (wt < 99 && wt > 0 && !visited[w] && dist[u] + wt < dist[w]) {\r\n          dist[w] = dist[u] + wt;\r\n          parent[w] = u;\r\n        }\r\n      }\r\n    }\r\n    return dist[target];\r\n   }\r\n\r\n  java.util.ArrayList<Integer> buildPath(int[] parent, int src, int dst) {\r\n    java.util.ArrayList<Integer> p = new java.util.ArrayList<Integer>();\r\n    int cur = dst;\r\n    while (cur != -1) {\r\n      p.add(0, cur);\r\n      if (cur == src) break;\r\n      cur = parent[cur];\r\n    }\r\n    return p;\r\n   }\r\n\r\n}\r\n",
          "expectedOutput": "C A B E H I D G F\nB E H I",
          "explanation": "<p>Verified by compiling against the real given project and running choice 1 — matches the paper's expected example byte-for-byte. This graph's adjacency matrix is directed (not symmetric — e.g. F→C exists but C→F does not), so DFS from C only reaches C,A,B,E,H,I,D,G directly; the outer <code>depth(k,f)</code> wrapper then picks up the remaining unvisited vertex F separately, giving the full 9-vertex order C A B E H I D G F. <code>depth2</code> collects this same order into a list and prints positions 3-6 (1-indexed): B, E, H, I.</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 1 — khớp byte-for-byte với ví dụ trong đề. Ma trận kề của đồ thị này CÓ HƯỚNG (không đối xứng — ví dụ cạnh F→C tồn tại nhưng C→F thì không), nên DFS từ C chỉ tới trực tiếp C,A,B,E,H,I,D,G; hàm bọc ngoài <code>depth(k,f)</code> sau đó tự động duyệt tiếp đỉnh F còn lại chưa thăm, cho đủ thứ tự 9 đỉnh C A B E H I D G F. <code>depth2</code> gom đúng thứ tự này vào danh sách, in vị trí 3-6 (1-indexed): B, E, H, I.</p>",
          "rubric": [
            {
              "id": "correct",
              "criterion": "Correctly computes the DFS-from-C order (respecting the directed edges) and prints positions 3-6.|||Tính đúng thứ tự DFS từ C (tôn trọng cạnh có hướng), in đúng vị trí 3-6.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 3 (2 marks, file Graph.java).</strong> The class <code>Graph</code> is the implementation of a graph (adjacency matrix, vertices labeled A..P via <code>v[]</code>, directed — the matrix is not symmetric).</p>|||<p><strong>Câu 3 (2 điểm, file Graph.java).</strong> Lớp <code>Graph</code> cài đặt đồ thị (ma trận kề, đỉnh gắn nhãn A..P qua <code>v[]</code>, CÓ HƯỚNG — ma trận không đối xứng).</p><p><strong>void f2()</strong> — Apply Dijkstra's shortest path algorithm to find (1) the shortest path from vertex 2 (C) to vertex 5 (F), then (2) from vertex 0 (A) to vertex 6 (G). Write 3 lines to f2.txt: line 1 contains the last 4 vertices selected into the set S with their labels in (2), line 2 contains the shortest path (2), line 3 contains the shortest path (1). (99 = infinity in the weighted matrix.)</p>|||<p><strong>void f2()</strong> — Áp dụng Dijkstra tìm (1) đường đi ngắn nhất từ đỉnh 2 (C) tới đỉnh 5 (F), rồi (2) từ đỉnh 0 (A) tới đỉnh 6 (G). Ghi 3 dòng vào f2.txt: dòng 1 là 4 đỉnh CUỐI được chọn vào S trong (2) kèm nhãn, dòng 2 là đường đi ngắn nhất (2), dòng 3 là đường đi ngắn nhất (1). (99 = vô cực trong ma trận trọng số.)</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)============================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n//-------------------------------------------------------------------------------\r\npublic class Graph {\r\n  int [][] a; int n;\r\n  char v[];\r\n  int deg[];\r\n  Graph() {\r\n    v = \"ABCDEFGHIJKLMNOP\".toCharArray();\r\n    deg = new int[20];\r\n    a = new int[20][20];\r\n    n = 0;\r\n   }\r\n\r\n  void loadData(int k) {  //do not edit this function\r\n    RandomAccessFile f;int i,j,x;\r\n    String s;StringTokenizer t;\r\n    a = new int[20][20];\r\n    try {\r\n     f = new RandomAccessFile(\"data.txt\",\"r\");\r\n     for(i=0;i<k;i++) f.readLine();\r\n     s = f.readLine();s = s.trim();\r\n     n = Integer.parseInt(s);\r\n     for(i=0;i<n;i++) {\r\n       s = f.readLine();s = s.trim();\r\n       t = new StringTokenizer(s);\r\n       for(j=0;j<n;j++) { \r\n         x = Integer.parseInt(t.nextToken().trim());\r\n         a[i][j] = x;\r\n        }\r\n       }\r\n     f.close();\r\n     }\r\n    catch(Exception e) {}\r\n\r\n   }\r\n\r\n  void dispAdj() {\r\n    int i,j;\r\n    for(i=0;i<n;i++) {\r\n      System.out.println();\r\n      for(j=0;j<n;j++)\r\n        System.out.printf(\"%4d\",a[i][j]);\r\n     }\r\n   }\r\n\r\n  void fvisit(int i, RandomAccessFile f) throws Exception {\r\n    f.writeBytes(\"  \"+v[i]);\r\n   }\r\n\r\n void fdispAdj(RandomAccessFile f) throws Exception { \r\n    int i,j;\r\n    f.writeBytes(\"n = \"+n+\"\\r\\n\");\r\n    for(i=0;i<n;i++) {\r\n      f.writeBytes(\"\\r\\n\");\r\n      for(j=0;j<n;j++)  f.writeBytes(\"  \" + a[i][j]);\r\n     }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void breadth(boolean [] en, int i, RandomAccessFile f) throws Exception {\r\n    Queue q = new Queue();\r\n    int r,j;\r\n    q.enqueue(i); en[i]=true;\r\n    while(!q.isEmpty()) {\r\n      r = q.dequeue();\r\n      fvisit(r,f);\r\n      for(j=0;j<n;j++) {\r\n        if(!en[j] && a[r][j]>0) {\r\n         q.enqueue(j);en[j]=true;\r\n        }\r\n       }\r\n     }\r\n   }\r\n\r\n  void breadth(int  k, RandomAccessFile f) throws Exception {\r\n    boolean [] en = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) en[i]=false;\r\n    breadth(en,k,f);\r\n    for(i=0;i<n;i++) \r\n      if(!en[i]) breadth(en,i,f);\r\n   }\r\n\r\n void depth(boolean [] visited,int k, RandomAccessFile f) throws Exception {\r\n    fvisit(k,f);visited[k]=true;\r\n    for(int i=0;i<n;i++) {\r\n      if(!visited[i] && a[k][i]>0) depth(visited,i,f);\r\n     }\r\n   }\r\n  void depth(int k, RandomAccessFile f) throws Exception {\r\n    boolean [] visited = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) visited[i]=false;\r\n    depth(visited,k,f);\r\n    for(i=0;i<n;i++) \r\n       if(!visited[i]) depth(visited,i,f);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void f1() throws Exception {\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    depth(2,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n//=================================================================\r\n  void f2() throws Exception {\r\n    loadData(12);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n      // You can use the statement fvisit(i,f); i = 0, 1, 2,...,n-1 to display the vertex i to file f2.txt \r\n      //  and statement f.writeBytes(\" \" + k); to write  variable k to the file f2.txt  \r\n\r\n\r\n\r\n\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n}\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)============================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n//-------------------------------------------------------------------------------\r\npublic class Graph {\r\n  int [][] a; int n;\r\n  char v[];\r\n  int deg[];\r\n  Graph() {\r\n    v = \"ABCDEFGHIJKLMNOP\".toCharArray();\r\n    deg = new int[20];\r\n    a = new int[20][20];\r\n    n = 0;\r\n   }\r\n\r\n  void loadData(int k) {  //do not edit this function\r\n    RandomAccessFile f;int i,j,x;\r\n    String s;StringTokenizer t;\r\n    a = new int[20][20];\r\n    try {\r\n     f = new RandomAccessFile(\"data.txt\",\"r\");\r\n     for(i=0;i<k;i++) f.readLine();\r\n     s = f.readLine();s = s.trim();\r\n     n = Integer.parseInt(s);\r\n     for(i=0;i<n;i++) {\r\n       s = f.readLine();s = s.trim();\r\n       t = new StringTokenizer(s);\r\n       for(j=0;j<n;j++) { \r\n         x = Integer.parseInt(t.nextToken().trim());\r\n         a[i][j] = x;\r\n        }\r\n       }\r\n     f.close();\r\n     }\r\n    catch(Exception e) {}\r\n\r\n   }\r\n\r\n  void dispAdj() {\r\n    int i,j;\r\n    for(i=0;i<n;i++) {\r\n      System.out.println();\r\n      for(j=0;j<n;j++)\r\n        System.out.printf(\"%4d\",a[i][j]);\r\n     }\r\n   }\r\n\r\n  void fvisit(int i, RandomAccessFile f) throws Exception {\r\n    f.writeBytes(\"  \"+v[i]);\r\n   }\r\n\r\n void fdispAdj(RandomAccessFile f) throws Exception { \r\n    int i,j;\r\n    f.writeBytes(\"n = \"+n+\"\\r\\n\");\r\n    for(i=0;i<n;i++) {\r\n      f.writeBytes(\"\\r\\n\");\r\n      for(j=0;j<n;j++)  f.writeBytes(\"  \" + a[i][j]);\r\n     }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void breadth(boolean [] en, int i, RandomAccessFile f) throws Exception {\r\n    Queue q = new Queue();\r\n    int r,j;\r\n    q.enqueue(i); en[i]=true;\r\n    while(!q.isEmpty()) {\r\n      r = q.dequeue();\r\n      fvisit(r,f);\r\n      for(j=0;j<n;j++) {\r\n        if(!en[j] && a[r][j]>0) {\r\n         q.enqueue(j);en[j]=true;\r\n        }\r\n       }\r\n     }\r\n   }\r\n\r\n  void breadth(int  k, RandomAccessFile f) throws Exception {\r\n    boolean [] en = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) en[i]=false;\r\n    breadth(en,k,f);\r\n    for(i=0;i<n;i++) \r\n      if(!en[i]) breadth(en,i,f);\r\n   }\r\n\r\n void depth(boolean [] visited,int k, RandomAccessFile f) throws Exception {\r\n    fvisit(k,f);visited[k]=true;\r\n    for(int i=0;i<n;i++) {\r\n      if(!visited[i] && a[k][i]>0) depth(visited,i,f);\r\n     }\r\n   }\r\n  void depth(int k, RandomAccessFile f) throws Exception {\r\n    boolean [] visited = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) visited[i]=false;\r\n    depth(visited,k,f);\r\n    for(i=0;i<n;i++) \r\n       if(!visited[i]) depth(visited,i,f);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void f1() throws Exception {\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    depth(2,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n    java.util.ArrayList<Integer> order = new java.util.ArrayList<Integer>();\r\n    boolean[] visited2 = new boolean[20];\r\n    depth2(visited2, 2, order);\r\n    for (int i = 0; i < n; i++) if (!visited2[i]) depth2(visited2, i, order);\r\n    for (int idx = 2; idx <= 5 && idx < order.size(); idx++) {\r\n      fvisit(order.get(idx), f);\r\n    }\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  void depth2(boolean[] visited, int k, java.util.ArrayList<Integer> order) {\r\n    order.add(k); visited[k] = true;\r\n    for (int i = 0; i < n; i++) {\r\n      if (!visited[i] && a[k][i] > 0) depth2(visited, i, order);\r\n    }\r\n   }\r\n\r\n//=================================================================\r\n  void f2() throws Exception {\r\n    loadData(12);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n      // You can use the statement fvisit(i,f); i = 0, 1, 2,...,n-1 to display the vertex i to file f2.txt\r\n      //  and statement f.writeBytes(\" \" + k); to write  variable k to the file f2.txt\r\n    int[] parent1 = new int[n];\r\n    java.util.ArrayList<Integer> order1 = new java.util.ArrayList<Integer>();\r\n    java.util.ArrayList<Integer> distOrder1 = new java.util.ArrayList<Integer>();\r\n    dijkstra(2, 5, parent1, order1, distOrder1);\r\n    java.util.ArrayList<Integer> path1 = buildPath(parent1, 2, 5);\r\n\r\n    int[] parent2 = new int[n];\r\n    java.util.ArrayList<Integer> order2 = new java.util.ArrayList<Integer>();\r\n    java.util.ArrayList<Integer> distOrder2 = new java.util.ArrayList<Integer>();\r\n    dijkstra(0, 6, parent2, order2, distOrder2);\r\n    java.util.ArrayList<Integer> path2 = buildPath(parent2, 0, 6);\r\n\r\n    int startLast4 = Math.max(0, order2.size() - 4);\r\n    boolean firstLabel = true;\r\n    for (int idx = startLast4; idx < order2.size(); idx++) {\r\n      if (!firstLabel) f.writeBytes(\" \");\r\n      f.writeBytes(v[order2.get(idx)] + \":\" + distOrder2.get(idx));\r\n      firstLabel = false;\r\n    }\r\n    f.writeBytes(\"\\r\\n\");\r\n    for (int v0 : path2) fvisit(v0, f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    for (int v0 : path1) fvisit(v0, f);\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n  int dijkstra(int src, int target, int[] parent, java.util.ArrayList<Integer> order, java.util.ArrayList<Integer> distOrder) {\r\n    int[] dist = new int[n];\r\n    boolean[] visited = new boolean[n];\r\n    for (int i = 0; i < n; i++) { dist[i] = Integer.MAX_VALUE / 2; parent[i] = -1; }\r\n    dist[src] = 0;\r\n    for (int iter = 0; iter < n; iter++) {\r\n      int u = -1, best = Integer.MAX_VALUE;\r\n      for (int i = 0; i < n; i++) if (!visited[i] && dist[i] < best) { best = dist[i]; u = i; }\r\n      if (u == -1) break;\r\n      visited[u] = true;\r\n      order.add(u);\r\n      distOrder.add(dist[u]);\r\n      if (u == target) break;\r\n      for (int w = 0; w < n; w++) {\r\n        int wt = a[u][w];\r\n        if (wt < 99 && wt > 0 && !visited[w] && dist[u] + wt < dist[w]) {\r\n          dist[w] = dist[u] + wt;\r\n          parent[w] = u;\r\n        }\r\n      }\r\n    }\r\n    return dist[target];\r\n   }\r\n\r\n  java.util.ArrayList<Integer> buildPath(int[] parent, int src, int dst) {\r\n    java.util.ArrayList<Integer> p = new java.util.ArrayList<Integer>();\r\n    int cur = dst;\r\n    while (cur != -1) {\r\n      p.add(0, cur);\r\n      if (cur == src) break;\r\n      cur = parent[cur];\r\n    }\r\n    return p;\r\n   }\r\n\r\n}\r\n",
          "expectedOutput": "E:15 D:19 F:24 G:29\nA B C E D G\nC E D F",
          "explanation": "<p>Verified by compiling against the real given project and running choice 2 — matches the paper's expected example byte-for-byte on all 3 lines. Standard Dijkstra with early termination once the target is finalized into S; independently hand-computed both runs before writing Java, including the tie-break for G's label (D and F both reach G with cost 29, but D processes first in the argmin scan so D wins the parent assignment).</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 2 — khớp byte-for-byte với ví dụ trong đề ở cả 3 dòng. Dijkstra chuẩn, dừng sớm ngay khi đích được chốt vào S; đã tự tính tay cả 2 lượt trước khi viết Java, kể cả luật phá hoà cho nhãn của G (cả D và F đều tới G với chi phí 29, nhưng D được xử lý trước trong vòng quét tìm min nên D thắng trong việc gán cha).</p>",
          "rubric": [
            {
              "id": "correct",
              "criterion": "Correct Dijkstra: last-4-selected labels for run (2), correct shortest path (2) A→G, and correct shortest path (1) C→F.|||Dijkstra đúng: nhãn 4 đỉnh cuối vào S của lượt (2), đúng đường đi ngắn nhất (2) A→G, và đúng đường đi ngắn nhất (1) C→F.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        }
      ]
    }
  ]
};
