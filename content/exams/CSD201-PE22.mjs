export default {
  "course": {
    "courseCode": "CSD201"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "CODE",
      "code": "PE22-T1",
      "title": "PE Đề 22 — Practical Exam (T1)|||PE Đề 22 — Thi thực hành (T1)",
      "description": "CSD201 PE (CODE): singly linked list (hotel rooms) + queue (rental requests), AI-graded.|||PE CSD201 (viết mã): danh sách liên kết đơn (phòng khách sạn) + hàng đợi (yêu cầu thuê), chấm AI.",
      "durationMinutes": 60,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "attachmentUrl": "https://media.cuongthai.com/files/exam-attachments/CSD201/PE22-Given.zip",
      "attachmentName": "CSD201-PE22-Given.zip",
      "instructions": "<div class=\"ml-en\"><p><strong>CSD201 PE INSTRUCTIONS</strong> (transcribed from the real exam paper, \"T1\").</p>\n   <ol>\n     <li>Software: NetBeans IDE 8.x, Java JDK 1.8. Download the given materials above.</li>\n     <li>Do not use accented Vietnamese when writing comments in the programs.</li>\n     <li>Do not add new import statement(s) to given files.</li>\n     <li>This exam room cannot run NetBeans/JDK in the browser, so type your code directly into the code box — it is graded by AI against the rubric. You can build &amp; run the real given project on your own machine and cross-check against the sample runs shown below.</li>\n   </ol></div><div class=\"ml-vi\"><p><strong>HƯỚNG DẪN PE CSD201</strong> (dịch nguyên văn từ đề thi thật, \"T1\").</p>\n   <ol>\n     <li>Phần mềm: NetBeans IDE 8.x, Java JDK 1.8. Tải given materials ở trên.</li>\n     <li>Không dùng tiếng Việt có dấu khi viết chú thích trong chương trình.</li>\n     <li>Không thêm import mới vào các file đã cho.</li>\n     <li>Phòng thi web không chạy được NetBeans/JDK trực tiếp nên gõ thẳng mã vào ô, được AI chấm theo tiêu chí — bạn có thể build &amp; chạy project given thật trên máy mình rồi đối chiếu với mẫu chạy bên dưới.</li>\n   </ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "CODE",
          "points": 2.5,
          "language": "java",
          "prompt": "<p><strong>Scenario: Hotel Management.</strong></p>\n   <p><strong>Class Structure:</strong> <code>Room</code> (code, status — 0 empty / 1 occupied, size — must be &gt; 0, price — must be &gt; 0) · <code>Node</code> — an info object (Room) plus a next pointer · <code>dataList</code> — a singly linked list managing all Room objects in the hotel · <code>requestQueue</code> — a queue (implemented as a linked list) whose nodes' info contains only size (number of people renting) and price (max amount the customer can pay); size &gt; 0 and price &gt; 0 · <code>MyStore</code> — the main class, holding a dataList (rooms) and a requestQueue (pending orders).</p>|||<p><strong>Bối cảnh: Quản lý Khách sạn.</strong></p>\n   <p><strong>Cấu trúc lớp:</strong> <code>Room</code> (code, status — 0 trống / 1 đang có khách, size — phải &gt; 0, price — phải &gt; 0) · <code>Node</code> — đối tượng info (Room) và con trỏ next · <code>dataList</code> — danh sách liên kết đơn quản lý toàn bộ phòng · <code>requestQueue</code> — hàng đợi (cài bằng danh sách liên kết) mà info của node chỉ chứa size (số người thuê) và price (số tiền tối đa khách trả được); size &gt; 0 và price &gt; 0 · <code>MyStore</code> — lớp chính, chứa 1 dataList (các phòng) và 1 requestQueue (các đơn chờ).</p><p><strong>a. f1(): 2.5 marks – Load data</strong></p>\n     <p>To complete f1(), students need to carry out two specific tasks: implement the function <code>addLast()</code> in the dataList class and the function <code>enQueue()</code> (similar to addLast) in the requestQueue class.</p>|||<p><strong>a. f1(): 2.5 điểm – Nạp dữ liệu</strong></p>\n     <p>Để hoàn thành f1(), cần 2 việc: cài <code>addLast()</code> trong dataList và <code>enQueue()</code> (tương tự addLast) trong requestQueue.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\nclass dataList {\r\n\r\n    Node head, tail;\r\n\r\n    dataList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (head == null);\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void loadDataRoom(int k) //do not edit this function\r\n    {\r\n        String[] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 1);\r\n        int[] c = Lib.readLineToIntArray(\"data.txt\", k + 2);\r\n        int[] d = Lib.readLineToIntArray(\"data.txt\", k + 3);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            addLast(a[i], b[i], c[i], d[i]);\r\n        }\r\n    }\r\n\r\n    void addLast(String code, int status, int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n}\r\n\r\nclass requestQueue {\r\n\r\n    Node front, rear;\r\n\r\n    requestQueue() {\r\n        front = rear = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (front == null);\r\n    }\r\n\r\n    void clear() {\r\n        front = rear = null;\r\n    }\r\n\r\n    void loadDataRequest(int k) //do not edit this function\r\n    {\r\n        int[] a = Lib.readLineToIntArray(\"data.txt\", k + 4);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 5);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            enQueue(a[i], b[i]);\r\n        }\r\n    }\r\n\r\n    void enQueue(int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    Room deQueue() {\r\n        Room tmp = new Room();\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        return tmp;\r\n    }\r\n\r\n}\r\n\r\nclass MyStore {\r\n\r\n    dataList dList = new dataList();\r\n    requestQueue RQueue = new requestQueue();\r\n\r\n    MyStore() {\r\n    }\r\n\r\n    void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void ftraverse(RandomAccessFile f) throws Exception {\r\n        Node p = dList.head;\r\n        f.writeBytes(\"Data List: \");\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            fvisit(p, f); // You will use this statement to write information of the node p to the file\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Request  : \");\r\n        p = RQueue.front;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(\"(\" + p.info.getSize() + \",\" + p.info.getPrice() + \") \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception //do not edit this function\r\n    {\r\n        dList.loadDataRoom(k);\r\n        RQueue.loadDataRequest(k);\r\n    }\r\n\r\n//===========================================================================\r\n//=======YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n    void f1() throws Exception {\r\n        load(1);\r\n        String fname = \"f1.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void rent(Room t) throws Exception {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        load(1);\r\n        String fname = \"f2.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        load(1);\r\n        String fname = \"f3.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        load(1);\r\n        String fname = \"f4.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        int count =0;\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.writeBytes(\"Available Room(s): \" + count + \" \");\r\n        f.close();\r\n    }\r\n\r\n}\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\nclass dataList {\r\n\r\n    Node head, tail;\r\n\r\n    dataList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (head == null);\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void loadDataRoom(int k) //do not edit this function\r\n    {\r\n        String[] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 1);\r\n        int[] c = Lib.readLineToIntArray(\"data.txt\", k + 2);\r\n        int[] d = Lib.readLineToIntArray(\"data.txt\", k + 3);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            addLast(a[i], b[i], c[i], d[i]);\r\n        }\r\n    }\r\n\r\n    void addLast(String code, int status, int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (size <= 0 || price <= 0) return;\r\n        Node node = new Node(new Room(code, status, size, price));\r\n        if (head == null) { head = tail = node; }\r\n        else { tail.next = node; tail = node; }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n}\r\n\r\nclass requestQueue {\r\n\r\n    Node front, rear;\r\n\r\n    requestQueue() {\r\n        front = rear = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (front == null);\r\n    }\r\n\r\n    void clear() {\r\n        front = rear = null;\r\n    }\r\n\r\n    void loadDataRequest(int k) //do not edit this function\r\n    {\r\n        int[] a = Lib.readLineToIntArray(\"data.txt\", k + 4);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 5);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            enQueue(a[i], b[i]);\r\n        }\r\n    }\r\n\r\n    void enQueue(int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (size <= 0 || price <= 0) return;\r\n        Node node = new Node(new Room(size, price));\r\n        if (rear == null) { front = rear = node; }\r\n        else { rear.next = node; rear = node; }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    Room deQueue() {\r\n        Room tmp = new Room();\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (front == null) return null;\r\n        tmp = front.info;\r\n        front = front.next;\r\n        if (front == null) rear = null;\r\n        //---------------------------------------------------------\r\n        return tmp;\r\n    }\r\n\r\n}\r\n\r\nclass MyStore {\r\n\r\n    dataList dList = new dataList();\r\n    requestQueue RQueue = new requestQueue();\r\n\r\n    MyStore() {\r\n    }\r\n\r\n    void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void ftraverse(RandomAccessFile f) throws Exception {\r\n        Node p = dList.head;\r\n        f.writeBytes(\"Data List: \");\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            fvisit(p, f); // You will use this statement to write information of the node p to the file\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Request  : \");\r\n        p = RQueue.front;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(\"(\" + p.info.getSize() + \",\" + p.info.getPrice() + \") \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception //do not edit this function\r\n    {\r\n        dList.loadDataRoom(k);\r\n        RQueue.loadDataRequest(k);\r\n    }\r\n\r\n//===========================================================================\r\n//=======YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n    void f1() throws Exception {\r\n        load(1);\r\n        String fname = \"f1.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void rent(Room t) throws Exception {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (t == null) return;\r\n        Node best = null;\r\n        Node p = dList.head;\r\n        while (p != null) {\r\n            if (p.info.getStatus() == 0 && p.info.getSize() >= t.getSize() && p.info.getPrice() <= t.getPrice()) {\r\n                if (best == null || p.info.getPrice() <= best.info.getPrice()) {\r\n                    best = p;\r\n                }\r\n            }\r\n            p = p.next;\r\n        }\r\n        if (best != null) {\r\n            best.info.setStatus(1);\r\n        }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        load(1);\r\n        String fname = \"f2.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        Room req = RQueue.deQueue();\r\n        rent(req);\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        load(1);\r\n        String fname = \"f3.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        while (!RQueue.isEmpty()) {\r\n            Room req = RQueue.deQueue();\r\n            rent(req);\r\n        }\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        load(1);\r\n        String fname = \"f4.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        int count =0;\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        while (!RQueue.isEmpty()) {\r\n            Room req = RQueue.deQueue();\r\n            rent(req);\r\n        }\r\n        Node p = dList.head;\r\n        while (p != null) {\r\n            if (p.info.getStatus() == 0) count++;\r\n            p = p.next;\r\n        }\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.writeBytes(\"Available Room(s): \" + count + \" \");\r\n        f.close();\r\n    }\r\n\r\n}\r\n",
          "expectedOutput": "Data List: (001,0,10,200) (003,0,3,70) (004,0,4,100) (005,0,3,70)\nRequest  : (1,100) (12,500) (4,50) (4,400)",
          "explanation": "<p>Verified by compiling against the real given project (Room.java/Node.java/Main.java/Lib.java unchanged) and running choice 1 — output matches the paper's expected example byte-for-byte. The bundled data.txt lists room \"002\" with size=0 and one request with size=0 — both violate the documented invariant (\"size must be &gt; 0\", \"price must be &gt; 0\"); <code>addLast()</code> and <code>enQueue()</code> both skip such invalid entries, matching the paper's shorter lists exactly.</p>|||<p>Đã kiểm bằng cách biên dịch cùng project given thật (Room.java/Node.java/Main.java/Lib.java giữ nguyên) và chạy lựa chọn 1 — output khớp byte-for-byte với ví dụ trong đề. data.txt đi kèm liệt kê phòng \"002\" size=0 và 1 yêu cầu size=0 — cả hai vi phạm ràng buộc đã ghi (\"size phải &gt; 0\", \"price phải &gt; 0\"); cả <code>addLast()</code> và <code>enQueue()</code> đều bỏ qua các bản ghi vi phạm này, khớp đúng danh sách ngắn hơn của đề.</p>",
          "rubric": [
            {
              "id": "addlast",
              "criterion": "addLast() correctly appends valid rooms (size>0, price>0) to the dataList tail, rejecting invalid ones.|||addLast() thêm đúng phòng hợp lệ (size>0, price>0) vào cuối dataList, loại bỏ bản ghi không hợp lệ.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "enqueue",
              "criterion": "enQueue() correctly appends valid requests (size>0, price>0) to the requestQueue rear.|||enQueue() thêm đúng yêu cầu hợp lệ (size>0, price>0) vào cuối requestQueue.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 2.5,
          "language": "java",
          "prompt": "<p><strong>Scenario: Hotel Management.</strong></p>\n   <p><strong>Class Structure:</strong> <code>Room</code> (code, status — 0 empty / 1 occupied, size — must be &gt; 0, price — must be &gt; 0) · <code>Node</code> — an info object (Room) plus a next pointer · <code>dataList</code> — a singly linked list managing all Room objects in the hotel · <code>requestQueue</code> — a queue (implemented as a linked list) whose nodes' info contains only size (number of people renting) and price (max amount the customer can pay); size &gt; 0 and price &gt; 0 · <code>MyStore</code> — the main class, holding a dataList (rooms) and a requestQueue (pending orders).</p>|||<p><strong>Bối cảnh: Quản lý Khách sạn.</strong></p>\n   <p><strong>Cấu trúc lớp:</strong> <code>Room</code> (code, status — 0 trống / 1 đang có khách, size — phải &gt; 0, price — phải &gt; 0) · <code>Node</code> — đối tượng info (Room) và con trỏ next · <code>dataList</code> — danh sách liên kết đơn quản lý toàn bộ phòng · <code>requestQueue</code> — hàng đợi (cài bằng danh sách liên kết) mà info của node chỉ chứa size (số người thuê) và price (số tiền tối đa khách trả được); size &gt; 0 và price &gt; 0 · <code>MyStore</code> — lớp chính, chứa 1 dataList (các phòng) và 1 requestQueue (các đơn chờ).</p><p><strong>b. f2(): 2.5 marks – Serve the first request in the requestQueue</strong></p>\n     <p>To complete f2(), students need to perform three specific tasks: (1) implement the <code>deQueue()</code> function of requestQueue, (2) use the result returned by deQueue() to perform the <code>rent()</code> function, then (3) use these two functions to serve the first request.</p>\n     <p><strong>rent() logic — search for the best Room:</strong> An empty Room (status=0), size &gt;= the request's size, price &lt;= the request's price. If more than one Room satisfies these, choose the one with the LOWEST price. If still tied, choose the LAST such Room in the dataList. If found, update its status from 0 to 1.</p>|||<p><strong>b. f2(): 2.5 điểm – Phục vụ yêu cầu đầu tiên trong requestQueue</strong></p>\n     <p>Để hoàn thành f2(), cần 3 việc: (1) cài <code>deQueue()</code> của requestQueue, (2) dùng kết quả trả về của deQueue() để gọi <code>rent()</code>, rồi (3) dùng cả 2 hàm này để phục vụ yêu cầu đầu tiên.</p>\n     <p><strong>Logic rent() — tìm phòng tốt nhất:</strong> Phòng trống (status=0), size &gt;= size của yêu cầu, price &lt;= price của yêu cầu. Nếu nhiều phòng thoả, chọn phòng có price THẤP NHẤT. Nếu vẫn hoà, chọn phòng CUỐI CÙNG (theo thứ tự dataList) thoả điều kiện đó. Nếu tìm được, cập nhật status từ 0 sang 1.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\nclass dataList {\r\n\r\n    Node head, tail;\r\n\r\n    dataList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (head == null);\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void loadDataRoom(int k) //do not edit this function\r\n    {\r\n        String[] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 1);\r\n        int[] c = Lib.readLineToIntArray(\"data.txt\", k + 2);\r\n        int[] d = Lib.readLineToIntArray(\"data.txt\", k + 3);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            addLast(a[i], b[i], c[i], d[i]);\r\n        }\r\n    }\r\n\r\n    void addLast(String code, int status, int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n}\r\n\r\nclass requestQueue {\r\n\r\n    Node front, rear;\r\n\r\n    requestQueue() {\r\n        front = rear = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (front == null);\r\n    }\r\n\r\n    void clear() {\r\n        front = rear = null;\r\n    }\r\n\r\n    void loadDataRequest(int k) //do not edit this function\r\n    {\r\n        int[] a = Lib.readLineToIntArray(\"data.txt\", k + 4);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 5);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            enQueue(a[i], b[i]);\r\n        }\r\n    }\r\n\r\n    void enQueue(int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    Room deQueue() {\r\n        Room tmp = new Room();\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        return tmp;\r\n    }\r\n\r\n}\r\n\r\nclass MyStore {\r\n\r\n    dataList dList = new dataList();\r\n    requestQueue RQueue = new requestQueue();\r\n\r\n    MyStore() {\r\n    }\r\n\r\n    void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void ftraverse(RandomAccessFile f) throws Exception {\r\n        Node p = dList.head;\r\n        f.writeBytes(\"Data List: \");\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            fvisit(p, f); // You will use this statement to write information of the node p to the file\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Request  : \");\r\n        p = RQueue.front;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(\"(\" + p.info.getSize() + \",\" + p.info.getPrice() + \") \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception //do not edit this function\r\n    {\r\n        dList.loadDataRoom(k);\r\n        RQueue.loadDataRequest(k);\r\n    }\r\n\r\n//===========================================================================\r\n//=======YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n    void f1() throws Exception {\r\n        load(1);\r\n        String fname = \"f1.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void rent(Room t) throws Exception {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        load(1);\r\n        String fname = \"f2.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        load(1);\r\n        String fname = \"f3.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        load(1);\r\n        String fname = \"f4.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        int count =0;\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.writeBytes(\"Available Room(s): \" + count + \" \");\r\n        f.close();\r\n    }\r\n\r\n}\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\nclass dataList {\r\n\r\n    Node head, tail;\r\n\r\n    dataList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (head == null);\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void loadDataRoom(int k) //do not edit this function\r\n    {\r\n        String[] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 1);\r\n        int[] c = Lib.readLineToIntArray(\"data.txt\", k + 2);\r\n        int[] d = Lib.readLineToIntArray(\"data.txt\", k + 3);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            addLast(a[i], b[i], c[i], d[i]);\r\n        }\r\n    }\r\n\r\n    void addLast(String code, int status, int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (size <= 0 || price <= 0) return;\r\n        Node node = new Node(new Room(code, status, size, price));\r\n        if (head == null) { head = tail = node; }\r\n        else { tail.next = node; tail = node; }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n}\r\n\r\nclass requestQueue {\r\n\r\n    Node front, rear;\r\n\r\n    requestQueue() {\r\n        front = rear = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (front == null);\r\n    }\r\n\r\n    void clear() {\r\n        front = rear = null;\r\n    }\r\n\r\n    void loadDataRequest(int k) //do not edit this function\r\n    {\r\n        int[] a = Lib.readLineToIntArray(\"data.txt\", k + 4);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 5);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            enQueue(a[i], b[i]);\r\n        }\r\n    }\r\n\r\n    void enQueue(int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (size <= 0 || price <= 0) return;\r\n        Node node = new Node(new Room(size, price));\r\n        if (rear == null) { front = rear = node; }\r\n        else { rear.next = node; rear = node; }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    Room deQueue() {\r\n        Room tmp = new Room();\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (front == null) return null;\r\n        tmp = front.info;\r\n        front = front.next;\r\n        if (front == null) rear = null;\r\n        //---------------------------------------------------------\r\n        return tmp;\r\n    }\r\n\r\n}\r\n\r\nclass MyStore {\r\n\r\n    dataList dList = new dataList();\r\n    requestQueue RQueue = new requestQueue();\r\n\r\n    MyStore() {\r\n    }\r\n\r\n    void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void ftraverse(RandomAccessFile f) throws Exception {\r\n        Node p = dList.head;\r\n        f.writeBytes(\"Data List: \");\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            fvisit(p, f); // You will use this statement to write information of the node p to the file\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Request  : \");\r\n        p = RQueue.front;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(\"(\" + p.info.getSize() + \",\" + p.info.getPrice() + \") \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception //do not edit this function\r\n    {\r\n        dList.loadDataRoom(k);\r\n        RQueue.loadDataRequest(k);\r\n    }\r\n\r\n//===========================================================================\r\n//=======YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n    void f1() throws Exception {\r\n        load(1);\r\n        String fname = \"f1.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void rent(Room t) throws Exception {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (t == null) return;\r\n        Node best = null;\r\n        Node p = dList.head;\r\n        while (p != null) {\r\n            if (p.info.getStatus() == 0 && p.info.getSize() >= t.getSize() && p.info.getPrice() <= t.getPrice()) {\r\n                if (best == null || p.info.getPrice() <= best.info.getPrice()) {\r\n                    best = p;\r\n                }\r\n            }\r\n            p = p.next;\r\n        }\r\n        if (best != null) {\r\n            best.info.setStatus(1);\r\n        }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        load(1);\r\n        String fname = \"f2.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        Room req = RQueue.deQueue();\r\n        rent(req);\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        load(1);\r\n        String fname = \"f3.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        while (!RQueue.isEmpty()) {\r\n            Room req = RQueue.deQueue();\r\n            rent(req);\r\n        }\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        load(1);\r\n        String fname = \"f4.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        int count =0;\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        while (!RQueue.isEmpty()) {\r\n            Room req = RQueue.deQueue();\r\n            rent(req);\r\n        }\r\n        Node p = dList.head;\r\n        while (p != null) {\r\n            if (p.info.getStatus() == 0) count++;\r\n            p = p.next;\r\n        }\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.writeBytes(\"Available Room(s): \" + count + \" \");\r\n        f.close();\r\n    }\r\n\r\n}\r\n",
          "expectedOutput": "Data List: (001,0,10,200) (003,0,3,70) (004,0,4,100) (005,0,3,70)\nRequest  : (1,100) (12,500) (4,50) (4,400)\nData List: (001,0,10,200) (003,0,3,70) (004,0,4,100) (005,1,3,70)\nRequest  : (12,500) (4,50) (4,400)",
          "explanation": "<p>Verified by compiling against the real given project and running choice 2 — output matches the paper's expected example byte-for-byte. Request (1,100): both room 003 and 005 qualify at the tied-lowest price 70; since 005 appears later in the dataList, it is chosen (implemented by scanning front-to-back and updating \"best\" on <code>&lt;=</code>, so a later tie overwrites an earlier one) and its status set to 1.</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 2 — khớp byte-for-byte với ví dụ trong đề. Yêu cầu (1,100): cả phòng 003 và 005 đều thoả cùng giá thấp nhất 70; vì 005 đứng sau trong dataList nên được chọn (cài bằng cách quét từ đầu tới cuối, cập nhật \"best\" khi <code>&lt;=</code>, nên phòng hoà giá đứng sau ghi đè phòng đứng trước), status của nó đặt thành 1.</p>",
          "rubric": [
            {
              "id": "dequeue",
              "criterion": "deQueue() correctly removes and returns the front request.|||deQueue() gỡ và trả đúng yêu cầu ở đầu hàng đợi.",
              "weight": 1,
              "maxScore": 0.75
            },
            {
              "id": "rent_search",
              "criterion": "rent() correctly finds the best room (empty, size>=req, price<=req, lowest price, last on tie).|||rent() tìm đúng phòng tốt nhất (trống, size đủ, price đủ, giá thấp nhất, hoà thì chọn phòng cuối).",
              "weight": 1,
              "maxScore": 1.25
            },
            {
              "id": "rent_update",
              "criterion": "rent() correctly updates the chosen room's status to 1.|||rent() cập nhật đúng status phòng được chọn thành 1.",
              "weight": 1,
              "maxScore": 0.5
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 2.5,
          "language": "java",
          "prompt": "<p><strong>Scenario: Hotel Management.</strong></p>\n   <p><strong>Class Structure:</strong> <code>Room</code> (code, status — 0 empty / 1 occupied, size — must be &gt; 0, price — must be &gt; 0) · <code>Node</code> — an info object (Room) plus a next pointer · <code>dataList</code> — a singly linked list managing all Room objects in the hotel · <code>requestQueue</code> — a queue (implemented as a linked list) whose nodes' info contains only size (number of people renting) and price (max amount the customer can pay); size &gt; 0 and price &gt; 0 · <code>MyStore</code> — the main class, holding a dataList (rooms) and a requestQueue (pending orders).</p>|||<p><strong>Bối cảnh: Quản lý Khách sạn.</strong></p>\n   <p><strong>Cấu trúc lớp:</strong> <code>Room</code> (code, status — 0 trống / 1 đang có khách, size — phải &gt; 0, price — phải &gt; 0) · <code>Node</code> — đối tượng info (Room) và con trỏ next · <code>dataList</code> — danh sách liên kết đơn quản lý toàn bộ phòng · <code>requestQueue</code> — hàng đợi (cài bằng danh sách liên kết) mà info của node chỉ chứa size (số người thuê) và price (số tiền tối đa khách trả được); size &gt; 0 và price &gt; 0 · <code>MyStore</code> — lớp chính, chứa 1 dataList (các phòng) và 1 requestQueue (các đơn chờ).</p><p><strong>c. f3(): 2.5 marks – Serve all requests in the requestQueue</strong></p>\n     <p>Perform the pair of operations <code>deQueue()</code> and <code>rent()</code> for all elements in the requestQueue.</p>|||<p><strong>c. f3(): 2.5 điểm – Phục vụ TOÀN BỘ yêu cầu trong requestQueue</strong></p>\n     <p>Áp dụng cặp thao tác <code>deQueue()</code> và <code>rent()</code> cho mọi phần tử trong requestQueue.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\nclass dataList {\r\n\r\n    Node head, tail;\r\n\r\n    dataList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (head == null);\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void loadDataRoom(int k) //do not edit this function\r\n    {\r\n        String[] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 1);\r\n        int[] c = Lib.readLineToIntArray(\"data.txt\", k + 2);\r\n        int[] d = Lib.readLineToIntArray(\"data.txt\", k + 3);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            addLast(a[i], b[i], c[i], d[i]);\r\n        }\r\n    }\r\n\r\n    void addLast(String code, int status, int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n}\r\n\r\nclass requestQueue {\r\n\r\n    Node front, rear;\r\n\r\n    requestQueue() {\r\n        front = rear = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (front == null);\r\n    }\r\n\r\n    void clear() {\r\n        front = rear = null;\r\n    }\r\n\r\n    void loadDataRequest(int k) //do not edit this function\r\n    {\r\n        int[] a = Lib.readLineToIntArray(\"data.txt\", k + 4);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 5);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            enQueue(a[i], b[i]);\r\n        }\r\n    }\r\n\r\n    void enQueue(int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    Room deQueue() {\r\n        Room tmp = new Room();\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        return tmp;\r\n    }\r\n\r\n}\r\n\r\nclass MyStore {\r\n\r\n    dataList dList = new dataList();\r\n    requestQueue RQueue = new requestQueue();\r\n\r\n    MyStore() {\r\n    }\r\n\r\n    void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void ftraverse(RandomAccessFile f) throws Exception {\r\n        Node p = dList.head;\r\n        f.writeBytes(\"Data List: \");\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            fvisit(p, f); // You will use this statement to write information of the node p to the file\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Request  : \");\r\n        p = RQueue.front;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(\"(\" + p.info.getSize() + \",\" + p.info.getPrice() + \") \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception //do not edit this function\r\n    {\r\n        dList.loadDataRoom(k);\r\n        RQueue.loadDataRequest(k);\r\n    }\r\n\r\n//===========================================================================\r\n//=======YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n    void f1() throws Exception {\r\n        load(1);\r\n        String fname = \"f1.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void rent(Room t) throws Exception {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        load(1);\r\n        String fname = \"f2.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        load(1);\r\n        String fname = \"f3.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        load(1);\r\n        String fname = \"f4.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        int count =0;\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.writeBytes(\"Available Room(s): \" + count + \" \");\r\n        f.close();\r\n    }\r\n\r\n}\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\nclass dataList {\r\n\r\n    Node head, tail;\r\n\r\n    dataList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (head == null);\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void loadDataRoom(int k) //do not edit this function\r\n    {\r\n        String[] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 1);\r\n        int[] c = Lib.readLineToIntArray(\"data.txt\", k + 2);\r\n        int[] d = Lib.readLineToIntArray(\"data.txt\", k + 3);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            addLast(a[i], b[i], c[i], d[i]);\r\n        }\r\n    }\r\n\r\n    void addLast(String code, int status, int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (size <= 0 || price <= 0) return;\r\n        Node node = new Node(new Room(code, status, size, price));\r\n        if (head == null) { head = tail = node; }\r\n        else { tail.next = node; tail = node; }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n}\r\n\r\nclass requestQueue {\r\n\r\n    Node front, rear;\r\n\r\n    requestQueue() {\r\n        front = rear = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (front == null);\r\n    }\r\n\r\n    void clear() {\r\n        front = rear = null;\r\n    }\r\n\r\n    void loadDataRequest(int k) //do not edit this function\r\n    {\r\n        int[] a = Lib.readLineToIntArray(\"data.txt\", k + 4);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 5);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            enQueue(a[i], b[i]);\r\n        }\r\n    }\r\n\r\n    void enQueue(int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (size <= 0 || price <= 0) return;\r\n        Node node = new Node(new Room(size, price));\r\n        if (rear == null) { front = rear = node; }\r\n        else { rear.next = node; rear = node; }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    Room deQueue() {\r\n        Room tmp = new Room();\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (front == null) return null;\r\n        tmp = front.info;\r\n        front = front.next;\r\n        if (front == null) rear = null;\r\n        //---------------------------------------------------------\r\n        return tmp;\r\n    }\r\n\r\n}\r\n\r\nclass MyStore {\r\n\r\n    dataList dList = new dataList();\r\n    requestQueue RQueue = new requestQueue();\r\n\r\n    MyStore() {\r\n    }\r\n\r\n    void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void ftraverse(RandomAccessFile f) throws Exception {\r\n        Node p = dList.head;\r\n        f.writeBytes(\"Data List: \");\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            fvisit(p, f); // You will use this statement to write information of the node p to the file\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Request  : \");\r\n        p = RQueue.front;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(\"(\" + p.info.getSize() + \",\" + p.info.getPrice() + \") \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception //do not edit this function\r\n    {\r\n        dList.loadDataRoom(k);\r\n        RQueue.loadDataRequest(k);\r\n    }\r\n\r\n//===========================================================================\r\n//=======YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n    void f1() throws Exception {\r\n        load(1);\r\n        String fname = \"f1.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void rent(Room t) throws Exception {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (t == null) return;\r\n        Node best = null;\r\n        Node p = dList.head;\r\n        while (p != null) {\r\n            if (p.info.getStatus() == 0 && p.info.getSize() >= t.getSize() && p.info.getPrice() <= t.getPrice()) {\r\n                if (best == null || p.info.getPrice() <= best.info.getPrice()) {\r\n                    best = p;\r\n                }\r\n            }\r\n            p = p.next;\r\n        }\r\n        if (best != null) {\r\n            best.info.setStatus(1);\r\n        }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        load(1);\r\n        String fname = \"f2.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        Room req = RQueue.deQueue();\r\n        rent(req);\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        load(1);\r\n        String fname = \"f3.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        while (!RQueue.isEmpty()) {\r\n            Room req = RQueue.deQueue();\r\n            rent(req);\r\n        }\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        load(1);\r\n        String fname = \"f4.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        int count =0;\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        while (!RQueue.isEmpty()) {\r\n            Room req = RQueue.deQueue();\r\n            rent(req);\r\n        }\r\n        Node p = dList.head;\r\n        while (p != null) {\r\n            if (p.info.getStatus() == 0) count++;\r\n            p = p.next;\r\n        }\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.writeBytes(\"Available Room(s): \" + count + \" \");\r\n        f.close();\r\n    }\r\n\r\n}\r\n",
          "expectedOutput": "Data List: (001,0,10,200) (003,0,3,70) (004,0,4,100) (005,0,3,70)\nRequest  : (1,100) (12,500) (4,50) (4,400)\nData List: (001,0,10,200) (003,0,3,70) (004,1,4,100) (005,1,3,70)\nRequest  : Empty",
          "explanation": "<p>Verified by compiling against the real given project and running choice 3 — output matches the paper's expected example byte-for-byte. Request (1,100) rents 005. Request (12,500) finds no room (max available size is 10). Request (4,50) finds no room (cheapest remaining is 70&gt;50). Request (4,400) qualifies rooms 001(200) and 004(100) — 004 has the lower price and is rented.</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 3 — khớp byte-for-byte với ví dụ trong đề. Yêu cầu (1,100) thuê 005. Yêu cầu (12,500) không tìm được phòng (size lớn nhất còn trống là 10). Yêu cầu (4,50) không tìm được phòng (rẻ nhất còn lại là 70&gt;50). Yêu cầu (4,400) đủ điều kiện với 001(200) và 004(100) — 004 giá thấp hơn nên được thuê.</p>",
          "rubric": [
            {
              "id": "serve_all",
              "criterion": "Correctly serves every request in the queue in order via deQueue()+rent(), leaving the queue empty.|||Phục vụ đúng mọi yêu cầu trong hàng đợi theo thứ tự qua deQueue()+rent(), để hàng đợi rỗng.",
              "weight": 1,
              "maxScore": 2.5
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 2.5,
          "language": "java",
          "prompt": "<p><strong>Scenario: Hotel Management.</strong></p>\n   <p><strong>Class Structure:</strong> <code>Room</code> (code, status — 0 empty / 1 occupied, size — must be &gt; 0, price — must be &gt; 0) · <code>Node</code> — an info object (Room) plus a next pointer · <code>dataList</code> — a singly linked list managing all Room objects in the hotel · <code>requestQueue</code> — a queue (implemented as a linked list) whose nodes' info contains only size (number of people renting) and price (max amount the customer can pay); size &gt; 0 and price &gt; 0 · <code>MyStore</code> — the main class, holding a dataList (rooms) and a requestQueue (pending orders).</p>|||<p><strong>Bối cảnh: Quản lý Khách sạn.</strong></p>\n   <p><strong>Cấu trúc lớp:</strong> <code>Room</code> (code, status — 0 trống / 1 đang có khách, size — phải &gt; 0, price — phải &gt; 0) · <code>Node</code> — đối tượng info (Room) và con trỏ next · <code>dataList</code> — danh sách liên kết đơn quản lý toàn bộ phòng · <code>requestQueue</code> — hàng đợi (cài bằng danh sách liên kết) mà info của node chỉ chứa size (số người thuê) và price (số tiền tối đa khách trả được); size &gt; 0 và price &gt; 0 · <code>MyStore</code> — lớp chính, chứa 1 dataList (các phòng) và 1 requestQueue (các đơn chờ).</p><p><strong>d. f4(): 2.5 marks – Count the available Rooms after serve all requests in the requestQueue</strong></p>\n     <p>You should: (1) perform the pair of operations deQueue() and rent() for all elements in the requestQueue, then (2) count the available Rooms with status = 0.</p>|||<p><strong>d. f4(): 2.5 điểm – Đếm số phòng còn trống sau khi phục vụ hết yêu cầu</strong></p>\n     <p>Cần: (1) áp dụng deQueue()+rent() cho mọi phần tử trong requestQueue, rồi (2) đếm số phòng còn status = 0.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\nclass dataList {\r\n\r\n    Node head, tail;\r\n\r\n    dataList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (head == null);\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void loadDataRoom(int k) //do not edit this function\r\n    {\r\n        String[] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 1);\r\n        int[] c = Lib.readLineToIntArray(\"data.txt\", k + 2);\r\n        int[] d = Lib.readLineToIntArray(\"data.txt\", k + 3);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            addLast(a[i], b[i], c[i], d[i]);\r\n        }\r\n    }\r\n\r\n    void addLast(String code, int status, int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n}\r\n\r\nclass requestQueue {\r\n\r\n    Node front, rear;\r\n\r\n    requestQueue() {\r\n        front = rear = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (front == null);\r\n    }\r\n\r\n    void clear() {\r\n        front = rear = null;\r\n    }\r\n\r\n    void loadDataRequest(int k) //do not edit this function\r\n    {\r\n        int[] a = Lib.readLineToIntArray(\"data.txt\", k + 4);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 5);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            enQueue(a[i], b[i]);\r\n        }\r\n    }\r\n\r\n    void enQueue(int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    Room deQueue() {\r\n        Room tmp = new Room();\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        return tmp;\r\n    }\r\n\r\n}\r\n\r\nclass MyStore {\r\n\r\n    dataList dList = new dataList();\r\n    requestQueue RQueue = new requestQueue();\r\n\r\n    MyStore() {\r\n    }\r\n\r\n    void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void ftraverse(RandomAccessFile f) throws Exception {\r\n        Node p = dList.head;\r\n        f.writeBytes(\"Data List: \");\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            fvisit(p, f); // You will use this statement to write information of the node p to the file\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Request  : \");\r\n        p = RQueue.front;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(\"(\" + p.info.getSize() + \",\" + p.info.getPrice() + \") \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception //do not edit this function\r\n    {\r\n        dList.loadDataRoom(k);\r\n        RQueue.loadDataRequest(k);\r\n    }\r\n\r\n//===========================================================================\r\n//=======YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n    void f1() throws Exception {\r\n        load(1);\r\n        String fname = \"f1.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void rent(Room t) throws Exception {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        load(1);\r\n        String fname = \"f2.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        load(1);\r\n        String fname = \"f3.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        load(1);\r\n        String fname = \"f4.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        int count =0;\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.writeBytes(\"Available Room(s): \" + count + \" \");\r\n        f.close();\r\n    }\r\n\r\n}\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\nclass dataList {\r\n\r\n    Node head, tail;\r\n\r\n    dataList() {\r\n        head = tail = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (head == null);\r\n    }\r\n\r\n    void clear() {\r\n        head = tail = null;\r\n    }\r\n\r\n    void loadDataRoom(int k) //do not edit this function\r\n    {\r\n        String[] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 1);\r\n        int[] c = Lib.readLineToIntArray(\"data.txt\", k + 2);\r\n        int[] d = Lib.readLineToIntArray(\"data.txt\", k + 3);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            addLast(a[i], b[i], c[i], d[i]);\r\n        }\r\n    }\r\n\r\n    void addLast(String code, int status, int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (size <= 0 || price <= 0) return;\r\n        Node node = new Node(new Room(code, status, size, price));\r\n        if (head == null) { head = tail = node; }\r\n        else { tail.next = node; tail = node; }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n}\r\n\r\nclass requestQueue {\r\n\r\n    Node front, rear;\r\n\r\n    requestQueue() {\r\n        front = rear = null;\r\n    }\r\n\r\n    boolean isEmpty() {\r\n        return (front == null);\r\n    }\r\n\r\n    void clear() {\r\n        front = rear = null;\r\n    }\r\n\r\n    void loadDataRequest(int k) //do not edit this function\r\n    {\r\n        int[] a = Lib.readLineToIntArray(\"data.txt\", k + 4);\r\n        int[] b = Lib.readLineToIntArray(\"data.txt\", k + 5);\r\n        int n = a.length;\r\n        for (int i = 0; i < n; i++) {\r\n            enQueue(a[i], b[i]);\r\n        }\r\n    }\r\n\r\n    void enQueue(int size, int price) {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (size <= 0 || price <= 0) return;\r\n        Node node = new Node(new Room(size, price));\r\n        if (rear == null) { front = rear = node; }\r\n        else { rear.next = node; rear = node; }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    Room deQueue() {\r\n        Room tmp = new Room();\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (front == null) return null;\r\n        tmp = front.info;\r\n        front = front.next;\r\n        if (front == null) rear = null;\r\n        //---------------------------------------------------------\r\n        return tmp;\r\n    }\r\n\r\n}\r\n\r\nclass MyStore {\r\n\r\n    dataList dList = new dataList();\r\n    requestQueue RQueue = new requestQueue();\r\n\r\n    MyStore() {\r\n    }\r\n\r\n    void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n        if (p != null) {\r\n            f.writeBytes(p.info + \" \");\r\n        }\r\n    }\r\n\r\n    void ftraverse(RandomAccessFile f) throws Exception {\r\n        Node p = dList.head;\r\n        f.writeBytes(\"Data List: \");\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            fvisit(p, f); // You will use this statement to write information of the node p to the file\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n        f.writeBytes(\"Request  : \");\r\n        p = RQueue.front;\r\n        if (p == null) {\r\n            f.writeBytes(\"Empty\");\r\n        }\r\n        while (p != null) {\r\n            f.writeBytes(\"(\" + p.info.getSize() + \",\" + p.info.getPrice() + \") \");\r\n            p = p.next;\r\n        }\r\n        f.writeBytes(\"\\r\\n\");\r\n    }\r\n\r\n    void load(int k) throws Exception //do not edit this function\r\n    {\r\n        dList.loadDataRoom(k);\r\n        RQueue.loadDataRequest(k);\r\n    }\r\n\r\n//===========================================================================\r\n//=======YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n    void f1() throws Exception {\r\n        load(1);\r\n        String fname = \"f1.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void rent(Room t) throws Exception {\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        if (t == null) return;\r\n        Node best = null;\r\n        Node p = dList.head;\r\n        while (p != null) {\r\n            if (p.info.getStatus() == 0 && p.info.getSize() >= t.getSize() && p.info.getPrice() <= t.getPrice()) {\r\n                if (best == null || p.info.getPrice() <= best.info.getPrice()) {\r\n                    best = p;\r\n                }\r\n            }\r\n            p = p.next;\r\n        }\r\n        if (best != null) {\r\n            best.info.setStatus(1);\r\n        }\r\n        //---------------------------------------------------------\r\n    }\r\n\r\n    void f2() throws Exception {\r\n        load(1);\r\n        String fname = \"f2.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        Room req = RQueue.deQueue();\r\n        rent(req);\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f3() throws Exception {\r\n        load(1);\r\n        String fname = \"f3.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        while (!RQueue.isEmpty()) {\r\n            Room req = RQueue.deQueue();\r\n            rent(req);\r\n        }\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.close();\r\n    }\r\n\r\n    void f4() throws Exception {\r\n        load(1);\r\n        String fname = \"f4.txt\";\r\n        File g123 = new File(fname);\r\n        if (g123.exists()) {\r\n            g123.delete();\r\n        }\r\n        RandomAccessFile f = new RandomAccessFile(fname, \"rw\");\r\n        ftraverse(f);\r\n        int count =0;\r\n        //You should write here appropriate statements to complete this function.\r\n        //--------------------------------------------------------\r\n        while (!RQueue.isEmpty()) {\r\n            Room req = RQueue.deQueue();\r\n            rent(req);\r\n        }\r\n        Node p = dList.head;\r\n        while (p != null) {\r\n            if (p.info.getStatus() == 0) count++;\r\n            p = p.next;\r\n        }\r\n        //---------------------------------------------------------\r\n        ftraverse(f);\r\n        f.writeBytes(\"Available Room(s): \" + count + \" \");\r\n        f.close();\r\n    }\r\n\r\n}\r\n",
          "expectedOutput": "Data List: (001,0,10,200) (003,0,3,70) (004,0,4,100) (005,0,3,70)\nRequest  : (1,100) (12,500) (4,50) (4,400)\nData List: (001,0,10,200) (003,0,3,70) (004,1,4,100) (005,1,3,70)\nRequest  : Empty\nAvailable Room(s): 2",
          "explanation": "<p>Verified by compiling against the real given project and running choice 4 — output matches the paper's expected example byte-for-byte. Same serve-all logic as f3 (004 and 005 end up rented), then counts rooms with <code>status==0</code>: 001 and 003 → 2.</p>|||<p>Đã kiểm bằng biên dịch project given thật và chạy lựa chọn 4 — khớp byte-for-byte với ví dụ trong đề. Cùng logic phục vụ hết như f3 (004 và 005 được thuê), rồi đếm phòng có <code>status==0</code>: 001 và 003 → 2.</p>",
          "rubric": [
            {
              "id": "serve_all_again",
              "criterion": "Correctly repeats the serve-all-requests logic.|||Lặp đúng logic phục vụ hết yêu cầu.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "count_available",
              "criterion": "Correctly counts rooms with status == 0.|||Đếm đúng phòng có status == 0.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        }
      ]
    }
  ]
};
