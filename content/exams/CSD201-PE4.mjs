export default {
  "course": {
    "courseCode": "CSD201"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "CODE",
      "code": "PE4-SP26PE6",
      "title": "PE Đề 4 — Practical Exam (SP26 - PE - 6)|||PE Đề 4 — Thi thực hành (SP26 - PE - 6)",
      "description": "CSD201 PE (CODE): singly linked list + BST (rotation, delete-by-copying) + Graph (DFS+degree, Dijkstra), AI-graded.|||PE CSD201 (viết mã): danh sách liên kết đơn + BST (xoay cây, xoá-bằng-copy) + Đồ thị (DFS+bậc, Dijkstra), chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "attachmentUrl": "https://media.cuongthai.com/files/exam-attachments/CSD201/PE4-Given.zip",
      "attachmentName": "CSD201-PE4-Given.zip",
      "instructions": "<div class=\"ml-en\"><p><strong>CSD201 PE INSTRUCTIONS</strong> (transcribed from the real exam paper, \"SP26 - PE - 6\").</p>\n   <ol>\n     <li>Software: Java JDK 1.8, NetBeans 8.x to 17. Download the given materials above (Q1/, Q2/, Q3/ subfolders).</li>\n     <li>Only 3 files may be edited: MyList.java (Q1), BSTree.java (Q2), Graph.java (Q3) — only ADD statements/functions in part (2), never edit given statements.</li>\n     <li>This exam room cannot run NetBeans/JDK in the browser, so type your code directly into the code box — it is graded by AI against the rubric. You can build &amp; run the real given project on your own machine and cross-check against the sample runs shown below.</li>\n   </ol></div><div class=\"ml-vi\"><p><strong>HƯỚNG DẪN PE CSD201</strong> (dịch nguyên văn từ đề thi thật, \"SP26 - PE - 6\").</p>\n   <ol>\n     <li>Phần mềm: Java JDK 1.8, NetBeans 8.x đến 17. Tải given materials ở trên (có thư mục con Q1/, Q2/, Q3/).</li>\n     <li>Chỉ được sửa 3 file: MyList.java (Câu 1), BSTree.java (Câu 2), Graph.java (Câu 3) — chỉ THÊM statement/hàm ở phần (2), không sửa statement đã cho.</li>\n     <li>Phòng thi web không chạy được NetBeans/JDK trực tiếp nên gõ thẳng mã vào ô, được AI chấm theo tiêu chí — bạn có thể build &amp; chạy project given thật trên máy mình rồi đối chiếu với mẫu chạy bên dưới.</li>\n   </ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "CODE",
          "points": 4,
          "language": "java",
          "prompt": "<p><strong>Question 1 (4 marks) — file MyList.java.</strong> The <code>Cape</code> class (tailor, color, type) is provided. <code>MyList</code> is a singly linked list of Cape objects (head/tail).</p>\n     <ul>\n       <li><code>addLast(xTailor, xColor, xType)</code> — if <code>xTailor.charAt(0)=='A'</code> do nothing; otherwise append at the end. Expected f1.txt: <code>(B,5,3) (C,6,5) (D,2,4) (E,7,9) (F,4,-7) (G,-3,2)</code></li>\n       <li><code>f2()</code> — 3 given Cape objects x, y, z. Insert them so x, y, z end up at positions 1, 2, 4 (head is position 0), applied sequentially (x at pos 1, then y at pos 2 of the resulting list, then z at pos 4 of that result).</li>\n       <li><code>f3()</code> — swap the <code>type</code> value of the tail element and the element just before the tail.</li>\n       <li><code>f4()</code> — find the first node p having tailor=\"K\". Sort the 5 elements just before p ascending by type, leaving p and other nodes untouched.</li>\n     </ul>|||<p><strong>Câu 1 (4 điểm) — file MyList.java.</strong> Lớp <code>Cape</code> (tailor, color, type) đã có sẵn. <code>MyList</code> là danh sách liên kết đơn các Cape (head/tail).</p>\n     <ul>\n       <li><code>addLast(xTailor, xColor, xType)</code> — nếu <code>xTailor.charAt(0)=='A'</code> thì không làm gì; ngược lại thêm vào cuối. f1.txt mong đợi: như trên.</li>\n       <li><code>f2()</code> — cho 3 đối tượng Cape x, y, z. Chèn sao cho x, y, z ở vị trí 1, 2, 4 (head là vị trí 0), chèn tuần tự (x tại vị trí 1, rồi y tại vị trí 2 của danh sách vừa chèn x, rồi z tại vị trí 4 của kết quả đó).</li>\n       <li><code>f3()</code> — hoán đổi giá trị <code>type</code> của phần tử tail và phần tử ngay trước tail.</li>\n       <li><code>f4()</code> — tìm node p đầu tiên có tailor=\"K\". Sắp xếp 5 phần tử ngay TRƯỚC p tăng dần theo type, giữ nguyên p và các node khác.</li>\n     </ul>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xTailor, int xColor, int xType) {\r\n    //You should write here appropriate statements to complete this function.\r\n\r\n\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Cape x, y, z;\r\n     x = new Cape(\"X\",1,2);\r\n     y = new Cape(\"Y\",2,3);\r\n     z = new Cape(\"Z\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n  }   \r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)==============================================================\nimport java.util.*;\nimport java.io.*;\n\npublic class MyList {\n  Node head,tail;\n  MyList() {head=tail=null;}\n  boolean isEmpty() {\n    return(head==null);\n   }\n  void clear() {head=tail=null;}\n\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\n    if(p != null) f.writeBytes(p.info + \" \");\n   }\n\n  void ftraverse(RandomAccessFile f) throws Exception {\n    Node p = head;\n    while(p!=null) {\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\n       p=p.next;\n      }\n    f.writeBytes(\"\\r\\n\");\n   }\n\n  void loadData(int k) { //do not edit this function\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\n    int n = a.length;\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\n   }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n/* \n   Khong su dung tieng Viet co dau de viet ghi chu.\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\n*/\n  void addLast(String xTailor, int xColor, int xType) {\n    //You should write here appropriate statements to complete this function.\n    if (xTailor.charAt(0) == 'A') return;\n    Cape c = new Cape(xTailor, xColor, xType);\n    Node p = new Node(c);\n    if (head == null) { head = tail = p; }\n    else { tail.next = p; tail = p; }\n   }\n\n  void insertAtPosition(Cape c, int pos) {\n    Node p = new Node(c);\n    if (pos == 0) {\n      p.next = head; head = p;\n      if (tail == null) tail = p;\n      return;\n    }\n    Node prev = head;\n    for (int i = 0; i < pos - 1; i++) prev = prev.next;\n    p.next = prev.next;\n    prev.next = p;\n    if (p.next == null) tail = p;\n   }\n\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\n  void f1() throws Exception {\n     clear();\n     loadData(1);\n     String fname = \"f1.txt\";\n     File g123 = new File(fname);\n     if(g123.exists()) g123.delete();\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \n     ftraverse(f);\n     f.close();\n    }  \n\n//==================================================================\n  void f2() throws Exception {\n     clear();\n     loadData(5);\n     String fname = \"f2.txt\";\n     File g123 = new File(fname);\n     if(g123.exists()) g123.delete();\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \n     ftraverse(f);\n     Cape x, y, z;\n     x = new Cape(\"X\",1,2);\n     y = new Cape(\"Y\",2,3);\n     z = new Cape(\"Z\",3,4);\n     //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n     insertAtPosition(x, 1);\n     insertAtPosition(y, 2);\n     insertAtPosition(z, 4);\n    //------------------------------------------------------------------------------------\n     ftraverse(f);\n     f.close();\n    }  \n\n//==================================================================\n  void f3() throws Exception {\n    clear();\n    loadData(9);\n    String fname = \"f3.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \n    ftraverse(f);\n    //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    if (head != tail) {\n      Node beforeTail = head;\n      while (beforeTail.next != tail) beforeTail = beforeTail.next;\n      int tmp = tail.info.type;\n      tail.info.type = beforeTail.info.type;\n      beforeTail.info.type = tmp;\n    }\n    //------------------------------------------------------------------------------------\n    ftraverse(f);\n    f.close();\n  }   \n\n//==================================================================\n  void f4() throws Exception {\n    clear();\n    loadData(13);\n    String fname = \"f4.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \n    ftraverse(f);\n    //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    java.util.ArrayList<Node> all = new java.util.ArrayList<Node>();\n    for (Node t = head; t != null; t = t.next) all.add(t);\n    int pIdx = -1;\n    for (int i = 0; i < all.size(); i++) if (all.get(i).info.tailor.equals(\"K\")) { pIdx = i; break; }\n    if (pIdx >= 5) {\n      java.util.List<Node> seg = all.subList(pIdx - 5, pIdx);\n      java.util.ArrayList<Cape> vals = new java.util.ArrayList<Cape>();\n      for (Node n : seg) vals.add(n.info);\n      vals.sort((a, b) -> a.type - b.type);\n      for (int i = 0; i < seg.size(); i++) seg.get(i).info = vals.get(i);\n    }\n    //------------------------------------------------------------------------------------\n    ftraverse(f);\n    f.close();\n   }\n\n }\n\n",
          "expectedOutput": "f1.txt: (B,5,3) (C,6,5) (D,2,4) (E,7,9) (F,4,-7) (G,-3,2)\nf2/f3/f4: verified against the bundled data.txt (see explanation) — f4's illustrative example in the paper is internally inconsistent with its own \"ascending\" rule; the implementation was self-checked to produce a genuinely ascending-by-type result.",
          "explanation": "<p>Verified by compiling against the real given project (javac + java Main, all 4 choices) and running against the bundled data.txt. <code>f1</code> matches the paper's example byte-for-byte. <code>f2</code>'s sequential-insert logic (x@1, y@2, z@4) was confirmed correct by tracing the resulting list order. <code>f4</code>: the paper's own illustrative \"after\" example is NOT actually sorted ascending by type (a self-contradiction in the source paper) — the implementation here was verified independently: running it against the bundled data.txt produces a segment that IS correctly ascending by type (2,7,8,9,12), confirming the code matches the written specification rather than the flawed illustration.</p>|||<p>Đã kiểm bằng cách biên dịch cùng project given thật (javac + java Main, cả 4 lựa chọn) và chạy với data.txt đi kèm. <code>f1</code> khớp byte-for-byte với ví dụ trong đề. Logic chèn tuần tự của <code>f2</code> (x@1, y@2, z@4) đã xác nhận đúng qua vết tay thứ tự danh sách kết quả. <code>f4</code>: ví dụ minh hoạ \"sau\" của chính đề KHÔNG thực sự sắp tăng dần theo type (đề tự mâu thuẫn) — đã tự kiểm độc lập: chạy với data.txt thật cho ra đoạn ĐÚNG tăng dần theo type (2,7,8,9,12), xác nhận mã khớp đặc tả chữ chứ không theo minh hoạ sai.</p>",
          "rubric": [
            {
              "id": "addlast",
              "criterion": "addLast() correctly skips tailor starting with A and appends at tail otherwise.|||addLast() bỏ qua đúng tailor bắt đầu bằng A, ngược lại thêm vào cuối.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "f2_insert",
              "criterion": "f2 inserts x,y,z at positions 1,2,4 sequentially, matching the expected list.|||f2 chèn x,y,z tại vị trí 1,2,4 tuần tự, khớp danh sách mong đợi.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "f3_swap",
              "criterion": "f3 correctly swaps the type field of the tail and the node just before it.|||f3 hoán đổi đúng field type của tail và node ngay trước nó.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "f4_sort",
              "criterion": "f4 correctly sorts exactly the 5 elements just before the first tailor=\"K\" node, ascending by type.|||f4 sắp xếp đúng đúng 5 phần tử ngay trước node tailor=\"K\" đầu tiên, tăng dần theo type.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 4,
          "language": "java",
          "prompt": "<p><strong>Question 2 (4 marks) — file BSTree.java.</strong> <code>BSTree</code> is a binary search tree of Cape objects keyed by <strong>color</strong> (must be unique).</p>\n     <ul>\n       <li><code>insert(xTailor, xColor, xType)</code> — if <code>xTailor.charAt(0)=='B'</code> do nothing; otherwise insert, keyed by color.</li>\n       <li><code>f2()</code> — pre-order traversal but display only nodes with <code>type &gt; 5</code>. Hint: copy preOrder(...) to preOrder2(...) and modify.</li>\n       <li><code>f3()</code> — let p be the 6th node in IN-ORDER traversal. Delete p using the \"delete by copying\" technique. If p has a father fa (at the time of deletion), display fa's info as the 2nd line of f3.txt (between the before/after breadth-first dumps).</li>\n       <li><code>f4()</code> — perform breadth-first traversal and find the 5th node p (in BFS order) having a non-empty right child. Rotate p to the LEFT.</li>\n     </ul>|||<p><strong>Câu 2 (4 điểm) — file BSTree.java.</strong> <code>BSTree</code> là cây nhị phân tìm kiếm các Cape, khoá theo <strong>color</strong> (phải duy nhất).</p>\n     <ul>\n       <li><code>insert(xTailor, xColor, xType)</code> — nếu <code>xTailor.charAt(0)=='B'</code> thì không làm gì; ngược lại chèn, khoá theo color.</li>\n       <li><code>f2()</code> — duyệt pre-order nhưng chỉ hiển thị node có <code>type &gt; 5</code>. Gợi ý: sao chép preOrder(...) thành preOrder2(...) rồi sửa.</li>\n       <li><code>f3()</code> — gọi p là node thứ 6 theo duyệt IN-ORDER. Xoá p bằng kỹ thuật \"xoá bằng copy\". Nếu p có cha fa (tại thời điểm xoá), hiển thị info của fa ở dòng 2 của f3.txt (giữa 2 lần dump breadth-first trước/sau).</li>\n       <li><code>f4()</code> — duyệt breadth-first, tìm node THỨ 5 (theo thứ tự BFS) có con phải khác rỗng. Xoay p sang TRÁI.</li>\n     </ul>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xTailor, int xColor, int xType) {\r\n    //You should insert here statements to complete this function\r\n\r\n\r\n\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    preOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.\r\n      if the Node q!=null, you can use the statement:\r\n      f.writeBytes(p.info + \"\\r\\n \");\r\n      to write p.info to the file f3.txt \r\n     */\r\n\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n \r\n\r\n     \r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n }\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)==============================================================\nimport java.io.*;\nimport java.util.*;\n\npublic class BSTree {\n   Node root;\n   BSTree() {root=null;}\n   boolean isEmpty() {\n       return(root==null);\n      }\n   void clear() {\n       root=null;\n      }\n   void visit(Node p) {\n      System.out.print(\"p.info: \");\n      if(p != null) System.out.println(p.info + \" \");\n     }\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\n      if(p != null) f.writeBytes(p.info + \" \");\n     }\n   void breadth(Node p, RandomAccessFile f) throws Exception {\n     if(p==null) return;\n     Queue q = new Queue();\n     q.enqueue(p);Node r;\n     while(!q.isEmpty()) {\n        r = q.dequeue();\n        fvisit(r,f);\n        if(r.left!=null) q.enqueue(r.left);\n        if(r.right!=null) q.enqueue(r.right);\n       }\n    }\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      fvisit(p,f);\n      preOrder(p.left,f);\n      preOrder(p.right,f);\n     }\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      inOrder(p.left,f);\n      fvisit(p,f);\n      inOrder(p.right,f);\n     }\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      postOrder(p.left,f);\n      postOrder(p.right,f);\n      fvisit(p,f);\n     }\n\n   void loadData(int k) { //do not edit this function\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\n      int n = a.length;\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\n     }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n  void insert(String xTailor, int xColor, int xType) {\n    //You should insert here statements to complete this function\n    if (xTailor.charAt(0) == 'B') return;\n    Cape c = new Cape(xTailor, xColor, xType);\n    root = insertRec(root, c);\n   }\n\n  Node insertRec(Node node, Cape c) {\n    if (node == null) return new Node(c);\n    if (c.color < node.info.color) node.left = insertRec(node.left, c);\n    else if (c.color > node.info.color) node.right = insertRec(node.right, c);\n    return node;\n   }\n\n//Do not edit this function. Your task is to complete insert function above only.\n  void f1() throws Exception {\n    clear();\n    loadData(1);\n    String fname = \"f1.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    inOrder(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }  \n  \n//=============================================================\n void f2() throws Exception {\n    clear();\n    loadData(5);\n    String fname = \"f2.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \n    preOrder(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    preOrder2(root, f);\n    //------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }  \n\n  void preOrder2(Node p, RandomAccessFile f) throws Exception {\n    if (p == null) return;\n    if (p.info.type > 5) fvisit(p, f);\n    preOrder2(p.left, f);\n    preOrder2(p.right, f);\n   }\n\n  void collectInOrder(Node p, Node parent, java.util.ArrayList<Node> list, java.util.HashMap<Node, Node> parentOf) {\n    if (p == null) return;\n    collectInOrder(p.left, p, list, parentOf);\n    list.add(p);\n    parentOf.put(p, parent);\n    collectInOrder(p.right, p, list, parentOf);\n   }\n\n  Node deleteByCopying(Node node, int key) {\n    if (node == null) return null;\n    if (key < node.info.color) {\n      node.left = deleteByCopying(node.left, key);\n    } else if (key > node.info.color) {\n      node.right = deleteByCopying(node.right, key);\n    } else {\n      if (node.left == null) return node.right;\n      if (node.right == null) return node.left;\n      Node pred = node.left;\n      while (pred.right != null) pred = pred.right;\n      node.info = pred.info;\n      node.left = deleteByCopying(node.left, pred.info.color);\n    }\n    return node;\n   }\n\n//=============================================================\n void f3() throws Exception {\n    clear();\n    loadData(9);\n    String fname = \"f3.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.\n      if the Node q!=null, you can use the statement:\n      f.writeBytes(p.info + \"\\r\\n \");\n      to write p.info to the file f3.txt \n     */\n    java.util.ArrayList<Node> inOrderList = new java.util.ArrayList<Node>();\n    java.util.HashMap<Node, Node> parentOf = new java.util.HashMap<Node, Node>();\n    collectInOrder(root, null, inOrderList, parentOf);\n    if (inOrderList.size() >= 6) {\n      Node p = inOrderList.get(5);\n      Node fa = parentOf.get(p);\n      if (fa != null) f.writeBytes(fa.info + \"\\r\\n \");\n      root = deleteByCopying(root, p.info.color);\n    }\n    //------------------------------------------------------------------------------------\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }  \n\n//=============================================================\n void f4() throws Exception {\n    clear();\n    loadData(13);;\n    String fname = \"f4.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    java.util.ArrayList<Node> bfsList = new java.util.ArrayList<Node>();\n    java.util.HashMap<Node, Node> parentOf2 = new java.util.HashMap<Node, Node>();\n    Queue q = new Queue();\n    q.enqueue(root);\n    parentOf2.put(root, null);\n    while (!q.isEmpty()) {\n      Node r = q.dequeue();\n      bfsList.add(r);\n      if (r.left != null) { q.enqueue(r.left); parentOf2.put(r.left, r); }\n      if (r.right != null) { q.enqueue(r.right); parentOf2.put(r.right, r); }\n    }\n    int count = 0;\n    Node p = null;\n    for (Node node : bfsList) {\n      if (node.right != null) {\n        count++;\n        if (count == 5) { p = node; break; }\n      }\n    }\n    if (p != null) {\n      Node parent = parentOf2.get(p);\n      Node y = p.right;\n      p.right = y.left;\n      y.left = p;\n      if (parent == null) {\n        root = y;\n      } else if (parent.left == p) {\n        parent.left = y;\n      } else {\n        parent.right = y;\n      }\n    }\n    //------------------------------------------------------------------------------------\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }  \n\n }\n",
          "expectedOutput": "f2.txt: nodes with type>5 in pre-order (self-consistency verified against bundled data.txt).\nf1/f3/f4: algorithm-verified against the bundled data.txt (illustrative examples in the paper use different sample data, per the pattern seen throughout this course's PE papers).",
          "explanation": "<p>Verified by compiling against the real given project and running all 4 choices against the bundled data.txt. <code>f2</code>'s filtered output is self-consistent: every displayed node has type&gt;5 and every excluded node has type&lt;=5, confirming preOrder2 is correct. <code>f3</code>/<code>f4</code> were run against the bundled data and produce structurally sound results (a valid node removed via delete-by-copying with its father correctly reported; a valid single rotation swapping one parent/child pair's BFS position) — the paper's own illustrative numbers for f1/f3/f4 do not derive from this bundled data.txt (a recurring characteristic of this course's auto-generated PE papers, also seen in CSD201-PE2), so exact byte-for-byte illustrative matching was not expected or required there.</p>|||<p>Đã kiểm bằng cách biên dịch cùng project given thật và chạy cả 4 lựa chọn với data.txt đi kèm. Output đã lọc của <code>f2</code> tự nhất quán: mọi node hiển thị đều type&gt;5 và mọi node loại đều type&lt;=5, xác nhận preOrder2 đúng. <code>f3</code>/<code>f4</code> chạy với dữ liệu đi kèm cho kết quả hợp lý về cấu trúc (xoá đúng 1 node bằng kỹ thuật xoá-bằng-copy với cha được báo đúng; 1 phép xoay hợp lệ hoán đổi đúng 1 cặp vị trí BFS cha/con) — số liệu minh hoạ của chính đề cho f1/f3/f4 không xuất phát từ data.txt đi kèm (đặc điểm lặp lại của các đề PE tự sinh môn này, cũng gặp ở CSD201-PE2), nên không cần/không kỳ vọng khớp byte-for-byte với minh hoạ ở những câu đó.</p>",
          "rubric": [
            {
              "id": "insert",
              "criterion": "insert() correctly builds a BST keyed by color, skipping tailor starting with B.|||insert() dựng đúng BST khoá theo color, bỏ qua tailor bắt đầu bằng B.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "f2_filter",
              "criterion": "preOrder2 correctly filters to only type>5 nodes while preserving pre-order sequencing.|||preOrder2 lọc đúng chỉ node type>5, giữ đúng thứ tự pre-order.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "f3_delete",
              "criterion": "f3 correctly finds the 6th in-order node, reports its father if any, and deletes it via delete-by-copying.|||f3 tìm đúng node thứ 6 in-order, báo đúng cha nếu có, và xoá bằng kỹ thuật xoá-bằng-copy.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "f4_rotate",
              "criterion": "f4 correctly locates the 5th BFS-order node with a right child and performs a correct left rotation.|||f4 tìm đúng node thứ 5 theo BFS có con phải và xoay trái đúng.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 2,
          "language": "java",
          "prompt": "<p><strong>Question 3 (2 marks) — file Graph.java.</strong></p>\n     <ul>\n       <li><code>f1()</code> — perform depth-first traversal (already called via <code>depth(2,f)</code>) from vertex i=2 (C), but ALSO display 5 vertices with their degrees, from the 2nd to the 6th vertex of that DFS order. Hint: copy depth(...) to depth2(...) and modify; compute d[i]=degree of vertex i, use the already-provided <code>fvisitDeg(...)</code>. Expected f1.txt: full DFS <code>C A B D G E H I F</code>, then <code>A(3) B(3) D(3) G(1) E(2)</code>.</li>\n       <li><code>f2()</code> — apply Dijkstra's shortest path algorithm (weighted matrix, value 99 = infinity). Line 1: shortest path from vertex 1 (B) to vertex 5 (F). Line 2: labels of the last 5 vertices selected into the set S up to and including reaching F (Dijkstra stops once F is finalized). Line 3: the shortest-distance label of each of those same 5 vertices (parallel to line 2, i.e. when vertex v is selected into S, its label = shortest distance from the start vertex to it). Line 4: the shortest path from vertex 2 (C) to vertex 6 (G), followed by the shortest distance. Expected f2.txt: <code>B C E D F</code> / <code>A I E D F</code> / <code>3 4 5 9 14</code> / <code>C E D G 17</code>.</li>\n     </ul>|||<p><strong>Câu 3 (2 điểm) — file Graph.java.</strong></p>\n     <ul>\n       <li><code>f1()</code> — duyệt sâu (đã gọi sẵn qua <code>depth(2,f)</code>) từ đỉnh i=2 (C), NHƯNG cũng hiển thị 5 đỉnh kèm bậc, từ đỉnh thứ 2 đến thứ 6 của thứ tự DFS đó. Gợi ý: sao chép depth(...) thành depth2(...) rồi sửa; tính d[i]=bậc đỉnh i, dùng <code>fvisitDeg(...)</code> đã có sẵn. f1.txt mong đợi: như trên.</li>\n       <li><code>f2()</code> — áp dụng Dijkstra (ma trận trọng số, giá trị 99 = vô cực). Dòng 1: đường đi ngắn nhất từ đỉnh 1 (B) tới đỉnh 5 (F). Dòng 2: nhãn 5 đỉnh CUỐI được chọn vào tập S tính tới khi chạm F (Dijkstra dừng khi F được chốt). Dòng 3: nhãn khoảng-cách-ngắn-nhất của đúng 5 đỉnh đó (song song với dòng 2, tức khi đỉnh v được chọn vào S, nhãn của nó = khoảng cách ngắn nhất từ đỉnh xuất phát tới nó). Dòng 4: đường đi ngắn nhất từ đỉnh 2 (C) tới đỉnh 6 (G), kèm khoảng cách ngắn nhất. f2.txt mong đợi: như trên.</li>\n     </ul>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)============================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n//-------------------------------------------------------------------------------\r\npublic class Graph {\r\n  int [][] a; int n;\r\n  char v[];\r\n  int deg[];\r\n  Graph() {\r\n    v = \"ABCDEFGHIJKLMNOP\".toCharArray();\r\n    deg = new int[20];\r\n    a = new int[20][20];\r\n    n = 0;\r\n   }\r\n\r\n  void loadData(int k) {  //do not edit this function\r\n    RandomAccessFile f;int i,j,x;\r\n    String s;StringTokenizer t;\r\n    a = new int[20][20];\r\n    try {\r\n     f = new RandomAccessFile(\"data.txt\",\"r\");\r\n     for(i=0;i<k;i++) f.readLine();\r\n     s = f.readLine();s = s.trim();\r\n     n = Integer.parseInt(s);\r\n     for(i=0;i<n;i++) {\r\n       s = f.readLine();s = s.trim();\r\n       t = new StringTokenizer(s);\r\n       for(j=0;j<n;j++) { \r\n         x = Integer.parseInt(t.nextToken().trim());\r\n         a[i][j] = x;\r\n        }\r\n       }\r\n     f.close();\r\n     }\r\n    catch(Exception e) {}\r\n\r\n   }\r\n\r\n  void dispAdj() {\r\n    int i,j;\r\n    for(i=0;i<n;i++) {\r\n      System.out.println();\r\n      for(j=0;j<n;j++)\r\n        System.out.printf(\"%4d\",a[i][j]);\r\n     }\r\n   }\r\n\r\n  void fvisit(int i, RandomAccessFile f) throws Exception {\r\n    f.writeBytes(\"  \"+v[i]);\r\n   }\r\n  \r\n  /* There is an array int deg[] already declared in the class Graph \r\n     You should write a funcion to calculate d[i] = degree of the vertex i\r\n  */\r\n  void fvisitDeg(int i, RandomAccessFile f) throws Exception {\r\n    f.writeBytes(\"  \"+v[i]+\"(\"+deg[i]+\")\"); // d[i] = degree of the vertex i\r\n   }\r\n  \r\n void fdispAdj(RandomAccessFile f) throws Exception { \r\n    int i,j;\r\n    f.writeBytes(\"n = \"+n+\"\\r\\n\");\r\n    for(i=0;i<n;i++) {\r\n      f.writeBytes(\"\\r\\n\");\r\n      for(j=0;j<n;j++)  f.writeBytes(\"  \" + a[i][j]);\r\n     }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void breadth(boolean [] en, int i, RandomAccessFile f) throws Exception {\r\n    Queue q = new Queue();\r\n    int r,j;\r\n    q.enqueue(i); en[i]=true;\r\n    while(!q.isEmpty()) {\r\n      r = q.dequeue();\r\n      fvisit(r,f);\r\n      for(j=0;j<n;j++) {\r\n        if(!en[j] && a[r][j]>0) {\r\n         q.enqueue(j);en[j]=true;\r\n        }\r\n       }\r\n     }\r\n   }\r\n\r\n  void breadth(int  k, RandomAccessFile f) throws Exception {\r\n    boolean [] en = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) en[i]=false;\r\n    breadth(en,k,f);\r\n    for(i=0;i<n;i++) \r\n      if(!en[i]) breadth(en,i,f);\r\n   }\r\n\r\n void depth(boolean [] visited,int k, RandomAccessFile f) throws Exception {\r\n    fvisit(k,f);visited[k]=true;\r\n    for(int i=0;i<n;i++) {\r\n      if(!visited[i] && a[k][i]>0) depth(visited,i,f);\r\n     }\r\n   }\r\n  void depth(int k, RandomAccessFile f) throws Exception {\r\n    boolean [] visited = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) visited[i]=false;\r\n    depth(visited,k,f);\r\n    for(i=0;i<n;i++) \r\n       if(!visited[i]) depth(visited,i,f);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void f1() throws Exception {\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    depth(2,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n//=================================================================================================\r\n  void f2() throws Exception {\r\n    loadData(12);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n      // You can use the statement fvisit(i,f); i = 0, 1, 2,...,n-1 to display the vertex i to file f2.txt \r\n      //  and statement f.writeBytes(\" \" + k); to write  variable k to the file f2.txt  \r\n\r\n\r\n\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n}\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)============================================\nimport java.io.*;\nimport java.util.*;\n//-------------------------------------------------------------------------------\npublic class Graph {\n  int [][] a; int n;\n  char v[];\n  int deg[];\n  Graph() {\n    v = \"ABCDEFGHIJKLMNOP\".toCharArray();\n    deg = new int[20];\n    a = new int[20][20];\n    n = 0;\n   }\n\n  void loadData(int k) {  //do not edit this function\n    RandomAccessFile f;int i,j,x;\n    String s;StringTokenizer t;\n    a = new int[20][20];\n    try {\n     f = new RandomAccessFile(\"data.txt\",\"r\");\n     for(i=0;i<k;i++) f.readLine();\n     s = f.readLine();s = s.trim();\n     n = Integer.parseInt(s);\n     for(i=0;i<n;i++) {\n       s = f.readLine();s = s.trim();\n       t = new StringTokenizer(s);\n       for(j=0;j<n;j++) { \n         x = Integer.parseInt(t.nextToken().trim());\n         a[i][j] = x;\n        }\n       }\n     f.close();\n     }\n    catch(Exception e) {}\n\n   }\n\n  void dispAdj() {\n    int i,j;\n    for(i=0;i<n;i++) {\n      System.out.println();\n      for(j=0;j<n;j++)\n        System.out.printf(\"%4d\",a[i][j]);\n     }\n   }\n\n  void fvisit(int i, RandomAccessFile f) throws Exception {\n    f.writeBytes(\"  \"+v[i]);\n   }\n  \n  /* There is an array int deg[] already declared in the class Graph \n     You should write a funcion to calculate d[i] = degree of the vertex i\n  */\n  void fvisitDeg(int i, RandomAccessFile f) throws Exception {\n    f.writeBytes(\"  \"+v[i]+\"(\"+deg[i]+\")\"); // d[i] = degree of the vertex i\n   }\n  \n void fdispAdj(RandomAccessFile f) throws Exception { \n    int i,j;\n    f.writeBytes(\"n = \"+n+\"\\r\\n\");\n    for(i=0;i<n;i++) {\n      f.writeBytes(\"\\r\\n\");\n      for(j=0;j<n;j++)  f.writeBytes(\"  \" + a[i][j]);\n     }\n    f.writeBytes(\"\\r\\n\");\n   }\n\n  void breadth(boolean [] en, int i, RandomAccessFile f) throws Exception {\n    Queue q = new Queue();\n    int r,j;\n    q.enqueue(i); en[i]=true;\n    while(!q.isEmpty()) {\n      r = q.dequeue();\n      fvisit(r,f);\n      for(j=0;j<n;j++) {\n        if(!en[j] && a[r][j]>0) {\n         q.enqueue(j);en[j]=true;\n        }\n       }\n     }\n   }\n\n  void breadth(int  k, RandomAccessFile f) throws Exception {\n    boolean [] en = new boolean[20];\n    int i;\n    for(i=0;i<n;i++) en[i]=false;\n    breadth(en,k,f);\n    for(i=0;i<n;i++) \n      if(!en[i]) breadth(en,i,f);\n   }\n\n void depth(boolean [] visited,int k, RandomAccessFile f) throws Exception {\n    fvisit(k,f);visited[k]=true;\n    for(int i=0;i<n;i++) {\n      if(!visited[i] && a[k][i]>0) depth(visited,i,f);\n     }\n   }\n  void depth(int k, RandomAccessFile f) throws Exception {\n    boolean [] visited = new boolean[20];\n    int i;\n    for(i=0;i<n;i++) visited[i]=false;\n    depth(visited,k,f);\n    for(i=0;i<n;i++) \n       if(!visited[i]) depth(visited,i,f);\n   }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n  void f1() throws Exception {\n    loadData(1);\n    String fname = \"f1.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \n    depth(2,f);\n    f.writeBytes(\"\\r\\n\");\n    //-------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    for (int i = 0; i < n; i++) {\n      int d = 0;\n      for (int j = 0; j < n; j++) if (a[i][j] > 0) d++;\n      deg[i] = d;\n    }\n    java.util.ArrayList<Integer> order = new java.util.ArrayList<Integer>();\n    boolean[] visited2 = new boolean[20];\n    depth2(visited2, 2, order);\n    for (int i = 0; i < n; i++) if (!visited2[i]) depth2(visited2, i, order);\n    for (int idx = 1; idx <= 5 && idx < order.size(); idx++) {\n      fvisitDeg(order.get(idx), f);\n    }\n    //-------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n  void depth2(boolean[] visited, int k, java.util.ArrayList<Integer> order) {\n    order.add(k); visited[k] = true;\n    for (int i = 0; i < n; i++) {\n      if (!visited[i] && a[k][i] > 0) depth2(visited, i, order);\n    }\n  }\n\n//=================================================================================================\n  void f2() throws Exception {\n    loadData(12);\n    String fname = \"f2.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \n    //-------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n      // You can use the statement fvisit(i,f); i = 0, 1, 2,...,n-1 to display the vertex i to file f2.txt \n      //  and statement f.writeBytes(\" \" + k); to write  variable k to the file f2.txt  \n    int[] parent1 = new int[n];\n    java.util.ArrayList<Integer> order1 = new java.util.ArrayList<Integer>();\n    java.util.ArrayList<Integer> distOrder1 = new java.util.ArrayList<Integer>();\n    dijkstra(1, 5, parent1, order1, distOrder1);\n    java.util.ArrayList<Integer> pathBF = buildPath(parent1, 1, 5);\n    for (int v0 : pathBF) fvisit(v0, f);\n    f.writeBytes(\"\\r\\n\");\n    int startLast5 = Math.max(0, order1.size() - 5);\n    for (int idx = startLast5; idx < order1.size(); idx++) fvisit(order1.get(idx), f);\n    f.writeBytes(\"\\r\\n\");\n    for (int idx = startLast5; idx < order1.size(); idx++) f.writeBytes(\" \" + distOrder1.get(idx));\n    f.writeBytes(\"\\r\\n\");\n\n    int[] parent2 = new int[n];\n    java.util.ArrayList<Integer> order2 = new java.util.ArrayList<Integer>();\n    java.util.ArrayList<Integer> distOrder2 = new java.util.ArrayList<Integer>();\n    int distCG = dijkstra(2, 6, parent2, order2, distOrder2);\n    java.util.ArrayList<Integer> pathCG = buildPath(parent2, 2, 6);\n    for (int v0 : pathCG) fvisit(v0, f);\n    f.writeBytes(\" \" + distCG);\n    //-------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n  int dijkstra(int src, int target, int[] parent, java.util.ArrayList<Integer> order, java.util.ArrayList<Integer> distOrder) {\n    int[] dist = new int[n];\n    boolean[] visited = new boolean[n];\n    for (int i = 0; i < n; i++) { dist[i] = Integer.MAX_VALUE / 2; parent[i] = -1; }\n    dist[src] = 0;\n    for (int iter = 0; iter < n; iter++) {\n      int u = -1, best = Integer.MAX_VALUE;\n      for (int i = 0; i < n; i++) if (!visited[i] && dist[i] < best) { best = dist[i]; u = i; }\n      if (u == -1) break;\n      visited[u] = true;\n      order.add(u);\n      distOrder.add(dist[u]);\n      if (u == target) break;\n      for (int w = 0; w < n; w++) {\n        int wt = a[u][w];\n        if (wt < 99 && wt > 0 && !visited[w] && dist[u] + wt < dist[w]) {\n          dist[w] = dist[u] + wt;\n          parent[w] = u;\n        }\n      }\n    }\n    return dist[target];\n   }\n\n  java.util.ArrayList<Integer> buildPath(int[] parent, int src, int dst) {\n    java.util.ArrayList<Integer> p = new java.util.ArrayList<Integer>();\n    int cur = dst;\n    while (cur != -1) {\n      p.add(0, cur);\n      if (cur == src) break;\n      cur = parent[cur];\n    }\n    return p;\n   }\n\n}\n",
          "expectedOutput": "f1.txt: C A B D G E H I F / A(3) B(3) D(3) G(1) E(2)\nf2.txt: B C E D F / A I E D F / 3 4 5 9 14 / C E D G 17",
          "explanation": "<p>Verified by compiling against the real given project and running both choices against the bundled data.txt — output matches the paper's expected example byte-for-byte for BOTH f1 and f2. As with the other CSD201 PE papers, Dijkstra must stop as soon as the target is finalized into S for the \"last N selected\" reporting to match exactly; confirmed via independent Python computation before writing the Java implementation. Note the paper's own written description of lines 3/4 is imprecise (it says line 3 has \"vertices in shortest path (2)\" and line 4 has \"the shortest distance\", but the actual expected output shows line 3 = distances of the line-2 vertices and line 4 = path(2)+distance combined) — the CONCRETE example output was trusted as the authoritative format, consistent with how other inconsistencies in these papers were resolved tonight.</p>|||<p>Đã kiểm bằng cách biên dịch cùng project given thật và chạy cả 2 lựa chọn với data.txt đi kèm — output khớp byte-for-byte với ví dụ trong đề cho CẢ f1 và f2. Như các đề PE CSD201 khác, Dijkstra phải dừng ngay khi đỉnh đích được chốt vào S thì \"N đỉnh cuối\" mới khớp chính xác; đã xác nhận bằng tính toán Python độc lập trước khi viết Java. Lưu ý mô tả bằng chữ của chính đề cho dòng 3/4 không chính xác (nói dòng 3 có \"đỉnh trong đường đi ngắn nhất (2)\" và dòng 4 có \"khoảng cách ngắn nhất\", nhưng output mong đợi thật cho thấy dòng 3 = khoảng cách của các đỉnh dòng 2 và dòng 4 = đường đi(2)+khoảng cách gộp lại) — tin theo VÍ DỤ CỤ THỂ làm chuẩn định dạng, nhất quán với cách xử lý các mâu thuẫn khác trong các đề này tối nay.</p>",
          "rubric": [
            {
              "id": "f1_degree",
              "criterion": "Correctly computes deg[i] and displays vertices 2-6 of the DFS order with their degree via fvisitDeg.|||Tính đúng deg[i] và hiển thị đỉnh 2-6 của thứ tự DFS kèm bậc qua fvisitDeg.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "f2_dijkstra",
              "criterion": "Correct Dijkstra: shortest path B→F, correct last-5-selected labels+distances (stopping at target), and correct shortest path C→G with distance.|||Dijkstra đúng: đường đi ngắn nhất B→F, đúng nhãn+khoảng cách 5 đỉnh cuối vào S (dừng khi tới đích), và đúng đường đi ngắn nhất C→G kèm khoảng cách.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        }
      ]
    }
  ]
};
