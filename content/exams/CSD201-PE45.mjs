export default {
  "course": {
    "courseCode": "CSD201"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "CODE",
      "code": "PE45",
      "title": "PE Đề 45 — Practical Exam (SU2023, Hola Slot 1)|||PE Đề 45 — Thi thực hành (SU2023, Hola Slot 1)",
      "description": "CSD201 PE (CODE): complete MyList<Boat> (4 marks) + BSTree<Boat> (4 marks) + Graph (2 marks) across 3 given NetBeans projects; every expected output verified against the original exam paper by compiling and running the real project, AI-graded.|||PE CSD201 (viết mã): hoàn thiện MyList<Boat> (4 điểm) + BSTree<Boat> (4 điểm) + Graph (2 điểm) trên 3 project NetBeans given; mọi kết quả mong đợi đã đối chiếu với đề gốc bằng cách biên dịch và chạy thật project, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "attachmentUrl": "https://media.cuongthai.com/files/exam-attachments/CSD201/PE45-Given.zip",
      "attachmentName": "CSD201-PE45-Given.zip",
      "instructions": "<div class=\"ml-en\"><p><strong>CSD201 PE INSTRUCTIONS</strong> (Summer 2023, Hola Slot 1 — PaperNo_4). Read the instructions carefully before start coding.</p>\n   <ol>\n     <li>Software: NetBeans IDE 8.x, Java JDK 1.8. Create a folder to save the given projects, e.g. CSD_given. Download the given materials above into it — it contains 3 separate NetBeans projects: Q1 (MyList), Q2 (BSTree), Q3 (Graph).</li>\n     <li>Steps for question 1 (do the same for questions 2 and 3): open NetBeans, open the given Q1 project, then edit MyList.java according to the requirements below (edit BSTree.java for Q2 and Graph.java for Q3).</li>\n     <li>Before submission: run \"Clean and Build Project\" (Shift+F11) to ensure BUILD SUCCESSFUL — if not, the project gets 0 mark.</li>\n     <li>Do not use accented Vietnamese when writing comments in programs. Do not add new import statement(s) to the given files.</li>\n     <li>Do not pay attention to the real meaning of objects, variables and their values in the questions below.</li>\n     <li>This exam room cannot run NetBeans/JDK in the browser, so type your code directly into each code box — it is graded by AI against the rubric. You can build &amp; run the real given projects on your own machine and cross-check against the sample runs shown below.</li>\n   </ol></div><div class=\"ml-vi\"><p><strong>HƯỚNG DẪN PE CSD201</strong> (Summer 2023, Hola Slot 1 — PaperNo_4). Đọc kỹ hướng dẫn trước khi bắt đầu viết mã.</p>\n   <ol>\n     <li>Phần mềm: NetBeans IDE 8.x, Java JDK 1.8. Tạo một thư mục để lưu project given, ví dụ CSD_given. Tải given materials ở trên vào đó — gồm 3 project NetBeans riêng: Q1 (MyList), Q2 (BSTree), Q3 (Graph).</li>\n     <li>Các bước cho câu 1 (làm tương tự cho câu 2 và 3): mở NetBeans, mở project Q1 given, rồi sửa MyList.java theo yêu cầu bên dưới (sửa BSTree.java cho Q2 và Graph.java cho Q3).</li>\n     <li>Trước khi nộp: chạy \"Clean and Build Project\" (Shift+F11) để chắc chắn BUILD SUCCESSFUL — nếu không, project bị 0 điểm.</li>\n     <li>Không dùng tiếng Việt có dấu khi viết chú thích trong chương trình. Không thêm import mới vào các file cho sẵn.</li>\n     <li>Không cần để ý ý nghĩa thật của đối tượng, biến và giá trị của chúng trong các câu dưới đây.</li>\n     <li>Phòng thi web không chạy được NetBeans/JDK trực tiếp nên gõ thẳng mã vào ô, được AI chấm theo tiêu chí — bạn có thể build &amp; chạy project given thật trên máy mình rồi đối chiếu với mẫu chạy bên dưới.</li>\n   </ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 1 (4 marks) — Scenario:</strong> in this question you should complete some methods in <code>MyList.java</code>. The class <code>Boat</code> with 3 data members (sea, sail, paddle) is given and you do not need to edit it. The <code>MyList</code> class is a linked list of Boat objects. Each of the 4 methods below is graded independently against its own worked example.</p><p><strong>1.1 addLast() – 1 mark</strong></p><ul><li>File: MyList.java</li><li>Method to Complete: <code>void addLast(String xSea, int xSail, int xPaddle)</code></li><li>Task: check if <code>xSea.charAt(0) == 'A'</code> then <b>do nothing</b>, otherwise add a new node with sea=xSea, sail=xSail, paddle=xPaddle to the end of the list. (sail and paddle can get arbitrary, even negative values.)</li><li><code>void f1()</code> — do not edit this method; your task is to complete <code>addLast(...)</code> only. Its result is written to f1.txt.</li></ul><p><b>Note:</b> the expected output below is the real output of compiling and running the given project (javac/java) on its own <code>data.txt</code>, and it matches the worked example printed in the original exam paper character for character.</p>|||<p><strong>Câu 1 (4 điểm) — Bối cảnh:</strong> ở câu này bạn cần hoàn thiện một số method trong <code>MyList.java</code>. Lớp <code>Boat</code> với 3 thành viên dữ liệu (sea, sail, paddle) đã cho sẵn, không cần sửa. Lớp <code>MyList</code> là danh sách liên kết các đối tượng Boat. Mỗi trong 4 method dưới đây được chấm độc lập theo đúng ví dụ minh hoạ của nó.</p><p><strong>1.1 addLast() – 1 điểm</strong></p><ul><li>File: MyList.java</li><li>Method cần hoàn thiện: <code>void addLast(String xSea, int xSail, int xPaddle)</code></li><li>Nhiệm vụ: kiểm tra nếu <code>xSea.charAt(0) == 'A'</code> thì <b>không làm gì</b>; ngược lại thêm node mới sea=xSea, sail=xSail, paddle=xPaddle vào CUỐI danh sách. (sail và paddle có thể nhận giá trị bất kỳ, kể cả âm.)</li><li><code>void f1()</code> — không sửa method này; nhiệm vụ của bạn chỉ là hoàn thiện <code>addLast(...)</code>. Kết quả được ghi ra f1.txt.</li></ul><p><b>Lưu ý:</b> kết quả mong đợi dưới đây là output THẬT của việc biên dịch và chạy project given (javac/java) trên chính <code>data.txt</code> của nó, và khớp từng ký tự với ví dụ minh hoạ in trong đề gốc.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xSea, int xSail, int xPaddle) {\r\n    //You should write here appropriate statements to complete this function.\r\n\r\n\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Boat x, y, z;\r\n     x = new Boat(\"X\",1,2);\r\n     y = new Boat(\"Y\",2,3);\r\n     z = new Boat(\"Z\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n \r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n  }   \r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)==============================================================\nimport java.util.*;\nimport java.io.*;\n\npublic class MyList {\n  Node head,tail;\n  MyList() {head=tail=null;}\n  boolean isEmpty() {\n    return(head==null);\n   }\n  void clear() {head=tail=null;}\n\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\n    if(p != null) f.writeBytes(p.info + \" \");\n   }\n\n  void ftraverse(RandomAccessFile f) throws Exception {\n    Node p = head;\n    while(p!=null) {\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\n       p=p.next;\n      }\n    f.writeBytes(\"\\r\\n\");\n   }\n\n  void loadData(int k) { //do not edit this function\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\n    int n = a.length;\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\n   }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n/*\n   Khong su dung tieng Viet co dau de viet ghi chu.\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\n*/\n  void addLast(String xSea, int xSail, int xPaddle) {\n    //You should write here appropriate statements to complete this function.\n    if(xSea == null || xSea.length() == 0) return;\n    if(xSea.charAt(0) == 'A') return;   // do nothing when the sea starts with 'A'\n    Node q = new Node(new Boat(xSea, xSail, xPaddle));\n    if(isEmpty()) head = tail = q;\n    else { tail.next = q; tail = q; }\n   }\n\n  //Helper: insert the Boat x so that it becomes the node at position pos (head's position is 0)\n  void insertAt(int pos, Boat x) {\n    if(pos <= 0) {\n      Node q = new Node(x, head);\n      head = q;\n      if(tail == null) tail = q;\n      return;\n     }\n    Node p = head; int i = 0;\n    while(p != null && i < pos-1) { p = p.next; i++; }\n    if(p == null) return;\n    Node q = new Node(x, p.next);\n    p.next = q;\n    if(q.next == null) tail = q;\n   }\n\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\n  void f1() throws Exception {\n     clear();\n     loadData(1);\n     String fname = \"f1.txt\";\n     File g123 = new File(fname);\n     if(g123.exists()) g123.delete();\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n     ftraverse(f);\n     f.close();\n    }\n\n//==================================================================\n  void f2() throws Exception {\n     clear();\n     loadData(5);\n     String fname = \"f2.txt\";\n     File g123 = new File(fname);\n     if(g123.exists()) g123.delete();\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n     ftraverse(f);\n     Boat x, y, z;\n     x = new Boat(\"X\",1,2);\n     y = new Boat(\"Y\",2,3);\n     z = new Boat(\"Z\",3,4);\n     //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n     insertAt(1, x);\n     insertAt(3, y);\n     insertAt(4, z);\n\n    //------------------------------------------------------------------------------------\n     ftraverse(f);\n     f.close();\n    }\n\n//==================================================================\n  void f3() throws Exception {\n    clear();\n    loadData(9);\n    String fname = \"f3.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    ftraverse(f);\n    //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    int max = Integer.MIN_VALUE;\n    for(Node p = head; p != null; p = p.next)\n      if(p.info.paddle > max) max = p.info.paddle;\n    Node target = null, tprev = null, prev = null;\n    int count = 0;\n    for(Node p = head; p != null; p = p.next) {\n       if(p.info.paddle == max) {\n          count++;\n          if(count == 2) { target = p; tprev = prev; break; }\n         }\n       prev = p;\n      }\n    if(target != null && target != tail) {\n       if(tprev == null) head = target.next;\n       else tprev.next = target.next;\n       target.next = null;\n       tail.next = target;\n       tail = target;\n      }\n\n    //------------------------------------------------------------------------------------\n    ftraverse(f);\n    f.close();\n  }\n\n//==================================================================\n  void f4() throws Exception {\n    clear();\n    loadData(13);\n    String fname = \"f4.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    ftraverse(f);\n    //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    Node start = head;\n    int i;\n    for(i = 0; i < 2 && start != null; i++) start = start.next;\n    Boat [] arr = new Boat[5];\n    int m = 0;\n    Node p = start;\n    while(p != null && m < 5) { arr[m++] = p.info; p = p.next; }\n    for(i = 0; i < m-1; i++)\n      for(int j = i+1; j < m; j++)\n        if(arr[j].paddle < arr[i].paddle) { Boat t = arr[i]; arr[i] = arr[j]; arr[j] = t; }\n    p = start; m = 0;\n    while(p != null && m < 5) { p.info = arr[m++]; p = p.next; }\n\n    //------------------------------------------------------------------------------------\n    ftraverse(f);\n    f.close();\n   }\n\n }\n",
          "expectedOutput": "(B,5,3) (C,6,5) (D,2,4) (E,7,9) (F,4,-7) (G,-3,2)",
          "explanation": "<p>data.txt supplies sea = A B C D E F G, sail = 9 5 6 2 7 4 -3, paddle = 8 3 5 4 9 -7 2. Only the first candidate, \"A\", starts with 'A', so it is skipped; the remaining 6 are appended in the order they are read. Note that a negative sail or paddle is perfectly legal — (F,4,-7) and (G,-3,2) must still be inserted.</p>|||<p>data.txt cho sea = A B C D E F G, sail = 9 5 6 2 7 4 -3, paddle = 8 3 5 4 9 -7 2. Chỉ ứng viên đầu tiên \"A\" bắt đầu bằng 'A' nên bị bỏ qua; 6 phần tử còn lại được nối vào cuối theo đúng thứ tự đọc. Lưu ý sail hay paddle âm vẫn hợp lệ — (F,4,-7) và (G,-3,2) vẫn phải được chèn.</p>",
          "rubric": [
            {
              "id": "sea_filter",
              "criterion": "Correctly does nothing when xSea.charAt(0) == 'A' (and does NOT filter on sail/paddle sign).|||Không làm gì đúng khi xSea.charAt(0) == 'A' (và KHÔNG lọc theo dấu của sail/paddle).",
              "weight": 1,
              "maxScore": 0.4
            },
            {
              "id": "append",
              "criterion": "Correctly appends the new node at the end of the list, updating head/tail for both the empty and non-empty cases.|||Nối đúng node mới vào cuối danh sách, cập nhật head/tail đúng cho cả trường hợp danh sách rỗng và không rỗng.",
              "weight": 1,
              "maxScore": 0.6
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>1.2 f2() – 1 mark: insert x, y, z at positions 1, 3 and 4</strong></p><ul><li>File: MyList.java</li><li>Method to Complete: the body of <code>void f2()</code>, just after the marked comment (keep every pre-given statement)</li><li>Task: there are 3 given Boat objects x, y, z in this function. Suppose the list contains at least 3 elements. Write statements to insert x, y and z into the list so that x, y, z will be at positions 1, 3 and 4 (the head's position is 0).</li></ul><p><b>Note:</b> the expected output below is the real output of compiling and running the given project (javac/java) on its own <code>data.txt</code>, and it matches the worked example printed in the original exam paper character for character.</p>|||<p><strong>1.2 f2() – 1 điểm: chèn x, y, z vào vị trí 1, 3 và 4</strong></p><ul><li>File: MyList.java</li><li>Method cần hoàn thiện: phần thân <code>void f2()</code>, ngay sau chú thích đánh dấu (giữ nguyên mọi câu lệnh cho sẵn)</li><li>Nhiệm vụ: trong hàm này có 3 đối tượng Boat x, y, z cho sẵn. Giả sử danh sách có ít nhất 3 phần tử. Viết lệnh chèn x, y và z vào danh sách sao cho x, y, z nằm ở vị trí 1, 3 và 4 (vị trí của head là 0).</li></ul><p><b>Lưu ý:</b> kết quả mong đợi dưới đây là output THẬT của việc biên dịch và chạy project given (javac/java) trên chính <code>data.txt</code> của nó, và khớp từng ký tự với ví dụ minh hoạ in trong đề gốc.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xSea, int xSail, int xPaddle) {\r\n    //You should write here appropriate statements to complete this function.\r\n\r\n\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Boat x, y, z;\r\n     x = new Boat(\"X\",1,2);\r\n     y = new Boat(\"Y\",2,3);\r\n     z = new Boat(\"Z\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n \r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n  }   \r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)==============================================================\nimport java.util.*;\nimport java.io.*;\n\npublic class MyList {\n  Node head,tail;\n  MyList() {head=tail=null;}\n  boolean isEmpty() {\n    return(head==null);\n   }\n  void clear() {head=tail=null;}\n\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\n    if(p != null) f.writeBytes(p.info + \" \");\n   }\n\n  void ftraverse(RandomAccessFile f) throws Exception {\n    Node p = head;\n    while(p!=null) {\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\n       p=p.next;\n      }\n    f.writeBytes(\"\\r\\n\");\n   }\n\n  void loadData(int k) { //do not edit this function\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\n    int n = a.length;\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\n   }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n/*\n   Khong su dung tieng Viet co dau de viet ghi chu.\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\n*/\n  void addLast(String xSea, int xSail, int xPaddle) {\n    //You should write here appropriate statements to complete this function.\n    if(xSea == null || xSea.length() == 0) return;\n    if(xSea.charAt(0) == 'A') return;   // do nothing when the sea starts with 'A'\n    Node q = new Node(new Boat(xSea, xSail, xPaddle));\n    if(isEmpty()) head = tail = q;\n    else { tail.next = q; tail = q; }\n   }\n\n  //Helper: insert the Boat x so that it becomes the node at position pos (head's position is 0)\n  void insertAt(int pos, Boat x) {\n    if(pos <= 0) {\n      Node q = new Node(x, head);\n      head = q;\n      if(tail == null) tail = q;\n      return;\n     }\n    Node p = head; int i = 0;\n    while(p != null && i < pos-1) { p = p.next; i++; }\n    if(p == null) return;\n    Node q = new Node(x, p.next);\n    p.next = q;\n    if(q.next == null) tail = q;\n   }\n\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\n  void f1() throws Exception {\n     clear();\n     loadData(1);\n     String fname = \"f1.txt\";\n     File g123 = new File(fname);\n     if(g123.exists()) g123.delete();\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n     ftraverse(f);\n     f.close();\n    }\n\n//==================================================================\n  void f2() throws Exception {\n     clear();\n     loadData(5);\n     String fname = \"f2.txt\";\n     File g123 = new File(fname);\n     if(g123.exists()) g123.delete();\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n     ftraverse(f);\n     Boat x, y, z;\n     x = new Boat(\"X\",1,2);\n     y = new Boat(\"Y\",2,3);\n     z = new Boat(\"Z\",3,4);\n     //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n     insertAt(1, x);\n     insertAt(3, y);\n     insertAt(4, z);\n\n    //------------------------------------------------------------------------------------\n     ftraverse(f);\n     f.close();\n    }\n\n//==================================================================\n  void f3() throws Exception {\n    clear();\n    loadData(9);\n    String fname = \"f3.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    ftraverse(f);\n    //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    int max = Integer.MIN_VALUE;\n    for(Node p = head; p != null; p = p.next)\n      if(p.info.paddle > max) max = p.info.paddle;\n    Node target = null, tprev = null, prev = null;\n    int count = 0;\n    for(Node p = head; p != null; p = p.next) {\n       if(p.info.paddle == max) {\n          count++;\n          if(count == 2) { target = p; tprev = prev; break; }\n         }\n       prev = p;\n      }\n    if(target != null && target != tail) {\n       if(tprev == null) head = target.next;\n       else tprev.next = target.next;\n       target.next = null;\n       tail.next = target;\n       tail = target;\n      }\n\n    //------------------------------------------------------------------------------------\n    ftraverse(f);\n    f.close();\n  }\n\n//==================================================================\n  void f4() throws Exception {\n    clear();\n    loadData(13);\n    String fname = \"f4.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    ftraverse(f);\n    //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    Node start = head;\n    int i;\n    for(i = 0; i < 2 && start != null; i++) start = start.next;\n    Boat [] arr = new Boat[5];\n    int m = 0;\n    Node p = start;\n    while(p != null && m < 5) { arr[m++] = p.info; p = p.next; }\n    for(i = 0; i < m-1; i++)\n      for(int j = i+1; j < m; j++)\n        if(arr[j].paddle < arr[i].paddle) { Boat t = arr[i]; arr[i] = arr[j]; arr[j] = t; }\n    p = start; m = 0;\n    while(p != null && m < 5) { p.info = arr[m++]; p = p.next; }\n\n    //------------------------------------------------------------------------------------\n    ftraverse(f);\n    f.close();\n   }\n\n }\n",
          "expectedOutput": "(C,9,8) (D,6,3) (E,8,5) (F,5,4) (I,4,9) (J,3,7)\n(C,9,8) (X,1,2) (D,6,3) (Y,2,3) (Z,3,4) (E,8,5) (F,5,4) (I,4,9) (J,3,7)",
          "explanation": "<p>Line 1 is the list before the insertions (written by the pre-given ftraverse call), line 2 is the list after. Insert one at a time, in the order x, y, z: X lands right after the head (position 1); with the list now C X D E F I J, inserting Y at position 3 gives C X D Y E F I J; inserting Z at position 4 gives C X D Y Z E F I J. Counting from 0 in the final list: position 1 = X, position 3 = Y, position 4 = Z — as required.</p>|||<p>Dòng 1 là danh sách TRƯỚC khi chèn (do lệnh ftraverse cho sẵn ghi ra), dòng 2 là sau khi chèn. Chèn lần lượt theo thứ tự x, y, z: X vào ngay sau head (vị trí 1); lúc này danh sách là C X D E F I J, chèn Y vào vị trí 3 được C X D Y E F I J; chèn Z vào vị trí 4 được C X D Y Z E F I J. Đếm từ 0 trên danh sách cuối: vị trí 1 = X, vị trí 3 = Y, vị trí 4 = Z — đúng yêu cầu.</p>",
          "rubric": [
            {
              "id": "insert_x",
              "criterion": "Correctly inserts x so it ends up at position 1 (right after the head).|||Chèn đúng x để nó nằm ở vị trí 1 (ngay sau head).",
              "weight": 1,
              "maxScore": 0.3
            },
            {
              "id": "insert_yz",
              "criterion": "Correctly inserts y and z so they end up at positions 3 and 4 of the final list.|||Chèn đúng y và z để chúng nằm ở vị trí 3 và 4 của danh sách cuối cùng.",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "links_intact",
              "criterion": "Keeps the list links (and tail) consistent, and does not remove or reorder any pre-existing node.|||Giữ liên kết danh sách (và tail) nhất quán, không xoá hay đảo thứ tự node có sẵn nào.",
              "weight": 1,
              "maxScore": 0.2
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>1.3 f3() – 1 mark: move the second maximum-paddle node to the tail</strong></p><ul><li>File: MyList.java</li><li>Method to Complete: the body of <code>void f3()</code>, just after the marked comment (keep every pre-given statement)</li><li>Task: move the second node having maximum paddle to the tail (thus if there is only one maximum paddle then do nothing).</li></ul><p><b>Note:</b> the expected output below is the real output of compiling and running the given project (javac/java) on its own <code>data.txt</code>, and it matches the worked example printed in the original exam paper character for character.</p>|||<p><strong>1.3 f3() – 1 điểm: đưa node có paddle lớn nhất THỨ HAI về cuối</strong></p><ul><li>File: MyList.java</li><li>Method cần hoàn thiện: phần thân <code>void f3()</code>, ngay sau chú thích đánh dấu (giữ nguyên mọi câu lệnh cho sẵn)</li><li>Nhiệm vụ: chuyển node THỨ HAI có paddle lớn nhất về cuối danh sách (nếu chỉ có duy nhất một node đạt paddle lớn nhất thì không làm gì).</li></ul><p><b>Lưu ý:</b> kết quả mong đợi dưới đây là output THẬT của việc biên dịch và chạy project given (javac/java) trên chính <code>data.txt</code> của nó, và khớp từng ký tự với ví dụ minh hoạ in trong đề gốc.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xSea, int xSail, int xPaddle) {\r\n    //You should write here appropriate statements to complete this function.\r\n\r\n\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Boat x, y, z;\r\n     x = new Boat(\"X\",1,2);\r\n     y = new Boat(\"Y\",2,3);\r\n     z = new Boat(\"Z\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n \r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n  }   \r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)==============================================================\nimport java.util.*;\nimport java.io.*;\n\npublic class MyList {\n  Node head,tail;\n  MyList() {head=tail=null;}\n  boolean isEmpty() {\n    return(head==null);\n   }\n  void clear() {head=tail=null;}\n\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\n    if(p != null) f.writeBytes(p.info + \" \");\n   }\n\n  void ftraverse(RandomAccessFile f) throws Exception {\n    Node p = head;\n    while(p!=null) {\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\n       p=p.next;\n      }\n    f.writeBytes(\"\\r\\n\");\n   }\n\n  void loadData(int k) { //do not edit this function\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\n    int n = a.length;\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\n   }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n/*\n   Khong su dung tieng Viet co dau de viet ghi chu.\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\n*/\n  void addLast(String xSea, int xSail, int xPaddle) {\n    //You should write here appropriate statements to complete this function.\n    if(xSea == null || xSea.length() == 0) return;\n    if(xSea.charAt(0) == 'A') return;   // do nothing when the sea starts with 'A'\n    Node q = new Node(new Boat(xSea, xSail, xPaddle));\n    if(isEmpty()) head = tail = q;\n    else { tail.next = q; tail = q; }\n   }\n\n  //Helper: insert the Boat x so that it becomes the node at position pos (head's position is 0)\n  void insertAt(int pos, Boat x) {\n    if(pos <= 0) {\n      Node q = new Node(x, head);\n      head = q;\n      if(tail == null) tail = q;\n      return;\n     }\n    Node p = head; int i = 0;\n    while(p != null && i < pos-1) { p = p.next; i++; }\n    if(p == null) return;\n    Node q = new Node(x, p.next);\n    p.next = q;\n    if(q.next == null) tail = q;\n   }\n\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\n  void f1() throws Exception {\n     clear();\n     loadData(1);\n     String fname = \"f1.txt\";\n     File g123 = new File(fname);\n     if(g123.exists()) g123.delete();\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n     ftraverse(f);\n     f.close();\n    }\n\n//==================================================================\n  void f2() throws Exception {\n     clear();\n     loadData(5);\n     String fname = \"f2.txt\";\n     File g123 = new File(fname);\n     if(g123.exists()) g123.delete();\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n     ftraverse(f);\n     Boat x, y, z;\n     x = new Boat(\"X\",1,2);\n     y = new Boat(\"Y\",2,3);\n     z = new Boat(\"Z\",3,4);\n     //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n     insertAt(1, x);\n     insertAt(3, y);\n     insertAt(4, z);\n\n    //------------------------------------------------------------------------------------\n     ftraverse(f);\n     f.close();\n    }\n\n//==================================================================\n  void f3() throws Exception {\n    clear();\n    loadData(9);\n    String fname = \"f3.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    ftraverse(f);\n    //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    int max = Integer.MIN_VALUE;\n    for(Node p = head; p != null; p = p.next)\n      if(p.info.paddle > max) max = p.info.paddle;\n    Node target = null, tprev = null, prev = null;\n    int count = 0;\n    for(Node p = head; p != null; p = p.next) {\n       if(p.info.paddle == max) {\n          count++;\n          if(count == 2) { target = p; tprev = prev; break; }\n         }\n       prev = p;\n      }\n    if(target != null && target != tail) {\n       if(tprev == null) head = target.next;\n       else tprev.next = target.next;\n       target.next = null;\n       tail.next = target;\n       tail = target;\n      }\n\n    //------------------------------------------------------------------------------------\n    ftraverse(f);\n    f.close();\n  }\n\n//==================================================================\n  void f4() throws Exception {\n    clear();\n    loadData(13);\n    String fname = \"f4.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    ftraverse(f);\n    //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    Node start = head;\n    int i;\n    for(i = 0; i < 2 && start != null; i++) start = start.next;\n    Boat [] arr = new Boat[5];\n    int m = 0;\n    Node p = start;\n    while(p != null && m < 5) { arr[m++] = p.info; p = p.next; }\n    for(i = 0; i < m-1; i++)\n      for(int j = i+1; j < m; j++)\n        if(arr[j].paddle < arr[i].paddle) { Boat t = arr[i]; arr[i] = arr[j]; arr[j] = t; }\n    p = start; m = 0;\n    while(p != null && m < 5) { p.info = arr[m++]; p = p.next; }\n\n    //------------------------------------------------------------------------------------\n    ftraverse(f);\n    f.close();\n   }\n\n }\n",
          "expectedOutput": "(C,8,6) (D,3,9) (E,9,2) (F,5,9) (G,9,7) (H,6,8) (I,7,3)\n(C,8,6) (D,3,9) (E,9,2) (G,9,7) (H,6,8) (I,7,3) (F,5,9)",
          "explanation": "<p>The paddles are 6, 9, 2, 9, 7, 8, 3 so the maximum is 9, reached twice: at D (the 1st) and at F (the 2nd). The 2nd one, (F,5,9), is unlinked from its position and appended after (I,7,3); every other node keeps its relative order. Had 9 occurred only once, the method would have left the list untouched.</p>|||<p>Các giá trị paddle là 6, 9, 2, 9, 7, 8, 3 nên lớn nhất là 9, xuất hiện hai lần: ở D (lần 1) và ở F (lần 2). Node thứ 2 là (F,5,9) được gỡ khỏi vị trí cũ và nối vào sau (I,7,3); mọi node khác giữ nguyên thứ tự tương đối. Nếu 9 chỉ xuất hiện một lần thì method phải để nguyên danh sách.</p>",
          "rubric": [
            {
              "id": "find_second_max",
              "criterion": "Correctly computes the maximum paddle and identifies the SECOND node (scanning from head) whose paddle equals it.|||Tính đúng paddle lớn nhất và xác định đúng node THỨ HAI (quét từ head) có paddle bằng giá trị đó.",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "move_to_tail",
              "criterion": "Correctly unlinks that node and re-attaches it as the new tail (tail.next and tail updated, previous node relinked).|||Gỡ đúng node đó và nối lại làm tail mới (cập nhật tail.next và tail, nối lại node đứng trước).",
              "weight": 1,
              "maxScore": 0.35
            },
            {
              "id": "noop_case",
              "criterion": "Correctly does nothing when only one node has the maximum paddle (and does not crash on that case).|||Không làm gì đúng khi chỉ có một node đạt paddle lớn nhất (và không lỗi ở trường hợp đó).",
              "weight": 1,
              "maxScore": 0.15
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>1.4 f4() – 1 mark: sort positions 2..6 ascending by paddle</strong></p><ul><li>File: MyList.java</li><li>Method to Complete: the body of <code>void f4()</code>, just after the marked comment (keep every pre-given statement)</li><li>Task: suppose the list contains at least 7 elements. Sort the 5 elements from position 2 to position 6 ascendingly by paddle (the head's position is 0). Every node outside that range must stay where it is.</li></ul><p><b>Note:</b> the expected output below is the real output of compiling and running the given project (javac/java) on its own <code>data.txt</code>, and it matches the worked example printed in the original exam paper character for character.</p>|||<p><strong>1.4 f4() – 1 điểm: sắp xếp vị trí 2..6 tăng dần theo paddle</strong></p><ul><li>File: MyList.java</li><li>Method cần hoàn thiện: phần thân <code>void f4()</code>, ngay sau chú thích đánh dấu (giữ nguyên mọi câu lệnh cho sẵn)</li><li>Nhiệm vụ: giả sử danh sách có ít nhất 7 phần tử. Sắp xếp 5 phần tử từ vị trí 2 đến vị trí 6 tăng dần theo paddle (vị trí của head là 0). Mọi node ngoài đoạn đó phải giữ nguyên vị trí.</li></ul><p><b>Lưu ý:</b> kết quả mong đợi dưới đây là output THẬT của việc biên dịch và chạy project given (javac/java) trên chính <code>data.txt</code> của nó, và khớp từng ký tự với ví dụ minh hoạ in trong đề gốc.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.util.*;\r\nimport java.io.*;\r\n\r\npublic class MyList {\r\n  Node head,tail;\r\n  MyList() {head=tail=null;}\r\n  boolean isEmpty() {\r\n    return(head==null);\r\n   }\r\n  void clear() {head=tail=null;}\r\n\r\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n    if(p != null) f.writeBytes(p.info + \" \");\r\n   }\r\n\r\n  void ftraverse(RandomAccessFile f) throws Exception {\r\n    Node p = head;\r\n    while(p!=null) {\r\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\r\n       p=p.next;\r\n      }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void loadData(int k) { //do not edit this function\r\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n    int n = a.length;\r\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n/* \r\n   Khong su dung tieng Viet co dau de viet ghi chu.\r\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\r\n*/\r\n  void addLast(String xSea, int xSail, int xPaddle) {\r\n    //You should write here appropriate statements to complete this function.\r\n\r\n\r\n   }\r\n\r\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\r\n  void f1() throws Exception {\r\n     clear();\r\n     loadData(1);\r\n     String fname = \"f1.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f2() throws Exception {\r\n     clear();\r\n     loadData(5);\r\n     String fname = \"f2.txt\";\r\n     File g123 = new File(fname);\r\n     if(g123.exists()) g123.delete();\r\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n     ftraverse(f);\r\n     Boat x, y, z;\r\n     x = new Boat(\"X\",1,2);\r\n     y = new Boat(\"Y\",2,3);\r\n     z = new Boat(\"Z\",3,4);\r\n     //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n     ftraverse(f);\r\n     f.close();\r\n    }  \r\n\r\n//==================================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n \r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n  }   \r\n\r\n//==================================================================\r\n  void f4() throws Exception {\r\n    clear();\r\n    loadData(13);\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    ftraverse(f);\r\n    //------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    ftraverse(f);\r\n    f.close();\r\n   }\r\n\r\n }\r\n\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)==============================================================\nimport java.util.*;\nimport java.io.*;\n\npublic class MyList {\n  Node head,tail;\n  MyList() {head=tail=null;}\n  boolean isEmpty() {\n    return(head==null);\n   }\n  void clear() {head=tail=null;}\n\n  void fvisit(Node p, RandomAccessFile f) throws Exception {\n    if(p != null) f.writeBytes(p.info + \" \");\n   }\n\n  void ftraverse(RandomAccessFile f) throws Exception {\n    Node p = head;\n    while(p!=null) {\n       fvisit(p,f); // You will use this statement to write information of the node p to the file\n       p=p.next;\n      }\n    f.writeBytes(\"\\r\\n\");\n   }\n\n  void loadData(int k) { //do not edit this function\n    String [] a = Lib.readLineToStrArray(\"data.txt\", k);\n    int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\n    int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\n    int n = a.length;\n    for(int i=0;i<n;i++) addLast(a[i],b[i],c[i]);\n   }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n/*\n   Khong su dung tieng Viet co dau de viet ghi chu.\n   Neu dung khi chay truc tiep se bao loi va nhan 0 diem\n*/\n  void addLast(String xSea, int xSail, int xPaddle) {\n    //You should write here appropriate statements to complete this function.\n    if(xSea == null || xSea.length() == 0) return;\n    if(xSea.charAt(0) == 'A') return;   // do nothing when the sea starts with 'A'\n    Node q = new Node(new Boat(xSea, xSail, xPaddle));\n    if(isEmpty()) head = tail = q;\n    else { tail.next = q; tail = q; }\n   }\n\n  //Helper: insert the Boat x so that it becomes the node at position pos (head's position is 0)\n  void insertAt(int pos, Boat x) {\n    if(pos <= 0) {\n      Node q = new Node(x, head);\n      head = q;\n      if(tail == null) tail = q;\n      return;\n     }\n    Node p = head; int i = 0;\n    while(p != null && i < pos-1) { p = p.next; i++; }\n    if(p == null) return;\n    Node q = new Node(x, p.next);\n    p.next = q;\n    if(q.next == null) tail = q;\n   }\n\n  //You do not need to edit this function. Your task is to complete the addLast function above only.\n  void f1() throws Exception {\n     clear();\n     loadData(1);\n     String fname = \"f1.txt\";\n     File g123 = new File(fname);\n     if(g123.exists()) g123.delete();\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n     ftraverse(f);\n     f.close();\n    }\n\n//==================================================================\n  void f2() throws Exception {\n     clear();\n     loadData(5);\n     String fname = \"f2.txt\";\n     File g123 = new File(fname);\n     if(g123.exists()) g123.delete();\n     RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n     ftraverse(f);\n     Boat x, y, z;\n     x = new Boat(\"X\",1,2);\n     y = new Boat(\"Y\",2,3);\n     z = new Boat(\"Z\",3,4);\n     //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n     insertAt(1, x);\n     insertAt(3, y);\n     insertAt(4, z);\n\n    //------------------------------------------------------------------------------------\n     ftraverse(f);\n     f.close();\n    }\n\n//==================================================================\n  void f3() throws Exception {\n    clear();\n    loadData(9);\n    String fname = \"f3.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    ftraverse(f);\n    //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    int max = Integer.MIN_VALUE;\n    for(Node p = head; p != null; p = p.next)\n      if(p.info.paddle > max) max = p.info.paddle;\n    Node target = null, tprev = null, prev = null;\n    int count = 0;\n    for(Node p = head; p != null; p = p.next) {\n       if(p.info.paddle == max) {\n          count++;\n          if(count == 2) { target = p; tprev = prev; break; }\n         }\n       prev = p;\n      }\n    if(target != null && target != tail) {\n       if(tprev == null) head = target.next;\n       else tprev.next = target.next;\n       target.next = null;\n       tail.next = target;\n       tail = target;\n      }\n\n    //------------------------------------------------------------------------------------\n    ftraverse(f);\n    f.close();\n  }\n\n//==================================================================\n  void f4() throws Exception {\n    clear();\n    loadData(13);\n    String fname = \"f4.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    ftraverse(f);\n    //------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    Node start = head;\n    int i;\n    for(i = 0; i < 2 && start != null; i++) start = start.next;\n    Boat [] arr = new Boat[5];\n    int m = 0;\n    Node p = start;\n    while(p != null && m < 5) { arr[m++] = p.info; p = p.next; }\n    for(i = 0; i < m-1; i++)\n      for(int j = i+1; j < m; j++)\n        if(arr[j].paddle < arr[i].paddle) { Boat t = arr[i]; arr[i] = arr[j]; arr[j] = t; }\n    p = start; m = 0;\n    while(p != null && m < 5) { p.info = arr[m++]; p = p.next; }\n\n    //------------------------------------------------------------------------------------\n    ftraverse(f);\n    f.close();\n   }\n\n }\n",
          "expectedOutput": "(C,9,8) (D,11,12) (E,10,11) (F,1,19) (I,7,9) (J,6,8) (K,5,6) (L,4,5) (M,3,4)\n(C,9,8) (D,11,12) (K,5,6) (J,6,8) (I,7,9) (E,10,11) (F,1,19) (L,4,5) (M,3,4)",
          "explanation": "<p>Positions 2..6 hold E(paddle 11), F(19), I(9), J(8), K(6). Sorted ascending by paddle they become K(6), J(8), I(9), E(11), F(19), and they are written back into those same 5 slots. Positions 0, 1 (C, D) and 7, 8 (L, M) are untouched. Swapping the node <i>info</i> values is the simplest correct approach — relinking the nodes themselves is equally acceptable as long as the result is identical.</p>|||<p>Vị trí 2..6 chứa E(paddle 11), F(19), I(9), J(8), K(6). Sắp tăng dần theo paddle được K(6), J(8), I(9), E(11), F(19), rồi ghi ngược lại đúng 5 ô đó. Vị trí 0, 1 (C, D) và 7, 8 (L, M) giữ nguyên. Hoán đổi trường <i>info</i> của node là cách đơn giản mà đúng — nối lại chính các node cũng chấp nhận được miễn kết quả giống hệt.</p>",
          "rubric": [
            {
              "id": "range",
              "criterion": "Correctly targets exactly the 5 nodes at 0-indexed positions 2 through 6 and leaves every node outside that range untouched.|||Nhắm đúng 5 node ở vị trí 2 đến 6 (đếm từ 0) và giữ nguyên mọi node ngoài đoạn đó.",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "sort_asc_paddle",
              "criterion": "Correctly sorts those 5 elements ascending by paddle (not by sail, not descending).|||Sắp đúng 5 phần tử đó tăng dần theo paddle (không phải theo sail, không phải giảm dần).",
              "weight": 1,
              "maxScore": 0.5
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 2 (4 marks) — Scenario:</strong> in this question you should complete some methods in <code>BSTree.java</code>. The class <code>Boat</code> with 3 data members (sea, sail, paddle) is given and you do not need to edit it. The <code>BSTree</code> class is a binary search tree of Boat objects. The variable <b>sail is the key of the tree, thus it must be unique</b>.</p><p><strong>2.1 insert() – 1 mark</strong></p><ul><li>File: BSTree.java</li><li>Method to Complete: <code>void insert(String xSea, int xSail, int xPaddle)</code></li><li>Task: check if <code>xSea.charAt(0) == 'B'</code> then <b>do nothing</b>, otherwise insert a new Boat object with sea=xSea, sail=xSail, paddle=xPaddle into the tree (sail and paddle can get arbitrary, even negative values).</li><li><code>void f1()</code> — do not edit this method; your task is to complete <code>insert(...)</code> only. It writes the breadth-first traversal on line 1 and the in-order traversal on line 2 of f1.txt.</li></ul><p><b>Note:</b> the expected output below is the real output of compiling and running the given project (javac/java) on its own <code>data.txt</code>, and it matches the worked example printed in the original exam paper character for character.</p>|||<p><strong>Câu 2 (4 điểm) — Bối cảnh:</strong> ở câu này bạn cần hoàn thiện một số method trong <code>BSTree.java</code>. Lớp <code>Boat</code> với 3 thành viên dữ liệu (sea, sail, paddle) đã cho sẵn, không cần sửa. Lớp <code>BSTree</code> là cây nhị phân tìm kiếm các đối tượng Boat. Biến <b>sail là khoá của cây, do đó nó phải duy nhất</b>.</p><p><strong>2.1 insert() – 1 điểm</strong></p><ul><li>File: BSTree.java</li><li>Method cần hoàn thiện: <code>void insert(String xSea, int xSail, int xPaddle)</code></li><li>Nhiệm vụ: kiểm tra nếu <code>xSea.charAt(0) == 'B'</code> thì <b>không làm gì</b>; ngược lại chèn đối tượng Boat mới sea=xSea, sail=xSail, paddle=xPaddle vào cây (sail và paddle có thể nhận giá trị bất kỳ, kể cả âm).</li><li><code>void f1()</code> — không sửa method này; nhiệm vụ của bạn chỉ là hoàn thiện <code>insert(...)</code>. Nó ghi duyệt breadth-first ở dòng 1 và duyệt in-order ở dòng 2 của f1.txt.</li></ul><p><b>Lưu ý:</b> kết quả mong đợi dưới đây là output THẬT của việc biên dịch và chạy project given (javac/java) trên chính <code>data.txt</code> của nó, và khớp từng ký tự với ví dụ minh hoạ in trong đề gốc.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xSea, int xSail, int xPaddle) {\r\n    //You should insert here statements to complete this function\r\n\r\n\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n \r\n\r\n     \r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n }\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)==============================================================\nimport java.io.*;\nimport java.util.*;\n\npublic class BSTree {\n   Node root;\n   BSTree() {root=null;}\n   boolean isEmpty() {\n       return(root==null);\n      }\n   void clear() {\n       root=null;\n      }\n   void visit(Node p) {\n      System.out.print(\"p.info: \");\n      if(p != null) System.out.println(p.info + \" \");\n     }\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\n      if(p != null) f.writeBytes(p.info + \" \");\n     }\n   void breadth(Node p, RandomAccessFile f) throws Exception {\n     if(p==null) return;\n     Queue q = new Queue();\n     q.enqueue(p);Node r;\n     while(!q.isEmpty()) {\n        r = q.dequeue();\n        fvisit(r,f);\n        if(r.left!=null) q.enqueue(r.left);\n        if(r.right!=null) q.enqueue(r.right);\n       }\n    }\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      fvisit(p,f);\n      preOrder(p.left,f);\n      preOrder(p.right,f);\n     }\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      inOrder(p.left,f);\n      fvisit(p,f);\n      inOrder(p.right,f);\n     }\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      postOrder(p.left,f);\n      postOrder(p.right,f);\n      fvisit(p,f);\n     }\n\n   void loadData(int k) { //do not edit this function\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\n      int n = a.length;\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\n     }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n  void insert(String xSea, int xSail, int xPaddle) {\n    //You should insert here statements to complete this function\n    if(xSea == null || xSea.length() == 0) return;\n    if(xSea.charAt(0) == 'B') return;      // do nothing when the sea starts with 'B'\n    Node q = new Node(new Boat(xSea, xSail, xPaddle));\n    if(root == null) { root = q; return; }\n    Node p = root, f = null;\n    while(p != null) {\n       f = p;\n       if(xSail == p.info.sail) return;    // the key 'sail' must be unique\n       if(xSail < p.info.sail) p = p.left; else p = p.right;\n      }\n    if(xSail < f.info.sail) f.left = q; else f.right = q;\n   }\n\n  //Helper: breadth-first traversal writing only the nodes with paddle > 5\n  void breadth2(Node p, RandomAccessFile f) throws Exception {\n    if(p==null) return;\n    Queue q = new Queue();\n    q.enqueue(p);Node r;\n    while(!q.isEmpty()) {\n       r = q.dequeue();\n       if(r.info.paddle > 5) fvisit(r,f);\n       if(r.left!=null) q.enqueue(r.left);\n       if(r.right!=null) q.enqueue(r.right);\n      }\n   }\n\n  //Helper: collect the in-order sequence of nodes (with their fathers)\n  void inOrderCollect(Node p, Node father, ArrayList<Node> nodes, ArrayList<Node> fathers) {\n    if(p==null) return;\n    inOrderCollect(p.left, p, nodes, fathers);\n    nodes.add(p); fathers.add(father);\n    inOrderCollect(p.right, p, nodes, fathers);\n   }\n\n  //Helper: index (in the in-order sequence) of the second node having a left child; -1 if none\n  int secondWithLeft(ArrayList<Node> nodes) {\n    int count = 0;\n    for(int i=0;i<nodes.size();i++) {\n       if(nodes.get(i).left != null) {\n          count++;\n          if(count == 2) return i;\n         }\n      }\n    return -1;\n   }\n\n  //Helper: delete the node q (whose father is fq) by copying\n  void delByCopying(Node q, Node fq) {\n    if(q == null) return;\n    if(q.left == null) {           // no left subtree: q is replaced by its right child\n       Node c = q.right;\n       if(fq == null) root = c;\n       else if(fq.left == q) fq.left = c; else fq.right = c;\n       return;\n      }\n    Node r = q.left, fr = q;       // r = the in-order predecessor of q\n    while(r.right != null) { fr = r; r = r.right; }\n    q.info = r.info;               // copy the predecessor's data into q\n    if(fr == q) fr.left = r.left; else fr.right = r.left;\n   }\n\n//Do not edit this function. Your task is to complete insert function above only.\n  void f1() throws Exception {\n    clear();\n    loadData(1);\n    String fname = \"f1.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    inOrder(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n void f2() throws Exception {\n    clear();\n    loadData(5);\n    String fname = \"f2.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    breadth2(root,f);\n\n    //------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n  void f3() throws Exception {\n    clear();\n    loadData(9);\n    String fname = \"f3.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    ArrayList<Node> nodes = new ArrayList<Node>();\n    ArrayList<Node> fathers = new ArrayList<Node>();\n    inOrderCollect(root, null, nodes, fathers);\n    int k = secondWithLeft(nodes);\n    if(k >= 0) {\n       Node p = nodes.get(k);\n       delByCopying(p.left, p);    // delete q = p.left by copying\n      }\n\n    //------------------------------------------------------------------------------------\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n void f4() throws Exception {\n    clear();\n    loadData(13);;\n    String fname = \"f4.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    ArrayList<Node> nodes2 = new ArrayList<Node>();\n    ArrayList<Node> fathers2 = new ArrayList<Node>();\n    inOrderCollect(root, null, nodes2, fathers2);\n    int k2 = secondWithLeft(nodes2);\n    if(k2 >= 0) {\n       Node p = nodes2.get(k2);\n       Node fp = fathers2.get(k2);\n       Node q = p.left;            // rotate p to the right about its left child q\n       p.left = q.right;\n       q.right = p;\n       if(fp == null) root = q;\n       else if(fp.left == p) fp.left = q; else fp.right = q;\n      }\n\n    //------------------------------------------------------------------------------------\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n }\n",
          "expectedOutput": "(A,7,9) (C,4,3) (D,8,6) (E,2,5) (Y,6,-7) (F,-6,7)\n(F,-6,7) (E,2,5) (C,4,3) (Y,6,-7) (A,7,9) (D,8,6)",
          "explanation": "<p>data.txt supplies 8 candidates: A(7,9) B(9,4) C(4,3) D(8,6) E(2,5) F(-6,7) X(4,5) Y(6,-7). Two of them never make it into the tree, for two different reasons: B is rejected by the sea filter, and X is rejected because its key sail=4 already belongs to C — the key must be unique. The remaining 6 build the tree A(7) at the root, C(4) and D(8) as its children, E(2) and Y(6) under C, and F(-6) under E. That gives the breadth-first line 1 and the in-order line 2 shown above. Note that the in-order line is sorted by sail (-6, 2, 4, 6, 7, 8), which is the quickest way to sanity-check your own tree.</p>|||<p>data.txt cho 8 ứng viên: A(7,9) B(9,4) C(4,3) D(8,6) E(2,5) F(-6,7) X(4,5) Y(6,-7). Hai trong số đó không bao giờ vào cây, vì hai lý do khác nhau: B bị lọc theo sea, còn X bị từ chối vì khoá sail=4 đã thuộc về C — khoá phải duy nhất. Sáu phần tử còn lại dựng nên cây gốc A(7), hai con C(4) và D(8), dưới C là E(2) và Y(6), dưới E là F(-6). Từ đó ra dòng 1 breadth-first và dòng 2 in-order như trên. Lưu ý dòng in-order được sắp theo sail (-6, 2, 4, 6, 7, 8) — đó là cách nhanh nhất để tự kiểm tra cây của mình.</p>",
          "rubric": [
            {
              "id": "sea_filter",
              "criterion": "Correctly does nothing when xSea.charAt(0) == 'B'.|||Không làm gì đúng khi xSea.charAt(0) == 'B'.",
              "weight": 1,
              "maxScore": 0.25
            },
            {
              "id": "bst_order",
              "criterion": "Correctly walks down the tree comparing sail and links the new node as a left/right child of the right father (empty-tree case handled).|||Đi đúng xuống cây theo so sánh sail và nối node mới làm con trái/phải của đúng node cha (xử lý được trường hợp cây rỗng).",
              "weight": 1,
              "maxScore": 0.45
            },
            {
              "id": "unique_key",
              "criterion": "Correctly refuses the insertion when a node with the same sail already exists (unique key).|||Từ chối đúng khi đã có node cùng sail (khoá duy nhất).",
              "weight": 1,
              "maxScore": 0.3
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>2.2 f2() – 1 mark: breadth-first traversal filtered by paddle</strong></p><ul><li>File: BSTree.java</li><li>Method to Complete: the body of <code>void f2()</code>, just after the marked comment (keep every pre-given statement)</li><li>Task: perform the breadth-first traversal from the root but display to file f2.txt the nodes with <b>paddle &gt; 5</b> only. Hint: copy the function <code>breadth(...)</code> to a function <code>breadth2(...)</code> and modify it.</li></ul><p><b>Note:</b> the expected output below is the real output of compiling and running the given project (javac/java) on its own <code>data.txt</code>, and it matches the worked example printed in the original exam paper character for character.</p>|||<p><strong>2.2 f2() – 1 điểm: duyệt breadth-first có lọc theo paddle</strong></p><ul><li>File: BSTree.java</li><li>Method cần hoàn thiện: phần thân <code>void f2()</code>, ngay sau chú thích đánh dấu (giữ nguyên mọi câu lệnh cho sẵn)</li><li>Nhiệm vụ: duyệt breadth-first từ gốc nhưng CHỈ ghi ra file f2.txt các node có <b>paddle &gt; 5</b>. Gợi ý: copy hàm <code>breadth(...)</code> thành hàm <code>breadth2(...)</code> rồi sửa nó.</li></ul><p><b>Lưu ý:</b> kết quả mong đợi dưới đây là output THẬT của việc biên dịch và chạy project given (javac/java) trên chính <code>data.txt</code> của nó, và khớp từng ký tự với ví dụ minh hoạ in trong đề gốc.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xSea, int xSail, int xPaddle) {\r\n    //You should insert here statements to complete this function\r\n\r\n\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n \r\n\r\n     \r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n }\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)==============================================================\nimport java.io.*;\nimport java.util.*;\n\npublic class BSTree {\n   Node root;\n   BSTree() {root=null;}\n   boolean isEmpty() {\n       return(root==null);\n      }\n   void clear() {\n       root=null;\n      }\n   void visit(Node p) {\n      System.out.print(\"p.info: \");\n      if(p != null) System.out.println(p.info + \" \");\n     }\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\n      if(p != null) f.writeBytes(p.info + \" \");\n     }\n   void breadth(Node p, RandomAccessFile f) throws Exception {\n     if(p==null) return;\n     Queue q = new Queue();\n     q.enqueue(p);Node r;\n     while(!q.isEmpty()) {\n        r = q.dequeue();\n        fvisit(r,f);\n        if(r.left!=null) q.enqueue(r.left);\n        if(r.right!=null) q.enqueue(r.right);\n       }\n    }\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      fvisit(p,f);\n      preOrder(p.left,f);\n      preOrder(p.right,f);\n     }\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      inOrder(p.left,f);\n      fvisit(p,f);\n      inOrder(p.right,f);\n     }\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      postOrder(p.left,f);\n      postOrder(p.right,f);\n      fvisit(p,f);\n     }\n\n   void loadData(int k) { //do not edit this function\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\n      int n = a.length;\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\n     }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n  void insert(String xSea, int xSail, int xPaddle) {\n    //You should insert here statements to complete this function\n    if(xSea == null || xSea.length() == 0) return;\n    if(xSea.charAt(0) == 'B') return;      // do nothing when the sea starts with 'B'\n    Node q = new Node(new Boat(xSea, xSail, xPaddle));\n    if(root == null) { root = q; return; }\n    Node p = root, f = null;\n    while(p != null) {\n       f = p;\n       if(xSail == p.info.sail) return;    // the key 'sail' must be unique\n       if(xSail < p.info.sail) p = p.left; else p = p.right;\n      }\n    if(xSail < f.info.sail) f.left = q; else f.right = q;\n   }\n\n  //Helper: breadth-first traversal writing only the nodes with paddle > 5\n  void breadth2(Node p, RandomAccessFile f) throws Exception {\n    if(p==null) return;\n    Queue q = new Queue();\n    q.enqueue(p);Node r;\n    while(!q.isEmpty()) {\n       r = q.dequeue();\n       if(r.info.paddle > 5) fvisit(r,f);\n       if(r.left!=null) q.enqueue(r.left);\n       if(r.right!=null) q.enqueue(r.right);\n      }\n   }\n\n  //Helper: collect the in-order sequence of nodes (with their fathers)\n  void inOrderCollect(Node p, Node father, ArrayList<Node> nodes, ArrayList<Node> fathers) {\n    if(p==null) return;\n    inOrderCollect(p.left, p, nodes, fathers);\n    nodes.add(p); fathers.add(father);\n    inOrderCollect(p.right, p, nodes, fathers);\n   }\n\n  //Helper: index (in the in-order sequence) of the second node having a left child; -1 if none\n  int secondWithLeft(ArrayList<Node> nodes) {\n    int count = 0;\n    for(int i=0;i<nodes.size();i++) {\n       if(nodes.get(i).left != null) {\n          count++;\n          if(count == 2) return i;\n         }\n      }\n    return -1;\n   }\n\n  //Helper: delete the node q (whose father is fq) by copying\n  void delByCopying(Node q, Node fq) {\n    if(q == null) return;\n    if(q.left == null) {           // no left subtree: q is replaced by its right child\n       Node c = q.right;\n       if(fq == null) root = c;\n       else if(fq.left == q) fq.left = c; else fq.right = c;\n       return;\n      }\n    Node r = q.left, fr = q;       // r = the in-order predecessor of q\n    while(r.right != null) { fr = r; r = r.right; }\n    q.info = r.info;               // copy the predecessor's data into q\n    if(fr == q) fr.left = r.left; else fr.right = r.left;\n   }\n\n//Do not edit this function. Your task is to complete insert function above only.\n  void f1() throws Exception {\n    clear();\n    loadData(1);\n    String fname = \"f1.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    inOrder(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n void f2() throws Exception {\n    clear();\n    loadData(5);\n    String fname = \"f2.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    breadth2(root,f);\n\n    //------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n  void f3() throws Exception {\n    clear();\n    loadData(9);\n    String fname = \"f3.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    ArrayList<Node> nodes = new ArrayList<Node>();\n    ArrayList<Node> fathers = new ArrayList<Node>();\n    inOrderCollect(root, null, nodes, fathers);\n    int k = secondWithLeft(nodes);\n    if(k >= 0) {\n       Node p = nodes.get(k);\n       delByCopying(p.left, p);    // delete q = p.left by copying\n      }\n\n    //------------------------------------------------------------------------------------\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n void f4() throws Exception {\n    clear();\n    loadData(13);;\n    String fname = \"f4.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    ArrayList<Node> nodes2 = new ArrayList<Node>();\n    ArrayList<Node> fathers2 = new ArrayList<Node>();\n    inOrderCollect(root, null, nodes2, fathers2);\n    int k2 = secondWithLeft(nodes2);\n    if(k2 >= 0) {\n       Node p = nodes2.get(k2);\n       Node fp = fathers2.get(k2);\n       Node q = p.left;            // rotate p to the right about its left child q\n       p.left = q.right;\n       q.right = p;\n       if(fp == null) root = q;\n       else if(fp.left == p) fp.left = q; else fp.right = q;\n      }\n\n    //------------------------------------------------------------------------------------\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n }\n",
          "expectedOutput": "(C,8,2) (D,6,1) (E,9,4) (F,2,-1) (G,7,3) (H,10,8) (I,1,7) (J,3,9) (K,-1,5) (L,5,10) (M,4,6)\n(H,10,8) (I,1,7) (J,3,9) (L,5,10) (M,4,6)",
          "explanation": "<p>Line 1 is the full breadth-first traversal (written by the pre-given call), line 2 is your filtered one. The paddles in breadth order are 2, 1, 4, -1, 3, 8, 7, 9, 5, 10, 6; the ones strictly greater than 5 are H(8), I(7), J(9), L(10), M(6). Note K has paddle exactly 5 and is therefore excluded — the condition is &gt; 5, not ≥ 5. Crucially, the filter must only suppress the <i>writing</i>: the traversal itself must still descend into the children of a skipped node, otherwise L(10) and M(6) — which sit below J and L — would never be reached.</p>|||<p>Dòng 1 là duyệt breadth-first đầy đủ (do lệnh cho sẵn ghi), dòng 2 là bản đã lọc của bạn. Các paddle theo thứ tự breadth là 2, 1, 4, -1, 3, 8, 7, 9, 5, 10, 6; những giá trị lớn hơn hẳn 5 là H(8), I(7), J(9), L(10), M(6). Lưu ý K có paddle đúng bằng 5 nên bị loại — điều kiện là &gt; 5 chứ không phải ≥ 5. Quan trọng nhất: bộ lọc chỉ được chặn việc GHI RA; phép duyệt vẫn phải đi xuống con của node bị bỏ qua, nếu không L(10) và M(6) — nằm dưới J và L — sẽ không bao giờ tới được.</p>",
          "rubric": [
            {
              "id": "breadth_copy",
              "criterion": "Correctly performs a full breadth-first (level-order, queue-based) traversal of the whole tree, still enqueuing the children of skipped nodes.|||Duyệt đúng breadth-first đầy đủ (theo mức, dùng hàng đợi) trên toàn cây, vẫn đưa con của node bị bỏ qua vào hàng đợi.",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "paddle_filter",
              "criterion": "Only writes nodes whose paddle is strictly greater than 5.|||Chỉ ghi ra các node có paddle lớn hơn hẳn 5.",
              "weight": 1,
              "maxScore": 0.5
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>2.3 f3() – 1 mark: delete q = p.left by copying</strong></p><ul><li>File: BSTree.java</li><li>Method to Complete: the body of <code>void f3()</code>, just after the marked comment (keep every pre-given statement)</li><li>Task: perform an in-order traversal from the root and find the <b>second node p having a left child</b>. If such a node does not exist then do nothing, otherwise delete the node <code>q = p.left</code> <b>by copying</b>.</li></ul><p><b>Note:</b> the expected output below is the real output of compiling and running the given project (javac/java) on its own <code>data.txt</code>, and it matches the worked example printed in the original exam paper character for character.</p>|||<p><strong>2.3 f3() – 1 điểm: xoá q = p.left bằng phương pháp copy</strong></p><ul><li>File: BSTree.java</li><li>Method cần hoàn thiện: phần thân <code>void f3()</code>, ngay sau chú thích đánh dấu (giữ nguyên mọi câu lệnh cho sẵn)</li><li>Nhiệm vụ: duyệt in-order từ gốc và tìm <b>node p THỨ HAI có con trái</b>. Nếu không tồn tại node như vậy thì không làm gì; ngược lại xoá node <code>q = p.left</code> <b>bằng phương pháp copy</b>.</li></ul><p><b>Lưu ý:</b> kết quả mong đợi dưới đây là output THẬT của việc biên dịch và chạy project given (javac/java) trên chính <code>data.txt</code> của nó, và khớp từng ký tự với ví dụ minh hoạ in trong đề gốc.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xSea, int xSail, int xPaddle) {\r\n    //You should insert here statements to complete this function\r\n\r\n\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n \r\n\r\n     \r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n }\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)==============================================================\nimport java.io.*;\nimport java.util.*;\n\npublic class BSTree {\n   Node root;\n   BSTree() {root=null;}\n   boolean isEmpty() {\n       return(root==null);\n      }\n   void clear() {\n       root=null;\n      }\n   void visit(Node p) {\n      System.out.print(\"p.info: \");\n      if(p != null) System.out.println(p.info + \" \");\n     }\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\n      if(p != null) f.writeBytes(p.info + \" \");\n     }\n   void breadth(Node p, RandomAccessFile f) throws Exception {\n     if(p==null) return;\n     Queue q = new Queue();\n     q.enqueue(p);Node r;\n     while(!q.isEmpty()) {\n        r = q.dequeue();\n        fvisit(r,f);\n        if(r.left!=null) q.enqueue(r.left);\n        if(r.right!=null) q.enqueue(r.right);\n       }\n    }\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      fvisit(p,f);\n      preOrder(p.left,f);\n      preOrder(p.right,f);\n     }\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      inOrder(p.left,f);\n      fvisit(p,f);\n      inOrder(p.right,f);\n     }\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      postOrder(p.left,f);\n      postOrder(p.right,f);\n      fvisit(p,f);\n     }\n\n   void loadData(int k) { //do not edit this function\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\n      int n = a.length;\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\n     }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n  void insert(String xSea, int xSail, int xPaddle) {\n    //You should insert here statements to complete this function\n    if(xSea == null || xSea.length() == 0) return;\n    if(xSea.charAt(0) == 'B') return;      // do nothing when the sea starts with 'B'\n    Node q = new Node(new Boat(xSea, xSail, xPaddle));\n    if(root == null) { root = q; return; }\n    Node p = root, f = null;\n    while(p != null) {\n       f = p;\n       if(xSail == p.info.sail) return;    // the key 'sail' must be unique\n       if(xSail < p.info.sail) p = p.left; else p = p.right;\n      }\n    if(xSail < f.info.sail) f.left = q; else f.right = q;\n   }\n\n  //Helper: breadth-first traversal writing only the nodes with paddle > 5\n  void breadth2(Node p, RandomAccessFile f) throws Exception {\n    if(p==null) return;\n    Queue q = new Queue();\n    q.enqueue(p);Node r;\n    while(!q.isEmpty()) {\n       r = q.dequeue();\n       if(r.info.paddle > 5) fvisit(r,f);\n       if(r.left!=null) q.enqueue(r.left);\n       if(r.right!=null) q.enqueue(r.right);\n      }\n   }\n\n  //Helper: collect the in-order sequence of nodes (with their fathers)\n  void inOrderCollect(Node p, Node father, ArrayList<Node> nodes, ArrayList<Node> fathers) {\n    if(p==null) return;\n    inOrderCollect(p.left, p, nodes, fathers);\n    nodes.add(p); fathers.add(father);\n    inOrderCollect(p.right, p, nodes, fathers);\n   }\n\n  //Helper: index (in the in-order sequence) of the second node having a left child; -1 if none\n  int secondWithLeft(ArrayList<Node> nodes) {\n    int count = 0;\n    for(int i=0;i<nodes.size();i++) {\n       if(nodes.get(i).left != null) {\n          count++;\n          if(count == 2) return i;\n         }\n      }\n    return -1;\n   }\n\n  //Helper: delete the node q (whose father is fq) by copying\n  void delByCopying(Node q, Node fq) {\n    if(q == null) return;\n    if(q.left == null) {           // no left subtree: q is replaced by its right child\n       Node c = q.right;\n       if(fq == null) root = c;\n       else if(fq.left == q) fq.left = c; else fq.right = c;\n       return;\n      }\n    Node r = q.left, fr = q;       // r = the in-order predecessor of q\n    while(r.right != null) { fr = r; r = r.right; }\n    q.info = r.info;               // copy the predecessor's data into q\n    if(fr == q) fr.left = r.left; else fr.right = r.left;\n   }\n\n//Do not edit this function. Your task is to complete insert function above only.\n  void f1() throws Exception {\n    clear();\n    loadData(1);\n    String fname = \"f1.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    inOrder(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n void f2() throws Exception {\n    clear();\n    loadData(5);\n    String fname = \"f2.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    breadth2(root,f);\n\n    //------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n  void f3() throws Exception {\n    clear();\n    loadData(9);\n    String fname = \"f3.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    ArrayList<Node> nodes = new ArrayList<Node>();\n    ArrayList<Node> fathers = new ArrayList<Node>();\n    inOrderCollect(root, null, nodes, fathers);\n    int k = secondWithLeft(nodes);\n    if(k >= 0) {\n       Node p = nodes.get(k);\n       delByCopying(p.left, p);    // delete q = p.left by copying\n      }\n\n    //------------------------------------------------------------------------------------\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n void f4() throws Exception {\n    clear();\n    loadData(13);;\n    String fname = \"f4.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    ArrayList<Node> nodes2 = new ArrayList<Node>();\n    ArrayList<Node> fathers2 = new ArrayList<Node>();\n    inOrderCollect(root, null, nodes2, fathers2);\n    int k2 = secondWithLeft(nodes2);\n    if(k2 >= 0) {\n       Node p = nodes2.get(k2);\n       Node fp = fathers2.get(k2);\n       Node q = p.left;            // rotate p to the right about its left child q\n       p.left = q.right;\n       q.right = p;\n       if(fp == null) root = q;\n       else if(fp.left == p) fp.left = q; else fp.right = q;\n      }\n\n    //------------------------------------------------------------------------------------\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n }\n",
          "expectedOutput": "(C,8,2) (D,6,1) (E,9,4) (F,2,-1) (G,7,3) (H,10,8) (I,1,7) (J,3,9) (K,-1,5) (L,5,10) (M,4,6)\n(C,8,2) (D,6,1) (E,9,4) (F,2,-1) (G,7,3) (H,10,8) (K,-1,5) (J,3,9) (L,5,10) (M,4,6)",
          "explanation": "<p>Both lines are breadth-first traversals, before and after. The tree built from data.txt has C(8) at the root; D(6) and E(9) below it; F(2) and G(7) under D; H(10) under E; I(1) and J(3) under F; K(-1) under I; L(5) under J; M(4) under L. The in-order sequence by sail is K(-1), I(1), F(2), J(3), M(4), L(5), D(6), G(7), C(8), E(9), H(10). Scanning it, the nodes that have a left child are I (1st), F (2nd), L (3rd), D, C — so p = F and q = F.left = I. Deleting I by copying: I has a left subtree, so its in-order predecessor is the right-most node of that subtree, which is K itself; K's data is copied into I's node and K is unlinked. That is why the node in I's old position now reads (K,-1,5) and the tree has 10 nodes instead of 11.</p>|||<p>Cả hai dòng đều là duyệt breadth-first, trước và sau. Cây dựng từ data.txt có C(8) ở gốc; dưới nó là D(6) và E(9); dưới D là F(2) và G(7); dưới E là H(10); dưới F là I(1) và J(3); dưới I là K(-1); dưới J là L(5); dưới L là M(4). Dãy in-order theo sail là K(-1), I(1), F(2), J(3), M(4), L(5), D(6), G(7), C(8), E(9), H(10). Quét dãy đó, các node có con trái là I (thứ 1), F (thứ 2), L (thứ 3), D, C — vậy p = F và q = F.left = I. Xoá I bằng copy: I có cây con trái nên node tiền nhiệm in-order là node phải nhất của cây con đó, chính là K; dữ liệu của K được chép vào node I rồi gỡ K ra. Vì thế node ở vị trí cũ của I giờ đọc là (K,-1,5) và cây còn 10 node thay vì 11.</p>",
          "rubric": [
            {
              "id": "find_p",
              "criterion": "Correctly walks the tree in in-order and picks the SECOND node that has a non-null left child (not the second node overall, not the second with two children).|||Duyệt đúng in-order và chọn node THỨ HAI có con trái khác null (không phải node thứ hai nói chung, cũng không phải node thứ hai có 2 con).",
              "weight": 1,
              "maxScore": 0.4
            },
            {
              "id": "delete_by_copying",
              "criterion": "Correctly deletes q = p.left by copying: copy the in-order predecessor's (right-most node of q's left subtree) data into q, then unlink that predecessor — or bypass q with its only child when q has no left subtree.|||Xoá đúng q = p.left bằng copy: chép dữ liệu của node tiền nhiệm in-order (node phải nhất trong cây con trái của q) vào q rồi gỡ node tiền nhiệm — hoặc thay q bằng con duy nhất của nó khi q không có cây con trái.",
              "weight": 1,
              "maxScore": 0.45
            },
            {
              "id": "noop_and_links",
              "criterion": "Keeps every other link intact and correctly does nothing when no such second node exists.|||Giữ nguyên mọi liên kết khác và không làm gì đúng khi không tồn tại node thứ hai như vậy.",
              "weight": 1,
              "maxScore": 0.15
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>2.4 f4() – 1 mark: rotate p to the right</strong></p><ul><li>File: BSTree.java</li><li>Method to Complete: the body of <code>void f4()</code>, just after the marked comment (keep every pre-given statement)</li><li>Task: perform an in-order traversal from the root and find the <b>second node p having a left child</b>. If such a node does not exist then do nothing, otherwise <b>rotate p to the right</b>.</li></ul><p><b>Note:</b> the expected output below is the real output of compiling and running the given project (javac/java) on its own <code>data.txt</code>, and it matches the worked example printed in the original exam paper character for character.</p>|||<p><strong>2.4 f4() – 1 điểm: xoay p sang phải</strong></p><ul><li>File: BSTree.java</li><li>Method cần hoàn thiện: phần thân <code>void f4()</code>, ngay sau chú thích đánh dấu (giữ nguyên mọi câu lệnh cho sẵn)</li><li>Nhiệm vụ: duyệt in-order từ gốc và tìm <b>node p THỨ HAI có con trái</b>. Nếu không tồn tại node như vậy thì không làm gì; ngược lại <b>xoay p sang phải</b>.</li></ul><p><b>Lưu ý:</b> kết quả mong đợi dưới đây là output THẬT của việc biên dịch và chạy project given (javac/java) trên chính <code>data.txt</code> của nó, và khớp từng ký tự với ví dụ minh hoạ in trong đề gốc.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)==============================================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n\r\npublic class BSTree {\r\n   Node root;\r\n   BSTree() {root=null;}\r\n   boolean isEmpty() {\r\n       return(root==null);\r\n      }\r\n   void clear() {\r\n       root=null;\r\n      }\r\n   void visit(Node p) {\r\n      System.out.print(\"p.info: \");\r\n      if(p != null) System.out.println(p.info + \" \");\r\n     }\r\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\r\n      if(p != null) f.writeBytes(p.info + \" \");\r\n     }\r\n   void breadth(Node p, RandomAccessFile f) throws Exception {\r\n     if(p==null) return;\r\n     Queue q = new Queue();\r\n     q.enqueue(p);Node r;\r\n     while(!q.isEmpty()) {\r\n        r = q.dequeue();\r\n        fvisit(r,f);\r\n        if(r.left!=null) q.enqueue(r.left);\r\n        if(r.right!=null) q.enqueue(r.right);\r\n       }\r\n    }\r\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      fvisit(p,f);\r\n      preOrder(p.left,f);\r\n      preOrder(p.right,f);\r\n     }\r\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      inOrder(p.left,f);\r\n      fvisit(p,f);\r\n      inOrder(p.right,f);\r\n     }\r\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\r\n      if(p==null) return;\r\n      postOrder(p.left,f);\r\n      postOrder(p.right,f);\r\n      fvisit(p,f);\r\n     }\r\n\r\n   void loadData(int k) { //do not edit this function\r\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\r\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\r\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\r\n      int n = a.length;\r\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\r\n     }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void insert(String xSea, int xSail, int xPaddle) {\r\n    //You should insert here statements to complete this function\r\n\r\n\r\n   }\r\n\r\n//Do not edit this function. Your task is to complete insert function above only.\r\n  void f1() throws Exception {\r\n    clear();\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    inOrder(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n  \r\n//=============================================================\r\n void f2() throws Exception {\r\n    clear();\r\n    loadData(5);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n  void f3() throws Exception {\r\n    clear();\r\n    loadData(9);\r\n    String fname = \"f3.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n\r\n\r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n//=============================================================\r\n void f4() throws Exception {\r\n    clear();\r\n    loadData(13);;\r\n    String fname = \"f4.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //------------------------------------------------------------------------------------\r\n    /*You must keep statements pre-given in this function.\r\n      Your task is to insert statements here, just after this comment,\r\n      to complete the question in the exam paper.*/\r\n \r\n\r\n     \r\n    //------------------------------------------------------------------------------------\r\n    breadth(root,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }  \r\n\r\n }\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)==============================================================\nimport java.io.*;\nimport java.util.*;\n\npublic class BSTree {\n   Node root;\n   BSTree() {root=null;}\n   boolean isEmpty() {\n       return(root==null);\n      }\n   void clear() {\n       root=null;\n      }\n   void visit(Node p) {\n      System.out.print(\"p.info: \");\n      if(p != null) System.out.println(p.info + \" \");\n     }\n   void fvisit(Node p, RandomAccessFile f) throws Exception {\n      if(p != null) f.writeBytes(p.info + \" \");\n     }\n   void breadth(Node p, RandomAccessFile f) throws Exception {\n     if(p==null) return;\n     Queue q = new Queue();\n     q.enqueue(p);Node r;\n     while(!q.isEmpty()) {\n        r = q.dequeue();\n        fvisit(r,f);\n        if(r.left!=null) q.enqueue(r.left);\n        if(r.right!=null) q.enqueue(r.right);\n       }\n    }\n   void preOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      fvisit(p,f);\n      preOrder(p.left,f);\n      preOrder(p.right,f);\n     }\n   void inOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      inOrder(p.left,f);\n      fvisit(p,f);\n      inOrder(p.right,f);\n     }\n   void postOrder(Node p, RandomAccessFile f) throws Exception {\n      if(p==null) return;\n      postOrder(p.left,f);\n      postOrder(p.right,f);\n      fvisit(p,f);\n     }\n\n   void loadData(int k) { //do not edit this function\n      String [] a = Lib.readLineToStrArray(\"data.txt\", k);\n      int [] b = Lib.readLineToIntArray(\"data.txt\", k+1);\n      int [] c = Lib.readLineToIntArray(\"data.txt\", k+2);\n      int n = a.length;\n      for(int i=0;i<n;i++) insert(a[i],b[i],c[i]);\n     }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n  void insert(String xSea, int xSail, int xPaddle) {\n    //You should insert here statements to complete this function\n    if(xSea == null || xSea.length() == 0) return;\n    if(xSea.charAt(0) == 'B') return;      // do nothing when the sea starts with 'B'\n    Node q = new Node(new Boat(xSea, xSail, xPaddle));\n    if(root == null) { root = q; return; }\n    Node p = root, f = null;\n    while(p != null) {\n       f = p;\n       if(xSail == p.info.sail) return;    // the key 'sail' must be unique\n       if(xSail < p.info.sail) p = p.left; else p = p.right;\n      }\n    if(xSail < f.info.sail) f.left = q; else f.right = q;\n   }\n\n  //Helper: breadth-first traversal writing only the nodes with paddle > 5\n  void breadth2(Node p, RandomAccessFile f) throws Exception {\n    if(p==null) return;\n    Queue q = new Queue();\n    q.enqueue(p);Node r;\n    while(!q.isEmpty()) {\n       r = q.dequeue();\n       if(r.info.paddle > 5) fvisit(r,f);\n       if(r.left!=null) q.enqueue(r.left);\n       if(r.right!=null) q.enqueue(r.right);\n      }\n   }\n\n  //Helper: collect the in-order sequence of nodes (with their fathers)\n  void inOrderCollect(Node p, Node father, ArrayList<Node> nodes, ArrayList<Node> fathers) {\n    if(p==null) return;\n    inOrderCollect(p.left, p, nodes, fathers);\n    nodes.add(p); fathers.add(father);\n    inOrderCollect(p.right, p, nodes, fathers);\n   }\n\n  //Helper: index (in the in-order sequence) of the second node having a left child; -1 if none\n  int secondWithLeft(ArrayList<Node> nodes) {\n    int count = 0;\n    for(int i=0;i<nodes.size();i++) {\n       if(nodes.get(i).left != null) {\n          count++;\n          if(count == 2) return i;\n         }\n      }\n    return -1;\n   }\n\n  //Helper: delete the node q (whose father is fq) by copying\n  void delByCopying(Node q, Node fq) {\n    if(q == null) return;\n    if(q.left == null) {           // no left subtree: q is replaced by its right child\n       Node c = q.right;\n       if(fq == null) root = c;\n       else if(fq.left == q) fq.left = c; else fq.right = c;\n       return;\n      }\n    Node r = q.left, fr = q;       // r = the in-order predecessor of q\n    while(r.right != null) { fr = r; r = r.right; }\n    q.info = r.info;               // copy the predecessor's data into q\n    if(fr == q) fr.left = r.left; else fr.right = r.left;\n   }\n\n//Do not edit this function. Your task is to complete insert function above only.\n  void f1() throws Exception {\n    clear();\n    loadData(1);\n    String fname = \"f1.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    inOrder(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n void f2() throws Exception {\n    clear();\n    loadData(5);\n    String fname = \"f2.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    breadth2(root,f);\n\n    //------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n  void f3() throws Exception {\n    clear();\n    loadData(9);\n    String fname = \"f3.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    ArrayList<Node> nodes = new ArrayList<Node>();\n    ArrayList<Node> fathers = new ArrayList<Node>();\n    inOrderCollect(root, null, nodes, fathers);\n    int k = secondWithLeft(nodes);\n    if(k >= 0) {\n       Node p = nodes.get(k);\n       delByCopying(p.left, p);    // delete q = p.left by copying\n      }\n\n    //------------------------------------------------------------------------------------\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=============================================================\n void f4() throws Exception {\n    clear();\n    loadData(13);;\n    String fname = \"f4.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    //------------------------------------------------------------------------------------\n    /*You must keep statements pre-given in this function.\n      Your task is to insert statements here, just after this comment,\n      to complete the question in the exam paper.*/\n    ArrayList<Node> nodes2 = new ArrayList<Node>();\n    ArrayList<Node> fathers2 = new ArrayList<Node>();\n    inOrderCollect(root, null, nodes2, fathers2);\n    int k2 = secondWithLeft(nodes2);\n    if(k2 >= 0) {\n       Node p = nodes2.get(k2);\n       Node fp = fathers2.get(k2);\n       Node q = p.left;            // rotate p to the right about its left child q\n       p.left = q.right;\n       q.right = p;\n       if(fp == null) root = q;\n       else if(fp.left == p) fp.left = q; else fp.right = q;\n      }\n\n    //------------------------------------------------------------------------------------\n    breadth(root,f);\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n }\n",
          "expectedOutput": "(C,8,2) (D,6,1) (E,9,4) (F,2,-1) (G,7,3) (H,10,8) (I,1,7) (J,3,9) (K,-1,5) (L,5,10) (M,4,6)\n(C,8,2) (D,6,1) (E,9,4) (I,1,7) (G,7,3) (H,10,8) (K,-1,5) (F,2,-1) (J,3,9) (L,5,10) (M,4,6)",
          "explanation": "<p>The tree and the search for p are exactly the same as in the previous question, so again p = F(2) with left child q = I(1). A right rotation about p makes q take p's place: F.left receives q's old right child (I had none, so null), q.right becomes F, and F's father D now points to I instead of F. Nothing is deleted — all 11 nodes are still there — but F has moved one level down and now appears after K in the breadth-first order. The in-order sequence is unchanged by a rotation, which is the standard way to check the result.</p>|||<p>Cây và việc tìm p giống hệt câu trước, nên vẫn là p = F(2) với con trái q = I(1). Xoay phải quanh p khiến q lên thay chỗ p: F.left nhận con phải cũ của q (I không có nên là null), q.right thành F, và cha của F là D giờ trỏ tới I thay vì F. Không có gì bị xoá — vẫn đủ 11 node — nhưng F tụt xuống một mức và giờ xuất hiện sau K trong thứ tự breadth-first. Phép xoay không làm đổi dãy in-order, đó là cách chuẩn để kiểm tra kết quả.</p>",
          "rubric": [
            {
              "id": "find_p",
              "criterion": "Correctly finds, via in-order traversal, the SECOND node p having a left child, together with its father.|||Tìm đúng, qua duyệt in-order, node p THỨ HAI có con trái, cùng với cha của nó.",
              "weight": 1,
              "maxScore": 0.4
            },
            {
              "id": "rotation",
              "criterion": "Correctly performs the right rotation: q = p.left takes p's place, p.left receives q's old right subtree, q.right becomes p.|||Xoay phải đúng: q = p.left lên thay chỗ p, p.left nhận cây con phải cũ của q, q.right thành p.",
              "weight": 1,
              "maxScore": 0.45
            },
            {
              "id": "reattach_father",
              "criterion": "Correctly re-attaches q to p's father (or updates root when p was the root), and does nothing when no such node exists.|||Nối lại q vào cha của p đúng (hoặc cập nhật root khi p là gốc), và không làm gì khi không có node như vậy.",
              "weight": 1,
              "maxScore": 0.15
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>Question 3 (2 marks) — Scenario:</strong> in this question you should complete some methods in <code>Graph.java</code>. The class <code>Graph</code> is the implementation of a graph over 9 vertices A..I, stored as an adjacency matrix loaded from data.txt.</p><p><strong>3.1 f1() – 1 mark: breadth-first traversal displaying a vertex range</strong></p><ul><li>File: Graph.java</li><li>Method to Complete: the body of <code>void f1()</code>, just after the marked comment (keep every pre-given statement)</li><li>Task: perform the breadth-first traversal (to the file f1.txt) from the vertex i=4 (the vertex E) but display 5 vertices, <b>from the 2nd vertex to the 6th vertex only</b>. Hint: copy <code>breadth(...)</code> to <code>breadth2(...)</code> and modify the latter one.</li></ul><p><b>Note:</b> the expected output below is the real output of compiling and running the given project (javac/java) on its own <code>data.txt</code>, and it matches the worked example printed in the original exam paper character for character.</p>|||<p><strong>Câu 3 (2 điểm) — Bối cảnh:</strong> ở câu này bạn cần hoàn thiện một số method trong <code>Graph.java</code>. Lớp <code>Graph</code> là cài đặt một đồ thị trên 9 đỉnh A..I, lưu bằng ma trận kề nạp từ data.txt.</p><p><strong>3.1 f1() – 1 điểm: duyệt breadth-first, chỉ hiện một đoạn đỉnh</strong></p><ul><li>File: Graph.java</li><li>Method cần hoàn thiện: phần thân <code>void f1()</code>, ngay sau chú thích đánh dấu (giữ nguyên mọi câu lệnh cho sẵn)</li><li>Nhiệm vụ: duyệt breadth-first (ghi vào file f1.txt) từ đỉnh i=4 (đỉnh E) nhưng chỉ hiện 5 đỉnh, <b>từ đỉnh thứ 2 đến đỉnh thứ 6</b>. Gợi ý: copy <code>breadth(...)</code> thành <code>breadth2(...)</code> rồi sửa hàm mới.</li></ul><p><b>Lưu ý:</b> kết quả mong đợi dưới đây là output THẬT của việc biên dịch và chạy project given (javac/java) trên chính <code>data.txt</code> của nó, và khớp từng ký tự với ví dụ minh hoạ in trong đề gốc.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)============================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n//-------------------------------------------------------------------------------\r\npublic class Graph {\r\n  int [][] a; int n;\r\n  char v[];\r\n  int deg[];\r\n  Graph() {\r\n    v = \"ABCDEFGHIJKLMNOP\".toCharArray();\r\n    deg = new int[20];\r\n    a = new int[20][20];\r\n    n = 0;\r\n   }\r\n\r\n  void loadData(int k) {  //do not edit this function\r\n    RandomAccessFile f;int i,j,x;\r\n    String s;StringTokenizer t;\r\n    a = new int[20][20];\r\n    try {\r\n     f = new RandomAccessFile(\"data.txt\",\"r\");\r\n     for(i=0;i<k;i++) f.readLine();\r\n     s = f.readLine();s = s.trim();\r\n     n = Integer.parseInt(s);\r\n     for(i=0;i<n;i++) {\r\n       s = f.readLine();s = s.trim();\r\n       t = new StringTokenizer(s);\r\n       for(j=0;j<n;j++) { \r\n         x = Integer.parseInt(t.nextToken().trim());\r\n         a[i][j] = x;\r\n        }\r\n       }\r\n     f.close();\r\n     }\r\n    catch(Exception e) {}\r\n\r\n   }\r\n\r\n  void dispAdj() {\r\n    int i,j;\r\n    for(i=0;i<n;i++) {\r\n      System.out.println();\r\n      for(j=0;j<n;j++)\r\n        System.out.printf(\"%4d\",a[i][j]);\r\n     }\r\n   }\r\n\r\n  void fvisit(int i, RandomAccessFile f) throws Exception {\r\n    f.writeBytes(\"  \"+v[i]);\r\n   }\r\n\r\n void fdispAdj(RandomAccessFile f) throws Exception { \r\n    int i,j;\r\n    f.writeBytes(\"n = \"+n+\"\\r\\n\");\r\n    for(i=0;i<n;i++) {\r\n      f.writeBytes(\"\\r\\n\");\r\n      for(j=0;j<n;j++)  f.writeBytes(\"  \" + a[i][j]);\r\n     }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void breadth(boolean [] en, int i, RandomAccessFile f) throws Exception {\r\n    Queue q = new Queue();\r\n    int r,j;\r\n    q.enqueue(i); en[i]=true;\r\n    while(!q.isEmpty()) {\r\n      r = q.dequeue();\r\n      fvisit(r,f);\r\n      for(j=0;j<n;j++) {\r\n        if(!en[j] && a[r][j]>0) {\r\n         q.enqueue(j);en[j]=true;\r\n        }\r\n       }\r\n     }\r\n   }\r\n\r\n  void breadth(int  k, RandomAccessFile f) throws Exception {\r\n    boolean [] en = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) en[i]=false;\r\n    breadth(en,k,f);\r\n    for(i=0;i<n;i++) \r\n      if(!en[i]) breadth(en,i,f);\r\n   }\r\n\r\n void depth(boolean [] visited,int k, RandomAccessFile f) throws Exception {\r\n    fvisit(k,f);visited[k]=true;\r\n    for(int i=0;i<n;i++) {\r\n      if(!visited[i] && a[k][i]>0) depth(visited,i,f);\r\n     }\r\n   }\r\n  void depth(int k, RandomAccessFile f) throws Exception {\r\n    boolean [] visited = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) visited[i]=false;\r\n    depth(visited,k,f);\r\n    for(i=0;i<n;i++) \r\n       if(!visited[i]) depth(visited,i,f);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void f1() throws Exception {\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(4,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n//=================================================================================================\r\n  void f2() throws Exception {\r\n    loadData(12);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n      // You can use the statement fvisit(i,f); i = 0, 1, 2,...,n-1 to display the vertex i to file f2.txt \r\n      //  and statement f.writeBytes(\" \" + k); to write  variable k to the file f2.txt  \r\n\r\n\r\n\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n}\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)============================================\nimport java.io.*;\nimport java.util.*;\n//-------------------------------------------------------------------------------\npublic class Graph {\n  int [][] a; int n;\n  char v[];\n  int deg[];\n  Graph() {\n    v = \"ABCDEFGHIJKLMNOP\".toCharArray();\n    deg = new int[20];\n    a = new int[20][20];\n    n = 0;\n   }\n\n  void loadData(int k) {  //do not edit this function\n    RandomAccessFile f;int i,j,x;\n    String s;StringTokenizer t;\n    a = new int[20][20];\n    try {\n     f = new RandomAccessFile(\"data.txt\",\"r\");\n     for(i=0;i<k;i++) f.readLine();\n     s = f.readLine();s = s.trim();\n     n = Integer.parseInt(s);\n     for(i=0;i<n;i++) {\n       s = f.readLine();s = s.trim();\n       t = new StringTokenizer(s);\n       for(j=0;j<n;j++) {\n         x = Integer.parseInt(t.nextToken().trim());\n         a[i][j] = x;\n        }\n       }\n     f.close();\n     }\n    catch(Exception e) {}\n\n   }\n\n  void dispAdj() {\n    int i,j;\n    for(i=0;i<n;i++) {\n      System.out.println();\n      for(j=0;j<n;j++)\n        System.out.printf(\"%4d\",a[i][j]);\n     }\n   }\n\n  void fvisit(int i, RandomAccessFile f) throws Exception {\n    f.writeBytes(\"  \"+v[i]);\n   }\n\n void fdispAdj(RandomAccessFile f) throws Exception {\n    int i,j;\n    f.writeBytes(\"n = \"+n+\"\\r\\n\");\n    for(i=0;i<n;i++) {\n      f.writeBytes(\"\\r\\n\");\n      for(j=0;j<n;j++)  f.writeBytes(\"  \" + a[i][j]);\n     }\n    f.writeBytes(\"\\r\\n\");\n   }\n\n  void breadth(boolean [] en, int i, RandomAccessFile f) throws Exception {\n    Queue q = new Queue();\n    int r,j;\n    q.enqueue(i); en[i]=true;\n    while(!q.isEmpty()) {\n      r = q.dequeue();\n      fvisit(r,f);\n      for(j=0;j<n;j++) {\n        if(!en[j] && a[r][j]>0) {\n         q.enqueue(j);en[j]=true;\n        }\n       }\n     }\n   }\n\n  void breadth(int  k, RandomAccessFile f) throws Exception {\n    boolean [] en = new boolean[20];\n    int i;\n    for(i=0;i<n;i++) en[i]=false;\n    breadth(en,k,f);\n    for(i=0;i<n;i++)\n      if(!en[i]) breadth(en,i,f);\n   }\n\n void depth(boolean [] visited,int k, RandomAccessFile f) throws Exception {\n    fvisit(k,f);visited[k]=true;\n    for(int i=0;i<n;i++) {\n      if(!visited[i] && a[k][i]>0) depth(visited,i,f);\n     }\n   }\n  void depth(int k, RandomAccessFile f) throws Exception {\n    boolean [] visited = new boolean[20];\n    int i;\n    for(i=0;i<n;i++) visited[i]=false;\n    depth(visited,k,f);\n    for(i=0;i<n;i++)\n       if(!visited[i]) depth(visited,i,f);\n   }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n  int cnt2;   // counts how many vertices have been visited so far by breadth2\n\n  void breadth2(boolean [] en, int i, RandomAccessFile f) throws Exception {\n    Queue q = new Queue();\n    int r,j;\n    q.enqueue(i); en[i]=true;\n    while(!q.isEmpty()) {\n      r = q.dequeue();\n      cnt2++;\n      if(cnt2 >= 2 && cnt2 <= 6) fvisit(r,f);   // display the 2nd .. 6th vertex only\n      for(j=0;j<n;j++) {\n        if(!en[j] && a[r][j]>0) {\n         q.enqueue(j);en[j]=true;\n        }\n       }\n     }\n   }\n\n  void breadth2(int  k, RandomAccessFile f) throws Exception {\n    boolean [] en = new boolean[20];\n    int i;\n    cnt2 = 0;\n    for(i=0;i<n;i++) en[i]=false;\n    breadth2(en,k,f);\n    for(i=0;i<n;i++)\n      if(!en[i]) breadth2(en,i,f);\n   }\n\n  int [] dLabel, dPrev, dOrder;\n  int dCount;\n\n  //Dijkstra from the vertex s; stops as soon as the vertex t has been selected into S\n  void dijkstra(int s, int t) {\n    boolean [] inS = new boolean[20];\n    dLabel = new int[20]; dPrev = new int[20]; dOrder = new int[20];\n    int i,j;\n    for(i=0;i<n;i++) { dLabel[i] = 99; dPrev[i] = -1; inS[i] = false; }\n    dLabel[s] = 0;\n    dCount = 0;\n    while(true) {\n      int min = -1;\n      for(i=0;i<n;i++)\n        if(!inS[i] && dLabel[i] < 99 && (min == -1 || dLabel[i] < dLabel[min])) min = i;\n      if(min == -1) break;\n      inS[min] = true;\n      dOrder[dCount++] = min;\n      if(min == t) break;\n      for(j=0;j<n;j++)\n        if(!inS[j] && j != min && a[min][j] < 99 && a[min][j] > 0\n           && dLabel[min] + a[min][j] < dLabel[j]) {\n           dLabel[j] = dLabel[min] + a[min][j];\n           dPrev[j] = min;\n          }\n     }\n   }\n\n  void f1() throws Exception {\n    loadData(1);\n    String fname = \"f1.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(4,f);\n    f.writeBytes(\"\\r\\n\");\n    //-------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    breadth2(4,f);\n\n    //-------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=================================================================================================\n  void f2() throws Exception {\n    loadData(12);\n    String fname = \"f2.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    f.writeBytes(\"\\r\\n\");\n    //-------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n      // You can use the statement fvisit(i,f); i = 0, 1, 2,...,n-1 to display the vertex i to file f2.txt\n      //  and statement f.writeBytes(\" \" + k); to write  variable k to the file f2.txt\n    int i;\n    dijkstra(0,6);                       // (1) shortest path from A (0) to G (6)\n    int [] last3 = new int[3];\n    for(i=0;i<3;i++) last3[i] = dOrder[dCount-3+i];\n    for(i=0;i<3;i++) fvisit(last3[i],f);          // line 1: last 3 vertices selected into S\n    f.writeBytes(\"\\r\\n\");\n    for(i=0;i<3;i++) f.writeBytes(\" \" + dLabel[last3[i]]);  // line 2: their labels\n    f.writeBytes(\"\\r\\n\");\n\n    dijkstra(2,5);                       // (2) shortest path from C (2) to F (5)\n    int [] path = new int[20]; int m = 0;\n    int k = 5;\n    while(k != -1) { path[m++] = k; k = dPrev[k]; }\n    for(i=m-1;i>=0;i--) fvisit(path[i],f);        // line 3: vertices in shortest path (2)\n\n    //-------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n}\n",
          "expectedOutput": "E B H A I C D G F\nB H A I C",
          "explanation": "<p>Line 1 is the full breadth-first traversal (written by the pre-given <code>breadth(4,f)</code> call), line 2 is your ranged one. Starting at E the queue visits E, then E's neighbours B and H, then A (from B), I (from H), then C and D (from A), then G (from D) — that is 8 vertices. F is unreachable from E, so the pre-given two-phase <code>breadth(int, ...)</code> restarts on it and it comes last. Your <code>breadth2</code> must keep that same two-phase structure and count vertices <b>globally across both phases</b>, then write only counts 2 through 6: B, H, A, I, C. Counting per-phase or forgetting the restart both give a wrong answer.</p>|||<p>Dòng 1 là duyệt breadth-first đầy đủ (do lệnh <code>breadth(4,f)</code> cho sẵn ghi ra), dòng 2 là bản giới hạn của bạn. Bắt đầu từ E, hàng đợi thăm E, rồi hai láng giềng B và H của E, rồi A (từ B), I (từ H), rồi C và D (từ A), rồi G (từ D) — được 8 đỉnh. F không tới được từ E nên hàm <code>breadth(int, ...)</code> cho sẵn (hai pha) khởi động lại ở nó và nó nằm cuối. Hàm <code>breadth2</code> của bạn phải giữ nguyên cấu trúc hai pha đó và đếm đỉnh <b>chung cho cả hai pha</b>, rồi chỉ ghi các đỉnh thứ 2 đến thứ 6: B, H, A, I, C. Đếm riêng từng pha hoặc quên pha khởi động lại đều cho kết quả sai.</p>",
          "rubric": [
            {
              "id": "breadth_copy",
              "criterion": "Correctly reproduces the given two-phase breadth-first traversal (queue-based, then restarting on any still-unvisited vertex) starting from vertex 4.|||Tái tạo đúng phép duyệt breadth-first hai pha cho sẵn (dùng hàng đợi, sau đó khởi động lại ở đỉnh chưa thăm) bắt đầu từ đỉnh 4.",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "range_filter",
              "criterion": "Only writes the vertices that are the 2nd through 6th visited overall (5 vertices), counting across the whole traversal rather than per component.|||Chỉ ghi ra các đỉnh được thăm thứ 2 đến thứ 6 tính chung toàn bộ phép duyệt (5 đỉnh), không đếm riêng theo từng thành phần.",
              "weight": 1,
              "maxScore": 0.5
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 1,
          "language": "java",
          "prompt": "<p><strong>3.2 f2() – 1 mark: two Dijkstra shortest-path runs</strong></p><ul><li>File: Graph.java</li><li>Method to Complete: the body of <code>void f2()</code>, just after the marked comment (keep every pre-given statement)</li><li>Task: apply Dijkstra's shortest path algorithm to find (1) the shortest path from vertex 0 (A) to vertex 6 (G), then (2) from vertex 2 (C) to vertex 5 (F). Write 3 lines to the file f2.txt: line 1 contains the <b>last 3 vertices selected into the set S</b> for shortest path (1), line 2 contains the <b>labels</b> of the vertices in line 1 correspondingly, line 3 contains the <b>vertices in shortest path (2)</b>. (Note that in the weighted matrix, the value 99 is considered as infinity.)</li><li>You can use <code>fvisit(i,f)</code> to display the vertex i, and <code>f.writeBytes(\" \" + k)</code> to write the variable k.</li></ul><p><b>Note:</b> the expected output below is the real output of compiling and running the given project (javac/java) on its own <code>data.txt</code>, and it matches the worked example printed in the original exam paper character for character.</p>|||<p><strong>3.2 f2() – 1 điểm: hai lượt Dijkstra tìm đường đi ngắn nhất</strong></p><ul><li>File: Graph.java</li><li>Method cần hoàn thiện: phần thân <code>void f2()</code>, ngay sau chú thích đánh dấu (giữ nguyên mọi câu lệnh cho sẵn)</li><li>Nhiệm vụ: áp dụng thuật toán Dijkstra để tìm (1) đường đi ngắn nhất từ đỉnh 0 (A) tới đỉnh 6 (G), rồi (2) từ đỉnh 2 (C) tới đỉnh 5 (F). Ghi 3 dòng vào file f2.txt: dòng 1 là <b>3 đỉnh cuối cùng được chọn vào tập S</b> của đường đi (1), dòng 2 là <b>nhãn</b> của các đỉnh ở dòng 1 tương ứng, dòng 3 là <b>các đỉnh trên đường đi ngắn nhất (2)</b>. (Lưu ý trong ma trận trọng số, giá trị 99 được coi là vô cùng.)</li><li>Có thể dùng <code>fvisit(i,f)</code> để hiện đỉnh i, và <code>f.writeBytes(\" \" + k)</code> để ghi biến k.</li></ul><p><b>Lưu ý:</b> kết quả mong đợi dưới đây là output THẬT của việc biên dịch và chạy project given (javac/java) trên chính <code>data.txt</code> của nó, và khớp từng ký tự với ví dụ minh hoạ in trong đề gốc.</p>",
          "starterCode": "/* This program contains 2 parts: (1) and (2)\r\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\r\n */\r\n//(1)============================================\r\nimport java.io.*;\r\nimport java.util.*;\r\n//-------------------------------------------------------------------------------\r\npublic class Graph {\r\n  int [][] a; int n;\r\n  char v[];\r\n  int deg[];\r\n  Graph() {\r\n    v = \"ABCDEFGHIJKLMNOP\".toCharArray();\r\n    deg = new int[20];\r\n    a = new int[20][20];\r\n    n = 0;\r\n   }\r\n\r\n  void loadData(int k) {  //do not edit this function\r\n    RandomAccessFile f;int i,j,x;\r\n    String s;StringTokenizer t;\r\n    a = new int[20][20];\r\n    try {\r\n     f = new RandomAccessFile(\"data.txt\",\"r\");\r\n     for(i=0;i<k;i++) f.readLine();\r\n     s = f.readLine();s = s.trim();\r\n     n = Integer.parseInt(s);\r\n     for(i=0;i<n;i++) {\r\n       s = f.readLine();s = s.trim();\r\n       t = new StringTokenizer(s);\r\n       for(j=0;j<n;j++) { \r\n         x = Integer.parseInt(t.nextToken().trim());\r\n         a[i][j] = x;\r\n        }\r\n       }\r\n     f.close();\r\n     }\r\n    catch(Exception e) {}\r\n\r\n   }\r\n\r\n  void dispAdj() {\r\n    int i,j;\r\n    for(i=0;i<n;i++) {\r\n      System.out.println();\r\n      for(j=0;j<n;j++)\r\n        System.out.printf(\"%4d\",a[i][j]);\r\n     }\r\n   }\r\n\r\n  void fvisit(int i, RandomAccessFile f) throws Exception {\r\n    f.writeBytes(\"  \"+v[i]);\r\n   }\r\n\r\n void fdispAdj(RandomAccessFile f) throws Exception { \r\n    int i,j;\r\n    f.writeBytes(\"n = \"+n+\"\\r\\n\");\r\n    for(i=0;i<n;i++) {\r\n      f.writeBytes(\"\\r\\n\");\r\n      for(j=0;j<n;j++)  f.writeBytes(\"  \" + a[i][j]);\r\n     }\r\n    f.writeBytes(\"\\r\\n\");\r\n   }\r\n\r\n  void breadth(boolean [] en, int i, RandomAccessFile f) throws Exception {\r\n    Queue q = new Queue();\r\n    int r,j;\r\n    q.enqueue(i); en[i]=true;\r\n    while(!q.isEmpty()) {\r\n      r = q.dequeue();\r\n      fvisit(r,f);\r\n      for(j=0;j<n;j++) {\r\n        if(!en[j] && a[r][j]>0) {\r\n         q.enqueue(j);en[j]=true;\r\n        }\r\n       }\r\n     }\r\n   }\r\n\r\n  void breadth(int  k, RandomAccessFile f) throws Exception {\r\n    boolean [] en = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) en[i]=false;\r\n    breadth(en,k,f);\r\n    for(i=0;i<n;i++) \r\n      if(!en[i]) breadth(en,i,f);\r\n   }\r\n\r\n void depth(boolean [] visited,int k, RandomAccessFile f) throws Exception {\r\n    fvisit(k,f);visited[k]=true;\r\n    for(int i=0;i<n;i++) {\r\n      if(!visited[i] && a[k][i]>0) depth(visited,i,f);\r\n     }\r\n   }\r\n  void depth(int k, RandomAccessFile f) throws Exception {\r\n    boolean [] visited = new boolean[20];\r\n    int i;\r\n    for(i=0;i<n;i++) visited[i]=false;\r\n    depth(visited,k,f);\r\n    for(i=0;i<n;i++) \r\n       if(!visited[i]) depth(visited,i,f);\r\n   }\r\n\r\n//===========================================================================\r\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\r\n//===========================================================================\r\n  void f1() throws Exception {\r\n    loadData(1);\r\n    String fname = \"f1.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    breadth(4,f);\r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n\r\n\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n//=================================================================================================\r\n  void f2() throws Exception {\r\n    loadData(12);\r\n    String fname = \"f2.txt\";\r\n    File g123 = new File(fname);\r\n    if(g123.exists()) g123.delete();\r\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\"); \r\n    f.writeBytes(\"\\r\\n\");\r\n    //-------------------------------------------------------------------------------------\r\n     /*You must keep statements pre-given in this function.\r\n       Your task is to insert statements here, just after this comment,\r\n       to complete the question in the exam paper.*/\r\n      // You can use the statement fvisit(i,f); i = 0, 1, 2,...,n-1 to display the vertex i to file f2.txt \r\n      //  and statement f.writeBytes(\" \" + k); to write  variable k to the file f2.txt  \r\n\r\n\r\n\r\n    //-------------------------------------------------------------------------------------\r\n    f.writeBytes(\"\\r\\n\");\r\n    f.close();\r\n   }\r\n\r\n}\r\n",
          "sampleSolution": "/* This program contains 2 parts: (1) and (2)\n   YOUR TASK IS TO COMPLETE THE PART  (2)  ONLY\n */\n//(1)============================================\nimport java.io.*;\nimport java.util.*;\n//-------------------------------------------------------------------------------\npublic class Graph {\n  int [][] a; int n;\n  char v[];\n  int deg[];\n  Graph() {\n    v = \"ABCDEFGHIJKLMNOP\".toCharArray();\n    deg = new int[20];\n    a = new int[20][20];\n    n = 0;\n   }\n\n  void loadData(int k) {  //do not edit this function\n    RandomAccessFile f;int i,j,x;\n    String s;StringTokenizer t;\n    a = new int[20][20];\n    try {\n     f = new RandomAccessFile(\"data.txt\",\"r\");\n     for(i=0;i<k;i++) f.readLine();\n     s = f.readLine();s = s.trim();\n     n = Integer.parseInt(s);\n     for(i=0;i<n;i++) {\n       s = f.readLine();s = s.trim();\n       t = new StringTokenizer(s);\n       for(j=0;j<n;j++) {\n         x = Integer.parseInt(t.nextToken().trim());\n         a[i][j] = x;\n        }\n       }\n     f.close();\n     }\n    catch(Exception e) {}\n\n   }\n\n  void dispAdj() {\n    int i,j;\n    for(i=0;i<n;i++) {\n      System.out.println();\n      for(j=0;j<n;j++)\n        System.out.printf(\"%4d\",a[i][j]);\n     }\n   }\n\n  void fvisit(int i, RandomAccessFile f) throws Exception {\n    f.writeBytes(\"  \"+v[i]);\n   }\n\n void fdispAdj(RandomAccessFile f) throws Exception {\n    int i,j;\n    f.writeBytes(\"n = \"+n+\"\\r\\n\");\n    for(i=0;i<n;i++) {\n      f.writeBytes(\"\\r\\n\");\n      for(j=0;j<n;j++)  f.writeBytes(\"  \" + a[i][j]);\n     }\n    f.writeBytes(\"\\r\\n\");\n   }\n\n  void breadth(boolean [] en, int i, RandomAccessFile f) throws Exception {\n    Queue q = new Queue();\n    int r,j;\n    q.enqueue(i); en[i]=true;\n    while(!q.isEmpty()) {\n      r = q.dequeue();\n      fvisit(r,f);\n      for(j=0;j<n;j++) {\n        if(!en[j] && a[r][j]>0) {\n         q.enqueue(j);en[j]=true;\n        }\n       }\n     }\n   }\n\n  void breadth(int  k, RandomAccessFile f) throws Exception {\n    boolean [] en = new boolean[20];\n    int i;\n    for(i=0;i<n;i++) en[i]=false;\n    breadth(en,k,f);\n    for(i=0;i<n;i++)\n      if(!en[i]) breadth(en,i,f);\n   }\n\n void depth(boolean [] visited,int k, RandomAccessFile f) throws Exception {\n    fvisit(k,f);visited[k]=true;\n    for(int i=0;i<n;i++) {\n      if(!visited[i] && a[k][i]>0) depth(visited,i,f);\n     }\n   }\n  void depth(int k, RandomAccessFile f) throws Exception {\n    boolean [] visited = new boolean[20];\n    int i;\n    for(i=0;i<n;i++) visited[i]=false;\n    depth(visited,k,f);\n    for(i=0;i<n;i++)\n       if(!visited[i]) depth(visited,i,f);\n   }\n\n//===========================================================================\n//(2)===YOU CAN EDIT OR EVEN ADD NEW FUNCTIONS IN THE FOLLOWING PART========\n//===========================================================================\n  int cnt2;   // counts how many vertices have been visited so far by breadth2\n\n  void breadth2(boolean [] en, int i, RandomAccessFile f) throws Exception {\n    Queue q = new Queue();\n    int r,j;\n    q.enqueue(i); en[i]=true;\n    while(!q.isEmpty()) {\n      r = q.dequeue();\n      cnt2++;\n      if(cnt2 >= 2 && cnt2 <= 6) fvisit(r,f);   // display the 2nd .. 6th vertex only\n      for(j=0;j<n;j++) {\n        if(!en[j] && a[r][j]>0) {\n         q.enqueue(j);en[j]=true;\n        }\n       }\n     }\n   }\n\n  void breadth2(int  k, RandomAccessFile f) throws Exception {\n    boolean [] en = new boolean[20];\n    int i;\n    cnt2 = 0;\n    for(i=0;i<n;i++) en[i]=false;\n    breadth2(en,k,f);\n    for(i=0;i<n;i++)\n      if(!en[i]) breadth2(en,i,f);\n   }\n\n  int [] dLabel, dPrev, dOrder;\n  int dCount;\n\n  //Dijkstra from the vertex s; stops as soon as the vertex t has been selected into S\n  void dijkstra(int s, int t) {\n    boolean [] inS = new boolean[20];\n    dLabel = new int[20]; dPrev = new int[20]; dOrder = new int[20];\n    int i,j;\n    for(i=0;i<n;i++) { dLabel[i] = 99; dPrev[i] = -1; inS[i] = false; }\n    dLabel[s] = 0;\n    dCount = 0;\n    while(true) {\n      int min = -1;\n      for(i=0;i<n;i++)\n        if(!inS[i] && dLabel[i] < 99 && (min == -1 || dLabel[i] < dLabel[min])) min = i;\n      if(min == -1) break;\n      inS[min] = true;\n      dOrder[dCount++] = min;\n      if(min == t) break;\n      for(j=0;j<n;j++)\n        if(!inS[j] && j != min && a[min][j] < 99 && a[min][j] > 0\n           && dLabel[min] + a[min][j] < dLabel[j]) {\n           dLabel[j] = dLabel[min] + a[min][j];\n           dPrev[j] = min;\n          }\n     }\n   }\n\n  void f1() throws Exception {\n    loadData(1);\n    String fname = \"f1.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    breadth(4,f);\n    f.writeBytes(\"\\r\\n\");\n    //-------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n    breadth2(4,f);\n\n    //-------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n//=================================================================================================\n  void f2() throws Exception {\n    loadData(12);\n    String fname = \"f2.txt\";\n    File g123 = new File(fname);\n    if(g123.exists()) g123.delete();\n    RandomAccessFile  f = new RandomAccessFile(fname, \"rw\");\n    f.writeBytes(\"\\r\\n\");\n    //-------------------------------------------------------------------------------------\n     /*You must keep statements pre-given in this function.\n       Your task is to insert statements here, just after this comment,\n       to complete the question in the exam paper.*/\n      // You can use the statement fvisit(i,f); i = 0, 1, 2,...,n-1 to display the vertex i to file f2.txt\n      //  and statement f.writeBytes(\" \" + k); to write  variable k to the file f2.txt\n    int i;\n    dijkstra(0,6);                       // (1) shortest path from A (0) to G (6)\n    int [] last3 = new int[3];\n    for(i=0;i<3;i++) last3[i] = dOrder[dCount-3+i];\n    for(i=0;i<3;i++) fvisit(last3[i],f);          // line 1: last 3 vertices selected into S\n    f.writeBytes(\"\\r\\n\");\n    for(i=0;i<3;i++) f.writeBytes(\" \" + dLabel[last3[i]]);  // line 2: their labels\n    f.writeBytes(\"\\r\\n\");\n\n    dijkstra(2,5);                       // (2) shortest path from C (2) to F (5)\n    int [] path = new int[20]; int m = 0;\n    int k = 5;\n    while(k != -1) { path[m++] = k; k = dPrev[k]; }\n    for(i=m-1;i>=0;i--) fvisit(path[i],f);        // line 3: vertices in shortest path (2)\n\n    //-------------------------------------------------------------------------------------\n    f.writeBytes(\"\\r\\n\");\n    f.close();\n   }\n\n}\n",
          "expectedOutput": "D F G\n19 24 29\nC E D F",
          "explanation": "<p>Run (1), from A. Dijkstra selects vertices into S in this order, with these final labels: A(0), I(2), B(10), C(12), H(12), E(15), D(19), F(24), G(29). The last 3 selected are therefore D, F, G with labels 19, 24, 29 — lines 1 and 2. Two relaxations decide the answer: C lowers E from 25 to 15 (12+3), and E then lowers D from 20 to 19 (15+4), which in turn gives F = 24 and G = 29 through D. Run (2), from C: C(0), I(2), E(3), D(7), F(12); the predecessor chain of F is F←D←E←C, so the path printed on line 3 is C E D F. Note the graph is directed — a[i][j] is not a[j][i] — so you must follow the rows, and 99 must be treated as no edge at all.</p>|||<p>Lượt (1), từ A. Dijkstra chọn các đỉnh vào S theo thứ tự này, với nhãn cuối cùng: A(0), I(2), B(10), C(12), H(12), E(15), D(19), F(24), G(29). Vậy 3 đỉnh cuối được chọn là D, F, G với nhãn 19, 24, 29 — chính là dòng 1 và dòng 2. Hai lần nới lỏng quyết định đáp án: C hạ E từ 25 xuống 15 (12+3), rồi E hạ D từ 20 xuống 19 (15+4), từ đó qua D mới ra F = 24 và G = 29. Lượt (2), từ C: C(0), I(2), E(3), D(7), F(12); chuỗi tiền nhiệm của F là F←D←E←C nên đường đi ghi ở dòng 3 là C E D F. Lưu ý đồ thị có hướng — a[i][j] khác a[j][i] — nên phải đi theo hàng, và 99 phải được coi là hoàn toàn không có cạnh.</p>",
          "rubric": [
            {
              "id": "dijkstra_core",
              "criterion": "Implements Dijkstra correctly on the directed weighted matrix: repeatedly select the unselected vertex with the smallest label, relax its out-edges, and treat 99 as infinity (no edge).|||Cài đặt Dijkstra đúng trên ma trận trọng số có hướng: lặp lại việc chọn đỉnh chưa chọn có nhãn nhỏ nhất, nới lỏng các cung đi ra, và coi 99 là vô cùng (không có cạnh).",
              "weight": 1,
              "maxScore": 0.4
            },
            {
              "id": "last3_and_labels",
              "criterion": "Line 1 lists the last 3 vertices selected into S for run (1) in selection order, and line 2 lists their labels in the same order.|||Dòng 1 liệt kê đúng 3 đỉnh cuối được chọn vào S ở lượt (1) theo thứ tự chọn, dòng 2 liệt kê nhãn của chúng theo đúng thứ tự đó.",
              "weight": 1,
              "maxScore": 0.35
            },
            {
              "id": "path2",
              "criterion": "Line 3 reconstructs the full shortest path of run (2) from C to F, printed from source to destination.|||Dòng 3 dựng lại đầy đủ đường đi ngắn nhất của lượt (2) từ C tới F, in theo chiều từ nguồn tới đích.",
              "weight": 1,
              "maxScore": 0.25
            }
          ]
        }
      ]
    }
  ]
};
