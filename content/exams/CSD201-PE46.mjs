export default {
  "course": {
    "courseCode": "CSD201"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "CODE",
      "code": "PE46",
      "title": "PE Đề 46 — Practical Exam (SU 2023, Paper No: 2)|||PE Đề 46 — Thi thực hành (SU 2023, Paper No: 2)",
      "description": "CSD201 PE (CODE): 3 independent file-I/O Java programs on binary search trees — BFS filtered by V, BFS over a BST with duplicate counts, and full-child nodes by pre-order; every sample verified by compiling and running, AI-graded.|||PE CSD201 (viết mã): 3 chương trình Java độc lập I/O file về cây nhị phân tìm kiếm — duyệt BFS lọc theo V, duyệt BFS trên BST có giá trị trùng, và node đủ hai con theo duyệt trước; mọi bộ mẫu đã xác nhận bằng biên dịch và chạy thật, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "attachmentUrl": "https://media.cuongthai.com/files/exam-attachments/CSD201/PE46-Given.zip",
      "attachmentName": "CSD201-PE46-Given.zip",
      "instructions": "<div class=\"ml-en\"><p><strong>INSTRUCTIONS - PRACTICAL EXAM – CSD201</strong> (Summer 2023, Paper No: 2). Software tools must be used: <b>NetBeans IDE 8.x</b> and <b>Java JDK 1.8</b>.</p>\n   <p>Submission format required by the original paper: run \"Clean and Build Project\" (Shift+F11) to create the <b>dist</b> folder and the <b>.jar</b> file. For each question create two sub-folders <b>run</b> and <b>src</b>: copy the <code>*.jar</code> file into <b>run</b>, compress the source code into a <code>.zip</code> and copy that <code>.zip</code> into <b>src</b>. Then choose the question number in the PEA software and attach the corresponding solution folder.</p>\n   <p><b>Notes from the paper:</b> do not use accented Vietnamese when writing comments in programs; solutions are marked by Automated Marking Software; if at least one of the above requirements is not followed, the exam will get ZERO.</p>\n   <p>Each question below is an independent Java program: read the input from the fixed-name input file and write the result to the fixed-name output file (standard NetBeans working-directory file I/O). The given materials above contain the official empty submission skeleton for the 3 questions (<code>PaperNo_2/1/Q1.java</code>, <code>2/Q2.java</code>, <code>3/Q3.java</code>) — the per-question starter code shown in this exam room is a self-contained equivalent so you can work straight in the code box.</p>\n   <p><b>Note:</b> the sample input/output shown per question is only to help you test your own code — the real grading testcases use different data, so do not hardcode the specific results shown below.</p></div><div class=\"ml-vi\"><p><strong>HƯỚNG DẪN THI THỰC HÀNH – CSD201</strong> (Summer 2023, Paper No: 2). Công cụ bắt buộc: <b>NetBeans IDE 8.x</b> và <b>Java JDK 1.8</b>.</p>\n   <p>Cách nộp bài theo đúng đề gốc: chạy \"Clean and Build Project\" (Shift+F11) để tạo thư mục <b>dist</b> và file <b>.jar</b>. Mỗi câu tạo 2 thư mục con <b>run</b> và <b>src</b>: chép file <code>*.jar</code> vào <b>run</b>, nén mã nguồn thành <code>.zip</code> rồi chép file <code>.zip</code> đó vào <b>src</b>. Sau đó chọn số câu trong phần mềm PEA và đính kèm thư mục lời giải tương ứng.</p>\n   <p><b>Lưu ý của đề:</b> không dùng tiếng Việt có dấu khi viết comment trong chương trình; bài làm được chấm bằng phần mềm chấm tự động; nếu vi phạm dù chỉ một yêu cầu trên, bài thi bị điểm KHÔNG.</p>\n   <p>Mỗi câu dưới đây là 1 chương trình Java độc lập: đọc input từ file tên cố định và ghi kết quả ra file tên cố định khác (I/O file working-directory chuẩn của NetBeans). Given materials ở trên chứa khung nộp bài trống chính thức của 3 câu (<code>PaperNo_2/1/Q1.java</code>, <code>2/Q2.java</code>, <code>3/Q3.java</code>) — khung starter code hiển thị ở mỗi câu trong phòng thi này là bản tương đương tự chứa, để bạn làm được ngay trong ô mã.</p>\n   <p><b>Lưu ý:</b> input/output mẫu ở mỗi câu chỉ để bạn tự test — bộ test chấm điểm thật dùng dữ liệu khác, đừng hardcode kết quả mẫu dưới đây.</p></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "CODE",
          "points": 3,
          "language": "java",
          "prompt": "<p><strong>Question 1: (3 marks) – BFS traversing of a BST, filtered by V</strong></p>\n     <p>Write a program to build a <b>BST (Binary Search Tree)</b> by inserting N (1 ≤ N ≤ 100) integer values into the BST one by one. Note that the nodes have <b>no duplicate</b> values.</p>\n     <p>Your task is showing the <b>BFS (Breadth First Traversal)</b> traversing of the BST which node's value must be <b>greater than or equal to V</b>.</p>\n     <p><b>For example:</b> create a BST tree by inserting into the tree N integer values as follows: 7, 9, 4, 1, 12, 6, 10. The value of V is 7. The BFS traversing of the BST tree which node's value are greater than or equal to 7 is: 7, 9, 12, 10.</p>\n     <p><b>Input</b> (<code>BFS_input.txt</code>): line 1 — a positive integer N (1 ≤ N ≤ 100), the number of integer values to insert into the BST; line 2 — N integers that will be inserted into the BST one by one, each number separated by at least one space; line 3 — a positive integer V (the value of V is always guaranteed to have results when traversing the tree).</p>\n     <p><b>Output</b> (<code>BFS_output.txt</code>): one line contains the list of numbers representing the BFS traversing of the BST which node's value must be greater than or equal to V. Each number separated by one comma.</p>\n     <p><b>Sample 1:</b> N=7, values = 7 9 4 1 12 6 10, V=7 → output <code>7,9,12,10</code>.</p>\n     <p><b>Sample 2:</b> N=10, values = 78 21 36 18 30 91 74 55 98 100, V=90 → output <code>91,98,100</code>.</p>|||<p><strong>Câu 1: (3 điểm) – Duyệt BFS trên BST, lọc theo V</strong></p>\n     <p>Viết chương trình dựng 1 <b>BST (cây nhị phân tìm kiếm)</b> bằng cách chèn lần lượt N (1 ≤ N ≤ 100) số nguyên vào cây. Các node <b>không có giá trị trùng nhau</b>.</p>\n     <p>Nhiệm vụ: hiển thị kết quả <b>duyệt BFS (duyệt theo chiều rộng)</b> của BST, chỉ gồm các node có giá trị <b>lớn hơn hoặc bằng V</b>.</p>\n     <p><b>Ví dụ:</b> dựng cây BST bằng cách chèn lần lượt N số nguyên: 7, 9, 4, 1, 12, 6, 10. Giá trị V là 7. Kết quả duyệt BFS của cây với các node có giá trị ≥ 7 là: 7, 9, 12, 10.</p>\n     <p><b>Input</b> (<code>BFS_input.txt</code>): dòng 1 — số nguyên dương N (1 ≤ N ≤ 100), số lượng giá trị cần chèn vào BST; dòng 2 — N số nguyên sẽ được chèn lần lượt vào BST, mỗi số cách nhau ít nhất 1 khoảng trắng; dòng 3 — số nguyên dương V (V luôn được bảo đảm là có kết quả khi duyệt cây).</p>\n     <p><b>Output</b> (<code>BFS_output.txt</code>): 1 dòng chứa danh sách các số là kết quả duyệt BFS của BST, chỉ gồm node có giá trị lớn hơn hoặc bằng V. Mỗi số cách nhau 1 dấu phẩy.</p>\n     <p><b>Mẫu 1:</b> N=7, giá trị = 7 9 4 1 12 6 10, V=7 → output <code>7,9,12,10</code>.</p>\n     <p><b>Mẫu 2:</b> N=10, giá trị = 78 21 36 18 30 91 74 55 98 100, V=90 → output <code>91,98,100</code>.</p>",
          "starterCode": "import java.io.*;\nimport java.util.*;\n\npublic class BFSTraversing {\n    static class Node {\n        int val;\n        Node left, right;\n        Node(int v) { val = v; }\n    }\n\n    static Node root;\n\n    // Insert v into the BST (the nodes have no duplicate values).\n    static void insert(int v) {\n        //You should write here appropriate statements to complete this function.\n\n    }\n\n    static List<Integer> result = new ArrayList<>();\n\n    // Breadth-first (level-order) traversal of the BST, collecting into\n    // `result` every node value that is greater than or equal to v.\n    static void bfs(int v) {\n        //You should write here appropriate statements to complete this function.\n\n    }\n\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"BFS_input.txt\"));\n        int n = Integer.parseInt(sc.nextLine().trim());\n        // The whole second line is tokenized (n is read only for reference).\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        while (st.hasMoreTokens()) insert(Integer.parseInt(st.nextToken()));\n        int v = Integer.parseInt(sc.nextLine().trim());\n        sc.close();\n\n        bfs(v);\n\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < result.size(); i++) {\n            if (i > 0) sb.append(\",\");\n            sb.append(result.get(i));\n        }\n        PrintWriter pw = new PrintWriter(new FileWriter(\"BFS_output.txt\"));\n        pw.println(sb.toString());\n        pw.close();\n    }\n}\n",
          "sampleSolution": "import java.io.*;\nimport java.util.*;\n\npublic class BFSTraversing {\n    static class Node {\n        int val;\n        Node left, right;\n        Node(int v) { val = v; }\n    }\n\n    static Node root;\n\n    // Insert v into the BST (the nodes have no duplicate values).\n    static void insert(int v) {\n        Node p = new Node(v);\n        if (root == null) { root = p; return; }\n        Node q = root;\n        while (true) {\n            if (v == q.val) return;                      // no duplicate values\n            if (v < q.val) {\n                if (q.left == null) { q.left = p; return; }\n                q = q.left;\n            } else {\n                if (q.right == null) { q.right = p; return; }\n                q = q.right;\n            }\n        }\n    }\n\n    static List<Integer> result = new ArrayList<>();\n\n    // Breadth-first (level-order) traversal of the BST, collecting into\n    // `result` every node value that is greater than or equal to v.\n    static void bfs(int v) {\n        if (root == null) return;\n        Queue<Node> queue = new LinkedList<>();\n        queue.add(root);\n        while (!queue.isEmpty()) {\n            Node p = queue.remove();\n            if (p.val >= v) result.add(p.val);\n            if (p.left != null) queue.add(p.left);\n            if (p.right != null) queue.add(p.right);\n        }\n    }\n\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"BFS_input.txt\"));\n        int n = Integer.parseInt(sc.nextLine().trim());\n        // The whole second line is tokenized (n is read only for reference).\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        while (st.hasMoreTokens()) insert(Integer.parseInt(st.nextToken()));\n        int v = Integer.parseInt(sc.nextLine().trim());\n        sc.close();\n\n        bfs(v);\n\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < result.size(); i++) {\n            if (i > 0) sb.append(\",\");\n            sb.append(result.get(i));\n        }\n        PrintWriter pw = new PrintWriter(new FileWriter(\"BFS_output.txt\"));\n        pw.println(sb.toString());\n        pw.close();\n    }\n}\n",
          "expectedOutput": "Sample 1 (N=7, values=7 9 4 1 12 6 10, V=7): 7,9,12,10\nSample 2 (N=10, values=78 21 36 18 30 91 74 55 98 100, V=90): 91,98,100",
          "explanation": "<p>Verified by compiling and running with BOTH of this paper's own sample inputs — the output matches exactly for both. Standard BST insert (no duplicates), then a queue-driven level-order traversal: dequeue a node, keep its value if it is ≥ V, then enqueue its left child and its right child. Filtering does <b>not</b> prune the subtree — a node smaller than V may still hold descendants larger than V (in sample 1, node 4 is skipped but its subtree is still visited), so the filter must be applied per node, never as an early stop.</p>|||<p>Đã kiểm bằng cách biên dịch và chạy với CẢ HAI input mẫu của đề — output khớp chính xác cả 2. BST insert chuẩn (không trùng), rồi duyệt theo mức bằng hàng đợi: lấy 1 node ra, giữ lại giá trị nếu ≥ V, rồi đẩy con trái và con phải vào hàng đợi. Việc lọc <b>không</b> được cắt bỏ cây con — 1 node nhỏ hơn V vẫn có thể chứa hậu duệ lớn hơn V (ở mẫu 1, node 4 bị bỏ nhưng cây con của nó vẫn phải duyệt), nên phải lọc theo từng node chứ không dừng sớm.</p>",
          "rubric": [
            {
              "id": "bst_insert",
              "criterion": "Correctly builds the BST by inserting all values one by one, respecting BST ordering and rejecting duplicates.|||Dựng đúng BST bằng cách chèn lần lượt các giá trị, đúng thứ tự BST và loại giá trị trùng.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "bfs_order",
              "criterion": "Correctly implements a breadth-first (level-order) traversal using a queue: parent level before child level, left child before right child.|||Cài đúng duyệt theo chiều rộng (theo mức) bằng hàng đợi: hết mức cha rồi tới mức con, con trái trước con phải.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "filter_and_format",
              "criterion": "Applies the >= V filter per node without pruning subtrees, and formats the output as a comma-separated list with no spaces in the output file.|||Áp dụng bộ lọc >= V theo từng node mà không cắt cây con, và ghi output đúng dạng danh sách cách nhau dấu phẩy, không khoảng trắng, vào file kết quả.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 4,
          "language": "java",
          "prompt": "<p><strong>Question 2: (4 marks) – BFS traversing of a BST that allows duplicate values</strong></p>\n     <p>Write a program to build a binary search tree by inserting N (1 ≤ N ≤ 100) integer values into the BST one by one. Note that the nodes <b>can have duplicate values</b>.</p>\n     <p>Your task is to display the <b>BFS traversing</b> of the BST.</p>\n     <p><b>For example:</b> create a BST tree by adding into the tree N integer values as follows: 7, 9, 4, 9, 1, 12, 6, 7, 1, 10. The BFS traversing of the BST tree is: 7, 7, 4, 9, 9, 1, 1, 6, 12, 10.</p>\n     <p>In the figure of the original paper each node carries a <code>count</code>: node 7 has count=2, node 4 count=1, node 9 count=2, node 1 count=2, node 6 count=1, node 12 count=1, node 10 count=1. A repeated value therefore does <b>not</b> create a second node — it increases the count of the node already holding that value, and that node is then written out as many times as its count.</p>\n     <p><b>Input</b> (<code>duplicate_input.txt</code>): line 1 — a positive integer N (1 ≤ N ≤ 100), the number of integer values to insert into the BST; line 2 — N integers that will be inserted into the BST one by one, each number separated by at least one space.</p>\n     <p><b>Output</b> (<code>duplicate_output.txt</code>): the list of numbers representing the BFS traversing of the BST. Each number separated by one comma.</p>\n     <p><b>Sample 1:</b> N=10, values = 7 9 4 9 1 12 6 7 1 10 → output <code>7,7,4,9,9,1,1,6,12,10</code>.</p>\n     <p><b>Sample 2:</b> N=10, values = 78 21 36 18 30 91 74 55 98 100 → output <code>78,21,91,18,36,98,30,74,100,55</code>.</p>|||<p><strong>Câu 2: (4 điểm) – Duyệt BFS trên BST cho phép giá trị trùng</strong></p>\n     <p>Viết chương trình dựng 1 cây nhị phân tìm kiếm bằng cách chèn lần lượt N (1 ≤ N ≤ 100) số nguyên vào cây. Các node <b>có thể có giá trị trùng nhau</b>.</p>\n     <p>Nhiệm vụ: hiển thị kết quả <b>duyệt BFS</b> của cây.</p>\n     <p><b>Ví dụ:</b> dựng cây BST bằng cách thêm lần lượt N số nguyên: 7, 9, 4, 9, 1, 12, 6, 7, 1, 10. Kết quả duyệt BFS của cây là: 7, 7, 4, 9, 9, 1, 1, 6, 12, 10.</p>\n     <p>Trong hình của đề gốc mỗi node mang 1 giá trị <code>count</code>: node 7 có count=2, node 4 count=1, node 9 count=2, node 1 count=2, node 6 count=1, node 12 count=1, node 10 count=1. Vậy giá trị lặp lại <b>không</b> tạo node thứ hai — nó làm tăng count của node đang giữ giá trị đó, và khi duyệt thì node đó được ghi ra đúng bằng số lần count.</p>\n     <p><b>Input</b> (<code>duplicate_input.txt</code>): dòng 1 — số nguyên dương N (1 ≤ N ≤ 100), số lượng giá trị cần chèn vào BST; dòng 2 — N số nguyên sẽ được chèn lần lượt vào BST, mỗi số cách nhau ít nhất 1 khoảng trắng.</p>\n     <p><b>Output</b> (<code>duplicate_output.txt</code>): danh sách các số là kết quả duyệt BFS của BST. Mỗi số cách nhau 1 dấu phẩy.</p>\n     <p><b>Mẫu 1:</b> N=10, giá trị = 7 9 4 9 1 12 6 7 1 10 → output <code>7,7,4,9,9,1,1,6,12,10</code>.</p>\n     <p><b>Mẫu 2:</b> N=10, giá trị = 78 21 36 18 30 91 74 55 98 100 → output <code>78,21,91,18,36,98,30,74,100,55</code>.</p>",
          "starterCode": "import java.io.*;\nimport java.util.*;\n\npublic class DuplicateBFS {\n    static class Node {\n        int val;\n        int count;              // how many times this value has been inserted\n        Node left, right;\n        Node(int v) { val = v; count = 1; }\n    }\n\n    static Node root;\n\n    // Insert v into the BST. Duplicate values ARE allowed: instead of creating\n    // a second node, increase the `count` of the existing node holding v.\n    static void insert(int v) {\n        //You should write here appropriate statements to complete this function.\n\n    }\n\n    static List<Integer> result = new ArrayList<>();\n\n    // Breadth-first (level-order) traversal of the BST, appending each node's\n    // value to `result` exactly `count` times.\n    static void bfs() {\n        //You should write here appropriate statements to complete this function.\n\n    }\n\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"duplicate_input.txt\"));\n        int n = Integer.parseInt(sc.nextLine().trim());\n        // The whole second line is tokenized (n is read only for reference).\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        while (st.hasMoreTokens()) insert(Integer.parseInt(st.nextToken()));\n        sc.close();\n\n        bfs();\n\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < result.size(); i++) {\n            if (i > 0) sb.append(\",\");\n            sb.append(result.get(i));\n        }\n        PrintWriter pw = new PrintWriter(new FileWriter(\"duplicate_output.txt\"));\n        pw.println(sb.toString());\n        pw.close();\n    }\n}\n",
          "sampleSolution": "import java.io.*;\nimport java.util.*;\n\npublic class DuplicateBFS {\n    static class Node {\n        int val;\n        int count;              // how many times this value has been inserted\n        Node left, right;\n        Node(int v) { val = v; count = 1; }\n    }\n\n    static Node root;\n\n    // Insert v into the BST. Duplicate values ARE allowed: instead of creating\n    // a second node, increase the `count` of the existing node holding v.\n    static void insert(int v) {\n        if (root == null) { root = new Node(v); return; }\n        Node q = root;\n        while (true) {\n            if (v == q.val) { q.count++; return; }        // duplicate value\n            if (v < q.val) {\n                if (q.left == null) { q.left = new Node(v); return; }\n                q = q.left;\n            } else {\n                if (q.right == null) { q.right = new Node(v); return; }\n                q = q.right;\n            }\n        }\n    }\n\n    static List<Integer> result = new ArrayList<>();\n\n    // Breadth-first (level-order) traversal of the BST, appending each node's\n    // value to `result` exactly `count` times.\n    static void bfs() {\n        if (root == null) return;\n        Queue<Node> queue = new LinkedList<>();\n        queue.add(root);\n        while (!queue.isEmpty()) {\n            Node p = queue.remove();\n            for (int i = 0; i < p.count; i++) result.add(p.val);\n            if (p.left != null) queue.add(p.left);\n            if (p.right != null) queue.add(p.right);\n        }\n    }\n\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"duplicate_input.txt\"));\n        int n = Integer.parseInt(sc.nextLine().trim());\n        // The whole second line is tokenized (n is read only for reference).\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        while (st.hasMoreTokens()) insert(Integer.parseInt(st.nextToken()));\n        sc.close();\n\n        bfs();\n\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < result.size(); i++) {\n            if (i > 0) sb.append(\",\");\n            sb.append(result.get(i));\n        }\n        PrintWriter pw = new PrintWriter(new FileWriter(\"duplicate_output.txt\"));\n        pw.println(sb.toString());\n        pw.close();\n    }\n}\n",
          "expectedOutput": "Sample 1 (N=10, values=7 9 4 9 1 12 6 7 1 10): 7,7,4,9,9,1,1,6,12,10\nSample 2 (N=10, values=78 21 36 18 30 91 74 55 98 100): 78,21,91,18,36,98,30,74,100,55",
          "explanation": "<p>Verified by compiling and running with BOTH of this paper's own sample inputs — the output matches exactly for both. The whole question turns on how a duplicate is stored: a counter on the existing node, not a second node. Inserting a duplicate as a real node would still produce a valid BST but a different BFS order, so it would fail sample 1 (the two 7s must come out adjacent, at the root, before level 1 — which only happens if they live in the same node). Sample 2 has no duplicates at all, so every count stays 1 and the answer is simply the plain level-order traversal — it is there to check the traversal itself, sample 1 to check the counting.</p>|||<p>Đã kiểm bằng cách biên dịch và chạy với CẢ HAI input mẫu của đề — output khớp chính xác cả 2. Toàn bộ câu này xoay quanh cách lưu giá trị trùng: 1 bộ đếm trên node đã có, chứ không phải tạo node thứ hai. Nếu chèn giá trị trùng thành node thật thì cây vẫn hợp lệ nhưng thứ tự BFS khác đi, và sẽ sai mẫu 1 (hai số 7 phải ra liền nhau, ở gốc, trước cả mức 1 — chỉ xảy ra khi chúng nằm trong cùng 1 node). Mẫu 2 hoàn toàn không có giá trị trùng nên mọi count đều bằng 1 và kết quả chỉ là duyệt theo mức thuần tuý — mẫu 2 để kiểm phép duyệt, mẫu 1 để kiểm phần đếm.</p>",
          "rubric": [
            {
              "id": "bst_insert",
              "criterion": "Correctly builds the BST by inserting all values one by one, respecting BST ordering.|||Dựng đúng BST bằng cách chèn lần lượt các giá trị, đúng thứ tự BST.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "duplicate_count",
              "criterion": "Handles a repeated value by increasing the count of the existing node rather than creating a second node.|||Xử lý giá trị lặp lại bằng cách tăng count của node đã có thay vì tạo node thứ hai.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "bfs_order",
              "criterion": "Correctly implements a breadth-first (level-order) traversal using a queue, and writes each node value exactly count times.|||Cài đúng duyệt theo chiều rộng (theo mức) bằng hàng đợi, và ghi ra giá trị mỗi node đúng count lần.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "io_format",
              "criterion": "Reads the input file and writes the result to the output file as a comma-separated list with no spaces.|||Đọc đúng file input và ghi kết quả ra file output đúng dạng danh sách cách nhau dấu phẩy, không khoảng trắng.",
              "weight": 1,
              "maxScore": 0.5
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 3,
          "language": "java",
          "prompt": "<p><strong>Question 3: (3 marks) – Full-child nodes by pre-order traversing</strong></p>\n     <p>Write a program to build a binary search tree by inserting N (1 ≤ N ≤ 100) integer values into the BST one by one. Note that the nodes have <b>no duplicate</b> values.</p>\n     <p>Your task is showing the list of <b>full-child nodes</b> of the BST by using <b>pre-order traversing</b>. Note that a full-child node is a node that has left-child and right-child.</p>\n     <p><b>For example:</b> create a BST tree by inserting into the tree N integer values as follows: 7, 9, 4, 1, 12, 6, 10. The BST has 2 full-child nodes including 7 and 4.</p>\n     <p><b>Input</b> (<code>fullChildNode_input.txt</code>): line 1 — a positive integer N (1 ≤ N ≤ 100), the number of integer values to insert into the BST; line 2 — N integers that will be inserted into the BST one by one, each number separated by at least one space.</p>\n     <p><b>Output</b> (<code>fullChildNode_output.txt</code>): only one line contains the list of full-child nodes of the BST by using pre-order traversing, each number separated by one comma. If the BST doesn't have any full-child node, the result file will contain the message <code>There is a linked list</code>.</p>\n     <p><b>Sample 1:</b> N=7, values = 7 9 4 1 12 6 10 → output <code>7,4</code>.</p>\n     <p><b>Sample 2:</b> N=9, values = 10 11 12 15 18 12 11 → output <code>There is a linked list</code>.</p>\n     <p><b>Note on Sample 2:</b> the original paper prints N=9 but then lists only 7 values — a typo in the paper itself. The list of values is the meaningful part, so the starter code below tokenizes the whole second line instead of looping exactly N times; the paper's own answer is unaffected. The repeated 12 and 11 (highlighted in the paper) are duplicates and must be ignored, leaving the chain 10 → 11 → 12 → 15 → 18, in which no node has two children.</p>|||<p><strong>Câu 3: (3 điểm) – Node đủ hai con, liệt kê theo duyệt trước</strong></p>\n     <p>Viết chương trình dựng 1 cây nhị phân tìm kiếm bằng cách chèn lần lượt N (1 ≤ N ≤ 100) số nguyên vào cây. Các node <b>không có giá trị trùng nhau</b>.</p>\n     <p>Nhiệm vụ: hiển thị danh sách các <b>node đủ hai con (full-child node)</b> của BST bằng cách <b>duyệt trước (pre-order)</b>. Node đủ hai con là node có cả con trái lẫn con phải.</p>\n     <p><b>Ví dụ:</b> dựng cây BST bằng cách chèn lần lượt N số nguyên: 7, 9, 4, 1, 12, 6, 10. Cây có 2 node đủ hai con là 7 và 4.</p>\n     <p><b>Input</b> (<code>fullChildNode_input.txt</code>): dòng 1 — số nguyên dương N (1 ≤ N ≤ 100), số lượng giá trị cần chèn vào BST; dòng 2 — N số nguyên sẽ được chèn lần lượt vào BST, mỗi số cách nhau ít nhất 1 khoảng trắng.</p>\n     <p><b>Output</b> (<code>fullChildNode_output.txt</code>): chỉ 1 dòng chứa danh sách các node đủ hai con của BST theo thứ tự duyệt trước, mỗi số cách nhau 1 dấu phẩy. Nếu BST không có node đủ hai con nào thì file kết quả chứa dòng chữ <code>There is a linked list</code>.</p>\n     <p><b>Mẫu 1:</b> N=7, giá trị = 7 9 4 1 12 6 10 → output <code>7,4</code>.</p>\n     <p><b>Mẫu 2:</b> N=9, giá trị = 10 11 12 15 18 12 11 → output <code>There is a linked list</code>.</p>\n     <p><b>Lưu ý về Mẫu 2:</b> đề gốc in N=9 nhưng lại chỉ liệt kê 7 giá trị — đây là lỗi in trong chính đề. Danh sách giá trị mới là phần có nghĩa, nên khung code dưới đây tách token cả dòng thứ hai thay vì lặp đúng N lần; đáp án của đề không đổi. Hai số 12 và 11 lặp lại (được tô sáng trong đề) là giá trị trùng nên phải bỏ qua, cây còn lại là chuỗi 10 → 11 → 12 → 15 → 18, không node nào có đủ 2 con.</p>",
          "starterCode": "import java.io.*;\nimport java.util.*;\n\npublic class FullChildNode {\n    static class Node {\n        int val;\n        Node left, right;\n        Node(int v) { val = v; }\n    }\n\n    static Node root;\n\n    // Insert v into the BST (the nodes have no duplicate values, so a value\n    // already present in the tree must be ignored).\n    static void insert(int v) {\n        //You should write here appropriate statements to complete this function.\n\n    }\n\n    static List<Integer> result = new ArrayList<>();\n\n    // Pre-order traversal (current node, then left subtree, then right\n    // subtree) collecting into `result` every FULL-CHILD node, that is every\n    // node having BOTH a left child and a right child.\n    static void preOrder(Node p) {\n        //You should write here appropriate statements to complete this function.\n\n    }\n\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"fullChildNode_input.txt\"));\n        int n = Integer.parseInt(sc.nextLine().trim());\n        // The whole second line is tokenized (n is read only for reference).\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        while (st.hasMoreTokens()) insert(Integer.parseInt(st.nextToken()));\n        sc.close();\n\n        preOrder(root);\n\n        StringBuilder sb = new StringBuilder();\n        if (result.isEmpty()) {\n            sb.append(\"There is a linked list\");\n        } else {\n            for (int i = 0; i < result.size(); i++) {\n                if (i > 0) sb.append(\",\");\n                sb.append(result.get(i));\n            }\n        }\n        PrintWriter pw = new PrintWriter(new FileWriter(\"fullChildNode_output.txt\"));\n        pw.println(sb.toString());\n        pw.close();\n    }\n}\n",
          "sampleSolution": "import java.io.*;\nimport java.util.*;\n\npublic class FullChildNode {\n    static class Node {\n        int val;\n        Node left, right;\n        Node(int v) { val = v; }\n    }\n\n    static Node root;\n\n    // Insert v into the BST (the nodes have no duplicate values, so a value\n    // already present in the tree must be ignored).\n    static void insert(int v) {\n        Node p = new Node(v);\n        if (root == null) { root = p; return; }\n        Node q = root;\n        while (true) {\n            if (v == q.val) return;                      // ignore duplicates\n            if (v < q.val) {\n                if (q.left == null) { q.left = p; return; }\n                q = q.left;\n            } else {\n                if (q.right == null) { q.right = p; return; }\n                q = q.right;\n            }\n        }\n    }\n\n    static List<Integer> result = new ArrayList<>();\n\n    // Pre-order traversal (current node, then left subtree, then right\n    // subtree) collecting into `result` every FULL-CHILD node, that is every\n    // node having BOTH a left child and a right child.\n    static void preOrder(Node p) {\n        if (p == null) return;\n        if (p.left != null && p.right != null) result.add(p.val);\n        preOrder(p.left);\n        preOrder(p.right);\n    }\n\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"fullChildNode_input.txt\"));\n        int n = Integer.parseInt(sc.nextLine().trim());\n        // The whole second line is tokenized (n is read only for reference).\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        while (st.hasMoreTokens()) insert(Integer.parseInt(st.nextToken()));\n        sc.close();\n\n        preOrder(root);\n\n        StringBuilder sb = new StringBuilder();\n        if (result.isEmpty()) {\n            sb.append(\"There is a linked list\");\n        } else {\n            for (int i = 0; i < result.size(); i++) {\n                if (i > 0) sb.append(\",\");\n                sb.append(result.get(i));\n            }\n        }\n        PrintWriter pw = new PrintWriter(new FileWriter(\"fullChildNode_output.txt\"));\n        pw.println(sb.toString());\n        pw.close();\n    }\n}\n",
          "expectedOutput": "Sample 1 (N=7, values=7 9 4 1 12 6 10): 7,4\nSample 2 (N=9 per the paper's typo, values=10 11 12 15 18 12 11): There is a linked list",
          "explanation": "<p>Verified by compiling and running with BOTH of this paper's own sample inputs — the output matches exactly for both. In sample 1 the tree is 7(4(1,6), 9(_,12(10,_))): node 7 has both children, node 4 has both children, node 9 has only a right child and node 12 only a left child, so pre-order (node, left, right) yields 7 then 4. Two details decide this question: duplicate values must be silently ignored on insert (otherwise sample 2's repeated 12 and 11 would create extra nodes and the chain would no longer be a pure linked list), and the empty case must print the fixed sentence rather than an empty line.</p>|||<p>Đã kiểm bằng cách biên dịch và chạy với CẢ HAI input mẫu của đề — output khớp chính xác cả 2. Ở mẫu 1 cây là 7(4(1,6), 9(_,12(10,_))): node 7 có đủ 2 con, node 4 có đủ 2 con, node 9 chỉ có con phải và node 12 chỉ có con trái, nên duyệt trước (gốc, trái, phải) cho ra 7 rồi 4. Hai chi tiết quyết định câu này: giá trị trùng phải bị bỏ qua âm thầm khi chèn (nếu không, hai số 12 và 11 lặp lại ở mẫu 2 sẽ sinh thêm node và chuỗi không còn là danh sách liên kết thuần), và trường hợp rỗng phải in ra đúng câu cố định chứ không phải 1 dòng trống.</p>",
          "rubric": [
            {
              "id": "bst_insert_nodup",
              "criterion": "Correctly builds the BST by inserting all values one by one, respecting BST ordering and silently ignoring values already present.|||Dựng đúng BST bằng cách chèn lần lượt các giá trị, đúng thứ tự BST và bỏ qua âm thầm giá trị đã có trong cây.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "preorder_fullchild",
              "criterion": "Correctly performs a pre-order traversal (node, left subtree, right subtree) and selects exactly the nodes having BOTH a left child and a right child.|||Duyệt đúng thứ tự trước (gốc, cây con trái, cây con phải) và chọn đúng các node có CẢ con trái lẫn con phải.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "empty_case_and_format",
              "criterion": "Writes the exact message \"There is a linked list\" when no full-child node exists, otherwise a comma-separated list with no spaces.|||Ghi đúng dòng chữ \"There is a linked list\" khi không có node đủ hai con, ngược lại ghi danh sách cách nhau dấu phẩy, không khoảng trắng.",
              "weight": 1,
              "maxScore": 0.5
            }
          ]
        }
      ]
    }
  ]
};
