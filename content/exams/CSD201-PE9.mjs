export default {
  "course": {
    "courseCode": "CSD201"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "CODE",
      "code": "PE9",
      "title": "PE Đề 9 — Practical Exam (Fall 2025, \"CT\")|||PE Đề 9 — Thi thực hành (Fall 2025, \"CT\")",
      "description": "CSD201 PE (CODE): 3 independent file-I/O algorithm programs — BST reverse traversal, Dijkstra shortest path, happy-vertex detection, written from scratch (no given project), AI-graded.|||PE CSD201 (viết mã): 3 chương trình thuật toán độc lập dùng file I/O — duyệt ngược BST, đường đi ngắn nhất Dijkstra, tìm đỉnh hạnh phúc, viết từ đầu (không có project given), chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "attachmentUrl": null,
      "attachmentName": null,
      "instructions": "<div class=\"ml-en\"><p><strong>CSD201 PE INSTRUCTIONS</strong> (Fall 2025, \"CT\"). Do not add new import statement(s) to given files. Software tools: NetBeans IDE 13, Java JDK 1.8.</p>\n   <p>Each question below is an independent Java program: read input from the fixed-name input file, write output to the fixed-name output file (standard NetBeans working-directory file I/O, exactly like the other CSD201 PE decks in this exam room).</p>\n   <p><b>Note:</b> the sample input/output shown per question is only to help you test your own code — the real grading testcases use different data, so do not hardcode the specific results shown below.</p></div><div class=\"ml-vi\"><p><strong>HƯỚNG DẪN PE CSD201</strong> (Fall 2025, \"CT\"). Không thêm import mới vào file cho sẵn. Công cụ: NetBeans IDE 13, Java JDK 1.8.</p>\n   <p>Mỗi câu dưới đây là 1 chương trình Java độc lập: đọc input từ file tên cố định, ghi output ra file tên cố định khác (I/O file working-directory chuẩn của NetBeans, giống hệt các đề CSD201 PE khác trong phòng thi này).</p>\n   <p><b>Lưu ý:</b> input/output mẫu ở mỗi câu chỉ để bạn tự test — bộ test chấm điểm thật dùng dữ liệu khác, đừng hardcode kết quả mẫu dưới đây.</p></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "CODE",
          "points": 3,
          "language": "java",
          "prompt": "<p><strong>Question 1: (3 marks) – Reverse Traversing a BST</strong></p>\n     <p>Write a program to build a <b>BST (Binary Search Tree)</b> by inserting N (1 ≤ N ≤ 100) integer values into the BST one by one. Note that the nodes have <b>no duplicate</b> values.</p>\n     <p>Your task is to <b>reverse traverse</b> the BST and list the nodes whose values are <b>greater than or equal to V</b>. The reverse traversing algorithm: Step 1 — right-child traversing; Step 2 — process current node; Step 3 — left-child traversing.</p>\n     <p><b>Input</b> (<code>reverseTraversing_input.txt</code>): line 1 — a positive integer N; line 2 — N integers to insert one by one, space-separated; line 3 — a positive integer V (guaranteed to have results when traversing the tree).</p>\n     <p><b>Output</b> (<code>reverseTraversing_output.txt</code>): one line listing the reverse traversal of the BST restricted to node values ≥ V, comma-separated, no spaces.</p>\n     <p><b>Sample 1:</b> N=7, values = 7 9 4 1 12 6 10, V=7 → output <code>12,10,9,7</code>.</p>\n     <p><b>Sample 2:</b> N=10, values = 78 21 36 18 30 91 74 55 98 100, V=90 → output <code>100,98,91</code>.</p>|||<p><strong>Câu 1: (3 điểm) – Duyệt ngược BST</strong></p>\n     <p>Viết chương trình dựng 1 <b>BST (cây nhị phân tìm kiếm)</b> bằng cách chèn lần lượt N (1 ≤ N ≤ 100) số nguyên vào cây. Các node <b>không có giá trị trùng nhau</b>.</p>\n     <p>Nhiệm vụ: <b>duyệt ngược</b> BST và liệt kê các node có giá trị <b>lớn hơn hoặc bằng V</b>. Thuật toán duyệt ngược: Bước 1 — duyệt cây con phải; Bước 2 — xử lý node hiện tại; Bước 3 — duyệt cây con trái.</p>\n     <p><b>Input</b> (<code>reverseTraversing_input.txt</code>): dòng 1 — số nguyên dương N; dòng 2 — N số nguyên chèn lần lượt, cách nhau khoảng trắng; dòng 3 — số nguyên dương V (đảm bảo có kết quả khi duyệt).</p>\n     <p><b>Output</b> (<code>reverseTraversing_output.txt</code>): 1 dòng liệt kê kết quả duyệt ngược BST giới hạn ở node có giá trị ≥ V, cách nhau dấu phẩy, không khoảng trắng.</p>\n     <p><b>Mẫu 1:</b> N=7, giá trị = 7 9 4 1 12 6 10, V=7 → output <code>12,10,9,7</code>.</p>\n     <p><b>Mẫu 2:</b> N=10, giá trị = 78 21 36 18 30 91 74 55 98 100, V=90 → output <code>100,98,91</code>.</p>",
          "starterCode": "import java.io.*;\nimport java.util.*;\n\npublic class ReverseTraversing {\n    static class Node {\n        int val;\n        Node left, right;\n        Node(int v) { val = v; }\n    }\n\n    static Node root;\n\n    // Insert v into the BST (no duplicate values will ever be inserted).\n    static void insert(int v) {\n        //You should write here appropriate statements to complete this function.\n\n    }\n\n    static List<Integer> result = new ArrayList<>();\n\n    // Reverse-traverse the BST (Step 1: right-child, Step 2: process current\n    // node, Step 3: left-child), collecting into `result` every node value\n    // that is greater than or equal to v.\n    static void reverseTraverse(Node p, int v) {\n        //You should write here appropriate statements to complete this function.\n\n    }\n\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"reverseTraversing_input.txt\"));\n        int n = Integer.parseInt(sc.nextLine().trim());\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        for (int i = 0; i < n; i++) insert(Integer.parseInt(st.nextToken()));\n        int v = Integer.parseInt(sc.nextLine().trim());\n        sc.close();\n\n        reverseTraverse(root, v);\n\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < result.size(); i++) {\n            if (i > 0) sb.append(\",\");\n            sb.append(result.get(i));\n        }\n        PrintWriter pw = new PrintWriter(new FileWriter(\"reverseTraversing_output.txt\"));\n        pw.println(sb.toString());\n        pw.close();\n    }\n}\n",
          "sampleSolution": "import java.io.*;\nimport java.util.*;\n\npublic class ReverseTraversing {\n    static class Node {\n        int val;\n        Node left, right;\n        Node(int v) { val = v; }\n    }\n\n    static Node root;\n\n    static void insert(int v) {\n        Node p = new Node(v);\n        if (root == null) { root = p; return; }\n        Node cur = root, parent = null;\n        while (cur != null) {\n            parent = cur;\n            if (v < cur.val) cur = cur.left;\n            else cur = cur.right;\n        }\n        if (v < parent.val) parent.left = p;\n        else parent.right = p;\n    }\n\n    static List<Integer> result = new ArrayList<>();\n\n    static void reverseTraverse(Node p, int v) {\n        if (p == null) return;\n        reverseTraverse(p.right, v);\n        if (p.val >= v) result.add(p.val);\n        reverseTraverse(p.left, v);\n    }\n\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"reverseTraversing_input.txt\"));\n        int n = Integer.parseInt(sc.nextLine().trim());\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        for (int i = 0; i < n; i++) insert(Integer.parseInt(st.nextToken()));\n        int v = Integer.parseInt(sc.nextLine().trim());\n        sc.close();\n\n        reverseTraverse(root, v);\n\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < result.size(); i++) {\n            if (i > 0) sb.append(\",\");\n            sb.append(result.get(i));\n        }\n        PrintWriter pw = new PrintWriter(new FileWriter(\"reverseTraversing_output.txt\"));\n        pw.println(sb.toString());\n        pw.close();\n    }\n}\n",
          "expectedOutput": "Sample 1 (N=7, values=7 9 4 1 12 6 10, V=7): 12,10,9,7\nSample 2 (N=10, values=78 21 36 18 30 91 74 55 98 100, V=90): 100,98,91",
          "explanation": "<p>Verified by compiling and running with BOTH of this paper's own sample inputs — output matches exactly for both. Standard BST insert (no duplicates, per the guarantee), then a recursive reverse traversal (right subtree, then current node if value ≥ V, then left subtree) naturally visits nodes from largest to smallest, so no separate sort is needed.</p>|||<p>Đã kiểm bằng cách biên dịch và chạy với CẢ HAI input mẫu của đề — output khớp chính xác cả 2. BST insert chuẩn (không trùng, theo đảm bảo của đề), rồi duyệt ngược đệ quy (cây con phải, rồi node hiện tại nếu giá trị ≥ V, rồi cây con trái) tự nhiên thăm node từ lớn tới nhỏ, không cần sort riêng.</p>",
          "rubric": [
            {
              "id": "bst_insert",
              "criterion": "Correctly builds the BST by inserting all N values one by one, respecting BST ordering.|||Dựng đúng BST bằng cách chèn lần lượt N giá trị, đúng thứ tự BST.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "reverse_traverse_order",
              "criterion": "Correctly implements the reverse traversal order: right subtree, then current node, then left subtree.|||Cài đúng thứ tự duyệt ngược: cây con phải, rồi node hiện tại, rồi cây con trái.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "filter_and_format",
              "criterion": "Only includes node values >= V, and formats the output as a comma-separated list with no spaces.|||Chỉ lấy node có giá trị >= V, và định dạng output đúng dạng danh sách cách nhau dấu phẩy không khoảng trắng.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 4,
          "language": "java",
          "prompt": "<p><strong>Question 2: (4 marks) – Shortest Path (Dijkstra)</strong></p>\n     <p>Write a program to build an <b>undirected graph</b> from a given edge list. Your task is to find the shortest path from <code>startVertex</code> to <code>endVertex</code>.</p>\n     <p><b>Input</b> (<code>dijkstra_input.txt</code>): line 1 — 4 positive integers N, M, s, e (N: 1 ≤ N ≤ 20 vertices; M: N-1 ≤ M ≤ 190 edges; s: start vertex; e: end vertex, s ≠ e); next M lines — 3 positive integers u, v, d (edge between u and v with weight d, 0 ≤ d ≤ 10⁹).</p>\n     <p><b>Output</b> (<code>dijkstra_output.txt</code>): line 1 — the shortest distance; line 2 — the list of vertices of the shortest path from s to e, comma-separated.</p>\n     <p><b>Sample 1:</b> N=8,M=16,s=0,e=7, edges (0,1,5)(0,2,3)(0,5,11)(1,2,5)(1,6,7)(1,7,11)(2,3,11)(2,4,1)(2,5,7)(3,4,11)(3,6,11)(4,5,1)(4,6,5)(4,7,7)(5,7,42)(6,7,42) → distance <code>11</code>, path <code>0,2,4,7</code>.</p>\n     <p><b>Sample 2:</b> N=6,M=7,s=1,e=4, edges (0,1,7)(0,2,5)(0,5,1)(2,3,2)(2,4,3)(3,4,7)(3,5,9) → distance <code>15</code>, path <code>1,0,2,4</code>.</p>|||<p><strong>Câu 2: (4 điểm) – Đường đi ngắn nhất (Dijkstra)</strong></p>\n     <p>Viết chương trình dựng 1 <b>đồ thị vô hướng</b> từ danh sách cạnh cho trước. Nhiệm vụ: tìm đường đi ngắn nhất từ <code>startVertex</code> tới <code>endVertex</code>.</p>\n     <p><b>Input</b> (<code>dijkstra_input.txt</code>): dòng 1 — 4 số nguyên dương N, M, s, e (N: 1 ≤ N ≤ 20 đỉnh; M: N-1 ≤ M ≤ 190 cạnh; s: đỉnh bắt đầu; e: đỉnh kết thúc, s ≠ e); M dòng tiếp — 3 số nguyên dương u, v, d (cạnh nối u và v trọng số d, 0 ≤ d ≤ 10⁹).</p>\n     <p><b>Output</b> (<code>dijkstra_output.txt</code>): dòng 1 — khoảng cách ngắn nhất; dòng 2 — danh sách đỉnh của đường đi ngắn nhất từ s tới e, cách nhau dấu phẩy.</p>\n     <p><b>Mẫu 1:</b> N=8,M=16,s=0,e=7, cạnh (0,1,5)(0,2,3)(0,5,11)(1,2,5)(1,6,7)(1,7,11)(2,3,11)(2,4,1)(2,5,7)(3,4,11)(3,6,11)(4,5,1)(4,6,5)(4,7,7)(5,7,42)(6,7,42) → khoảng cách <code>11</code>, đường đi <code>0,2,4,7</code>.</p>\n     <p><b>Mẫu 2:</b> N=6,M=7,s=1,e=4, cạnh (0,1,7)(0,2,5)(0,5,1)(2,3,2)(2,4,3)(3,4,7)(3,5,9) → khoảng cách <code>15</code>, đường đi <code>1,0,2,4</code>.</p>",
          "starterCode": "import java.io.*;\nimport java.util.*;\n\npublic class Dijkstra {\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"dijkstra_input.txt\"));\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        int n = Integer.parseInt(st.nextToken());\n        int m = Integer.parseInt(st.nextToken());\n        int s = Integer.parseInt(st.nextToken());\n        int e = Integer.parseInt(st.nextToken());\n\n        final int INF = Integer.MAX_VALUE / 2;\n        int[][] w = new int[n][n];\n        for (int[] row : w) Arrays.fill(row, INF);\n\n        for (int i = 0; i < m; i++) {\n            StringTokenizer t = new StringTokenizer(sc.nextLine());\n            int u = Integer.parseInt(t.nextToken());\n            int v = Integer.parseInt(t.nextToken());\n            int d = Integer.parseInt(t.nextToken());\n            if (d < w[u][v]) { w[u][v] = d; w[v][u] = d; } // undirected\n        }\n        sc.close();\n\n        // Apply Dijkstra's algorithm on the weighted undirected graph `w`\n        // (n vertices, w[u][v] = INF means no edge) to find the shortest\n        // distance from s to e, and the actual vertex sequence of that path.\n        //\n        // You should write here appropriate statements to compute:\n        //   int[] dist  — dist[e] must hold the shortest distance from s to e\n        //   List<Integer> path — the vertex sequence of the shortest path,\n        //                        starting at s and ending at e (inclusive)\n\n\n        int[] dist = new int[n]; // TODO: compute\n        List<Integer> path = new ArrayList<>(); // TODO: compute\n\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < path.size(); i++) {\n            if (i > 0) sb.append(\",\");\n            sb.append(path.get(i));\n        }\n\n        PrintWriter pw = new PrintWriter(new FileWriter(\"dijkstra_output.txt\"));\n        pw.println(dist[e]);\n        pw.println(sb.toString());\n        pw.close();\n    }\n}\n",
          "sampleSolution": "import java.io.*;\nimport java.util.*;\n\npublic class Dijkstra {\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"dijkstra_input.txt\"));\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        int n = Integer.parseInt(st.nextToken());\n        int m = Integer.parseInt(st.nextToken());\n        int s = Integer.parseInt(st.nextToken());\n        int e = Integer.parseInt(st.nextToken());\n\n        final int INF = Integer.MAX_VALUE / 2;\n        int[][] w = new int[n][n];\n        for (int[] row : w) Arrays.fill(row, INF);\n\n        for (int i = 0; i < m; i++) {\n            StringTokenizer t = new StringTokenizer(sc.nextLine());\n            int u = Integer.parseInt(t.nextToken());\n            int v = Integer.parseInt(t.nextToken());\n            int d = Integer.parseInt(t.nextToken());\n            if (d < w[u][v]) { w[u][v] = d; w[v][u] = d; } // undirected\n        }\n        sc.close();\n\n        int[] dist = new int[n];\n        int[] pred = new int[n];\n        boolean[] done = new boolean[n];\n        Arrays.fill(dist, INF);\n        Arrays.fill(pred, -1);\n        dist[s] = 0;\n\n        for (int iter = 0; iter < n; iter++) {\n            int u = -1;\n            for (int i = 0; i < n; i++) {\n                if (!done[i] && (u == -1 || dist[i] < dist[u])) u = i;\n            }\n            if (u == -1 || dist[u] == INF) break;\n            done[u] = true;\n            for (int v = 0; v < n; v++) {\n                if (w[u][v] < INF && dist[u] + w[u][v] < dist[v]) {\n                    dist[v] = dist[u] + w[u][v];\n                    pred[v] = u;\n                }\n            }\n        }\n\n        List<Integer> path = new ArrayList<>();\n        for (int cur = e; cur != -1; cur = pred[cur]) path.add(cur);\n        Collections.reverse(path);\n\n        StringBuilder sb = new StringBuilder();\n        for (int i = 0; i < path.size(); i++) {\n            if (i > 0) sb.append(\",\");\n            sb.append(path.get(i));\n        }\n\n        PrintWriter pw = new PrintWriter(new FileWriter(\"dijkstra_output.txt\"));\n        pw.println(dist[e]);\n        pw.println(sb.toString());\n        pw.close();\n    }\n}\n",
          "expectedOutput": "Sample 1 (N=8,M=16,s=0,e=7): distance 11, path 0,2,4,7\nSample 2 (N=6,M=7,s=1,e=4): distance 15, path 1,0,2,4",
          "explanation": "<p>Verified by compiling and running with BOTH of this paper's own sample inputs — distance and path match exactly for both. Standard array-based Dijkstra on the undirected weighted adjacency matrix (each edge set both w[u][v] and w[v][u]), tracking a predecessor array to reconstruct the actual path by backtracking from e to s.</p>|||<p>Đã kiểm bằng cách biên dịch và chạy với CẢ HAI input mẫu của đề — khoảng cách và đường đi khớp chính xác cả 2. Dijkstra mảng chuẩn trên ma trận kề vô hướng có trọng số (mỗi cạnh gán cả w[u][v] và w[v][u]), theo dõi mảng predecessor để dựng lại đường đi thật bằng cách lần ngược từ e về s.</p>",
          "rubric": [
            {
              "id": "undirected_build",
              "criterion": "Correctly builds the weighted adjacency structure as UNDIRECTED (each edge sets the weight in both directions).|||Dựng đúng ma trận kề có trọng số VÔ HƯỚNG (mỗi cạnh gán trọng số cả 2 chiều).",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "dijkstra_correct",
              "criterion": "Correctly runs Dijkstra's algorithm and computes the correct shortest distance from s to e.|||Chạy đúng thuật toán Dijkstra và tính đúng khoảng cách ngắn nhất từ s tới e.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "path_reconstruction",
              "criterion": "Correctly reconstructs and outputs the actual vertex sequence of the shortest path, from s to e in order.|||Dựng lại và xuất đúng dãy đỉnh thật của đường đi ngắn nhất, theo đúng thứ tự từ s tới e.",
              "weight": 1,
              "maxScore": 1.5
            }
          ]
        },
        {
          "kind": "CODE",
          "points": 3,
          "language": "java",
          "prompt": "<p><strong>Question 3: (3 marks) – Happy Vertices</strong></p>\n     <p>Write a program to build an <b>undirected graph</b> from a given edge list. A <b>happy vertex</b> is a vertex with positive degree — an endpoint of at least one edge. Your task is to show all happy vertices of the given graph in ascending order.</p>\n     <p><b>Input</b> (<code>happyVertices_input.txt</code>): line 1 — 2 positive integers N, M (N: 1 ≤ N ≤ 20 vertices; M: N-1 ≤ M ≤ 190 edges); next M lines — 2 positive integers u, v (edge between u and v).</p>\n     <p><b>Output</b> (<code>happyVertices_output.txt</code>): one line listing the happy vertices in ascending order, comma-separated. If the graph has no happy vertex, output the message <code>All of the vertices are isolated vertices.</code></p>\n     <p><b>Sample 1:</b> N=9,M=9, edges (0,1)(0,2)(0,5)(1,7)(2,3)(2,4)(3,4)(4,5)(4,7) → output <code>0,1,2,3,4,5,7</code> (vertices 6 and 8 are isolated).</p>\n     <p><b>Sample 2:</b> N=6,M=7, edges (0,1)(0,2)(0,5)(2,3)(2,4)(3,4)(4,5) → output <code>0,1,2,3,4,5</code>.</p>|||<p><strong>Câu 3: (3 điểm) – Đỉnh hạnh phúc</strong></p>\n     <p>Viết chương trình dựng 1 <b>đồ thị vô hướng</b> từ danh sách cạnh cho trước. 1 <b>đỉnh hạnh phúc</b> là đỉnh có bậc dương — là đầu mút của ít nhất 1 cạnh. Nhiệm vụ: liệt kê mọi đỉnh hạnh phúc của đồ thị theo thứ tự tăng dần.</p>\n     <p><b>Input</b> (<code>happyVertices_input.txt</code>): dòng 1 — 2 số nguyên dương N, M (N: 1 ≤ N ≤ 20 đỉnh; M: N-1 ≤ M ≤ 190 cạnh); M dòng tiếp — 2 số nguyên dương u, v (cạnh nối u và v).</p>\n     <p><b>Output</b> (<code>happyVertices_output.txt</code>): 1 dòng liệt kê đỉnh hạnh phúc tăng dần, cách nhau dấu phẩy. Nếu đồ thị không có đỉnh hạnh phúc nào, xuất thông điệp <code>All of the vertices are isolated vertices.</code></p>\n     <p><b>Mẫu 1:</b> N=9,M=9, cạnh (0,1)(0,2)(0,5)(1,7)(2,3)(2,4)(3,4)(4,5)(4,7) → output <code>0,1,2,3,4,5,7</code> (đỉnh 6 và 8 bị cô lập).</p>\n     <p><b>Mẫu 2:</b> N=6,M=7, cạnh (0,1)(0,2)(0,5)(2,3)(2,4)(3,4)(4,5) → output <code>0,1,2,3,4,5</code>.</p>",
          "starterCode": "import java.io.*;\nimport java.util.*;\n\npublic class HappyVertices {\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"happyVertices_input.txt\"));\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        int n = Integer.parseInt(st.nextToken());\n        int m = Integer.parseInt(st.nextToken());\n\n        int[] degree = new int[n];\n        for (int i = 0; i < m; i++) {\n            StringTokenizer t = new StringTokenizer(sc.nextLine());\n            int u = Integer.parseInt(t.nextToken());\n            int v = Integer.parseInt(t.nextToken());\n            degree[u]++;\n            degree[v]++;\n        }\n        sc.close();\n\n        // A happy vertex has positive degree (an endpoint of at least one\n        // edge). You should write here appropriate statements to collect,\n        // in ascending order, every vertex index i (0 <= i < n) whose\n        // degree[i] > 0, into the `happy` list below.\n        List<Integer> happy = new ArrayList<>();\n\n\n        PrintWriter pw = new PrintWriter(new FileWriter(\"happyVertices_output.txt\"));\n        if (happy.isEmpty()) {\n            pw.println(\"All of the vertices are isolated vertices.\");\n        } else {\n            StringBuilder sb = new StringBuilder();\n            for (int i = 0; i < happy.size(); i++) {\n                if (i > 0) sb.append(\",\");\n                sb.append(happy.get(i));\n            }\n            pw.println(sb.toString());\n        }\n        pw.close();\n    }\n}\n",
          "sampleSolution": "import java.io.*;\nimport java.util.*;\n\npublic class HappyVertices {\n    public static void main(String[] args) throws Exception {\n        Scanner sc = new Scanner(new File(\"happyVertices_input.txt\"));\n        StringTokenizer st = new StringTokenizer(sc.nextLine());\n        int n = Integer.parseInt(st.nextToken());\n        int m = Integer.parseInt(st.nextToken());\n\n        int[] degree = new int[n];\n        for (int i = 0; i < m; i++) {\n            StringTokenizer t = new StringTokenizer(sc.nextLine());\n            int u = Integer.parseInt(t.nextToken());\n            int v = Integer.parseInt(t.nextToken());\n            degree[u]++;\n            degree[v]++;\n        }\n        sc.close();\n\n        List<Integer> happy = new ArrayList<>();\n        for (int i = 0; i < n; i++) if (degree[i] > 0) happy.add(i);\n\n        PrintWriter pw = new PrintWriter(new FileWriter(\"happyVertices_output.txt\"));\n        if (happy.isEmpty()) {\n            pw.println(\"All of the vertices are isolated vertices.\");\n        } else {\n            StringBuilder sb = new StringBuilder();\n            for (int i = 0; i < happy.size(); i++) {\n                if (i > 0) sb.append(\",\");\n                sb.append(happy.get(i));\n            }\n            pw.println(sb.toString());\n        }\n        pw.close();\n    }\n}\n",
          "expectedOutput": "Sample 1 (N=9,M=9): 0,1,2,3,4,5,7\nSample 2 (N=6,M=7): 0,1,2,3,4,5",
          "explanation": "<p>Verified by compiling and running with BOTH of this paper's own sample inputs — output matches exactly for both. Each edge (u,v) increments the degree of both u and v; a vertex is happy iff its final degree is positive, collected in ascending order (0..N-1) since the loop scans indices in that order already.</p>|||<p>Đã kiểm bằng cách biên dịch và chạy với CẢ HAI input mẫu của đề — output khớp chính xác cả 2. Mỗi cạnh (u,v) tăng bậc của cả u và v; 1 đỉnh hạnh phúc khi bậc cuối cùng dương, thu thập theo thứ tự tăng dần (0..N-1) vì vòng lặp đã quét chỉ số theo đúng thứ tự đó.</p>",
          "rubric": [
            {
              "id": "degree_count",
              "criterion": "Correctly computes the degree of every vertex from the undirected edge list.|||Tính đúng bậc của mọi đỉnh từ danh sách cạnh vô hướng.",
              "weight": 1,
              "maxScore": 1.2
            },
            {
              "id": "ascending_filter",
              "criterion": "Correctly collects only vertices with positive degree, in ascending order.|||Thu thập đúng chỉ các đỉnh có bậc dương, theo thứ tự tăng dần.",
              "weight": 1,
              "maxScore": 1.2
            },
            {
              "id": "isolated_message",
              "criterion": "Outputs the exact fallback message when there is no happy vertex at all.|||Xuất đúng thông điệp dự phòng khi không có đỉnh hạnh phúc nào.",
              "weight": 1,
              "maxScore": 0.6
            }
          ]
        }
      ]
    }
  ]
};
