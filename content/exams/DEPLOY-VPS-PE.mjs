/**
 * Deploy VPS — Practical Exam (PE): 5 câu thực hành, nộp .zip. Mỗi câu 2 điểm.
 *
 * Đề tự soạn, bám sát `content/courses/deploy-vps/s00…s11`. Khác đề FE (50 câu
 * trắc nghiệm, đọc terminal), đề này bắt VIẾT HIỆN VẬT: một script tráo phiên
 * bản mà cú lùi khôi phục cả TIẾN TRÌNH chứ không chỉ con trỏ, một migration mà
 * bản phát hành TRƯỚC vẫn sống sót cộng một lượt nạp bù chia lô tiếp tục được,
 * một cặp Compose + Dockerfile giữ mọi bí mật ra ngoài ảnh trong khi vẫn để
 * SIGTERM tới được tiến trình, một bộ kiểm khói bắt đúng ca "tuyến 404 vì ảnh
 * cũ", và một script chẩn đoán chỉ-đọc cho ca "deploy xanh mà production vẫn
 * chạy bản cũ".
 *
 * ⚠️ KHÔNG MỘT BYTE NÀO CHẠM VÀO HẠ TẦNG THẬT. Mọi phép đo dưới đây chạy trong
 * container Docker cục bộ, và mọi giá trị nhạy cảm trong đề là GIÁ TRỊ GIẢ, cố
 * ý viết cho khác hẳn giá trị thật (`MAT_KHAU_GIA_LAP`, `sk_ci_GIA_LAP…`).
 *
 * ⚙️ SÂN ĐO (dựng rồi xoá bằng `docker rm -f`):
 *   • "VPS giả": container `debian:12` (Debian 12.15, aarch64) — bash 5.2.15,
 *     nginx 1.22.1, curl 7.88.1, rsync 3.2.7, OpenSSH_9.2p1, coreutils 9.1,
 *     iproute2 (`ss`), lsof, e2fsprogs, strace 6.1, python3 3.11.2 đóng vai
 *     ứng dụng (nó chỉ cần phục vụ `/health`, `/ban` và hai tuyến API).
 *   • PostgreSQL 16.14 trong container `postgres:16` riêng, CSDL nháp `de`.
 *   • `node:22-slim` (Node v22.23.2 trong ảnh) cho câu 3.
 *   • Docker Engine 29.5.3 / Compose v5.1.4, nhân 6.12.76-linuxkit, linux/arm64.
 *
 * ⚠️ MỌI `sampleSolution` VÀ MỌI `expectedOutput` DƯỚI ĐÂY ĐÃ CHẠY THẬT:
 *   • Q1: hai lần tráo ping-pong 3501↔3502 thành công, rồi năm nhánh hỏng chạy
 *     từng cái một — mã thoát 2 / 4 / 7 / 8 và một lần bị khoá chặn; cửa trước
 *     giữ nguyên `v3` suốt cả bốn nhánh hỏng. Việc chạy thử ấy tìm ra HAI con bọ
 *     trong chính lời giải: (a) trap không tự tắt `errexit` nên một `grep` không
 *     khớp giết trap và biến `exit 7` thành `exit 1`; (b) cần `sleep` ngắn sau
 *     `nginx -s reload` trước khi hỏi cửa trước, vì reload là bất đồng bộ.
 *   • Q2: migration áp bằng `psql -v ON_ERROR_STOP=1 -f` trên bảng 200.000 dòng
 *     (17 MB), nạp bù 20 lô / 3.571 ms, rồi năm phép kiểm hậu-migration chạy
 *     thật — kể cả một lệnh chèn thực hiện đúng cách MÃ CŨ vẫn làm.
 *   • Q3: `docker compose build` + `up -d` thật; `docker history` khớp token
 *     đúng 0 lần, `Config.Env` không có JWT lẫn TOKEN, container đạt `healthy`
 *     bằng healthcheck viết bằng module http của node (ảnh KHÔNG có curl/wget),
 *     `docker stop` 225 ms với ExitCode 0, và bỏ `JWT_SECRET` thì container vào
 *     trạng thái `Restarting (1)`.
 *   • Q4: năm kết cục 0 / 5 / 6 / 8 / 9, trong đó ca 8 là một bản thật sự thiếu
 *     tuyến `/api/v1/bai`, và ca 5 tốn 1 ms thay vì 3.073 ms.
 *   • Q5: sáu kết cục 0 / 5 / 21 / 22 / 23 / 24, mỗi trạng thái được tạo ra có
 *     chủ đích (kể cả ca 23: làm ấm `proxy_cache` rồi mới tráo tiến trình).
 *
 * ⚠️ VÌ SAO Q1, Q4, Q5 CÓ `khongChayDuoc`. `scripts/exam-check.mjs` chỉ có
 * `node` và `bash`, và nhãn `bash` thì nó CHẠY THẬT bằng `/bin/bash` — đúng như
 * vậy, vì `LINUX-BASH-PE` phải tiếp tục được chạy thật. Ba câu ấy cần một nginx
 * đang chạy, hai cổng ứng dụng, `/proc/<pid>` của một tiến trình thật và một
 * tệp khoá trong `/var/lock`, nên chúng không tự chứa; mỗi câu khai lý do bằng
 * chữ, bộ kiểm in một dòng `ℹ` và vẫn kiểm trọn phần cấu trúc. Câu 2 khai
 * `language: 'sql'` và câu 3 khai `language: 'yaml'` — cả hai nhãn đã nằm sẵn
 * trong tập `KHONG_CHAY_DUOC` của bộ kiểm, nên chúng KHÔNG cần và KHÔNG nên
 * khai thêm field này.
 *
 * Kết quả: `0 lỗi · 0 cảnh báo`, kèm 5 dòng `ℹ` nói rõ vì sao không chạy.
 *
 * Seed: node scripts/academy-seed-exam.mjs --file ./content/exams/DEPLOY-VPS-PE.mjs --apply
 */
import { B, c, code, codeQ } from './_lib/deployvps-exam-kit.mjs';

/**
 * Rubric riêng cho từng câu: tổng `maxScore` đúng bằng `points` của câu (2,0),
 * và `weight === maxScore` ở mọi dòng, nên điểm từng tiêu chí cộng lại ra thẳng
 * điểm câu — không phải quy đổi.
 */
const rubric = (rows) => rows.map(([id, en, vi, maxScore]) => ({
  id,
  criterion: B(en, vi),
  weight: maxScore,
  maxScore,
}));

const INSTRUCTIONS =
  '<div class="ml-en">' +
  '<p><b>How to take this exam.</b></p>' +
  '<ol>' +
  '<li><b>Build the fixture first.</b> One privileged Linux container is enough: <code>docker run -d --name de-vps --privileged debian:12 sleep infinity</code>, then install <code>nginx curl python3 iproute2 util-linux</code> inside it. Create <code>/srv/pe/ban/v1 … v5</code>, each holding the same tiny <code>app.py</code> that reads <code>CONG</code> and <code>BAN</code> from the environment and serves <code>/health</code> (200), <code>/ban</code> (its own version), <code>/api/v1/bai</code> (200) and <code>/api/v1/rieng</code> (401); put an nginx on <code>127.0.0.1:3500</code> whose only mutable file is <code>/srv/pe/nx/upstream.conf</code>. To exercise a failure branch, make one release miss a route and another exit non-zero at start-up. For Q2, a scratch PostgreSQL is one command: <code>docker run --rm -d --name pg-de -e POSTGRES_PASSWORD=x -p 55439:5432 postgres:16</code>. Every reference answer here was produced on exactly that fixture.</li>' +
  '<li><b>Nothing in this exam touches a real server.</b> No SSH to any host, no real credential anywhere, and every sensitive-looking value is a placeholder written to be obviously fake. If your answer contains a value that could authenticate against anything, it is wrong regardless of whether it works.</li>' +
  '<li>Create five folders named <code>Q1 … Q5</code>. Each question names the exact files it wants inside its folder and shows a <b>Starter</b> block — copy it in <b>verbatim</b> and write your answer only between the two <code>ĐỀ CHO SẴN</code> markers. The fixture and the commands in the "expected output" block are part of the grading; changing them is how you fail a question you actually solved.</li>' +
  '<li><b>Run every failure branch, not just the happy path.</b> Three of these five questions are graded mostly on what happens when something goes wrong, and the reference answers for Q1 and Q5 each contain a fix for a bug that only appeared when the failure branches were actually run. Cause each failure on purpose, then check the <em>machine</em> — what the front door serves, what exit code the caller sees — rather than the log.</li>' +
  '<li>Zip the five folders into <b>one .zip</b> and upload it in the submit box.</li>' +
  '</ol>' +
  '<p><b>How it is graded.</b> Behaviour first — a script that does not run, a migration that does not apply, or a compose file that does not start cannot pass. But this is a deploy exam, so the <b>shape</b> is graded too: a swap that stops the old version before the new one answers, a rollback that moves the pointer without restarting the process, a refusal that exits 0, a secret reachable through <code>docker history</code>, a backfill inside the deploy script, a check that passes because it tested nothing, or a diagnostic that aborts with exit 1 because a <code>grep</code> found nothing all cost marks <em>even when the happy path works</em>. If in doubt, ask of every step: what does the <em>user</em> see if this step fails right here, and what exit code does the caller get?</p>' +
  '</div>' +
  '<div class="ml-vi">' +
  '<p><b>Cách làm bài thi.</b></p>' +
  '<ol>' +
  '<li><b>Dựng bộ đồ nghề trước.</b> Một container Linux privileged là đủ: <code>docker run -d --name de-vps --privileged debian:12 sleep infinity</code>, rồi cài <code>nginx curl python3 iproute2 util-linux</code> vào trong. Tạo <code>/srv/pe/ban/v1 … v5</code>, mỗi thư mục giữ cùng một tệp <code>app.py</code> tí hon đọc <code>CONG</code> và <code>BAN</code> từ môi trường rồi phục vụ <code>/health</code> (200), <code>/ban</code> (phiên bản của chính nó), <code>/api/v1/bai</code> (200) và <code>/api/v1/rieng</code> (401); dựng một nginx trên <code>127.0.0.1:3500</code> mà tệp DUY NHẤT được sửa là <code>/srv/pe/nx/upstream.conf</code>. Muốn thử một nhánh hỏng thì làm cho một bản phát hành THIẾU một tuyến và một bản khác thoát khác 0 ngay lúc khởi động. Cho câu 2, một PostgreSQL nháp chỉ tốn một lệnh: <code>docker run --rm -d --name pg-de -e POSTGRES_PASSWORD=x -p 55439:5432 postgres:16</code>. Mọi đáp án mẫu ở đây được tạo ra trên đúng bộ đồ nghề ấy.</li>' +
  '<li><b>Không có gì trong bài thi này chạm vào một máy chủ thật.</b> Không SSH tới máy nào, không có thông tin xác thực thật ở đâu cả, và mọi giá trị trông có vẻ nhạy cảm đều là chỗ giữ chỗ viết cho GIẢ một cách hiển nhiên. Nếu lời giải của bạn chứa một giá trị có thể xác thực được với bất cứ thứ gì thì nó SAI, bất kể nó có chạy hay không.</li>' +
  '<li>Tạo năm thư mục tên <code>Q1 … Q5</code>. Mỗi câu ghi rõ những tệp nào phải nằm trong thư mục của nó và có một khối <b>Mã cho sẵn</b> — chép <b>nguyên văn</b> vào rồi chỉ viết lời giải ở vùng giữa hai mốc <code>ĐỀ CHO SẴN</code>. Bộ đồ nghề và các câu lệnh trong khối "kết quả mong đợi" là một phần của việc chấm; sửa chúng là cách trượt một câu mà bạn thật ra đã làm được.</li>' +
  '<li><b>Hãy chạy MỌI nhánh hỏng, đừng chỉ chạy đường thành công.</b> Ba trong năm câu này chấm chủ yếu ở chỗ chuyện gì xảy ra khi có thứ hỏng, và đáp án mẫu của câu 1 với câu 5 mỗi cái đều mang theo một chỗ sửa cho một con bọ chỉ lòi ra khi các nhánh hỏng được chạy thật. Hãy gây ra từng cú hỏng một cách có chủ đích, rồi kiểm CÁI MÁY — cửa trước đang phục vụ gì, phía gọi nhận được mã thoát nào — chứ đừng kiểm cái nhật ký.</li>' +
  '<li>Nén năm thư mục thành <b>một file .zip</b> rồi tải lên ô nộp bài.</li>' +
  '</ol>' +
  '<p><b>Chấm thế nào.</b> Hành vi trước — một script không chạy, một migration không áp được, hay một tệp compose không khởi động nổi thì không thể qua. Nhưng đây là bài thi về deploy, nên <b>HÌNH DẠNG</b> cũng bị chấm: một cú tráo dừng bản cũ trước khi bản mới trả lời được, một cú lùi dời con trỏ mà không khởi động lại tiến trình, một lời từ chối thoát ra 0, một bí mật với tới được qua <code>docker history</code>, một lượt nạp bù nằm bên trong script deploy, một phép kiểm ĐẠT vì nó chẳng kiểm gì cả, hay một script chẩn đoán chết với mã thoát 1 vì một lệnh <code>grep</code> không tìm thấy gì — tất cả đều bị trừ điểm <em>ngay cả khi đường thành công vẫn chạy tốt</em>. Nếu phân vân, hãy hỏi ở mỗi bước: NGƯỜI DÙNG thấy gì nếu bước này hỏng ngay tại đây, và phía gọi nhận được mã thoát nào?</p>' +
  '</div>';


export default {
  course: { slug: 'deploy-vps' },
  exams: [
    {
      kind: 'PE',
      code: 'PE',
      peType: 'CODE',
      source: 'SAMPLE',
      sortOrder: 2,
      title: B(
        "Practical Exam — five deploy artifacts you have to run",
        "Thi thực hành — năm hiện vật deploy bạn buộc phải chạy thật",
      ),
      description: B(
        "Five practical questions submitted as one .zip: a swap script that starts the new version before stopping the old one and whose rollback restores the process rather than only the pointer, a migration the previous release survives together with a resumable batched backfill, a Compose and Dockerfile pair that keeps every secret out of the image while letting SIGTERM reach the process, a smoke test that fails the deploy on a 404 and cannot pass by accident, and a read-only diagnosis for the deploy that reported success while production stayed on the old version.",
        "Năm câu thực hành nộp trong một file .zip: một script tráo khởi động bản mới trước khi dừng bản cũ và có cú lùi khôi phục cả TIẾN TRÌNH chứ không chỉ con trỏ, một migration mà bản phát hành trước vẫn sống sót cùng một lượt nạp bù chia lô tiếp tục được, một cặp Compose với Dockerfile giữ mọi bí mật ra ngoài ảnh trong khi vẫn để SIGTERM tới được tiến trình, một bộ kiểm khói làm hỏng lần deploy khi gặp 404 và không thể ĐẠT một cách tình cờ, và một bài chẩn đoán CHỈ ĐỌC cho lần deploy báo thành công trong khi production vẫn nằm ở bản cũ.",
      ),
      durationMinutes: 120,
      totalPoints: 10,
      passMark: 5,
      shuffleQuestions: false,
      shuffleOptions: false,
      isPublished: true,
      instructions: INSTRUCTIONS,
      questions: [
        /* ── Q1 ─────────────────────────────────────────────────── */
        codeQ({
          points: 2,
          language: "bash",
          khongChayDuoc: "lời giải là một script deploy: nó cần một nginx đang chạy có `include upstream.conf`, HAI cổng ứng dụng để tráo qua lại, một tệp khoá trong `/var/lock`, và quyền `nginx -s reload` — không thứ nào tự chứa trong bộ kiểm. Đã chạy thật trong container `debian:12` (bash 5.2.15, nginx 1.22.1, python3 3.11.2 đóng vai ứng dụng): hai lần tráo thành công ping-pong 3501↔3502, rồi năm nhánh hỏng cho ra đúng mã thoát 2 / 4 / 7 / 8 và một lần bị khoá chặn — cửa trước giữ nguyên `v3` suốt cả bốn nhánh hỏng",
          prompt: B(
            "<p><b>Q1 — A swap that never drops a request, and a rollback that restores the process (chapters 3, 6 and 7).</b> Write <code>Q1/trien-khai.sh</code>, taking <code>[--dong-y] &lt;ten-ban&gt;</code>. The fixture is already on the machine: releases under <code>/srv/pe/ban/&lt;ten&gt;/app.py</code>, an nginx on <code>127.0.0.1:3500</code> whose only mutable file is <code>/srv/pe/nx/upstream.conf</code>, and two application ports <b>3501</b> and <b>3502</b>. The application serves <code>/health</code>, <code>/ban</code> (its own version), <code>/api/v1/bai</code> (200) and <code>/api/v1/rieng</code> (401), and takes about 800 ms to become ready.</p><ul><li><b>Refuse before you write.</b> Every check that can say no runs before the lock and before the first file is created, and each refusal carries its own exit code: <code>5</code> a required tool is missing, <code>2</code> no such release, <code>6</code> the artifact is malformed, <code>4</code> there is no terminal to ask on, <code>3</code> the user said no. Do not infer consent from silence: a prompt softened with <code>|| true</code> exits <b>0</b> without deploying.</li><li><b>One deploy at a time.</b> Hold a lock for the life of the script, and make sure the application you start does not inherit the lock descriptor and keep it after you exit.</li><li><b>Work out your own direction.</b> Read the port currently in <code>upstream.conf</code> and deploy to the other one, so the script takes no argument that can be wrong and running it twice gives two deploys rather than a broken state.</li><li><b>Start before you stop.</b> New release on the free port, poll <em>its</em> health until it genuinely answers (not <code>sleep</code>), then move the release symlink and switch traffic, and only then send <code>SIGTERM</code> to the old one — last.</li><li><b>Verify through the front door.</b> A smoke test on <code>127.0.0.1:3500</code> where 200 and 401 pass and <b>404 fails the deploy</b> (exit <code>8</code>), followed by a comparison of the <em>version served</em> against the release you asked for (exit <code>9</code>).</li><li><b>And roll back for real.</b> On any failure after the switch, put the upstream back, reload, and — because a running process does not follow a symlink that changes — make sure something is actually serving the old release again before you report the failure. A release that never came up exits <code>7</code> without having switched anything.</li></ul><p>Two things are graded that a script which merely works on the happy path will get wrong: whether every failure branch leaves the front door on the previous version, and whether the exit code the caller sees is the one your script chose.</p>",
            "<p><b>Câu 1 — Một cú tráo không rơi request nào, và một cú lùi khôi phục cả TIẾN TRÌNH (chương 3, 6 và 7).</b> Hãy viết <code>Q1/trien-khai.sh</code>, nhận <code>[--dong-y] &lt;ten-ban&gt;</code>. Bộ đồ nghề đã có sẵn trên máy: các bản phát hành nằm ở <code>/srv/pe/ban/&lt;ten&gt;/app.py</code>, một nginx trên <code>127.0.0.1:3500</code> mà tệp DUY NHẤT được phép sửa là <code>/srv/pe/nx/upstream.conf</code>, và hai cổng ứng dụng <b>3501</b> với <b>3502</b>. Ứng dụng phục vụ <code>/health</code>, <code>/ban</code> (phiên bản của chính nó), <code>/api/v1/bai</code> (200) và <code>/api/v1/rieng</code> (401), và mất khoảng 800 ms để sẵn sàng.</p><ul><li><b>Từ chối TRƯỚC khi ghi.</b> Mọi phép kiểm có thể nói không đều chạy trước cái khoá và trước tệp đầu tiên được tạo ra, và mỗi lời từ chối mang mã thoát riêng: <code>5</code> thiếu một công cụ bắt buộc, <code>2</code> không có bản phát hành đó, <code>6</code> tạo tác méo mó, <code>4</code> không có terminal để hỏi, <code>3</code> người dùng nói không. Đừng suy sự đồng ý ra từ sự im lặng: một lời hỏi bị làm mềm bằng <code>|| true</code> sẽ thoát ra <b>0</b> mà không deploy.</li><li><b>Một lần deploy tại một thời điểm.</b> Giữ một cái khoá suốt đời script, và bảo đảm ứng dụng bạn khởi động KHÔNG thừa kế mô tả tệp của khoá rồi giữ nó tiếp sau khi bạn thoát.</li><li><b>Tự tính ra chiều đi của mình.</b> Đọc cổng đang nằm trong <code>upstream.conf</code> rồi deploy sang cổng kia, để script không nhận một tham số nào có thể gõ sai và chạy nó hai lần thì ra hai lần deploy chứ không phải một trạng thái hỏng.</li><li><b>Khởi động TRƯỚC khi dừng.</b> Bản mới lên cổng đang rảnh, dò CHÍNH endpoint sức khoẻ của nó tới khi nó thật sự trả lời (không phải <code>sleep</code>), rồi mới dời symlink bản phát hành và chuyển lưu lượng, và chỉ tới lúc đó mới gửi <code>SIGTERM</code> cho bản cũ — SAU CÙNG.</li><li><b>Kiểm qua CỬA TRƯỚC.</b> Một bộ kiểm khói trên <code>127.0.0.1:3500</code> mà 200 và 401 là ĐẠT còn <b>404 làm hỏng cả lần deploy</b> (thoát <code>8</code>), tiếp theo là một phép so PHIÊN BẢN ĐANG ĐƯỢC PHỤC VỤ với bản bạn vừa yêu cầu (thoát <code>9</code>).</li><li><b>Và lùi bản cho ra lùi.</b> Với bất kỳ cú hỏng nào SAU bước chuyển, hãy trả upstream về, nạp lại, và — vì một tiến trình đang chạy KHÔNG đi theo symlink bị đổi — hãy bảo đảm thật sự có thứ gì đó đang phục vụ lại bản cũ trước khi bạn báo cú hỏng. Một bản không bao giờ lên được thì thoát <code>7</code> mà chưa chuyển gì cả.</li></ul><p>Có hai thứ bị chấm mà một script chỉ chạy được trên đường thành công sẽ làm sai: MỌI nhánh hỏng có để cửa trước nằm ở bản TRƯỚC không, và mã thoát mà phía gọi NHÌN THẤY có đúng là mã script bạn chọn không.</p>",
          ),
          starterCode: "# ── ĐỀ CHO SẴN — KHÔNG SỬA ─────────────────────────────────────────────\n# Q1/trien-khai.sh\n# Bo do nghe da co san: /srv/pe/ban/{v1..v5}/app.py, nginx tren 3500,\n# /srv/pe/nx/upstream.conf la tep DUY NHAT duoc sua, hai cong 3501 va 3502.\n# Chay lai bo do nghe: xem khoi \"cach lam bai\" o dau de.\n# Bien moi truong ung dung doc: CONG (cong lang nghe), BAN (ten phien ban).\n\n# ── VIẾT LỜI GIẢI CỦA BẠN Ở ĐÂY ────────────────────────────────────────\n\n\n# ── ĐỀ CHO SẴN — KHÔNG SỬA ─────────────────────────────────────────────\n# Chạy đúng những câu lệnh trong khối \"kết quả mong đợi\" rồi đối chiếu\n# từng dòng một trước khi nộp.\n",
          expectedOutput: "$ bash trien-khai.sh --dong-y v2\n21:54:54 -- deploy 'v2': v1 tren 3501 -> v2 tren 3502 --\n21:54:54 san sang sau 900ms\n21:54:54 da chuyen upstream sang 3502\n21:54:55     ok /health -> 200\n21:54:55     ok /api/v1/bai -> 200\n21:54:55     ok /api/v1/rieng -> 401\n21:54:55 cua truoc xac nhan 'v2'\n21:54:55 XONG — da SIGTERM ban cu tren 3501. Nhat ky: /srv/pe/nhat-ky/20260909-215454-v2.log\n  ma thoat: 0\n\n$ bash trien-khai.sh --dong-y v3        # cong ping-pong nguoc lai\n21:54:55 -- deploy 'v3': v2 tren 3502 -> v3 tren 3501 --\n21:54:56 san sang sau 900ms\n21:54:56 da chuyen upstream sang 3501\n21:54:56 cua truoc xac nhan 'v3'\n21:54:56 XONG — da SIGTERM ban cu tren 3502.\n  hien-tai -> /srv/pe/ban/v3        cua truoc: v3\n\n── NAM NHANH HONG, chay tung cai mot ─────────────────────────────────────\n$ bash trien-khai.sh --dong-y v9                 # ban khong ton tai\n21:56:31 x khong co ban 'v9'. Co: v1 v2 v3 v4 v5\n  ma thoat: 2\n\n$ bash trien-khai.sh v2 </dev/null               # khong co terminal de hoi\n21:56:31 x khong co terminal de hoi — dung --dong-y neu that su muon\n  ma thoat: 4\n\n$ bash trien-khai.sh --dong-y v4                 # ban THIEU mot tuyen\n21:56:31 -- deploy 'v4': v3 tren 3501 -> v4 tren 3502 --\n21:56:32 san sang sau 800ms\n21:56:32 da chuyen upstream sang 3502\n21:56:32     ok /health -> 200\n21:56:32 x     /api/v1/bai -> 404\n21:56:32     ok /api/v1/rieng -> 401\n21:56:32 x kiem khoi HONG — ban dung nua voi\n21:56:32 x hong (ma 8) — chuyen NGUOC upstream ve 3501 va nap lai nginx\n21:56:32 x da khoi phuc — cua truoc tra 'v3'\n  ma thoat: 8        → cua truoc: v3\n\n$ bash trien-khai.sh --dong-y v5                 # ban VO NGAY khi khoi dong\n21:56:34 -- deploy 'v5': v3 tren 3501 -> v5 tren 3502 --\n21:56:45 x ban 'v5' KHONG len duoc\n21:56:45 x hong (ma 7) — CHUA trao, ban cu tren 3501 van dang phuc vu\n  ma thoat: 7        → cua truoc: v3\n\n$ ( bash trien-khai.sh --dong-y v1 & ) ; sleep 0.15 ; bash trien-khai.sh --dong-y v3\n21:57:01 -- deploy 'v1': v2 tren 3502 -> v1 tren 3501 --      (A lay duoc khoa)\n21:57:03 XONG — da SIGTERM ban cu tren 3502.\n21:57:03 -- deploy 'v3': v1 tren 3501 -> v3 tren 3502 --      (B CHO roi moi chay)\n21:57:04 XONG — da SIGTERM ban cu tren 3501.\n  → cua truoc cuoi cung: v3      (thay doi MOI NHAT thang)",
          sampleSolution: "#!/bin/bash\nset -euo pipefail\nshopt -s inherit_errexit 2>/dev/null || true\n\nGOC=/srv/pe\nCUA_TRUOC=http://127.0.0.1:3500\nCONG_A=3501; CONG_B=3502\n\nDONG_Y=0; [ \"${1:-}\" = \"--dong-y\" ] && { DONG_Y=1; shift; }\nBAN=\"${1:?dung: trien-khai.sh [--dong-y] <ten-ban>}\"\n\nNK=\"$GOC/nhat-ky/$(date -u +%Y%m%d-%H%M%S)-$BAN.log\"\nghi() { printf '%s %s\\n' \"$(date -u +%H:%M:%S)\" \"$*\" | tee -a \"$NK\"; }\nloi() { printf '%s x %s\\n' \"$(date -u +%H:%M:%S)\" \"$*\" | tee -a \"$NK\" >&2; }\n\ngiu_cong() { ss -ltnp 2>/dev/null | grep \":$1 \" | grep -o 'pid=[0-9]*' | cut -d= -f2 | head -1; }\ncho_san_sang() {                        # $1=cong  $2=so lan thu\n  local i p\n  for i in $(seq 1 \"$2\"); do\n    p=$(curl -s -o /dev/null -w '%{http_code}' --max-time 1 \"http://127.0.0.1:$1/health\" || true)\n    [ \"$p\" = \"200\" ] && { echo \"$((i*100))\"; return 0; }\n    sleep 0.1\n  done\n  return 1\n}\n\nCU=\"\"; MOI=\"\"; DA_TRAO=0\ndon_dep() {\n  local ma=$?\n  # ⚠ TRAP PHAI TU TAT errexit. Neu khong, cau lenh dau tien tra khac 0 trong\n  #    trap (vi du grep khong tim thay tien trinh) se ket thuc trap va GHI DE\n  #    ma thoat that bang 1 — do that, exit 7 hien ra thanh exit 1.\n  set +e\n  if [ \"$ma\" -ne 0 ] && [ -n \"$MOI\" ]; then\n    local p; p=$(giu_cong \"$MOI\"); [ -n \"$p\" ] && kill -TERM \"$p\" 2>/dev/null\n    if [ \"$DA_TRAO\" = 1 ] && [ -n \"$CU\" ]; then\n      loi \"hong (ma $ma) — chuyen NGUOC upstream ve $CU va nap lai nginx\"\n      printf 'upstream ungdung { server 127.0.0.1:%s; keepalive 16; }\\n' \"$CU\" > \"$GOC/nx/upstream.conf\"\n      nginx -t -c \"$GOC/nx/nginx.conf\" -p \"$GOC/nx\" >/dev/null 2>&1 \\\n        && nginx -s reload -c \"$GOC/nx/nginx.conf\" -p \"$GOC/nx\" 2>/dev/null\n      # symlink KHONG phai tien trinh: neu ban cu da bi giet thi phai KHOI DONG LAI no\n      [ -n \"$(giu_cong \"$CU\")\" ] || {\n        CONG=\"$CU\" BAN=\"$CU_BAN\" setsid nohup python3 \"$GOC/hien-tai/app.py\" >>\"$NK\" 2>&1 </dev/null 9>&- &\n        cho_san_sang \"$CU\" 100 >/dev/null\n      }\n      sleep 0.3\n      loi \"da khoi phuc — cua truoc tra '$(curl -s --max-time 3 \"$CUA_TRUOC/ban\" | tr -d '\\n')'\"\n    else\n      loi \"hong (ma $ma) — CHUA trao, ban cu tren $CU van dang phuc vu\"\n    fi\n  fi\n  return $ma\n}\ntrap don_dep EXIT\n\n# 0. TIEN KIEM — moi loi tu choi o day deu xay ra TRUOC cai khoa va TRUOC\n#    tep dau tien duoc ghi, nen \"tu choi\" that su nghia la may KHONG bi dung toi.\nfor lenh in curl ss nginx python3 flock; do\n  command -v \"$lenh\" >/dev/null || { loi \"thieu cong cu '$lenh' — khong deploy duoc\"; exit 5; }\ndone\n[ -d \"$GOC/ban/$BAN\" ] || { loi \"khong co ban '$BAN'. Co: $(ls \"$GOC/ban\" | tr '\\n' ' ')\"; exit 2; }\n[ -f \"$GOC/ban/$BAN/app.py\" ] || { loi \"tao tac '$BAN' thieu app.py\"; exit 6; }\nif [ \"$DONG_Y\" != 1 ]; then\n  [ -t 0 ] || { loi \"khong co terminal de hoi — dung --dong-y neu that su muon\"; exit 4; }\n  read -rp \"Deploy '$BAN'? [y/N] \" tl || tl=\"\"\n  [ \"$tl\" = \"y\" ] || { ghi \"huy theo yeu cau nguoi dung\"; exit 3; }\nfi\n\n# 1. MOT lan deploy tai mot thoi diem. 9>&- o moi lenh nen, neu khong ung\n#    dung se thua ke fd 9 va giu khoa suot doi no.\nexec 9>/var/lock/trien-khai.lock\nflock -w 30 9 || { loi \"co lan deploy khac dang chay\"; exit 1; }\n\nCU=$(grep -oE '127\\.0\\.0\\.1:[0-9]+' \"$GOC/nx/upstream.conf\" | cut -d: -f2)\nMOI=$([ \"$CU\" = \"$CONG_A\" ] && echo \"$CONG_B\" || echo \"$CONG_A\")\nCU_BAN=$(curl -s --max-time 3 \"$CUA_TRUOC/ban\" | tr -d '\\n')\nghi \"-- deploy '$BAN': $CU_BAN tren $CU -> $BAN tren $MOI --\"\n\n# 2. Khoi dong ban MOI ben canh ban cu. Ban cu VAN dang phuc vu.\nCONG=\"$MOI\" BAN=\"$BAN\" setsid nohup python3 \"$GOC/ban/$BAN/app.py\" >>\"$NK\" 2>&1 </dev/null 9>&- &\nMS=$(cho_san_sang \"$MOI\" 100) || { loi \"ban '$BAN' KHONG len duoc\"; exit 7; }\nghi \"san sang sau ${MS}ms\"\n\n# 3. Chi khi no THAT SU tra loi moi tro symlink va chuyen luu luong.\nln -sfn \"$GOC/ban/$BAN\" \"$GOC/ht.moi\" && mv -Tf \"$GOC/ht.moi\" \"$GOC/hien-tai\"\nprintf 'upstream ungdung { server 127.0.0.1:%s; keepalive 16; }\\n' \"$MOI\" > \"$GOC/nx/upstream.conf\"\nnginx -t -c \"$GOC/nx/nginx.conf\" -p \"$GOC/nx\" >/dev/null 2>&1 || { loi \"nginx -t HONG\"; exit 6; }\nnginx -s reload -c \"$GOC/nx/nginx.conf\" -p \"$GOC/nx\" 2>/dev/null\nDA_TRAO=1\nghi \"da chuyen upstream sang $MOI\"\n\n# 4. KIEM QUA CUA TRUOC: route phai GAN (401 la DAT, 404 la ban cu),\n#    va PHIEN BAN phuc vu phai dung ban vua trao.\nsleep 0.3\nKL=0\nfor R in /health /api/v1/bai /api/v1/rieng; do\n  MA=$(curl -s -o /dev/null -w '%{http_code}' --max-time 2 \"$CUA_TRUOC$R\")\n  case \"$MA\" in 200|401) ghi \"    ok $R -> $MA\";; *) loi \"    $R -> $MA\"; KL=1;; esac\ndone\n[ \"$KL\" = 0 ] || { loi \"kiem khoi HONG — ban dung nua voi\"; exit 8; }\nTHAY=$(curl -s --max-time 3 \"$CUA_TRUOC/ban\" | tr -d '\\n')\n[ \"$THAY\" = \"$BAN\" ] || { loi \"cua truoc tra '$THAY', khong phai '$BAN'\"; exit 9; }\nghi \"cua truoc xac nhan '$THAY'\"\n\n# 5. GIO moi dung ban cu, TU TE. Buoc nay o CUOI: truoc no, moi cu hong\n#    deu lui duoc ngay lap tuc vi ban cu van dang chay.\nP=$(giu_cong \"$CU\"); [ -n \"$P\" ] && kill -TERM \"$P\" 2>/dev/null || true\nMOI=\"\"\nghi \"XONG — da SIGTERM ban cu tren $CU. Nhat ky: $NK\"",
          rubric: rubric([
            ["tuchoi",
              "Every refusal happens <em>before</em> the lock and before the first write, and each carries a distinct exit code — measured 2 for an unknown release and 4 for no terminal, with the machine byte-identical afterwards. A prompt that can exit 0 without deploying scores nothing here.",
              "Mọi lời từ chối xảy ra TRƯỚC cái khoá và trước lệnh ghi đầu tiên, và mỗi cái mang một mã thoát riêng — đo được 2 cho một bản phát hành lạ và 4 cho việc không có terminal, và sau đó cái máy giống hệt từng byte. Một lời hỏi có thể thoát ra 0 mà không deploy thì không được điểm dòng này.",
              0.5],
            ["trao",
              "The new release is started on the free port while the old one still serves, readiness is <em>polled</em> rather than slept through, traffic switches in one reload, and <code>SIGTERM</code> to the old process is the last step — so the whole thing is reversible right up to the end.",
              "Bản mới được khởi động trên cổng đang rảnh trong khi bản cũ VẪN đang phục vụ, mức sẵn sàng được DÒ chứ không phải ngủ cho qua, lưu lượng chuyển trong một lần nạp lại, và <code>SIGTERM</code> cho tiến trình cũ là bước CUỐI CÙNG — nên toàn bộ chuyện này lùi được cho tới tận phút chót.",
              0.5],
            ["kiem",
              "The verification goes through the front door: 404 on any listed route fails the deploy with exit 8, and the <em>version</em> served is compared against the release requested with exit 9 — a check that only reads status codes, or only asks the application port, scores nothing.",
              "Phép kiểm đi qua CỬA TRƯỚC: một cú 404 trên bất kỳ tuyến nào trong danh sách làm hỏng lần deploy với mã thoát 8, và PHIÊN BẢN được phục vụ được đem so với bản đã yêu cầu với mã thoát 9 — một phép kiểm chỉ đọc mã trạng thái, hoặc chỉ hỏi cổng ứng dụng, thì không được điểm.",
              0.5],
            ["lui",
              "The cleanup handler restores the process and not merely the pointer, disables errexit inside itself so a probe that finds nothing cannot overwrite the real exit code with 1, and the background application is started with the lock descriptor closed. Measured: exit 8 and exit 7 both left the front door on the previous release.",
              "Trình dọn dẹp khôi phục cả TIẾN TRÌNH chứ không chỉ con trỏ, tự tắt errexit bên trong chính nó để một phép dò không tìm thấy gì không thể ghi đè mã thoát thật bằng 1, và ứng dụng chạy nền được khởi động với mô tả tệp của khoá đã đóng. Đo thật: cả mã thoát 8 lẫn 7 đều để cửa trước nằm ở bản phát hành trước.",
              0.5],
          ]),
        }),

        /* ── Q2 ─────────────────────────────────────────────────── */
        codeQ({
          points: 2,
          language: "sql",
          prompt: B(
            "<p><b>Q2 — A migration the previous release survives, on a table with data in it (chapters 5 and 6).</b> <code>bai_viet</code> is deployed and populated: 200,000 rows, columns <code>id</code>, <code>tieu_de</code>, <code>trang_thai</code> (<code>NHAP</code> or <code>DANG</code>) and <code>dang_luc</code>. The new release wants a <code>tom_tat</code> of at most 300 characters, <code>NOT NULL</code>, derived from the first 297 characters of <code>tieu_de</code> with <code>...</code> appended. Write two files.</p><ul><li><b><code>Q2/migration.sql</code></b> — applied <em>inside</em> the deploy, with <code>psql -v ON_ERROR_STOP=1 -f</code>. It must be safe for the release that is <em>currently running</em> and does not know the column exists, and it must leave that release able to run afterwards. The obvious one-liner fails on the seeded data with <code>ERROR:  column \"tom_tat\" of relation \"bai_viet\" contains null values</code> — the exam is testing whether you know why, and what the safe shape is. Open the file with the two session settings that stop an <code>ALTER</code> queued behind a long query from taking the whole table down with it.</li><li><b><code>Q2/nap-bu.sql</code></b> — <em>one batch</em>, run repeatedly from outside the deploy until nothing is left. It must be resumable after an interruption, must not wait behind a row a user's transaction is holding, and must hold its locks for a fraction of a second rather than for the whole table.</li><li>Also add a constraint named <code>bai_viet_dang_phai_co_dang_luc</code>: a post with <code>trang_thai = 'DANG'</code> must have a <code>dang_luc</code>. Add it in the two-step form that does not scan the whole table under an exclusive lock.</li></ul><p>Two things beyond \"it applies\" are graded. The <em>order</em> inside the migration: there is a window between backfilling and installing the synchronisation in which rows written by the running release would be missed forever, and only one of the two orders closes it. And where the backfill lives: a batched loop inside the deploy script is a deploy that holds the deploy lock for its entire duration and gets killed by its own health-check timeout.</p>",
            "<p><b>Câu 2 — Một migration mà bản phát hành TRƯỚC vẫn sống sót, trên một cái bảng đã có dữ liệu (chương 5 và 6).</b> Bảng <code>bai_viet</code> đã deploy và đã có dữ liệu: 200.000 dòng, các cột <code>id</code>, <code>tieu_de</code>, <code>trang_thai</code> (<code>NHAP</code> hoặc <code>DANG</code>) và <code>dang_luc</code>. Bản phát hành mới muốn có cột <code>tom_tat</code> dài tối đa 300 ký tự, <code>NOT NULL</code>, suy ra từ 297 ký tự đầu của <code>tieu_de</code> rồi nối thêm <code>...</code>. Hãy viết HAI tệp.</p><ul><li><b><code>Q2/migration.sql</code></b> — áp TRONG lần deploy, bằng <code>psql -v ON_ERROR_STOP=1 -f</code>. Nó phải an toàn với bản phát hành ĐANG CHẠY, cái bản không hề biết có cột đó, và phải để lại một lược đồ mà bản ấy vẫn chạy được sau đó. Cách viết một dòng hiển nhiên sẽ hỏng trên dữ liệu đã có với <code>ERROR:  column \"tom_tat\" of relation \"bai_viet\" contains null values</code> — đề đang kiểm xem bạn có biết VÌ SAO, và dạng an toàn là gì. Hãy mở đầu tệp bằng hai thiết lập phiên chặn một câu <code>ALTER</code> phải xếp hàng sau một truy vấn dài kéo cả cái bảng chết theo.</li><li><b><code>Q2/nap-bu.sql</code></b> — MỘT LÔ, chạy lặp lại từ NGOÀI lần deploy cho tới khi không còn dòng nào. Nó phải tiếp tục được sau một cú cắt ngang, không được xếp hàng chờ một dòng mà giao dịch của người dùng đang giữ, và phải giữ khoá trong một phần nhỏ của giây chứ không phải suốt cả bảng.</li><li>Thêm một ràng buộc tên <code>bai_viet_dang_phai_co_dang_luc</code>: một bài có <code>trang_thai = 'DANG'</code> thì bắt buộc phải có <code>dang_luc</code>. Hãy thêm nó theo dạng HAI BƯỚC không quét cả bảng dưới một khoá độc quyền.</li></ul><p>Ngoài chuyện \"áp được\" thì còn hai thứ bị chấm. THỨ TỰ bên trong migration: có một cửa sổ nằm giữa việc nạp bù với việc gắn cơ chế đồng bộ, và trong cửa sổ ấy những dòng do bản đang chạy ghi ra sẽ bị bỏ sót vĩnh viễn — chỉ một trong hai thứ tự đóng được nó. Và chỗ đứng của bước nạp bù: một vòng lặp chia lô nằm bên trong script deploy là một lần deploy giữ cái khoá deploy suốt cả thời gian nó chạy rồi bị chính hạn giờ của chốt kiểm sức khoẻ giết đi.</p>",
          ),
          starterCode: "-- ── ĐỀ CHO SẴN — KHÔNG SỬA ─────────────────────────────────────────────\n-- Q2/migration.sql + nap-bu.sql\n-- Ap migration:  psql \"$DATABASE_URL\" -v ON_ERROR_STOP=1 -f Q2/migration.sql\n-- Chay nap bu :  lap lai psql -f Q2/nap-bu.sql toi khi khong con dong nao\n-- Bang bai_viet dang co 200.000 dong. Khong duoc xoa hay tao lai bang;\n-- khong duoc dung DEFAULT de lach buoc nap bu.\n\n-- ── VIẾT LỜI GIẢI CỦA BẠN Ở ĐÂY ────────────────────────────────────────\n\n\n-- ── ĐỀ CHO SẴN — KHÔNG SỬA ─────────────────────────────────────────────\n-- Chạy đúng những câu lệnh trong khối \"kết quả mong đợi\" rồi đối chiếu\n-- từng dòng một trước khi nộp.\n",
          expectedOutput: "$ psql -U postgres -d de -v ON_ERROR_STOP=1 -f migration.sql\nSET\nSET\nALTER TABLE\nCREATE FUNCTION\nCREATE TRIGGER\nALTER TABLE\nALTER TABLE\n  psql ma thoat: 0\n\n$ bash nap-bu.sh                    # chay NGOAI lan deploy\n  lo 15 xong — con lai chua nap: 50000\n  lo 16 xong — con lai chua nap: 40000\n  lo 17 xong — con lai chua nap: 30000\n  lo 18 xong — con lai chua nap: 20000\n  lo 19 xong — con lai chua nap: 10000\n  lo 20 xong — con lai chua nap: 0\n  tong: 3571 ms cho 20 lo tren 200.000 dong\n\n-- Kiem 1: MA CU ghi mot dong (khong biet cot tom_tat), ma MOI doc duoc ngay\nINSERT 0 1\n    tieu_de    |     tom_tat\n---------------+------------------\n Bai cua ma CU | Bai cua ma CU...\n(1 row)\n\n-- Kiem 2: da nap bu het chua\ncon NULL: 0\n\n-- Kiem 3: THU HEP — gio moi siet NOT NULL duoc\nALTER TABLE\n\n-- Kiem 4: rang buoc CHECK can duoc mot lenh ghi sai\nERROR:  new row for relation \"bai_viet\" violates check constraint \"bai_viet_dang_phai_co_dang_luc\"\nDETAIL:  Failing row contains (10005, Bai viet so 10005, DANG, null, Bai viet so 10005...).\n\n-- Kiem 5: hai dong dau\n id |     tom_tat\n----+------------------\n  1 | Bai viet so 1...\n  2 | Bai viet so 2...\n(2 rows)",
          sampleSolution: "-- ══ migration.sql — ap trong lan deploy, bang prisma migrate deploy hoac psql -v ON_ERROR_STOP=1 -f\n-- Bang nay dang duoc doc boi ma CU trong suot lan deploy, nen moi cau lenh\n-- duoi day phai an toan voi no. Hai dong dau chan mot ALTER phai xep hang\n-- sau mot truy van dai roi keo ca bang xuong theo.\nSET lock_timeout = '3s';\nSET statement_timeout = '30s';\n\n-- 1) MO RONG: them cot MOI, cho phep NULL. Mac dinh HANG nen day la mot\n--    lan sua danh muc, khong ghi lai dong nao (do: 0,8 ms tren 400k dong).\nALTER TABLE bai_viet ADD COLUMN tom_tat varchar(300);\n\n-- 2) TRIGGER dong bo TRUOC khi nap bu: tu giay nay tro di, moi dong ma\n--    MA CU ghi ra deu tu dong co tom_tat. Neu nap bu truoc roi moi gan\n--    trigger thi nhung dong ghi trong khe giua se khong bao gio duoc lap.\nCREATE OR REPLACE FUNCTION dong_bo_tom_tat() RETURNS trigger AS $$\n  BEGIN\n    IF NEW.tom_tat IS NULL THEN\n      NEW.tom_tat := left(NEW.tieu_de, 297) || '...';\n    END IF;\n    RETURN NEW;\n  END\n$$ LANGUAGE plpgsql;\n\nCREATE TRIGGER tg_dong_bo_tom_tat\n  BEFORE INSERT OR UPDATE ON bai_viet\n  FOR EACH ROW EXECUTE FUNCTION dong_bo_tom_tat();\n\n-- 3) RANG BUOC CHECK them theo dang HAI BUOC: NOT VALID la tuc thi va chi\n--    ap cho dong MOI; VALIDATE quet dong cu duoi mot khoa YEU hon.\nALTER TABLE bai_viet\n  ADD CONSTRAINT bai_viet_dang_phai_co_dang_luc\n  CHECK (trang_thai <> 'DANG' OR dang_luc IS NOT NULL) NOT VALID;\nALTER TABLE bai_viet VALIDATE CONSTRAINT bai_viet_dang_phai_co_dang_luc;\n\n-- ══ nap-bu.sql — mot LO. Chay NGOAI lan deploy, lap toi khi khong con dong nao\n-- Chay NGOAI lan deploy. Moi lo la MOT giao dich rieng, nen dung giua chung\n-- van giu duoc phan da lam; chay lai thi no di tiep tu cho do.\nWITH c AS (\n  SELECT id FROM bai_viet\n  WHERE tom_tat IS NULL\n  ORDER BY id\n  LIMIT 10000\n  FOR UPDATE SKIP LOCKED\n)\nUPDATE bai_viet b\n   SET tom_tat = left(b.tieu_de, 297) || '...'\n  FROM c WHERE b.id = c.id;",
          rubric: rubric([
            ["mo-rong",
              "The column is added nullable in its own statement rather than as <code>ADD COLUMN … NOT NULL</code>, and the file opens with <code>SET lock_timeout</code> and <code>SET statement_timeout</code>; the whole file applies cleanly under <code>ON_ERROR_STOP=1</code> and exits 0.",
              "Cột được thêm ở dạng cho phép NULL bằng một câu lệnh riêng chứ không phải <code>ADD COLUMN … NOT NULL</code>, và tệp mở đầu bằng <code>SET lock_timeout</code> với <code>SET statement_timeout</code>; cả tệp áp sạch dưới <code>ON_ERROR_STOP=1</code> và thoát ra 0.",
              0.5],
            ["thu-tu",
              "The synchronisation is installed <b>before</b> the backfill, so no row written by the running release during the backfill can be missed — demonstrated by an insert made the way the <em>old</em> code makes it, whose <code>tom_tat</code> comes back filled without the new code touching it.",
              "Cơ chế đồng bộ được gắn TRƯỚC bước nạp bù, để không dòng nào do bản đang chạy ghi ra trong lúc nạp bù bị bỏ sót — chứng minh bằng một lệnh chèn thực hiện đúng theo cách MÃ CŨ vẫn làm, mà <code>tom_tat</code> của nó vẫn về đầy đủ dù mã mới không hề đụng vào.",
              0.5],
            ["nap-bu",
              "The backfill is a separate file that does one bounded batch, selects only rows still missing the value so a re-run resumes where it stopped, and uses <code>FOR UPDATE SKIP LOCKED</code> so it steps over a row a live transaction is holding instead of queueing behind ordinary traffic.",
              "Bước nạp bù là một tệp RIÊNG làm đúng một lô có giới hạn, chỉ chọn những dòng còn thiếu giá trị nên chạy lại là tiếp tục từ chỗ đã dừng, và dùng <code>FOR UPDATE SKIP LOCKED</code> để nó BƯỚC QUA một dòng đang bị giao dịch sống giữ thay vì xếp hàng sau lưu lượng thường.",
              0.5],
            ["thu-hep",
              "The check constraint carries the required name, expresses \"published implies a publish time\" correctly for the enum, and is added <code>NOT VALID</code> followed by a separate <code>VALIDATE CONSTRAINT</code>; and <code>SET NOT NULL</code> is left for the contract phase, after the backfill reports zero rows remaining.",
              "Ràng buộc check mang đúng tên yêu cầu, diễn đạt đúng ý \"đã đăng thì phải có thời điểm đăng\" cho tập giá trị đó, và được thêm bằng <code>NOT VALID</code> rồi mới tới một câu <code>VALIDATE CONSTRAINT</code> riêng; còn <code>SET NOT NULL</code> thì để dành cho giai đoạn THU HẸP, sau khi bước nạp bù báo không còn dòng nào.",
              0.5],
          ]),
        }),

        /* ── Q3 ─────────────────────────────────────────────────── */
        codeQ({
          points: 2,
          language: "yaml",
          prompt: B(
            "<p><b>Q3 — Keep the secrets out of the artifact, and the signals reaching the process (chapters 3, 4 and 8).</b> Write <code>Q3/docker-compose.yml</code> and <code>Q3/Dockerfile</code> for a small Node service, plus the two files they read from outside the image. The application listens on 8080, refuses to start when a required variable is absent, serves <code>/cau-hinh</code> returning both a value baked at <em>build</em> time and one read at <em>run</em> time, and handles <code>SIGTERM</code>.</p><ul><li><b>Two kinds of value, two mechanisms.</b> A public URL is baked in during the build and must therefore arrive as a build argument; the database URL and the signing key are read at run time from a file on the host that no deploy ever touches. Neither may end up in the image.</li><li><b>A secret the build itself needs.</b> A CI registry token has to be available to one <code>RUN</code> step. It must not appear in <code>docker history</code> and must not be an <code>ARG</code> — <code>--build-arg</code> is not a secret mechanism.</li><li><b>The signal must reach the application.</b> A container whose PID 1 ignores <code>SIGTERM</code> takes the whole grace period and exits <b>137</b>; yours must shut down cleanly. Give the container more grace than the application's own shutdown deadline.</li><li><b>A health check that can run inside this image.</b> The image ships with neither <code>curl</code> nor <code>wget</code>. A probe that calls a tool that is not there loops, burns seconds on every deploy, and verifies nothing.</li><li><b>And the file permissions.</b> Both host-side files hold credentials.</li></ul><p>The measurements that decide the marks are in the expected output: <code>docker history</code> and <code>Config.Env</code> must not name the token or the signing key, changing the run-time variable must take effect on a restart while the build-time one must <em>not</em>, <code>docker stop</code> must be fast with a clean exit code, and removing a required variable must make the container refuse to start rather than serve 500s.</p>",
            "<p><b>Câu 3 — Giữ bí mật ra ngoài tạo tác, và giữ cho tín hiệu tới được tiến trình (chương 3, 4 và 8).</b> Hãy viết <code>Q3/docker-compose.yml</code> và <code>Q3/Dockerfile</code> cho một dịch vụ Node nhỏ, cộng hai tệp mà chúng đọc từ NGOÀI ảnh. Ứng dụng lắng nghe cổng 8080, TỪ CHỐI khởi động khi thiếu một biến bắt buộc, phục vụ <code>/cau-hinh</code> trả về cả một giá trị nướng lúc DỰNG lẫn một giá trị đọc lúc CHẠY, và có xử lý <code>SIGTERM</code>.</p><ul><li><b>Hai loại giá trị, hai cơ chế.</b> Một URL công khai được nướng vào lúc dựng nên phải tới nơi dưới dạng tham số dựng; còn URL cơ sở dữ liệu và khoá ký thì đọc lúc CHẠY từ một tệp trên máy chủ mà không lần deploy nào đụng tới. Không cái nào được phép nằm lại trong ảnh.</li><li><b>Một bí mật mà chính lần DỰNG cần tới.</b> Một token registry của CI phải dùng được ở đúng một bước <code>RUN</code>. Nó không được xuất hiện trong <code>docker history</code> và không được là một <code>ARG</code> — <code>--build-arg</code> KHÔNG phải một cơ chế bí mật.</li><li><b>Tín hiệu phải TỚI ĐƯỢC ứng dụng.</b> Một container mà PID 1 lờ <code>SIGTERM</code> sẽ tốn trọn khoảng ân hạn rồi thoát ra <b>137</b>; cái của bạn phải tắt sạch sẽ. Hãy cho container nhiều ân hạn hơn hạn chót tắt máy của chính ứng dụng.</li><li><b>Một chốt kiểm sức khoẻ CHẠY ĐƯỢC bên trong ảnh này.</b> Ảnh không có <code>curl</code> lẫn <code>wget</code>. Một phép thăm dò gọi một công cụ không có ở đó sẽ quay vòng, đốt vài giây mỗi lần deploy, và chẳng kiểm được gì.</li><li><b>Và quyền tệp.</b> Cả hai tệp phía máy chủ đều giữ thông tin xác thực.</li></ul><p>Những phép đo quyết định điểm nằm trong khối kết quả mong đợi: <code>docker history</code> và <code>Config.Env</code> không được gọi tên token hay khoá ký, đổi biến LÚC CHẠY thì phải có hiệu lực sau một lần khởi động lại còn biến LÚC DỰNG thì KHÔNG, <code>docker stop</code> phải nhanh với một mã thoát sạch, và bỏ đi một biến bắt buộc thì container phải TỪ CHỐI khởi động chứ không phải phục vụ hàng loạt cú 500.</p>",
          ),
          starterCode: "# ── ĐỀ CHO SẴN — KHÔNG SỬA ─────────────────────────────────────────────\n# Q3/docker-compose.yml + Dockerfile + ngoai-anh/\n# Ma nguon Node da co san o Q3/nguon/app.js. No:\n#   - doc /app/dist/duoc-nuong.txt (gia tri NUONG luc DUNG)\n#   - doc process.env.MUC_LOG      (gia tri doc luc CHAY)\n#   - thoat 1 neu thieu DATABASE_URL hoac JWT_SECRET\n#   - bat SIGTERM roi server.close()\n# Moi gia tri nhay cam trong bai nay la GIA LAP — dung gia tri that o dau ca.\n\n# ── VIẾT LỜI GIẢI CỦA BẠN Ở ĐÂY ────────────────────────────────────────\n\n\n# ── ĐỀ CHO SẴN — KHÔNG SỬA ─────────────────────────────────────────────\n# Chạy đúng những câu lệnh trong khối \"kết quả mong đợi\" rồi đối chiếu\n# từng dòng một trước khi nộp.\n",
          expectedOutput: "$ docker compose -p pecfg build\n Image pe-cauhinh:v1 Built\n\n=== 1) bi mat luc DUNG co lot vao anh khong? ===\n  docker history nhac toi 'sk_ci': 0\n  Config.Env nhac toi JWT hay TOKEN: 0\n  Config.Env day du:\n    PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin\n    NODE_VERSION=22.23.2\n    YARN_VERSION=1.22.22\n    NODE_ENV=production\n  anh co curl/wget khong: KHONG\n\n=== 2) khoi dong, bien BAT BUOC den tu env_file NGOAI anh ===\n Container pecfg-ungdung-1 Started\npecfg-ungdung-1  running  Up 7 seconds (healthy)\n  /cau-hinh: {\"nuong_luc_dung\":\"https://api.vi-du.test\",\"doc_luc_chay\":\"info\"}\n\n=== 3) doi bien LUC CHAY roi khoi dong lai (khong dung lai anh) ===\n  /cau-hinh: {\"nuong_luc_dung\":\"https://api.vi-du.test\",\"doc_luc_chay\":\"debug\"}\n\n=== 4) doi gia tri LUC DUNG roi CHI khoi dong lai ===\n  /cau-hinh: {\"nuong_luc_dung\":\"https://api.vi-du.test\",\"doc_luc_chay\":\"info\"}\n  --- gio DUNG LAI roi tráo ---\n  /cau-hinh: {\"nuong_luc_dung\":\"https://api.MOI.test\",\"doc_luc_chay\":\"info\"}\n\n=== 5) docker stop: tin hieu co toi ung dung khong? ===\n  docker stop mat 225 ms  ExitCode=0\n    nghe 8080\n    SIGTERM — tat tu te\n\n=== 6) thieu mot bien BAT BUOC: ung dung phai TU CHOI khoi dong ===\npecfg-ungdung-1 restarting Restarting (1) Less than a second ago\n    Thieu bien moi truong bat buoc: JWT_SECRET",
          sampleSolution: "# ══ docker-compose.yml\nservices:\n  ungdung:\n    image: pe-cauhinh:v1\n    build:\n      context: .\n      args:\n        URL_API_CONG_KHAI: ${URL_API_CONG_KHAI:-https://api.vi-du.test}\n      secrets:\n        - token_ci\n    # Bi mat luc CHAY nam NGOAI tao tac va ngoai kho ma: mot tep tren may chu\n    # ma khong lan deploy nao dung toi.\n    env_file:\n      - ./ngoai-anh/.env\n    environment:\n      MUC_LOG: ${MUC_LOG:-info}\n    init: true                 # PID 1 chuyen tiep tin hieu → 143 chu khong 137\n    stop_grace_period: 20s     # dai hon han chot tat may cua chinh ung dung\n    restart: unless-stopped\n    healthcheck:\n      # Anh nay KHONG co curl lan wget — moi phep kiem HTTP ben trong no\n      # phai dung module http cua node.\n      test: [\"CMD\", \"node\", \"kiem-suc-khoe.js\"]\n      interval: 5s\n      timeout: 3s\n      retries: 3\n      start_period: 5s\nsecrets:\n  token_ci:\n    file: ./ngoai-anh/token_ci\n\n# ══ Dockerfile\n# syntax=docker/dockerfile:1\nFROM node:22-slim AS dung\nWORKDIR /app\n# Gia tri CONG KHAI, nuong vao luc DUNG — doi no doi hoi DUNG LAI, khong phai\n# khoi dong lai. Dat ARG o day la co y: no khong bao gio la bi mat.\nARG URL_API_CONG_KHAI=https://api.vi-du.test\nCOPY nguon/ ./nguon/\nRUN mkdir -p dist && printf '%s' \"$URL_API_CONG_KHAI\" > dist/duoc-nuong.txt\n# Bi mat luc DUNG di qua MOT MOUNT, khong qua ARG: no khong vao tang anh\n# nao va khong vao `docker history`.\nRUN --mount=type=secret,id=token_ci \\\n    test -s /run/secrets/token_ci && echo \"da xac thuc voi registry noi bo\" > dist/dau-vet.txt\n\nFROM node:22-slim\nWORKDIR /app\nENV NODE_ENV=production\nCOPY --from=dung /app/dist ./dist\nCOPY nguon/ ./nguon/\nCOPY kiem-suc-khoe.js ./\n# exec-form: PID 1 la chinh node, nen SIGTERM toi thang no\nCMD [\"node\", \"nguon/app.js\"]\n\n# ══ kiem-suc-khoe.js  (anh KHONG co curl lan wget, nen phep kiem HTTP ben trong phai dung module http cua node)\nrequire('http').get({host:'127.0.0.1',port:8080,path:'/',timeout:2000},r=>process.exit(r.statusCode===200?0:1)).on('error',()=>process.exit(1));\n\n# ══ ngoai-anh/.env      chmod 600, KHONG vao kho ma, KHONG vao anh\nDATABASE_URL=\"postgres://ung_dung:MAT_KHAU_GIA_LAP@csdl:5432/ung_dung\"\nJWT_SECRET=\"KHOA_KY_GIA_LAP_KHONG_PHAI_THAT\"\n# ══ ngoai-anh/token_ci  chmod 600, chi di qua --mount=type=secret luc DUNG\nsk_ci_GIA_LAP_KHONG_PHAI_THAT",
          rubric: rubric([
            ["bi-mat",
              "The build secret travels through <code>--mount=type=secret</code> rather than an <code>ARG</code>, and the run-time credentials come from a file outside the image: measured, <code>docker history</code> matches the token zero times and <code>Config.Env</code> names neither the token nor the signing key.",
              "Bí mật lúc dựng đi qua <code>--mount=type=secret</code> chứ không phải một <code>ARG</code>, và thông tin xác thực lúc chạy tới từ một tệp NGOÀI ảnh: đo thật, <code>docker history</code> khớp token đúng KHÔNG lần và <code>Config.Env</code> không gọi tên token lẫn khoá ký.",
              0.5],
            ["dung-vs-chay",
              "The two moments are kept apart and behave as measured: changing the run-time variable and restarting changes the answer, while changing the build-time argument and restarting does not — only a rebuild does. A solution that reads the public URL from the environment at run time has removed the distinction the question is about.",
              "Hai thời điểm được giữ tách bạch và cư xử đúng như đo được: đổi biến LÚC CHẠY rồi khởi động lại thì câu trả lời đổi, còn đổi tham số LÚC DỰNG rồi khởi động lại thì KHÔNG — chỉ dựng lại mới đổi. Một lời giải đọc cái URL công khai từ môi trường lúc chạy là đã xoá mất đúng cái phân biệt mà câu hỏi này nói về.",
              0.5],
            ["tin-hieu",
              "<code>docker stop</code> returns in well under the grace period with a clean exit code and a shutdown line in the log — so PID 1 either is the application or forwards to it — and the compose grace period is longer than the application's own deadline rather than the 10-second default.",
              "<code>docker stop</code> trả về trong thời gian ngắn hơn hẳn khoảng ân hạn, với một mã thoát sạch và một dòng tắt máy trong nhật ký — tức PID 1 hoặc chính là ứng dụng hoặc biết chuyển tiếp tới nó — và khoảng ân hạn trong compose DÀI HƠN hạn chót của chính ứng dụng chứ không phải mặc định mười giây.",
              0.5],
            ["tu-choi",
              "The health check runs with a tool the image actually has and the container reaches <code>healthy</code>; the host-side credential files are mode 0600; and removing a required variable makes the container exit non-zero with a message naming it, rather than starting and answering 500 to everything.",
              "Chốt kiểm sức khoẻ chạy bằng một công cụ mà ảnh THẬT SỰ có và container đạt trạng thái <code>healthy</code>; hai tệp thông tin xác thực phía máy chủ để quyền 0600; và bỏ đi một biến bắt buộc thì container thoát ra khác 0 kèm một thông báo GỌI TÊN biến ấy, chứ không phải khởi động lên rồi trả 500 cho mọi thứ.",
              0.5],
          ]),
        }),

        /* ── Q4 ─────────────────────────────────────────────────── */
        codeQ({
          points: 2,
          language: "bash",
          khongChayDuoc: "lời giải là một bộ kiểm khói: nó cần một proxy đang chạy và một ứng dụng thật phía sau để mà gọi, nên nó không tự chứa trong bộ kiểm. Đã chạy thật trong container `debian:12` (bash 5.2.15, curl 7.88.1, nginx 1.22.1) và cho ra đủ năm kết cục: mã thoát 0 trên bản đủ tuyến, 8 trên một bản thiếu `/api/v1/bai` (404), 9 khi cửa trước trả `v3` trong lúc chờ `v7`, 6 với danh sách tuyến rỗng, và 5 sau 1 mili giây khi `curl` không có trong `PATH`",
          prompt: B(
            "<p><b>Q4 — A smoke test that catches the stale build, and one that cannot pass by accident (chapters 7 and 9).</b> Write <code>Q4/kiem-khoi.sh</code> and <code>Q4/tuyen-kiem-khoi.txt</code>. The script takes <code>&lt;cua-truoc&gt; &lt;ban-mong-doi&gt; [tep-danh-sach-tuyen]</code> and is meant to run as the last step of a deploy, with a non-zero exit triggering the rollback.</p><ul><li><b>The failure it exists for</b> is the partial or stale build: the process starts, the health check passes, and one router was never mounted. An unauthenticated GET returns <b>401 if the route exists</b> and <b>404 if it does not</b>, so every protected endpoint is testable without a single credential.</li><li><b>Check the checker first.</b> \"The check cannot run\" is a third outcome, not a variant of \"the check failed\", and it needs its own exit code. A readiness loop calling a tool that is not installed burns three seconds on every deploy, reports the application down while it is serving, and exits 0.</li><li><b>Refuse to pass trivially.</b> An empty route list must be an error, not a success — a suite that passes because it tested nothing is the most expensive kind.</li><li><b>Only list routes that answer a bare unauthenticated GET.</b> Put the rule in the list file itself: a POST-only or parameter-required route makes every deploy false-fail, and a check that cries wolf is commented out within a week.</li><li><b>Finish with the version.</b> The last check compares the version the <em>front door</em> serves against the release that was just deployed.</li><li><b>Emit one machine-readable line</b> that can be appended to the deploy log and compared later.</li></ul><p>Use distinct exit codes: <code>5</code> the check cannot run, <code>6</code> the route list is unusable, <code>8</code> a route is missing, <code>9</code> the front door serves the wrong version. And demonstrate each one — a check nobody has watched fail is a check nobody has tested.</p>",
            "<p><b>Câu 4 — Một bộ kiểm khói bắt được bản dựng cũ, và một bộ kiểm không thể ĐẠT một cách tình cờ (chương 7 và 9).</b> Hãy viết <code>Q4/kiem-khoi.sh</code> và <code>Q4/tuyen-kiem-khoi.txt</code>. Script nhận <code>&lt;cua-truoc&gt; &lt;ban-mong-doi&gt; [tep-danh-sach-tuyen]</code> và được dùng làm bước CUỐI của một lần deploy, với một mã thoát khác 0 kích hoạt cú lùi bản.</p><ul><li><b>Cú hỏng mà nó sinh ra để bắt</b> là bản dựng nửa vời hoặc bản dựng cũ: tiến trình khởi động lên, chốt kiểm sức khoẻ đạt, và một cái router chưa từng được gắn. Một lệnh GET không đăng nhập trả về <b>401 NẾU tuyến tồn tại</b> và <b>404 nếu không</b>, nên mọi endpoint có bảo vệ đều kiểm được mà không cần một thông tin đăng nhập nào.</li><li><b>Kiểm chính BỘ KIỂM trước.</b> \"Phép kiểm không chạy được\" là một kết cục THỨ BA, không phải một biến thể của \"phép kiểm hỏng\", và nó cần mã thoát riêng. Một vòng lặp sẵn sàng gọi một công cụ chưa cài sẽ đốt ba giây mỗi lần deploy, báo ứng dụng đã chết trong lúc nó đang phục vụ, rồi thoát ra 0.</li><li><b>Từ chối ĐẠT một cách tầm thường.</b> Một danh sách tuyến RỖNG phải là LỖI, không phải thành công — một bộ kiểm đạt vì nó chẳng kiểm gì cả là loại đắt tiền nhất.</li><li><b>Chỉ liệt kê những tuyến trả lời được một lệnh GET trần, không đăng nhập.</b> Hãy viết cái luật ấy vào chính tệp danh sách: một tuyến chỉ nhận POST hoặc đòi tham số đường dẫn sẽ làm MỌI lần deploy hỏng giả, và một bộ kiểm kêu oan thì một tuần sau bị chú thích đi.</li><li><b>Kết bằng PHIÊN BẢN.</b> Phép kiểm cuối so phiên bản mà CỬA TRƯỚC đang phục vụ với bản phát hành vừa deploy.</li><li><b>Nhả ra một dòng MÁY ĐỌC ĐƯỢC</b> để nối vào nhật ký deploy và đối chiếu về sau.</li></ul><p>Hãy dùng mã thoát riêng biệt: <code>5</code> phép kiểm không chạy được, <code>6</code> danh sách tuyến không dùng được, <code>8</code> có tuyến bị thiếu, <code>9</code> cửa trước phục vụ sai phiên bản. Và hãy chứng minh TỪNG cái — một phép kiểm chưa ai nhìn thấy nó hỏng là một phép kiểm chưa ai kiểm thử.</p>",
          ),
          starterCode: "# ── ĐỀ CHO SẴN — KHÔNG SỬA ─────────────────────────────────────────────\n# Q4/kiem-khoi.sh + tuyen-kiem-khoi.txt\n# Ung dung phuc vu: /health (200), /ban (200, tra ve ten phien ban),\n# /api/v1/bai (200), /api/v1/rieng (401 khi khong dang nhap).\n# Cua truoc trong bo do nghe la http://127.0.0.1:3500.\n# Cach tao mot ban THIEU tuyen de thu: xem khoi \"cach lam bai\" o dau de.\n\n# ── VIẾT LỜI GIẢI CỦA BẠN Ở ĐÂY ────────────────────────────────────────\n\n\n# ── ĐỀ CHO SẴN — KHÔNG SỬA ─────────────────────────────────────────────\n# Chạy đúng những câu lệnh trong khối \"kết quả mong đợi\" rồi đối chiếu\n# từng dòng một trước khi nộp.\n",
          expectedOutput: "$ bash kiem-khoi.sh http://127.0.0.1:3500 v3\n  ok  /health            -> 200\n  ok  /api/v1/bai        -> 200\n  ok  /api/v1/rieng      -> 401\nkiem-khoi ban=v3 dat=3 hong=0 luc=2026-09-09T21:57:54Z\n  ok  cua truoc xac nhan 'v3'\n  ma thoat: 0\n\n$ bash kiem-khoi.sh http://127.0.0.1:3500 v4      # ANH CU: mot tuyen 404\n  ok  /health            -> 200\n  x   /api/v1/bai        -> 404  (KHONG gan — anh cu hoac dung nua voi)\n  ok  /api/v1/rieng      -> 401\nkiem-khoi ban=v4 dat=2 hong=1 luc=2026-09-09T21:58:58Z\nx kiem khoi HONG: 1 tuyen\n  ma thoat: 8\n\n$ bash kiem-khoi.sh http://127.0.0.1:3500 v7      # phien ban khong khop\nkiem-khoi ban=v7 dat=3 hong=0 luc=2026-09-09T21:57:54Z\nx cua truoc tra 'v3', khong phai 'v7'\n  ma thoat: 9\n\n$ : > rong.txt ; bash kiem-khoi.sh http://127.0.0.1:3500 v3 rong.txt\nx danh sach tuyen RONG — bo kiem nay se dat MOT CACH TAM THUONG\n  ma thoat: 6\n\n$ env PATH=/tmp/bin-thieu /bin/bash kiem-khoi.sh http://127.0.0.1:3500 v4\nx bo kiem KHONG chay duoc: thieu 'curl'\n  ma thoat: 5  (1 ms — khong phai 3.073 ms roi bao nham la ung dung chet)",
          sampleSolution: "#!/bin/bash\n# kiem-khoi.sh <cua-truoc> <ban-mong-doi> [tep-danh-sach-tuyen]\n# Ma thoat:  0 dat · 5 bo kiem khong chay duoc · 6 danh sach tuyen rong\n#            8 co tuyen 404 (anh cu / dung nua voi) · 9 cua truoc sai phien ban\nset -euo pipefail\n\nCUA_TRUOC=\"${1:?dung: kiem-khoi.sh <cua-truoc> <ban-mong-doi> [tep-tuyen]}\"\nMONG_DOI=\"${2:?thieu ban mong doi}\"\nTEP_TUYEN=\"${3:-/srv/pe/tuyen-kiem-khoi.txt}\"\n\n# 0. KIEM CHINH BO KIEM TRUOC. \"Khong chay duoc\" la ket cuc THU BA va phai\n#    co ma thoat rieng — neu khong no lan vao \"kiem hong\" va bi lam ngo.\nfor lenh in curl date; do\n  command -v \"$lenh\" >/dev/null || { echo \"x bo kiem KHONG chay duoc: thieu '$lenh'\" >&2; exit 5; }\ndone\n[ -r \"$TEP_TUYEN\" ] || { echo \"x khong doc duoc danh sach tuyen '$TEP_TUYEN'\" >&2; exit 6; }\n\nTUYEN=()\nwhile read -r d; do\n  d=\"${d%%#*}\"; d=\"$(echo \"$d\" | tr -d '[:space:]')\"\n  [ -n \"$d\" ] && TUYEN+=(\"$d\")\ndone < \"$TEP_TUYEN\"\n[ \"${#TUYEN[@]}\" -gt 0 ] || { echo \"x danh sach tuyen RONG — bo kiem nay se dat MOT CACH TAM THUONG\" >&2; exit 6; }\n\ngoi() { curl -s -o /dev/null -w '%{http_code}' --max-time 3 \"$1\" || echo 000; }\n\nDAT=0; HONG=0\nfor R in \"${TUYEN[@]}\"; do\n  MA=$(goi \"$CUA_TRUOC$R\")\n  case \"$MA\" in\n    # 401 la DAT: tuyen co bao ve van TON TAI thi tra 401, KHONG ton tai thi\n    # tra 404 — nen kiem duoc moi tuyen can dang nhap ma khong can mot cai\n    # thong tin dang nhap nao.\n    200|401|403) printf '  ok  %-18s -> %s\\n' \"$R\" \"$MA\"; DAT=$((DAT+1)) ;;\n    404)         printf '  x   %-18s -> 404  (KHONG gan — anh cu hoac dung nua voi)\\n' \"$R\"; HONG=$((HONG+1)) ;;\n    *)           printf '  ?   %-18s -> %s  (khong ro)\\n' \"$R\" \"$MA\"; HONG=$((HONG+1)) ;;\n  esac\ndone\n\n# Mot dong may doc duoc, de gan vao nhat ky deploy va doi chieu ve sau.\nprintf 'kiem-khoi ban=%s dat=%d hong=%d luc=%s\\n' \\\n  \"$MONG_DOI\" \"$DAT\" \"$HONG\" \"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"\n\n[ \"$HONG\" -eq 0 ] || { echo \"x kiem khoi HONG: $HONG tuyen\" >&2; exit 8; }\n\n# Cuoi cung, va day moi la bang chung: PHIEN BAN nao dang duoc phuc vu?\nTHAY=$(curl -s --max-time 3 \"$CUA_TRUOC/ban\" | tr -d '\\n')\n[ \"$THAY\" = \"$MONG_DOI\" ] || { echo \"x cua truoc tra '$THAY', khong phai '$MONG_DOI'\" >&2; exit 9; }\necho \"  ok  cua truoc xac nhan '$THAY'\"\n\n# ══ tuyen-kiem-khoi.txt\n# Chi liet ke tuyen tra ve KHAC 404 voi mot lenh GET tran, KHONG dang nhap.\n# KHONG them tuyen chi nhan POST hay doi tham so duong dan — moi lan deploy\n# se hong gia, va mot bo kiem keu oan thi mot tuan sau bi chu thich di.\n/health\n/api/v1/bai\n/api/v1/rieng      # 401 = da gan, dung: khong can thong tin dang nhap",
          rubric: rubric([
            ["bo-kiem",
              "The script verifies its own tools before doing anything and exits with a dedicated code naming the missing one — measured at 1 ms rather than three seconds of retrying followed by a false report — and an empty or unreadable route list is an error rather than a pass.",
              "Script kiểm chính công cụ của nó TRƯỚC khi làm bất cứ việc gì rồi thoát bằng một mã riêng có GỌI TÊN công cụ bị thiếu — đo được 1 mili giây thay vì ba giây thử lại rồi báo cáo sai — và một danh sách tuyến rỗng hay không đọc được là LỖI chứ không phải ĐẠT.",
              0.5],
            ["404",
              "401 and 200 are treated as pass and 404 as a failed deploy with its own exit code, so a protected route is verified without credentials; the route list file carries the rule that only bare-GET routes may be listed, and no POST-only or parameter-required route is in it.",
              "401 và 200 được coi là ĐẠT còn 404 là một lần deploy HỎNG kèm mã thoát riêng, nên một tuyến có bảo vệ vẫn kiểm được mà không cần thông tin đăng nhập; tệp danh sách tuyến mang theo cái luật rằng chỉ được liệt kê những tuyến trả lời GET trần, và trong đó không có tuyến chỉ nhận POST hay đòi tham số nào.",
              0.5],
            ["phien-ban",
              "The final check compares the version served through the user-facing address against the release requested, with its own exit code — measured, it caught a front door still serving <code>v3</code> while <code>v7</code> was expected, on a run where every route had already passed.",
              "Phép kiểm cuối so PHIÊN BẢN được phục vụ qua địa chỉ hướng người dùng với bản phát hành đã yêu cầu, kèm mã thoát riêng — đo thật, nó bắt được một cửa trước vẫn đang phục vụ <code>v3</code> trong khi đang chờ <code>v7</code>, ở một lượt chạy mà mọi tuyến đều đã ĐẠT.",
              0.5],
            ["bang-chung",
              "It prints one machine-readable summary line carrying the release, the pass and fail counts and a timestamp, and every exit code claimed in the answer is demonstrated by actually causing that failure rather than asserted.",
              "Nó in ra MỘT dòng tóm tắt máy đọc được mang theo tên bản phát hành, số tuyến đạt, số tuyến hỏng và một dấu thời gian, và mọi mã thoát mà lời giải tuyên bố đều được CHỨNG MINH bằng cách gây ra đúng cú hỏng ấy chứ không phải khẳng định suông.",
              0.5],
          ]),
        }),

        /* ── Q5 ─────────────────────────────────────────────────── */
        codeQ({
          points: 2,
          language: "bash",
          khongChayDuoc: "lời giải phải đọc `/proc/<pid>` của một tiến trình thật, hỏi một proxy đang chạy và một ứng dụng đang chạy, nên nó không tự chứa trong bộ kiểm. Đã chạy thật trong container `debian:12` (bash 5.2.15, iproute2 `ss`, nginx 1.22.1 có `proxy_cache`) và cho ra đủ sáu kết cục: 0 khi mọi thứ khớp, 21 con trỏ chưa đổi, 22 con trỏ mới với tiến trình cũ, 23 ứng dụng mới mà cửa trước vẫn cũ vì bộ đệm, 24 không ai nghe cổng upstream, và 5 khi thiếu `curl`",
          prompt: B(
            "<p><b>Q5 — \"The deploy said XONG and production is still on the old version\" (chapters 6, 7 and 11).</b> Write <code>Q5/chan-doan.sh</code>, taking the release that was supposedly just deployed. It <b>changes nothing</b>: it interrogates the four sources of truth, prints them, and ends with one verdict and a distinct exit code.</p><ul><li><b>The four facts</b>, and they can disagree with each other: what the release pointer resolves to, which upstream the proxy is being told to use, which process is holding that port and what it answers when asked directly, and what the <em>front door</em> returns.</li><li><b>Four verdicts.</b> <code>21</code> the pointer never moved — the deploy exited before the swap, so read the numbered steps in the deploy log. <code>22</code> the pointer is new and the process is old — nothing was restarted, because a running process does not follow a symlink that changes. <code>23</code> both are new and the front door is old — something is caching, or a proxy was never reloaded. <code>24</code> nothing is listening on the upstream port at all.</li><li><b>Do not identify the running release by guessing.</b> Read what the process actually holds, and ask the application itself which version it is rather than parsing a process name.</li><li><b>A diagnostic script must survive not finding things.</b> Under <code>set -o pipefail</code> a pipeline containing a <code>grep</code> that matches nothing aborts the script with exit 1 — in a tool whose whole job is to report absence, that is exactly the wrong behaviour, and it will overwrite the verdict you meant to return.</li><li>Print the last line of the most recent deploy log too: a deploy that stopped at step 3 of 5 says so.</li></ul><p>Reserve <code>5</code> for \"this script cannot run\", and <code>0</code> for everything agreeing. Demonstrate all of them by producing each state on purpose.</p>",
            "<p><b>Câu 5 — \"Lần deploy báo XONG mà production vẫn đang chạy bản cũ\" (chương 6, 7 và 11).</b> Hãy viết <code>Q5/chan-doan.sh</code>, nhận vào tên bản phát hành mà người ta tưởng là vừa deploy xong. Nó <b>KHÔNG sửa gì cả</b>: nó tra hỏi bốn nguồn sự thật, in chúng ra, và kết bằng MỘT phán quyết cùng một mã thoát riêng.</p><ul><li><b>Bốn sự thật</b>, và chúng có thể bất đồng với nhau: con trỏ bản phát hành phân giải ra đâu, proxy đang ĐƯỢC BẢO là gửi vào upstream nào, tiến trình nào đang giữ cổng đó và nó trả lời gì khi bị hỏi thẳng, và CỬA TRƯỚC trả về cái gì.</li><li><b>Bốn phán quyết.</b> <code>21</code> con trỏ chưa hề dời — lần deploy thoát ra trước bước tráo, nên hãy đọc các bước đánh số trong nhật ký deploy. <code>22</code> con trỏ mới mà tiến trình cũ — chẳng ai khởi động lại, vì một tiến trình đang chạy không đi theo một symlink bị đổi. <code>23</code> cả hai đều mới mà cửa trước thì cũ — có thứ gì đó đang đệm, hoặc một proxy chưa từng được nạp lại. <code>24</code> chẳng ai đang lắng nghe cổng upstream cả.</li><li><b>Đừng ĐOÁN xem bản phát hành nào đang chạy.</b> Hãy đọc thứ mà tiến trình THẬT SỰ đang giữ, và hỏi chính ứng dụng xem nó là phiên bản nào thay vì phân tích tên tiến trình.</li><li><b>Một script chẩn đoán phải sống sót khi KHÔNG tìm thấy thứ gì đó.</b> Dưới <code>set -o pipefail</code>, một ống dẫn có chứa một <code>grep</code> không khớp gì sẽ giết cả script với mã thoát 1 — ở một công cụ mà toàn bộ công việc là BÁO CÁO SỰ VẮNG MẶT thì đó đúng là hành vi sai, và nó sẽ ghi đè lên đúng cái phán quyết bạn định trả về.</li><li>Hãy in ra cả dòng cuối của nhật ký deploy gần nhất: một lần deploy dừng ở bước 3 trên 5 thì tự nó nói ra.</li></ul><p>Hãy dành <code>5</code> cho \"script này không chạy được\", và <code>0</code> cho trường hợp mọi thứ khớp nhau. Hãy chứng minh tất cả bằng cách tạo ra từng trạng thái một cách có chủ đích.</p>",
          ),
          starterCode: "# ── ĐỀ CHO SẴN — KHÔNG SỬA ─────────────────────────────────────────────\n# Q5/chan-doan.sh\n# Bo do nghe: con tro /srv/pe/hien-tai, upstream o /srv/pe/nx/upstream.conf,\n# cua truoc http://127.0.0.1:3500, nhat ky deploy trong /srv/pe/nhat-ky/.\n# Ung dung tra ve ten phien ban cua chinh no o tuyen /ban.\n# Script nay chi DOC. No khong duoc sua, khoi dong lai hay xoa bat cu thu gi.\n\n# ── VIẾT LỜI GIẢI CỦA BẠN Ở ĐÂY ────────────────────────────────────────\n\n\n# ── ĐỀ CHO SẴN — KHÔNG SỬA ─────────────────────────────────────────────\n# Chạy đúng những câu lệnh trong khối \"kết quả mong đợi\" rồi đối chiếu\n# từng dòng một trước khi nộp.\n",
          expectedOutput: "$ bash chan-doan.sh v3                       # moi thu khop\n  mong doi            : v3\n  con tro hien-tai    : v3\n  upstream tro vao    : cong 3501\n  tien trinh giu cong : pid 12920  cwd=/srv/pe\n  doi so cua no       : python3 /srv/pe/hien-tai/app.py\n  cua SAU tra         : v3\n  cua TRUOC tra       : v3\n  nhat ky gan nhat    : /srv/pe/nhat-ky/20260909-215702-v3.log\n    dong cuoi         : 21:57:04 XONG — da SIGTERM ban cu tren 3501.\n=> KHOP: con tro, tien trinh va cua truoc deu la 'v3'.\n  ma thoat: 0\n\n$ bash chan-doan.sh v9                       # con tro CHUA doi\n=> CON TRO CHUA DOI. Lan deploy thoat TRUOC buoc trao — doc nhat ky xem buoc danh so nao la buoc cuoi.\n  ma thoat: 21\n\n$ bash chan-doan.sh v2                       # con tro moi, tien trinh cu\n  con tro hien-tai    : v2\n  cua SAU tra         : v3\n=> CON TRO MOI, TIEN TRINH CU. Mot tien trinh dang chay KHONG di theo symlink doi — phai giet no roi khoi dong lai tu '/srv/pe/hien-tai'.\n  ma thoat: 22\n\n$ bash chan-doan.sh v1                       # ung dung moi, cua truoc cu\n  con tro hien-tai    : v1\n  cua SAU tra         : v1\n  cua TRUOC tra       : v2\n=> UNG DUNG DA MOI, CUA TRUOC VAN CU. Mot bo dem (hoac mot proxy chua nap lai) dang dung giua. Don bo dem roi nap lai, va kiem lai QUA CUA TRUOC.\n  ma thoat: 23\n  --- don bo dem roi nap lai, roi kiem lai ---\n=> KHOP: con tro, tien trinh va cua truoc deu la 'v1'.\n  ma thoat: 0\n\n$ bash chan-doan.sh v2                       # khong ai nghe cong upstream\n  tien trinh giu cong : pid <khong co>  cwd=–\n  cua SAU tra         : <khong tra loi>\n  cua TRUOC tra       : <html><head><title>502 Bad Gateway</title>…\n=> KHONG AI NGHE cong 3501: ban moi khong len duoc, hoac ban cu da bi giet truoc khi ban moi san sang.\n  ma thoat: 24\n\n$ env PATH=/tmp/bin-thieu /bin/bash chan-doan.sh v2\nx chan doan KHONG chay duoc: thieu 'curl'\n  ma thoat: 5",
          sampleSolution: "#!/bin/bash\n# chan-doan.sh <ban-vua-deploy>\n# \"Deploy bao XONG ma production van chay ban cu\" — bon nguyen nhan, bon\n# ma thoat. Script nay KHONG SUA gi ca: no chi hoi bon nguon su that.\n#   0  moi thu khop\n#  21  con tro CHUA doi          — deploy thoat truoc buoc trao\n#  22  con tro moi, TIEN TRINH cu — khong ai khoi dong lai (symlink != tien trinh)\n#  23  ca hai moi, CUA TRUOC cu   — bo dem, hoac proxy con tro upstream cu\n#  24  khong ai nghe cong upstream\nset -euo pipefail\n\nGOC=/srv/pe\nCUA_TRUOC=http://127.0.0.1:3500\nMONG_DOI=\"${1:?dung: chan-doan.sh <ban-vua-deploy>}\"\n\nfor lenh in curl ss readlink; do\n  command -v \"$lenh\" >/dev/null || { echo \"x chan doan KHONG chay duoc: thieu '$lenh'\" >&2; exit 5; }\ndone\n\n# 1. Con tro ban phat hanh dang tro vao dau?\nTRO=$(basename \"$(readlink -f \"$GOC/hien-tai\" 2>/dev/null || echo khong-co)\") || TRO=\"khong-co\"\n# 2. Proxy dang duoc bao la gui di dau?\n# ⚠ Voi `set -o pipefail`, mot ong dan co grep KHONG TIM THAY se lam ca script\n#   chet voi ma 1 — dung cai ta muon o mot script CHAN DOAN, noi \"khong tim\n#   thay\" chinh la mot cau tra loi. Nen moi phep do deu ket bang `|| true`.\nCONG=$(grep -oE '127\\.0\\.0\\.1:[0-9]+' \"$GOC/nx/upstream.conf\" | cut -d: -f2 | head -1 || true)\n# 3. Tien trinh nao dang giu cong do, va no chay TU thu muc nao?\nPID=$(ss -ltnp 2>/dev/null | grep \":$CONG \" | grep -o 'pid=[0-9]*' | cut -d= -f2 | head -1 || true)\nif [ -n \"$PID\" ]; then\n  # Duong dan THAT ma tien trinh da MO, giai phan symlink — khong doan theo ten\n  # tien trinh va khong gia dinh doi so trong ra sao.\n  TEP=$(readlink -f \"/proc/$PID/cwd\" 2>/dev/null || echo '?')\n  DOI_SO=$(tr '\\0' ' ' < \"/proc/$PID/cmdline\" 2>/dev/null || echo '?')\n  # Nguon su that ve \"tien trinh nay LA ban nao\" la chinh no, qua cua SAU.\n  SAU=$(curl -s --max-time 3 \"http://127.0.0.1:$CONG/ban\" | tr -d '\\n' || true)\nelse\n  TEP=\"\"; DOI_SO=\"\"; SAU=\"\"\nfi\n# 4. Nguoi dung THAT SU dang nhan gi?\nTRUOC=$(curl -s --max-time 3 \"$CUA_TRUOC/ban\" | tr -d '\\n' || true)\n# 5. Dong cuoi cua nhat ky deploy gan nhat\nNK=$(ls -1t \"$GOC\"/nhat-ky/*.log 2>/dev/null | head -1 || true) || NK=\"\"\n\nprintf '  mong doi            : %s\\n' \"$MONG_DOI\"\nprintf '  con tro hien-tai    : %s\\n' \"$TRO\"\nprintf '  upstream tro vao    : cong %s\\n' \"$CONG\"\nprintf '  tien trinh giu cong : pid %s  cwd=%s\\n' \"${PID:-<khong co>}\" \"${TEP:-–}\"\nprintf '  doi so cua no       : %s\\n' \"${DOI_SO:-–}\"\nprintf '  cua SAU tra         : %s\\n' \"${SAU:-<khong tra loi>}\"\nprintf '  cua TRUOC tra       : %s\\n' \"${TRUOC:-<khong tra loi>}\"\nprintf '  nhat ky gan nhat    : %s\\n' \"${NK:-<khong co>}\"\n[ -n \"$NK\" ] && printf '    dong cuoi         : %s\\n' \"$(tail -1 \"$NK\")\"\n\nif [ -z \"$PID\" ]; then\n  echo \"=> KHONG AI NGHE cong $CONG: ban moi khong len duoc, hoac ban cu da bi giet truoc khi ban moi san sang.\" >&2\n  exit 24\nfi\nif [ \"$TRO\" != \"$MONG_DOI\" ]; then\n  echo \"=> CON TRO CHUA DOI. Lan deploy thoat TRUOC buoc trao — doc nhat ky xem buoc danh so nao la buoc cuoi.\" >&2\n  exit 21\nfi\nif [ \"$SAU\" != \"$MONG_DOI\" ]; then\n  echo \"=> CON TRO MOI, TIEN TRINH CU. Mot tien trinh dang chay KHONG di theo symlink doi — phai giet no roi khoi dong lai tu '$GOC/hien-tai'.\" >&2\n  exit 22\nfi\nif [ \"$TRUOC\" != \"$MONG_DOI\" ]; then\n  echo \"=> UNG DUNG DA MOI, CUA TRUOC VAN CU. Mot bo dem (hoac mot proxy chua nap lai) dang dung giua. Don bo dem roi nap lai, va kiem lai QUA CUA TRUOC.\" >&2\n  exit 23\nfi\necho \"=> KHOP: con tro, tien trinh va cua truoc deu la '$MONG_DOI'.\"",
          rubric: rubric([
            ["bon-nguon",
              "All four facts are read and printed — the resolved pointer, the upstream the proxy is configured for, the process holding that port together with what it answers directly, and what the front door returns — so the operator can see the disagreement rather than only the conclusion.",
              "Cả bốn sự thật đều được đọc và IN RA — con trỏ đã phân giải, cái upstream mà proxy được cấu hình cho, tiến trình đang giữ cổng đó cùng với thứ nó trả lời khi bị hỏi thẳng, và thứ cửa trước trả về — để người vận hành NHÌN THẤY sự bất đồng chứ không chỉ nhận một kết luận.",
              0.5],
            ["phan-quyet",
              "The four verdicts map to the right states and to distinct exit codes, and each is demonstrated by producing that state deliberately: a pointer left behind, a pointer moved without a restart, a cache in front of an updated application, and an upstream port with nothing on it.",
              "Bốn phán quyết ánh xạ đúng vào bốn trạng thái và vào các mã thoát riêng biệt, và mỗi cái được chứng minh bằng cách CỐ Ý tạo ra trạng thái ấy: một con trỏ bị bỏ lại, một con trỏ đã dời mà không khởi động lại, một bộ đệm đứng trước một ứng dụng đã cập nhật, và một cổng upstream chẳng có ai trên đó.",
              0.5],
            ["khong-doan",
              "The running release is identified from what the process actually holds and from the application's own answer, not from a process name or a hard-coded path assumption — a solution that greps the command line for a release directory breaks the moment the process is started through the symlink instead.",
              "Bản phát hành đang chạy được nhận diện từ thứ mà tiến trình THẬT SỰ đang giữ và từ chính câu trả lời của ứng dụng, chứ không phải từ một cái tên tiến trình hay một giả định đường dẫn cứng — một lời giải đi grep dòng lệnh để tìm thư mục bản phát hành sẽ vỡ ngay khi tiến trình được khởi động qua symlink.",
              0.5],
            ["ben-bi",
              "The script is read-only and survives absence: every probe that can find nothing is written so a non-matching <code>grep</code> cannot abort it, and the verdict and exit code it returns are its own rather than a 1 leaked from a pipeline. The \"cannot run\" case has its own code too.",
              "Script chỉ ĐỌC và sống sót được với sự vắng mặt: mọi phép dò có thể không tìm thấy gì đều được viết sao cho một <code>grep</code> không khớp không giết được nó, và phán quyết cùng mã thoát nó trả về là của CHÍNH NÓ chứ không phải một cú 1 rò ra từ một ống dẫn. Ca \"không chạy được\" cũng có mã riêng.",
              0.5],
          ]),
        }),

      ],
    },
  ],
};
