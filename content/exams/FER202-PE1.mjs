export default {
  "course": {
    "courseCode": "FER202"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE1-SP26B5PE",
      "title": "FER202 — Practical Exam Đề 1 (SP26 - B5 - PE)|||FER202 — Đề thi thực hành số 1 (SP26 - B5 - PE)",
      "description": "React PE (WRITE): build components against a REST API, AI-graded.|||Đề PE React (viết): dựng component theo REST API, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>React / Front-end Development</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>React / Front-end Development</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 2,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p>FER202 Practical Exam (React). A REST API is served by <b>json-server</b> from <code>database.json</code> on <code>http://localhost:9999</code> (started with <code>json-server --watch database.json --port 9999</code>), while the React app runs at <code>http://localhost:3000</code> (<code>npm start</code>).</p><p>The <code>database.json</code> is not shipped with this bank, so the schema below is reconstructed from the requirements and demo screenshots. Expected collections:</p><ul><li><b>departments</b>: <code>{ id, depName, managerId }</code> — managerId points to an employee id.</li><li><b>employees</b>: <code>{ id, fullname, gender, birthdate, supervisorId, depId, empSalary, dependents: [] }</code> — <code>supervisorId === null</code> marks a Manager; <code>dependents</code> is a nested array holding <code>{ id, name, birthdate, relationship }</code>.</li><li><b>projects</b>: <code>{ id, projectName }</code>.</li><li><b>works</b>: <code>{ id, employeeId, projectId, workHours }</code> — join table between employees and projects.</li></ul><p>Salary rule: <code>hourly = empSalary / 176</code>, <code>salary = hourly * workHours</code>. Money is shown with Vietnamese thousand separators (dots), e.g. <code>4.261.363</code>.</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p>Bài thi thực hành FER202 (React). API REST do <b>json-server</b> phục vụ từ <code>database.json</code> tại <code>http://localhost:9999</code> (chạy bằng <code>json-server --watch database.json --port 9999</code>), còn ứng dụng React chạy ở <code>http://localhost:3000</code> (<code>npm start</code>).</p><p>File <code>database.json</code> không đi kèm ngân hàng đề này, nên cấu trúc dưới đây được dựng lại từ yêu cầu và ảnh demo. Các bảng dự kiến:</p><ul><li><b>departments</b>: <code>{ id, depName, managerId }</code> — managerId trỏ tới id của một employee.</li><li><b>employees</b>: <code>{ id, fullname, gender, birthdate, supervisorId, depId, empSalary, dependents: [] }</code> — <code>supervisorId === null</code> nghĩa là Manager; <code>dependents</code> là mảng lồng chứa <code>{ id, name, birthdate, relationship }</code>.</li><li><b>projects</b>: <code>{ id, projectName }</code>.</li><li><b>works</b>: <code>{ id, employeeId, projectId, workHours }</code> — bảng nối giữa employees và projects.</li></ul><p>Công thức lương: <code>hourly = empSalary / 176</code>, <code>salary = hourly * workHours</code>. Tiền hiển thị theo định dạng Việt Nam (dấu chấm ngăn hàng nghìn), ví dụ <code>4.261.363</code>.</p></div></div><div class=\"ml-en\"><p><b>Question 1 (2 points)</b></p><p><b>Question 1 (2 points) - Department List Page.</b> Create a React page (routed at <code>http://localhost:3000</code>, 0 points if the URL is incorrect) that displays the list of departments retrieved from the REST API.</p><ul><li>Fetch data from the API.</li><li>Display the departments in a table: <code>| DepName | Manager | Employees |</code>.</li><li>Show the manager's full name, obtained from <b>employees</b> via <code>managerId</code>.</li><li>Provide a <b>View</b> button to show that department's employees.</li><li>When <b>View</b> is clicked, filter and display employees by <code>depId</code> (client-side filtering or API call — Question 2).</li></ul><p><i>Demo:</i> Title \"COMPANY MANAGEMENT\", heading \"Department List\", a 3-column table (Human Resources / An Nguyen / [View]; IT / Binh Tran / [View]; Finance / Khanh Do / [View]) with blue View buttons.</p></div><div class=\"ml-vi\"><p><b>Câu 1 (2 điểm)</b></p><p><b>Câu 1 (2 điểm) - Trang danh sách phòng ban.</b> Tạo một trang React (định tuyến tại <code>http://localhost:3000</code>, 0 điểm nếu sai URL) hiển thị danh sách phòng ban lấy từ REST API.</p><ul><li>Lấy dữ liệu từ API.</li><li>Hiển thị phòng ban dạng bảng: <code>| DepName | Manager | Employees |</code>.</li><li>Hiển thị họ tên đầy đủ của manager, lấy từ bảng <b>employees</b> qua <code>managerId</code>.</li><li>Có nút <b>View</b> để xem nhân viên của phòng ban đó.</li><li>Khi bấm <b>View</b>, lọc và hiển thị nhân viên theo <code>depId</code> (lọc phía client hoặc gọi API — Câu 2).</li></ul><p><i>Demo:</i> Tiêu đề \"COMPANY MANAGEMENT\", đầu mục \"Department List\", bảng 3 cột (Human Resources / An Nguyen / [View]; IT / Binh Tran / [View]; Finance / Khanh Do / [View]) với nút View màu xanh.</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Fetch both <code>departments</code> and <code>employees</code>; resolve each department's manager name by looking up <code>managerId</code> inside the employees list. The <b>View</b> button lifts the chosen <code>depId</code> to a parent so Question 2 can filter on it.</p><pre><code>import { useEffect, useState } from \"react\";\n\nconst API = \"http://localhost:9999\";\n\nfunction DepartmentList({ onView }) {\n  const [departments, setDepartments] = useState([]);\n  const [employees, setEmployees] = useState([]);\n\n  useEffect(() =&gt; {\n    fetch(`${API}/departments`).then(r =&gt; r.json()).then(setDepartments);\n    fetch(`${API}/employees`).then(r =&gt; r.json()).then(setEmployees);\n  }, []);\n\n  const managerName = (managerId) =&gt; {\n    const m = employees.find(e =&gt; e.id === managerId);\n    return m ? m.fullname : \"N/A\";\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;h1&gt;COMPANY MANAGEMENT&lt;/h1&gt;\n      &lt;h2&gt;Department List&lt;/h2&gt;\n      &lt;table border=\"1\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;&lt;th&gt;DepName&lt;/th&gt;&lt;th&gt;Manager&lt;/th&gt;&lt;th&gt;Employees&lt;/th&gt;&lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {departments.map(d =&gt; (\n            &lt;tr key={d.id}&gt;\n              &lt;td&gt;{d.depName}&lt;/td&gt;\n              &lt;td&gt;{managerName(d.managerId)}&lt;/td&gt;\n              &lt;td&gt;\n                &lt;button onClick={() =&gt; onView(d.id)}&gt;View&lt;/button&gt;\n              &lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}\n\nexport default DepartmentList;</code></pre><p><b>Key points:</b> (1) two <code>fetch</code> calls in <code>useEffect</code> with <code>[]</code> so they run once; (2) manager name = <code>employees.find(e =&gt; e.id === managerId)</code>; (3) <code>onView(d.id)</code> passes <code>depId</code> upward to drive Question 2.</p></div><div class=\"ml-vi\"><p>Lấy cả <code>departments</code> và <code>employees</code>; tìm tên manager của mỗi phòng ban bằng cách tra <code>managerId</code> trong danh sách employees. Nút <b>View</b> đẩy <code>depId</code> đã chọn lên component cha để Câu 2 lọc.</p><pre><code>import { useEffect, useState } from \"react\";\n\nconst API = \"http://localhost:9999\";\n\nfunction DepartmentList({ onView }) {\n  const [departments, setDepartments] = useState([]);\n  const [employees, setEmployees] = useState([]);\n\n  useEffect(() =&gt; {\n    fetch(`${API}/departments`).then(r =&gt; r.json()).then(setDepartments);\n    fetch(`${API}/employees`).then(r =&gt; r.json()).then(setEmployees);\n  }, []);\n\n  const managerName = (managerId) =&gt; {\n    const m = employees.find(e =&gt; e.id === managerId);\n    return m ? m.fullname : \"N/A\";\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;h1&gt;COMPANY MANAGEMENT&lt;/h1&gt;\n      &lt;h2&gt;Department List&lt;/h2&gt;\n      &lt;table border=\"1\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;&lt;th&gt;DepName&lt;/th&gt;&lt;th&gt;Manager&lt;/th&gt;&lt;th&gt;Employees&lt;/th&gt;&lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {departments.map(d =&gt; (\n            &lt;tr key={d.id}&gt;\n              &lt;td&gt;{d.depName}&lt;/td&gt;\n              &lt;td&gt;{managerName(d.managerId)}&lt;/td&gt;\n              &lt;td&gt;\n                &lt;button onClick={() =&gt; onView(d.id)}&gt;View&lt;/button&gt;\n              &lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}\n\nexport default DepartmentList;</code></pre><p><b>Điểm mấu chốt:</b> (1) hai lời gọi <code>fetch</code> trong <code>useEffect</code> với <code>[]</code> nên chỉ chạy một lần; (2) tên manager = <code>employees.find(e =&gt; e.id === managerId)</code>; (3) <code>onView(d.id)</code> đẩy <code>depId</code> lên cha để phục vụ Câu 2.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Fetch departments and employees from the API in useEffect and route the page at http://localhost:3000|||Lấy departments và employees từ API trong useEffect và trang chạy đúng http://localhost:3000",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Render the 3-column table (DepName, Manager full name resolved via managerId, View button)|||Hiển thị bảng 3 cột (DepName, họ tên Manager tra qua managerId, nút View)",
              "weight": 1,
              "maxScore": 1
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE1.png"
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2 (4 points)</b></p><p><b>Question 2 (4 points) - Employee &amp; Project Information.</b> Show the list of employees of a department together with their project work info.</p><ul><li>Fetch data from the API.</li><li>Render the employee list using an <b>Accordion</b> component.</li><li>For each employee show: <b>EmployeeId, Fullname, Gender, Birthdate</b>. If <code>supervisorId === null</code>, display <b>Manager</b>.</li><li>For each employee show the projects worked on: <code>| WorkId | Project Name | Work Hours | Salary |</code>.</li><li>Salary formula: <code>hourly = empSalary / 176</code>, <code>salary = hourly * workHours</code>.</li><li>Show the total across all projects: <code>Projects Salary: XXX VND</code>.</li></ul><p><i>Demo:</i> \"#1 - Tran Binh - Manager\" open, showing EmployeeId: 2, Gender: Male, BirthDate: 1983-03-12, a \"View Dependents\" button, and a \"Work on projects\" table with rows 2 / Website Development / 30 / 4.261.363 and 6 / Accounting Software / 20 / 2.840.909, then a highlighted \"Projects Salary: 7.102.272 VND\". Other items (#2 Le Chi, #3 Pham Dung, #4 Vo Hanh) collapsed.</p></div><div class=\"ml-vi\"><p><b>Câu 2 (4 điểm)</b></p><p><b>Câu 2 (4 điểm) - Thông tin nhân viên &amp; dự án.</b> Hiển thị danh sách nhân viên của một phòng ban kèm thông tin làm dự án.</p><ul><li>Lấy dữ liệu từ API.</li><li>Hiển thị danh sách nhân viên bằng component <b>Accordion</b>.</li><li>Mỗi nhân viên hiển thị: <b>EmployeeId, Fullname, Gender, Birthdate</b>. Nếu <code>supervisorId === null</code> thì hiển thị <b>Manager</b>.</li><li>Mỗi nhân viên hiển thị danh sách dự án đã làm: <code>| WorkId | Project Name | Work Hours | Salary |</code>.</li><li>Công thức lương: <code>hourly = empSalary / 176</code>, <code>salary = hourly * workHours</code>.</li><li>Hiển thị tổng lương tất cả dự án: <code>Projects Salary: XXX VND</code>.</li></ul><p><i>Demo:</i> \"#1 - Tran Binh - Manager\" đang mở, hiển thị EmployeeId: 2, Gender: Male, BirthDate: 1983-03-12, nút \"View Dependents\", và bảng \"Work on projects\" gồm 2 / Website Development / 30 / 4.261.363 và 6 / Accounting Software / 20 / 2.840.909, rồi dòng \"Projects Salary: 7.102.272 VND\" được tô nổi bật. Các mục khác (#2 Le Chi, #3 Pham Dung, #4 Vo Hanh) đang thu gọn.</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Filter employees by the <code>depId</code> chosen in Question 1, render them in a react-bootstrap <code>Accordion</code>. For each employee, join <code>works</code> by <code>employeeId</code>, look up the project name, and compute each salary with <code>Math.floor(empSalary / 176 * workHours)</code> — the floor makes 30h at 25,000,000 give 4.261.363 exactly. Format money with <code>toLocaleString(\"vi-VN\")</code>.</p><pre><code>import { useEffect, useState } from \"react\";\nimport { Accordion, Table, Button } from \"react-bootstrap\";\n\nconst API = \"http://localhost:9999\";\n\nfunction EmployeeList({ depId, onViewDependents }) {\n  const [employees, setEmployees] = useState([]);\n  const [projects, setProjects] = useState([]);\n  const [works, setWorks] = useState([]);\n\n  useEffect(() =&gt; {\n    fetch(`${API}/employees`).then(r =&gt; r.json()).then(setEmployees);\n    fetch(`${API}/projects`).then(r =&gt; r.json()).then(setProjects);\n    fetch(`${API}/works`).then(r =&gt; r.json()).then(setWorks);\n  }, []);\n\n  const list = depId ? employees.filter(e =&gt; e.depId === depId) : employees;\n  const projectName = (pid) =&gt; {\n    const p = projects.find(x =&gt; x.id === pid);\n    return p ? p.projectName : \"\";\n  };\n  const salaryOf = (emp, w) =&gt; Math.floor((emp.empSalary / 176) * w.workHours);\n\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;Employee List&lt;/h2&gt;\n      &lt;Accordion&gt;\n        {list.map((emp, idx) =&gt; {\n          const ws = works.filter(w =&gt; w.employeeId === emp.id);\n          const total = ws.reduce((s, w) =&gt; s + salaryOf(emp, w), 0);\n          return (\n            &lt;Accordion.Item eventKey={String(idx)} key={emp.id}&gt;\n              &lt;Accordion.Header&gt;\n                #{idx + 1} - {emp.fullname}\n                {emp.supervisorId === null ? \" - Manager\" : \"\"}\n              &lt;/Accordion.Header&gt;\n              &lt;Accordion.Body&gt;\n                &lt;ul&gt;\n                  &lt;li&gt;EmployeeId: {emp.id}&lt;/li&gt;\n                  &lt;li&gt;Gender: {emp.gender}&lt;/li&gt;\n                  &lt;li&gt;BirthDate: {emp.birthdate}&lt;/li&gt;\n                &lt;/ul&gt;\n                &lt;Button onClick={() =&gt; onViewDependents(emp)}&gt;View Dependents&lt;/Button&gt;\n                &lt;h5&gt;Work on projects&lt;/h5&gt;\n                &lt;Table bordered&gt;\n                  &lt;thead&gt;\n                    &lt;tr&gt;\n                      &lt;th&gt;WorkId&lt;/th&gt;&lt;th&gt;Project name&lt;/th&gt;\n                      &lt;th&gt;Work hours&lt;/th&gt;&lt;th&gt;Salary (VND)&lt;/th&gt;\n                    &lt;/tr&gt;\n                  &lt;/thead&gt;\n                  &lt;tbody&gt;\n                    {ws.map(w =&gt; (\n                      &lt;tr key={w.id}&gt;\n                        &lt;td&gt;{w.id}&lt;/td&gt;\n                        &lt;td&gt;{projectName(w.projectId)}&lt;/td&gt;\n                        &lt;td&gt;{w.workHours}&lt;/td&gt;\n                        &lt;td&gt;{salaryOf(emp, w).toLocaleString(\"vi-VN\")}&lt;/td&gt;\n                      &lt;/tr&gt;\n                    ))}\n                  &lt;/tbody&gt;\n                &lt;/Table&gt;\n                &lt;div style={{ background: \"#d1e7dd\", padding: 8 }}&gt;\n                  Projects Salary: {total.toLocaleString(\"vi-VN\")} VND\n                &lt;/div&gt;\n              &lt;/Accordion.Body&gt;\n            &lt;/Accordion.Item&gt;\n          );\n        })}\n      &lt;/Accordion&gt;\n    &lt;/div&gt;\n  );\n}\n\nexport default EmployeeList;</code></pre><p><b>Key points:</b> (1) client-side filter <code>employees.filter(e =&gt; e.depId === depId)</code>; (2) Manager label only when <code>supervisorId === null</code>; (3) salary uses <code>Math.floor(empSalary/176*workHours)</code> — verified: 25,000,000/176*30 → 4.261.363, *20 → 2.840.909, total 7.102.272; (4) <code>reduce</code> sums the projects; (5) <code>toLocaleString(\"vi-VN\")</code> renders the dot separators.</p></div><div class=\"ml-vi\"><p>Lọc nhân viên theo <code>depId</code> đã chọn ở Câu 1, hiển thị bằng <code>Accordion</code> của react-bootstrap. Với mỗi nhân viên, nối bảng <code>works</code> theo <code>employeeId</code>, tra tên dự án và tính lương bằng <code>Math.floor(empSalary / 176 * workHours)</code> — dùng floor để 30h với 25.000.000 ra đúng 4.261.363. Định dạng tiền bằng <code>toLocaleString(\"vi-VN\")</code>.</p><pre><code>import { useEffect, useState } from \"react\";\nimport { Accordion, Table, Button } from \"react-bootstrap\";\n\nconst API = \"http://localhost:9999\";\n\nfunction EmployeeList({ depId, onViewDependents }) {\n  const [employees, setEmployees] = useState([]);\n  const [projects, setProjects] = useState([]);\n  const [works, setWorks] = useState([]);\n\n  useEffect(() =&gt; {\n    fetch(`${API}/employees`).then(r =&gt; r.json()).then(setEmployees);\n    fetch(`${API}/projects`).then(r =&gt; r.json()).then(setProjects);\n    fetch(`${API}/works`).then(r =&gt; r.json()).then(setWorks);\n  }, []);\n\n  const list = depId ? employees.filter(e =&gt; e.depId === depId) : employees;\n  const projectName = (pid) =&gt; {\n    const p = projects.find(x =&gt; x.id === pid);\n    return p ? p.projectName : \"\";\n  };\n  const salaryOf = (emp, w) =&gt; Math.floor((emp.empSalary / 176) * w.workHours);\n\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;Employee List&lt;/h2&gt;\n      &lt;Accordion&gt;\n        {list.map((emp, idx) =&gt; {\n          const ws = works.filter(w =&gt; w.employeeId === emp.id);\n          const total = ws.reduce((s, w) =&gt; s + salaryOf(emp, w), 0);\n          return (\n            &lt;Accordion.Item eventKey={String(idx)} key={emp.id}&gt;\n              &lt;Accordion.Header&gt;\n                #{idx + 1} - {emp.fullname}\n                {emp.supervisorId === null ? \" - Manager\" : \"\"}\n              &lt;/Accordion.Header&gt;\n              &lt;Accordion.Body&gt;\n                &lt;ul&gt;\n                  &lt;li&gt;EmployeeId: {emp.id}&lt;/li&gt;\n                  &lt;li&gt;Gender: {emp.gender}&lt;/li&gt;\n                  &lt;li&gt;BirthDate: {emp.birthdate}&lt;/li&gt;\n                &lt;/ul&gt;\n                &lt;Button onClick={() =&gt; onViewDependents(emp)}&gt;View Dependents&lt;/Button&gt;\n                &lt;h5&gt;Work on projects&lt;/h5&gt;\n                &lt;Table bordered&gt;\n                  &lt;thead&gt;\n                    &lt;tr&gt;\n                      &lt;th&gt;WorkId&lt;/th&gt;&lt;th&gt;Project name&lt;/th&gt;\n                      &lt;th&gt;Work hours&lt;/th&gt;&lt;th&gt;Salary (VND)&lt;/th&gt;\n                    &lt;/tr&gt;\n                  &lt;/thead&gt;\n                  &lt;tbody&gt;\n                    {ws.map(w =&gt; (\n                      &lt;tr key={w.id}&gt;\n                        &lt;td&gt;{w.id}&lt;/td&gt;\n                        &lt;td&gt;{projectName(w.projectId)}&lt;/td&gt;\n                        &lt;td&gt;{w.workHours}&lt;/td&gt;\n                        &lt;td&gt;{salaryOf(emp, w).toLocaleString(\"vi-VN\")}&lt;/td&gt;\n                      &lt;/tr&gt;\n                    ))}\n                  &lt;/tbody&gt;\n                &lt;/Table&gt;\n                &lt;div style={{ background: \"#d1e7dd\", padding: 8 }}&gt;\n                  Projects Salary: {total.toLocaleString(\"vi-VN\")} VND\n                &lt;/div&gt;\n              &lt;/Accordion.Body&gt;\n            &lt;/Accordion.Item&gt;\n          );\n        })}\n      &lt;/Accordion&gt;\n    &lt;/div&gt;\n  );\n}\n\nexport default EmployeeList;</code></pre><p><b>Điểm mấu chốt:</b> (1) lọc phía client <code>employees.filter(e =&gt; e.depId === depId)</code>; (2) nhãn Manager chỉ khi <code>supervisorId === null</code>; (3) lương dùng <code>Math.floor(empSalary/176*workHours)</code> — đã kiểm: 25.000.000/176*30 → 4.261.363, *20 → 2.840.909, tổng 7.102.272; (4) <code>reduce</code> cộng tổng các dự án; (5) <code>toLocaleString(\"vi-VN\")</code> cho dấu chấm ngăn hàng nghìn.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Fetch employees/projects/works and filter employees by depId|||Lấy employees/projects/works và lọc nhân viên theo depId",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Accordion showing EmployeeId, Fullname, Gender, Birthdate and the Manager label when supervisorId is null|||Accordion hiển thị EmployeeId, Fullname, Gender, Birthdate và nhãn Manager khi supervisorId là null",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "Projects table with correct salary = floor(empSalary/176*workHours) and VN number format|||Bảng dự án với lương đúng = floor(empSalary/176*workHours) và định dạng số kiểu Việt Nam",
              "weight": 1.5,
              "maxScore": 1.5
            },
            {
              "id": "c4",
              "criterion": "Correct total 'Projects Salary' summed across all projects|||Tổng 'Projects Salary' cộng đúng qua tất cả dự án",
              "weight": 0.5,
              "maxScore": 0.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE1.png"
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 3 (4 points)</b></p><p><b>Question 3 (4 points) - Dependents Management.</b> Manage the dependents of employees.</p><ul><li>When <b>View Dependents</b> is clicked, display the dependents: <code>| No. | Name | Birthdate | Relationship |</code>. If the employee has none, show <b>No dependents</b>.</li><li><b>Add New Dependent</b> form with fields <b>Fullname, Birthdate, Relationship</b>. Relationship options: Wife, Son, Daughter, Father, Mother, Father in law, Mother in law.</li><li>Validation: Fullname not empty; Birthdate earlier than current date; Relationship must be selected. On failure show a list of error messages.</li><li>On success: add the dependent to the selected employee, send <code>PUT http://localhost:9999/employees/:id</code>, and update the UI immediately.</li></ul><p><i>Demo:</i> Fig1 validation fails → pink box with \"Fullname is required\", \"Birthdate is required\", \"Please choose a relationship\". Fig2 future birthdate → \"Birthdate must be before today\". Fig3 success → the dependents list shows the new row and the form resets.</p></div><div class=\"ml-vi\"><p><b>Câu 3 (4 điểm)</b></p><p><b>Câu 3 (4 điểm) - Quản lý người phụ thuộc.</b> Quản lý người phụ thuộc của nhân viên.</p><ul><li>Khi bấm <b>View Dependents</b>, hiển thị danh sách người phụ thuộc: <code>| No. | Name | Birthdate | Relationship |</code>. Nếu nhân viên không có ai, hiển thị <b>No dependents</b>.</li><li>Form <b>Add New Dependent</b> gồm các trường <b>Fullname, Birthdate, Relationship</b>. Các lựa chọn Relationship: Wife, Son, Daughter, Father, Mother, Father in law, Mother in law.</li><li>Kiểm tra: Fullname không được rỗng; Birthdate phải trước ngày hiện tại; phải chọn Relationship. Nếu sai hiển thị danh sách thông báo lỗi.</li><li>Nếu hợp lệ: thêm người phụ thuộc vào nhân viên đang chọn, gửi <code>PUT http://localhost:9999/employees/:id</code>, và cập nhật giao diện ngay lập tức.</li></ul><p><i>Demo:</i> Fig1 lỗi hợp lệ → hộp hồng \"Fullname is required\", \"Birthdate is required\", \"Please choose a relationship\". Fig2 ngày sinh tương lai → \"Birthdate must be before today\". Fig3 thành công → danh sách hiện dòng mới và form reset.</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Keep the selected employee in state, read its <code>dependents</code> array (nested in the employee record). Validate on submit into an error array; if any errors, render them and stop. Otherwise append the new dependent and PUT the whole updated employee object, then update local state so the UI refreshes without a re-fetch.</p><pre><code>import { useState } from \"react\";\nimport { Table, Form, Button, Alert } from \"react-bootstrap\";\n\nconst API = \"http://localhost:9999\";\nconst RELATIONSHIPS = [\n  \"Wife\", \"Son\", \"Daughter\", \"Father\",\n  \"Mother\", \"Father in law\", \"Mother in law\",\n];\n\nfunction Dependents({ employee }) {\n  const [emp, setEmp] = useState(employee);\n  const [form, setForm] = useState({ fullname: \"\", birthdate: \"\", relationship: \"\" });\n  const [errors, setErrors] = useState([]);\n\n  const deps = emp.dependents || [];\n\n  const validate = () =&gt; {\n    const errs = [];\n    if (!form.fullname.trim()) errs.push(\"Fullname is required\");\n    if (!form.birthdate) errs.push(\"Birthdate is required\");\n    else if (new Date(form.birthdate) &gt;= new Date()) errs.push(\"Birthdate must be before today\");\n    if (!form.relationship) errs.push(\"Please choose a relationship\");\n    return errs;\n  };\n\n  const handleSubmit = async (e) =&gt; {\n    e.preventDefault();\n    const errs = validate();\n    if (errs.length &gt; 0) { setErrors(errs); return; }\n\n    const newDep = {\n      id: Date.now(),\n      name: form.fullname.trim(),\n      birthdate: form.birthdate,\n      relationship: form.relationship,\n    };\n    const updated = { ...emp, dependents: [...deps, newDep] };\n\n    await fetch(`${API}/employees/${emp.id}`, {\n      method: \"PUT\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(updated),\n    });\n\n    setEmp(updated);                                   // update UI immediately\n    setForm({ fullname: \"\", birthdate: \"\", relationship: \"\" });\n    setErrors([]);\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;h3&gt;Dependents List&lt;/h3&gt;\n      {deps.length === 0 ? (\n        &lt;p&gt;No dependents&lt;/p&gt;\n      ) : (\n        &lt;Table bordered&gt;\n          &lt;thead&gt;\n            &lt;tr&gt;&lt;th&gt;No.&lt;/th&gt;&lt;th&gt;Name&lt;/th&gt;&lt;th&gt;Birthdate&lt;/th&gt;&lt;th&gt;Relationship&lt;/th&gt;&lt;/tr&gt;\n          &lt;/thead&gt;\n          &lt;tbody&gt;\n            {deps.map((d, i) =&gt; (\n              &lt;tr key={d.id}&gt;\n                &lt;td&gt;{i + 1}&lt;/td&gt;\n                &lt;td&gt;{d.name}&lt;/td&gt;\n                &lt;td&gt;{d.birthdate}&lt;/td&gt;\n                &lt;td&gt;{d.relationship}&lt;/td&gt;\n              &lt;/tr&gt;\n            ))}\n          &lt;/tbody&gt;\n        &lt;/Table&gt;\n      )}\n\n      &lt;h4&gt;Add new dependents&lt;/h4&gt;\n      {errors.length &gt; 0 &amp;&amp; (\n        &lt;Alert variant=\"danger\"&gt;\n          &lt;ul&gt;{errors.map((msg, i) =&gt; &lt;li key={i}&gt;{msg}&lt;/li&gt;)}&lt;/ul&gt;\n        &lt;/Alert&gt;\n      )}\n      &lt;Form onSubmit={handleSubmit}&gt;\n        &lt;Form.Control\n          placeholder=\"Fullname\"\n          value={form.fullname}\n          onChange={(e) =&gt; setForm({ ...form, fullname: e.target.value })}\n        /&gt;\n        &lt;Form.Control\n          type=\"date\"\n          value={form.birthdate}\n          onChange={(e) =&gt; setForm({ ...form, birthdate: e.target.value })}\n        /&gt;\n        &lt;Form.Select\n          value={form.relationship}\n          onChange={(e) =&gt; setForm({ ...form, relationship: e.target.value })}\n        &gt;\n          &lt;option value=\"\"&gt;--- Type of relationship ---&lt;/option&gt;\n          {RELATIONSHIPS.map((r) =&gt; &lt;option key={r} value={r}&gt;{r}&lt;/option&gt;)}\n        &lt;/Form.Select&gt;\n        &lt;Button type=\"submit\"&gt;Add&lt;/Button&gt;\n      &lt;/Form&gt;\n    &lt;/div&gt;\n  );\n}\n\nexport default Dependents;</code></pre><p><b>Key points:</b> (1) dependents live inside the employee, so we PUT the whole employee (json-server PUT replaces the record at <code>/employees/:id</code>); (2) validation collects all messages into an array and short-circuits before the request; (3) future date caught with <code>new Date(form.birthdate) &gt;= new Date()</code>; (4) <code>setEmp(updated)</code> refreshes the list immediately without re-fetching, and the form is reset.</p></div><div class=\"ml-vi\"><p>Giữ nhân viên đang chọn trong state, đọc mảng <code>dependents</code> của nó (lồng trong bản ghi employee). Kiểm tra khi submit vào một mảng lỗi; nếu có lỗi thì hiển thị và dừng. Ngược lại thêm người phụ thuộc mới rồi PUT toàn bộ object employee đã cập nhật, sau đó cập nhật state cục bộ để giao diện làm mới mà không cần fetch lại.</p><pre><code>import { useState } from \"react\";\nimport { Table, Form, Button, Alert } from \"react-bootstrap\";\n\nconst API = \"http://localhost:9999\";\nconst RELATIONSHIPS = [\n  \"Wife\", \"Son\", \"Daughter\", \"Father\",\n  \"Mother\", \"Father in law\", \"Mother in law\",\n];\n\nfunction Dependents({ employee }) {\n  const [emp, setEmp] = useState(employee);\n  const [form, setForm] = useState({ fullname: \"\", birthdate: \"\", relationship: \"\" });\n  const [errors, setErrors] = useState([]);\n\n  const deps = emp.dependents || [];\n\n  const validate = () =&gt; {\n    const errs = [];\n    if (!form.fullname.trim()) errs.push(\"Fullname is required\");\n    if (!form.birthdate) errs.push(\"Birthdate is required\");\n    else if (new Date(form.birthdate) &gt;= new Date()) errs.push(\"Birthdate must be before today\");\n    if (!form.relationship) errs.push(\"Please choose a relationship\");\n    return errs;\n  };\n\n  const handleSubmit = async (e) =&gt; {\n    e.preventDefault();\n    const errs = validate();\n    if (errs.length &gt; 0) { setErrors(errs); return; }\n\n    const newDep = {\n      id: Date.now(),\n      name: form.fullname.trim(),\n      birthdate: form.birthdate,\n      relationship: form.relationship,\n    };\n    const updated = { ...emp, dependents: [...deps, newDep] };\n\n    await fetch(`${API}/employees/${emp.id}`, {\n      method: \"PUT\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(updated),\n    });\n\n    setEmp(updated);                                   // cap nhat UI ngay\n    setForm({ fullname: \"\", birthdate: \"\", relationship: \"\" });\n    setErrors([]);\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;h3&gt;Dependents List&lt;/h3&gt;\n      {deps.length === 0 ? (\n        &lt;p&gt;No dependents&lt;/p&gt;\n      ) : (\n        &lt;Table bordered&gt;\n          &lt;thead&gt;\n            &lt;tr&gt;&lt;th&gt;No.&lt;/th&gt;&lt;th&gt;Name&lt;/th&gt;&lt;th&gt;Birthdate&lt;/th&gt;&lt;th&gt;Relationship&lt;/th&gt;&lt;/tr&gt;\n          &lt;/thead&gt;\n          &lt;tbody&gt;\n            {deps.map((d, i) =&gt; (\n              &lt;tr key={d.id}&gt;\n                &lt;td&gt;{i + 1}&lt;/td&gt;\n                &lt;td&gt;{d.name}&lt;/td&gt;\n                &lt;td&gt;{d.birthdate}&lt;/td&gt;\n                &lt;td&gt;{d.relationship}&lt;/td&gt;\n              &lt;/tr&gt;\n            ))}\n          &lt;/tbody&gt;\n        &lt;/Table&gt;\n      )}\n\n      &lt;h4&gt;Add new dependents&lt;/h4&gt;\n      {errors.length &gt; 0 &amp;&amp; (\n        &lt;Alert variant=\"danger\"&gt;\n          &lt;ul&gt;{errors.map((msg, i) =&gt; &lt;li key={i}&gt;{msg}&lt;/li&gt;)}&lt;/ul&gt;\n        &lt;/Alert&gt;\n      )}\n      &lt;Form onSubmit={handleSubmit}&gt;\n        &lt;Form.Control\n          placeholder=\"Fullname\"\n          value={form.fullname}\n          onChange={(e) =&gt; setForm({ ...form, fullname: e.target.value })}\n        /&gt;\n        &lt;Form.Control\n          type=\"date\"\n          value={form.birthdate}\n          onChange={(e) =&gt; setForm({ ...form, birthdate: e.target.value })}\n        /&gt;\n        &lt;Form.Select\n          value={form.relationship}\n          onChange={(e) =&gt; setForm({ ...form, relationship: e.target.value })}\n        &gt;\n          &lt;option value=\"\"&gt;--- Type of relationship ---&lt;/option&gt;\n          {RELATIONSHIPS.map((r) =&gt; &lt;option key={r} value={r}&gt;{r}&lt;/option&gt;)}\n        &lt;/Form.Select&gt;\n        &lt;Button type=\"submit\"&gt;Add&lt;/Button&gt;\n      &lt;/Form&gt;\n    &lt;/div&gt;\n  );\n}\n\nexport default Dependents;</code></pre><p><b>Điểm mấu chốt:</b> (1) người phụ thuộc nằm trong bản ghi employee, nên ta PUT cả object employee (json-server PUT thay thế bản ghi tại <code>/employees/:id</code>); (2) validate gom mọi thông báo vào một mảng và dừng trước khi gửi request; (3) ngày tương lai bắt bằng <code>new Date(form.birthdate) &gt;= new Date()</code>; (4) <code>setEmp(updated)</code> làm mới danh sách ngay mà không fetch lại, và form được reset.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "View Dependents shows the list table (No./Name/Birthdate/Relationship) or 'No dependents' when empty|||View Dependents hiển thị bảng (No./Name/Birthdate/Relationship) hoặc 'No dependents' khi rỗng",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Add form with Fullname, Birthdate and the 7 Relationship options|||Form thêm với Fullname, Birthdate và 7 lựa chọn Relationship",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "Validation (non-empty name, birthdate before today, relationship chosen) with a list of error messages|||Kiểm tra hợp lệ (tên không rỗng, ngày sinh trước hôm nay, đã chọn quan hệ) kèm danh sách lỗi",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c4",
              "criterion": "On success PUT /employees/:id and update the UI immediately with form reset|||Khi hợp lệ gửi PUT /employees/:id và cập nhật UI ngay, reset form",
              "weight": 1,
              "maxScore": 1
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE1.png"
        }
      ]
    }
  ]
};
