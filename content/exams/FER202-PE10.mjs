export default {
  "course": {
    "courseCode": "FER202"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE10-PESP2025s1",
      "title": "FER202 — Practical Exam Đề 10 (PE - SP 2025 - Đề số 1)|||FER202 — Đề thi thực hành số 10 (PE - SP 2025 - Đề số 1)",
      "description": "React PE (WRITE): build components against a REST API, AI-graded.|||Đề PE React (viết): dựng component theo REST API, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>React / Front-end Development</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>React / Front-end Development</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p>FER202 Practical Exam (PE) — SP 2025, Paper No. 1. React front-end application consuming a json-server REST API (database.json). Run the API with <code>json-server --watch database.json --port 9999</code> and the React app with <code>npm start</code>. The app is a \"Shopping System\": list products, add them to a cart, place an order (persisted via POST), and view order history. Downloading extra libraries is not allowed; react-router-dom is available from the provided template. Endpoints: <code>GET/POST http://localhost:9999/products</code> and <code>http://localhost:9999/orders</code>. This bank gives a correct reference React solution plus grading rubric for each question (total 10 marks).</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p>Bài thi thực hành FER202 (PE) — SP 2025, Đề số 1. Ứng dụng React front-end gọi REST API bằng json-server (database.json). Chạy API bằng <code>json-server --watch database.json --port 9999</code> và chạy app React bằng <code>npm start</code>. App là một \"Shopping System\": liệt kê sản phẩm, thêm vào giỏ hàng, đặt hàng (lưu qua POST), và xem lịch sử đơn hàng. Không được tải thêm thư viện ngoài; react-router-dom đã có sẵn trong template được cung cấp. Endpoint: <code>GET/POST http://localhost:9999/products</code> và <code>http://localhost:9999/orders</code>. Ngân hàng này cung cấp lời giải React tham chiếu đúng kèm rubric chấm điểm cho từng câu (tổng 10 điểm).</p></div></div><div class=\"ml-en\"><p><b>Question 1 (4 points)</b></p><p><b>Question 1 (4 marks) - Fetch and Display Products</b></p><p>Build a component that fetches and displays all products from the API endpoint: <code>GET http://localhost:9999/products</code></p><p><b>Requirements:</b></p><ul><li>Component is routed at URL: <code>http://localhost:3000</code> (<b>0 points if incorrect the URL</b>).</li><li>Design the correct layout.</li><li>Show the <b>Title, Category, Price</b>, and <b>Rate</b> (average rating of reviews) in a table.</li><li>Include a dropdown list box for filtering products by category, with the categories taken from the API.</li></ul><p><b>Figure 1:</b> Page \"Shopping System\". Top has a dropdown \"-- Select all category --\" and a green <b>Orders History</b> button. Table columns: Title | Category | Price | Rate | Action (green <b>Add To Cart</b> button). On the right is a <b>Cart</b> panel showing \"Your cart is empty!\".</p><p><b>Figure 2:</b> When filtering category=beauty → the table shows only the 5 beauty products. Cart still \"Your cart is empty!\".</p></div><div class=\"ml-vi\"><p><b>Câu 1 (4 điểm)</b></p><p><b>Câu 1 (4 điểm) - Lấy và Hiển thị Sản phẩm</b></p><p>Xây dựng một component lấy về và hiển thị tất cả sản phẩm từ endpoint API: <code>GET http://localhost:9999/products</code></p><p><b>Yêu cầu:</b></p><ul><li>Component được định tuyến tại URL: <code>http://localhost:3000</code> (<b>0 điểm nếu sai URL</b>).</li><li>Thiết kế bố cục đúng.</li><li>Hiển thị <b>Title (Tên), Category (Danh mục), Price (Giá)</b> và <b>Rate</b> (điểm đánh giá trung bình của các review) trong một bảng.</li><li>Thêm một dropdown list box để lọc sản phẩm theo danh mục, danh sách danh mục lấy từ API.</li></ul><p><b>Hình 1:</b> Trang \"Shopping System\". Phía trên có dropdown \"-- Select all category --\" và nút <b>Orders History</b> màu xanh lá. Bảng gồm các cột: Title | Category | Price | Rate | Action (nút <b>Add To Cart</b> xanh). Bên phải là khung <b>Cart</b> hiển thị \"Your cart is empty!\".</p><p><b>Hình 2:</b> Khi lọc category=beauty → bảng chỉ hiển thị 5 sản phẩm beauty. Giỏ hàng vẫn \"Your cart is empty!\".</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Route the product listing at the root path <code>/</code> (i.e. http://localhost:3000). Fetch products in <code>useEffect</code>, compute the average review rating for the Rate column, and build the category dropdown from the unique categories in the API data.</p><pre><code>// App.js — routing (react-router-dom v6)\nimport { BrowserRouter, Routes, Route } from \"react-router-dom\";\nimport ProductList from \"./ProductList\";\nimport Orders from \"./Orders\";\n\nexport default function App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        {/* routed at http://localhost:3000 */}\n        &lt;Route path=\"/\" element={&lt;ProductList /&gt;} /&gt;\n        &lt;Route path=\"/orders\" element={&lt;Orders /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\n\n// ProductList.js\nimport { useEffect, useState } from \"react\";\nimport { Link } from \"react-router-dom\";\n\nexport default function ProductList() {\n  const [products, setProducts] = useState([]);\n  const [category, setCategory] = useState(\"\");\n\n  useEffect(() =&gt; {\n    fetch(\"http://localhost:9999/products\")\n      .then((res) =&gt; res.json())\n      .then((data) =&gt; setProducts(data))\n      .catch((err) =&gt; console.error(err));\n  }, []);\n\n  // Rate = average rating of the product's reviews\n  const avgRate = (reviews) =&gt; {\n    if (!reviews || reviews.length === 0) return \"0.00\";\n    const sum = reviews.reduce((s, r) =&gt; s + r.rating, 0);\n    return (sum / reviews.length).toFixed(2);\n  };\n\n  // categories taken from the API (unique)\n  const categories = [...new Set(products.map((p) =&gt; p.category))];\n\n  const shown = category\n    ? products.filter((p) =&gt; p.category === category)\n    : products;\n\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;Shopping System&lt;/h2&gt;\n      &lt;select value={category} onChange={(e) =&gt; setCategory(e.target.value)}&gt;\n        &lt;option value=\"\"&gt;-- Select all category --&lt;/option&gt;\n        {categories.map((c) =&gt; (\n          &lt;option key={c} value={c}&gt;{c}&lt;/option&gt;\n        ))}\n      &lt;/select&gt;\n      &lt;Link to=\"/orders\"&gt;\n        &lt;button style={{ background: \"green\", color: \"#fff\" }}&gt;Orders History&lt;/button&gt;\n      &lt;/Link&gt;\n\n      &lt;table border=\"1\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;\n            &lt;th&gt;Title&lt;/th&gt;&lt;th&gt;Category&lt;/th&gt;&lt;th&gt;Price&lt;/th&gt;&lt;th&gt;Rate&lt;/th&gt;\n          &lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {shown.map((p) =&gt; (\n            &lt;tr key={p.id}&gt;\n              &lt;td&gt;{p.title}&lt;/td&gt;\n              &lt;td&gt;{p.category}&lt;/td&gt;\n              &lt;td&gt;{p.price}&lt;/td&gt;\n              &lt;td&gt;{avgRate(p.reviews)}&lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Explanation:</b> <code>new Set(...)</code> yields the distinct categories for the dropdown; when a category is selected the list is filtered client-side. The Rate column reduces each product's <code>reviews</code> array to an average and formats it to 2 decimals (matching \"3.00 / 4.67\" in the figure).</p></div><div class=\"ml-vi\"><p>Định tuyến danh sách sản phẩm tại đường dẫn gốc <code>/</code> (tức http://localhost:3000). Lấy sản phẩm trong <code>useEffect</code>, tính điểm review trung bình cho cột Rate, và dựng dropdown danh mục từ các danh mục duy nhất trong dữ liệu API.</p><pre><code>// App.js — định tuyến (react-router-dom v6)\nimport { BrowserRouter, Routes, Route } from \"react-router-dom\";\nimport ProductList from \"./ProductList\";\nimport Orders from \"./Orders\";\n\nexport default function App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        {/* định tuyến tại http://localhost:3000 */}\n        &lt;Route path=\"/\" element={&lt;ProductList /&gt;} /&gt;\n        &lt;Route path=\"/orders\" element={&lt;Orders /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\n\n// ProductList.js\nimport { useEffect, useState } from \"react\";\nimport { Link } from \"react-router-dom\";\n\nexport default function ProductList() {\n  const [products, setProducts] = useState([]);\n  const [category, setCategory] = useState(\"\");\n\n  useEffect(() =&gt; {\n    fetch(\"http://localhost:9999/products\")\n      .then((res) =&gt; res.json())\n      .then((data) =&gt; setProducts(data))\n      .catch((err) =&gt; console.error(err));\n  }, []);\n\n  // Rate = điểm trung bình các review của sản phẩm\n  const avgRate = (reviews) =&gt; {\n    if (!reviews || reviews.length === 0) return \"0.00\";\n    const sum = reviews.reduce((s, r) =&gt; s + r.rating, 0);\n    return (sum / reviews.length).toFixed(2);\n  };\n\n  // danh mục lấy từ API (duy nhất)\n  const categories = [...new Set(products.map((p) =&gt; p.category))];\n\n  const shown = category\n    ? products.filter((p) =&gt; p.category === category)\n    : products;\n\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;Shopping System&lt;/h2&gt;\n      &lt;select value={category} onChange={(e) =&gt; setCategory(e.target.value)}&gt;\n        &lt;option value=\"\"&gt;-- Select all category --&lt;/option&gt;\n        {categories.map((c) =&gt; (\n          &lt;option key={c} value={c}&gt;{c}&lt;/option&gt;\n        ))}\n      &lt;/select&gt;\n      &lt;Link to=\"/orders\"&gt;\n        &lt;button style={{ background: \"green\", color: \"#fff\" }}&gt;Orders History&lt;/button&gt;\n      &lt;/Link&gt;\n\n      &lt;table border=\"1\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;\n            &lt;th&gt;Title&lt;/th&gt;&lt;th&gt;Category&lt;/th&gt;&lt;th&gt;Price&lt;/th&gt;&lt;th&gt;Rate&lt;/th&gt;\n          &lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {shown.map((p) =&gt; (\n            &lt;tr key={p.id}&gt;\n              &lt;td&gt;{p.title}&lt;/td&gt;\n              &lt;td&gt;{p.category}&lt;/td&gt;\n              &lt;td&gt;{p.price}&lt;/td&gt;\n              &lt;td&gt;{avgRate(p.reviews)}&lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Giải thích:</b> <code>new Set(...)</code> cho ra các danh mục riêng biệt để đổ vào dropdown; khi chọn một danh mục thì danh sách được lọc phía client. Cột Rate rút gọn mảng <code>reviews</code> của mỗi sản phẩm thành điểm trung bình và định dạng 2 chữ số thập phân (khớp \"3.00 / 4.67\" trong hình).</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Component routed at http://localhost:3000 (root path) and fetches GET /products correctly (0 if URL wrong).|||Component định tuyến tại http://localhost:3000 (đường dẫn gốc) và fetch GET /products đúng (0 điểm nếu sai URL).",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Table shows Title, Category, Price and Rate = average review rating, computed correctly.|||Bảng hiển thị Title, Category, Price và Rate = điểm review trung bình, tính đúng.",
              "weight": 1,
              "maxScore": 2
            },
            {
              "id": "c3",
              "criterion": "Category dropdown built from API categories filters the product table.|||Dropdown danh mục dựng từ danh mục của API và lọc được bảng sản phẩm.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2 (4 points)</b></p><p><b>Question 2 (4 marks) - Create a New Order</b></p><p>Build a feature that lets customers place an order.</p><p><b>Requirements:</b></p><ul><li>The customer finds the product they want to buy and clicks the <b>Add To Cart</b> button to put it in the cart. If the product is already in the cart, the quantity increases by one each time the button is clicked.</li><li>Click the <b>Remove</b> button to take a product out of the cart. When the cart has no products left, the cart's status is displayed as in <b>Figure 2</b> (\"Your cart is empty!\").</li><li>When the <b>Place Order</b> button is clicked: the cart details, including the ShipAddress, are saved to orders via the API <code>POST http://localhost:9999/orders</code>; and a success message is shown.</li></ul><p><b>Figure 3:</b> Add products with IDs 2, 3, 1 to the cart one by one with different quantities. Cart table columns: Id | Name | Price | Quantity | Action (Remove). Below the table is a <b>Ship Address</b> textarea (\"Tòa BE, Đại học FPT, Hà Nội\") and a yellow <b>Place Order</b> button.</p><p><b>Figure 4:</b> After clicking Place Order → Cart shows \"Your cart is empty!\" and a green message \"<b>Thank you for your order!</b> Your order has been placed successfully.\"</p></div><div class=\"ml-vi\"><p><b>Câu 2 (4 điểm)</b></p><p><b>Câu 2 (4 điểm) - Tạo Đơn hàng Mới</b></p><p>Xây dựng tính năng cho phép khách hàng đặt hàng.</p><p><b>Yêu cầu:</b></p><ul><li>Khách tìm sản phẩm muốn mua và bấm nút <b>Add To Cart</b> để thêm vào giỏ. Nếu sản phẩm đã có trong giỏ, số lượng tăng thêm 1 mỗi lần bấm nút.</li><li>Bấm nút <b>Remove</b> để bỏ một sản phẩm khỏi giỏ. Khi giỏ không còn sản phẩm nào, trạng thái giỏ hiển thị như <b>Hình 2</b> (\"Your cart is empty!\").</li><li>Khi bấm nút <b>Place Order</b>: chi tiết giỏ hàng, bao gồm cả ShipAddress, được lưu vào orders qua API <code>POST http://localhost:9999/orders</code>; và hiển thị một thông báo thành công.</li></ul><p><b>Hình 3:</b> Thêm các sản phẩm ID 2, 3, 1 vào giỏ lần lượt với số lượng khác nhau. Bảng giỏ hàng có cột: Id | Name | Price | Quantity | Action (Remove). Dưới bảng có ô textarea <b>Ship Address</b> (\"Tòa BE, Đại học FPT, Hà Nội\") và nút <b>Place Order</b> màu vàng.</p><p><b>Hình 4:</b> Sau khi bấm Place Order → Giỏ hiển thị \"Your cart is empty!\" và thông báo màu xanh lá \"<b>Thank you for your order!</b> Your order has been placed successfully.\"</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Hold the cart in a shared Context. <code>addToCart</code> increments quantity when the product already exists; <code>removeFromCart</code> filters it out; an empty cart renders \"Your cart is empty!\". <code>Place Order</code> POSTs the cart plus ShipAddress to /orders, clears the cart, and shows the success message.</p><pre><code>// CartContext.js\nimport { createContext, useContext, useState } from \"react\";\nconst CartContext = createContext();\nexport const useCart = () =&gt; useContext(CartContext);\n\nexport function CartProvider({ children }) {\n  const [cart, setCart] = useState([]);\n\n  const addToCart = (product) =&gt; {\n    setCart((prev) =&gt; {\n      const found = prev.find((i) =&gt; i.id === product.id);\n      if (found) {\n        // already in cart → quantity + 1\n        return prev.map((i) =&gt;\n          i.id === product.id ? { ...i, quantity: i.quantity + 1 } : i\n        );\n      }\n      return [...prev, { ...product, quantity: 1 }];\n    });\n  };\n\n  const removeFromCart = (id) =&gt;\n    setCart((prev) =&gt; prev.filter((i) =&gt; i.id !== id));\n\n  const clearCart = () =&gt; setCart([]);\n\n  return (\n    &lt;CartContext.Provider value={{ cart, addToCart, removeFromCart, clearCart }}&gt;\n      {children}\n    &lt;/CartContext.Provider&gt;\n  );\n}\n\n// In ProductList.js — add an Action column with Add To Cart\n// import { useCart } from \"./CartContext\";  const { addToCart } = useCart();\n// &lt;th&gt;Action&lt;/th&gt; in the header, and per row:\n// &lt;td&gt;&lt;button style={{ background: \"green\", color: \"#fff\" }}\n//        onClick={() =&gt; addToCart(p)}&gt;Add To Cart&lt;/button&gt;&lt;/td&gt;\n\n// Cart.js\nimport { useState } from \"react\";\nimport { useCart } from \"./CartContext\";\n\nexport default function Cart() {\n  const { cart, removeFromCart, clearCart } = useCart();\n  const [shipAddress, setShipAddress] = useState(\"\");\n  const [message, setMessage] = useState(\"\");\n\n  const placeOrder = () =&gt; {\n    const order = {\n      shipAddress,\n      orderDate: new Date().toISOString(),\n      products: cart.map((i) =&gt; ({\n        id: i.id,\n        title: i.title,\n        price: i.price,\n        quantity: i.quantity,\n      })),\n      totalPrice: cart.reduce((s, i) =&gt; s + i.price * i.quantity, 0),\n    };\n    fetch(\"http://localhost:9999/orders\", {\n      method: \"POST\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(order),\n    })\n      .then((res) =&gt; res.json())\n      .then(() =&gt; {\n        clearCart();\n        setShipAddress(\"\");\n        setMessage(\"Thank you for your order! Your order has been placed successfully.\");\n      })\n      .catch((err) =&gt; console.error(err));\n  };\n\n  if (cart.length === 0) {\n    return (\n      &lt;div&gt;\n        {message &amp;&amp; &lt;p style={{ color: \"green\" }}&gt;{message}&lt;/p&gt;}\n        &lt;p&gt;Your cart is empty!&lt;/p&gt;\n      &lt;/div&gt;\n    );\n  }\n\n  return (\n    &lt;div&gt;\n      &lt;table border=\"1\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;\n            &lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Name&lt;/th&gt;&lt;th&gt;Price&lt;/th&gt;&lt;th&gt;Quantity&lt;/th&gt;&lt;th&gt;Action&lt;/th&gt;\n          &lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {cart.map((i) =&gt; (\n            &lt;tr key={i.id}&gt;\n              &lt;td&gt;{i.id}&lt;/td&gt;\n              &lt;td&gt;{i.title}&lt;/td&gt;\n              &lt;td&gt;{i.price}&lt;/td&gt;\n              &lt;td&gt;{i.quantity}&lt;/td&gt;\n              &lt;td&gt;&lt;button onClick={() =&gt; removeFromCart(i.id)}&gt;Remove&lt;/button&gt;&lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n      &lt;div&gt;\n        &lt;label&gt;Ship Address&lt;/label&gt;&lt;br /&gt;\n        &lt;textarea value={shipAddress}\n          onChange={(e) =&gt; setShipAddress(e.target.value)} /&gt;\n      &lt;/div&gt;\n      &lt;button style={{ background: \"gold\" }} onClick={placeOrder}&gt;Place Order&lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Explanation:</b> Wrap the app in <code>&lt;CartProvider&gt;</code> so ProductList and Cart share the same cart state. Increment-on-existing is handled inside <code>addToCart</code>. After a successful POST the cart is cleared, so the empty-cart branch renders both the success message and \"Your cart is empty!\".</p></div><div class=\"ml-vi\"><p>Giữ giỏ hàng trong một Context dùng chung. <code>addToCart</code> tăng số lượng khi sản phẩm đã tồn tại; <code>removeFromCart</code> lọc bỏ nó; giỏ rỗng hiển thị \"Your cart is empty!\". <code>Place Order</code> POST giỏ hàng kèm ShipAddress lên /orders, xoá giỏ, và hiển thị thông báo thành công.</p><pre><code>// CartContext.js\nimport { createContext, useContext, useState } from \"react\";\nconst CartContext = createContext();\nexport const useCart = () =&gt; useContext(CartContext);\n\nexport function CartProvider({ children }) {\n  const [cart, setCart] = useState([]);\n\n  const addToCart = (product) =&gt; {\n    setCart((prev) =&gt; {\n      const found = prev.find((i) =&gt; i.id === product.id);\n      if (found) {\n        // đã có trong giỏ → số lượng + 1\n        return prev.map((i) =&gt;\n          i.id === product.id ? { ...i, quantity: i.quantity + 1 } : i\n        );\n      }\n      return [...prev, { ...product, quantity: 1 }];\n    });\n  };\n\n  const removeFromCart = (id) =&gt;\n    setCart((prev) =&gt; prev.filter((i) =&gt; i.id !== id));\n\n  const clearCart = () =&gt; setCart([]);\n\n  return (\n    &lt;CartContext.Provider value={{ cart, addToCart, removeFromCart, clearCart }}&gt;\n      {children}\n    &lt;/CartContext.Provider&gt;\n  );\n}\n\n// Trong ProductList.js — thêm cột Action với Add To Cart\n// import { useCart } from \"./CartContext\";  const { addToCart } = useCart();\n// &lt;th&gt;Action&lt;/th&gt; ở header, và mỗi dòng:\n// &lt;td&gt;&lt;button style={{ background: \"green\", color: \"#fff\" }}\n//        onClick={() =&gt; addToCart(p)}&gt;Add To Cart&lt;/button&gt;&lt;/td&gt;\n\n// Cart.js\nimport { useState } from \"react\";\nimport { useCart } from \"./CartContext\";\n\nexport default function Cart() {\n  const { cart, removeFromCart, clearCart } = useCart();\n  const [shipAddress, setShipAddress] = useState(\"\");\n  const [message, setMessage] = useState(\"\");\n\n  const placeOrder = () =&gt; {\n    const order = {\n      shipAddress,\n      orderDate: new Date().toISOString(),\n      products: cart.map((i) =&gt; ({\n        id: i.id,\n        title: i.title,\n        price: i.price,\n        quantity: i.quantity,\n      })),\n      totalPrice: cart.reduce((s, i) =&gt; s + i.price * i.quantity, 0),\n    };\n    fetch(\"http://localhost:9999/orders\", {\n      method: \"POST\",\n      headers: { \"Content-Type\": \"application/json\" },\n      body: JSON.stringify(order),\n    })\n      .then((res) =&gt; res.json())\n      .then(() =&gt; {\n        clearCart();\n        setShipAddress(\"\");\n        setMessage(\"Thank you for your order! Your order has been placed successfully.\");\n      })\n      .catch((err) =&gt; console.error(err));\n  };\n\n  if (cart.length === 0) {\n    return (\n      &lt;div&gt;\n        {message &amp;&amp; &lt;p style={{ color: \"green\" }}&gt;{message}&lt;/p&gt;}\n        &lt;p&gt;Your cart is empty!&lt;/p&gt;\n      &lt;/div&gt;\n    );\n  }\n\n  return (\n    &lt;div&gt;\n      &lt;table border=\"1\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;\n            &lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Name&lt;/th&gt;&lt;th&gt;Price&lt;/th&gt;&lt;th&gt;Quantity&lt;/th&gt;&lt;th&gt;Action&lt;/th&gt;\n          &lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {cart.map((i) =&gt; (\n            &lt;tr key={i.id}&gt;\n              &lt;td&gt;{i.id}&lt;/td&gt;\n              &lt;td&gt;{i.title}&lt;/td&gt;\n              &lt;td&gt;{i.price}&lt;/td&gt;\n              &lt;td&gt;{i.quantity}&lt;/td&gt;\n              &lt;td&gt;&lt;button onClick={() =&gt; removeFromCart(i.id)}&gt;Remove&lt;/button&gt;&lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n      &lt;div&gt;\n        &lt;label&gt;Ship Address&lt;/label&gt;&lt;br /&gt;\n        &lt;textarea value={shipAddress}\n          onChange={(e) =&gt; setShipAddress(e.target.value)} /&gt;\n      &lt;/div&gt;\n      &lt;button style={{ background: \"gold\" }} onClick={placeOrder}&gt;Place Order&lt;/button&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Giải thích:</b> Bọc app bằng <code>&lt;CartProvider&gt;</code> để ProductList và Cart dùng chung state giỏ hàng. Việc tăng số lượng khi đã tồn tại được xử lý trong <code>addToCart</code>. Sau khi POST thành công, giỏ được xoá nên nhánh giỏ-rỗng hiển thị cả thông báo thành công và \"Your cart is empty!\".</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Add To Cart adds the product; quantity increments by 1 when the product is already in the cart.|||Add To Cart thêm sản phẩm; số lượng tăng 1 khi sản phẩm đã có trong giỏ.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Remove deletes an item; when cart is empty it shows \"Your cart is empty!\".|||Remove xoá một mục; khi giỏ rỗng hiển thị \"Your cart is empty!\".",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "Place Order POSTs the cart + ShipAddress to http://localhost:9999/orders.|||Place Order gửi POST giỏ hàng + ShipAddress lên http://localhost:9999/orders.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c4",
              "criterion": "Success message shown and cart cleared after a successful order.|||Hiển thị thông báo thành công và xoá giỏ sau khi đặt hàng thành công.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        },
        {
          "kind": "WRITE",
          "points": 2,
          "prompt": "<div class=\"ml-en\"><p><b>Question 3 (2 points)</b></p><p><b>Question 3 (2 marks) - View Order Details</b></p><p>Click the <b>Orders History</b> button so that users can view all <b>existing orders</b>.</p><p><b>Requirements:</b></p><ul><li>Fetch data from <code>GET http://localhost:9999/orders</code>.</li><li>Show the columns: <b>OrderId, OrderDate, ShipAddress, ProductList, TotalPrice</b> in a table at URL: <code>http://localhost:3000/orders</code>.</li><li>The <b>orderDate</b> must be formatted as <code>dd/MM/yyyy</code>.</li><li>The <b>TotalPrice</b> of each order is calculated from the products in that order.</li></ul><p><b>Figure 5:</b> Page \"Order History\". Columns OrderId | OrderDate | ShipAddress | ProductList | TotalPrice ($). Sample rows: a478 | 18/02/2025 | \"Đại học FPT, Hòa Lạc, Thạch Thất, Hà Nội\" | (1 Essence Mascara Lash 9.99 x1; 3 Powder Canister 14.99 x3; 5 Red Nail Polish 8.99 x1; 4 Red Lipstick 12.99 x4) | 115.91. And two more rows totalling 289.94 and 99.93 respectively.</p></div><div class=\"ml-vi\"><p><b>Câu 3 (2 điểm)</b></p><p><b>Câu 3 (2 điểm) - Xem Chi tiết Đơn hàng</b></p><p>Bấm nút <b>Orders History</b> để người dùng có thể xem tất cả <b>các đơn hàng hiện có</b>.</p><p><b>Yêu cầu:</b></p><ul><li>Lấy dữ liệu từ <code>GET http://localhost:9999/orders</code>.</li><li>Hiển thị các cột: <b>OrderId, OrderDate, ShipAddress, ProductList, TotalPrice</b> trong một bảng tại URL: <code>http://localhost:3000/orders</code>.</li><li><b>orderDate</b> phải được định dạng theo <code>dd/MM/yyyy</code>.</li><li><b>TotalPrice</b> của mỗi đơn được tính từ các sản phẩm trong đơn đó.</li></ul><p><b>Hình 5:</b> Trang \"Order History\". Cột OrderId | OrderDate | ShipAddress | ProductList | TotalPrice ($). Dòng mẫu: a478 | 18/02/2025 | \"Đại học FPT, Hòa Lạc, Thạch Thất, Hà Nội\" | (1 Essence Mascara Lash 9.99 x1; 3 Powder Canister 14.99 x3; 5 Red Nail Polish 8.99 x1; 4 Red Lipstick 12.99 x4) | 115.91. Và hai dòng nữa với tổng lần lượt 289.94 và 99.93.</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Route the page at <code>/orders</code>. Fetch all orders, format each <code>orderDate</code> to dd/MM/yyyy manually, join the products into a readable ProductList string, and compute TotalPrice by summing price × quantity across the order's products.</p><pre><code>// Orders.js  (routed at http://localhost:3000/orders)\nimport { useEffect, useState } from \"react\";\n\nexport default function Orders() {\n  const [orders, setOrders] = useState([]);\n\n  useEffect(() =&gt; {\n    fetch(\"http://localhost:9999/orders\")\n      .then((res) =&gt; res.json())\n      .then((data) =&gt; setOrders(data))\n      .catch((err) =&gt; console.error(err));\n  }, []);\n\n  // format orderDate as dd/MM/yyyy\n  const formatDate = (d) =&gt; {\n    const date = new Date(d);\n    const dd = String(date.getDate()).padStart(2, \"0\");\n    const mm = String(date.getMonth() + 1).padStart(2, \"0\");\n    const yyyy = date.getFullYear();\n    return `${dd}/${mm}/${yyyy}`;\n  };\n\n  // TotalPrice calculated from the products of the order\n  const totalPrice = (products) =&gt;\n    products.reduce((s, p) =&gt; s + p.price * p.quantity, 0).toFixed(2);\n\n  // readable product list string\n  const productList = (products) =&gt;\n    products\n      .map((p) =&gt; `${p.quantity} ${p.title} ${p.price}`)\n      .join(\"; \");\n\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;Order History&lt;/h2&gt;\n      &lt;table border=\"1\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;\n            &lt;th&gt;OrderId&lt;/th&gt;&lt;th&gt;OrderDate&lt;/th&gt;&lt;th&gt;ShipAddress&lt;/th&gt;\n            &lt;th&gt;ProductList&lt;/th&gt;&lt;th&gt;TotalPrice&lt;/th&gt;\n          &lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {orders.map((o) =&gt; (\n            &lt;tr key={o.id}&gt;\n              &lt;td&gt;{o.id}&lt;/td&gt;\n              &lt;td&gt;{formatDate(o.orderDate)}&lt;/td&gt;\n              &lt;td&gt;{o.shipAddress}&lt;/td&gt;\n              &lt;td&gt;{productList(o.products)}&lt;/td&gt;\n              &lt;td&gt;{totalPrice(o.products)}&lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Explanation:</b> The <b>Orders History</b> button in Q1 (a <code>&lt;Link to=\"/orders\"&gt;</code>) navigates here. <code>formatDate</code> builds dd/MM/yyyy with <code>padStart</code> so single-digit days/months keep a leading zero. <code>totalPrice</code> re-derives the total from the stored products so it always matches the order contents even if a stored <code>totalPrice</code> field were absent.</p></div><div class=\"ml-vi\"><p>Định tuyến trang tại <code>/orders</code>. Lấy tất cả đơn hàng, định dạng mỗi <code>orderDate</code> thành dd/MM/yyyy thủ công, nối các sản phẩm thành chuỗi ProductList dễ đọc, và tính TotalPrice bằng cách cộng giá × số lượng của các sản phẩm trong đơn.</p><pre><code>// Orders.js  (định tuyến tại http://localhost:3000/orders)\nimport { useEffect, useState } from \"react\";\n\nexport default function Orders() {\n  const [orders, setOrders] = useState([]);\n\n  useEffect(() =&gt; {\n    fetch(\"http://localhost:9999/orders\")\n      .then((res) =&gt; res.json())\n      .then((data) =&gt; setOrders(data))\n      .catch((err) =&gt; console.error(err));\n  }, []);\n\n  // định dạng orderDate thành dd/MM/yyyy\n  const formatDate = (d) =&gt; {\n    const date = new Date(d);\n    const dd = String(date.getDate()).padStart(2, \"0\");\n    const mm = String(date.getMonth() + 1).padStart(2, \"0\");\n    const yyyy = date.getFullYear();\n    return `${dd}/${mm}/${yyyy}`;\n  };\n\n  // TotalPrice tính từ các sản phẩm của đơn\n  const totalPrice = (products) =&gt;\n    products.reduce((s, p) =&gt; s + p.price * p.quantity, 0).toFixed(2);\n\n  // chuỗi danh sách sản phẩm dễ đọc\n  const productList = (products) =&gt;\n    products\n      .map((p) =&gt; `${p.quantity} ${p.title} ${p.price}`)\n      .join(\"; \");\n\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;Order History&lt;/h2&gt;\n      &lt;table border=\"1\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;\n            &lt;th&gt;OrderId&lt;/th&gt;&lt;th&gt;OrderDate&lt;/th&gt;&lt;th&gt;ShipAddress&lt;/th&gt;\n            &lt;th&gt;ProductList&lt;/th&gt;&lt;th&gt;TotalPrice&lt;/th&gt;\n          &lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {orders.map((o) =&gt; (\n            &lt;tr key={o.id}&gt;\n              &lt;td&gt;{o.id}&lt;/td&gt;\n              &lt;td&gt;{formatDate(o.orderDate)}&lt;/td&gt;\n              &lt;td&gt;{o.shipAddress}&lt;/td&gt;\n              &lt;td&gt;{productList(o.products)}&lt;/td&gt;\n              &lt;td&gt;{totalPrice(o.products)}&lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Giải thích:</b> Nút <b>Orders History</b> ở Câu 1 (một <code>&lt;Link to=\"/orders\"&gt;</code>) điều hướng tới đây. <code>formatDate</code> dựng dd/MM/yyyy bằng <code>padStart</code> để ngày/tháng một chữ số vẫn giữ số 0 ở đầu. <code>totalPrice</code> tính lại tổng từ các sản phẩm đã lưu nên luôn khớp nội dung đơn kể cả khi thiếu trường <code>totalPrice</code> đã lưu.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Page routed at http://localhost:3000/orders; fetches GET /orders and shows OrderId, OrderDate, ShipAddress, ProductList, TotalPrice.|||Trang định tuyến tại http://localhost:3000/orders; fetch GET /orders và hiển thị OrderId, OrderDate, ShipAddress, ProductList, TotalPrice.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "orderDate formatted as dd/MM/yyyy and TotalPrice computed from the order's products.|||orderDate định dạng dd/MM/yyyy và TotalPrice tính từ các sản phẩm của đơn.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        }
      ]
    }
  ]
};
