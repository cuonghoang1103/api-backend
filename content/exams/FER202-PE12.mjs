export default {
  "course": {
    "courseCode": "FER202"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE12-FA2024Block5",
      "title": "FER202 — Practical Exam Đề 12 (FA 2024 - Block 5 - PE)|||FER202 — Đề thi thực hành số 12 (FA 2024 - Block 5 - PE)",
      "description": "React PE (WRITE): build components against a REST API, AI-graded.|||Đề PE React (viết): dựng component theo REST API, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>React / Front-end Development</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>React / Front-end Development</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p>FER202 Practical Exam (PE) - FA 2024, Block 5, Paper 12. A React (Create React App) single-page application that consumes a json-server fake REST API. Run the API with <code>json-server --watch database.json --port 9999</code> and the React app with <code>npm start</code> (served at <b>http://localhost:3000</b>). Core skills tested: React hooks (useState / useEffect), fetching data from a json-server endpoint (axios or fetch), rendering a data table, a live title-search filter, a master-detail 'Reviews details' panel, an 'Add a new Review' form with required-field validation (radio-button rating 1-5), and reloading the data after a successful write. API base endpoint: <code>http://localhost:9999/products</code>; single product endpoint: <code>http://localhost:9999/products/{id}</code>.</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p>Đề thi thực hành (PE) môn FER202 - FA 2024, Block 5, Đề 12. Ứng dụng một trang bằng React (Create React App) gọi API REST giả của json-server. Chạy API bằng <code>json-server --watch database.json --port 9999</code> và chạy app React bằng <code>npm start</code> (mở tại <b>http://localhost:3000</b>). Kỹ năng kiểm tra: React hooks (useState / useEffect), lấy dữ liệu từ endpoint json-server (axios hoặc fetch), dựng bảng dữ liệu, ô tìm kiếm lọc theo title theo thời gian thực, khung master-detail 'Reviews details', form 'Add a new Review' có kiểm tra trường bắt buộc (rating dạng radio 1-5), và nạp lại dữ liệu sau khi ghi thành công. Endpoint API gốc: <code>http://localhost:9999/products</code>; endpoint một sản phẩm: <code>http://localhost:9999/products/{id}</code>.</p></div></div><div class=\"ml-en\"><p><b>Question 1. (4 marks)</b></p><p>Build a component that communicates with the Back-End application (Json server - free fake RESTful API) to fetch data from the database as shown in <b>Figure 1</b>.</p><ul><li>At URL: <code>http://localhost:3000</code> (<b>0 points if incorrect the URL</b>).</li></ul><p><b>Figure 1</b> - The display data is returned to the component: a 'Product List' page. It has a search box 'Enter title search ...' and a table with columns: <b>Id, Title, Category, Price, Functions</b>. Each row has a <b>View Details</b> button. On the right, display 'Reviews details: Please select a product!'.</p><p><b>Requirements:</b></p><ul><li>Display all products returned from the API end-point <code>http://localhost:9999/products</code> in the data table.</li><li>The search field and the <b>View Details</b> buttons on each displayed product must be designed correctly.</li><li>Display <b>Please select a product!</b> when there are no products added to the 'Reviews details'.</li><li>When the user types the title of the desired product into the search field, a list of products containing that keyword will be displayed as shown in <b>Figure 2</b> (e.g. typing 'p' keeps only titles containing 'p': Essence Mascara Lash Princess, Eyeshadow Palette with Mirror, Powder Canister, Red Lipstick, Red Nail Polish).</li></ul></div><div class=\"ml-vi\"><p><b>Câu 1. (4 điểm)</b></p><p>Xây dựng một component giao tiếp với ứng dụng Back-End (Json server - REST API giả miễn phí) để lấy dữ liệu từ cơ sở dữ liệu như minh hoạ ở <b>Hình 1</b>.</p><ul><li>Tại URL: <code>http://localhost:3000</code> (<b>0 điểm nếu sai URL</b>).</li></ul><p><b>Hình 1</b> - Dữ liệu hiển thị trả về cho component: trang 'Product List'. Có ô tìm kiếm 'Enter title search ...' và một bảng gồm các cột: <b>Id, Title, Category, Price, Functions</b>. Mỗi dòng có nút <b>View Details</b>. Bên phải hiển thị 'Reviews details: Please select a product!'.</p><p><b>Yêu cầu:</b></p><ul><li>Hiển thị tất cả sản phẩm trả về từ endpoint API <code>http://localhost:9999/products</code> trong bảng dữ liệu.</li><li>Ô tìm kiếm và nút <b>View Details</b> trên mỗi sản phẩm hiển thị phải được thiết kế đúng.</li><li>Hiển thị <b>Please select a product!</b> khi chưa có sản phẩm nào được thêm vào khung 'Reviews details'.</li><li>Khi người dùng gõ title của sản phẩm cần tìm vào ô tìm kiếm, danh sách các sản phẩm chứa từ khoá đó sẽ được hiển thị như ở <b>Hình 2</b> (ví dụ gõ 'p' thì bảng chỉ còn các sản phẩm có 'p' trong title: Essence Mascara Lash Princess, Eyeshadow Palette with Mirror, Powder Canister, Red Lipstick, Red Nail Polish).</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>A <code>ProductList</code> component using <code>useEffect</code> to fetch <code>http://localhost:9999/products</code> once on mount, <code>useState</code> for the product list, the search keyword and the selected product. The table is rendered from a case-insensitive <code>filter</code> on <code>title</code>, so the list narrows live as the user types. The right panel shows 'Please select a product!' while nothing is selected. Each row's <b>View Details</b> button sets the selected product (used in Question 2).</p><pre><code>import React, { useState, useEffect } from \"react\";\nimport axios from \"axios\";\n\nconst API = \"http://localhost:9999/products\";\n\nfunction ProductList() {\n  const [products, setProducts] = useState([]);\n  const [search, setSearch] = useState(\"\");\n  const [selected, setSelected] = useState(null);\n\n  // Fetch all products once when the component mounts\n  useEffect(() =&gt; {\n    axios.get(API).then((res) =&gt; setProducts(res.data));\n  }, []);\n\n  // Live filter by title (case-insensitive \"contains\")\n  const filtered = products.filter((p) =&gt;\n    p.title.toLowerCase().includes(search.toLowerCase())\n  );\n\n  return (\n    &lt;div className=\"container mt-4\"&gt;\n      &lt;div className=\"row\"&gt;\n        &lt;div className=\"col-md-8\"&gt;\n          &lt;h3&gt;Product List&lt;/h3&gt;\n          &lt;input\n            className=\"form-control mb-3\"\n            placeholder=\"Enter title search ...\"\n            value={search}\n            onChange={(e) =&gt; setSearch(e.target.value)}\n          /&gt;\n          &lt;table className=\"table table-bordered\"&gt;\n            &lt;thead&gt;\n              &lt;tr&gt;\n                &lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Title&lt;/th&gt;&lt;th&gt;Category&lt;/th&gt;\n                &lt;th&gt;Price&lt;/th&gt;&lt;th&gt;Functions&lt;/th&gt;\n              &lt;/tr&gt;\n            &lt;/thead&gt;\n            &lt;tbody&gt;\n              {filtered.map((p) =&gt; (\n                &lt;tr key={p.id}&gt;\n                  &lt;td&gt;{p.id}&lt;/td&gt;\n                  &lt;td&gt;{p.title}&lt;/td&gt;\n                  &lt;td&gt;{p.category}&lt;/td&gt;\n                  &lt;td&gt;{p.price}&lt;/td&gt;\n                  &lt;td&gt;\n                    &lt;button\n                      className=\"btn btn-primary\"\n                      onClick={() =&gt; setSelected(p)}&gt;\n                      View Details\n                    &lt;/button&gt;\n                  &lt;/td&gt;\n                &lt;/tr&gt;\n              ))}\n            &lt;/tbody&gt;\n          &lt;/table&gt;\n        &lt;/div&gt;\n        &lt;div className=\"col-md-4\"&gt;\n          &lt;h4&gt;Reviews details&lt;/h4&gt;\n          {!selected &amp;&amp; &lt;p&gt;Please select a product!&lt;/p&gt;}\n        &lt;/div&gt;\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\n\nexport default ProductList;</code></pre></div><div class=\"ml-vi\"><p>Một component <code>ProductList</code> dùng <code>useEffect</code> để gọi <code>http://localhost:9999/products</code> một lần khi mount, dùng <code>useState</code> cho danh sách sản phẩm, từ khoá tìm kiếm và sản phẩm đang chọn. Bảng được dựng từ <code>filter</code> không phân biệt hoa thường trên <code>title</code>, nên danh sách lọc ngay khi người dùng gõ. Khung bên phải hiển thị 'Please select a product!' khi chưa chọn gì. Nút <b>View Details</b> ở mỗi dòng đặt sản phẩm đang chọn (dùng ở Câu 2).</p><pre><code>import React, { useState, useEffect } from \"react\";\nimport axios from \"axios\";\n\nconst API = \"http://localhost:9999/products\";\n\nfunction ProductList() {\n  const [products, setProducts] = useState([]);\n  const [search, setSearch] = useState(\"\");\n  const [selected, setSelected] = useState(null);\n\n  // Lấy toàn bộ sản phẩm một lần khi component mount\n  useEffect(() =&gt; {\n    axios.get(API).then((res) =&gt; setProducts(res.data));\n  }, []);\n\n  // Lọc theo title theo thời gian thực (\"chứa\", không phân biệt hoa thường)\n  const filtered = products.filter((p) =&gt;\n    p.title.toLowerCase().includes(search.toLowerCase())\n  );\n\n  return (\n    &lt;div className=\"container mt-4\"&gt;\n      &lt;div className=\"row\"&gt;\n        &lt;div className=\"col-md-8\"&gt;\n          &lt;h3&gt;Product List&lt;/h3&gt;\n          &lt;input\n            className=\"form-control mb-3\"\n            placeholder=\"Enter title search ...\"\n            value={search}\n            onChange={(e) =&gt; setSearch(e.target.value)}\n          /&gt;\n          &lt;table className=\"table table-bordered\"&gt;\n            &lt;thead&gt;\n              &lt;tr&gt;\n                &lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Title&lt;/th&gt;&lt;th&gt;Category&lt;/th&gt;\n                &lt;th&gt;Price&lt;/th&gt;&lt;th&gt;Functions&lt;/th&gt;\n              &lt;/tr&gt;\n            &lt;/thead&gt;\n            &lt;tbody&gt;\n              {filtered.map((p) =&gt; (\n                &lt;tr key={p.id}&gt;\n                  &lt;td&gt;{p.id}&lt;/td&gt;\n                  &lt;td&gt;{p.title}&lt;/td&gt;\n                  &lt;td&gt;{p.category}&lt;/td&gt;\n                  &lt;td&gt;{p.price}&lt;/td&gt;\n                  &lt;td&gt;\n                    &lt;button\n                      className=\"btn btn-primary\"\n                      onClick={() =&gt; setSelected(p)}&gt;\n                      View Details\n                    &lt;/button&gt;\n                  &lt;/td&gt;\n                &lt;/tr&gt;\n              ))}\n            &lt;/tbody&gt;\n          &lt;/table&gt;\n        &lt;/div&gt;\n        &lt;div className=\"col-md-4\"&gt;\n          &lt;h4&gt;Reviews details&lt;/h4&gt;\n          {!selected &amp;&amp; &lt;p&gt;Please select a product!&lt;/p&gt;}\n        &lt;/div&gt;\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\n\nexport default ProductList;</code></pre></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Correct URL (http://localhost:3000) and fetch ALL products from http://localhost:9999/products with useEffect/useState, displayed in a table with columns Id, Title, Category, Price, Functions.|||Đúng URL (http://localhost:3000) và lấy TẤT CẢ sản phẩm từ http://localhost:9999/products bằng useEffect/useState, hiển thị trong bảng đủ cột Id, Title, Category, Price, Functions.",
              "weight": 2,
              "maxScore": 2
            },
            {
              "id": "c2",
              "criterion": "Search box 'Enter title search ...' filters the table live by title (case-insensitive contains), and each row renders a working View Details button.|||Ô tìm 'Enter title search ...' lọc bảng theo title theo thời gian thực (chứa, không phân biệt hoa thường), và mỗi dòng có nút View Details hoạt động.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "Right panel shows 'Please select a product!' when no product is selected in Reviews details.|||Khung bên phải hiển thị 'Please select a product!' khi chưa chọn sản phẩm nào trong Reviews details.",
              "weight": 1,
              "maxScore": 1
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE12-q1.png"
        },
        {
          "kind": "WRITE",
          "points": 6,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2. (6 marks)</b></p><p>When the user clicks the <b>View Details</b> button, the detailed information of the product is displayed in 'Reviews details' as shown in <b>Figure 3</b>.</p><pre><code><span class=\"hljs-section\">&lt;b&gt;</span><span class=\"hljs-attribute\">Figure</span> <span class=\"hljs-number\">3</span>&lt;/b&gt; (View Details product with Id = <span class=\"hljs-number\">2</span>) must display:\n<span class=\"hljs-attribute\">Reviews</span> details: ProductId: <span class=\"hljs-number\">2</span>, Title: Eyeshadow Palette with Mirror, Category: beauty, Price: <span class=\"hljs-number\">19</span>.<span class=\"hljs-number\">99</span>.\n<span class=\"hljs-attribute\">A</span> review table with columns &lt;b&gt;ReviewerName | Comment | Date | Rating&lt;/b&gt;, sample rows: Liam Garcia | Very satisfied! | <span class=\"hljs-number\">23</span>/<span class=\"hljs-number\">05</span>/<span class=\"hljs-number\">2024</span> | <span class=\"hljs-number\">4</span> ; Nora Russell | Very disappointed! | <span class=\"hljs-number\">23</span>/<span class=\"hljs-number\">05</span>/<span class=\"hljs-number\">2024</span> | <span class=\"hljs-number\">1</span> ; Elena Baker | Highly impressed! | <span class=\"hljs-number\">23</span>/<span class=\"hljs-number\">05</span>/<span class=\"hljs-number\">2024</span> | <span class=\"hljs-number\">5</span>. A &lt;b&gt;Clear Reviews&lt;/b&gt; button at the bottom right.</code></pre><p><b>Requirements:</b></p><ul><li>Display the correct product information including <b>id, title, category, price</b>.</li><li>Build the correct <b>Add a new Review</b> form.</li><li>Retrieve product reviews from the API end-point <code>http://localhost:9999/products/{id}</code> - <b>id</b> is the identifier of the current product and is displayed as a data table.</li><li>When the <b>Send Review</b> button is clicked, the validity of the data must be checked as shown in <b>Figure 4</b>: when Reviewer Name and Comment are left empty, show the error messages 'Reviewer Name is required' and 'Comment is required' below each input.</li><li>When a review is added to the product successfully, the data is displayed as shown in <b>Figure 5</b>, and at the same time the data in the Reviews table is reloaded. A dialog 'localhost:3000 says / Add Review successfully!' appears with an OK button. After clicking OK the review table gets the new row (e.g. 'Minh Anh | Great!!! | 08/12/2024 | 4', rating radio button chosen = 4).</li><li>Comparing database.json 'Before Add new Review' vs 'After Add new Review': products[0].reviews has an extra new object <code>{reviewerName: \"Minh Anh\", comment: \"Great!!!\", rating: \"4\", date: \"2024-12-07T18:53:57.125Z\"}</code>.</li><li>Click the <b>Clear Reviews</b> button and the results will be displayed as shown in <b>Figure 2</b> (the Reviews details panel returns to 'Please select a product!').</li></ul></div><div class=\"ml-vi\"><p><b>Câu 2. (6 điểm)</b></p><p>Khi người dùng bấm nút <b>View Details</b>, thông tin chi tiết của sản phẩm được hiển thị trong 'Reviews details' như ở <b>Hình 3</b>.</p><pre><code><span class=\"hljs-section\">&lt;b&gt;</span><span class=\"hljs-attribute\">H</span>ình <span class=\"hljs-number\">3</span>&lt;/b&gt; (View Details sản phẩm Id = <span class=\"hljs-number\">2</span>) phải hiển thị:\n<span class=\"hljs-attribute\">Reviews</span> details: ProductId: <span class=\"hljs-number\">2</span>, Title: Eyeshadow Palette with Mirror, Category: beauty, Price: <span class=\"hljs-number\">19</span>.<span class=\"hljs-number\">99</span>.\n<span class=\"hljs-attribute\">B</span>ảng review có các cột &lt;b&gt;ReviewerName | Comment | Date | Rating&lt;/b&gt;, dữ liệu mẫu: Liam Garcia | Very satisfied! | <span class=\"hljs-number\">23</span>/<span class=\"hljs-number\">05</span>/<span class=\"hljs-number\">2024</span> | <span class=\"hljs-number\">4</span> ; Nora Russell | Very disappointed! | <span class=\"hljs-number\">23</span>/<span class=\"hljs-number\">05</span>/<span class=\"hljs-number\">2024</span> | <span class=\"hljs-number\">1</span> ; Elena Baker | Highly impressed! | <span class=\"hljs-number\">23</span>/<span class=\"hljs-number\">05</span>/<span class=\"hljs-number\">2024</span> | <span class=\"hljs-number\">5</span>. Nút &lt;b&gt;Clear Reviews&lt;/b&gt; ở góc phải dưới.</code></pre><p><b>Yêu cầu:</b></p><ul><li>Hiển thị đúng thông tin sản phẩm gồm <b>id, title, category, price</b>.</li><li>Dựng đúng form <b>Add a new Review</b>.</li><li>Lấy review của sản phẩm từ endpoint API <code>http://localhost:9999/products/{id}</code> - <b>id</b> là định danh của sản phẩm hiện tại và được hiển thị dạng bảng dữ liệu.</li><li>Khi bấm nút <b>Send Review</b>, phải kiểm tra tính hợp lệ của dữ liệu như ở <b>Hình 4</b>: khi Reviewer Name và Comment để trống thì hiển thị thông báo lỗi 'Reviewer Name is required' và 'Comment is required' dưới mỗi input.</li><li>Khi thêm review vào sản phẩm thành công, dữ liệu hiển thị như ở <b>Hình 5</b>, đồng thời dữ liệu ở bảng Reviews được nạp lại. Hộp thoại 'localhost:3000 says / Add Review successfully!' xuất hiện với nút OK. Sau khi bấm OK, bảng review được bổ sung dòng mới (ví dụ 'Minh Anh | Great!!! | 08/12/2024 | 4', radio rating chọn = 4).</li><li>So sánh database.json 'Before Add new Review' và 'After Add new Review': products[0].reviews có thêm object mới <code>{reviewerName: \"Minh Anh\", comment: \"Great!!!\", rating: \"4\", date: \"2024-12-07T18:53:57.125Z\"}</code>.</li><li>Bấm nút <b>Clear Reviews</b> thì kết quả hiển thị như ở <b>Hình 2</b> (khung Reviews details trở về 'Please select a product!').</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>The full app: the left <code>ProductList</code> from Question 1 plus a <code>ReviewsDetails</code> panel. On <b>View Details</b> the panel shows id / title / category / price and the product's <code>reviews</code> in a table. <code>Send Review</code> validates that Reviewer Name and Comment are non-empty (else it renders 'Reviewer Name is required' / 'Comment is required'), then persists the new review by re-reading the product from <code>http://localhost:9999/products/{id}</code>, appending the object <code>{reviewerName, comment, rating, date}</code>, and PUT-ing it back so json-server writes database.json. It then <code>alert('Add Review successfully!')</code> and re-fetches so the table reloads. <b>Clear Reviews</b> deselects the product, returning to 'Please select a product!'.</p><pre><code>import React, { useState, useEffect } from \"react\";\nimport axios from \"axios\";\n\nconst API = \"http://localhost:9999/products\";\n\nfunction App() {\n  const [products, setProducts] = useState([]);\n  const [search, setSearch] = useState(\"\");\n  const [selected, setSelected] = useState(null);\n\n  // form state\n  const [reviewerName, setReviewerName] = useState(\"\");\n  const [comment, setComment] = useState(\"\");\n  const [rating, setRating] = useState(\"1\");\n  const [errors, setErrors] = useState({});\n\n  const loadProducts = () =&gt; axios.get(API).then((res) =&gt; setProducts(res.data));\n  useEffect(() =&gt; { loadProducts(); }, []);\n\n  const filtered = products.filter((p) =&gt;\n    p.title.toLowerCase().includes(search.toLowerCase())\n  );\n\n  // View Details -&gt; load the current product by id\n  const viewDetails = (id) =&gt; {\n    axios.get(`${API}/${id}`).then((res) =&gt; setSelected(res.data));\n  };\n\n  const validate = () =&gt; {\n    const e = {};\n    if (!reviewerName.trim()) e.reviewerName = \"Reviewer Name is required\";\n    if (!comment.trim()) e.comment = \"Comment is required\";\n    setErrors(e);\n    return Object.keys(e).length === 0;\n  };\n\n  const sendReview = () =&gt; {\n    if (!validate()) return;\n    const newReview = {\n      reviewerName,\n      comment,\n      rating, // string \"1\".. \"5\" from the radio group\n      date: new Date().toISOString(),\n    };\n    // re-read the product, append, then write back\n    axios.get(`${API}/${selected.id}`).then((res) =&gt; {\n      const updated = {\n        ...res.data,\n        reviews: [...(res.data.reviews || []), newReview],\n      };\n      axios.put(`${API}/${selected.id}`, updated).then(() =&gt; {\n        alert(\"Add Review successfully!\");\n        setReviewerName(\"\"); setComment(\"\"); setRating(\"1\"); setErrors({});\n        setSelected(updated); // reload the Reviews table\n        loadProducts();\n      });\n    });\n  };\n\n  const clearReviews = () =&gt; setSelected(null);\n\n  return (\n    &lt;div className=\"container mt-4\"&gt;\n      &lt;div className=\"row\"&gt;\n        &lt;div className=\"col-md-7\"&gt;\n          &lt;h3&gt;Product List&lt;/h3&gt;\n          &lt;input className=\"form-control mb-3\" placeholder=\"Enter title search ...\"\n            value={search} onChange={(e) =&gt; setSearch(e.target.value)} /&gt;\n          &lt;table className=\"table table-bordered\"&gt;\n            &lt;thead&gt;&lt;tr&gt;&lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Title&lt;/th&gt;&lt;th&gt;Category&lt;/th&gt;&lt;th&gt;Price&lt;/th&gt;&lt;th&gt;Functions&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;\n            &lt;tbody&gt;\n              {filtered.map((p) =&gt; (\n                &lt;tr key={p.id}&gt;\n                  &lt;td&gt;{p.id}&lt;/td&gt;&lt;td&gt;{p.title}&lt;/td&gt;&lt;td&gt;{p.category}&lt;/td&gt;&lt;td&gt;{p.price}&lt;/td&gt;\n                  &lt;td&gt;&lt;button className=\"btn btn-primary\" onClick={() =&gt; viewDetails(p.id)}&gt;View Details&lt;/button&gt;&lt;/td&gt;\n                &lt;/tr&gt;\n              ))}\n            &lt;/tbody&gt;\n          &lt;/table&gt;\n        &lt;/div&gt;\n\n        &lt;div className=\"col-md-5\"&gt;\n          &lt;h4&gt;Reviews details&lt;/h4&gt;\n          {!selected ? (\n            &lt;p&gt;Please select a product!&lt;/p&gt;\n          ) : (\n            &lt;div&gt;\n              &lt;p&gt;ProductId: {selected.id}&lt;/p&gt;\n              &lt;p&gt;Title: {selected.title}&lt;/p&gt;\n              &lt;p&gt;Category: {selected.category}&lt;/p&gt;\n              &lt;p&gt;Price: {selected.price}&lt;/p&gt;\n\n              &lt;table className=\"table table-sm table-bordered\"&gt;\n                &lt;thead&gt;&lt;tr&gt;&lt;th&gt;ReviewerName&lt;/th&gt;&lt;th&gt;Comment&lt;/th&gt;&lt;th&gt;Date&lt;/th&gt;&lt;th&gt;Rating&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;\n                &lt;tbody&gt;\n                  {(selected.reviews || []).map((r, i) =&gt; (\n                    &lt;tr key={i}&gt;\n                      &lt;td&gt;{r.reviewerName}&lt;/td&gt;&lt;td&gt;{r.comment}&lt;/td&gt;&lt;td&gt;{r.date}&lt;/td&gt;&lt;td&gt;{r.rating}&lt;/td&gt;\n                    &lt;/tr&gt;\n                  ))}\n                &lt;/tbody&gt;\n              &lt;/table&gt;\n              &lt;div className=\"text-end\"&gt;\n                &lt;button className=\"btn btn-secondary\" onClick={clearReviews}&gt;Clear Reviews&lt;/button&gt;\n              &lt;/div&gt;\n\n              &lt;h5 className=\"mt-3\"&gt;Add a new Review&lt;/h5&gt;\n              &lt;div className=\"mb-2\"&gt;\n                &lt;label&gt;Reviewer Name&lt;/label&gt;\n                &lt;input className=\"form-control\" value={reviewerName}\n                  onChange={(e) =&gt; setReviewerName(e.target.value)} /&gt;\n                {errors.reviewerName &amp;&amp; &lt;small className=\"text-danger\"&gt;{errors.reviewerName}&lt;/small&gt;}\n              &lt;/div&gt;\n              &lt;div className=\"mb-2\"&gt;\n                &lt;label&gt;Comment&lt;/label&gt;\n                &lt;input className=\"form-control\" value={comment}\n                  onChange={(e) =&gt; setComment(e.target.value)} /&gt;\n                {errors.comment &amp;&amp; &lt;small className=\"text-danger\"&gt;{errors.comment}&lt;/small&gt;}\n              &lt;/div&gt;\n              &lt;div className=\"mb-2\"&gt;\n                &lt;label className=\"me-2\"&gt;Rating:&lt;/label&gt;\n                {[1, 2, 3, 4, 5].map((n) =&gt; (\n                  &lt;label key={n} className=\"me-2\"&gt;\n                    &lt;input type=\"radio\" name=\"rating\" value={n}\n                      checked={rating === String(n)}\n                      onChange={(e) =&gt; setRating(e.target.value)} /&gt; {n}\n                  &lt;/label&gt;\n                ))}\n              &lt;/div&gt;\n              &lt;button className=\"btn btn-primary\" onClick={sendReview}&gt;Send Review&lt;/button&gt;\n            &lt;/div&gt;\n          )}\n        &lt;/div&gt;\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\n\nexport default App;</code></pre><p>Notes: <code>rating</code> is kept as a string to match the sample <code>rating: \"4\"</code> in database.json; <code>date</code> uses <code>new Date().toISOString()</code> to match <code>\"2024-12-07T18:53:57.125Z\"</code>. Using PUT on <code>/products/{id}</code> makes json-server persist the whole product (with the appended review) to database.json.</p></div><div class=\"ml-vi\"><p>Toàn bộ app: phần <code>ProductList</code> bên trái từ Câu 1 cùng khung <code>ReviewsDetails</code>. Khi bấm <b>View Details</b>, khung hiển thị id / title / category / price và <code>reviews</code> của sản phẩm dạng bảng. <code>Send Review</code> kiểm tra Reviewer Name và Comment không được rỗng (nếu rỗng thì hiện 'Reviewer Name is required' / 'Comment is required'), rồi lưu review mới bằng cách đọc lại sản phẩm từ <code>http://localhost:9999/products/{id}</code>, thêm object <code>{reviewerName, comment, rating, date}</code>, và PUT trả lại để json-server ghi vào database.json. Sau đó <code>alert('Add Review successfully!')</code> và nạp lại để bảng cập nhật. <b>Clear Reviews</b> bỏ chọn sản phẩm, trở về 'Please select a product!'.</p><pre><code>import React, { useState, useEffect } from \"react\";\nimport axios from \"axios\";\n\nconst API = \"http://localhost:9999/products\";\n\nfunction App() {\n  const [products, setProducts] = useState([]);\n  const [search, setSearch] = useState(\"\");\n  const [selected, setSelected] = useState(null);\n\n  // state cua form\n  const [reviewerName, setReviewerName] = useState(\"\");\n  const [comment, setComment] = useState(\"\");\n  const [rating, setRating] = useState(\"1\");\n  const [errors, setErrors] = useState({});\n\n  const loadProducts = () =&gt; axios.get(API).then((res) =&gt; setProducts(res.data));\n  useEffect(() =&gt; { loadProducts(); }, []);\n\n  const filtered = products.filter((p) =&gt;\n    p.title.toLowerCase().includes(search.toLowerCase())\n  );\n\n  // View Details -&gt; lay san pham hien tai theo id\n  const viewDetails = (id) =&gt; {\n    axios.get(`${API}/${id}`).then((res) =&gt; setSelected(res.data));\n  };\n\n  const validate = () =&gt; {\n    const e = {};\n    if (!reviewerName.trim()) e.reviewerName = \"Reviewer Name is required\";\n    if (!comment.trim()) e.comment = \"Comment is required\";\n    setErrors(e);\n    return Object.keys(e).length === 0;\n  };\n\n  const sendReview = () =&gt; {\n    if (!validate()) return;\n    const newReview = {\n      reviewerName,\n      comment,\n      rating, // chuoi \"1\"..\"5\" tu nhom radio\n      date: new Date().toISOString(),\n    };\n    // doc lai san pham, them review, roi ghi tra lai\n    axios.get(`${API}/${selected.id}`).then((res) =&gt; {\n      const updated = {\n        ...res.data,\n        reviews: [...(res.data.reviews || []), newReview],\n      };\n      axios.put(`${API}/${selected.id}`, updated).then(() =&gt; {\n        alert(\"Add Review successfully!\");\n        setReviewerName(\"\"); setComment(\"\"); setRating(\"1\"); setErrors({});\n        setSelected(updated); // nap lai bang Reviews\n        loadProducts();\n      });\n    });\n  };\n\n  const clearReviews = () =&gt; setSelected(null);\n\n  return (\n    &lt;div className=\"container mt-4\"&gt;\n      &lt;div className=\"row\"&gt;\n        &lt;div className=\"col-md-7\"&gt;\n          &lt;h3&gt;Product List&lt;/h3&gt;\n          &lt;input className=\"form-control mb-3\" placeholder=\"Enter title search ...\"\n            value={search} onChange={(e) =&gt; setSearch(e.target.value)} /&gt;\n          &lt;table className=\"table table-bordered\"&gt;\n            &lt;thead&gt;&lt;tr&gt;&lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Title&lt;/th&gt;&lt;th&gt;Category&lt;/th&gt;&lt;th&gt;Price&lt;/th&gt;&lt;th&gt;Functions&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;\n            &lt;tbody&gt;\n              {filtered.map((p) =&gt; (\n                &lt;tr key={p.id}&gt;\n                  &lt;td&gt;{p.id}&lt;/td&gt;&lt;td&gt;{p.title}&lt;/td&gt;&lt;td&gt;{p.category}&lt;/td&gt;&lt;td&gt;{p.price}&lt;/td&gt;\n                  &lt;td&gt;&lt;button className=\"btn btn-primary\" onClick={() =&gt; viewDetails(p.id)}&gt;View Details&lt;/button&gt;&lt;/td&gt;\n                &lt;/tr&gt;\n              ))}\n            &lt;/tbody&gt;\n          &lt;/table&gt;\n        &lt;/div&gt;\n\n        &lt;div className=\"col-md-5\"&gt;\n          &lt;h4&gt;Reviews details&lt;/h4&gt;\n          {!selected ? (\n            &lt;p&gt;Please select a product!&lt;/p&gt;\n          ) : (\n            &lt;div&gt;\n              &lt;p&gt;ProductId: {selected.id}&lt;/p&gt;\n              &lt;p&gt;Title: {selected.title}&lt;/p&gt;\n              &lt;p&gt;Category: {selected.category}&lt;/p&gt;\n              &lt;p&gt;Price: {selected.price}&lt;/p&gt;\n\n              &lt;table className=\"table table-sm table-bordered\"&gt;\n                &lt;thead&gt;&lt;tr&gt;&lt;th&gt;ReviewerName&lt;/th&gt;&lt;th&gt;Comment&lt;/th&gt;&lt;th&gt;Date&lt;/th&gt;&lt;th&gt;Rating&lt;/th&gt;&lt;/tr&gt;&lt;/thead&gt;\n                &lt;tbody&gt;\n                  {(selected.reviews || []).map((r, i) =&gt; (\n                    &lt;tr key={i}&gt;\n                      &lt;td&gt;{r.reviewerName}&lt;/td&gt;&lt;td&gt;{r.comment}&lt;/td&gt;&lt;td&gt;{r.date}&lt;/td&gt;&lt;td&gt;{r.rating}&lt;/td&gt;\n                    &lt;/tr&gt;\n                  ))}\n                &lt;/tbody&gt;\n              &lt;/table&gt;\n              &lt;div className=\"text-end\"&gt;\n                &lt;button className=\"btn btn-secondary\" onClick={clearReviews}&gt;Clear Reviews&lt;/button&gt;\n              &lt;/div&gt;\n\n              &lt;h5 className=\"mt-3\"&gt;Add a new Review&lt;/h5&gt;\n              &lt;div className=\"mb-2\"&gt;\n                &lt;label&gt;Reviewer Name&lt;/label&gt;\n                &lt;input className=\"form-control\" value={reviewerName}\n                  onChange={(e) =&gt; setReviewerName(e.target.value)} /&gt;\n                {errors.reviewerName &amp;&amp; &lt;small className=\"text-danger\"&gt;{errors.reviewerName}&lt;/small&gt;}\n              &lt;/div&gt;\n              &lt;div className=\"mb-2\"&gt;\n                &lt;label&gt;Comment&lt;/label&gt;\n                &lt;input className=\"form-control\" value={comment}\n                  onChange={(e) =&gt; setComment(e.target.value)} /&gt;\n                {errors.comment &amp;&amp; &lt;small className=\"text-danger\"&gt;{errors.comment}&lt;/small&gt;}\n              &lt;/div&gt;\n              &lt;div className=\"mb-2\"&gt;\n                &lt;label className=\"me-2\"&gt;Rating:&lt;/label&gt;\n                {[1, 2, 3, 4, 5].map((n) =&gt; (\n                  &lt;label key={n} className=\"me-2\"&gt;\n                    &lt;input type=\"radio\" name=\"rating\" value={n}\n                      checked={rating === String(n)}\n                      onChange={(e) =&gt; setRating(e.target.value)} /&gt; {n}\n                  &lt;/label&gt;\n                ))}\n              &lt;/div&gt;\n              &lt;button className=\"btn btn-primary\" onClick={sendReview}&gt;Send Review&lt;/button&gt;\n            &lt;/div&gt;\n          )}\n        &lt;/div&gt;\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\n\nexport default App;</code></pre><p>Ghi chú: giữ <code>rating</code> dạng chuỗi để khớp mẫu <code>rating: \"4\"</code> trong database.json; <code>date</code> dùng <code>new Date().toISOString()</code> để khớp <code>\"2024-12-07T18:53:57.125Z\"</code>. Dùng PUT lên <code>/products/{id}</code> để json-server ghi cả sản phẩm (kèm review vừa thêm) vào database.json.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "On View Details, the Reviews details panel shows the correct product info: ProductId (id), Title, Category, Price.|||Khi bấm View Details, khung Reviews details hiển thị đúng thông tin sản phẩm: ProductId (id), Title, Category, Price.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c2",
              "criterion": "Reviews loaded from http://localhost:9999/products/{id} shown in a table with columns ReviewerName | Comment | Date | Rating, plus a working Clear Reviews button that returns to 'Please select a product!'.|||Reviews lấy từ http://localhost:9999/products/{id} hiển thị trong bảng đủ cột ReviewerName | Comment | Date | Rating, kèm nút Clear Reviews hoạt động trả về 'Please select a product!'.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c3",
              "criterion": "Add a new Review form built correctly (Reviewer Name, Comment, Rating radio 1-5, Send Review) with required-field validation showing 'Reviewer Name is required' / 'Comment is required'.|||Form Add a new Review dựng đúng (Reviewer Name, Comment, Rating radio 1-5, Send Review) có kiểm tra trường bắt buộc hiển thị 'Reviewer Name is required' / 'Comment is required'.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c4",
              "criterion": "On valid submit, a new review object {reviewerName, comment, rating, date} is persisted to products[id].reviews (POST/PUT to json-server), an 'Add Review successfully!' alert shows, and the Reviews table reloads with the new row.|||Khi submit hợp lệ, object review mới {reviewerName, comment, rating, date} được ghi vào products[id].reviews (POST/PUT tới json-server), hiện alert 'Add Review successfully!', và bảng Reviews nạp lại kèm dòng mới.",
              "weight": 1,
              "maxScore": 1.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE12-q2.png"
        }
      ]
    }
  ]
};
