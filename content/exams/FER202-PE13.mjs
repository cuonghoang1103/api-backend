export default {
  "course": {
    "courseCode": "FER202"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE13-FA2024PE",
      "title": "FER202 — Practical Exam Đề 13 (FA 2024 - PE)|||FER202 — Đề thi thực hành số 13 (FA 2024 - PE)",
      "description": "React PE (WRITE): build components against a REST API, AI-graded.|||Đề PE React (viết): dựng component theo REST API, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>React / Front-end Development</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>React / Front-end Development</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 6,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p>React (FER202) Practical Exam. A json-server fake REST API runs from <code>database.json</code>: <code>json-server --watch database.json --port 9999</code> (0 points if it is not running). The React app runs with <code>npm start</code> at <code>http://localhost:3000</code>. Endpoints available: <code>/category</code>, <code>/brand</code>, <code>/product</code>. Use hooks (useState/useEffect), axios/fetch and react-router-dom. Remove <code>node_modules</code> before submitting.</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p>Bai thi thuc hanh React (FER202). Mot fake REST API json-server chay tu <code>database.json</code>: <code>json-server --watch database.json --port 9999</code> (0 diem neu khong chay json-server). Ung dung React chay bang <code>npm start</code> tai <code>http://localhost:3000</code>. Cac endpoint: <code>/category</code>, <code>/brand</code>, <code>/product</code>. Dung hooks (useState/useEffect), axios/fetch va react-router-dom. Xoa <code>node_modules</code> truoc khi nop bai.</p></div></div><div class=\"ml-en\"><p><b>Question 1 (6 points)</b></p><p>Build a component that talks to the json-server REST API to display the <b>List of Products</b> page at <code>http://localhost:3000</code> (0 points if the URL is wrong).</p><p><b>Requirements:</b></p><ul><li>Design the layout exactly as in Figure 1: left side has a <b>Categories</b> radio group (smartphones, home-decoration, laptops, skincare) and a <b>Brands</b> radio group (Apple, Samsung, OPPO, Huawei, Microsoft Surface, Infinix); right side shows a 4-column grid of product cards (0.5).</li><li>Fetch <code>http://localhost:9999/category</code> to build the Categories filter (0.5).</li><li>Fetch <code>http://localhost:9999/brand</code> to build the Brands filter (0.5).</li><li>Fetch <code>http://localhost:9999/product</code> to build the product list, 4 products per row (0.5).</li><li>Each card shows the product image (0.5); the fields title, price, discount (0.5); the brand name and category name (0.5).</li><li>Show a green <b>View details</b> button and the <b>New price</b> computed from Price and Discount (0.5). New price = Price - Price * Discount / 100.</li><li>When the user clicks a radio in <b>both</b> Categories and Brands, the product list is filtered by both conditions at the same time — e.g. Brand=Apple and Category=smartphones leaves only iPhone 9 and iPhone X (2 marks; deduct 1 if only one filter works at a time).</li></ul></div><div class=\"ml-vi\"><p><b>Câu 1 (6 điểm)</b></p><p>Xay dung mot component giao tiep voi REST API json-server de hien thi trang <b>List of Products</b> tai <code>http://localhost:3000</code> (0 diem neu sai URL).</p><p><b>Yeu cau:</b></p><ul><li>Thiet ke layout dung nhu Figure 1: ben trai co nhom radio <b>Categories</b> (smartphones, home-decoration, laptops, skincare) va nhom radio <b>Brands</b> (Apple, Samsung, OPPO, Huawei, Microsoft Surface, Infinix); ben phai la luoi product card 4 cot (0.5).</li><li>Goi <code>http://localhost:9999/category</code> de dung bo loc Categories (0.5).</li><li>Goi <code>http://localhost:9999/brand</code> de dung bo loc Brands (0.5).</li><li>Goi <code>http://localhost:9999/product</code> de dung danh sach san pham, 4 san pham moi hang (0.5).</li><li>Moi card hien anh san pham (0.5); cac truong title, price, discount (0.5); ten brand va ten category (0.5).</li><li>Hien nut <b>View details</b> mau xanh la va <b>New price</b> tinh tu Price va Discount (0.5). New price = Price - Price * Discount / 100.</li><li>Khi nguoi dung chon radio o <b>ca hai</b> Categories va Brands, danh sach san pham duoc loc theo ca hai dieu kien cung luc — vi du Brand=Apple va Category=smartphones chi con iPhone 9 va iPhone X (2 diem; tru 1 diem neu chi mot bo loc hoat dong tai mot thoi diem).</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><b>Explanation:</b> Fetch the three endpoints <code>/category</code>, <code>/brand</code>, <code>/product</code> with <code>useEffect</code>. Each filter is a group of radio buttons; the two selections are kept in state (<code>cat</code>, <code>brand</code>) and the product list is filtered by <b>both at the same time</b> with a single <code>filter()</code> that ANDs the two conditions. New price = Price - Price * Discount / 100. <i>View details</i> navigates to <code>/product/:id</code>.</p><pre><code>// index.js — cai dat Router\nimport React from &quot;react&quot;;\nimport ReactDOM from &quot;react-dom/client&quot;;\nimport { BrowserRouter, Routes, Route } from &quot;react-router-dom&quot;;\nimport ProductList from &quot;./ProductList&quot;;\nimport ProductDetail from &quot;./ProductDetail&quot;;\nimport &quot;bootstrap/dist/css/bootstrap.min.css&quot;;\n\nReactDOM.createRoot(document.getElementById(&quot;root&quot;)).render(\n  &lt;BrowserRouter&gt;\n    &lt;Routes&gt;\n      &lt;Route path=&quot;/&quot; element={&lt;ProductList /&gt;} /&gt;\n      &lt;Route path=&quot;/product/:id&quot; element={&lt;ProductDetail /&gt;} /&gt;\n    &lt;/Routes&gt;\n  &lt;/BrowserRouter&gt;\n);\n\n// ProductList.js\nimport { useEffect, useState } from &quot;react&quot;;\nimport axios from &quot;axios&quot;;\nimport { useNavigate } from &quot;react-router-dom&quot;;\n\nexport default function ProductList() {\n  const [categories, setCategories] = useState([]);\n  const [brands, setBrands] = useState([]);\n  const [products, setProducts] = useState([]);\n  const [cat, setCat] = useState(&quot;&quot;);     // category dang chon\n  const [brand, setBrand] = useState(&quot;&quot;); // brand dang chon\n  const nav = useNavigate();\n\n  useEffect(() =&gt; {\n    axios.get(&quot;http://localhost:9999/category&quot;).then((r) =&gt; setCategories(r.data));\n    axios.get(&quot;http://localhost:9999/brand&quot;).then((r) =&gt; setBrands(r.data));\n    axios.get(&quot;http://localhost:9999/product&quot;).then((r) =&gt; setProducts(r.data));\n  }, []);\n\n  // New price = Price - Price * Discount / 100\n  const newPrice = (p, d) =&gt; (p - (p * d) / 100).toFixed(2);\n\n  // Loc theo CA HAI bo loc cung luc (Category VA Brand)\n  const shown = products.filter(\n    (p) =&gt;\n      (cat === &quot;&quot; || p.category === cat) &amp;&amp;\n      (brand === &quot;&quot; || p.brand === brand)\n  );\n\n  return (\n    &lt;div className=&quot;container-fluid mt-3&quot;&gt;\n      &lt;h2&gt;List of Products&lt;/h2&gt;\n      &lt;div className=&quot;row&quot;&gt;\n        {/* Ben trai: bo loc */}\n        &lt;div className=&quot;col-3&quot;&gt;\n          &lt;h5&gt;Categories&lt;/h5&gt;\n          {categories.map((c) =&gt; (\n            &lt;div key={c.id} className=&quot;form-check&quot;&gt;\n              &lt;input\n                className=&quot;form-check-input&quot;\n                type=&quot;radio&quot;\n                name=&quot;category&quot;\n                value={c.name}\n                checked={cat === c.name}\n                onChange={(e) =&gt; setCat(e.target.value)}\n              /&gt;\n              &lt;label className=&quot;form-check-label&quot;&gt;{c.name}&lt;/label&gt;\n            &lt;/div&gt;\n          ))}\n\n          &lt;h5 className=&quot;mt-3&quot;&gt;Brands&lt;/h5&gt;\n          {brands.map((b) =&gt; (\n            &lt;div key={b.id} className=&quot;form-check&quot;&gt;\n              &lt;input\n                className=&quot;form-check-input&quot;\n                type=&quot;radio&quot;\n                name=&quot;brand&quot;\n                value={b.name}\n                checked={brand === b.name}\n                onChange={(e) =&gt; setBrand(e.target.value)}\n              /&gt;\n              &lt;label className=&quot;form-check-label&quot;&gt;{b.name}&lt;/label&gt;\n            &lt;/div&gt;\n          ))}\n        &lt;/div&gt;\n\n        {/* Ben phai: luoi san pham 4 cot moi hang */}\n        &lt;div className=&quot;col-9&quot;&gt;\n          &lt;div className=&quot;row&quot;&gt;\n            {shown.map((p) =&gt; (\n              &lt;div className=&quot;col-3 mb-3&quot; key={p.id}&gt;\n                &lt;div className=&quot;card h-100&quot;&gt;\n                  &lt;img src={p.thumbnail} className=&quot;card-img-top&quot; alt={p.title} /&gt;\n                  &lt;div className=&quot;card-body&quot;&gt;\n                    &lt;h6 className=&quot;card-title&quot;&gt;{p.title}&lt;/h6&gt;\n                    &lt;p className=&quot;mb-1&quot;&gt;Brand: {p.brand}&lt;/p&gt;\n                    &lt;p className=&quot;mb-1&quot;&gt;Category: {p.category}&lt;/p&gt;\n                    &lt;p className=&quot;mb-1&quot;&gt;\n                      Price: &lt;del&gt;{p.price}&lt;/del&gt;\n                    &lt;/p&gt;\n                    &lt;p className=&quot;mb-1&quot;&gt;Discount: {p.discount}%&lt;/p&gt;\n                    &lt;p className=&quot;mb-1 text-danger&quot;&gt;\n                      New price: {newPrice(p.price, p.discount)}\n                    &lt;/p&gt;\n                    &lt;button\n                      className=&quot;btn btn-success&quot;\n                      onClick={() =&gt; nav(`/product/${p.id}`)}\n                    &gt;\n                      View details\n                    &lt;/button&gt;\n                  &lt;/div&gt;\n                &lt;/div&gt;\n              &lt;/div&gt;\n            ))}\n          &lt;/div&gt;\n        &lt;/div&gt;\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\n</code></pre></div><div class=\"ml-vi\"><p><b>Giai thich:</b> Goi ba endpoint <code>/category</code>, <code>/brand</code>, <code>/product</code> bang <code>useEffect</code>. Moi bo loc la mot nhom radio button; hai lua chon luu trong state (<code>cat</code>, <code>brand</code>) va danh sach san pham duoc loc theo <b>ca hai cung luc</b> bang mot <code>filter()</code> nhan (AND) hai dieu kien. New price = Price - Price * Discount / 100. Nut <i>View details</i> dieu huong sang <code>/product/:id</code>.</p><pre><code>// index.js — cai dat Router\nimport React from &quot;react&quot;;\nimport ReactDOM from &quot;react-dom/client&quot;;\nimport { BrowserRouter, Routes, Route } from &quot;react-router-dom&quot;;\nimport ProductList from &quot;./ProductList&quot;;\nimport ProductDetail from &quot;./ProductDetail&quot;;\nimport &quot;bootstrap/dist/css/bootstrap.min.css&quot;;\n\nReactDOM.createRoot(document.getElementById(&quot;root&quot;)).render(\n  &lt;BrowserRouter&gt;\n    &lt;Routes&gt;\n      &lt;Route path=&quot;/&quot; element={&lt;ProductList /&gt;} /&gt;\n      &lt;Route path=&quot;/product/:id&quot; element={&lt;ProductDetail /&gt;} /&gt;\n    &lt;/Routes&gt;\n  &lt;/BrowserRouter&gt;\n);\n\n// ProductList.js\nimport { useEffect, useState } from &quot;react&quot;;\nimport axios from &quot;axios&quot;;\nimport { useNavigate } from &quot;react-router-dom&quot;;\n\nexport default function ProductList() {\n  const [categories, setCategories] = useState([]);\n  const [brands, setBrands] = useState([]);\n  const [products, setProducts] = useState([]);\n  const [cat, setCat] = useState(&quot;&quot;);     // category dang chon\n  const [brand, setBrand] = useState(&quot;&quot;); // brand dang chon\n  const nav = useNavigate();\n\n  useEffect(() =&gt; {\n    axios.get(&quot;http://localhost:9999/category&quot;).then((r) =&gt; setCategories(r.data));\n    axios.get(&quot;http://localhost:9999/brand&quot;).then((r) =&gt; setBrands(r.data));\n    axios.get(&quot;http://localhost:9999/product&quot;).then((r) =&gt; setProducts(r.data));\n  }, []);\n\n  // New price = Price - Price * Discount / 100\n  const newPrice = (p, d) =&gt; (p - (p * d) / 100).toFixed(2);\n\n  // Loc theo CA HAI bo loc cung luc (Category VA Brand)\n  const shown = products.filter(\n    (p) =&gt;\n      (cat === &quot;&quot; || p.category === cat) &amp;&amp;\n      (brand === &quot;&quot; || p.brand === brand)\n  );\n\n  return (\n    &lt;div className=&quot;container-fluid mt-3&quot;&gt;\n      &lt;h2&gt;List of Products&lt;/h2&gt;\n      &lt;div className=&quot;row&quot;&gt;\n        {/* Ben trai: bo loc */}\n        &lt;div className=&quot;col-3&quot;&gt;\n          &lt;h5&gt;Categories&lt;/h5&gt;\n          {categories.map((c) =&gt; (\n            &lt;div key={c.id} className=&quot;form-check&quot;&gt;\n              &lt;input\n                className=&quot;form-check-input&quot;\n                type=&quot;radio&quot;\n                name=&quot;category&quot;\n                value={c.name}\n                checked={cat === c.name}\n                onChange={(e) =&gt; setCat(e.target.value)}\n              /&gt;\n              &lt;label className=&quot;form-check-label&quot;&gt;{c.name}&lt;/label&gt;\n            &lt;/div&gt;\n          ))}\n\n          &lt;h5 className=&quot;mt-3&quot;&gt;Brands&lt;/h5&gt;\n          {brands.map((b) =&gt; (\n            &lt;div key={b.id} className=&quot;form-check&quot;&gt;\n              &lt;input\n                className=&quot;form-check-input&quot;\n                type=&quot;radio&quot;\n                name=&quot;brand&quot;\n                value={b.name}\n                checked={brand === b.name}\n                onChange={(e) =&gt; setBrand(e.target.value)}\n              /&gt;\n              &lt;label className=&quot;form-check-label&quot;&gt;{b.name}&lt;/label&gt;\n            &lt;/div&gt;\n          ))}\n        &lt;/div&gt;\n\n        {/* Ben phai: luoi san pham 4 cot moi hang */}\n        &lt;div className=&quot;col-9&quot;&gt;\n          &lt;div className=&quot;row&quot;&gt;\n            {shown.map((p) =&gt; (\n              &lt;div className=&quot;col-3 mb-3&quot; key={p.id}&gt;\n                &lt;div className=&quot;card h-100&quot;&gt;\n                  &lt;img src={p.thumbnail} className=&quot;card-img-top&quot; alt={p.title} /&gt;\n                  &lt;div className=&quot;card-body&quot;&gt;\n                    &lt;h6 className=&quot;card-title&quot;&gt;{p.title}&lt;/h6&gt;\n                    &lt;p className=&quot;mb-1&quot;&gt;Brand: {p.brand}&lt;/p&gt;\n                    &lt;p className=&quot;mb-1&quot;&gt;Category: {p.category}&lt;/p&gt;\n                    &lt;p className=&quot;mb-1&quot;&gt;\n                      Price: &lt;del&gt;{p.price}&lt;/del&gt;\n                    &lt;/p&gt;\n                    &lt;p className=&quot;mb-1&quot;&gt;Discount: {p.discount}%&lt;/p&gt;\n                    &lt;p className=&quot;mb-1 text-danger&quot;&gt;\n                      New price: {newPrice(p.price, p.discount)}\n                    &lt;/p&gt;\n                    &lt;button\n                      className=&quot;btn btn-success&quot;\n                      onClick={() =&gt; nav(`/product/${p.id}`)}\n                    &gt;\n                      View details\n                    &lt;/button&gt;\n                  &lt;/div&gt;\n                &lt;/div&gt;\n              &lt;/div&gt;\n            ))}\n          &lt;/div&gt;\n        &lt;/div&gt;\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\n</code></pre></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Layout matches Figure 1 (Categories + Brands radio groups on the left, 4-column product grid on the right) at URL localhost:3000.|||Layout dung Figure 1 (nhom radio Categories + Brands ben trai, luoi san pham 4 cot ben phai) tai URL localhost:3000.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Fetch /category and /brand to build the two radio filters correctly.|||Goi /category va /brand de dung dung hai bo loc radio.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "Fetch /product and render each card with image, title, price, discount, brand name, category name, New price and a View details button.|||Goi /product va render moi card voi anh, title, price, discount, ten brand, ten category, New price va nut View details.",
              "weight": 1,
              "maxScore": 2
            },
            {
              "id": "c4",
              "criterion": "Both filters (Category AND Brand) narrow the list simultaneously; New price = Price - Price*Discount/100.|||Ca hai bo loc (Category VA Brand) loc danh sach cung luc; New price = Price - Price*Discount/100.",
              "weight": 1,
              "maxScore": 2
            }
          ]
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2 (4 points)</b></p><p>When the user clicks <b>View details</b> in Figure 4, route to a component showing the product details at <code>http://localhost:3000/product/:id</code> as in Figure 6 (0 points if the URL is wrong).</p><p><b>Requirements:</b></p><ul><li>Design the correct layout (0.5).</li><li>Display the corresponding product image (0.5).</li><li>Display the fields title, description, price, discount, rating and stock (0.5).</li><li>Show the <b>New price</b> computed from Price and Discount (0.5). New price = Price - Price * Discount / 100.</li><li>Show two buttons: <b>Back to list</b> (green) and <b>Delete</b> (red) (0.5).</li><li>When the user clicks <b>Delete</b>, a confirm box appears like Figure 7 (message \"Do you want to delete this product?\" with Cancel and OK) (0.5).</li><li>On confirm, delete the product successfully and redirect to <code>http://localhost:3000</code> (1 mark).</li></ul></div><div class=\"ml-vi\"><p><b>Câu 2 (4 điểm)</b></p><p>Khi nguoi dung bam <b>View details</b> o Figure 4, dieu huong toi mot component hien thi chi tiet san pham tai <code>http://localhost:3000/product/:id</code> nhu Figure 6 (0 diem neu sai URL).</p><p><b>Yeu cau:</b></p><ul><li>Thiet ke layout dung (0.5).</li><li>Hien thi anh cua san pham tuong ung (0.5).</li><li>Hien thi cac truong title, description, price, discount, rating va stock (0.5).</li><li>Hien <b>New price</b> tinh tu Price va Discount (0.5). New price = Price - Price * Discount / 100.</li><li>Hien hai nut: <b>Back to list</b> (xanh la) va <b>Delete</b> (do) (0.5).</li><li>Khi bam <b>Delete</b>, hien hop xac nhan nhu Figure 7 (thong bao \"Do you want to delete this product?\" voi Cancel va OK) (0.5).</li><li>Khi xac nhan, xoa san pham thanh cong va dieu huong ve <code>http://localhost:3000</code> (1 diem).</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><b>Explanation:</b> Read <code>:id</code> with <code>useParams()</code>, fetch that single product, and render title, image, id, description, price (struck through), discount, rating, stock and the new price. <i>Back to list</i> goes to <code>/</code>. <i>Delete</i> shows <code>window.confirm</code> (Cancel/OK); on OK it sends an axios <code>DELETE</code> to json-server then redirects to <code>http://localhost:3000</code>.</p><pre><code>// ProductDetail.js\nimport { useEffect, useState } from &quot;react&quot;;\nimport axios from &quot;axios&quot;;\nimport { useParams, useNavigate } from &quot;react-router-dom&quot;;\n\nexport default function ProductDetail() {\n  const { id } = useParams();          // lay :id tu URL /product/:id\n  const [p, setP] = useState(null);\n  const nav = useNavigate();\n\n  useEffect(() =&gt; {\n    axios\n      .get(`http://localhost:9999/product/${id}`)\n      .then((r) =&gt; setP(r.data));\n  }, [id]);\n\n  if (!p) return &lt;p&gt;Loading...&lt;/p&gt;;\n\n  const newPrice = (price, d) =&gt; (price - (price * d) / 100).toFixed(2);\n\n  const handleDelete = () =&gt; {\n    // Hop thoai xac nhan (Figure 7): Cancel / OK\n    if (window.confirm(&quot;Do you want to delete this product?&quot;)) {\n      axios\n        .delete(`http://localhost:9999/product/${id}`)\n        .then(() =&gt; nav(&quot;/&quot;)); // xoa xong -&gt; ve http://localhost:3000\n    }\n  };\n\n  return (\n    &lt;div className=&quot;container mt-3&quot;&gt;\n      &lt;h2&gt;Product details: {p.title}&lt;/h2&gt;\n      &lt;img src={p.thumbnail} alt={p.title} width=&quot;300&quot; /&gt;\n      &lt;p&gt;Id: {p.id}&lt;/p&gt;\n      &lt;p&gt;Description: {p.description}&lt;/p&gt;\n      &lt;p&gt;\n        Price: &lt;del&gt;{p.price}&lt;/del&gt;\n      &lt;/p&gt;\n      &lt;p&gt;Discount: {p.discount}%&lt;/p&gt;\n      &lt;p className=&quot;text-danger&quot;&gt;New price: {newPrice(p.price, p.discount)}&lt;/p&gt;\n      &lt;p&gt;Rating: {p.rating}&lt;/p&gt;\n      &lt;p&gt;Stock: {p.stock}&lt;/p&gt;\n\n      &lt;button className=&quot;btn btn-success me-2&quot; onClick={() =&gt; nav(&quot;/&quot;)}&gt;\n        Back to list\n      &lt;/button&gt;\n      &lt;button className=&quot;btn btn-danger&quot; onClick={handleDelete}&gt;\n        Delete\n      &lt;/button&gt;\n    &lt;/div&gt;\n  );\n}\n</code></pre></div><div class=\"ml-vi\"><p><b>Giai thich:</b> Doc <code>:id</code> bang <code>useParams()</code>, goi API lay dung mot san pham va hien thi title, anh, id, description, price (gach ngang), discount, rating, stock va new price. Nut <i>Back to list</i> ve <code>/</code>. Nut <i>Delete</i> mo <code>window.confirm</code> (Cancel/OK); bam OK thi gui axios <code>DELETE</code> toi json-server roi dieu huong ve <code>http://localhost:3000</code>.</p><pre><code>// ProductDetail.js\nimport { useEffect, useState } from &quot;react&quot;;\nimport axios from &quot;axios&quot;;\nimport { useParams, useNavigate } from &quot;react-router-dom&quot;;\n\nexport default function ProductDetail() {\n  const { id } = useParams();          // lay :id tu URL /product/:id\n  const [p, setP] = useState(null);\n  const nav = useNavigate();\n\n  useEffect(() =&gt; {\n    axios\n      .get(`http://localhost:9999/product/${id}`)\n      .then((r) =&gt; setP(r.data));\n  }, [id]);\n\n  if (!p) return &lt;p&gt;Loading...&lt;/p&gt;;\n\n  const newPrice = (price, d) =&gt; (price - (price * d) / 100).toFixed(2);\n\n  const handleDelete = () =&gt; {\n    // Hop thoai xac nhan (Figure 7): Cancel / OK\n    if (window.confirm(&quot;Do you want to delete this product?&quot;)) {\n      axios\n        .delete(`http://localhost:9999/product/${id}`)\n        .then(() =&gt; nav(&quot;/&quot;)); // xoa xong -&gt; ve http://localhost:3000\n    }\n  };\n\n  return (\n    &lt;div className=&quot;container mt-3&quot;&gt;\n      &lt;h2&gt;Product details: {p.title}&lt;/h2&gt;\n      &lt;img src={p.thumbnail} alt={p.title} width=&quot;300&quot; /&gt;\n      &lt;p&gt;Id: {p.id}&lt;/p&gt;\n      &lt;p&gt;Description: {p.description}&lt;/p&gt;\n      &lt;p&gt;\n        Price: &lt;del&gt;{p.price}&lt;/del&gt;\n      &lt;/p&gt;\n      &lt;p&gt;Discount: {p.discount}%&lt;/p&gt;\n      &lt;p className=&quot;text-danger&quot;&gt;New price: {newPrice(p.price, p.discount)}&lt;/p&gt;\n      &lt;p&gt;Rating: {p.rating}&lt;/p&gt;\n      &lt;p&gt;Stock: {p.stock}&lt;/p&gt;\n\n      &lt;button className=&quot;btn btn-success me-2&quot; onClick={() =&gt; nav(&quot;/&quot;)}&gt;\n        Back to list\n      &lt;/button&gt;\n      &lt;button className=&quot;btn btn-danger&quot; onClick={handleDelete}&gt;\n        Delete\n      &lt;/button&gt;\n    &lt;/div&gt;\n  );\n}\n</code></pre></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Route /product/:id reads the id (useParams), fetches the single product, and lays it out like Figure 6 including the image.|||Route /product/:id doc id (useParams), goi lay dung mot san pham va bo cuc nhu Figure 6 gom ca anh.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Display title, description, price (struck through), discount, rating, stock and the correct New price.|||Hien title, description, price (gach ngang), discount, rating, stock va New price dung.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "Two buttons: Back to list (green, goes to /) and Delete (red).|||Hai nut: Back to list (xanh, ve /) va Delete (do).",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c4",
              "criterion": "Delete shows window.confirm (Cancel/OK); on OK sends axios DELETE and redirects to http://localhost:3000.|||Delete mo window.confirm (Cancel/OK); bam OK gui axios DELETE va dieu huong ve http://localhost:3000.",
              "weight": 1,
              "maxScore": 1
            }
          ]
        }
      ]
    }
  ]
};
