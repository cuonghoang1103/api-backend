export default {
  "course": {
    "courseCode": "FER202"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE14-PESU2024Bloc",
      "title": "FER202 — Practical Exam Đề 14 (PE - SU 2024 - Block 5)|||FER202 — Đề thi thực hành số 14 (PE - SU 2024 - Block 5)",
      "description": "React PE (WRITE): build components against a REST API, AI-graded.|||Đề PE React (viết): dựng component theo REST API, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 8.5,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>React / Front-end Development</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>React / Front-end Development</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 4.5,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p><b>FER202 – Practical Exam (PE) – SU 2024 – Block 5 – Đề 14.</b> A React single-page app named <b>Students Management</b> that reads data from <code>database.json</code> served by <b>json-server</b>. All URLs must be exactly as required (wrong URL = 0 points). Assumed collections in <code>database.json</code>: <code>students</code> (fields: <code>id, studentId, name, age, street, city, isRegularStudent</code>, an embedded <code>grades</code> array of <code>{grade, explanation}</code>, and a <code>subjects</code> array of subject ids) and <code>subjects</code> (<code>{id, name}</code>). Stack: React + react-router-dom v6 + fetch/axios. json-server runs at <code>http://localhost:9999</code> (adjust to the port given by the exam). The app itself runs at <code>http://localhost:3000</code>.</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p><b>FER202 – Thi thực hành (PE) – SU 2024 – Block 5 – Đề 14.</b> Ứng dụng React một trang tên <b>Students Management</b> đọc dữ liệu từ <code>database.json</code> do <b>json-server</b> phục vụ. Mọi URL phải đúng chính xác theo yêu cầu (sai URL = 0 điểm). Giả định các collection trong <code>database.json</code>: <code>students</code> (các trường: <code>id, studentId, name, age, street, city, isRegularStudent</code>, một mảng <code>grades</code> lồng gồm <code>{grade, explanation}</code>, và một mảng <code>subjects</code> chứa id môn học) và <code>subjects</code> (<code>{id, name}</code>). Công nghệ: React + react-router-dom v6 + fetch/axios. json-server chạy tại <code>http://localhost:9999</code> (đổi theo cổng đề cho). Ứng dụng chạy tại <code>http://localhost:3000</code>.</p></div></div><div class=\"ml-en\"><p><b>Question 1 (4.5 points)</b></p><p><b>Question 1 (4.5 points). At URL <code>http://localhost:3000</code></b>, load data from the database to the component as shown in Figure 1.</p><p><b>Figure 1</b> – Page \"Students Management\". A search box \"Enter student name to search ...\". On the left, a <b>Subjects</b> block (link list: Programming Fundamental, Programming Object Oriented, Database Introduction, Operating System, Programming Java Web). On the right, title \"List of Students\" with a table whose columns are: StudentId, Name, Age, Street, City, IsRegularStudent, View grades. Each row has a <b>Grades</b> link in the \"View grades\" column. Footer: \"Copyright by: HExxxxxxx\". Sample data: HE1710001 Phạm Hoàng Minh / 20 / 190 Nguyễn Tuân, Thanh Xuân / Hà Nội / Fulltime; HE1710002 Ngô Thị Hồng / 19 / Liên Minh, Vụ Bản / Nam Định / Applicant; HE1710003 Trần Hải Nam / 21 / 56 Trung Hòa, Cầu Giấy / Hà Nội / Fulltime; HE1710004 Trần Thu Hường / 20 / 56 Quang Trung, Phủ Lý / Hà Nam / Applicant.</p><ul><li>Design the layout as required (0.5 marks).</li><li>Design the search form correctly (0.5 marks).</li><li>Load all subjects from the database and display the subject names, as in Figure 2 (1 mark).</li><li>Design the table with the correct format (0.5 marks).</li><li>Display the correct data fields: StudentId, Name, Age (0.5 marks).</li><li>Display the correct data fields: street, city (1 mark).</li><li>Show the correct data field isRegularStudent. If isRegularStudent = true, display \"Fulltime\"; otherwise display \"Applicant\" (0.25 marks).</li><li>Show the correct link in the \"View grades\" column (0.25 marks).</li></ul></div><div class=\"ml-vi\"><p><b>Câu 1 (4.5 điểm)</b></p><p><b>Câu 1 (4.5 điểm). Tại URL <code>http://localhost:3000</code></b>, nạp dữ liệu từ database vào component như Figure 1.</p><p><b>Figure 1</b> – Trang \"Students Management\". Có ô tìm kiếm \"Enter student name to search ...\". Bên trái là khối <b>Subjects</b> (danh sách link: Programming Fundamental, Programming Object Oriented, Database Introduction, Operating System, Programming Java Web). Bên phải là tiêu đề \"List of Students\" với bảng gồm các cột: StudentId, Name, Age, Street, City, IsRegularStudent, View grades. Mỗi dòng có link <b>Grades</b> ở cột \"View grades\". Footer: \"Copyright by: HExxxxxxx\". Dữ liệu mẫu: HE1710001 Phạm Hoàng Minh / 20 / 190 Nguyễn Tuân, Thanh Xuân / Hà Nội / Fulltime; HE1710002 Ngô Thị Hồng / 19 / Liên Minh, Vụ Bản / Nam Định / Applicant; HE1710003 Trần Hải Nam / 21 / 56 Trung Hòa, Cầu Giấy / Hà Nội / Fulltime; HE1710004 Trần Thu Hường / 20 / 56 Quang Trung, Phủ Lý / Hà Nam / Applicant.</p><ul><li>Thiết kế layout đúng yêu cầu (0.5 điểm).</li><li>Thiết kế form tìm kiếm đúng (0.5 điểm).</li><li>Nạp toàn bộ subjects từ database và hiển thị tên môn, như Figure 2 (1 điểm).</li><li>Thiết kế bảng đúng định dạng (0.5 điểm).</li><li>Hiển thị đúng các trường: StudentId, Name, Age (0.5 điểm).</li><li>Hiển thị đúng các trường: street, city (1 điểm).</li><li>Hiển thị đúng trường isRegularStudent. Nếu isRegularStudent = true hiển thị \"Fulltime\"; ngược lại hiển thị \"Applicant\" (0.25 điểm).</li><li>Hiển thị đúng link ở cột \"View grades\" (0.25 điểm).</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Route <code>/</code> renders <code>StudentsManagement</code>. It loads <code>subjects</code> and <code>students</code> once on mount with <code>useEffect</code>, renders the subjects as router <code>Link</code>s, and the students in a table. <code>isRegularStudent</code> is mapped to Fulltime/Applicant with a ternary; the View grades link points to <code>/student/&lt;studentId&gt;</code>.</p><pre><code>// App.js\nimport { BrowserRouter, Routes, Route } from 'react-router-dom';\nimport StudentsManagement from './StudentsManagement';\nimport StudentGrades from './StudentGrades';\n\nexport default function App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/\" element={&lt;StudentsManagement /&gt;} /&gt;\n        &lt;Route path=\"/student/:studentid\" element={&lt;StudentGrades /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\n\n// StudentsManagement.js\nimport { useEffect, useState } from 'react';\nimport { Link } from 'react-router-dom';\n\nconst API = 'http://localhost:9999';\n\nexport default function StudentsManagement() {\n  const [subjects, setSubjects] = useState([]);\n  const [students, setStudents] = useState([]);\n  const [keyword, setKeyword] = useState('');\n\n  useEffect(() =&gt; {\n    fetch(`${API}/subjects`).then(r =&gt; r.json()).then(setSubjects);\n    fetch(`${API}/students`).then(r =&gt; r.json()).then(setStudents);\n  }, []);\n\n  return (\n    &lt;div className=\"container-fluid mt-3\"&gt;\n      &lt;h3&gt;Students Management&lt;/h3&gt;\n      &lt;form onSubmit={e =&gt; e.preventDefault()}&gt;\n        &lt;input\n          className=\"form-control w-50\"\n          placeholder=\"Enter student name to search ...\"\n          value={keyword}\n          onChange={e =&gt; setKeyword(e.target.value)}\n        /&gt;\n      &lt;/form&gt;\n      &lt;div className=\"row mt-3\"&gt;\n        &lt;div className=\"col-3\"&gt;\n          &lt;h5&gt;Subjects&lt;/h5&gt;\n          &lt;ul className=\"list-unstyled\"&gt;\n            {subjects.map(s =&gt; (\n              &lt;li key={s.id}&gt;\n                &lt;Link to={`/student?subject=${s.id}`}&gt;{s.name}&lt;/Link&gt;\n              &lt;/li&gt;\n            ))}\n          &lt;/ul&gt;\n        &lt;/div&gt;\n        &lt;div className=\"col-9\"&gt;\n          &lt;h5&gt;List of Students&lt;/h5&gt;\n          &lt;table className=\"table table-bordered\"&gt;\n            &lt;thead&gt;\n              &lt;tr&gt;\n                &lt;th&gt;StudentId&lt;/th&gt;&lt;th&gt;Name&lt;/th&gt;&lt;th&gt;Age&lt;/th&gt;\n                &lt;th&gt;Street&lt;/th&gt;&lt;th&gt;City&lt;/th&gt;\n                &lt;th&gt;IsRegularStudent&lt;/th&gt;&lt;th&gt;View grades&lt;/th&gt;\n              &lt;/tr&gt;\n            &lt;/thead&gt;\n            &lt;tbody&gt;\n              {students.map(st =&gt; (\n                &lt;tr key={st.id}&gt;\n                  &lt;td&gt;{st.studentId}&lt;/td&gt;\n                  &lt;td&gt;{st.name}&lt;/td&gt;\n                  &lt;td&gt;{st.age}&lt;/td&gt;\n                  &lt;td&gt;{st.street}&lt;/td&gt;\n                  &lt;td&gt;{st.city}&lt;/td&gt;\n                  &lt;td&gt;{st.isRegularStudent ? 'Fulltime' : 'Applicant'}&lt;/td&gt;\n                  &lt;td&gt;&lt;Link to={`/student/${st.studentId}`}&gt;Grades&lt;/Link&gt;&lt;/td&gt;\n                &lt;/tr&gt;\n              ))}\n            &lt;/tbody&gt;\n          &lt;/table&gt;\n        &lt;/div&gt;\n      &lt;/div&gt;\n      &lt;footer className=\"text-muted\"&gt;Copyright by: HExxxxxxx&lt;/footer&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div><div class=\"ml-vi\"><p>Route <code>/</code> render <code>StudentsManagement</code>. Component nạp <code>subjects</code> và <code>students</code> một lần khi mount bằng <code>useEffect</code>, render subjects thành các <code>Link</code> của router và students trong một bảng. <code>isRegularStudent</code> được ánh xạ sang Fulltime/Applicant bằng toán tử ba ngôi; link View grades trỏ tới <code>/student/&lt;studentId&gt;</code>.</p><pre><code>// App.js\nimport { BrowserRouter, Routes, Route } from 'react-router-dom';\nimport StudentsManagement from './StudentsManagement';\nimport StudentGrades from './StudentGrades';\n\nexport default function App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/\" element={&lt;StudentsManagement /&gt;} /&gt;\n        &lt;Route path=\"/student/:studentid\" element={&lt;StudentGrades /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\n\n// StudentsManagement.js\nimport { useEffect, useState } from 'react';\nimport { Link } from 'react-router-dom';\n\nconst API = 'http://localhost:9999';\n\nexport default function StudentsManagement() {\n  const [subjects, setSubjects] = useState([]);\n  const [students, setStudents] = useState([]);\n  const [keyword, setKeyword] = useState('');\n\n  useEffect(() =&gt; {\n    fetch(`${API}/subjects`).then(r =&gt; r.json()).then(setSubjects);\n    fetch(`${API}/students`).then(r =&gt; r.json()).then(setStudents);\n  }, []);\n\n  return (\n    &lt;div className=\"container-fluid mt-3\"&gt;\n      &lt;h3&gt;Students Management&lt;/h3&gt;\n      &lt;form onSubmit={e =&gt; e.preventDefault()}&gt;\n        &lt;input\n          className=\"form-control w-50\"\n          placeholder=\"Enter student name to search ...\"\n          value={keyword}\n          onChange={e =&gt; setKeyword(e.target.value)}\n        /&gt;\n      &lt;/form&gt;\n      &lt;div className=\"row mt-3\"&gt;\n        &lt;div className=\"col-3\"&gt;\n          &lt;h5&gt;Subjects&lt;/h5&gt;\n          &lt;ul className=\"list-unstyled\"&gt;\n            {subjects.map(s =&gt; (\n              &lt;li key={s.id}&gt;\n                &lt;Link to={`/student?subject=${s.id}`}&gt;{s.name}&lt;/Link&gt;\n              &lt;/li&gt;\n            ))}\n          &lt;/ul&gt;\n        &lt;/div&gt;\n        &lt;div className=\"col-9\"&gt;\n          &lt;h5&gt;List of Students&lt;/h5&gt;\n          &lt;table className=\"table table-bordered\"&gt;\n            &lt;thead&gt;\n              &lt;tr&gt;\n                &lt;th&gt;StudentId&lt;/th&gt;&lt;th&gt;Name&lt;/th&gt;&lt;th&gt;Age&lt;/th&gt;\n                &lt;th&gt;Street&lt;/th&gt;&lt;th&gt;City&lt;/th&gt;\n                &lt;th&gt;IsRegularStudent&lt;/th&gt;&lt;th&gt;View grades&lt;/th&gt;\n              &lt;/tr&gt;\n            &lt;/thead&gt;\n            &lt;tbody&gt;\n              {students.map(st =&gt; (\n                &lt;tr key={st.id}&gt;\n                  &lt;td&gt;{st.studentId}&lt;/td&gt;\n                  &lt;td&gt;{st.name}&lt;/td&gt;\n                  &lt;td&gt;{st.age}&lt;/td&gt;\n                  &lt;td&gt;{st.street}&lt;/td&gt;\n                  &lt;td&gt;{st.city}&lt;/td&gt;\n                  &lt;td&gt;{st.isRegularStudent ? 'Fulltime' : 'Applicant'}&lt;/td&gt;\n                  &lt;td&gt;&lt;Link to={`/student/${st.studentId}`}&gt;Grades&lt;/Link&gt;&lt;/td&gt;\n                &lt;/tr&gt;\n              ))}\n            &lt;/tbody&gt;\n          &lt;/table&gt;\n        &lt;/div&gt;\n      &lt;/div&gt;\n      &lt;footer className=\"text-muted\"&gt;Copyright by: HExxxxxxx&lt;/footer&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Layout designed as required and search form present/working (controlled input with the required placeholder).|||Layout đúng yêu cầu và có form tìm kiếm hoạt động (input controlled với đúng placeholder).",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "All subjects loaded from the database and subject names displayed as links.|||Nạp toàn bộ subjects từ database và hiển thị tên môn dưới dạng link.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "Table has correct format and shows StudentId, Name, Age, street, city correctly.|||Bảng đúng định dạng và hiển thị đúng StudentId, Name, Age, street, city.",
              "weight": 1,
              "maxScore": 2
            },
            {
              "id": "c4",
              "criterion": "isRegularStudent shown as Fulltime/Applicant, and the View grades link is correct.|||isRegularStudent hiển thị Fulltime/Applicant, và link View grades đúng.",
              "weight": 1,
              "maxScore": 0.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE14.png"
        },
        {
          "kind": "WRITE",
          "points": 1,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2 (1 point)</b></p><p><b>Question 2 (1 point). When the user clicks the \"Grades\" link of a student</b>, navigate to the URL <code>http://localhost:3000/student/:studentid</code>, as shown in Figure 4.</p><p><b>Figure 4</b> – <code>http://localhost:3000/student/HE1710001</code>: keep the Students Management layout + a <b>Back to Home</b> button (green) + show \"Phạm Hoàng Minh's Grade Details:\" with an \"Add a new grade\" form (input Enter grade, input Enter additional explanation, blue <b>Add new</b> button). Below is a table with 2 columns Grade | Explanation. Sample data: 7.5 / PRF192 course; 7 / PRO192 course; 7.5 / DBI202 course.</p><ul><li>Design the \"Back to Home\" button correctly and navigate to <code>http://localhost:3000</code> when chosen (0.25 marks).</li><li>Display the student's name correctly on the detailed transcript (0.25 marks).</li><li>Load the entire student's score based on the corresponding studentId (0.5 marks).</li></ul></div><div class=\"ml-vi\"><p><b>Câu 2 (1 điểm)</b></p><p><b>Câu 2 (1 điểm). Khi người dùng click link \"Grades\" của một student</b>, điều hướng tới URL <code>http://localhost:3000/student/:studentid</code>, như Figure 4.</p><p><b>Figure 4</b> – <code>http://localhost:3000/student/HE1710001</code>: Giữ nguyên layout Students Management + nút <b>Back to Home</b> (màu xanh lá) + hiển thị \"Phạm Hoàng Minh's Grade Details:\" với form \"Add a new grade\" (input Enter grade, input Enter additional explanation, nút <b>Add new</b> màu xanh). Bảng phía dưới có 2 cột Grade | Explanation. Dữ liệu mẫu: 7.5 / PRF192 course; 7 / PRO192 course; 7.5 / DBI202 course.</p><ul><li>Thiết kế nút \"Back to Home\" đúng và điều hướng về <code>http://localhost:3000</code> khi chọn (0.25 điểm).</li><li>Hiển thị đúng tên student trên trang bảng điểm chi tiết (0.25 điểm).</li><li>Nạp toàn bộ điểm của student theo studentId tương ứng (0.5 điểm).</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><code>StudentGrades</code> reads the <code>:studentid</code> route param with <code>useParams</code>, fetches the matching student via <code>students?studentId=&lt;id&gt;</code> (json-server filter, returns an array — take element 0), then renders the name, the grades table, and a green <b>Back to Home</b> link to <code>/</code>. The add form is shown here too (its behaviour is graded in Question 4).</p><pre><code>// StudentGrades.js\nimport { useEffect, useState } from 'react';\nimport { useParams, Link } from 'react-router-dom';\n\nconst API = 'http://localhost:9999';\n\nexport default function StudentGrades() {\n  const { studentid } = useParams();\n  const [student, setStudent] = useState(null);\n\n  useEffect(() =&gt; {\n    fetch(`${API}/students?studentId=${studentid}`)\n      .then(r =&gt; r.json())\n      .then(data =&gt; setStudent(data[0]));\n  }, [studentid]);\n\n  if (!student) return &lt;p&gt;Loading...&lt;/p&gt;;\n\n  return (\n    &lt;div className=\"container mt-3\"&gt;\n      &lt;Link to=\"/\" className=\"btn btn-success mb-3\"&gt;Back to Home&lt;/Link&gt;\n      &lt;h3&gt;{student.name}'s Grade Details:&lt;/h3&gt;\n\n      {/* Add a new grade form — logic in Question 4 */}\n\n      &lt;table className=\"table table-bordered w-50\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;&lt;th&gt;Grade&lt;/th&gt;&lt;th&gt;Explanation&lt;/th&gt;&lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {(student.grades || []).map((g, i) =&gt; (\n            &lt;tr key={i}&gt;\n              &lt;td&gt;{g.grade}&lt;/td&gt;\n              &lt;td&gt;{g.explanation}&lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div><div class=\"ml-vi\"><p><code>StudentGrades</code> đọc param route <code>:studentid</code> bằng <code>useParams</code>, lấy student khớp qua <code>students?studentId=&lt;id&gt;</code> (bộ lọc json-server trả về mảng — lấy phần tử 0), rồi render tên, bảng điểm và nút <b>Back to Home</b> màu xanh lá trỏ về <code>/</code>. Form thêm điểm cũng hiển thị ở đây (hành vi của nó được chấm ở Câu 4).</p><pre><code>// StudentGrades.js\nimport { useEffect, useState } from 'react';\nimport { useParams, Link } from 'react-router-dom';\n\nconst API = 'http://localhost:9999';\n\nexport default function StudentGrades() {\n  const { studentid } = useParams();\n  const [student, setStudent] = useState(null);\n\n  useEffect(() =&gt; {\n    fetch(`${API}/students?studentId=${studentid}`)\n      .then(r =&gt; r.json())\n      .then(data =&gt; setStudent(data[0]));\n  }, [studentid]);\n\n  if (!student) return &lt;p&gt;Loading...&lt;/p&gt;;\n\n  return (\n    &lt;div className=\"container mt-3\"&gt;\n      &lt;Link to=\"/\" className=\"btn btn-success mb-3\"&gt;Back to Home&lt;/Link&gt;\n      &lt;h3&gt;{student.name}'s Grade Details:&lt;/h3&gt;\n\n      {/* Form Add a new grade — logic ở Câu 4 */}\n\n      &lt;table className=\"table table-bordered w-50\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;&lt;th&gt;Grade&lt;/th&gt;&lt;th&gt;Explanation&lt;/th&gt;&lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {(student.grades || []).map((g, i) =&gt; (\n            &lt;tr key={i}&gt;\n              &lt;td&gt;{g.grade}&lt;/td&gt;\n              &lt;td&gt;{g.explanation}&lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Route /student/:studentid works; \"Back to Home\" button navigates to http://localhost:3000.|||Route /student/:studentid hoạt động; nút \"Back to Home\" điều hướng về http://localhost:3000.",
              "weight": 1,
              "maxScore": 0.25
            },
            {
              "id": "c2",
              "criterion": "Student's name displayed correctly on the detail page (\"<name>'s Grade Details:\").|||Hiển thị đúng tên student trên trang chi tiết (\"<tên>'s Grade Details:\").",
              "weight": 1,
              "maxScore": 0.25
            },
            {
              "id": "c3",
              "criterion": "All of the student's grades loaded by the corresponding studentId and shown in the Grade/Explanation table.|||Nạp toàn bộ điểm của student theo studentId tương ứng và hiển thị trong bảng Grade/Explanation.",
              "weight": 1,
              "maxScore": 0.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE14.png"
        },
        {
          "kind": "WRITE",
          "points": 1.5,
          "prompt": "<div class=\"ml-en\"><p><b>Question 3 (1.5 points)</b></p><p><b>Question 3 (1.5 points). Filter by name (search) and by subject.</b></p><p><b>Figure 5 – Example: Filter by \"tr\" keyword:</b> When the user types \"tr\" into the search box, the table shows only students whose name contains \"tr\" (HE1710003 Trần Hải Nam, HE1710004 Trần Thu Hường).</p><ul><li>When the user clicks a subject's name link, the list of students is filtered by subjectid and mounted on the URL <code>http://localhost:3000/student?subject=subjectid</code>, as shown in Figure 6 (1 mark). <b>Figure 6</b> – <code>http://localhost:3000/student?subject=PRF192</code>: the List of Students is filtered by subject PRF192 (HE1710001 Phạm Hoàng Minh, HE1710002 Ngô Thị Hồng).</li><li>Combine the data filter by <b>subject</b> and <b>form search</b>, as shown in Figure 7 (0.5 marks). <b>Figure 7</b>: URL has subject=PRF192 and the search box contains \"n\" → only student HE1710002 Ngô Thị Hồng is shown (has \"n\" in the name and studies PRF192).</li></ul></div><div class=\"ml-vi\"><p><b>Câu 3 (1.5 điểm)</b></p><p><b>Câu 3 (1.5 điểm). Lọc theo tên (search) và theo môn học (subject).</b></p><p><b>Figure 5 – Ví dụ: Lọc theo từ khóa \"tr\":</b> Khi người dùng gõ \"tr\" vào ô tìm kiếm, bảng chỉ hiển thị các student có name chứa \"tr\" (HE1710003 Trần Hải Nam, HE1710004 Trần Thu Hường).</p><ul><li>Khi người dùng click vào link tên môn, danh sách student được lọc theo subjectid và gắn lên URL <code>http://localhost:3000/student?subject=subjectid</code>, như Figure 6 (1 điểm). <b>Figure 6</b> – <code>http://localhost:3000/student?subject=PRF192</code>: Bảng List of Students được lọc theo subject PRF192 (HE1710001 Phạm Hoàng Minh, HE1710002 Ngô Thị Hồng).</li><li>Kết hợp lọc dữ liệu theo <b>subject</b> và <b>form search</b>, như Figure 7 (0.5 điểm). <b>Figure 7</b>: URL có subject=PRF192, đồng thời ô search chứa \"n\" → chỉ hiển thị student HE1710002 Ngô Thị Hồng (có \"n\" trong tên và học PRF192).</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>The subject filter comes from the query string, read with <code>useSearchParams</code>; the name filter comes from the controlled <code>keyword</code> state. Both are combined in one <code>filter</code>: a student passes when (no subject param OR its <code>subjects</code> array includes the param) AND (no keyword OR its name contains the keyword, case-insensitive). This replaces the plain <code>students.map</code> from Question 1.</p><pre><code>// inside StudentsManagement.js\nimport { Link, useSearchParams } from 'react-router-dom';\n\n// ...state: subjects, students, keyword as before\nconst [searchParams] = useSearchParams();\nconst subjectId = searchParams.get('subject'); // null when absent\n\nconst visible = students.filter(st =&gt; {\n  const bySubject = !subjectId ||\n    (st.subjects || []).includes(subjectId);\n  const byName = !keyword ||\n    st.name.toLowerCase().includes(keyword.toLowerCase());\n  return bySubject &amp;&amp; byName;\n});\n\n// subject links carry the id into the query string:\n// &lt;Link to={`/student?subject=${s.id}`}&gt;{s.name}&lt;/Link&gt;\n\n// render {visible.map(...)} instead of {students.map(...)}\nreturn (\n  &lt;tbody&gt;\n    {visible.map(st =&gt; (\n      &lt;tr key={st.id}&gt;\n        &lt;td&gt;{st.studentId}&lt;/td&gt;\n        &lt;td&gt;{st.name}&lt;/td&gt;\n        &lt;td&gt;{st.age}&lt;/td&gt;\n        &lt;td&gt;{st.street}&lt;/td&gt;\n        &lt;td&gt;{st.city}&lt;/td&gt;\n        &lt;td&gt;{st.isRegularStudent ? 'Fulltime' : 'Applicant'}&lt;/td&gt;\n        &lt;td&gt;&lt;Link to={`/student/${st.studentId}`}&gt;Grades&lt;/Link&gt;&lt;/td&gt;\n      &lt;/tr&gt;\n    ))}\n  &lt;/tbody&gt;\n);</code></pre><p>Note: for the URL to read <code>/student?subject=PRF192</code>, the same list component must also be mounted on the <code>/student</code> route (add <code>&lt;Route path=\"/student\" element={&lt;StudentsManagement /&gt;} /&gt;</code>), or render the list at that path.</p></div><div class=\"ml-vi\"><p>Bộ lọc subject lấy từ query string, đọc bằng <code>useSearchParams</code>; bộ lọc tên lấy từ state controlled <code>keyword</code>. Cả hai kết hợp trong một <code>filter</code>: một student được giữ khi (không có param subject HOẶC mảng <code>subjects</code> của nó chứa param) VÀ (không có keyword HOẶC name chứa keyword, không phân biệt hoa thường). Đây thay cho <code>students.map</code> đơn thuần ở Câu 1.</p><pre><code>// trong StudentsManagement.js\nimport { Link, useSearchParams } from 'react-router-dom';\n\n// ...state: subjects, students, keyword như trước\nconst [searchParams] = useSearchParams();\nconst subjectId = searchParams.get('subject'); // null khi không có\n\nconst visible = students.filter(st =&gt; {\n  const bySubject = !subjectId ||\n    (st.subjects || []).includes(subjectId);\n  const byName = !keyword ||\n    st.name.toLowerCase().includes(keyword.toLowerCase());\n  return bySubject &amp;&amp; byName;\n});\n\n// link môn gắn id vào query string:\n// &lt;Link to={`/student?subject=${s.id}`}&gt;{s.name}&lt;/Link&gt;\n\n// render {visible.map(...)} thay cho {students.map(...)}\nreturn (\n  &lt;tbody&gt;\n    {visible.map(st =&gt; (\n      &lt;tr key={st.id}&gt;\n        &lt;td&gt;{st.studentId}&lt;/td&gt;\n        &lt;td&gt;{st.name}&lt;/td&gt;\n        &lt;td&gt;{st.age}&lt;/td&gt;\n        &lt;td&gt;{st.street}&lt;/td&gt;\n        &lt;td&gt;{st.city}&lt;/td&gt;\n        &lt;td&gt;{st.isRegularStudent ? 'Fulltime' : 'Applicant'}&lt;/td&gt;\n        &lt;td&gt;&lt;Link to={`/student/${st.studentId}`}&gt;Grades&lt;/Link&gt;&lt;/td&gt;\n      &lt;/tr&gt;\n    ))}\n  &lt;/tbody&gt;\n);</code></pre><p>Lưu ý: để URL đọc thành <code>/student?subject=PRF192</code>, chính component danh sách cũng phải được mount trên route <code>/student</code> (thêm <code>&lt;Route path=\"/student\" element={&lt;StudentsManagement /&gt;} /&gt;</code>), hoặc render danh sách tại path đó.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Clicking a subject link filters the list by subjectid and reflects it on the URL /student?subject=subjectid.|||Click link môn lọc danh sách theo subjectid và phản ánh lên URL /student?subject=subjectid.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Search keyword filter by name is combined (AND) with the subject filter — both active at once.|||Bộ lọc tìm kiếm theo tên kết hợp (AND) với bộ lọc subject — cả hai cùng có hiệu lực.",
              "weight": 1,
              "maxScore": 0.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE14.png"
        },
        {
          "kind": "WRITE",
          "points": 1.5,
          "prompt": "<div class=\"ml-en\"><p><b>Question 4 (1.5 points)</b></p><p><b>Question 4 (1.5 points). Add new grade.</b></p><ul><li>When the user clicks the \"Add new\" button on the Add a new grade form, display the error message when both data fields are empty, as shown in Figure 8 (0.5 marks). <b>Figure 8</b>: Dialog \"localhost:3000 says / Grade and AdditionalExplanation are required\" with an OK button.</li><li>Add a grade successfully, insert the data into the database, reload the page, and display the success notification, as shown in Figure 9, Figure 10 (1 mark). <b>Figure 9</b>: Dialog \"localhost:3000 says / Add a new grade success\" with OK, appearing after filling Grade=7.5, Explanation=FER202 and clicking Add new. <b>Figure 10</b>: the grade details page after reload, the table has an extra row 7.5 - FER202.</li></ul></div><div class=\"ml-vi\"><p><b>Câu 4 (1.5 điểm)</b></p><p><b>Câu 4 (1.5 điểm). Thêm điểm mới (Add new grade).</b></p><ul><li>Khi người dùng click nút \"Add new\" trên form Add a new grade, hiển thị thông báo lỗi khi cả hai trường dữ liệu đều rỗng, như Figure 8 (0.5 điểm). <b>Figure 8</b>: Dialog \"localhost:3000 says / Grade and AdditionalExplanation are required\" với nút OK.</li><li>Thêm điểm thành công, chèn dữ liệu vào database, reload trang và hiển thị thông báo thành công, như Figure 9, Figure 10 (1 điểm). <b>Figure 9</b>: Dialog \"localhost:3000 says / Add a new grade success\" với OK, xuất hiện sau khi điền Grade=7.5, Explanation=FER202 và bấm Add new. <b>Figure 10</b>: Trang grade details sau khi reload, bảng có thêm dòng 7.5 - FER202.</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>The form is controlled with <code>grade</code> and <code>explanation</code> state. On submit: if both are empty, <code>alert('Grade and AdditionalExplanation are required')</code> and stop. Otherwise PATCH the student record with the grade appended to its <code>grades</code> array (json-server persists it), then <code>alert('Add a new grade success')</code> and reload with <code>window.location.reload()</code> so Figure 10 shows the new row.</p><pre><code>// add to StudentGrades.js\nconst [grade, setGrade] = useState('');\nconst [explanation, setExplanation] = useState('');\n\nconst handleAdd = async () =&gt; {\n  if (grade.trim() === '' &amp;&amp; explanation.trim() === '') {\n    alert('Grade and AdditionalExplanation are required');\n    return;\n  }\n  const newGrades = [...(student.grades || []),\n                     { grade, explanation }];\n  await fetch(`${API}/students/${student.id}`, {\n    method: 'PATCH',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ grades: newGrades })\n  });\n  alert('Add a new grade success');\n  window.location.reload();\n};\n\n// JSX (placed above the grades table)\n&lt;div className=\"mb-3\"&gt;\n  &lt;input\n    className=\"form-control mb-2\"\n    placeholder=\"Enter grade\"\n    value={grade}\n    onChange={e =&gt; setGrade(e.target.value)}\n  /&gt;\n  &lt;input\n    className=\"form-control mb-2\"\n    placeholder=\"Enter additional explanation\"\n    value={explanation}\n    onChange={e =&gt; setExplanation(e.target.value)}\n  /&gt;\n  &lt;button className=\"btn btn-primary\" onClick={handleAdd}&gt;Add new&lt;/button&gt;\n&lt;/div&gt;</code></pre><p>Note: if grades were a separate json-server collection instead of an embedded array, replace the PATCH with a <code>POST /grades</code> carrying <code>{ studentId: student.studentId, grade, explanation }</code>.</p></div><div class=\"ml-vi\"><p>Form được controlled bằng state <code>grade</code> và <code>explanation</code>. Khi submit: nếu cả hai rỗng thì <code>alert('Grade and AdditionalExplanation are required')</code> và dừng. Ngược lại PATCH bản ghi student với điểm mới nối vào mảng <code>grades</code> (json-server lưu lại), rồi <code>alert('Add a new grade success')</code> và reload bằng <code>window.location.reload()</code> để Figure 10 hiển thị dòng mới.</p><pre><code>// thêm vào StudentGrades.js\nconst [grade, setGrade] = useState('');\nconst [explanation, setExplanation] = useState('');\n\nconst handleAdd = async () =&gt; {\n  if (grade.trim() === '' &amp;&amp; explanation.trim() === '') {\n    alert('Grade and AdditionalExplanation are required');\n    return;\n  }\n  const newGrades = [...(student.grades || []),\n                     { grade, explanation }];\n  await fetch(`${API}/students/${student.id}`, {\n    method: 'PATCH',\n    headers: { 'Content-Type': 'application/json' },\n    body: JSON.stringify({ grades: newGrades })\n  });\n  alert('Add a new grade success');\n  window.location.reload();\n};\n\n// JSX (đặt phía trên bảng điểm)\n&lt;div className=\"mb-3\"&gt;\n  &lt;input\n    className=\"form-control mb-2\"\n    placeholder=\"Enter grade\"\n    value={grade}\n    onChange={e =&gt; setGrade(e.target.value)}\n  /&gt;\n  &lt;input\n    className=\"form-control mb-2\"\n    placeholder=\"Enter additional explanation\"\n    value={explanation}\n    onChange={e =&gt; setExplanation(e.target.value)}\n  /&gt;\n  &lt;button className=\"btn btn-primary\" onClick={handleAdd}&gt;Add new&lt;/button&gt;\n&lt;/div&gt;</code></pre><p>Lưu ý: nếu grades là một collection json-server riêng thay vì mảng lồng, đổi PATCH thành <code>POST /grades</code> mang <code>{ studentId: student.studentId, grade, explanation }</code>.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Clicking Add new with both fields empty shows the exact alert \"Grade and AdditionalExplanation are required\".|||Bấm Add new khi cả hai trường rỗng hiện đúng alert \"Grade and AdditionalExplanation are required\".",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "c2",
              "criterion": "Valid input is persisted to the database (PATCH/POST) and the success alert \"Add a new grade success\" is shown.|||Dữ liệu hợp lệ được lưu vào database (PATCH/POST) và hiện alert thành công \"Add a new grade success\".",
              "weight": 1,
              "maxScore": 0.75
            },
            {
              "id": "c3",
              "criterion": "Page reloads and the new grade row appears in the table (Figure 10).|||Trang reload và dòng điểm mới xuất hiện trong bảng (Figure 10).",
              "weight": 1,
              "maxScore": 0.25
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE14.png"
        }
      ]
    }
  ]
};
