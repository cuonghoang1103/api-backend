export default {
  "course": {
    "courseCode": "FER202"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE15-PESU2024",
      "title": "FER202 — Practical Exam Đề 15 (PE - SU 2024)|||FER202 — Đề thi thực hành số 15 (PE - SU 2024)",
      "description": "React PE (WRITE): build components against a REST API, AI-graded.|||Đề PE React (viết): dựng component theo REST API, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10.25,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>React / Front-end Development</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>React / Front-end Development</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 6.25,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p>FER202 Practical Exam (PE) - React application. Copy <b>database.json</b> from the given folder into your exam, run <code>json-server --watch database.json --port 9999</code> in one terminal and <code>npm start</code> in another (<b>0 points if json-server is not running</b>). Remove <b>node_modules</b> before submitting and browse the resource directory with <b>PEA_Client</b>. Apply ReactJS techniques (hooks, axios/fetch, react-router-dom) to implement the required functions. Do not download extra libraries. The app assumes <code>database.json</code> exposes a <code>movies</code> collection (each movie has id, title, release, description, producerId, director, genres[], stars[]) and a <code>producers</code> collection (id, name); genres and star options are derived from the data.</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p>Bài thi thực hành FER202 (PE) - ứng dụng React. Chép <b>database.json</b> từ thư mục given vào bài thi, chạy <code>json-server --watch database.json --port 9999</code> ở một cửa sổ terminal và <code>npm start</code> ở cửa sổ còn lại (<b>0 điểm nếu không chạy json-server</b>). Xoá thư mục <b>node_modules</b> trước khi nộp và dùng <b>PEA_Client</b> để duyệt thư mục resource. Áp dụng kỹ thuật ReactJS (hooks, axios/fetch, react-router-dom) để hiện thực các chức năng yêu cầu. Không được tải thêm thư viện. Bài giải giả định <code>database.json</code> có collection <code>movies</code> (mỗi phim gồm id, title, release, description, producerId, director, genres[], stars[]) và collection <code>producers</code> (id, name); danh sách genres và stars được suy ra từ dữ liệu.</p></div></div><div class=\"ml-en\"><p><b>Question 1 (6.25 points) - Create a component to load data from the database with the design shown in Figure 1. Route this component at URL <code>http://localhost:3000/movie</code>.</b></p><ul><li><b>Figure 1 - Load all data from database:</b> The \"React Application\" page has a genres menu <b>COMEDY | ACTION | CARTOON | DRAMA</b> and a \"List of Movies\" heading. On the left is a <b>Producers</b> block (Claire Lawry, Cary Geely, Ynez Mancktelow, Corie Awde, Lorna Lockhurst) plus a \"Show all movies\" link. On the right is a table with columns: Id, Title, Release, Description, Producer, Director, Genres, Stars. Each movie row has an <b>Add stars</b> link in the Stars cell.</li><li>The design layout must be displayed exactly as shown in Figure 1 - <b>(0.5 marks)</b>.</li><li>Load all movie genres exactly as in Figure 2. Each genre appears only once (unique values) - <b>(0.5 marks)</b>. Format the content as required - <b>(0.25 marks)</b>.</li><li>Load the producer list and display the producer names as shown in Figure 3 - <b>(1 mark)</b>.</li><li>Load the information of all movies into the table as in Figure 4:<ul><li>Show the correct columns: ID, Title, Release, Description - <b>(1 mark)</b>. Reduce 0.25 if the Release column is formatted incorrectly.</li><li>Display correct data in the Producer column - <b>(0.5 marks)</b>.</li><li>Display correct data in the Director column - <b>(0.5 marks)</b>.</li><li>Display correct data in the Genres column - <b>(0.5 marks)</b>.</li><li>Display correct data in the Stars column - <b>(1.5 marks)</b>. Reduce 0.25 if the stars are not numbered. Reduce 0.5 if there is no \"Add stars\" link after each star's name. 0 points if you cannot load all the stars.</li></ul></li></ul></div><div class=\"ml-vi\"><p><b>Câu 1 (6.25 điểm) - Tạo một component để tải dữ liệu từ database với thiết kế như Hình 1. Định tuyến component này tại URL <code>http://localhost:3000/movie</code>.</b></p><ul><li><b>Hình 1 - Tải toàn bộ dữ liệu từ database:</b> Trang \"React Application\" có menu genres <b>COMEDY | ACTION | CARTOON | DRAMA</b> và tiêu đề \"List of Movies\". Bên trái là khối <b>Producers</b> (Claire Lawry, Cary Geely, Ynez Mancktelow, Corie Awde, Lorna Lockhurst) kèm link \"Show all movies\". Bên phải là bảng gồm các cột: Id, Title, Release, Description, Producer, Director, Genres, Stars. Mỗi dòng phim có link <b>Add stars</b> ở cột Stars.</li><li>Bố cục phải hiển thị đúng như Hình 1 - <b>(0.5 điểm)</b>.</li><li>Tải tất cả genres đúng như Hình 2. Mỗi genre chỉ xuất hiện một lần (giá trị duy nhất) - <b>(0.5 điểm)</b>. Định dạng nội dung theo yêu cầu - <b>(0.25 điểm)</b>.</li><li>Tải danh sách producer và hiển thị tên producer như Hình 3 - <b>(1 điểm)</b>.</li><li>Tải thông tin tất cả phim vào bảng như Hình 4:<ul><li>Hiển thị đúng các cột: ID, Title, Release, Description - <b>(1 điểm)</b>. Trừ 0.25 nếu cột Release định dạng sai.</li><li>Hiển thị đúng dữ liệu cột Producer - <b>(0.5 điểm)</b>.</li><li>Hiển thị đúng dữ liệu cột Director - <b>(0.5 điểm)</b>.</li><li>Hiển thị đúng dữ liệu cột Genres - <b>(0.5 điểm)</b>.</li><li>Hiển thị đúng dữ liệu cột Stars - <b>(1.5 điểm)</b>. Trừ 0.25 nếu stars không được đánh số. Trừ 0.5 nếu không có link \"Add stars\" sau tên mỗi star. 0 điểm nếu không tải được toàn bộ stars.</li></ul></li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><b>Approach:</b> Route <code>MovieList</code> at <code>/movie</code>. Fetch <code>movies</code> and <code>producers</code> with axios inside <code>useEffect</code>. Build the genres menu with <code>new Set</code> (unique) and <code>.toUpperCase()</code> (format). Render the Producers block and the full table; the Producer column maps <code>producerId</code> to the producer name; the Stars cell numbers each star (<code>i + 1 - name</code>) and appends an <b>Add stars</b> link to <code>/movie/:id/add-stars</code>.</p><pre><code>// src/App.js\nimport { BrowserRouter, Routes, Route } from 'react-router-dom';\nimport MovieList from './components/MovieList';\nimport AddStars from './components/AddStars';\n\nfunction App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/movie\" element={&lt;MovieList /&gt;} /&gt;\n        &lt;Route path=\"/movie/:id/add-stars\" element={&lt;AddStars /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\nexport default App;\n\n\n// src/components/MovieList.js\nimport { useState, useEffect } from 'react';\nimport { Link, useSearchParams } from 'react-router-dom';\nimport axios from 'axios';\n\nconst BASE = 'http://localhost:9999';\n\nfunction MovieList() {\n  const [movies, setMovies] = useState([]);\n  const [producers, setProducers] = useState([]);\n  const [params] = useSearchParams();\n\n  useEffect(() =&gt; {\n    axios.get(`${BASE}/movies`).then(res =&gt; setMovies(res.data));\n    axios.get(`${BASE}/producers`).then(res =&gt; setProducers(res.data));\n  }, []);\n\n  // Figure 2: unique genres from all movies\n  const genres = [...new Set(movies.flatMap(m =&gt; m.genres))];\n\n  // Producer column: look up name by producerId\n  const producerName = (pid) =&gt; producers.find(p =&gt; p.id === pid)?.name || '';\n\n  // (Question 2) filter by query string\n  const genre = params.get('genre');\n  const producerId = params.get('producer-id');\n  let list = movies;\n  if (genre)\n    list = movies.filter(m =&gt; m.genres.some(g =&gt; g.toLowerCase() === genre.toLowerCase()));\n  else if (producerId)\n    list = movies.filter(m =&gt; m.producerId === Number(producerId));\n\n  return (\n    &lt;div className=\"container\"&gt;\n      &lt;h2&gt;React Application&lt;/h2&gt;\n\n      {/* Figure 2: genres menu (unique, uppercase) */}\n      &lt;nav className=\"genres\"&gt;\n        {genres.map(g =&gt; (\n          &lt;Link key={g} to={`/movie/?genre=${g}`}&gt;{g.toUpperCase()}&lt;/Link&gt;\n        ))}\n      &lt;/nav&gt;\n\n      &lt;div className=\"row\"&gt;\n        {/* Figure 3: producers */}\n        &lt;aside className=\"producers\"&gt;\n          &lt;h4&gt;Producers&lt;/h4&gt;\n          &lt;Link to=\"/movie\"&gt;Show all movies&lt;/Link&gt;\n          &lt;ul&gt;\n            {producers.map(p =&gt; (\n              &lt;li key={p.id}&gt;\n                &lt;Link to={`/movie/?producer-id=${p.id}`}&gt;{p.name}&lt;/Link&gt;\n              &lt;/li&gt;\n            ))}\n          &lt;/ul&gt;\n        &lt;/aside&gt;\n\n        {/* Figure 4: movies table */}\n        &lt;main&gt;\n          &lt;h3&gt;List of Movies&lt;/h3&gt;\n          &lt;table border=\"1\"&gt;\n            &lt;thead&gt;\n              &lt;tr&gt;\n                &lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Title&lt;/th&gt;&lt;th&gt;Release&lt;/th&gt;&lt;th&gt;Description&lt;/th&gt;\n                &lt;th&gt;Producer&lt;/th&gt;&lt;th&gt;Director&lt;/th&gt;&lt;th&gt;Genres&lt;/th&gt;&lt;th&gt;Stars&lt;/th&gt;\n              &lt;/tr&gt;\n            &lt;/thead&gt;\n            &lt;tbody&gt;\n              {list.map(m =&gt; (\n                &lt;tr key={m.id}&gt;\n                  &lt;td&gt;{m.id}&lt;/td&gt;\n                  &lt;td&gt;{m.title}&lt;/td&gt;\n                  &lt;td&gt;{m.release}&lt;/td&gt;\n                  &lt;td&gt;{m.description}&lt;/td&gt;\n                  &lt;td&gt;{producerName(m.producerId)}&lt;/td&gt;\n                  &lt;td&gt;{m.director}&lt;/td&gt;\n                  &lt;td&gt;{m.genres.join(', ')}&lt;/td&gt;\n                  &lt;td&gt;\n                    {m.stars.map((s, i) =&gt; (\n                      &lt;span key={i}&gt;{i + 1} - {s} &lt;/span&gt;\n                    ))}\n                    &lt;Link to={`/movie/${m.id}/add-stars`}&gt;Add stars&lt;/Link&gt;\n                  &lt;/td&gt;\n                &lt;/tr&gt;\n              ))}\n            &lt;/tbody&gt;\n          &lt;/table&gt;\n        &lt;/main&gt;\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\nexport default MovieList;</code></pre><p><b>Notes:</b> <code>new Set(movies.flatMap(m =&gt; m.genres))</code> guarantees unique genres. Numbering the stars with <code>i + 1</code> and appending the <code>Add stars</code> link satisfies the 1.5-mark Stars rule. The Release string is rendered as stored to keep its format.</p></div><div class=\"ml-vi\"><p><b>Hướng làm:</b> Định tuyến <code>MovieList</code> tại <code>/movie</code>. Dùng axios trong <code>useEffect</code> để lấy <code>movies</code> và <code>producers</code>. Tạo menu genres bằng <code>new Set</code> (giá trị duy nhất) và <code>.toUpperCase()</code> (định dạng). Render khối Producers và bảng đầy đủ; cột Producer tra tên theo <code>producerId</code>; ô Stars đánh số từng star (<code>i + 1 - tên</code>) và thêm link <b>Add stars</b> tới <code>/movie/:id/add-stars</code>.</p><pre><code>// src/App.js\nimport { BrowserRouter, Routes, Route } from 'react-router-dom';\nimport MovieList from './components/MovieList';\nimport AddStars from './components/AddStars';\n\nfunction App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/movie\" element={&lt;MovieList /&gt;} /&gt;\n        &lt;Route path=\"/movie/:id/add-stars\" element={&lt;AddStars /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\nexport default App;\n\n\n// src/components/MovieList.js\nimport { useState, useEffect } from 'react';\nimport { Link, useSearchParams } from 'react-router-dom';\nimport axios from 'axios';\n\nconst BASE = 'http://localhost:9999';\n\nfunction MovieList() {\n  const [movies, setMovies] = useState([]);\n  const [producers, setProducers] = useState([]);\n  const [params] = useSearchParams();\n\n  useEffect(() =&gt; {\n    axios.get(`${BASE}/movies`).then(res =&gt; setMovies(res.data));\n    axios.get(`${BASE}/producers`).then(res =&gt; setProducers(res.data));\n  }, []);\n\n  // Hinh 2: genres duy nhat tu tat ca phim\n  const genres = [...new Set(movies.flatMap(m =&gt; m.genres))];\n\n  // Cot Producer: tra ten theo producerId\n  const producerName = (pid) =&gt; producers.find(p =&gt; p.id === pid)?.name || '';\n\n  // (Cau 2) loc theo query string\n  const genre = params.get('genre');\n  const producerId = params.get('producer-id');\n  let list = movies;\n  if (genre)\n    list = movies.filter(m =&gt; m.genres.some(g =&gt; g.toLowerCase() === genre.toLowerCase()));\n  else if (producerId)\n    list = movies.filter(m =&gt; m.producerId === Number(producerId));\n\n  return (\n    &lt;div className=\"container\"&gt;\n      &lt;h2&gt;React Application&lt;/h2&gt;\n\n      {/* Hinh 2: menu genres (duy nhat, in hoa) */}\n      &lt;nav className=\"genres\"&gt;\n        {genres.map(g =&gt; (\n          &lt;Link key={g} to={`/movie/?genre=${g}`}&gt;{g.toUpperCase()}&lt;/Link&gt;\n        ))}\n      &lt;/nav&gt;\n\n      &lt;div className=\"row\"&gt;\n        {/* Hinh 3: producers */}\n        &lt;aside className=\"producers\"&gt;\n          &lt;h4&gt;Producers&lt;/h4&gt;\n          &lt;Link to=\"/movie\"&gt;Show all movies&lt;/Link&gt;\n          &lt;ul&gt;\n            {producers.map(p =&gt; (\n              &lt;li key={p.id}&gt;\n                &lt;Link to={`/movie/?producer-id=${p.id}`}&gt;{p.name}&lt;/Link&gt;\n              &lt;/li&gt;\n            ))}\n          &lt;/ul&gt;\n        &lt;/aside&gt;\n\n        {/* Hinh 4: bang phim */}\n        &lt;main&gt;\n          &lt;h3&gt;List of Movies&lt;/h3&gt;\n          &lt;table border=\"1\"&gt;\n            &lt;thead&gt;\n              &lt;tr&gt;\n                &lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Title&lt;/th&gt;&lt;th&gt;Release&lt;/th&gt;&lt;th&gt;Description&lt;/th&gt;\n                &lt;th&gt;Producer&lt;/th&gt;&lt;th&gt;Director&lt;/th&gt;&lt;th&gt;Genres&lt;/th&gt;&lt;th&gt;Stars&lt;/th&gt;\n              &lt;/tr&gt;\n            &lt;/thead&gt;\n            &lt;tbody&gt;\n              {list.map(m =&gt; (\n                &lt;tr key={m.id}&gt;\n                  &lt;td&gt;{m.id}&lt;/td&gt;\n                  &lt;td&gt;{m.title}&lt;/td&gt;\n                  &lt;td&gt;{m.release}&lt;/td&gt;\n                  &lt;td&gt;{m.description}&lt;/td&gt;\n                  &lt;td&gt;{producerName(m.producerId)}&lt;/td&gt;\n                  &lt;td&gt;{m.director}&lt;/td&gt;\n                  &lt;td&gt;{m.genres.join(', ')}&lt;/td&gt;\n                  &lt;td&gt;\n                    {m.stars.map((s, i) =&gt; (\n                      &lt;span key={i}&gt;{i + 1} - {s} &lt;/span&gt;\n                    ))}\n                    &lt;Link to={`/movie/${m.id}/add-stars`}&gt;Add stars&lt;/Link&gt;\n                  &lt;/td&gt;\n                &lt;/tr&gt;\n              ))}\n            &lt;/tbody&gt;\n          &lt;/table&gt;\n        &lt;/main&gt;\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\nexport default MovieList;</code></pre><p><b>Ghi chú:</b> <code>new Set(movies.flatMap(m =&gt; m.genres))</code> đảm bảo genres không trùng. Đánh số star bằng <code>i + 1</code> và thêm link <code>Add stars</code> đáp ứng đủ tiêu chí 1.5 điểm của cột Stars. Chuỗi Release được render nguyên như trong dữ liệu để giữ định dạng.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Component routed at /movie; layout matches Figure 1 and the genres menu loads unique, correctly-formatted (uppercase) genres from data.|||Component định tuyến tại /movie; bố cục khớp Hình 1 và menu genres tải giá trị duy nhất, định dạng đúng (in hoa) từ dữ liệu.",
              "weight": 1,
              "maxScore": 1.25
            },
            {
              "id": "c2",
              "criterion": "Producer list loaded and producer names displayed as in Figure 3.|||Tải danh sách producer và hiển thị tên producer như Hình 3.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "Table shows correct columns Id/Title/Release/Description (Release formatted right) plus correct Producer, Director and Genres data.|||Bảng hiển thị đúng các cột Id/Title/Release/Description (Release định dạng đúng) cùng dữ liệu Producer, Director, Genres chính xác.",
              "weight": 1,
              "maxScore": 2.5
            },
            {
              "id": "c4",
              "criterion": "Stars column loads all stars, numbered, with an \"Add stars\" link after each movie's stars.|||Cột Stars tải đủ stars, có đánh số, kèm link \"Add stars\" sau stars của mỗi phim.",
              "weight": 1,
              "maxScore": 1.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE15.png"
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2 (4 points) - Handling events on the components.</b></p><ul><li>When the user clicks a movie genre name in the Figure 2 menu, the movies of that genre are filtered and displayed as in Figure 5 - <b>(1 mark)</b>. URL: <code>http://localhost:3000/movie/?genre=Cartoon</code> (e.g. genre Cartoon shows Quartet, Each Dawn I Die, Bigger Than Life, Screamers...).</li><li>When the user clicks a producer name in Figure 3, the movies of that producer are filtered by producer id as in Figure 6 - <b>(0.5 marks)</b>. URL: <code>http://localhost:3000/movie/?producer-id=1</code> (producer name is Claire Lawry; shows Quartet, Green Slime The).</li><li>When the user clicks the \"Show all movies\" link, all movies are displayed - <b>(0.25 marks)</b>.</li><li>When the user clicks the \"Add stars\" link in the stars column, the app navigates to <code>http://localhost:3000/movie/:id/add-stars</code> (:id = id of the movie that needs stars) as in Figure 7.</li><li><b>Figure 7 - Add stars to the movie:</b> the \"Add stars to the movie\" form contains:<ul><li>Movie title: \"Keeping Up with the Steins\" (read-only).</li><li>Stars: a checkbox list (Leeanne Dyson, Fee Passo, Magdalene Oswick, Mikael Ginnally, Hugues MacKey, Nannette Hebburn) - some already checked.</li><li>An <b>Add Stars</b> button in green.</li><li>Design the form and load the correct data for the selected movie - <b>(0.5 marks)</b>.</li><li>Add stars to the movie successfully - <b>(1 mark)</b>.</li><li>Display a success message after adding stars as in Figure 8 (browser dialog \"localhost:3000 says / Add the Stars success\" with OK) - <b>(0.25 marks)</b>.</li><li>Navigate to <code>http://localhost:3000/movie</code> and obtain the result as in Figure 9 (the List of Movies now shows the newly added stars) - <b>(0.25 marks)</b>.</li></ul></li></ul></div><div class=\"ml-vi\"><p><b>Câu 2 (4 điểm) - Xử lý sự kiện trên các component.</b></p><ul><li>Khi người dùng bấm vào một tên genre trong menu Hình 2, các phim thuộc genre đó được lọc và hiển thị như Hình 5 - <b>(1 điểm)</b>. URL: <code>http://localhost:3000/movie/?genre=Cartoon</code> (ví dụ genre Cartoon hiển thị Quartet, Each Dawn I Die, Bigger Than Life, Screamers...).</li><li>Khi người dùng bấm tên producer trong Hình 3, các phim của producer đó được lọc theo producer id như Hình 6 - <b>(0.5 điểm)</b>. URL: <code>http://localhost:3000/movie/?producer-id=1</code> (producer là Claire Lawry; hiển thị Quartet, Green Slime The).</li><li>Khi người dùng bấm link \"Show all movies\", hiển thị tất cả phim - <b>(0.25 điểm)</b>.</li><li>Khi người dùng bấm link \"Add stars\" ở cột stars, ứng dụng điều hướng tới <code>http://localhost:3000/movie/:id/add-stars</code> (:id = id phim cần thêm stars) như Hình 7.</li><li><b>Hình 7 - Add stars to the movie:</b> form \"Add stars to the movie\" gồm:<ul><li>Movie title: \"Keeping Up with the Steins\" (chỉ đọc).</li><li>Stars: danh sách checkbox (Leeanne Dyson, Fee Passo, Magdalene Oswick, Mikael Ginnally, Hugues MacKey, Nannette Hebburn) - một số ô đã được tick.</li><li>Nút <b>Add Stars</b> màu xanh lá.</li><li>Thiết kế form và tải đúng dữ liệu cho phim đã chọn - <b>(0.5 điểm)</b>.</li><li>Thêm stars vào phim thành công - <b>(1 điểm)</b>.</li><li>Hiển thị thông báo thành công sau khi thêm như Hình 8 (hộp thoại trình duyệt \"localhost:3000 says / Add the Stars success\" với nút OK) - <b>(0.25 điểm)</b>.</li><li>Điều hướng về <code>http://localhost:3000/movie</code> và thu được kết quả như Hình 9 (List of Movies giờ hiển thị stars vừa thêm) - <b>(0.25 điểm)</b>.</li></ul></li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><b>Approach:</b> The genre / producer-id / show-all events are plain <code>&lt;Link&gt;</code> navigations that change the query string; <code>MovieList</code> (Question 1) already reads <code>useSearchParams</code> and filters the list accordingly, so clicking any link re-renders the filtered table. The \"Add stars\" link routes to <code>/movie/:id/add-stars</code>, handled by the <code>AddStars</code> component below: it loads the selected movie (read-only title) and a checkbox list of all stars (pre-checking those already in the movie), PUTs the updated stars back to json-server, shows the success <code>alert</code> (Figure 8), then navigates back to <code>/movie</code> (Figure 9).</p><pre><code>// Question 2 - the filter events live in MovieList (see Question 1):\n// &lt;Link to={`/movie/?genre=${g}`}&gt;        -&gt; ?genre=Cartoon   (Figure 5)\n// &lt;Link to={`/movie/?producer-id=${p.id}`}&gt; -&gt; ?producer-id=1 (Figure 6)\n// &lt;Link to=\"/movie\"&gt;Show all movies&lt;/Link&gt;  -&gt; all movies\n// and useSearchParams() decides which subset to render.\n\n// src/components/AddStars.js\nimport { useState, useEffect } from 'react';\nimport { useParams, useNavigate } from 'react-router-dom';\nimport axios from 'axios';\n\nconst BASE = 'http://localhost:9999';\n\nfunction AddStars() {\n  const { id } = useParams();\n  const navigate = useNavigate();\n  const [movie, setMovie] = useState(null);\n  const [allStars, setAllStars] = useState([]);\n  const [selected, setSelected] = useState([]);\n\n  useEffect(() =&gt; {\n    // load the movie that needs stars (Figure 7 - read-only title)\n    axios.get(`${BASE}/movies/${id}`).then(res =&gt; {\n      setMovie(res.data);\n      setSelected(res.data.stars || []);   // pre-check current stars\n    });\n    // checkbox options = all unique stars in the database\n    axios.get(`${BASE}/movies`).then(res =&gt; {\n      setAllStars([...new Set(res.data.flatMap(m =&gt; m.stars))]);\n    });\n  }, [id]);\n\n  const toggle = (name) =&gt;\n    setSelected(prev =&gt;\n      prev.includes(name) ? prev.filter(n =&gt; n !== name) : [...prev, name]);\n\n  const handleSubmit = (e) =&gt; {\n    e.preventDefault();\n    axios.put(`${BASE}/movies/${id}`, { ...movie, stars: selected })\n      .then(() =&gt; {\n        alert('Add the Stars success');   // Figure 8\n        navigate('/movie');               // Figure 9\n      });\n  };\n\n  if (!movie) return &lt;p&gt;Loading...&lt;/p&gt;;\n\n  return (\n    &lt;form onSubmit={handleSubmit} className=\"add-stars\"&gt;\n      &lt;h3&gt;Add stars to the movie&lt;/h3&gt;\n      &lt;p&gt;Movie title: &lt;b&gt;{movie.title}&lt;/b&gt;&lt;/p&gt;\n      {allStars.map(name =&gt; (\n        &lt;label key={name} style={{ display: 'block' }}&gt;\n          &lt;input\n            type=\"checkbox\"\n            checked={selected.includes(name)}\n            onChange={() =&gt; toggle(name)}\n          /&gt;\n          {name}\n        &lt;/label&gt;\n      ))}\n      &lt;button type=\"submit\" style={{ color: 'green' }}&gt;Add Stars&lt;/button&gt;\n    &lt;/form&gt;\n  );\n}\nexport default AddStars;</code></pre><p><b>Notes:</b> Using controlled checkboxes (<code>checked</code> + <code>onChange</code>) means the movie's existing stars appear pre-ticked, satisfying \"load the correct data\". <code>axios.put</code> with the spread <code>...movie</code> keeps the other fields and only replaces <code>stars</code>. <code>alert</code> produces the exact Figure 8 dialog; <code>navigate('/movie')</code> returns to the updated list (Figure 9).</p></div><div class=\"ml-vi\"><p><b>Hướng làm:</b> Các sự kiện lọc theo genre / producer-id / show-all chỉ là các <code>&lt;Link&gt;</code> đổi query string; <code>MovieList</code> (Câu 1) đã đọc <code>useSearchParams</code> và lọc danh sách tương ứng, nên bấm link nào bảng cũng render lại đúng tập con. Link \"Add stars\" điều hướng tới <code>/movie/:id/add-stars</code>, do component <code>AddStars</code> bên dưới xử lý: tải phim đã chọn (title chỉ đọc) và danh sách checkbox tất cả stars (tự tick những star đã có trong phim), PUT stars mới lên json-server, hiện <code>alert</code> thành công (Hình 8), rồi điều hướng về <code>/movie</code> (Hình 9).</p><pre><code>// Cau 2 - cac su kien loc nam trong MovieList (xem Cau 1):\n// &lt;Link to={`/movie/?genre=${g}`}&gt;        -&gt; ?genre=Cartoon   (Hinh 5)\n// &lt;Link to={`/movie/?producer-id=${p.id}`}&gt; -&gt; ?producer-id=1 (Hinh 6)\n// &lt;Link to=\"/movie\"&gt;Show all movies&lt;/Link&gt;  -&gt; tat ca phim\n// va useSearchParams() quyet dinh render tap con nao.\n\n// src/components/AddStars.js\nimport { useState, useEffect } from 'react';\nimport { useParams, useNavigate } from 'react-router-dom';\nimport axios from 'axios';\n\nconst BASE = 'http://localhost:9999';\n\nfunction AddStars() {\n  const { id } = useParams();\n  const navigate = useNavigate();\n  const [movie, setMovie] = useState(null);\n  const [allStars, setAllStars] = useState([]);\n  const [selected, setSelected] = useState([]);\n\n  useEffect(() =&gt; {\n    // tai phim can them stars (Hinh 7 - title chi doc)\n    axios.get(`${BASE}/movies/${id}`).then(res =&gt; {\n      setMovie(res.data);\n      setSelected(res.data.stars || []);   // tick san stars hien co\n    });\n    // danh sach checkbox = tat ca stars duy nhat trong database\n    axios.get(`${BASE}/movies`).then(res =&gt; {\n      setAllStars([...new Set(res.data.flatMap(m =&gt; m.stars))]);\n    });\n  }, [id]);\n\n  const toggle = (name) =&gt;\n    setSelected(prev =&gt;\n      prev.includes(name) ? prev.filter(n =&gt; n !== name) : [...prev, name]);\n\n  const handleSubmit = (e) =&gt; {\n    e.preventDefault();\n    axios.put(`${BASE}/movies/${id}`, { ...movie, stars: selected })\n      .then(() =&gt; {\n        alert('Add the Stars success');   // Hinh 8\n        navigate('/movie');               // Hinh 9\n      });\n  };\n\n  if (!movie) return &lt;p&gt;Loading...&lt;/p&gt;;\n\n  return (\n    &lt;form onSubmit={handleSubmit} className=\"add-stars\"&gt;\n      &lt;h3&gt;Add stars to the movie&lt;/h3&gt;\n      &lt;p&gt;Movie title: &lt;b&gt;{movie.title}&lt;/b&gt;&lt;/p&gt;\n      {allStars.map(name =&gt; (\n        &lt;label key={name} style={{ display: 'block' }}&gt;\n          &lt;input\n            type=\"checkbox\"\n            checked={selected.includes(name)}\n            onChange={() =&gt; toggle(name)}\n          /&gt;\n          {name}\n        &lt;/label&gt;\n      ))}\n      &lt;button type=\"submit\" style={{ color: 'green' }}&gt;Add Stars&lt;/button&gt;\n    &lt;/form&gt;\n  );\n}\nexport default AddStars;</code></pre><p><b>Ghi chú:</b> Dùng checkbox có kiểm soát (<code>checked</code> + <code>onChange</code>) nên các star sẵn có của phim được tick sẵn, đáp ứng \"tải đúng dữ liệu\". <code>axios.put</code> với spread <code>...movie</code> giữ nguyên các trường khác, chỉ thay <code>stars</code>. <code>alert</code> tạo đúng hộp thoại Hình 8; <code>navigate('/movie')</code> quay về danh sách đã cập nhật (Hình 9).</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Clicking a genre in the menu filters and displays movies of that genre (?genre=...) as in Figure 5.|||Bấm genre trong menu lọc và hiển thị phim thuộc genre đó (?genre=...) như Hình 5.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Clicking a producer filters by producer id (?producer-id=...) as in Figure 6, and \"Show all movies\" displays every movie.|||Bấm producer lọc theo producer id (?producer-id=...) như Hình 6, và \"Show all movies\" hiển thị tất cả phim.",
              "weight": 1,
              "maxScore": 0.75
            },
            {
              "id": "c3",
              "criterion": "\"Add stars\" navigates to /movie/:id/add-stars; the form is designed correctly and loads the selected movie's data (read-only title, pre-checked stars).|||\"Add stars\" điều hướng tới /movie/:id/add-stars; form thiết kế đúng và tải dữ liệu phim đã chọn (title chỉ đọc, stars tick sẵn).",
              "weight": 1,
              "maxScore": 0.75
            },
            {
              "id": "c4",
              "criterion": "Submitting adds stars successfully (PUT), shows the success alert (Figure 8), and navigates back to /movie with the updated list (Figure 9).|||Submit thêm stars thành công (PUT), hiện alert thành công (Hình 8), và điều hướng về /movie với danh sách đã cập nhật (Hình 9).",
              "weight": 1,
              "maxScore": 1.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE15.png"
        }
      ]
    }
  ]
};
