export default {
  "course": {
    "courseCode": "FER202"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE16-PESP2024s2",
      "title": "FER202 — Practical Exam Đề 16 (PESP 2024 ( Đề số 2))|||FER202 — Đề thi thực hành số 16 (PESP 2024 ( Đề số 2))",
      "description": "React PE (WRITE): build components against a REST API, AI-graded.|||Đề PE React (viết): dựng component theo REST API, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>React / Front-end Development</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>React / Front-end Development</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p><b>FER202 - React Front-End - Practical Exam (PESP 2024, Paper 2 / Đề 16).</b> Build a ReactJS app that reads data from <code>database.json</code> served by <b>json-server</b>. The base URL below assumes json-server runs on port <code>9999</code> (adjust to your actual port). Inferred data shape: <code>projects</code> {id, name, description, startDate, type, function, departmentId}, <code>departments</code> {id, name}, <code>employees</code> {id, name, dob, gender, position, departmentId}. Uses <code>react-router-dom</code> v6, <code>axios</code>, and React hooks (<code>useState</code>, <code>useEffect</code>, <code>useParams</code>, <code>useNavigate</code>).</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p><b>FER202 - React Front-End - Thi thực hành (PESP 2024, Đề số 2 / Đề 16).</b> Xây dựng ứng dụng ReactJS đọc dữ liệu từ <code>database.json</code> phục vụ bởi <b>json-server</b>. Base URL bên dưới giả định json-server chạy ở cổng <code>9999</code> (chỉnh lại đúng cổng thật của bạn). Cấu trúc dữ liệu suy ra: <code>projects</code> {id, name, description, startDate, type, function, departmentId}, <code>departments</code> {id, name}, <code>employees</code> {id, name, dob, gender, position, departmentId}. Dùng <code>react-router-dom</code> v6, <code>axios</code> và React hooks (<code>useState</code>, <code>useEffect</code>, <code>useParams</code>, <code>useNavigate</code>).</p></div></div><div class=\"ml-en\"><p><b>Question 1 (4 points). Create a component to load data from the database with the design shown in Figure 1. Route this component at URL:</b> <code>http://localhost:3000</code></p><ul><li><b>(0.5 points)</b> The design layout must be displayed exactly as shown in Figure 1 (\"List of Projects\").</li><li><b>(1.5 points)</b> List all <b>Projects</b> along with the <b>Department name</b> of the corresponding project.</li><li><b>(0.5 points)</b> List all Departments that act as a data filter as shown in Figure 2 (radio buttons). <i>Note: reduced by 0.25 points if there is no \"All\" option.</i></li><li><b>(1 point)</b> When a user clicks the radio button of a Department name, the Project list is filtered by that Department (Figure 3).</li><li><b>(0.5 points)</b> When the \"All\" radio button is clicked, all existing projects are displayed.</li></ul></div><div class=\"ml-vi\"><p><b>Câu 1 (4 điểm). Tạo một component để nạp dữ liệu từ cơ sở dữ liệu với thiết kế như Hình 1. Định tuyến component này tại URL:</b> <code>http://localhost:3000</code></p><ul><li><b>(0.5 điểm)</b> Bố cục thiết kế phải hiển thị đúng như Hình 1 (\"List of Projects\").</li><li><b>(1.5 điểm)</b> Liệt kê tất cả <b>Projects</b> kèm theo <b>tên Department</b> tương ứng của mỗi project.</li><li><b>(0.5 điểm)</b> Liệt kê tất cả Departments đóng vai trò bộ lọc dữ liệu như Hình 2 (các radio button). <i>Lưu ý: trừ 0.25 điểm nếu không có tùy chọn \"All\".</i></li><li><b>(1 điểm)</b> Khi người dùng bấm radio button của một Department, danh sách Project được lọc theo Department đó (Hình 3).</li><li><b>(0.5 điểm)</b> Khi bấm radio button \"All\", hiển thị lại toàn bộ project.</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Set up routing in <code>App.js</code>, then build <code>ProjectList</code>. It fetches both <code>projects</code> and <code>departments</code> once on mount, joins each project to its department name via <code>departmentId</code>, renders a radio-button filter (with an <b>All</b> option), and filters the table by the selected department id. The Department cell links to Question 2's route and the Function cell links to Question 3's Edit route.</p><pre><code>// App.js\nimport { BrowserRouter, Routes, Route } from 'react-router-dom';\nimport ProjectList from './ProjectList';\nimport EmployeeList from './EmployeeList';\nimport EditProject from './EditProject';\n\nfunction App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/\" element={&lt;ProjectList /&gt;} /&gt;\n        &lt;Route path=\"/departments/:id/employees\" element={&lt;EmployeeList /&gt;} /&gt;\n        &lt;Route path=\"/projects/edit/:id\" element={&lt;EditProject /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\nexport default App;\n\n// ProjectList.js\nimport { useState, useEffect } from 'react';\nimport axios from 'axios';\nimport { Link } from 'react-router-dom';\n\nconst API = 'http://localhost:9999';\n\nfunction ProjectList() {\n  const [projects, setProjects] = useState([]);\n  const [departments, setDepartments] = useState([]);\n  const [selectedDept, setSelectedDept] = useState('all'); // 'all' or a department id\n\n  useEffect(() =&gt; {\n    axios.get(`${API}/projects`).then(res =&gt; setProjects(res.data));\n    axios.get(`${API}/departments`).then(res =&gt; setDepartments(res.data));\n  }, []);\n\n  // join: find the department name for a given departmentId\n  const getDeptName = (deptId) =&gt; {\n    const d = departments.find(x =&gt; String(x.id) === String(deptId));\n    return d ? d.name : '';\n  };\n\n  // filter the project list by the selected radio\n  const filtered = selectedDept === 'all'\n    ? projects\n    : projects.filter(p =&gt; String(p.departmentId) === String(selectedDept));\n\n  return (\n    &lt;div&gt;\n      &lt;h1 style={{ textAlign: 'center' }}&gt;List of Projects&lt;/h1&gt;\n      &lt;div style={{ display: 'flex', gap: 24 }}&gt;\n        &lt;div&gt;\n          &lt;h3&gt;Departments&lt;/h3&gt;\n          &lt;label style={{ display: 'block' }}&gt;\n            &lt;input type=\"radio\" name=\"dept\"\n              checked={selectedDept === 'all'}\n              onChange={() =&gt; setSelectedDept('all')} /&gt; All\n          &lt;/label&gt;\n          {departments.map(d =&gt; (\n            &lt;label key={d.id} style={{ display: 'block' }}&gt;\n              &lt;input type=\"radio\" name=\"dept\"\n                checked={String(selectedDept) === String(d.id)}\n                onChange={() =&gt; setSelectedDept(d.id)} /&gt; {d.name}\n            &lt;/label&gt;\n          ))}\n        &lt;/div&gt;\n        &lt;table border=\"1\" cellPadding=\"6\"&gt;\n          &lt;thead&gt;\n            &lt;tr&gt;\n              &lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Project name&lt;/th&gt;&lt;th&gt;Description&lt;/th&gt;\n              &lt;th&gt;Start date&lt;/th&gt;&lt;th&gt;Type&lt;/th&gt;&lt;th&gt;Department&lt;/th&gt;&lt;th&gt;Function&lt;/th&gt;\n            &lt;/tr&gt;\n          &lt;/thead&gt;\n          &lt;tbody&gt;\n            {filtered.map(p =&gt; (\n              &lt;tr key={p.id}&gt;\n                &lt;td&gt;{p.id}&lt;/td&gt;\n                &lt;td&gt;{p.name}&lt;/td&gt;\n                &lt;td&gt;{p.description}&lt;/td&gt;\n                &lt;td&gt;{p.startDate}&lt;/td&gt;\n                &lt;td&gt;{p.type}&lt;/td&gt;\n                &lt;td&gt;\n                  &lt;Link to={`/departments/${p.departmentId}/employees`}&gt;\n                    {getDeptName(p.departmentId)}\n                  &lt;/Link&gt;\n                &lt;/td&gt;\n                &lt;td&gt;&lt;Link to={`/projects/edit/${p.id}`}&gt;Edit&lt;/Link&gt;&lt;/td&gt;\n              &lt;/tr&gt;\n            ))}\n          &lt;/tbody&gt;\n        &lt;/table&gt;\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\nexport default ProjectList;</code></pre><p><b>Key points:</b> fetch once in <code>useEffect</code> with empty deps; <code>getDeptName</code> joins project to department; the <b>All</b> radio resets the filter; compare ids with <code>String()</code> because json-server ids may be numbers or strings.</p></div><div class=\"ml-vi\"><p>Thiết lập định tuyến trong <code>App.js</code>, rồi xây <code>ProjectList</code>. Component nạp <code>projects</code> và <code>departments</code> một lần khi mount, nối mỗi project với tên department qua <code>departmentId</code>, hiển thị bộ lọc bằng radio button (có tùy chọn <b>All</b>), và lọc bảng theo id department được chọn. Ô Department liên kết tới route Câu 2, ô Function liên kết tới route Edit của Câu 3.</p><pre><code>// App.js\nimport { BrowserRouter, Routes, Route } from 'react-router-dom';\nimport ProjectList from './ProjectList';\nimport EmployeeList from './EmployeeList';\nimport EditProject from './EditProject';\n\nfunction App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/\" element={&lt;ProjectList /&gt;} /&gt;\n        &lt;Route path=\"/departments/:id/employees\" element={&lt;EmployeeList /&gt;} /&gt;\n        &lt;Route path=\"/projects/edit/:id\" element={&lt;EditProject /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\nexport default App;\n\n// ProjectList.js\nimport { useState, useEffect } from 'react';\nimport axios from 'axios';\nimport { Link } from 'react-router-dom';\n\nconst API = 'http://localhost:9999';\n\nfunction ProjectList() {\n  const [projects, setProjects] = useState([]);\n  const [departments, setDepartments] = useState([]);\n  const [selectedDept, setSelectedDept] = useState('all'); // 'all' hoac id department\n\n  useEffect(() =&gt; {\n    axios.get(`${API}/projects`).then(res =&gt; setProjects(res.data));\n    axios.get(`${API}/departments`).then(res =&gt; setDepartments(res.data));\n  }, []);\n\n  // noi: tim ten department theo departmentId\n  const getDeptName = (deptId) =&gt; {\n    const d = departments.find(x =&gt; String(x.id) === String(deptId));\n    return d ? d.name : '';\n  };\n\n  // loc danh sach project theo radio duoc chon\n  const filtered = selectedDept === 'all'\n    ? projects\n    : projects.filter(p =&gt; String(p.departmentId) === String(selectedDept));\n\n  return (\n    &lt;div&gt;\n      &lt;h1 style={{ textAlign: 'center' }}&gt;List of Projects&lt;/h1&gt;\n      &lt;div style={{ display: 'flex', gap: 24 }}&gt;\n        &lt;div&gt;\n          &lt;h3&gt;Departments&lt;/h3&gt;\n          &lt;label style={{ display: 'block' }}&gt;\n            &lt;input type=\"radio\" name=\"dept\"\n              checked={selectedDept === 'all'}\n              onChange={() =&gt; setSelectedDept('all')} /&gt; All\n          &lt;/label&gt;\n          {departments.map(d =&gt; (\n            &lt;label key={d.id} style={{ display: 'block' }}&gt;\n              &lt;input type=\"radio\" name=\"dept\"\n                checked={String(selectedDept) === String(d.id)}\n                onChange={() =&gt; setSelectedDept(d.id)} /&gt; {d.name}\n            &lt;/label&gt;\n          ))}\n        &lt;/div&gt;\n        &lt;table border=\"1\" cellPadding=\"6\"&gt;\n          &lt;thead&gt;\n            &lt;tr&gt;\n              &lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Project name&lt;/th&gt;&lt;th&gt;Description&lt;/th&gt;\n              &lt;th&gt;Start date&lt;/th&gt;&lt;th&gt;Type&lt;/th&gt;&lt;th&gt;Department&lt;/th&gt;&lt;th&gt;Function&lt;/th&gt;\n            &lt;/tr&gt;\n          &lt;/thead&gt;\n          &lt;tbody&gt;\n            {filtered.map(p =&gt; (\n              &lt;tr key={p.id}&gt;\n                &lt;td&gt;{p.id}&lt;/td&gt;\n                &lt;td&gt;{p.name}&lt;/td&gt;\n                &lt;td&gt;{p.description}&lt;/td&gt;\n                &lt;td&gt;{p.startDate}&lt;/td&gt;\n                &lt;td&gt;{p.type}&lt;/td&gt;\n                &lt;td&gt;\n                  &lt;Link to={`/departments/${p.departmentId}/employees`}&gt;\n                    {getDeptName(p.departmentId)}\n                  &lt;/Link&gt;\n                &lt;/td&gt;\n                &lt;td&gt;&lt;Link to={`/projects/edit/${p.id}`}&gt;Edit&lt;/Link&gt;&lt;/td&gt;\n              &lt;/tr&gt;\n            ))}\n          &lt;/tbody&gt;\n        &lt;/table&gt;\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\nexport default ProjectList;</code></pre><p><b>Điểm cốt lõi:</b> nạp dữ liệu một lần trong <code>useEffect</code> với mảng phụ thuộc rỗng; <code>getDeptName</code> nối project với department; radio <b>All</b> đặt lại bộ lọc; so sánh id bằng <code>String()</code> vì id của json-server có thể là số hoặc chuỗi.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Component routed at \"/\" and the \"List of Projects\" layout is displayed as in Figure 1.|||Component được định tuyến tại \"/\" và bố cục \"List of Projects\" hiển thị như Hình 1.",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "c2",
              "criterion": "All projects are listed in the table, each showing the correct Department name (joined via departmentId).|||Liệt kê toàn bộ project trong bảng, mỗi dòng hiển thị đúng tên Department (nối qua departmentId).",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c3",
              "criterion": "All departments are listed as radio-button filters, including an \"All\" option.|||Liệt kê tất cả department dưới dạng radio-button lọc, bao gồm tùy chọn \"All\".",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "c4",
              "criterion": "Selecting a department radio filters the project list by that department; selecting \"All\" shows every project.|||Chọn radio một department thì lọc danh sách project theo department đó; chọn \"All\" thì hiện lại mọi project.",
              "weight": 1,
              "maxScore": 1.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE16-q1.png"
        },
        {
          "kind": "WRITE",
          "points": 2,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2 (2 points). Create a component that lists all Employees by the corresponding Department as shown in Figure 4.</b></p><ul><li><b>(1.5 points)</b> When a user clicks a link in the Departments column of the Projects table (Figure 1), the list of employees is displayed at the URL <code>http://localhost:3000/departments/:id/employees</code> (<code>:id</code> is the identifier of the selected Department).</li><li><b>(0.5 points)</b> Show the correct Department name.</li></ul><p><i>Note: reduced by 0.5 points if there is no \"Home page\" link.</i></p></div><div class=\"ml-vi\"><p><b>Câu 2 (2 điểm). Tạo một component liệt kê tất cả Employees theo Department tương ứng như Hình 4.</b></p><ul><li><b>(1.5 điểm)</b> Khi người dùng bấm liên kết ở cột Departments trong bảng Projects (Hình 1), danh sách nhân viên hiển thị tại URL <code>http://localhost:3000/departments/:id/employees</code> (<code>:id</code> là mã của Department được chọn).</li><li><b>(0.5 điểm)</b> Hiển thị đúng tên Department.</li></ul><p><i>Lưu ý: trừ 0.5 điểm nếu không có liên kết \"Home page\".</i></p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><code>EmployeeList</code> reads <code>:id</code> from the URL with <code>useParams</code>, fetches employees filtered by that department (<code>?departmentId=</code>, a json-server query) plus the single department record for its name, and renders a <b>Home page</b> link back to <code>/</code>.</p><pre><code>// EmployeeList.js\nimport { useState, useEffect } from 'react';\nimport axios from 'axios';\nimport { useParams, Link } from 'react-router-dom';\n\nconst API = 'http://localhost:9999';\n\nfunction EmployeeList() {\n  const { id } = useParams();          // :id from /departments/:id/employees\n  const [employees, setEmployees] = useState([]);\n  const [department, setDepartment] = useState(null);\n\n  useEffect(() =&gt; {\n    // json-server filters by field: /employees?departmentId=1\n    axios.get(`${API}/employees?departmentId=${id}`)\n      .then(res =&gt; setEmployees(res.data));\n    axios.get(`${API}/departments/${id}`)\n      .then(res =&gt; setDepartment(res.data));\n  }, [id]);\n\n  return (\n    &lt;div&gt;\n      &lt;Link to=\"/\"&gt;Home page&lt;/Link&gt;\n      &lt;h1 style={{ textAlign: 'center' }}&gt;List of Employees&lt;/h1&gt;\n      &lt;h3&gt;Department: {department ? department.name : ''}&lt;/h3&gt;\n      &lt;table border=\"1\" cellPadding=\"6\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;\n            &lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Employee name&lt;/th&gt;&lt;th&gt;Date of birth&lt;/th&gt;\n            &lt;th&gt;Gender&lt;/th&gt;&lt;th&gt;Position&lt;/th&gt;\n          &lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {employees.map(e =&gt; (\n            &lt;tr key={e.id}&gt;\n              &lt;td&gt;{e.id}&lt;/td&gt;\n              &lt;td&gt;{e.name}&lt;/td&gt;\n              &lt;td&gt;{e.dob}&lt;/td&gt;\n              &lt;td&gt;{e.gender}&lt;/td&gt;\n              &lt;td&gt;{e.position}&lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}\nexport default EmployeeList;</code></pre><p><b>Key points:</b> the route is registered in <code>App.js</code> (see Question 1); <code>[id]</code> is in the dependency array so the list re-fetches when navigating between departments; the <b>Home page</b> link is required.</p></div><div class=\"ml-vi\"><p><code>EmployeeList</code> đọc <code>:id</code> từ URL bằng <code>useParams</code>, nạp danh sách nhân viên lọc theo department đó (<code>?departmentId=</code>, truy vấn của json-server) cùng với bản ghi department để lấy tên, và hiển thị liên kết <b>Home page</b> quay về <code>/</code>.</p><pre><code>// EmployeeList.js\nimport { useState, useEffect } from 'react';\nimport axios from 'axios';\nimport { useParams, Link } from 'react-router-dom';\n\nconst API = 'http://localhost:9999';\n\nfunction EmployeeList() {\n  const { id } = useParams();          // :id tu /departments/:id/employees\n  const [employees, setEmployees] = useState([]);\n  const [department, setDepartment] = useState(null);\n\n  useEffect(() =&gt; {\n    // json-server loc theo truong: /employees?departmentId=1\n    axios.get(`${API}/employees?departmentId=${id}`)\n      .then(res =&gt; setEmployees(res.data));\n    axios.get(`${API}/departments/${id}`)\n      .then(res =&gt; setDepartment(res.data));\n  }, [id]);\n\n  return (\n    &lt;div&gt;\n      &lt;Link to=\"/\"&gt;Home page&lt;/Link&gt;\n      &lt;h1 style={{ textAlign: 'center' }}&gt;List of Employees&lt;/h1&gt;\n      &lt;h3&gt;Department: {department ? department.name : ''}&lt;/h3&gt;\n      &lt;table border=\"1\" cellPadding=\"6\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;\n            &lt;th&gt;Id&lt;/th&gt;&lt;th&gt;Employee name&lt;/th&gt;&lt;th&gt;Date of birth&lt;/th&gt;\n            &lt;th&gt;Gender&lt;/th&gt;&lt;th&gt;Position&lt;/th&gt;\n          &lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {employees.map(e =&gt; (\n            &lt;tr key={e.id}&gt;\n              &lt;td&gt;{e.id}&lt;/td&gt;\n              &lt;td&gt;{e.name}&lt;/td&gt;\n              &lt;td&gt;{e.dob}&lt;/td&gt;\n              &lt;td&gt;{e.gender}&lt;/td&gt;\n              &lt;td&gt;{e.position}&lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}\nexport default EmployeeList;</code></pre><p><b>Điểm cốt lõi:</b> route được khai báo trong <code>App.js</code> (xem Câu 1); <code>[id]</code> nằm trong mảng phụ thuộc nên danh sách được nạp lại khi chuyển giữa các department; liên kết <b>Home page</b> là bắt buộc.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Route /departments/:id/employees works from the Department link; employees of that department are fetched and listed correctly.|||Route /departments/:id/employees hoạt động khi bấm liên kết Department; nhân viên của department đó được nạp và liệt kê đúng.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c2",
              "criterion": "The correct Department name is shown, and a \"Home page\" link back to \"/\" is present.|||Hiển thị đúng tên Department, và có liên kết \"Home page\" quay về \"/\".",
              "weight": 1,
              "maxScore": 0.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE16-q2.png"
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 3 (4 points). Create a component to update a Project.</b></p><ul><li><b>(0.5 points)</b> When the user clicks the \"Edit\" link in Figure 1, the app navigates to <code>http://localhost:3000/projects/edit/:id</code> (<code>:id</code> is the Project identifier) and the data is filled into the corresponding form fields (Figure 5).</li><li><b>(0.5 points)</b> Design the form format correctly as shown in Figure 5.</li><li><b>(1 point)</b> Load all Departments into the drop-down list and show the correct Department of this Project.</li><li><b>(0.5 points)</b> Display the message \"<b>Please enter the form fields that are required.</b>\" when the Project name field is left blank (Figure 6).</li><li><b>(1.5 points)</b> After a successful update, show the message \"<b>Update success.</b>\" and navigate to the home page.</li></ul></div><div class=\"ml-vi\"><p><b>Câu 3 (4 điểm). Tạo một component để cập nhật một Project.</b></p><ul><li><b>(0.5 điểm)</b> Khi người dùng bấm liên kết \"Edit\" ở Hình 1, ứng dụng điều hướng tới <code>http://localhost:3000/projects/edit/:id</code> (<code>:id</code> là mã của Project) và dữ liệu được đổ vào các trường form tương ứng (Hình 5).</li><li><b>(0.5 điểm)</b> Thiết kế đúng định dạng form như Hình 5.</li><li><b>(1 điểm)</b> Nạp tất cả Departments vào drop-down và hiển thị đúng Department của Project này.</li><li><b>(0.5 điểm)</b> Hiển thị thông báo \"<b>Please enter the form fields that are required.</b>\" khi trường Project name để trống (Hình 6).</li><li><b>(1.5 điểm)</b> Sau khi cập nhật thành công, hiển thị thông báo \"<b>Update success.</b>\" và điều hướng về trang chủ.</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><code>EditProject</code> reads <code>:id</code> with <code>useParams</code>, fetches the project into a controlled form and all departments into the drop-down (its <code>value={project.departmentId}</code> auto-selects the current department). On submit it validates a non-empty <code>name</code>, sends a <code>PUT</code>, alerts <b>Update success.</b>, then navigates home with <code>useNavigate</code>.</p><pre><code>// EditProject.js\nimport { useState, useEffect } from 'react';\nimport axios from 'axios';\nimport { useParams, useNavigate, Link } from 'react-router-dom';\n\nconst API = 'http://localhost:9999';\n\nfunction EditProject() {\n  const { id } = useParams();\n  const navigate = useNavigate();\n  const [project, setProject] = useState({\n    name: '', description: '', startDate: '', type: '', departmentId: ''\n  });\n  const [departments, setDepartments] = useState([]);\n\n  useEffect(() =&gt; {\n    // pre-fill the form with the project's data\n    axios.get(`${API}/projects/${id}`).then(res =&gt; setProject(res.data));\n    axios.get(`${API}/departments`).then(res =&gt; setDepartments(res.data));\n  }, [id]);\n\n  const handleChange = (e) =&gt;\n    setProject({ ...project, [e.target.name]: e.target.value });\n\n  const handleSubmit = (e) =&gt; {\n    e.preventDefault();\n    // required-field validation for Project name\n    if (!project.name || !project.name.trim()) {\n      alert('Please enter the form fields that are required.');\n      return;\n    }\n    axios.put(`${API}/projects/${id}`, project).then(() =&gt; {\n      alert('Update success.');\n      navigate('/');           // back to the home page\n    });\n  };\n\n  return (\n    &lt;form onSubmit={handleSubmit}&gt;\n      &lt;Link to=\"/\"&gt;Home page&lt;/Link&gt;\n      &lt;div&gt;\n        &lt;label&gt;Project name *&lt;/label&gt;&lt;br /&gt;\n        &lt;input name=\"name\" value={project.name} onChange={handleChange} /&gt;\n      &lt;/div&gt;\n      &lt;div&gt;\n        &lt;label&gt;Description&lt;/label&gt;&lt;br /&gt;\n        &lt;textarea name=\"description\" value={project.description} onChange={handleChange} /&gt;\n      &lt;/div&gt;\n      &lt;div&gt;\n        &lt;label&gt;Start date&lt;/label&gt;&lt;br /&gt;\n        &lt;input type=\"date\" name=\"startDate\" value={project.startDate} onChange={handleChange} /&gt;\n      &lt;/div&gt;\n      &lt;div&gt;\n        &lt;label&gt;Type&lt;/label&gt;&lt;br /&gt;\n        &lt;input name=\"type\" value={project.type} onChange={handleChange} /&gt;\n      &lt;/div&gt;\n      &lt;div&gt;\n        &lt;label&gt;Department&lt;/label&gt;&lt;br /&gt;\n        &lt;select name=\"departmentId\" value={project.departmentId} onChange={handleChange}&gt;\n          {departments.map(d =&gt; (\n            &lt;option key={d.id} value={d.id}&gt;{d.name}&lt;/option&gt;\n          ))}\n        &lt;/select&gt;\n      &lt;/div&gt;\n      &lt;button type=\"submit\"&gt;Update&lt;/button&gt;\n    &lt;/form&gt;\n  );\n}\nexport default EditProject;</code></pre><p><b>Key points:</b> a controlled form (<code>value</code> + <code>onChange</code>) makes pre-filling automatic; the <code>&lt;select&gt;</code> value equals <code>project.departmentId</code> so the current department is pre-selected; validate before the <code>PUT</code>; <code>useNavigate('/')</code> returns home after success.</p></div><div class=\"ml-vi\"><p><code>EditProject</code> đọc <code>:id</code> bằng <code>useParams</code>, nạp project vào form có kiểm soát và nạp tất cả department vào drop-down (thuộc tính <code>value={project.departmentId}</code> tự chọn đúng department hiện tại). Khi submit, kiểm tra <code>name</code> không rỗng, gửi <code>PUT</code>, hiện thông báo <b>Update success.</b> rồi điều hướng về trang chủ bằng <code>useNavigate</code>.</p><pre><code>// EditProject.js\nimport { useState, useEffect } from 'react';\nimport axios from 'axios';\nimport { useParams, useNavigate, Link } from 'react-router-dom';\n\nconst API = 'http://localhost:9999';\n\nfunction EditProject() {\n  const { id } = useParams();\n  const navigate = useNavigate();\n  const [project, setProject] = useState({\n    name: '', description: '', startDate: '', type: '', departmentId: ''\n  });\n  const [departments, setDepartments] = useState([]);\n\n  useEffect(() =&gt; {\n    // do san du lieu project vao form\n    axios.get(`${API}/projects/${id}`).then(res =&gt; setProject(res.data));\n    axios.get(`${API}/departments`).then(res =&gt; setDepartments(res.data));\n  }, [id]);\n\n  const handleChange = (e) =&gt;\n    setProject({ ...project, [e.target.name]: e.target.value });\n\n  const handleSubmit = (e) =&gt; {\n    e.preventDefault();\n    // kiem tra truong bat buoc Project name\n    if (!project.name || !project.name.trim()) {\n      alert('Please enter the form fields that are required.');\n      return;\n    }\n    axios.put(`${API}/projects/${id}`, project).then(() =&gt; {\n      alert('Update success.');\n      navigate('/');           // quay ve trang chu\n    });\n  };\n\n  return (\n    &lt;form onSubmit={handleSubmit}&gt;\n      &lt;Link to=\"/\"&gt;Home page&lt;/Link&gt;\n      &lt;div&gt;\n        &lt;label&gt;Project name *&lt;/label&gt;&lt;br /&gt;\n        &lt;input name=\"name\" value={project.name} onChange={handleChange} /&gt;\n      &lt;/div&gt;\n      &lt;div&gt;\n        &lt;label&gt;Description&lt;/label&gt;&lt;br /&gt;\n        &lt;textarea name=\"description\" value={project.description} onChange={handleChange} /&gt;\n      &lt;/div&gt;\n      &lt;div&gt;\n        &lt;label&gt;Start date&lt;/label&gt;&lt;br /&gt;\n        &lt;input type=\"date\" name=\"startDate\" value={project.startDate} onChange={handleChange} /&gt;\n      &lt;/div&gt;\n      &lt;div&gt;\n        &lt;label&gt;Type&lt;/label&gt;&lt;br /&gt;\n        &lt;input name=\"type\" value={project.type} onChange={handleChange} /&gt;\n      &lt;/div&gt;\n      &lt;div&gt;\n        &lt;label&gt;Department&lt;/label&gt;&lt;br /&gt;\n        &lt;select name=\"departmentId\" value={project.departmentId} onChange={handleChange}&gt;\n          {departments.map(d =&gt; (\n            &lt;option key={d.id} value={d.id}&gt;{d.name}&lt;/option&gt;\n          ))}\n        &lt;/select&gt;\n      &lt;/div&gt;\n      &lt;button type=\"submit\"&gt;Update&lt;/button&gt;\n    &lt;/form&gt;\n  );\n}\nexport default EditProject;</code></pre><p><b>Điểm cốt lõi:</b> form có kiểm soát (<code>value</code> + <code>onChange</code>) giúp đổ dữ liệu tự động; giá trị của <code>&lt;select&gt;</code> bằng <code>project.departmentId</code> nên department hiện tại được chọn sẵn; kiểm tra hợp lệ trước khi <code>PUT</code>; <code>useNavigate('/')</code> quay về trang chủ sau khi thành công.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Route /projects/edit/:id opens from the \"Edit\" link and the form is pre-filled with the project's current data.|||Route /projects/edit/:id mở được từ liên kết \"Edit\" và form được đổ sẵn dữ liệu hiện tại của project.",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "c2",
              "criterion": "Form is laid out as in Figure 5, and the Department drop-down is loaded with all departments and pre-selects this project's department.|||Form bố cục như Hình 5, và drop-down Department nạp đủ mọi department và chọn sẵn đúng department của project.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c3",
              "criterion": "Blank Project name shows the message \"Please enter the form fields that are required.\" and blocks the update.|||Project name để trống thì hiện thông báo \"Please enter the form fields that are required.\" và chặn cập nhật.",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "c4",
              "criterion": "Successful PUT update shows \"Update success.\" and navigates back to the home page.|||Cập nhật PUT thành công thì hiện \"Update success.\" và điều hướng về trang chủ.",
              "weight": 1,
              "maxScore": 1.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE16-q3.png"
        }
      ]
    }
  ]
};
