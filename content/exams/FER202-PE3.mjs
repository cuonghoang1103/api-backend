export default {
  "course": {
    "courseCode": "FER202"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE3-SP26B5PEPape",
      "title": "FER202 — Practical Exam Đề 3 (SP26 - B5 - PE - Paper 2)|||FER202 — Đề thi thực hành số 3 (SP26 - B5 - PE - Paper 2)",
      "description": "React PE (WRITE): build components against a REST API, AI-graded.|||Đề PE React (viết): dựng component theo REST API, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>React / Front-end Development</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>React / Front-end Development</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 2,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p><b>FER202 – Practical Exam (PE) – SP26 – Paper 2 – Company Management (React).</b> A single React SPA built on top of a <code>json-server</code> REST API. Run the mock API with <code>json-server --watch database.json --port 9999</code> and the app with <code>npm start</code> (component routing at <code>http://localhost:3000</code>). The data model in <code>database.json</code> is: <b>departments</b> (<code>id/depId</code>, <code>DepName</code>/<code>depName</code>, <code>managerId</code>), <b>employees</b> (<code>EmployeeId</code>/<code>id</code>, <code>Fullname</code>, <code>Gender</code>, <code>Birthdate</code>, <code>depId</code>, <code>supervisorId</code>, <code>empSalary</code>), <b>dependents</b> (<code>Name</code>, <code>Birthdate</code>, <code>Relationship</code>, <code>employeeId</code>), <b>projects</b> (<code>id</code>, <code>projectName</code>) and <b>workons</b> (<code>WorkId</code>, <code>employeeId</code>, <code>projectId</code>/<code>projectName</code>, <code>workHours</code>). The three questions build ONE page incrementally: Department List (Q1) → Employee &amp; Dependents accordion (Q2) → Work Hours management (Q3). Do NOT create files outside <code>src/</code>. Total: 10 points.</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p><b>FER202 – Bài thi thực hành (PE) – SP26 – Đề 2 – Quản lý Công ty (React).</b> Một ứng dụng React SPA dựng trên REST API của <code>json-server</code>. Chạy API giả bằng <code>json-server --watch database.json --port 9999</code> và chạy app bằng <code>npm start</code> (định tuyến component ở <code>http://localhost:3000</code>). Mô hình dữ liệu trong <code>database.json</code>: <b>departments</b> (<code>id/depId</code>, <code>DepName</code>/<code>depName</code>, <code>managerId</code>), <b>employees</b> (<code>EmployeeId</code>/<code>id</code>, <code>Fullname</code>, <code>Gender</code>, <code>Birthdate</code>, <code>depId</code>, <code>supervisorId</code>, <code>empSalary</code>), <b>dependents</b> (<code>Name</code>, <code>Birthdate</code>, <code>Relationship</code>, <code>employeeId</code>), <b>projects</b> (<code>id</code>, <code>projectName</code>) và <b>workons</b> (<code>WorkId</code>, <code>employeeId</code>, <code>projectId</code>/<code>projectName</code>, <code>workHours</code>). Ba câu cùng dựng MỘT trang theo từng bước: Danh sách phòng ban (Q1) → Accordion nhân viên &amp; người phụ thuộc (Q2) → Quản lý giờ làm (Q3). KHÔNG tạo file ngoài thư mục <code>src/</code>. Tổng: 10 điểm.</p></div></div><div class=\"ml-en\"><p><b>Question 1 (2 points)</b></p><p><b>Question 1 (2 points) – Department List Page.</b></p><p><b>Objective:</b> Build a React page that displays a list of departments retrieved from a REST API. Component routing at <code>http://localhost:3000</code> (0 points if the URL is incorrect).</p><p><b>Requirements:</b></p><ul><li>Fetch data from the API.</li><li>Show the list of departments in a table format with columns: <code>| DepName | Manager | Employees |</code></li><li>Show the manager's full name (retrieved from employees using <code>managerId</code>).</li><li>Provide a <b>View</b> button to display the employees of that department.</li><li>When the <b>View</b> button is clicked: filter and display employees based on <code>depId</code> (client-side filtering or API call) — feeds Question 2.</li></ul><p>Page title: <b>COMPANY MANAGEMENT</b>; section heading <b>Department List</b>. Example rows: Human Resources / An Nguyen / [View]; IT / Binh Tran / [View]; Finance / Khanh Do / [View].</p></div><div class=\"ml-vi\"><p><b>Câu 1 (2 điểm)</b></p><p><b>Câu 1 (2 điểm) – Trang Danh sách Phòng ban.</b></p><p><b>Mục tiêu:</b> Dựng một trang React hiển thị danh sách phòng ban lấy từ REST API. Định tuyến component ở <code>http://localhost:3000</code> (0 điểm nếu URL sai).</p><p><b>Yêu cầu:</b></p><ul><li>Lấy dữ liệu từ API.</li><li>Hiển thị danh sách phòng ban dạng bảng với các cột: <code>| DepName | Manager | Employees |</code></li><li>Hiển thị họ tên đầy đủ của người quản lý (lấy từ bảng employees qua <code>managerId</code>).</li><li>Cung cấp nút <b>View</b> để hiển thị nhân viên của phòng ban đó.</li><li>Khi bấm nút <b>View</b>: lọc và hiển thị nhân viên theo <code>depId</code> (lọc phía client hoặc gọi API) — nối sang Câu 2.</li></ul><p>Tiêu đề trang: <b>COMPANY MANAGEMENT</b>; tiêu đề mục <b>Department List</b>. Các dòng ví dụ: Human Resources / An Nguyen / [View]; IT / Binh Tran / [View]; Finance / Khanh Do / [View].</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><b>Key points:</b> fetch <code>departments</code> and <code>employees</code> once with <code>useEffect</code>; resolve the manager name by looking up <code>managerId</code> in the employees array; the <b>View</b> button lifts the selected <code>depId</code> to the parent App via a callback so Q2 can render that department's employees.</p><pre><code>// src/App.js\nimport { useState } from \"react\";\nimport DepartmentList from \"./components/DepartmentList\";\nimport EmployeeList from \"./components/EmployeeList\";\n\nexport default function App() {\n  const [selectedDepId, setSelectedDepId] = useState(null);\n  return (\n    &lt;div className=\"container\"&gt;\n      &lt;h1 style={{ textAlign: \"center\" }}&gt;COMPANY MANAGEMENT&lt;/h1&gt;\n      &lt;div style={{ display: \"flex\", gap: 24 }}&gt;\n        &lt;DepartmentList onView={setSelectedDepId} /&gt;\n        {selectedDepId != null &amp;&amp; &lt;EmployeeList depId={selectedDepId} /&gt;}\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\n\n// src/components/DepartmentList.js\nimport { useEffect, useState } from \"react\";\nimport axios from \"axios\";\n\nconst API = \"http://localhost:9999\";\n\nexport default function DepartmentList({ onView }) {\n  const [departments, setDepartments] = useState([]);\n  const [employees, setEmployees] = useState([]);\n\n  useEffect(() =&gt; {\n    axios.get(`${API}/departments`).then((r) =&gt; setDepartments(r.data));\n    axios.get(`${API}/employees`).then((r) =&gt; setEmployees(r.data));\n  }, []);\n\n  const managerName = (managerId) =&gt; {\n    const m = employees.find((e) =&gt; e.EmployeeId === managerId || e.id === managerId);\n    return m ? m.Fullname : \"\";\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;Department List&lt;/h2&gt;\n      &lt;table border=\"1\" cellPadding=\"8\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;&lt;th&gt;DepName&lt;/th&gt;&lt;th&gt;Manager&lt;/th&gt;&lt;th&gt;Employees&lt;/th&gt;&lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {departments.map((d) =&gt; (\n            &lt;tr key={d.id ?? d.depId}&gt;\n              &lt;td&gt;{d.DepName ?? d.depName}&lt;/td&gt;\n              &lt;td&gt;{managerName(d.managerId)}&lt;/td&gt;\n              &lt;td&gt;\n                &lt;button onClick={() =&gt; onView(d.id ?? d.depId)}&gt;View&lt;/button&gt;\n              &lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}</code></pre></div><div class=\"ml-vi\"><p><b>Điểm mấu chốt:</b> dùng <code>useEffect</code> lấy một lần <code>departments</code> và <code>employees</code>; tra tên quản lý bằng cách tìm <code>managerId</code> trong mảng employees; nút <b>View</b> đẩy <code>depId</code> đã chọn lên App cha qua callback để Câu 2 hiển thị nhân viên của phòng ban đó.</p><pre><code>// src/App.js\nimport { useState } from \"react\";\nimport DepartmentList from \"./components/DepartmentList\";\nimport EmployeeList from \"./components/EmployeeList\";\n\nexport default function App() {\n  const [selectedDepId, setSelectedDepId] = useState(null);\n  return (\n    &lt;div className=\"container\"&gt;\n      &lt;h1 style={{ textAlign: \"center\" }}&gt;COMPANY MANAGEMENT&lt;/h1&gt;\n      &lt;div style={{ display: \"flex\", gap: 24 }}&gt;\n        &lt;DepartmentList onView={setSelectedDepId} /&gt;\n        {selectedDepId != null &amp;&amp; &lt;EmployeeList depId={selectedDepId} /&gt;}\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}\n\n// src/components/DepartmentList.js\nimport { useEffect, useState } from \"react\";\nimport axios from \"axios\";\n\nconst API = \"http://localhost:9999\";\n\nexport default function DepartmentList({ onView }) {\n  const [departments, setDepartments] = useState([]);\n  const [employees, setEmployees] = useState([]);\n\n  useEffect(() =&gt; {\n    axios.get(`${API}/departments`).then((r) =&gt; setDepartments(r.data));\n    axios.get(`${API}/employees`).then((r) =&gt; setEmployees(r.data));\n  }, []);\n\n  // Tra tên quản lý theo managerId\n  const managerName = (managerId) =&gt; {\n    const m = employees.find((e) =&gt; e.EmployeeId === managerId || e.id === managerId);\n    return m ? m.Fullname : \"\";\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;Department List&lt;/h2&gt;\n      &lt;table border=\"1\" cellPadding=\"8\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;&lt;th&gt;DepName&lt;/th&gt;&lt;th&gt;Manager&lt;/th&gt;&lt;th&gt;Employees&lt;/th&gt;&lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {departments.map((d) =&gt; (\n            &lt;tr key={d.id ?? d.depId}&gt;\n              &lt;td&gt;{d.DepName ?? d.depName}&lt;/td&gt;\n              &lt;td&gt;{managerName(d.managerId)}&lt;/td&gt;\n              &lt;td&gt;\n                &lt;button onClick={() =&gt; onView(d.id ?? d.depId)}&gt;View&lt;/button&gt;\n              &lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><i>Ghi chú:</i> có thể thay <code>axios</code> bằng <code>fetch</code>; dùng <code>d.DepName ?? d.depName</code> để phòng khi tên trường trong <code>database.json</code> khác nhau.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Fetches departments from the API with useEffect and renders them in a table with columns DepName | Manager | Employees.|||Lấy departments từ API bằng useEffect và hiển thị dạng bảng với các cột DepName | Manager | Employees.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Resolves and shows the manager's full name via managerId lookup, and the View button lifts the department's depId to show its employees (Q2 wiring).|||Tra và hiển thị đúng họ tên quản lý qua managerId, và nút View đẩy depId của phòng ban để hiển thị nhân viên (nối Câu 2).",
              "weight": 1,
              "maxScore": 1
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE3.png"
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2 (4 points)</b></p><p><b>Question 2 (4 points) – Employee &amp; Dependents Information.</b></p><p><b>Objective:</b> Show the employees of the selected department along with their personal and dependents information.</p><p><b>Requirements:</b></p><ul><li>Fetch data from the API.</li><li>Display the employee list with an <b>Accordion</b> component (each panel header <code>#N - Fullname</code>, expand/collapse).</li><li>For each employee, show the following information: <b>EmployeeId, Fullname, Gender, Birthdate</b>.</li><li>If <code>supervisorId = null</code>, display \"<b>Manager</b>\" (badge next to the name).</li><li>For each employee, show their dependents in a table: <code>| No. | Name | Birthdate | Relationship |</code></li><li>If no dependents exist: display an empty table or no rows.</li></ul><p><b>Figure 1</b>: an expanded panel \"#1 - Tran Binh - Manager\" showing EmployeeId: 2, Gender: Male, BirthDate: 1983-03-12, a <b>View Work Hours</b> button, and a Dependents List with 4 rows (Hoa Tran / Wife, Nam Tran / Son, Linh Tran / Daughter, Ba Tran / Mother). <b>Figure 2</b>: \"#2 - Le Chi\" expanded with the Dependents List header shown but no rows (empty).</p></div><div class=\"ml-vi\"><p><b>Câu 2 (4 điểm)</b></p><p><b>Câu 2 (4 điểm) – Thông tin Nhân viên &amp; Người phụ thuộc.</b></p><p><b>Mục tiêu:</b> Hiển thị nhân viên của phòng ban đã chọn cùng thông tin cá nhân và người phụ thuộc của họ.</p><p><b>Yêu cầu:</b></p><ul><li>Lấy dữ liệu từ API.</li><li>Hiển thị danh sách nhân viên bằng component <b>Accordion</b> (mỗi panel có header <code>#N - Fullname</code>, mở/đóng được).</li><li>Với mỗi nhân viên, hiển thị: <b>EmployeeId, Fullname, Gender, Birthdate</b>.</li><li>Nếu <code>supervisorId = null</code>, hiển thị \"<b>Manager</b>\" (nhãn cạnh tên).</li><li>Với mỗi nhân viên, hiển thị người phụ thuộc dạng bảng: <code>| No. | Name | Birthdate | Relationship |</code></li><li>Nếu không có người phụ thuộc: hiển thị bảng rỗng hoặc không có dòng nào.</li></ul><p><b>Hình 1</b>: panel mở \"#1 - Tran Binh - Manager\" hiển thị EmployeeId: 2, Gender: Male, BirthDate: 1983-03-12, nút <b>View Work Hours</b>, và bảng Dependents List 4 dòng (Hoa Tran / Wife, Nam Tran / Son, Linh Tran / Daughter, Ba Tran / Mother). <b>Hình 2</b>: \"#2 - Le Chi\" mở với header bảng Dependents nhưng không có dòng nào (rỗng).</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><b>Key points:</b> fetch employees for this <code>depId</code> and all dependents; a lightweight controlled Accordion tracks the open panel index; the header appends \"Manager\" when <code>supervisorId == null</code>; the dependents table always renders its header and maps the employee's dependents (empty array → header only). The <b>View Work Hours</b> button hands the employee to Q3.</p><pre><code>// src/components/EmployeeList.js\nimport { useEffect, useState } from \"react\";\nimport axios from \"axios\";\nimport WorkHours from \"./WorkHours\";\n\nconst API = \"http://localhost:9999\";\n\nexport default function EmployeeList({ depId }) {\n  const [employees, setEmployees] = useState([]);\n  const [dependents, setDependents] = useState([]);\n  const [openId, setOpenId] = useState(null);   // accordion open panel\n  const [workEmp, setWorkEmp] = useState(null);  // employee for Q3\n\n  useEffect(() =&gt; {\n    axios.get(`${API}/employees?depId=${depId}`).then((r) =&gt; setEmployees(r.data));\n    axios.get(`${API}/dependents`).then((r) =&gt; setDependents(r.data));\n    setOpenId(null);\n    setWorkEmp(null);\n  }, [depId]);\n\n  const depsOf = (empId) =&gt;\n    dependents.filter((d) =&gt; d.employeeId === empId);\n\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;Employee List&lt;/h2&gt;\n      {employees.map((e, i) =&gt; {\n        const id = e.EmployeeId ?? e.id;\n        const isOpen = openId === id;\n        return (\n          &lt;div key={id} className=\"accordion-item\"&gt;\n            &lt;div\n              className=\"accordion-header\"\n              onClick={() =&gt; setOpenId(isOpen ? null : id)}\n              style={{ cursor: \"pointer\", fontWeight: 600 }}\n            &gt;\n              #{i + 1} - {e.Fullname}\n              {e.supervisorId == null &amp;&amp; &lt;span&gt; - Manager&lt;/span&gt;}\n            &lt;/div&gt;\n            {isOpen &amp;&amp; (\n              &lt;div className=\"accordion-body\"&gt;\n                &lt;ul&gt;\n                  &lt;li&gt;EmployeeId: {id}&lt;/li&gt;\n                  &lt;li&gt;Gender: {e.Gender}&lt;/li&gt;\n                  &lt;li&gt;BirthDate: {e.Birthdate}&lt;/li&gt;\n                &lt;/ul&gt;\n                &lt;button onClick={() =&gt; setWorkEmp(e)}&gt;View Work Hours&lt;/button&gt;\n\n                &lt;h4&gt;Dependents List&lt;/h4&gt;\n                &lt;table border=\"1\" cellPadding=\"6\"&gt;\n                  &lt;thead&gt;\n                    &lt;tr&gt;&lt;th&gt;No.&lt;/th&gt;&lt;th&gt;Name&lt;/th&gt;&lt;th&gt;Birthdate&lt;/th&gt;&lt;th&gt;Relationship&lt;/th&gt;&lt;/tr&gt;\n                  &lt;/thead&gt;\n                  &lt;tbody&gt;\n                    {depsOf(id).map((d, j) =&gt; (\n                      &lt;tr key={j}&gt;\n                        &lt;td&gt;{j + 1}&lt;/td&gt;\n                        &lt;td&gt;{d.Name}&lt;/td&gt;\n                        &lt;td&gt;{d.Birthdate}&lt;/td&gt;\n                        &lt;td&gt;{d.Relationship}&lt;/td&gt;\n                      &lt;/tr&gt;\n                    ))}\n                  &lt;/tbody&gt;\n                &lt;/table&gt;\n\n                {workEmp &amp;&amp; (workEmp.EmployeeId ?? workEmp.id) === id &amp;&amp; (\n                  &lt;WorkHours employee={workEmp} /&gt;\n                )}\n              &lt;/div&gt;\n            )}\n          &lt;/div&gt;\n        );\n      })}\n    &lt;/div&gt;\n  );\n}</code></pre></div><div class=\"ml-vi\"><p><b>Điểm mấu chốt:</b> lấy nhân viên theo <code>depId</code> và toàn bộ dependents; Accordion tự viết dùng state <code>openId</code> theo dõi panel đang mở; header thêm \"Manager\" khi <code>supervisorId == null</code>; bảng dependents luôn hiển thị header và map người phụ thuộc của nhân viên (mảng rỗng → chỉ header). Nút <b>View Work Hours</b> chuyển nhân viên sang Câu 3.</p><pre><code>// src/components/EmployeeList.js\nimport { useEffect, useState } from \"react\";\nimport axios from \"axios\";\nimport WorkHours from \"./WorkHours\";\n\nconst API = \"http://localhost:9999\";\n\nexport default function EmployeeList({ depId }) {\n  const [employees, setEmployees] = useState([]);\n  const [dependents, setDependents] = useState([]);\n  const [openId, setOpenId] = useState(null);   // panel đang mở\n  const [workEmp, setWorkEmp] = useState(null);  // nhân viên cho Câu 3\n\n  useEffect(() =&gt; {\n    axios.get(`${API}/employees?depId=${depId}`).then((r) =&gt; setEmployees(r.data));\n    axios.get(`${API}/dependents`).then((r) =&gt; setDependents(r.data));\n    setOpenId(null);\n    setWorkEmp(null);\n  }, [depId]);\n\n  const depsOf = (empId) =&gt;\n    dependents.filter((d) =&gt; d.employeeId === empId);\n\n  return (\n    &lt;div&gt;\n      &lt;h2&gt;Employee List&lt;/h2&gt;\n      {employees.map((e, i) =&gt; {\n        const id = e.EmployeeId ?? e.id;\n        const isOpen = openId === id;\n        return (\n          &lt;div key={id} className=\"accordion-item\"&gt;\n            &lt;div\n              className=\"accordion-header\"\n              onClick={() =&gt; setOpenId(isOpen ? null : id)}\n              style={{ cursor: \"pointer\", fontWeight: 600 }}\n            &gt;\n              #{i + 1} - {e.Fullname}\n              {e.supervisorId == null &amp;&amp; &lt;span&gt; - Manager&lt;/span&gt;}\n            &lt;/div&gt;\n            {isOpen &amp;&amp; (\n              &lt;div className=\"accordion-body\"&gt;\n                &lt;ul&gt;\n                  &lt;li&gt;EmployeeId: {id}&lt;/li&gt;\n                  &lt;li&gt;Gender: {e.Gender}&lt;/li&gt;\n                  &lt;li&gt;BirthDate: {e.Birthdate}&lt;/li&gt;\n                &lt;/ul&gt;\n                &lt;button onClick={() =&gt; setWorkEmp(e)}&gt;View Work Hours&lt;/button&gt;\n\n                &lt;h4&gt;Dependents List&lt;/h4&gt;\n                &lt;table border=\"1\" cellPadding=\"6\"&gt;\n                  &lt;thead&gt;\n                    &lt;tr&gt;&lt;th&gt;No.&lt;/th&gt;&lt;th&gt;Name&lt;/th&gt;&lt;th&gt;Birthdate&lt;/th&gt;&lt;th&gt;Relationship&lt;/th&gt;&lt;/tr&gt;\n                  &lt;/thead&gt;\n                  &lt;tbody&gt;\n                    {depsOf(id).map((d, j) =&gt; (\n                      &lt;tr key={j}&gt;\n                        &lt;td&gt;{j + 1}&lt;/td&gt;\n                        &lt;td&gt;{d.Name}&lt;/td&gt;\n                        &lt;td&gt;{d.Birthdate}&lt;/td&gt;\n                        &lt;td&gt;{d.Relationship}&lt;/td&gt;\n                      &lt;/tr&gt;\n                    ))}\n                  &lt;/tbody&gt;\n                &lt;/table&gt;\n\n                {workEmp &amp;&amp; (workEmp.EmployeeId ?? workEmp.id) === id &amp;&amp; (\n                  &lt;WorkHours employee={workEmp} /&gt;\n                )}\n              &lt;/div&gt;\n            )}\n          &lt;/div&gt;\n        );\n      })}\n    &lt;/div&gt;\n  );\n}</code></pre><p><i>Ghi chú:</i> có thể dùng <code>Accordion</code> của <code>react-bootstrap</code> thay cho phiên bản tự viết; điều quan trọng là mở/đóng đúng, hiển thị đủ 4 trường và nhãn \"Manager\", cùng bảng dependents (rỗng thì chỉ có header).</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Employees of the selected department are displayed in an Accordion (expand/collapse) showing EmployeeId, Fullname, Gender, Birthdate.|||Nhân viên của phòng ban đã chọn hiển thị trong Accordion (mở/đóng) kèm EmployeeId, Fullname, Gender, Birthdate.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c2",
              "criterion": "\"Manager\" label shown when supervisorId is null.|||Hiển thị nhãn \"Manager\" khi supervisorId là null.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "Dependents table with columns No. | Name | Birthdate | Relationship renders correctly, including the empty case (header/no rows).|||Bảng người phụ thuộc No. | Name | Birthdate | Relationship hiển thị đúng, kể cả trường hợp rỗng (header/không có dòng).",
              "weight": 1,
              "maxScore": 1.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE3.png"
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 3 (4 points)</b></p><p><b>Question 3 (4 points) – Work Hours Management.</b></p><p><b>Objective:</b> Manage and display the work hours of employees on projects.</p><p><b>1. View Work Hours:</b> When the <b>View Work Hours</b> button is clicked, show all work records of that employee in a table <code>| WorkId | Project Name | Work Hours | Salary |</code>. Fetch data from the API.</p><p><b>2. Salary Calculation:</b> Calculate salary using:</p><pre><code>hourly = empSalary / 176\nsalary = hourly * workHours</code></pre><p>Display total salary: <b>Projects Salary: XXX VND</b>.</p><p><b>3. Handle Empty Data:</b> If no work records exist, display \"<b>No work hours</b>\".</p><p><b>4. Add New Work Hours:</b> Provide a form with: Project name (dropdown from project list, fetch from API) and Work hours (number input).</p><p><b>5. Validation:</b> Project must be selected; Work hours must be greater than 0. If validation fails, show error messages (e.g. \"Project name is required\", \"Work hours must be greater than zero\").</p><p><b>6. Business Rule:</b> Do NOT allow duplicate project assignment for the same employee. If the project already exists → show error: \"<b>This project already exists. Please update work hours instead</b>\".</p><p><b>7. Submit Data:</b> If valid, send request <code>POST http://localhost:9999/workons</code>. Update the UI immediately after a successful submission (new row appears and Projects Salary is recalculated; form resets).</p></div><div class=\"ml-vi\"><p><b>Câu 3 (4 điểm)</b></p><p><b>Câu 3 (4 điểm) – Quản lý Giờ làm.</b></p><p><b>Mục tiêu:</b> Quản lý và hiển thị giờ làm của nhân viên trên các dự án.</p><p><b>1. Xem giờ làm:</b> Khi bấm nút <b>View Work Hours</b>, hiển thị tất cả bản ghi giờ làm của nhân viên đó dạng bảng <code>| WorkId | Project Name | Work Hours | Salary |</code>. Lấy dữ liệu từ API.</p><p><b>2. Tính lương:</b> Tính lương theo công thức:</p><pre><code>hourly = empSalary / 176\nsalary = hourly * workHours</code></pre><p>Hiển thị tổng lương: <b>Projects Salary: XXX VND</b>.</p><p><b>3. Xử lý dữ liệu rỗng:</b> Nếu không có bản ghi nào, hiển thị \"<b>No work hours</b>\".</p><p><b>4. Thêm giờ làm mới:</b> Cung cấp form gồm: Project name (dropdown lấy từ danh sách project qua API) và Work hours (ô nhập số).</p><p><b>5. Kiểm tra hợp lệ:</b> Phải chọn project; Work hours phải lớn hơn 0. Nếu không hợp lệ, hiển thị thông báo lỗi (ví dụ \"Project name is required\", \"Work hours must be greater than zero\").</p><p><b>6. Quy tắc nghiệp vụ:</b> KHÔNG cho phép gán trùng project cho cùng một nhân viên. Nếu project đã tồn tại → báo lỗi: \"<b>This project already exists. Please update work hours instead</b>\".</p><p><b>7. Gửi dữ liệu:</b> Nếu hợp lệ, gửi request <code>POST http://localhost:9999/workons</code>. Cập nhật UI ngay sau khi gửi thành công (dòng mới xuất hiện và Projects Salary tính lại; form reset).</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><b>Key points:</b> load the employee's <code>workons</code> plus the <code>projects</code> list; compute <code>hourly = empSalary/176</code> and <code>salary = hourly * workHours</code> per row, and sum into \"Projects Salary\"; render \"No work hours\" when the list is empty; validate project selected + hours &gt; 0; block duplicate projects with the exact required message; on success POST and prepend the new row to state so the UI and total update immediately, then reset the form.</p><pre><code>// src/components/WorkHours.js\nimport { useEffect, useState } from \"react\";\nimport axios from \"axios\";\n\nconst API = \"http://localhost:9999\";\nconst fmt = (n) =&gt; Math.round(n).toLocaleString(\"vi-VN\");\n\nexport default function WorkHours({ employee }) {\n  const empId = employee.EmployeeId ?? employee.id;\n  const empSalary = employee.empSalary ?? 0;\n\n  const [works, setWorks] = useState([]);\n  const [projects, setProjects] = useState([]);\n  const [projectId, setProjectId] = useState(\"\");\n  const [hours, setHours] = useState(0);\n  const [errors, setErrors] = useState([]);\n\n  useEffect(() =&gt; {\n    axios.get(`${API}/workons?employeeId=${empId}`).then((r) =&gt; setWorks(r.data));\n    axios.get(`${API}/projects`).then((r) =&gt; setProjects(r.data));\n  }, [empId]);\n\n  const projName = (w) =&gt;\n    w.projectName ?? projects.find((p) =&gt; p.id === w.projectId)?.projectName ?? \"\";\n\n  const salaryOf = (workHours) =&gt; (empSalary / 176) * Number(workHours);\n  const totalSalary = works.reduce((s, w) =&gt; s + salaryOf(w.workHours), 0);\n\n  const handleAdd = async (e) =&gt; {\n    e.preventDefault();\n    const errs = [];\n    if (!projectId) errs.push(\"Project name is required\");\n    if (!(Number(hours) &gt; 0)) errs.push(\"Work hours must be greater than zero\");\n    // Business rule: no duplicate project for the same employee\n    const dup = works.some(\n      (w) =&gt; String(w.projectId) === String(projectId) || projName(w) === selectedName()\n    );\n    if (errs.length === 0 &amp;&amp; dup)\n      errs.push(\"This project already exists. Please update work hours instead\");\n    setErrors(errs);\n    if (errs.length) return;\n\n    const p = projects.find((x) =&gt; String(x.id) === String(projectId));\n    const body = {\n      employeeId: empId,\n      projectId: p.id,\n      projectName: p.projectName,\n      workHours: Number(hours),\n    };\n    const { data } = await axios.post(`${API}/workons`, body);\n    setWorks((prev) =&gt; [...prev, data]); // update UI immediately\n    setProjectId(\"\");\n    setHours(0);\n    setErrors([]);\n  };\n\n  const selectedName = () =&gt;\n    projects.find((x) =&gt; String(x.id) === String(projectId))?.projectName;\n\n  return (\n    &lt;div&gt;\n      &lt;h4&gt;Work on projects&lt;/h4&gt;\n      {works.length === 0 ? (\n        &lt;p&gt;No work hours&lt;/p&gt;\n      ) : (\n        &lt;table border=\"1\" cellPadding=\"6\"&gt;\n          &lt;thead&gt;\n            &lt;tr&gt;&lt;th&gt;WorkId&lt;/th&gt;&lt;th&gt;Project Name&lt;/th&gt;&lt;th&gt;Work Hours&lt;/th&gt;&lt;th&gt;Salary&lt;/th&gt;&lt;/tr&gt;\n          &lt;/thead&gt;\n          &lt;tbody&gt;\n            {works.map((w) =&gt; (\n              &lt;tr key={w.WorkId ?? w.id}&gt;\n                &lt;td&gt;{w.WorkId ?? w.id}&lt;/td&gt;\n                &lt;td&gt;{projName(w)}&lt;/td&gt;\n                &lt;td&gt;{w.workHours}&lt;/td&gt;\n                &lt;td&gt;{fmt(salaryOf(w.workHours))}&lt;/td&gt;\n              &lt;/tr&gt;\n            ))}\n          &lt;/tbody&gt;\n        &lt;/table&gt;\n      )}\n      {works.length &gt; 0 &amp;&amp; (\n        &lt;p&gt;&lt;b&gt;Projects Salary: {fmt(totalSalary)} VND&lt;/b&gt;&lt;/p&gt;\n      )}\n\n      &lt;h4&gt;Add new work hours&lt;/h4&gt;\n      {errors.length &gt; 0 &amp;&amp; (\n        &lt;div style={{ color: \"crimson\" }}&gt;\n          Errors: {errors.join(\", \")}\n        &lt;/div&gt;\n      )}\n      &lt;form onSubmit={handleAdd}&gt;\n        &lt;select value={projectId} onChange={(e) =&gt; setProjectId(e.target.value)}&gt;\n          &lt;option value=\"\"&gt;--- Choice a project name ---&lt;/option&gt;\n          {projects.map((p) =&gt; (\n            &lt;option key={p.id} value={p.id}&gt;{p.projectName}&lt;/option&gt;\n          ))}\n        &lt;/select&gt;\n        &lt;input\n          type=\"number\"\n          value={hours}\n          onChange={(e) =&gt; setHours(e.target.value)}\n        /&gt;\n        &lt;button type=\"submit\"&gt;Add&lt;/button&gt;\n      &lt;/form&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><i>Note:</i> the POST target shown in one figure is <code>:9999/workons</code> (same json-server port used for GET); the prompt text also writes <code>:9999/workons</code>. Use the port your json-server runs on (9999). Salary is formatted with thousands separators to match \"7.102.273 VND\".</p></div><div class=\"ml-vi\"><p><b>Điểm mấu chốt:</b> tải <code>workons</code> của nhân viên cùng danh sách <code>projects</code>; tính <code>hourly = empSalary/176</code> và <code>salary = hourly * workHours</code> cho từng dòng rồi cộng lại thành \"Projects Salary\"; hiển thị \"No work hours\" khi rỗng; kiểm tra đã chọn project + hours &gt; 0; chặn project trùng với đúng thông báo yêu cầu; khi hợp lệ thì POST và thêm dòng mới vào state để UI và tổng cập nhật ngay, rồi reset form.</p><pre><code>// src/components/WorkHours.js\nimport { useEffect, useState } from \"react\";\nimport axios from \"axios\";\n\nconst API = \"http://localhost:9999\";\nconst fmt = (n) =&gt; Math.round(n).toLocaleString(\"vi-VN\");\n\nexport default function WorkHours({ employee }) {\n  const empId = employee.EmployeeId ?? employee.id;\n  const empSalary = employee.empSalary ?? 0;\n\n  const [works, setWorks] = useState([]);\n  const [projects, setProjects] = useState([]);\n  const [projectId, setProjectId] = useState(\"\");\n  const [hours, setHours] = useState(0);\n  const [errors, setErrors] = useState([]);\n\n  useEffect(() =&gt; {\n    axios.get(`${API}/workons?employeeId=${empId}`).then((r) =&gt; setWorks(r.data));\n    axios.get(`${API}/projects`).then((r) =&gt; setProjects(r.data));\n  }, [empId]);\n\n  const projName = (w) =&gt;\n    w.projectName ?? projects.find((p) =&gt; p.id === w.projectId)?.projectName ?? \"\";\n\n  // hourly = empSalary / 176 ; salary = hourly * workHours\n  const salaryOf = (workHours) =&gt; (empSalary / 176) * Number(workHours);\n  const totalSalary = works.reduce((s, w) =&gt; s + salaryOf(w.workHours), 0);\n\n  const selectedName = () =&gt;\n    projects.find((x) =&gt; String(x.id) === String(projectId))?.projectName;\n\n  const handleAdd = async (e) =&gt; {\n    e.preventDefault();\n    const errs = [];\n    if (!projectId) errs.push(\"Project name is required\");\n    if (!(Number(hours) &gt; 0)) errs.push(\"Work hours must be greater than zero\");\n    // Quy tắc: không cho trùng project cho cùng nhân viên\n    const dup = works.some(\n      (w) =&gt; String(w.projectId) === String(projectId) || projName(w) === selectedName()\n    );\n    if (errs.length === 0 &amp;&amp; dup)\n      errs.push(\"This project already exists. Please update work hours instead\");\n    setErrors(errs);\n    if (errs.length) return;\n\n    const p = projects.find((x) =&gt; String(x.id) === String(projectId));\n    const body = {\n      employeeId: empId,\n      projectId: p.id,\n      projectName: p.projectName,\n      workHours: Number(hours),\n    };\n    const { data } = await axios.post(`${API}/workons`, body);\n    setWorks((prev) =&gt; [...prev, data]); // cập nhật UI ngay\n    setProjectId(\"\");\n    setHours(0);\n    setErrors([]);\n  };\n\n  return (\n    &lt;div&gt;\n      &lt;h4&gt;Work on projects&lt;/h4&gt;\n      {works.length === 0 ? (\n        &lt;p&gt;No work hours&lt;/p&gt;\n      ) : (\n        &lt;table border=\"1\" cellPadding=\"6\"&gt;\n          &lt;thead&gt;\n            &lt;tr&gt;&lt;th&gt;WorkId&lt;/th&gt;&lt;th&gt;Project Name&lt;/th&gt;&lt;th&gt;Work Hours&lt;/th&gt;&lt;th&gt;Salary&lt;/th&gt;&lt;/tr&gt;\n          &lt;/thead&gt;\n          &lt;tbody&gt;\n            {works.map((w) =&gt; (\n              &lt;tr key={w.WorkId ?? w.id}&gt;\n                &lt;td&gt;{w.WorkId ?? w.id}&lt;/td&gt;\n                &lt;td&gt;{projName(w)}&lt;/td&gt;\n                &lt;td&gt;{w.workHours}&lt;/td&gt;\n                &lt;td&gt;{fmt(salaryOf(w.workHours))}&lt;/td&gt;\n              &lt;/tr&gt;\n            ))}\n          &lt;/tbody&gt;\n        &lt;/table&gt;\n      )}\n      {works.length &gt; 0 &amp;&amp; (\n        &lt;p&gt;&lt;b&gt;Projects Salary: {fmt(totalSalary)} VND&lt;/b&gt;&lt;/p&gt;\n      )}\n\n      &lt;h4&gt;Add new work hours&lt;/h4&gt;\n      {errors.length &gt; 0 &amp;&amp; (\n        &lt;div style={{ color: \"crimson\" }}&gt;\n          Errors: {errors.join(\", \")}\n        &lt;/div&gt;\n      )}\n      &lt;form onSubmit={handleAdd}&gt;\n        &lt;select value={projectId} onChange={(e) =&gt; setProjectId(e.target.value)}&gt;\n          &lt;option value=\"\"&gt;--- Choice a project name ---&lt;/option&gt;\n          {projects.map((p) =&gt; (\n            &lt;option key={p.id} value={p.id}&gt;{p.projectName}&lt;/option&gt;\n          ))}\n        &lt;/select&gt;\n        &lt;input\n          type=\"number\"\n          value={hours}\n          onChange={(e) =&gt; setHours(e.target.value)}\n        /&gt;\n        &lt;button type=\"submit\"&gt;Add&lt;/button&gt;\n      &lt;/form&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><i>Ghi chú:</i> ảnh minh hoạ ghi POST tới <code>:9999/workons</code> (cùng cổng json-server dùng cho GET); phần chữ của đề cũng ghi <code>:9999/workons</code>. Dùng đúng cổng json-server đang chạy (9999). Lương định dạng dấu phân cách nghìn để khớp \"7.102.273 VND\".</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "View Work Hours shows a table WorkId | Project Name | Work Hours | Salary from the API, computes salary via hourly = empSalary/176 and salary = hourly*workHours, displays \"Projects Salary: XXX VND\" total, and shows \"No work hours\" when empty.|||View Work Hours hiển thị bảng WorkId | Project Name | Work Hours | Salary từ API, tính lương theo hourly = empSalary/176 và salary = hourly*workHours, hiển thị tổng \"Projects Salary: XXX VND\", và \"No work hours\" khi rỗng.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c2",
              "criterion": "Add form has a project dropdown (fetched from API) and a number input; validation rejects empty project and hours <= 0 with error messages.|||Form thêm có dropdown project (lấy từ API) và ô nhập số; kiểm tra chặn project rỗng và hours <= 0 kèm thông báo lỗi.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "Duplicate-project business rule shows the exact message \"This project already exists. Please update work hours instead\".|||Quy tắc trùng project hiển thị đúng thông báo \"This project already exists. Please update work hours instead\".",
              "weight": 1,
              "maxScore": 0.5
            },
            {
              "id": "c4",
              "criterion": "Valid submit POSTs to /workons and updates the UI immediately (new row + recalculated total, form reset).|||Submit hợp lệ POST tới /workons và cập nhật UI ngay (dòng mới + tính lại tổng, reset form).",
              "weight": 1,
              "maxScore": 1
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE3.png"
        }
      ]
    }
  ]
};
