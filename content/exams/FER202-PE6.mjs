export default {
  "course": {
    "courseCode": "FER202"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE6-PESP2025s2",
      "title": "FER202 — Practical Exam Đề 6 (PE - SP 2025 - Đề số 2)|||FER202 — Đề thi thực hành số 6 (PE - SP 2025 - Đề số 2)",
      "description": "React PE (WRITE): build components against a REST API, AI-graded.|||Đề PE React (viết): dựng component theo REST API, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>React / Front-end Development</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>React / Front-end Development</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p><b>FER202 Practical Exam — SP 2025 (Paper 2): Products Review System.</b></p><p>A React (Vite/CRA + hooks) single-page app backed by <b>json-server</b>. Before coding, copy <code>database.json</code> from the given folder and run two terminals:</p><ul><li><code>json-server --watch database.json --port 9999</code> (0 points if json-server is not running)</li><li><code>npm start</code> (app served at <code>http://localhost:3000</code>)</li></ul><p>The API exposes <code>GET/PATCH/PUT http://localhost:9999/products</code>. Each product has fields such as <code>id</code>, <code>Title</code>, <code>Price</code>, <code>Category</code> and an array <code>reviews</code> where each review has <code>Date</code>, <code>Reviewer</code>, <code>Comment</code>, <code>Rating</code>. Install <code>react-router-dom</code> and <code>axios</code> only from the already-provided packages (downloading extra libraries is forbidden). Answers below use React hooks, axios and react-router.</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p><b>Thi thực hành FER202 — SP 2025 (Đề 2): Hệ thống Đánh giá Sản phẩm (Products Review System).</b></p><p>Ứng dụng React một trang (Vite/CRA + hooks) dùng <b>json-server</b> làm API. Trước khi code, chép <code>database.json</code> từ thư mục given rồi mở hai Terminal:</p><ul><li><code>json-server --watch database.json --port 9999</code> (0 điểm nếu không chạy json-server)</li><li><code>npm start</code> (ứng dụng ở <code>http://localhost:3000</code>)</li></ul><p>API cung cấp <code>GET/PATCH/PUT http://localhost:9999/products</code>. Mỗi product có các trường như <code>id</code>, <code>Title</code>, <code>Price</code>, <code>Category</code> và mảng <code>reviews</code>, mỗi review gồm <code>Date</code>, <code>Reviewer</code>, <code>Comment</code>, <code>Rating</code>. Chỉ dùng <code>react-router-dom</code> và <code>axios</code> có sẵn (cấm tải thêm thư viện). Lời giải dưới đây dùng React hooks, axios và react-router.</p></div></div><div class=\"ml-en\"><p><b>Question 1 (4 marks) — Fetch and Display Products.</b></p><p>Build a component that fetches and displays all products from the API endpoint:</p><p><code>GET http://localhost:9999/products</code></p><p><b>Requirements:</b></p><ul><li>Route the component at URL <code>http://localhost:3000</code> (0 points if incorrect URL).</li><li>Design the layout correctly.</li><li>Show the <b>Title</b>, <b>Price</b>, <b>Category</b> and <b>Average Rate</b> (average rating of reviews) in the Card(s).</li><li>Add a dropdown list box to filter products by category, using the categories returned from the API.</li></ul><p><i>Figure 1/2:</i> \"Products Review System\" screen with a category dropdown (\"-- Select all category --\") and a green <b>Show Review List</b> button; a 3-column card grid shows each product's Title, Price, Category, Average Rate. Choosing a category (e.g. \"fragrances\") shows only products of that category.</p></div><div class=\"ml-vi\"><p><b>Câu 1 (4 điểm) — Lấy và Hiển thị Sản phẩm.</b></p><p>Xây dựng component lấy về và hiển thị tất cả sản phẩm từ endpoint:</p><p><code>GET http://localhost:9999/products</code></p><p><b>Yêu cầu:</b></p><ul><li>Định tuyến component tại URL <code>http://localhost:3000</code> (0 điểm nếu sai URL).</li><li>Thiết kế bố cục đúng.</li><li>Hiển thị <b>Title</b>, <b>Price</b>, <b>Category</b> và <b>Average Rate</b> (điểm trung bình của các review) trong (các) Card.</li><li>Thêm dropdown lọc sản phẩm theo category, dùng các category lấy từ API.</li></ul><p><i>Hình 1/2:</i> Màn hình \"Products Review System\" có dropdown category (\"-- Select all category --\") và nút <b>Show Review List</b> màu xanh; lưới card 3 cột hiển thị Title, Price, Category, Average Rate của từng sản phẩm. Chọn một category (vd \"fragrances\") thì chỉ hiện sản phẩm thuộc category đó.</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Set up routing so the product list lives at <code>/</code>, fetch once in <code>useEffect</code>, compute each product's average rating from its <code>reviews</code>, and derive the dropdown options from the unique categories in the data.</p><pre><code>// App.js\nimport { BrowserRouter, Routes, Route, Link } from 'react-router-dom';\nimport ProductList from './ProductList';\nimport ReviewList from './ReviewList';\n\nexport default function App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;nav style={{ padding: 10 }}&gt;\n        &lt;Link to=\"/\"&gt;Products&lt;/Link&gt; | &lt;Link to=\"/reviews\"&gt;Reviews&lt;/Link&gt;\n      &lt;/nav&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/\" element={&lt;ProductList /&gt;} /&gt;\n        &lt;Route path=\"/reviews\" element={&lt;ReviewList /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\n\n// ProductList.js\nimport { useState, useEffect } from 'react';\nimport { useNavigate } from 'react-router-dom';\nimport axios from 'axios';\n\nexport default function ProductList() {\n  const [products, setProducts] = useState([]);\n  const [category, setCategory] = useState('all');\n  const navigate = useNavigate();\n\n  useEffect(() =&gt; {\n    axios.get('http://localhost:9999/products')\n      .then(res =&gt; setProducts(res.data))\n      .catch(err =&gt; console.log(err));\n  }, []);\n\n  // average of the reviews' Rating, one decimal\n  const avgRate = (reviews) =&gt; {\n    if (!reviews || reviews.length === 0) return 0;\n    const sum = reviews.reduce((s, r) =&gt; s + Number(r.Rating), 0);\n    return (sum / reviews.length).toFixed(1);\n  };\n\n  // unique categories from the API data\n  const categories = [...new Set(products.map(p =&gt; p.Category))];\n\n  const filtered = category === 'all'\n    ? products\n    : products.filter(p =&gt; p.Category === category);\n\n  return (\n    &lt;div style={{ padding: 20 }}&gt;\n      &lt;h2&gt;Products Review System&lt;/h2&gt;\n      &lt;div style={{ display: 'flex', justifyContent: 'space-between' }}&gt;\n        &lt;select value={category} onChange={e =&gt; setCategory(e.target.value)}&gt;\n          &lt;option value=\"all\"&gt;-- Select all category --&lt;/option&gt;\n          {categories.map(c =&gt; (\n            &lt;option key={c} value={c}&gt;{c}&lt;/option&gt;\n          ))}\n        &lt;/select&gt;\n        &lt;button style={{ background: 'green', color: '#fff' }}\n          onClick={() =&gt; navigate('/reviews')}&gt;Show Review List&lt;/button&gt;\n      &lt;/div&gt;\n\n      &lt;div style={{ display: 'grid', gridTemplateColumns: 'repeat(3,1fr)', gap: 12, marginTop: 16 }}&gt;\n        {filtered.map(p =&gt; (\n          &lt;div key={p.id} style={{ border: '1px solid #ddd', padding: 12 }}&gt;\n            &lt;h5&gt;{p.Title}&lt;/h5&gt;\n            &lt;p&gt;Price: {p.Price}&lt;/p&gt;\n            &lt;p&gt;Category: {p.Category}&lt;/p&gt;\n            &lt;p&gt;Average Rate: {avgRate(p.reviews)}&lt;/p&gt;\n          &lt;/div&gt;\n        ))}\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Explanation:</b> <code>useEffect([])</code> fetches once on mount; <code>avgRate</code> averages the <code>Rating</code> of every review; <code>new Set(...)</code> gives the distinct categories for the dropdown; changing the select filters the card grid. The list is mounted on route <code>/</code> so the URL is exactly <code>http://localhost:3000</code>.</p></div><div class=\"ml-vi\"><p>Định tuyến để danh sách sản phẩm nằm ở <code>/</code>, fetch một lần trong <code>useEffect</code>, tính điểm trung bình từ mảng <code>reviews</code>, và lấy các option của dropdown từ tập category duy nhất trong dữ liệu.</p><pre><code>// App.js\nimport { BrowserRouter, Routes, Route, Link } from 'react-router-dom';\nimport ProductList from './ProductList';\nimport ReviewList from './ReviewList';\n\nexport default function App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;nav style={{ padding: 10 }}&gt;\n        &lt;Link to=\"/\"&gt;Products&lt;/Link&gt; | &lt;Link to=\"/reviews\"&gt;Reviews&lt;/Link&gt;\n      &lt;/nav&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/\" element={&lt;ProductList /&gt;} /&gt;\n        &lt;Route path=\"/reviews\" element={&lt;ReviewList /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\n\n// ProductList.js\nimport { useState, useEffect } from 'react';\nimport { useNavigate } from 'react-router-dom';\nimport axios from 'axios';\n\nexport default function ProductList() {\n  const [products, setProducts] = useState([]);\n  const [category, setCategory] = useState('all');\n  const navigate = useNavigate();\n\n  useEffect(() =&gt; {\n    axios.get('http://localhost:9999/products')\n      .then(res =&gt; setProducts(res.data))\n      .catch(err =&gt; console.log(err));\n  }, []);\n\n  // trung binh Rating cua cac review, 1 chu so thap phan\n  const avgRate = (reviews) =&gt; {\n    if (!reviews || reviews.length === 0) return 0;\n    const sum = reviews.reduce((s, r) =&gt; s + Number(r.Rating), 0);\n    return (sum / reviews.length).toFixed(1);\n  };\n\n  // cac category duy nhat tu du lieu API\n  const categories = [...new Set(products.map(p =&gt; p.Category))];\n\n  const filtered = category === 'all'\n    ? products\n    : products.filter(p =&gt; p.Category === category);\n\n  return (\n    &lt;div style={{ padding: 20 }}&gt;\n      &lt;h2&gt;Products Review System&lt;/h2&gt;\n      &lt;div style={{ display: 'flex', justifyContent: 'space-between' }}&gt;\n        &lt;select value={category} onChange={e =&gt; setCategory(e.target.value)}&gt;\n          &lt;option value=\"all\"&gt;-- Select all category --&lt;/option&gt;\n          {categories.map(c =&gt; (\n            &lt;option key={c} value={c}&gt;{c}&lt;/option&gt;\n          ))}\n        &lt;/select&gt;\n        &lt;button style={{ background: 'green', color: '#fff' }}\n          onClick={() =&gt; navigate('/reviews')}&gt;Show Review List&lt;/button&gt;\n      &lt;/div&gt;\n\n      &lt;div style={{ display: 'grid', gridTemplateColumns: 'repeat(3,1fr)', gap: 12, marginTop: 16 }}&gt;\n        {filtered.map(p =&gt; (\n          &lt;div key={p.id} style={{ border: '1px solid #ddd', padding: 12 }}&gt;\n            &lt;h5&gt;{p.Title}&lt;/h5&gt;\n            &lt;p&gt;Price: {p.Price}&lt;/p&gt;\n            &lt;p&gt;Category: {p.Category}&lt;/p&gt;\n            &lt;p&gt;Average Rate: {avgRate(p.reviews)}&lt;/p&gt;\n          &lt;/div&gt;\n        ))}\n      &lt;/div&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Giải thích:</b> <code>useEffect([])</code> fetch một lần khi mount; <code>avgRate</code> tính trung bình <code>Rating</code> của mọi review; <code>new Set(...)</code> cho danh sách category không trùng cho dropdown; đổi select thì lọc lại lưới card. Danh sách gắn ở route <code>/</code> nên URL đúng là <code>http://localhost:3000</code>.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Fetches products from GET http://localhost:9999/products (axios/fetch in useEffect) and renders cards at route http://localhost:3000 (path /).|||Fetch sản phẩm từ GET http://localhost:9999/products (axios/fetch trong useEffect) và render card tại route http://localhost:3000 (path /).",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c2",
              "criterion": "Each card shows Title, Price, Category and the correctly computed Average Rate (mean of the reviews' Rating).|||Mỗi card hiển thị Title, Price, Category và Average Rate tính đúng (trung bình Rating của các review).",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c3",
              "criterion": "Category dropdown built from the API's categories filters the displayed products correctly.|||Dropdown category dựng từ category của API lọc đúng sản phẩm hiển thị.",
              "weight": 1,
              "maxScore": 1
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE6-q1.png"
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2 (4 marks) — Create a New Review.</b></p><p>Build a feature that allows customers to add a new Review.</p><ul><li>The customer searches for the product they want to review, then clicks the <b>Add New Review</b> button to add it to the <b>Review details</b> container. For each selected product, the information is displayed accordingly.</li><li>When the <b>Send Review</b> button is clicked:<ul><li><b>Validation:</b> Show error messages if the <b>Reviewer name</b> or <b>Comment</b> is blank (\"Reviewer Name is required\", \"Comment is required\").</li><li>The review information — <b>Reviewer name</b>, <b>Comment</b>, <b>Rating</b> (radio 1-5) and <b>Date</b> (current date) — is saved to products via <code>PATCH (or PUT) http://localhost:9999/products/:id</code>.</li><li>Display a success message (\"Thanks for your review!\").</li></ul></li></ul><p><i>Figure 3/4/5:</i> Select a product (e.g. Essence Mascara Lash, id 1) into Review details; leaving Reviewer/Comment blank + Send Review shows the required errors; entering \"FPTU Tester\" / \"Great!\" / Rating 5 then Send Review saves it and shows \"Thanks for your review!\" in green.</p></div><div class=\"ml-vi\"><p><b>Câu 2 (4 điểm) — Tạo Review Mới.</b></p><p>Xây dựng tính năng cho phép khách hàng thêm một Review mới.</p><ul><li>Khách hàng tìm sản phẩm muốn đánh giá, rồi bấm nút <b>Add New Review</b> để đưa vào khung <b>Review details</b>. Với mỗi sản phẩm được chọn, thông tin được hiển thị tương ứng.</li><li>Khi bấm nút <b>Send Review</b>:<ul><li><b>Validation:</b> Hiện thông báo lỗi nếu <b>Reviewer name</b> hoặc <b>Comment</b> để trống (\"Reviewer Name is required\", \"Comment is required\").</li><li>Thông tin review — <b>Reviewer name</b>, <b>Comment</b>, <b>Rating</b> (radio 1-5) và <b>Date</b> (ngày hiện tại) — được lưu vào products qua <code>PATCH (hoặc PUT) http://localhost:9999/products/:id</code>.</li><li>Hiển thị thông báo thành công (\"Thanks for your review!\").</li></ul></li></ul><p><i>Hình 3/4/5:</i> Chọn một sản phẩm (vd Essence Mascara Lash, id 1) vào Review details; để trống Reviewer/Comment rồi Send Review thì hiện lỗi bắt buộc; nhập \"FPTU Tester\" / \"Great!\" / Rating 5 rồi Send Review thì lưu và hiện \"Thanks for your review!\" màu xanh.</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Search filters the product list; \"Add New Review\" puts the chosen product into the Review details panel; on Send Review we validate the two required fields, append the new review (with the current date) to the product's <code>reviews</code>, and PATCH it back.</p><pre><code>// AddReview.js\nimport { useState, useEffect } from 'react';\nimport axios from 'axios';\n\nexport default function AddReview() {\n  const [products, setProducts] = useState([]);\n  const [search, setSearch] = useState('');\n  const [selected, setSelected] = useState(null);   // product in \"Review details\"\n  const [reviewer, setReviewer] = useState('');\n  const [comment, setComment] = useState('');\n  const [rating, setRating] = useState(5);\n  const [errors, setErrors] = useState({});\n  const [message, setMessage] = useState('');\n\n  useEffect(() =&gt; {\n    axios.get('http://localhost:9999/products').then(res =&gt; setProducts(res.data));\n  }, []);\n\n  const results = products.filter(p =&gt;\n    p.Title.toLowerCase().includes(search.toLowerCase()));\n\n  const handleSend = () =&gt; {\n    const errs = {};\n    if (!reviewer.trim()) errs.reviewer = 'Reviewer Name is required';\n    if (!comment.trim()) errs.comment = 'Comment is required';\n    setErrors(errs);\n    if (Object.keys(errs).length &gt; 0) return;   // stop on validation error\n\n    const newReview = {\n      Reviewer: reviewer,\n      Comment: comment,\n      Rating: Number(rating),\n      Date: new Date().toISOString()            // current date\n    };\n    const updatedReviews = [...(selected.reviews || []), newReview];\n\n    axios.patch(`http://localhost:9999/products/${selected.id}`,\n      { reviews: updatedReviews })\n      .then(res =&gt; {\n        setSelected(res.data);\n        setMessage('Thanks for your review!');\n        setReviewer(''); setComment(''); setRating(5); setErrors({});\n      });\n  };\n\n  return (\n    &lt;div style={{ padding: 20 }}&gt;\n      &lt;h3&gt;Search product&lt;/h3&gt;\n      &lt;input value={search} placeholder=\"Search...\"\n        onChange={e =&gt; setSearch(e.target.value)} /&gt;\n      &lt;ul&gt;\n        {search &amp;&amp; results.map(p =&gt; (\n          &lt;li key={p.id}&gt;\n            {p.Title}{' '}\n            &lt;button onClick={() =&gt; { setSelected(p); setMessage(''); }}&gt;\n              Add New Review\n            &lt;/button&gt;\n          &lt;/li&gt;\n        ))}\n      &lt;/ul&gt;\n\n      &lt;h3&gt;Review details&lt;/h3&gt;\n      {!selected &amp;&amp; &lt;p&gt;Select a product to review!&lt;/p&gt;}\n      {selected &amp;&amp; (\n        &lt;div style={{ border: '1px solid #ccc', padding: 12 }}&gt;\n          &lt;p&gt;&lt;b&gt;{selected.Title}&lt;/b&gt; — {selected.Category} — {selected.Price}&lt;/p&gt;\n\n          &lt;div&gt;\n            &lt;input placeholder=\"Reviewer Name\" value={reviewer}\n              onChange={e =&gt; setReviewer(e.target.value)} /&gt;\n            {errors.reviewer &amp;&amp; &lt;div style={{ color: 'red' }}&gt;{errors.reviewer}&lt;/div&gt;}\n          &lt;/div&gt;\n\n          &lt;div&gt;\n            &lt;textarea placeholder=\"Comment\" value={comment}\n              onChange={e =&gt; setComment(e.target.value)} /&gt;\n            {errors.comment &amp;&amp; &lt;div style={{ color: 'red' }}&gt;{errors.comment}&lt;/div&gt;}\n          &lt;/div&gt;\n\n          &lt;div&gt;\n            {[1, 2, 3, 4, 5].map(n =&gt; (\n              &lt;label key={n}&gt;\n                &lt;input type=\"radio\" name=\"rating\" value={n}\n                  checked={Number(rating) === n}\n                  onChange={e =&gt; setRating(e.target.value)} /&gt; {n}\n              &lt;/label&gt;\n            ))}\n          &lt;/div&gt;\n\n          &lt;button style={{ background: 'gold' }} onClick={handleSend}&gt;\n            Send Review\n          &lt;/button&gt;\n          {message &amp;&amp; &lt;p style={{ color: 'green' }}&gt;{message}&lt;/p&gt;}\n        &lt;/div&gt;\n      )}\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Explanation:</b> the search input filters products by <code>Title</code>; \"Add New Review\" sets <code>selected</code> so the details panel renders. <code>handleSend</code> first checks the two required fields and shows the exact error texts; only when both pass does it build <code>newReview</code> (with <code>new Date()</code>), append it to the existing <code>reviews</code>, and PATCH the product by id. On success it shows the green \"Thanks for your review!\" message and resets the form.</p></div><div class=\"ml-vi\"><p>Ô search lọc danh sách sản phẩm; \"Add New Review\" đưa sản phẩm được chọn vào khung Review details; khi Send Review thì validate hai trường bắt buộc, thêm review mới (kèm ngày hiện tại) vào mảng <code>reviews</code> của sản phẩm rồi PATCH lại.</p><pre><code>// AddReview.js\nimport { useState, useEffect } from 'react';\nimport axios from 'axios';\n\nexport default function AddReview() {\n  const [products, setProducts] = useState([]);\n  const [search, setSearch] = useState('');\n  const [selected, setSelected] = useState(null);   // san pham trong \"Review details\"\n  const [reviewer, setReviewer] = useState('');\n  const [comment, setComment] = useState('');\n  const [rating, setRating] = useState(5);\n  const [errors, setErrors] = useState({});\n  const [message, setMessage] = useState('');\n\n  useEffect(() =&gt; {\n    axios.get('http://localhost:9999/products').then(res =&gt; setProducts(res.data));\n  }, []);\n\n  const results = products.filter(p =&gt;\n    p.Title.toLowerCase().includes(search.toLowerCase()));\n\n  const handleSend = () =&gt; {\n    const errs = {};\n    if (!reviewer.trim()) errs.reviewer = 'Reviewer Name is required';\n    if (!comment.trim()) errs.comment = 'Comment is required';\n    setErrors(errs);\n    if (Object.keys(errs).length &gt; 0) return;   // dung lai neu loi validate\n\n    const newReview = {\n      Reviewer: reviewer,\n      Comment: comment,\n      Rating: Number(rating),\n      Date: new Date().toISOString()            // ngay hien tai\n    };\n    const updatedReviews = [...(selected.reviews || []), newReview];\n\n    axios.patch(`http://localhost:9999/products/${selected.id}`,\n      { reviews: updatedReviews })\n      .then(res =&gt; {\n        setSelected(res.data);\n        setMessage('Thanks for your review!');\n        setReviewer(''); setComment(''); setRating(5); setErrors({});\n      });\n  };\n\n  return (\n    &lt;div style={{ padding: 20 }}&gt;\n      &lt;h3&gt;Search product&lt;/h3&gt;\n      &lt;input value={search} placeholder=\"Search...\"\n        onChange={e =&gt; setSearch(e.target.value)} /&gt;\n      &lt;ul&gt;\n        {search &amp;&amp; results.map(p =&gt; (\n          &lt;li key={p.id}&gt;\n            {p.Title}{' '}\n            &lt;button onClick={() =&gt; { setSelected(p); setMessage(''); }}&gt;\n              Add New Review\n            &lt;/button&gt;\n          &lt;/li&gt;\n        ))}\n      &lt;/ul&gt;\n\n      &lt;h3&gt;Review details&lt;/h3&gt;\n      {!selected &amp;&amp; &lt;p&gt;Select a product to review!&lt;/p&gt;}\n      {selected &amp;&amp; (\n        &lt;div style={{ border: '1px solid #ccc', padding: 12 }}&gt;\n          &lt;p&gt;&lt;b&gt;{selected.Title}&lt;/b&gt; — {selected.Category} — {selected.Price}&lt;/p&gt;\n\n          &lt;div&gt;\n            &lt;input placeholder=\"Reviewer Name\" value={reviewer}\n              onChange={e =&gt; setReviewer(e.target.value)} /&gt;\n            {errors.reviewer &amp;&amp; &lt;div style={{ color: 'red' }}&gt;{errors.reviewer}&lt;/div&gt;}\n          &lt;/div&gt;\n\n          &lt;div&gt;\n            &lt;textarea placeholder=\"Comment\" value={comment}\n              onChange={e =&gt; setComment(e.target.value)} /&gt;\n            {errors.comment &amp;&amp; &lt;div style={{ color: 'red' }}&gt;{errors.comment}&lt;/div&gt;}\n          &lt;/div&gt;\n\n          &lt;div&gt;\n            {[1, 2, 3, 4, 5].map(n =&gt; (\n              &lt;label key={n}&gt;\n                &lt;input type=\"radio\" name=\"rating\" value={n}\n                  checked={Number(rating) === n}\n                  onChange={e =&gt; setRating(e.target.value)} /&gt; {n}\n              &lt;/label&gt;\n            ))}\n          &lt;/div&gt;\n\n          &lt;button style={{ background: 'gold' }} onClick={handleSend}&gt;\n            Send Review\n          &lt;/button&gt;\n          {message &amp;&amp; &lt;p style={{ color: 'green' }}&gt;{message}&lt;/p&gt;}\n        &lt;/div&gt;\n      )}\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Giải thích:</b> ô search lọc sản phẩm theo <code>Title</code>; \"Add New Review\" gán <code>selected</code> để hiện khung chi tiết. <code>handleSend</code> kiểm tra hai trường bắt buộc và hiện đúng chữ lỗi; chỉ khi cả hai hợp lệ mới tạo <code>newReview</code> (kèm <code>new Date()</code>), thêm vào mảng <code>reviews</code> hiện có và PATCH sản phẩm theo id. Thành công thì hiện dòng \"Thanks for your review!\" màu xanh và reset form.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Search + Add New Review: the chosen product is loaded into the Review details container and its info is displayed.|||Search + Add New Review: sản phẩm được chọn được nạp vào khung Review details và hiển thị thông tin.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Validation shows \"Reviewer Name is required\" / \"Comment is required\" and blocks sending when either field is blank.|||Validation hiện \"Reviewer Name is required\" / \"Comment is required\" và chặn gửi khi một trong hai trường trống.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c3",
              "criterion": "On Send Review, the review (Reviewer, Comment, Rating, current Date) is saved via PATCH/PUT to /products/:id and a success message is shown.|||Khi Send Review, review (Reviewer, Comment, Rating, Date hiện tại) được lưu qua PATCH/PUT tới /products/:id và hiện thông báo thành công.",
              "weight": 1,
              "maxScore": 1.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE6-q2.png"
        },
        {
          "kind": "WRITE",
          "points": 2,
          "prompt": "<div class=\"ml-en\"><p><b>Question 3 (2 marks) — View all Reviews.</b></p><p>Click the <b>Show Review List</b> button to view all existing reviews.</p><p><b>Requirements:</b></p><ul><li>Fetch data from <code>GET http://localhost:9999/products</code>.</li><li>Display the columns: <b>ProductId</b>, <b>Title</b>, <b>Reviews</b> (including: <b>Date</b>, <b>Reviewer</b>, <b>Comment</b>, <b>Rating</b>) in a table at URL <code>http://localhost:3000/reviews</code>.</li><li>The <b>Date</b> column must be formatted as <code>dd/MM/yyyy hh:mm:ss AM/PM</code>.</li></ul><p><i>Figure 6:</i> \"List of Reviews\" screen at <code>localhost:3000/reviews</code>. Table columns ProductId | Title | Reviews, with the Reviews cell holding sub-columns Date | Reviewer | Comment | Rating, one row per product (e.g. ProductId 1 Essence Mascara Lash with several reviews, ProductId 2 Eyeshadow Palette, etc.).</p></div><div class=\"ml-vi\"><p><b>Câu 3 (2 điểm) — Xem tất cả Reviews.</b></p><p>Bấm nút <b>Show Review List</b> để xem tất cả review đang có.</p><p><b>Yêu cầu:</b></p><ul><li>Fetch dữ liệu từ <code>GET http://localhost:9999/products</code>.</li><li>Hiển thị các cột: <b>ProductId</b>, <b>Title</b>, <b>Reviews</b> (gồm: <b>Date</b>, <b>Reviewer</b>, <b>Comment</b>, <b>Rating</b>) trong một bảng tại URL <code>http://localhost:3000/reviews</code>.</li><li>Cột <b>Date</b> phải được định dạng <code>dd/MM/yyyy hh:mm:ss AM/PM</code>.</li></ul><p><i>Hình 6:</i> Màn hình \"List of Reviews\" tại <code>localhost:3000/reviews</code>. Bảng có cột ProductId | Title | Reviews, ô Reviews chứa các cột con Date | Reviewer | Comment | Rating, mỗi sản phẩm một dòng (vd ProductId 1 Essence Mascara Lash có nhiều review, ProductId 2 Eyeshadow Palette, ...).</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p>Mount this component at <code>/reviews</code> (already routed in App.js from Q1). Fetch products, and for each product render a row whose Reviews cell is a nested table with the formatted date.</p><pre><code>// ReviewList.js\nimport { useState, useEffect } from 'react';\nimport axios from 'axios';\n\n// format an ISO date as dd/MM/yyyy hh:mm:ss AM/PM\nfunction formatDate(iso) {\n  const d = new Date(iso);\n  const pad = (n) =&gt; String(n).padStart(2, '0');\n  const day = pad(d.getDate());\n  const month = pad(d.getMonth() + 1);\n  const year = d.getFullYear();\n  let h = d.getHours();\n  const ampm = h &gt;= 12 ? 'PM' : 'AM';\n  h = h % 12 || 12;                       // 0 -&gt; 12\n  return `${day}/${month}/${year} ${pad(h)}:${pad(d.getMinutes())}:${pad(d.getSeconds())} ${ampm}`;\n}\n\nexport default function ReviewList() {\n  const [products, setProducts] = useState([]);\n\n  useEffect(() =&gt; {\n    axios.get('http://localhost:9999/products')\n      .then(res =&gt; setProducts(res.data));\n  }, []);\n\n  return (\n    &lt;div style={{ padding: 20 }}&gt;\n      &lt;h2&gt;List of Reviews&lt;/h2&gt;\n      &lt;table border=\"1\" cellPadding=\"6\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;&lt;th&gt;ProductId&lt;/th&gt;&lt;th&gt;Title&lt;/th&gt;&lt;th&gt;Reviews&lt;/th&gt;&lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {products.map(p =&gt; (\n            &lt;tr key={p.id}&gt;\n              &lt;td&gt;{p.id}&lt;/td&gt;\n              &lt;td&gt;{p.Title}&lt;/td&gt;\n              &lt;td&gt;\n                &lt;table border=\"1\" cellPadding=\"4\"&gt;\n                  &lt;thead&gt;\n                    &lt;tr&gt;&lt;th&gt;Date&lt;/th&gt;&lt;th&gt;Reviewer&lt;/th&gt;&lt;th&gt;Comment&lt;/th&gt;&lt;th&gt;Rating&lt;/th&gt;&lt;/tr&gt;\n                  &lt;/thead&gt;\n                  &lt;tbody&gt;\n                    {(p.reviews || []).map((r, i) =&gt; (\n                      &lt;tr key={i}&gt;\n                        &lt;td&gt;{formatDate(r.Date)}&lt;/td&gt;\n                        &lt;td&gt;{r.Reviewer}&lt;/td&gt;\n                        &lt;td&gt;{r.Comment}&lt;/td&gt;\n                        &lt;td&gt;{r.Rating}&lt;/td&gt;\n                      &lt;/tr&gt;\n                    ))}\n                  &lt;/tbody&gt;\n                &lt;/table&gt;\n              &lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Explanation:</b> the outer table has one row per product (ProductId, Title, Reviews); the Reviews cell nests a table listing every review with the required <code>Date | Reviewer | Comment | Rating</code> sub-columns. <code>formatDate</code> pads each part and converts the 24-hour clock to 12-hour with AM/PM, producing e.g. <code>23/05/2024 08:56:21 AM</code>. Because the route <code>/reviews</code> was declared in App.js, the \"Show Review List\" button (Q1) navigates here at <code>http://localhost:3000/reviews</code>.</p></div><div class=\"ml-vi\"><p>Gắn component này ở <code>/reviews</code> (đã định tuyến trong App.js ở Câu 1). Fetch products, mỗi sản phẩm render một dòng có ô Reviews là bảng lồng với ngày đã định dạng.</p><pre><code>// ReviewList.js\nimport { useState, useEffect } from 'react';\nimport axios from 'axios';\n\n// dinh dang ISO date thanh dd/MM/yyyy hh:mm:ss AM/PM\nfunction formatDate(iso) {\n  const d = new Date(iso);\n  const pad = (n) =&gt; String(n).padStart(2, '0');\n  const day = pad(d.getDate());\n  const month = pad(d.getMonth() + 1);\n  const year = d.getFullYear();\n  let h = d.getHours();\n  const ampm = h &gt;= 12 ? 'PM' : 'AM';\n  h = h % 12 || 12;                       // 0 -&gt; 12\n  return `${day}/${month}/${year} ${pad(h)}:${pad(d.getMinutes())}:${pad(d.getSeconds())} ${ampm}`;\n}\n\nexport default function ReviewList() {\n  const [products, setProducts] = useState([]);\n\n  useEffect(() =&gt; {\n    axios.get('http://localhost:9999/products')\n      .then(res =&gt; setProducts(res.data));\n  }, []);\n\n  return (\n    &lt;div style={{ padding: 20 }}&gt;\n      &lt;h2&gt;List of Reviews&lt;/h2&gt;\n      &lt;table border=\"1\" cellPadding=\"6\"&gt;\n        &lt;thead&gt;\n          &lt;tr&gt;&lt;th&gt;ProductId&lt;/th&gt;&lt;th&gt;Title&lt;/th&gt;&lt;th&gt;Reviews&lt;/th&gt;&lt;/tr&gt;\n        &lt;/thead&gt;\n        &lt;tbody&gt;\n          {products.map(p =&gt; (\n            &lt;tr key={p.id}&gt;\n              &lt;td&gt;{p.id}&lt;/td&gt;\n              &lt;td&gt;{p.Title}&lt;/td&gt;\n              &lt;td&gt;\n                &lt;table border=\"1\" cellPadding=\"4\"&gt;\n                  &lt;thead&gt;\n                    &lt;tr&gt;&lt;th&gt;Date&lt;/th&gt;&lt;th&gt;Reviewer&lt;/th&gt;&lt;th&gt;Comment&lt;/th&gt;&lt;th&gt;Rating&lt;/th&gt;&lt;/tr&gt;\n                  &lt;/thead&gt;\n                  &lt;tbody&gt;\n                    {(p.reviews || []).map((r, i) =&gt; (\n                      &lt;tr key={i}&gt;\n                        &lt;td&gt;{formatDate(r.Date)}&lt;/td&gt;\n                        &lt;td&gt;{r.Reviewer}&lt;/td&gt;\n                        &lt;td&gt;{r.Comment}&lt;/td&gt;\n                        &lt;td&gt;{r.Rating}&lt;/td&gt;\n                      &lt;/tr&gt;\n                    ))}\n                  &lt;/tbody&gt;\n                &lt;/table&gt;\n              &lt;/td&gt;\n            &lt;/tr&gt;\n          ))}\n        &lt;/tbody&gt;\n      &lt;/table&gt;\n    &lt;/div&gt;\n  );\n}</code></pre><p><b>Giải thích:</b> bảng ngoài mỗi sản phẩm một dòng (ProductId, Title, Reviews); ô Reviews lồng một bảng liệt kê mọi review với các cột con bắt buộc <code>Date | Reviewer | Comment | Rating</code>. <code>formatDate</code> đệm 0 từng phần và đổi giờ 24 sang 12 kèm AM/PM, cho ra vd <code>23/05/2024 08:56:21 AM</code>. Vì route <code>/reviews</code> đã khai trong App.js nên nút \"Show Review List\" (Câu 1) điều hướng tới đây ở <code>http://localhost:3000/reviews</code>.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Fetches products and renders a table at /reviews with columns ProductId, Title and Reviews (Date, Reviewer, Comment, Rating).|||Fetch products và render bảng tại /reviews với cột ProductId, Title và Reviews (Date, Reviewer, Comment, Rating).",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Date column is formatted exactly as dd/MM/yyyy hh:mm:ss AM/PM (zero-padded, 12-hour with AM/PM).|||Cột Date định dạng đúng dd/MM/yyyy hh:mm:ss AM/PM (đệm 0, 12 giờ kèm AM/PM).",
              "weight": 1,
              "maxScore": 1
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE6-q3.png"
        }
      ]
    }
  ]
};
