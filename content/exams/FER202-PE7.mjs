export default {
  "course": {
    "courseCode": "FER202"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE7-PESU2025Bloc",
      "title": "FER202 — Practical Exam Đề 7 (PE - SU 2025 - Block 1)|||FER202 — Đề thi thực hành số 7 (PE - SU 2025 - Block 1)",
      "description": "React PE (WRITE): build components against a REST API, AI-graded.|||Đề PE React (viết): dựng component theo REST API, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>React / Front-end Development</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>React / Front-end Development</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 6,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p>React front-end practical exam (FER202). Uses json-server as a fake REST API on port 9999 and a React app (react-router-dom + react-bootstrap) served on port 3000. Answers are complete, runnable React components implementing the required routing, data fetching (fetch), filtering/searching, and form handling with validation.</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p>Bài thi thực hành React front-end (FER202). Dùng json-server làm REST API giả ở cổng 9999 và ứng dụng React (react-router-dom + react-bootstrap) chạy ở cổng 3000. Đáp án là các component React hoàn chỉnh, chạy được, hiện thực đúng routing, gọi dữ liệu (fetch), lọc/tìm kiếm, và xử lý form kèm kiểm tra hợp lệ.</p></div></div><div class=\"ml-en\"><p><b>Question 1 (6 marks) - View and Filter Movie List.</b> Build a React component that renders the list of movies fetched from the API.</p><p><b>Endpoint:</b> <code>GET http://localhost:9999/movies</code> &nbsp; <b>Route:</b> <code>http://localhost:3000/movies</code></p><p><b>Requirements:</b></p><ul><li>Component is routed at URL <code>http://localhost:3000/movies</code> (0 points if incorrect the URL).</li><li>Build the layout and design according to the specified requirements.</li><li>Show the movies as specified in the design, each movie presenting: <b>poster, title, genre, releaseYear, duration, rating, booked</b>.</li><li>Sort the list in descending order by <b>rating</b>.</li><li>Add a search bar to filter movies by title (<b>startsWith</b>).</li><li>Provide filter dropdowns for: <b>Genres</b> (e.g., Sci-Fi, Crime, ...) - fetch unique value the <b>genre</b> from the books; and <b>Years</b> (e.g., 2019, 2014, ...) sort by descending - fetch unique value the <b>releaseYear</b> from the books.</li><li>Filters and search must work together (combined filtering).</li><li>Add a \"Book Ticket\" button that navigates to <code>http://localhost:3000/booking/create</code>.</li></ul></div><div class=\"ml-vi\"><p><b>Câu 1 (6 điểm) - Xem và Lọc Danh sách Phim.</b> Xây dựng một component React hiển thị danh sách phim lấy từ API.</p><p><b>Endpoint:</b> <code>GET http://localhost:9999/movies</code> &nbsp; <b>Route:</b> <code>http://localhost:3000/movies</code></p><p><b>Yêu cầu:</b></p><ul><li>Component nằm ở URL <code>http://localhost:3000/movies</code> (0 điểm nếu sai URL).</li><li>Dựng bố cục và thiết kế theo yêu cầu.</li><li>Hiển thị các phim theo thiết kế, mỗi phim gồm: <b>poster, title, genre, releaseYear, duration, rating, booked</b>.</li><li>Sắp xếp danh sách giảm dần theo <b>rating</b>.</li><li>Thêm thanh tìm kiếm để lọc phim theo tiêu đề (<b>startsWith</b>).</li><li>Cung cấp dropdown lọc cho: <b>Genres</b> (vd Sci-Fi, Crime, ...) - lấy giá trị <b>genre</b> duy nhất từ dữ liệu; và <b>Years</b> (vd 2019, 2014, ...) sắp xếp giảm dần - lấy giá trị <b>releaseYear</b> duy nhất từ dữ liệu.</li><li>Bộ lọc và tìm kiếm phải hoạt động đồng thời (lọc kết hợp).</li><li>Thêm nút \"Book Ticket\" điều hướng tới <code>http://localhost:3000/booking/create</code>.</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><pre><code>// src/App.jsx — React Router v6\nimport { BrowserRouter, Routes, Route, Navigate } from \"react-router-dom\";\nimport MovieList from \"./components/MovieList\";\nimport CreateBooking from \"./components/CreateBooking\";\n\nexport default function App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/\" element={&lt;Navigate to=\"/movies\" /&gt;} /&gt;\n        &lt;Route path=\"/movies\" element={&lt;MovieList /&gt;} /&gt;\n        &lt;Route path=\"/booking/create\" element={&lt;CreateBooking /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\n\n// src/components/MovieList.jsx\nimport { useEffect, useMemo, useState } from \"react\";\nimport { useNavigate } from \"react-router-dom\";\nimport { Container, Row, Col, Card, Form, Button, Badge } from \"react-bootstrap\";\n\nconst API = \"http://localhost:9999/movies\";\n\nexport default function MovieList() {\n  const navigate = useNavigate();\n  const [movies, setMovies] = useState([]);\n  const [search, setSearch] = useState(\"\");\n  const [genre, setGenre] = useState(\"All\");\n  const [year, setYear] = useState(\"All\");\n\n  // Fetch the movie list once on mount\n  useEffect(() =&gt; {\n    fetch(API)\n      .then((res) =&gt; res.json())\n      .then((data) =&gt; setMovies(data))\n      .catch((err) =&gt; console.error(err));\n  }, []);\n\n  // Unique genres pulled from the data\n  const genres = useMemo(\n    () =&gt; [...new Set(movies.map((m) =&gt; m.genre))],\n    [movies]\n  );\n\n  // Unique release years, sorted descending\n  const years = useMemo(\n    () =&gt; [...new Set(movies.map((m) =&gt; m.releaseYear))].sort((a, b) =&gt; b - a),\n    [movies]\n  );\n\n  // Combined filtering: search (startsWith) + genre + year, then sort by rating desc\n  const visible = useMemo(() =&gt; {\n    return movies\n      .filter((m) =&gt;\n        m.title.toLowerCase().startsWith(search.trim().toLowerCase())\n      )\n      .filter((m) =&gt; genre === \"All\" || m.genre === genre)\n      .filter((m) =&gt; year === \"All\" || String(m.releaseYear) === String(year))\n      .sort((a, b) =&gt; b.rating - a.rating);\n  }, [movies, search, genre, year]);\n\n  return (\n    &lt;Container className=\"py-4\"&gt;\n      &lt;h2 className=\"mb-3\"&gt;Movie List&lt;/h2&gt;\n\n      {/* Search bar */}\n      &lt;Form.Control\n        className=\"mb-3\"\n        placeholder=\"Search by title...\"\n        value={search}\n        onChange={(e) =&gt; setSearch(e.target.value)}\n      /&gt;\n\n      {/* Genre filter */}\n      &lt;div className=\"mb-2\"&gt;\n        &lt;Button\n          variant={genre === \"All\" ? \"primary\" : \"outline-primary\"}\n          size=\"sm\"\n          className=\"me-2 mb-2\"\n          onClick={() =&gt; setGenre(\"All\")}\n        &gt;\n          All Genres\n        &lt;/Button&gt;\n        {genres.map((g) =&gt; (\n          &lt;Button\n            key={g}\n            variant={genre === g ? \"primary\" : \"outline-primary\"}\n            size=\"sm\"\n            className=\"me-2 mb-2\"\n            onClick={() =&gt; setGenre(g)}\n          &gt;\n            {g}\n          &lt;/Button&gt;\n        ))}\n      &lt;/div&gt;\n\n      {/* Year filter (descending) */}\n      &lt;div className=\"mb-3\"&gt;\n        &lt;Button\n          variant={year === \"All\" ? \"success\" : \"outline-success\"}\n          size=\"sm\"\n          className=\"me-2 mb-2\"\n          onClick={() =&gt; setYear(\"All\")}\n        &gt;\n          All Years\n        &lt;/Button&gt;\n        {years.map((y) =&gt; (\n          &lt;Button\n            key={y}\n            variant={String(year) === String(y) ? \"success\" : \"outline-success\"}\n            size=\"sm\"\n            className=\"me-2 mb-2\"\n            onClick={() =&gt; setYear(y)}\n          &gt;\n            {y}\n          &lt;/Button&gt;\n        ))}\n      &lt;/div&gt;\n\n      {/* Movie grid, already sorted by rating desc */}\n      &lt;Row xs={1} md={2} lg={3} className=\"g-4\"&gt;\n        {visible.map((m) =&gt; (\n          &lt;Col key={m.id}&gt;\n            &lt;Card className=\"h-100\"&gt;\n              &lt;Card.Img\n                variant=\"top\"\n                src={m.poster}\n                style={{ height: 300, objectFit: \"cover\" }}\n              /&gt;\n              &lt;Card.Body&gt;\n                &lt;Card.Title&gt;{m.title}&lt;/Card.Title&gt;\n                &lt;div className=\"mb-2\"&gt;\n                  &lt;Badge bg=\"info\" className=\"me-1\"&gt;{m.genre}&lt;/Badge&gt;\n                  &lt;Badge bg=\"secondary\" className=\"me-1\"&gt;{m.releaseYear}&lt;/Badge&gt;\n                  &lt;Badge bg=\"dark\" className=\"me-1\"&gt;{m.duration} min&lt;/Badge&gt;\n                  &lt;Badge bg=\"warning\" text=\"dark\"&gt;Rating: {m.rating}&lt;/Badge&gt;\n                &lt;/div&gt;\n                &lt;Card.Text&gt;Booked: {m.booked}&lt;/Card.Text&gt;\n                &lt;Button onClick={() =&gt; navigate(\"/booking/create\")}&gt;\n                  Book Ticket\n                &lt;/Button&gt;\n              &lt;/Card.Body&gt;\n            &lt;/Card&gt;\n          &lt;/Col&gt;\n        ))}\n      &lt;/Row&gt;\n\n      {visible.length === 0 &amp;&amp; &lt;p className=\"mt-3\"&gt;No movies found.&lt;/p&gt;}\n    &lt;/Container&gt;\n  );\n}</code></pre><p><b>How it works:</b></p><ul><li><code>useEffect</code> fetches <code>GET http://localhost:9999/movies</code> once on mount and stores the array in state.</li><li>The dropdown options are derived from the data with <code>[...new Set(...)]</code>: <b>genre</b> gives the genre list, <b>releaseYear</b> gives the year list <b>sorted descending</b>.</li><li>Filtering is combined: title <code>startsWith</code> (case-insensitive) AND selected genre AND selected year, then the result is sorted <b>descending by rating</b>.</li><li>Each card shows poster, title, genre, releaseYear, duration, rating, booked. The <b>Book Ticket</b> button calls <code>navigate(\"/booking/create\")</code>.</li><li>The component is mounted at route <code>/movies</code> (0 points if the URL is wrong).</li></ul></div><div class=\"ml-vi\"><pre><code>// src/App.jsx — React Router v6\nimport { BrowserRouter, Routes, Route, Navigate } from \"react-router-dom\";\nimport MovieList from \"./components/MovieList\";\nimport CreateBooking from \"./components/CreateBooking\";\n\nexport default function App() {\n  return (\n    &lt;BrowserRouter&gt;\n      &lt;Routes&gt;\n        &lt;Route path=\"/\" element={&lt;Navigate to=\"/movies\" /&gt;} /&gt;\n        &lt;Route path=\"/movies\" element={&lt;MovieList /&gt;} /&gt;\n        &lt;Route path=\"/booking/create\" element={&lt;CreateBooking /&gt;} /&gt;\n      &lt;/Routes&gt;\n    &lt;/BrowserRouter&gt;\n  );\n}\n\n// src/components/MovieList.jsx\nimport { useEffect, useMemo, useState } from \"react\";\nimport { useNavigate } from \"react-router-dom\";\nimport { Container, Row, Col, Card, Form, Button, Badge } from \"react-bootstrap\";\n\nconst API = \"http://localhost:9999/movies\";\n\nexport default function MovieList() {\n  const navigate = useNavigate();\n  const [movies, setMovies] = useState([]);\n  const [search, setSearch] = useState(\"\");\n  const [genre, setGenre] = useState(\"All\");\n  const [year, setYear] = useState(\"All\");\n\n  // Fetch the movie list once on mount\n  useEffect(() =&gt; {\n    fetch(API)\n      .then((res) =&gt; res.json())\n      .then((data) =&gt; setMovies(data))\n      .catch((err) =&gt; console.error(err));\n  }, []);\n\n  // Unique genres pulled from the data\n  const genres = useMemo(\n    () =&gt; [...new Set(movies.map((m) =&gt; m.genre))],\n    [movies]\n  );\n\n  // Unique release years, sorted descending\n  const years = useMemo(\n    () =&gt; [...new Set(movies.map((m) =&gt; m.releaseYear))].sort((a, b) =&gt; b - a),\n    [movies]\n  );\n\n  // Combined filtering: search (startsWith) + genre + year, then sort by rating desc\n  const visible = useMemo(() =&gt; {\n    return movies\n      .filter((m) =&gt;\n        m.title.toLowerCase().startsWith(search.trim().toLowerCase())\n      )\n      .filter((m) =&gt; genre === \"All\" || m.genre === genre)\n      .filter((m) =&gt; year === \"All\" || String(m.releaseYear) === String(year))\n      .sort((a, b) =&gt; b.rating - a.rating);\n  }, [movies, search, genre, year]);\n\n  return (\n    &lt;Container className=\"py-4\"&gt;\n      &lt;h2 className=\"mb-3\"&gt;Movie List&lt;/h2&gt;\n\n      {/* Search bar */}\n      &lt;Form.Control\n        className=\"mb-3\"\n        placeholder=\"Search by title...\"\n        value={search}\n        onChange={(e) =&gt; setSearch(e.target.value)}\n      /&gt;\n\n      {/* Genre filter */}\n      &lt;div className=\"mb-2\"&gt;\n        &lt;Button\n          variant={genre === \"All\" ? \"primary\" : \"outline-primary\"}\n          size=\"sm\"\n          className=\"me-2 mb-2\"\n          onClick={() =&gt; setGenre(\"All\")}\n        &gt;\n          All Genres\n        &lt;/Button&gt;\n        {genres.map((g) =&gt; (\n          &lt;Button\n            key={g}\n            variant={genre === g ? \"primary\" : \"outline-primary\"}\n            size=\"sm\"\n            className=\"me-2 mb-2\"\n            onClick={() =&gt; setGenre(g)}\n          &gt;\n            {g}\n          &lt;/Button&gt;\n        ))}\n      &lt;/div&gt;\n\n      {/* Year filter (descending) */}\n      &lt;div className=\"mb-3\"&gt;\n        &lt;Button\n          variant={year === \"All\" ? \"success\" : \"outline-success\"}\n          size=\"sm\"\n          className=\"me-2 mb-2\"\n          onClick={() =&gt; setYear(\"All\")}\n        &gt;\n          All Years\n        &lt;/Button&gt;\n        {years.map((y) =&gt; (\n          &lt;Button\n            key={y}\n            variant={String(year) === String(y) ? \"success\" : \"outline-success\"}\n            size=\"sm\"\n            className=\"me-2 mb-2\"\n            onClick={() =&gt; setYear(y)}\n          &gt;\n            {y}\n          &lt;/Button&gt;\n        ))}\n      &lt;/div&gt;\n\n      {/* Movie grid, already sorted by rating desc */}\n      &lt;Row xs={1} md={2} lg={3} className=\"g-4\"&gt;\n        {visible.map((m) =&gt; (\n          &lt;Col key={m.id}&gt;\n            &lt;Card className=\"h-100\"&gt;\n              &lt;Card.Img\n                variant=\"top\"\n                src={m.poster}\n                style={{ height: 300, objectFit: \"cover\" }}\n              /&gt;\n              &lt;Card.Body&gt;\n                &lt;Card.Title&gt;{m.title}&lt;/Card.Title&gt;\n                &lt;div className=\"mb-2\"&gt;\n                  &lt;Badge bg=\"info\" className=\"me-1\"&gt;{m.genre}&lt;/Badge&gt;\n                  &lt;Badge bg=\"secondary\" className=\"me-1\"&gt;{m.releaseYear}&lt;/Badge&gt;\n                  &lt;Badge bg=\"dark\" className=\"me-1\"&gt;{m.duration} min&lt;/Badge&gt;\n                  &lt;Badge bg=\"warning\" text=\"dark\"&gt;Rating: {m.rating}&lt;/Badge&gt;\n                &lt;/div&gt;\n                &lt;Card.Text&gt;Booked: {m.booked}&lt;/Card.Text&gt;\n                &lt;Button onClick={() =&gt; navigate(\"/booking/create\")}&gt;\n                  Book Ticket\n                &lt;/Button&gt;\n              &lt;/Card.Body&gt;\n            &lt;/Card&gt;\n          &lt;/Col&gt;\n        ))}\n      &lt;/Row&gt;\n\n      {visible.length === 0 &amp;&amp; &lt;p className=\"mt-3\"&gt;No movies found.&lt;/p&gt;}\n    &lt;/Container&gt;\n  );\n}</code></pre><p><b>Cách hoạt động:</b></p><ul><li><code>useEffect</code> gọi <code>GET http://localhost:9999/movies</code> một lần khi mount và lưu mảng vào state.</li><li>Các lựa chọn dropdown được rút ra từ dữ liệu bằng <code>[...new Set(...)]</code>: <b>genre</b> cho danh sách thể loại, <b>releaseYear</b> cho danh sách năm <b>sắp xếp giảm dần</b>.</li><li>Lọc kết hợp: tiêu đề <code>startsWith</code> (không phân biệt hoa thường) VÀ thể loại đã chọn VÀ năm đã chọn, rồi kết quả được <b>sắp xếp giảm dần theo rating</b>.</li><li>Mỗi thẻ hiển thị poster, title, genre, releaseYear, duration, rating, booked. Nút <b>Book Ticket</b> gọi <code>navigate(\"/booking/create\")</code>.</li><li>Component nằm ở route <code>/movies</code> (0 điểm nếu sai URL).</li></ul></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Component routed correctly at <code>/movies</code> and fetches <code>GET http://localhost:9999/movies</code>, rendering each movie with poster, title, genre, releaseYear, duration, rating, booked.|||Component đặt đúng route <code>/movies</code> và gọi <code>GET http://localhost:9999/movies</code>, hiển thị mỗi phim với poster, title, genre, releaseYear, duration, rating, booked.",
              "weight": 1,
              "maxScore": 2
            },
            {
              "id": "c2",
              "criterion": "List sorted descending by rating; search bar filters by title using startsWith.|||Danh sách sắp xếp giảm dần theo rating; thanh tìm kiếm lọc theo tiêu đề dùng startsWith.",
              "weight": 1,
              "maxScore": 2
            },
            {
              "id": "c3",
              "criterion": "Genre and Year filters built from unique values (years descending), all filters + search combine correctly, and Book Ticket navigates to /booking/create.|||Bộ lọc Genre và Year dựng từ giá trị duy nhất (năm giảm dần), tất cả bộ lọc + tìm kiếm kết hợp đúng, và nút Book Ticket điều hướng tới /booking/create.",
              "weight": 1,
              "maxScore": 2
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE7.png"
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2 (4 marks) - Create a Movie Booking.</b> Build a React component for booking a movie.</p><p><b>Endpoint:</b> <code>POST http://localhost:9999/bookings</code> &nbsp; <b>Route:</b> <code>http://localhost:3000/booking/create</code> (0 points if incorrect the URL).</p><p><b>Requirements:</b> Implement a form to allow a user to book a movie ticket. Show a form containing the following fields:</p><ul><li><b>userId</b> (dropdown from <code>/users</code>) - Select User / John Doe / Jane Smith.</li><li><b>movieId</b> (dropdown from <code>/movies</code>) - Select Movie / Inception / Interstellar / Parasite / The Godfather / The Dark Knight / Spirited Away.</li><li><b>showTime</b> (datetime-local input).</li><li><b>seats</b> (number input).</li></ul><p>When \"Submit\" is clicked:</p><ul><li>Validate that all fields are filled.</li><li><code>seats</code> must be &gt; 0.</li><li>If validation fails &rarr; show error message (\"All fields are required and seats must be at least 1\").</li><li>If valid: Save booking to API <code>/bookings</code>; Increase <b>booked</b> count for the selected movie in API <code>/movies/:id</code>; Show success message (\"Booking successful!\"); Redirect to page <code>/movies</code> and reload list.</li></ul></div><div class=\"ml-vi\"><p><b>Câu 2 (4 điểm) - Tạo Đặt vé Phim.</b> Xây dựng một component React để đặt vé phim.</p><p><b>Endpoint:</b> <code>POST http://localhost:9999/bookings</code> &nbsp; <b>Route:</b> <code>http://localhost:3000/booking/create</code> (0 điểm nếu sai URL).</p><p><b>Yêu cầu:</b> Hiện thực một form cho phép người dùng đặt vé phim. Hiển thị form gồm các trường sau:</p><ul><li><b>userId</b> (dropdown từ <code>/users</code>) - Select User / John Doe / Jane Smith.</li><li><b>movieId</b> (dropdown từ <code>/movies</code>) - Select Movie / Inception / Interstellar / Parasite / The Godfather / The Dark Knight / Spirited Away.</li><li><b>showTime</b> (input datetime-local).</li><li><b>seats</b> (input number).</li></ul><p>Khi bấm \"Submit\":</p><ul><li>Kiểm tra tất cả các trường đã được điền.</li><li><code>seats</code> phải &gt; 0.</li><li>Nếu kiểm tra thất bại &rarr; hiện thông báo lỗi (\"All fields are required and seats must be at least 1\").</li><li>Nếu hợp lệ: Lưu booking vào API <code>/bookings</code>; Tăng số <b>booked</b> của phim đã chọn trong API <code>/movies/:id</code>; Hiện thông báo thành công (\"Booking successful!\"); Chuyển hướng về trang <code>/movies</code> và nạp lại danh sách.</li></ul></div>",
          "sampleSolution": "<div class=\"ml-en\"><pre><code>// src/components/CreateBooking.jsx\nimport { useEffect, useState } from \"react\";\nimport { useNavigate } from \"react-router-dom\";\nimport { Container, Form, Button, Alert } from \"react-bootstrap\";\n\nconst USERS = \"http://localhost:9999/users\";\nconst MOVIES = \"http://localhost:9999/movies\";\nconst BOOKINGS = \"http://localhost:9999/bookings\";\n\nexport default function CreateBooking() {\n  const navigate = useNavigate();\n  const [users, setUsers] = useState([]);\n  const [movies, setMovies] = useState([]);\n  const [form, setForm] = useState({\n    userId: \"\",\n    movieId: \"\",\n    showTime: \"\",\n    seats: \"\",\n  });\n  const [error, setError] = useState(\"\");\n\n  // Load dropdown data\n  useEffect(() =&gt; {\n    fetch(USERS).then((r) =&gt; r.json()).then(setUsers);\n    fetch(MOVIES).then((r) =&gt; r.json()).then(setMovies);\n  }, []);\n\n  const handleChange = (e) =&gt;\n    setForm({ ...form, [e.target.name]: e.target.value });\n\n  const handleSubmit = async (e) =&gt; {\n    e.preventDefault();\n    setError(\"\");\n\n    // Validate: all fields required, seats &gt; 0\n    if (!form.userId || !form.movieId || !form.showTime || !form.seats) {\n      setError(\"All fields are required and seats must be at least 1\");\n      return;\n    }\n    if (Number(form.seats) &lt;= 0) {\n      setError(\"All fields are required and seats must be at least 1\");\n      return;\n    }\n\n    try {\n      // 1) Save the booking\n      await fetch(BOOKINGS, {\n        method: \"POST\",\n        headers: { \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          userId: form.userId,\n          movieId: form.movieId,\n          showTime: form.showTime,\n          seats: Number(form.seats),\n        }),\n      });\n\n      // 2) Increase the booked count of the selected movie\n      const movie = movies.find((m) =&gt; String(m.id) === String(form.movieId));\n      await fetch(`${MOVIES}/${form.movieId}`, {\n        method: \"PATCH\",\n        headers: { \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({ booked: (movie?.booked || 0) + 1 }),\n      });\n\n      // 3) Success + redirect to /movies (list reloads on mount)\n      alert(\"Booking successful!\");\n      navigate(\"/movies\");\n    } catch (err) {\n      setError(\"Something went wrong. Please try again.\");\n    }\n  };\n\n  return (\n    &lt;Container className=\"py-4\" style={{ maxWidth: 520 }}&gt;\n      &lt;h2 className=\"mb-3\"&gt;Create Booking&lt;/h2&gt;\n      {error &amp;&amp; &lt;Alert variant=\"danger\"&gt;{error}&lt;/Alert&gt;}\n\n      &lt;Form onSubmit={handleSubmit}&gt;\n        &lt;Form.Group className=\"mb-3\"&gt;\n          &lt;Form.Label&gt;User&lt;/Form.Label&gt;\n          &lt;Form.Select name=\"userId\" value={form.userId} onChange={handleChange}&gt;\n            &lt;option value=\"\"&gt;Select User&lt;/option&gt;\n            {users.map((u) =&gt; (\n              &lt;option key={u.id} value={u.id}&gt;{u.name}&lt;/option&gt;\n            ))}\n          &lt;/Form.Select&gt;\n        &lt;/Form.Group&gt;\n\n        &lt;Form.Group className=\"mb-3\"&gt;\n          &lt;Form.Label&gt;Movie&lt;/Form.Label&gt;\n          &lt;Form.Select name=\"movieId\" value={form.movieId} onChange={handleChange}&gt;\n            &lt;option value=\"\"&gt;Select Movie&lt;/option&gt;\n            {movies.map((m) =&gt; (\n              &lt;option key={m.id} value={m.id}&gt;{m.title}&lt;/option&gt;\n            ))}\n          &lt;/Form.Select&gt;\n        &lt;/Form.Group&gt;\n\n        &lt;Form.Group className=\"mb-3\"&gt;\n          &lt;Form.Label&gt;Show Time&lt;/Form.Label&gt;\n          &lt;Form.Control\n            type=\"datetime-local\"\n            name=\"showTime\"\n            value={form.showTime}\n            onChange={handleChange}\n          /&gt;\n        &lt;/Form.Group&gt;\n\n        &lt;Form.Group className=\"mb-3\"&gt;\n          &lt;Form.Label&gt;Seats&lt;/Form.Label&gt;\n          &lt;Form.Control\n            type=\"number\"\n            name=\"seats\"\n            min={1}\n            value={form.seats}\n            onChange={handleChange}\n          /&gt;\n        &lt;/Form.Group&gt;\n\n        &lt;Button type=\"submit\"&gt;Submit&lt;/Button&gt;\n      &lt;/Form&gt;\n    &lt;/Container&gt;\n  );\n}</code></pre><p><b>How it works:</b></p><ul><li>Two <code>useEffect</code> fetches populate the <b>userId</b> dropdown from <code>/users</code> (shows <code>name</code>, value = <code>id</code>) and the <b>movieId</b> dropdown from <code>/movies</code> (shows <code>title</code>).</li><li><b>showTime</b> is a <code>datetime-local</code> input; <b>seats</b> is a <code>number</code> input.</li><li>On Submit: if any field is empty OR <code>seats &lt;= 0</code>, it shows the error <i>\"All fields are required and seats must be at least 1\"</i> and stops.</li><li>If valid it <code>POST</code>s to <code>/bookings</code>, then <code>PATCH</code>es <code>/movies/:id</code> to increment <code>booked</code>, alerts <i>\"Booking successful!\"</i> and <code>navigate(\"/movies\")</code> — the list re-fetches on mount so the new booked count shows.</li><li>Route <code>/booking/create</code> (0 points if the URL is wrong).</li></ul></div><div class=\"ml-vi\"><pre><code>// src/components/CreateBooking.jsx\nimport { useEffect, useState } from \"react\";\nimport { useNavigate } from \"react-router-dom\";\nimport { Container, Form, Button, Alert } from \"react-bootstrap\";\n\nconst USERS = \"http://localhost:9999/users\";\nconst MOVIES = \"http://localhost:9999/movies\";\nconst BOOKINGS = \"http://localhost:9999/bookings\";\n\nexport default function CreateBooking() {\n  const navigate = useNavigate();\n  const [users, setUsers] = useState([]);\n  const [movies, setMovies] = useState([]);\n  const [form, setForm] = useState({\n    userId: \"\",\n    movieId: \"\",\n    showTime: \"\",\n    seats: \"\",\n  });\n  const [error, setError] = useState(\"\");\n\n  // Load dropdown data\n  useEffect(() =&gt; {\n    fetch(USERS).then((r) =&gt; r.json()).then(setUsers);\n    fetch(MOVIES).then((r) =&gt; r.json()).then(setMovies);\n  }, []);\n\n  const handleChange = (e) =&gt;\n    setForm({ ...form, [e.target.name]: e.target.value });\n\n  const handleSubmit = async (e) =&gt; {\n    e.preventDefault();\n    setError(\"\");\n\n    // Validate: all fields required, seats &gt; 0\n    if (!form.userId || !form.movieId || !form.showTime || !form.seats) {\n      setError(\"All fields are required and seats must be at least 1\");\n      return;\n    }\n    if (Number(form.seats) &lt;= 0) {\n      setError(\"All fields are required and seats must be at least 1\");\n      return;\n    }\n\n    try {\n      // 1) Save the booking\n      await fetch(BOOKINGS, {\n        method: \"POST\",\n        headers: { \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({\n          userId: form.userId,\n          movieId: form.movieId,\n          showTime: form.showTime,\n          seats: Number(form.seats),\n        }),\n      });\n\n      // 2) Increase the booked count of the selected movie\n      const movie = movies.find((m) =&gt; String(m.id) === String(form.movieId));\n      await fetch(`${MOVIES}/${form.movieId}`, {\n        method: \"PATCH\",\n        headers: { \"Content-Type\": \"application/json\" },\n        body: JSON.stringify({ booked: (movie?.booked || 0) + 1 }),\n      });\n\n      // 3) Success + redirect to /movies (list reloads on mount)\n      alert(\"Booking successful!\");\n      navigate(\"/movies\");\n    } catch (err) {\n      setError(\"Something went wrong. Please try again.\");\n    }\n  };\n\n  return (\n    &lt;Container className=\"py-4\" style={{ maxWidth: 520 }}&gt;\n      &lt;h2 className=\"mb-3\"&gt;Create Booking&lt;/h2&gt;\n      {error &amp;&amp; &lt;Alert variant=\"danger\"&gt;{error}&lt;/Alert&gt;}\n\n      &lt;Form onSubmit={handleSubmit}&gt;\n        &lt;Form.Group className=\"mb-3\"&gt;\n          &lt;Form.Label&gt;User&lt;/Form.Label&gt;\n          &lt;Form.Select name=\"userId\" value={form.userId} onChange={handleChange}&gt;\n            &lt;option value=\"\"&gt;Select User&lt;/option&gt;\n            {users.map((u) =&gt; (\n              &lt;option key={u.id} value={u.id}&gt;{u.name}&lt;/option&gt;\n            ))}\n          &lt;/Form.Select&gt;\n        &lt;/Form.Group&gt;\n\n        &lt;Form.Group className=\"mb-3\"&gt;\n          &lt;Form.Label&gt;Movie&lt;/Form.Label&gt;\n          &lt;Form.Select name=\"movieId\" value={form.movieId} onChange={handleChange}&gt;\n            &lt;option value=\"\"&gt;Select Movie&lt;/option&gt;\n            {movies.map((m) =&gt; (\n              &lt;option key={m.id} value={m.id}&gt;{m.title}&lt;/option&gt;\n            ))}\n          &lt;/Form.Select&gt;\n        &lt;/Form.Group&gt;\n\n        &lt;Form.Group className=\"mb-3\"&gt;\n          &lt;Form.Label&gt;Show Time&lt;/Form.Label&gt;\n          &lt;Form.Control\n            type=\"datetime-local\"\n            name=\"showTime\"\n            value={form.showTime}\n            onChange={handleChange}\n          /&gt;\n        &lt;/Form.Group&gt;\n\n        &lt;Form.Group className=\"mb-3\"&gt;\n          &lt;Form.Label&gt;Seats&lt;/Form.Label&gt;\n          &lt;Form.Control\n            type=\"number\"\n            name=\"seats\"\n            min={1}\n            value={form.seats}\n            onChange={handleChange}\n          /&gt;\n        &lt;/Form.Group&gt;\n\n        &lt;Button type=\"submit\"&gt;Submit&lt;/Button&gt;\n      &lt;/Form&gt;\n    &lt;/Container&gt;\n  );\n}</code></pre><p><b>Cách hoạt động:</b></p><ul><li>Hai lệnh <code>useEffect</code> nạp dropdown <b>userId</b> từ <code>/users</code> (hiện <code>name</code>, value = <code>id</code>) và dropdown <b>movieId</b> từ <code>/movies</code> (hiện <code>title</code>).</li><li><b>showTime</b> là input <code>datetime-local</code>; <b>seats</b> là input <code>number</code>.</li><li>Khi bấm Submit: nếu bất kỳ trường nào trống HOẶC <code>seats &lt;= 0</code> thì hiện lỗi <i>\"All fields are required and seats must be at least 1\"</i> và dừng lại.</li><li>Nếu hợp lệ thì <code>POST</code> vào <code>/bookings</code>, rồi <code>PATCH</code> <code>/movies/:id</code> để tăng <code>booked</code>, hiện alert <i>\"Booking successful!\"</i> và <code>navigate(\"/movies\")</code> — danh sách được nạp lại khi mount nên số booked mới hiển thị.</li><li>Route <code>/booking/create</code> (0 điểm nếu sai URL).</li></ul></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Component routed at <code>/booking/create</code> with a form whose userId and movieId dropdowns are populated from <code>/users</code> and <code>/movies</code>, plus showTime (datetime-local) and seats (number) inputs.|||Component đặt route <code>/booking/create</code> với form có dropdown userId và movieId nạp từ <code>/users</code> và <code>/movies</code>, cùng input showTime (datetime-local) và seats (number).",
              "weight": 1,
              "maxScore": 2
            },
            {
              "id": "c2",
              "criterion": "Validation on Submit: all fields required and seats &gt; 0, showing the exact error message when it fails.|||Kiểm tra hợp lệ khi Submit: mọi trường bắt buộc và seats &gt; 0, hiện đúng thông báo lỗi khi thất bại.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c3",
              "criterion": "On valid submit: POST to /bookings, increment booked on /movies/:id, show success and redirect to /movies.|||Khi submit hợp lệ: POST vào /bookings, tăng booked trên /movies/:id, hiện thành công và chuyển hướng về /movies.",
              "weight": 1,
              "maxScore": 1
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/FER202/PE7.png"
        }
      ]
    }
  ]
};
