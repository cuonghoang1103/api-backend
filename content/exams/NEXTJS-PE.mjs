/**
 * Next.js & React — 10 đề thi thực hành (PE), mỗi đề 5 câu.
 *
 * Vì component React/JSX không chạy được bằng `node` thuần (không có DOM,
 * không có node_modules trong sandbox chấm), mỗi câu PE cô lập một mẩu LOGIC
 * cốt lõi của React/Next thành một hàm Node.js THUẦN đọc stdin, in stdout:
 * reducer, cập nhật bất biến, dò route của App Router, cache key, session,
 * validate form của Server Action… Nhờ vậy máy chấm chạy được và học viên
 * luyện đúng tư duy sẽ dùng trong component thật.
 *
 * ⚠️ Mọi `expectedOutput` dưới đây là stdout THẬT của `sampleSolution` khi chạy
 * bằng `node` với đúng phần INPUT ghi kèm (đã kiểm qua harness + exam-check).
 *
 * Seed: node scripts/academy-seed-exam.mjs --file ./content/exams/NEXTJS-PE.mjs --apply
 */
import { B } from './_lib/nextjs-exam-kit.mjs';

const RUBRIC = [
  { id: 'correct', criterion: B('The program produces exactly the expected output for the given input, including the edge cases named in the problem.', 'Chương trình cho đúng kết quả kỳ vọng với dữ liệu vào đã cho, kể cả các ca biên nêu trong đề.'), weight: 3, maxScore: 4 },
  { id: 'logic', criterion: B('The approach models the React/Next idea correctly — immutable updates, stable keys, the right Server/Client reasoning, no mutation where the problem forbids it.', 'Cách làm mô hình hoá đúng ý tưởng React/Next — cập nhật bất biến, key ổn định, suy luận Server/Client đúng chỗ, không mutate ở nơi đề cấm.'), weight: 2, maxScore: 4 },
  { id: 'quality', criterion: B('Readable Node.js: clear names, no dead code, no dependency beyond the standard library.', 'Mã Node.js dễ đọc: đặt tên rõ, không có mã thừa, không dùng thư viện ngoài thư viện chuẩn.'), weight: 1, maxScore: 4 },
];

const INSTR =
  '<div class="ml-en"><p><b>How to take this exam.</b></p><ol>' +
  '<li>Create five files named <code>Q1.js … Q5.js</code> in your own editor — one per question, plain Node.js, <b>no npm packages</b>. Each question isolates a piece of React/Next logic as a pure function.</li>' +
  '<li>Each program reads its input from <b>stdin</b> and writes the answer to <b>stdout</b>. Test with <code>node Q1.js &lt; input.txt</code>, or paste the input and press Ctrl+D.</li>' +
  '<li>The "expected output" block shows the exact input used and the exact output required — match it line for line.</li>' +
  '<li>Zip the five files into <b>one .zip</b> and upload it. AI grades each against the problem, the expected output and a reference solution.</li>' +
  '</ol></div>' +
  '<div class="ml-vi"><p><b>Cách làm bài thi.</b></p><ol>' +
  '<li>Tạo năm file tên <code>Q1.js … Q5.js</code> bằng trình soạn thảo của bạn — mỗi câu một file, Node.js thuần, <b>không dùng gói npm nào</b>. Mỗi câu cô lập một mẩu logic React/Next thành một hàm thuần.</li>' +
  '<li>Mỗi chương trình đọc dữ liệu từ <b>stdin</b> và in kết quả ra <b>stdout</b>. Chạy thử bằng <code>node Q1.js &lt; input.txt</code>, hoặc dán dữ liệu rồi bấm Ctrl+D.</li>' +
  '<li>Khối "kết quả mong đợi" ghi rõ dữ liệu vào đã dùng và kết quả bắt buộc — hãy khớp từng dòng.</li>' +
  '<li>Nén năm file thành <b>một file .zip</b> rồi tải lên. AI chấm từng câu theo đề, kết quả mong đợi và một đáp án mẫu.</li>' +
  '</ol></div>';

const R = 'const IN=require("fs").readFileSync(0,"utf8");';
const cq = (points, prompt, expectedOutput, sampleSolution) =>
  ({ kind: 'CODE', points, language: 'javascript', prompt, expectedOutput, sampleSolution, rubric: RUBRIC });

const PE = (n, sortOrder, title, description, questions) => ({
  kind: 'PE', peType: 'CODE', code: `PE-${String(n).padStart(2, '0')}`, source: 'SAMPLE', sortOrder,
  title, description, durationMinutes: 90, totalPoints: 10, passMark: 4, isPublished: true,
  instructions: INSTR, questions,
});

export default {
  course: { slug: 'nextjs' },
  exams: [
    // ── PE-01 — JSX & props foundations (Ch 1–2) ────────────────────────
    PE(1, 10,
      B('Practical Exam 01 — JSX and props foundations', 'Thi thực hành 01 — Nền tảng JSX và props'),
      B('Chapter 1–2: how JSX renders values, the zero bug, conditional classes, and component vs HTML naming.', 'Chương 1–2: JSX render giá trị thế nào, bẫy số 0, class theo điều kiện, và cách đặt tên component so với thẻ HTML.'),
      [
        cq(2,
          B('<p><b>Q1 — The zero bug.</b> Read one line of space-separated integers. For each, print what React renders for <code>{n &amp;&amp; "Y"}</code>: the number itself when it is <code>0</code>, otherwise <code>"Y"</code>. Join with single spaces on one line.</p>',
            '<p><b>Câu 1 — Bẫy số 0.</b> Đọc một dòng các số nguyên cách nhau bằng dấu cách. Với mỗi số, in ra thứ React render cho <code>{n &amp;&amp; "Y"}</code>: chính con số đó khi nó là <code>0</code>, ngược lại là <code>"Y"</code>. Nối bằng một dấu cách trên một dòng.</p>'),
          'INPUT:\n0 3 0 5\nOUTPUT:\n0 Y 0 Y',
          `${R}\nconst nums=IN.trim().split(/\\s+/).map(Number);\nconsole.log(nums.map(n=>n===0?"0":"Y").join(" "));`),
        cq(2,
          B('<p><b>Q2 — Conditional classes.</b> Read lines of the form <code>name:flag</code> where flag is <code>0</code> or <code>1</code>. Print, space-separated on one line, the class names whose flag is <code>1</code>, in input order.</p>',
            '<p><b>Câu 2 — Class theo điều kiện.</b> Đọc các dòng dạng <code>name:flag</code> với flag là <code>0</code> hoặc <code>1</code>. In ra trên một dòng, cách nhau bằng dấu cách, những tên class có flag là <code>1</code>, theo thứ tự nhập.</p>'),
          'INPUT:\nbtn:1\nactive:0\nlg:1\nOUTPUT:\nbtn lg',
          `${R}\nconst out=IN.trim().split(/\\n/).filter(Boolean).filter(l=>l.trim().endsWith(":1")).map(l=>l.split(":")[0]);\nconsole.log(out.join(" "));`),
        cq(2,
          B('<p><b>Q3 — Default prop.</b> Read one line: a name. Print <code>Hi, &lt;name&gt;!</code>. If the line is empty or only whitespace, use <code>guest</code> instead of the name.</p>',
            '<p><b>Câu 3 — Prop mặc định.</b> Đọc một dòng: một cái tên. In <code>Hi, &lt;tên&gt;!</code>. Nếu dòng rỗng hoặc chỉ có khoảng trắng, dùng <code>guest</code> thay cho tên.</p>'),
          'INPUT:\nBình\nOUTPUT:\nHi, Bình!',
          `${R}\nconst name=IN.split(/\\n/)[0].trim();\nconsole.log("Hi, "+(name||"guest")+"!");`),
        cq(2,
          B('<p><b>Q4 — Component vs HTML tag.</b> Read tag names, one per line. In JSX a capitalised tag is your component and a lowercase tag is an HTML element. Print <code>components=X tags=Y</code>.</p>',
            '<p><b>Câu 4 — Component so với thẻ HTML.</b> Đọc các tên thẻ, mỗi dòng một tên. Trong JSX, thẻ viết hoa là component của bạn còn thẻ viết thường là phần tử HTML. In <code>components=X tags=Y</code>.</p>'),
          'INPUT:\ndiv\nCard\nspan\nButton\nOUTPUT:\ncomponents=2 tags=2',
          `${R}\nconst tags=IN.trim().split(/\\n/).filter(Boolean);\nlet c=0,t=0; for(const x of tags){ if(/^[A-Z]/.test(x)) c++; else t++; }\nconsole.log("components="+c+" tags="+t);`),
        cq(2,
          B('<p><b>Q5 — Render a list.</b> Read one integer <code>n</code>. Print the HTML that <code>&lt;ul&gt;{Array.from({length:n}).map((_,i)=&gt;&lt;li&gt;{i+1}&lt;/li&gt;)}&lt;/ul&gt;</code> produces, all on one line, e.g. <code>&lt;ul&gt;&lt;li&gt;1&lt;/li&gt;…&lt;/ul&gt;</code>.</p>',
            '<p><b>Câu 5 — Render một danh sách.</b> Đọc một số nguyên <code>n</code>. In ra HTML mà <code>&lt;ul&gt;{Array.from({length:n}).map((_,i)=&gt;&lt;li&gt;{i+1}&lt;/li&gt;)}&lt;/ul&gt;</code> tạo ra, tất cả trên một dòng, ví dụ <code>&lt;ul&gt;&lt;li&gt;1&lt;/li&gt;…&lt;/ul&gt;</code>.</p>'),
          'INPUT:\n3\nOUTPUT:\n<ul><li>1</li><li>2</li><li>3</li></ul>',
          `${R}\nconst n=Number(IN.trim());\nconsole.log("<ul>"+Array.from({length:n},(_,i)=>"<li>"+(i+1)+"</li>").join("")+"</ul>");`),
      ]),

    // ── PE-02 — State & immutability (Ch 3) ─────────────────────────────
    PE(2, 11,
      B('Practical Exam 02 — State and immutability', 'Thi thực hành 02 — State và tính bất biến'),
      B('Chapter 3: the snapshot vs updater rule, immutable array/object updates, toggling by id, and derived state.', 'Chương 3: luật ảnh chụp so với hàm cập nhật, cập nhật mảng/object bất biến, đảo theo id, và dữ liệu suy ra.'),
      [
        cq(2,
          B('<p><b>Q1 — Snapshot vs updater.</b> Read one integer <code>k</code>. Calling <code>setCount(count+1)</code> k times in one handler lands on 1 (snapshot); calling <code>setCount(c=&gt;c+1)</code> k times lands on k (updater). Print <code>snapshot=1 functional=k</code>.</p>',
            '<p><b>Câu 1 — Ảnh chụp so với hàm cập nhật.</b> Đọc một số nguyên <code>k</code>. Gọi <code>setCount(count+1)</code> k lần trong một handler dừng ở 1 (ảnh chụp); gọi <code>setCount(c=&gt;c+1)</code> k lần dừng ở k (hàm cập nhật). In <code>snapshot=1 functional=k</code>.</p>'),
          'INPUT:\n3\nOUTPUT:\nsnapshot=1 functional=3',
          `${R}\nconst k=Number(IN.trim());\nconsole.log("snapshot=1 functional="+k);`),
        cq(2,
          B('<p><b>Q2 — Immutable append.</b> Line 1 is a comma-separated list (possibly empty). Line 2 is one value to append. Print the resulting array as JSON, without mutating the original.</p>',
            '<p><b>Câu 2 — Thêm phần tử bất biến.</b> Dòng 1 là danh sách cách nhau bởi dấu phẩy (có thể rỗng). Dòng 2 là một giá trị cần thêm vào cuối. In mảng kết quả dưới dạng JSON, không mutate mảng gốc.</p>'),
          'INPUT:\na,b,c\nd\nOUTPUT:\n["a","b","c","d"]',
          `${R}\nconst [a,b]=IN.split(/\\n/);\nconst arr=a.trim()?a.trim().split(","):[];\nconsole.log(JSON.stringify([...arr,b.trim()]));`),
        cq(2,
          B('<p><b>Q3 — Immutable object update.</b> Line 1 is a JSON object. Line 2 is <code>key=value</code>. Print a NEW object equal to the first but with <code>key</code> set to <code>value</code>. If value is a plain integer, store it as a number.</p>',
            '<p><b>Câu 3 — Cập nhật object bất biến.</b> Dòng 1 là một object JSON. Dòng 2 là <code>key=value</code>. In một object MỚI bằng object đầu nhưng đặt <code>key</code> thành <code>value</code>. Nếu value là số nguyên thuần, lưu dưới dạng số.</p>'),
          'INPUT:\n{"name":"x","age":0}\nage=30\nOUTPUT:\n{"name":"x","age":30}',
          `${R}\nconst [a,b]=IN.split(/\\n/);\nconst obj=JSON.parse(a); const [key,...rest]=b.trim().split("="); const val=rest.join("=");\nconst num=Number(val); const v=(val!==""&&!Number.isNaN(num)&&String(num)===val)?num:val;\nconsole.log(JSON.stringify({...obj,[key]:v}));`),
        cq(2,
          B('<p><b>Q4 — Toggle by id.</b> Line 1 is a JSON array of <code>{id,done}</code>. Line 2 is an id. Flip <code>done</code> on the matching item, immutably, and print the new array as JSON.</p>',
            '<p><b>Câu 4 — Đảo theo id.</b> Dòng 1 là mảng JSON các <code>{id,done}</code>. Dòng 2 là một id. Đảo <code>done</code> của phần tử khớp id, một cách bất biến, rồi in mảng mới dưới dạng JSON.</p>'),
          'INPUT:\n[{"id":1,"done":false},{"id":2,"done":true}]\n1\nOUTPUT:\n[{"id":1,"done":true},{"id":2,"done":true}]',
          `${R}\nconst [a,b]=IN.split(/\\n/);\nconst arr=JSON.parse(a); const id=Number(b.trim());\nconsole.log(JSON.stringify(arr.map(t=>t.id===id?{...t,done:!t.done}:t)));`),
        cq(2,
          B('<p><b>Q5 — Derived state.</b> Read a first name (line 1) and last name (line 2). Print <code>&lt;first&gt; &lt;last&gt; &lt;INITIALS&gt;</code> where initials are the two first letters, uppercased. Do not store the full name — derive it.</p>',
            '<p><b>Câu 5 — Dữ liệu suy ra.</b> Đọc họ (dòng 1) và tên (dòng 2). In <code>&lt;họ&gt; &lt;tên&gt; &lt;VIẾT_TẮT&gt;</code> với viết tắt là hai chữ cái đầu, viết hoa. Đừng lưu tên đầy đủ — hãy suy ra nó.</p>'),
          'INPUT:\nNguyen\nBinh\nOUTPUT:\nNguyen Binh NB',
          `${R}\nconst [f,l]=IN.split(/\\n/).map(s=>s.trim());\nconst initials=(f[0]||"")+(l[0]||"");\nconsole.log(f+" "+l+" "+initials.toUpperCase());`),
      ]),

    // ── PE-03 — Events & controlled forms (Ch 4) ────────────────────────
    PE(3, 12,
      B('Practical Exam 03 — Events and controlled forms', 'Thi thực hành 03 — Sự kiện và form có kiểm soát'),
      B('Chapter 4: validation, controlled transforms, counting form errors, the IME submit guard, and checkbox groups.', 'Chương 4: kiểm tra hợp lệ, biến đổi có kiểm soát, đếm lỗi form, chốt chặn IME khi submit, và nhóm checkbox.'),
      [
        cq(2,
          B('<p><b>Q1 — Validate email.</b> Read one line. Print <code>valid</code> if it looks like <code>x@y.z</code> (no spaces, a single-ish local, an @, a domain with a dot), else <code>invalid</code>.</p>',
            '<p><b>Câu 1 — Kiểm email.</b> Đọc một dòng. In <code>valid</code> nếu nó trông giống <code>x@y.z</code> (không khoảng trắng, phần đầu, một @, một tên miền có dấu chấm), ngược lại <code>invalid</code>.</p>'),
          'INPUT:\nbinh@ct.vn\nOUTPUT:\nvalid',
          `${R}\nconst e=IN.trim();\nconsole.log(/^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$/.test(e)?"valid":"invalid");`),
        cq(2,
          B('<p><b>Q2 — Controlled transform.</b> A controlled input can transform on change. Read one line and print it uppercased.</p>',
            '<p><b>Câu 2 — Biến đổi có kiểm soát.</b> Một ô nhập có kiểm soát có thể biến đổi giá trị khi onChange. Đọc một dòng và in ra bản viết hoa.</p>'),
          'INPUT:\nha noi\nOUTPUT:\nHA NOI',
          `${R}\nconsole.log(IN.split(/\\n/)[0].toUpperCase());`),
        cq(2,
          B('<p><b>Q3 — Count form errors.</b> Read a JSON object with <code>name</code>, <code>email</code>, <code>agree</code>. A field is invalid when: name is empty/whitespace, email is not <code>x@y.z</code>, or agree is not <code>true</code>. Print <code>&lt;count&gt; &lt;comma-separated invalid field names, in that order&gt;</code>.</p>',
            '<p><b>Câu 3 — Đếm lỗi form.</b> Đọc một object JSON có <code>name</code>, <code>email</code>, <code>agree</code>. Một trường sai khi: name rỗng/khoảng trắng, email không phải <code>x@y.z</code>, hoặc agree khác <code>true</code>. In <code>&lt;số lượng&gt; &lt;tên các trường sai, cách nhau bằng dấu phẩy, đúng thứ tự đó&gt;</code>.</p>'),
          'INPUT:\n{"name":"","email":"a@b","agree":true}\nOUTPUT:\n2 name,email',
          `${R}\nconst v=JSON.parse(IN.trim());\nconst errs=[];\nif(!String(v.name||"").trim()) errs.push("name");\nif(!/^[^@\\s]+@[^@\\s]+\\.[^@\\s]+$/.test(v.email||"")) errs.push("email");\nif(v.agree!==true) errs.push("agree");\nconsole.log(errs.length+" "+errs.join(","));`),
        cq(2,
          B('<p><b>Q4 — IME submit guard.</b> Read lines of the form <code>key composing</code> where composing is <code>1</code> (IME is composing) or <code>0</code>. Count how many are a real submit: an <code>Enter</code> that is NOT composing. Print <code>submits=N</code>.</p>',
            '<p><b>Câu 4 — Chốt chặn IME khi submit.</b> Đọc các dòng dạng <code>key composing</code> với composing là <code>1</code> (IME đang soạn) hoặc <code>0</code>. Đếm bao nhiêu lần là submit thật: một <code>Enter</code> mà KHÔNG đang soạn. In <code>submits=N</code>.</p>'),
          'INPUT:\nEnter 1\nEnter 0\nEnter 0\nEnter 1\nOUTPUT:\nsubmits=2',
          `${R}\nconst lines=IN.trim().split(/\\n/).filter(Boolean);\nlet submits=0; for(const l of lines){ const [key,comp]=l.trim().split(/\\s+/); if(key==="Enter"&&comp==="0") submits++; }\nconsole.log("submits="+submits);`),
        cq(2,
          B('<p><b>Q5 — Checkbox group.</b> Read lines <code>option:flag</code> (flag 0/1). Print the selected options (flag 1) as a comma-separated list on one line, in input order.</p>',
            '<p><b>Câu 5 — Nhóm checkbox.</b> Đọc các dòng <code>option:flag</code> (flag 0/1). In các option được chọn (flag 1) dưới dạng danh sách cách nhau bằng dấu phẩy trên một dòng, theo thứ tự nhập.</p>'),
          'INPUT:\nsms:0\nemail:1\npush:1\nOUTPUT:\nemail,push',
          `${R}\nconst lines=IN.trim().split(/\\n/).filter(Boolean);\nconst sel=lines.filter(l=>l.trim().endsWith(":1")).map(l=>l.split(":")[0]);\nconsole.log(sel.join(","));`),
      ]),

    // ── PE-04 — Effects & the hooks toolbox (Ch 5–6) ────────────────────
    PE(4, 13,
      B('Practical Exam 04 — Effects and the hooks toolbox', 'Thi thực hành 04 — Effect và hộp công cụ hooks'),
      B('Chapter 5–6: effect setup/cleanup order, StrictMode double-invoke, useMemo recomputes, useRef persistence, and usePrevious.', 'Chương 5–6: thứ tự setup/cleanup của effect, StrictMode chạy đôi, số lần tính lại của useMemo, ref bền qua render, và usePrevious.'),
      [
        cq(2,
          B('<p><b>Q1 — Setup/cleanup order.</b> An effect runs setup once on mount, then before each re-run it cleans up first. Read <code>n</code> = the number of times the effect runs. Print the log: <code>run</code>, then <code>clean run</code> repeated for each subsequent run, space-separated.</p>',
            '<p><b>Câu 1 — Thứ tự setup/cleanup.</b> Một effect chạy setup một lần lúc mount, rồi trước mỗi lần chạy lại nó dọn dẹp trước. Đọc <code>n</code> = số lần effect chạy. In nhật ký: <code>run</code>, rồi <code>clean run</code> lặp cho mỗi lần chạy tiếp theo, cách nhau bằng dấu cách.</p>'),
          'INPUT:\n3\nOUTPUT:\nrun clean run clean run',
          `${R}\nconst n=Number(IN.trim());\nconst out=["run"]; for(let i=1;i<n;i++){ out.push("clean","run"); }\nconsole.log(out.join(" "));`),
        cq(2,
          B('<p><b>Q2 — StrictMode.</b> Read <code>dev</code> or <code>prod</code>. In dev, StrictMode runs setup, cleanup, setup again, so print <code>run clean run</code>. In prod, print <code>run</code>.</p>',
            '<p><b>Câu 2 — StrictMode.</b> Đọc <code>dev</code> hoặc <code>prod</code>. Ở dev, StrictMode chạy setup, cleanup, rồi setup lại, nên in <code>run clean run</code>. Ở prod, in <code>run</code>.</p>'),
          'INPUT:\ndev\nOUTPUT:\nrun clean run',
          `${R}\nconst mode=IN.trim();\nconsole.log(mode==="dev"?"run clean run":"run");`),
        cq(2,
          B('<p><b>Q3 — useMemo recomputes.</b> A memo recomputes only when its dependency value changes from the previous render. Read space-separated values (the dep across renders). Print <code>recomputes=N</code>, counting the first render and each time the value differs from the one before it.</p>',
            '<p><b>Câu 3 — Số lần useMemo tính lại.</b> Một memo chỉ tính lại khi giá trị dependency đổi so với lần render trước. Đọc các giá trị cách nhau bằng dấu cách (dep qua các lần render). In <code>recomputes=N</code>, đếm lần render đầu và mỗi khi giá trị khác giá trị ngay trước.</p>'),
          'INPUT:\n2 2 3 3 3 2\nOUTPUT:\nrecomputes=3',
          `${R}\nconst vals=IN.trim().split(/\\s+/);\nlet recomputes=0,prev; for(const v of vals){ if(v!==prev) recomputes++; prev=v; }\nconsole.log("recomputes="+recomputes);`),
        cq(2,
          B('<p><b>Q4 — useRef persistence.</b> A ref survives renders but mutating it does NOT re-render. Read <code>n</code> = times you increment <code>ref.current</code> (starting 0). Print <code>ref=&lt;value&gt; renders=1</code>.</p>',
            '<p><b>Câu 4 — Ref bền qua render.</b> Một ref sống sót qua render nhưng sửa nó KHÔNG gây render lại. Đọc <code>n</code> = số lần bạn tăng <code>ref.current</code> (bắt đầu từ 0). In <code>ref=&lt;giá trị&gt; renders=1</code>.</p>'),
          'INPUT:\n5\nOUTPUT:\nref=5 renders=1',
          `${R}\nconst n=Number(IN.trim());\nconsole.log("ref="+n+" renders=1");`),
        cq(2,
          B('<p><b>Q5 — usePrevious.</b> A custom hook returns the value from the previous render. Read a line of space-separated values. Print <code>current:previous</code> for each, where the first has previous <code>undefined</code>. Space-separated.</p>',
            '<p><b>Câu 5 — usePrevious.</b> Một custom hook trả về giá trị của lần render trước. Đọc một dòng các giá trị cách nhau bằng dấu cách. In <code>current:previous</code> cho từng giá trị, giá trị đầu có previous là <code>undefined</code>. Cách nhau bằng dấu cách.</p>'),
          'INPUT:\na b c\nOUTPUT:\na:undefined b:a c:b',
          `${R}\nconst vals=IN.trim().split(/\\s+/);\nlet prev; const out=vals.map(v=>{ const p=v+":"+(prev===undefined?"undefined":prev); prev=v; return p; });\nconsole.log(out.join(" "));`),
      ]),

    // ── PE-05 — Lists, keys & reconciliation (Ch 7) ─────────────────────
    PE(5, 14,
      B('Practical Exam 05 — Lists, keys and reconciliation', 'Thi thực hành 05 — Danh sách, key và reconciliation'),
      B('Chapter 7: dedupe by id, stable sort, remount on type change, duplicate-key detection, and memo skip counting.', 'Chương 7: khử trùng theo id, sắp xếp ổn định, remount khi đổi kiểu, phát hiện key trùng, và đếm lần memo bỏ render.'),
      [
        cq(2,
          B('<p><b>Q1 — Dedupe by id (stable keys).</b> Read a JSON array of <code>{id,name}</code>. Print, as a JSON array, the ids after removing duplicates and keeping the first occurrence of each.</p>',
            '<p><b>Câu 1 — Khử trùng theo id (key ổn định).</b> Đọc một mảng JSON các <code>{id,name}</code>. In ra, dưới dạng mảng JSON, các id sau khi bỏ trùng và giữ lần xuất hiện đầu tiên của mỗi id.</p>'),
          'INPUT:\n[{"id":1,"name":"a"},{"id":2,"name":"b"},{"id":1,"name":"c"},{"id":3,"name":"d"}]\nOUTPUT:\n[1,2,3]',
          `${R}\nconst arr=JSON.parse(IN.trim()); const seen=new Set(); const out=[];\nfor(const it of arr){ if(!seen.has(it.id)){ seen.add(it.id); out.push(it.id); } }\nconsole.log(JSON.stringify(out));`),
        cq(2,
          B('<p><b>Q2 — Stable sort.</b> Read lines <code>name score</code>. Sort by score descending; ties keep input order (a stable sort). Print the names space-separated on one line.</p>',
            '<p><b>Câu 2 — Sắp xếp ổn định.</b> Đọc các dòng <code>name score</code>. Sắp theo score giảm dần; khi bằng điểm thì giữ thứ tự nhập (sắp xếp ổn định). In các tên cách nhau bằng dấu cách trên một dòng.</p>'),
          'INPUT:\nan 5\nbình 9\ncúc 5\ndũng 9\nOUTPUT:\nbình dũng an cúc',
          `${R}\nconst lines=IN.trim().split(/\\n/).filter(Boolean).map((l,i)=>{ const p=l.trim().split(/\\s+/); return {name:p[0],score:Number(p[1]),i}; });\nlines.sort((a,b)=> b.score-a.score || a.i-b.i);\nconsole.log(lines.map(x=>x.name).join(" "));`),
        cq(2,
          B('<p><b>Q3 — Remount on type change.</b> Line 1 and line 2 are the component types at each position, before and after a render. For each position print <code>keep</code> if the type is unchanged, else <code>remount</code>. Space-separated.</p>',
            '<p><b>Câu 3 — Remount khi đổi kiểu.</b> Dòng 1 và dòng 2 là kiểu component ở từng vị trí, trước và sau một lần render. Với mỗi vị trí, in <code>keep</code> nếu kiểu không đổi, ngược lại <code>remount</code>. Cách nhau bằng dấu cách.</p>'),
          'INPUT:\nLogin Nav Feed\nLogin Nav Dashboard\nOUTPUT:\nkeep keep remount',
          `${R}\nconst [a,b]=IN.split(/\\n/).map(s=>s.trim().split(/\\s+/));\nconsole.log(a.map((t,i)=> t===b[i]?"keep":"remount").join(" "));`),
        cq(2,
          B('<p><b>Q4 — Duplicate key detection.</b> Read one line of space-separated keys. Print <code>ok</code> if they are all unique, else <code>dup</code>.</p>',
            '<p><b>Câu 4 — Phát hiện key trùng.</b> Đọc một dòng các key cách nhau bằng dấu cách. In <code>ok</code> nếu tất cả duy nhất, ngược lại <code>dup</code>.</p>'),
          'INPUT:\n1 2 2 4\nOUTPUT:\ndup',
          `${R}\nconst keys=IN.trim().split(/\\s+/);\nconsole.log(new Set(keys).size===keys.length?"ok":"dup");`),
        cq(2,
          B('<p><b>Q5 — memo skip count.</b> A <code>React.memo</code> child re-renders only when a prop actually changed. Read lines of <code>1</code> (prop changed) or <code>0</code> (unchanged). Print <code>childRenders=N</code>, counting only the changed ones.</p>',
            '<p><b>Câu 5 — Đếm lần memo bỏ render.</b> Một con <code>React.memo</code> chỉ render lại khi có prop thật sự đổi. Đọc các dòng <code>1</code> (prop đổi) hoặc <code>0</code> (không đổi). In <code>childRenders=N</code>, chỉ đếm những lần đổi.</p>'),
          'INPUT:\n1\n0\n1\n0\n0\nOUTPUT:\nchildRenders=2',
          `${R}\nconst lines=IN.trim().split(/\\n/).filter(Boolean);\nlet renders=0; for(const l of lines){ if(l.trim()==="1") renders++; }\nconsole.log("childRenders="+renders);`),
      ]),

    // ── PE-06 — App Router matching (Ch 8, 11) ──────────────────────────
    PE(6, 15,
      B('Practical Exam 06 — App Router matching', 'Thi thực hành 06 — So khớp của App Router'),
      B('Chapter 8 & 11: match a pathname to a route, extract dynamic and catch-all params, strip route groups, and classify a segment.', 'Chương 8 & 11: khớp pathname với route, rút tham số động và catch-all, bỏ route group, và phân loại một đoạn.'),
      [
        cq(2,
          B('<p><b>Q1 — Match a route.</b> Line 1 is a pathname; the remaining lines are route patterns. A plain segment must equal; a <code>[x]</code> segment matches any one segment; segment counts must be equal. Print the first matching pattern, or <code>null</code>.</p>',
            '<p><b>Câu 1 — Khớp một route.</b> Dòng 1 là pathname; các dòng còn lại là các mẫu route. Đoạn thường phải bằng; đoạn <code>[x]</code> khớp một đoạn bất kỳ; số đoạn phải bằng nhau. In mẫu khớp đầu tiên, hoặc <code>null</code>.</p>'),
          'INPUT:\n/blog/react\n/\n/blog\n/blog/[slug]\nOUTPUT:\n/blog/[slug]',
          `${R}\nconst lines=IN.trim().split(/\\n/).filter(Boolean);\nconst pathname=lines[0].trim(); const routes=lines.slice(1).map(s=>s.trim());\nfunction match(pn,rt){ const s=pn.split("/").filter(Boolean),p=rt.split("/").filter(Boolean);\n if(p.length!==s.length) return false;\n for(let i=0;i<p.length;i++){ if(p[i].startsWith("[")&&p[i].endsWith("]")) continue; if(p[i]!==s[i]) return false; } return true; }\nconst hit=routes.find(r=>match(pathname,r));\nconsole.log(hit||"null");`),
        cq(2,
          B('<p><b>Q2 — Extract dynamic params.</b> Line 1 is a route pattern with <code>[name]</code> segments; line 2 is a matching pathname. Print the params object as JSON.</p>',
            '<p><b>Câu 2 — Rút tham số động.</b> Dòng 1 là mẫu route có các đoạn <code>[name]</code>; dòng 2 là pathname khớp. In object params dưới dạng JSON.</p>'),
          'INPUT:\n/shop/[cat]/[id]\n/shop/shoes/42\nOUTPUT:\n{"cat":"shoes","id":"42"}',
          `${R}\nconst [pattern,pathname]=IN.trim().split(/\\n/).map(s=>s.trim());\nconst p=pattern.split("/").filter(Boolean),s=pathname.split("/").filter(Boolean);\nconst params={}; for(let i=0;i<p.length;i++){ if(p[i].startsWith("[")&&p[i].endsWith("]")) params[p[i].slice(1,-1)]=s[i]; }\nconsole.log(JSON.stringify(params));`),
        cq(2,
          B('<p><b>Q3 — Route group to URL.</b> Read a file path under <code>app/</code>. Produce the URL by dropping <code>app</code>, the trailing <code>page</code>, and any <code>(group)</code> segment (parentheses). Print the URL starting with <code>/</code>.</p>',
            '<p><b>Câu 3 — Route group ra URL.</b> Đọc một đường dẫn file dưới <code>app/</code>. Tạo URL bằng cách bỏ <code>app</code>, chữ <code>page</code> ở cuối, và mọi đoạn <code>(group)</code> (có ngoặc đơn). In URL bắt đầu bằng <code>/</code>.</p>'),
          'INPUT:\napp/(marketing)/about/page\nOUTPUT:\n/about',
          `${R}\nconst fp=IN.trim();\nconst url="/"+fp.split("/").filter(seg=>seg&&seg!=="app"&&seg!=="page"&&!(seg.startsWith("(")&&seg.endsWith(")"))).join("/");\nconsole.log(url);`),
        cq(2,
          B('<p><b>Q4 — Catch-all params.</b> Line 1 is a pattern containing a <code>[...name]</code> segment; line 2 is a pathname. Capture the rest of the path as an ARRAY into <code>params.name</code> and print the params as JSON.</p>',
            '<p><b>Câu 4 — Tham số catch-all.</b> Dòng 1 là mẫu chứa một đoạn <code>[...name]</code>; dòng 2 là pathname. Bắt phần còn lại của đường dẫn thành một MẢNG vào <code>params.name</code> và in params dưới dạng JSON.</p>'),
          'INPUT:\n/docs/[...path]\n/docs/a/b/c\nOUTPUT:\n{"path":["a","b","c"]}',
          `${R}\nconst [pattern,pathname]=IN.trim().split(/\\n/).map(s=>s.trim());\nconst p=pattern.split("/").filter(Boolean),s=pathname.split("/").filter(Boolean);\nconst params={}; for(let i=0;i<p.length;i++){ if(p[i].startsWith("[...")){ params[p[i].slice(4,-1)]=s.slice(i); break; } }\nconsole.log(JSON.stringify(params));`),
        cq(2,
          B('<p><b>Q5 — Classify a segment.</b> Read one folder-segment name. Print <code>catchall</code> for <code>[...x]</code>, <code>dynamic</code> for <code>[x]</code>, otherwise <code>static</code>.</p>',
            '<p><b>Câu 5 — Phân loại một đoạn.</b> Đọc một tên đoạn thư mục. In <code>catchall</code> cho <code>[...x]</code>, <code>dynamic</code> cho <code>[x]</code>, ngược lại <code>static</code>.</p>'),
          'INPUT:\n[id]\nOUTPUT:\ndynamic',
          `${R}\nconst seg=IN.trim();\nlet kind="static"; if(seg.startsWith("[...")&&seg.endsWith("]")) kind="catchall"; else if(seg.startsWith("[")&&seg.endsWith("]")) kind="dynamic";\nconsole.log(kind);`),
      ]),

    // ── PE-07 — Data fetching & caching (Ch 10) ─────────────────────────
    PE(7, 16,
      B('Practical Exam 07 — Data fetching and caching', 'Thi thực hành 07 — Tải dữ liệu và cache'),
      B('Chapter 10: a deterministic cache key, revalidate decisions, pagination math, static vs dynamic, and request dedup.', 'Chương 10: cache key tất định, quyết định làm mới, tính phân trang, tĩnh so với động, và khử trùng request.'),
      [
        cq(2,
          B('<p><b>Q1 — Cache key.</b> Line 1 is a base path; line 2 is a JSON params object. Drop empty/null/undefined values, sort remaining keys ascending, URL-encode values, and print <code>base?k=v&amp;...</code> (or just base if empty).</p>',
            '<p><b>Câu 1 — Cache key.</b> Dòng 1 là base path; dòng 2 là object params JSON. Bỏ các giá trị rỗng/null/undefined, sắp các key còn lại tăng dần, URL-encode giá trị, và in <code>base?k=v&amp;...</code> (hoặc chỉ base nếu rỗng).</p>'),
          'INPUT:\n/api/posts\n{"page":2,"q":"","tag":"react"}\nOUTPUT:\n/api/posts?page=2&tag=react',
          `${R}\nconst [base,pjson]=IN.trim().split(/\\n/);\nconst params=JSON.parse(pjson);\nconst clean=Object.entries(params).filter(([,v])=>v!==""&&v!==null&&v!==undefined).map(([k,v])=>[k,String(v)]).sort((a,b)=>a[0]<b[0]?-1:a[0]>b[0]?1:0);\nconst qs=clean.map(([k,v])=>k+"="+encodeURIComponent(v)).join("&");\nconsole.log(qs?base+"?"+qs:base);`),
        cq(2,
          B('<p><b>Q2 — Revalidate decision.</b> Read three numbers: <code>generatedAt now revalidateSeconds</code>. Print <code>regenerate</code> if <code>now - generatedAt &gt;= revalidateSeconds</code>, else <code>fresh</code>.</p>',
            '<p><b>Câu 2 — Quyết định làm mới.</b> Đọc ba số: <code>generatedAt now revalidateSeconds</code>. In <code>regenerate</code> nếu <code>now - generatedAt &gt;= revalidateSeconds</code>, ngược lại <code>fresh</code>.</p>'),
          'INPUT:\n100 145 60\nOUTPUT:\nfresh',
          `${R}\nconst [gen,now,rev]=IN.trim().split(/\\s+/).map(Number);\nconsole.log(now-gen>=rev?"regenerate":"fresh");`),
        cq(2,
          B('<p><b>Q3 — Pagination.</b> Read three numbers: <code>total page limit</code> (page is 1-based). Print <code>offset=&lt;(page-1)*limit&gt; hasMore=&lt;true|false&gt;</code> where hasMore is whether a next page exists.</p>',
            '<p><b>Câu 3 — Phân trang.</b> Đọc ba số: <code>total page limit</code> (page tính từ 1). In <code>offset=&lt;(page-1)*limit&gt; hasMore=&lt;true|false&gt;</code> với hasMore cho biết có còn trang sau không.</p>'),
          'INPUT:\n25 2 10\nOUTPUT:\noffset=10 hasMore=true',
          `${R}\nconst [total,page,limit]=IN.trim().split(/\\s+/).map(Number);\nconst offset=(page-1)*limit; const hasMore=offset+limit<total;\nconsole.log("offset="+offset+" hasMore="+hasMore);`),
        cq(2,
          B('<p><b>Q4 — Static vs dynamic.</b> Read lines, each an API a route uses: <code>none</code>, <code>cookies</code>, <code>headers</code>, <code>searchParams</code>, or <code>no-store</code>. Print <code>dynamic</code> if any request-specific API is used, else <code>static</code>.</p>',
            '<p><b>Câu 4 — Tĩnh so với động.</b> Đọc các dòng, mỗi dòng là một API mà route dùng: <code>none</code>, <code>cookies</code>, <code>headers</code>, <code>searchParams</code>, hoặc <code>no-store</code>. In <code>dynamic</code> nếu dùng bất kỳ API riêng-request nào, ngược lại <code>static</code>.</p>'),
          'INPUT:\nnone\nsearchParams\nnone\nOUTPUT:\ndynamic',
          `${R}\nconst flags=IN.trim().split(/\\n/).map(s=>s.trim()).filter(Boolean);\nconst dyn=flags.some(f=>f==="cookies"||f==="headers"||f==="searchParams"||f==="no-store");\nconsole.log(dyn?"dynamic":"static");`),
        cq(2,
          B('<p><b>Q5 — Request dedup.</b> Within one render pass Next dedups identical fetches. Read URLs, one per line. Print <code>fetches=N</code> where N is the number of distinct URLs.</p>',
            '<p><b>Câu 5 — Khử trùng request.</b> Trong một lượt render, Next gộp các fetch giống nhau. Đọc các URL, mỗi dòng một URL. In <code>fetches=N</code> với N là số URL khác nhau.</p>'),
          'INPUT:\n/a\n/b\n/a\n/c\n/b\nOUTPUT:\nfetches=3',
          `${R}\nconst urls=IN.trim().split(/\\n/).map(s=>s.trim()).filter(Boolean);\nconsole.log("fetches="+new Set(urls).size);`),
      ]),

    // ── PE-08 — Server Actions & form validation (Ch 12, 16) ────────────
    PE(8, 17,
      B('Practical Exam 08 — Server Actions and form validation', 'Thi thực hành 08 — Server Actions và kiểm tra form'),
      B('Chapter 12 & 16: parse FormData, validate on the server, required-field checks, type coercion, and revalidatePath prefix logic.', 'Chương 12 & 16: parse FormData, kiểm tra trên server, kiểm trường bắt buộc, ép kiểu, và logic prefix của revalidatePath.'),
      [
        cq(2,
          B('<p><b>Q1 — Parse FormData.</b> Read lines of <code>key=value</code> (value may contain <code>=</code>). Build an object and print it as JSON. Later keys overwrite earlier ones.</p>',
            '<p><b>Câu 1 — Parse FormData.</b> Đọc các dòng <code>key=value</code> (value có thể chứa <code>=</code>). Dựng một object và in dưới dạng JSON. Key sau ghi đè key trước.</p>'),
          'INPUT:\nname=Binh\nemail=b@ct.vn\nOUTPUT:\n{"name":"Binh","email":"b@ct.vn"}',
          `${R}\nconst obj={}; for(const l of IN.trim().split(/\\n/).filter(Boolean)){ const i=l.indexOf("="); obj[l.slice(0,i)]=l.slice(i+1); }\nconsole.log(JSON.stringify(obj));`),
        cq(2,
          B('<p><b>Q2 — Server-side validation.</b> Read a JSON object with <code>username</code> and <code>password</code>. Rules: username required and at least 3 chars; password at least 8 chars. Print an <code>errors</code> object as JSON (key → message) with a key only for each failing field. <code>{}</code> if all valid.</p>',
            '<p><b>Câu 2 — Kiểm tra phía server.</b> Đọc một object JSON có <code>username</code> và <code>password</code>. Luật: username bắt buộc và ít nhất 3 ký tự; password ít nhất 8 ký tự. In một object <code>errors</code> dưới dạng JSON (key → thông báo), chỉ có key cho mỗi trường sai. <code>{}</code> nếu tất cả hợp lệ.</p>'),
          'INPUT:\n{"username":"ab","password":"secret12"}\nOUTPUT:\n{"username":"too short"}',
          `${R}\nconst v=JSON.parse(IN.trim()); const errors={};\nif(!String(v.username||"").trim()) errors.username="required";\nelse if(v.username.length<3) errors.username="too short";\nif((v.password||"").length<8) errors.password="too short";\nconsole.log(JSON.stringify(errors));`),
        cq(2,
          B('<p><b>Q3 — Required fields.</b> Read lines <code>key=value</code>. A field is missing when its value is empty after trimming. Print <code>missing:&lt;comma-separated keys&gt;</code> in input order, or <code>ok</code> if none are missing.</p>',
            '<p><b>Câu 3 — Trường bắt buộc.</b> Đọc các dòng <code>key=value</code>. Một trường thiếu khi value rỗng sau khi trim. In <code>missing:&lt;các key cách nhau bằng dấu phẩy&gt;</code> theo thứ tự nhập, hoặc <code>ok</code> nếu không thiếu gì.</p>'),
          'INPUT:\ntitle=Hello\nbody=   \nOUTPUT:\nmissing:body',
          `${R}\nconst obj={}; for(const l of IN.trim().split(/\\n/).filter(Boolean)){ const i=l.indexOf("="); obj[l.slice(0,i)]=l.slice(i+1).trim(); }\nconst errs=Object.entries(obj).filter(([,v])=>v==="").map(([k])=>k);\nconsole.log(errs.length?"missing:"+errs.join(","):"ok");`),
        cq(2,
          B('<p><b>Q4 — Type coercion.</b> FormData values are strings. Read lines <code>key=value</code>. Coerce <code>on</code> to boolean <code>true</code>, a plain integer to a number, and leave everything else a string. Print the object as JSON.</p>',
            '<p><b>Câu 4 — Ép kiểu.</b> Giá trị FormData đều là chuỗi. Đọc các dòng <code>key=value</code>. Ép <code>on</code> thành boolean <code>true</code>, một số nguyên thuần thành number, còn lại giữ nguyên chuỗi. In object dưới dạng JSON.</p>'),
          'INPUT:\nage=30\nagree=on\nname=Binh\nOUTPUT:\n{"age":30,"agree":true,"name":"Binh"}',
          `${R}\nconst obj={}; for(const l of IN.trim().split(/\\n/).filter(Boolean)){ const i=l.indexOf("="); const k=l.slice(0,i),raw=l.slice(i+1);\n let v=raw; if(raw==="on") v=true; else if(/^-?\\d+$/.test(raw)) v=Number(raw); obj[k]=v; }\nconsole.log(JSON.stringify(obj));`),
        cq(2,
          B('<p><b>Q5 — revalidatePath prefix.</b> Line 1 is the mutated path. The remaining lines are cached paths. Print, as a JSON array (in input order), the cached paths that must be invalidated: an exact match, or a path under it (starts with <code>mutated + "/"</code>).</p>',
            '<p><b>Câu 5 — Prefix của revalidatePath.</b> Dòng 1 là đường dẫn bị thay đổi. Các dòng còn lại là các đường dẫn đã cache. In ra, dưới dạng mảng JSON (theo thứ tự nhập), những đường dẫn cache phải bị vô hiệu: khớp đúng, hoặc là đường dẫn con của nó (bắt đầu bằng <code>mutated + "/"</code>).</p>'),
          'INPUT:\n/blog\n/blog\n/blog/react\n/about\n/blog/next\nOUTPUT:\n["/blog","/blog/react","/blog/next"]',
          `${R}\nconst lines=IN.trim().split(/\\n/).map(s=>s.trim()).filter(Boolean);\nconst mutated=lines[0]; const cached=lines.slice(1);\nconst invalidate=cached.filter(p=>p===mutated||p.startsWith(mutated+"/"));\nconsole.log(JSON.stringify(invalidate));`),
      ]),

    // ── PE-09 — Auth, cookies & middleware (Ch 15) ──────────────────────
    PE(9, 18,
      B('Practical Exam 09 — Auth, cookies and middleware', 'Thi thực hành 09 — Xác thực, cookie và middleware'),
      B('Chapter 15: parse a Cookie header, decide session state with a refresh window, match a middleware path, gate by role, and compute token expiry.', 'Chương 15: parse header Cookie, quyết định trạng thái phiên với cửa sổ refresh, khớp đường dẫn middleware, gác theo vai trò, và tính hạn token.'),
      [
        cq(2,
          B('<p><b>Q1 — Parse a Cookie header.</b> Read one line like <code>a=1; b=2; c=3</code>. Print an object mapping each name to its value as JSON.</p>',
            '<p><b>Câu 1 — Parse header Cookie.</b> Đọc một dòng dạng <code>a=1; b=2; c=3</code>. In một object ánh xạ mỗi tên tới giá trị của nó dưới dạng JSON.</p>'),
          'INPUT:\ntoken=abc; theme=dark; lang=vi\nOUTPUT:\n{"token":"abc","theme":"dark","lang":"vi"}',
          `${R}\nconst header=IN.trim(); const obj={};\nfor(const part of header.split(";")){ const s=part.trim(); if(!s) continue; const i=s.indexOf("="); obj[s.slice(0,i)]=s.slice(i+1); }\nconsole.log(JSON.stringify(obj));`),
        cq(2,
          B('<p><b>Q2 — Session state.</b> Read three numbers: <code>now exp window</code>. Print <code>expired</code> if <code>now &gt;= exp</code>; <code>refresh</code> if it expires within <code>window</code> from now (<code>exp - now &lt;= window</code>); else <code>valid</code>. (The cuongthai.com 24h-JWT bug.)</p>',
            '<p><b>Câu 2 — Trạng thái phiên.</b> Đọc ba số: <code>now exp window</code>. In <code>expired</code> nếu <code>now &gt;= exp</code>; <code>refresh</code> nếu sẽ hết trong <code>window</code> tính từ bây giờ (<code>exp - now &lt;= window</code>); ngược lại <code>valid</code>. (Lỗi JWT 24h của cuongthai.com.)</p>'),
          'INPUT:\n100 105 10\nOUTPUT:\nrefresh',
          `${R}\nconst [now,exp,win]=IN.trim().split(/\\s+/).map(Number);\nconsole.log(now>=exp?"expired":exp-now<=win?"refresh":"valid");`),
        cq(2,
          B('<p><b>Q3 — Middleware path matcher.</b> Line 1 is a matcher like <code>/dashboard/:path*</code>; line 2 is a pathname. Print <code>match</code> if the pathname equals the base (<code>/dashboard</code>) or is under it, else <code>no</code>.</p>',
            '<p><b>Câu 3 — Khớp đường dẫn middleware.</b> Dòng 1 là matcher dạng <code>/dashboard/:path*</code>; dòng 2 là pathname. In <code>match</code> nếu pathname bằng phần gốc (<code>/dashboard</code>) hoặc nằm dưới nó, ngược lại <code>no</code>.</p>'),
          'INPUT:\n/dashboard/:path*\n/dashboard/settings/profile\nOUTPUT:\nmatch',
          `${R}\nconst [pattern,pathname]=IN.trim().split(/\\n/).map(s=>s.trim());\nconst base=pattern.replace(/\\/:path\\*$/,"");\nconsole.log(pathname===base||pathname.startsWith(base+"/")?"match":"no");`),
        cq(2,
          B('<p><b>Q4 — Role gate.</b> Read <code>role required</code>. The hierarchy is <code>guest &lt; user &lt; editor &lt; admin</code>. Print <code>allow</code> if role rank &gt;= required rank, else <code>deny</code>.</p>',
            '<p><b>Câu 4 — Gác theo vai trò.</b> Đọc <code>role required</code>. Thứ bậc là <code>guest &lt; user &lt; editor &lt; admin</code>. In <code>allow</code> nếu hạng của role &gt;= hạng required, ngược lại <code>deny</code>.</p>'),
          'INPUT:\neditor user\nOUTPUT:\nallow',
          `${R}\nconst [role,required]=IN.trim().split(/\\s+/);\nconst rank={guest:0,user:1,editor:2,admin:3};\nconsole.log((rank[role]??-1)>=(rank[required]??99)?"allow":"deny");`),
        cq(2,
          B('<p><b>Q5 — Token expiry.</b> Read three numbers <code>iat ttl now</code> (issued-at, time-to-live, now). Compute <code>exp = iat + ttl</code> and print <code>exp=&lt;exp&gt; &lt;expired|valid&gt;</code> where expired means <code>now &gt;= exp</code>.</p>',
            '<p><b>Câu 5 — Hạn token.</b> Đọc ba số <code>iat ttl now</code> (thời điểm phát, thời gian sống, bây giờ). Tính <code>exp = iat + ttl</code> và in <code>exp=&lt;exp&gt; &lt;expired|valid&gt;</code> với expired nghĩa là <code>now &gt;= exp</code>.</p>'),
          'INPUT:\n1000 3600 5000\nOUTPUT:\nexp=4600 expired',
          `${R}\nconst [iat,ttl,now]=IN.trim().split(/\\s+/).map(Number);\nconst exp=iat+ttl;\nconsole.log("exp="+exp+" "+(now>=exp?"expired":"valid"));`),
      ]),

    // ── PE-10 — Performance & deploy (Ch 18, 20) ────────────────────────
    PE(10, 19,
      B('Practical Exam 10 — Performance and deploy', 'Thi thực hành 10 — Hiệu năng và deploy'),
      B('Chapter 18 & 20: a classnames joiner, bundle-budget check, .env parsing, NEXT_PUBLIC filtering, and a deploy smoke-test.', 'Chương 18 & 20: hàm ghép classnames, kiểm ngân sách bundle, parse .env, lọc NEXT_PUBLIC, và smoke-test khi deploy.'),
      [
        cq(2,
          B('<p><b>Q1 — Classnames joiner.</b> A <code>cn()</code> helper joins the class names whose flag is truthy. Read lines <code>name flag</code> (flag 0/1). Print the enabled class names space-separated on one line, in input order.</p>',
            '<p><b>Câu 1 — Hàm ghép classnames.</b> Một helper <code>cn()</code> nối các tên class có flag truthy. Đọc các dòng <code>name flag</code> (flag 0/1). In các tên class được bật, cách nhau bằng dấu cách trên một dòng, theo thứ tự nhập.</p>'),
          'INPUT:\nbtn 1\nactive 0\nlg 1\nrounded 1\nOUTPUT:\nbtn lg rounded',
          `${R}\nconst parts=IN.trim().split(/\\n/).filter(Boolean).filter(l=>l.trim().split(/\\s+/)[1]==="1").map(l=>l.trim().split(/\\s+/)[0]);\nconsole.log(parts.join(" "));`),
        cq(2,
          B('<p><b>Q2 — Bundle budget.</b> Read lines <code>chunk kb</code>. Sum the sizes. Print <code>total=&lt;sum&gt; &lt;over|ok&gt;</code>, where <code>over</code> means the total exceeds a 200 kB budget.</p>',
            '<p><b>Câu 2 — Ngân sách bundle.</b> Đọc các dòng <code>chunk kb</code>. Cộng các kích thước. In <code>total=&lt;tổng&gt; &lt;over|ok&gt;</code>, với <code>over</code> nghĩa là tổng vượt ngân sách 200 kB.</p>'),
          'INPUT:\nmain 120\nvendor 95\nchart 40\nOUTPUT:\ntotal=255 over',
          `${R}\nconst lines=IN.trim().split(/\\n/).filter(Boolean);\nlet total=0; for(const l of lines){ total+=Number(l.trim().split(/\\s+/)[1]); }\nconsole.log("total="+total+" "+(total>200?"over":"ok"));`),
        cq(2,
          B('<p><b>Q3 — Parse .env.</b> Read <code>.env</code> lines. Skip blank lines and lines starting with <code>#</code>. For <code>KEY=value</code>, build an object. Print it as JSON.</p>',
            '<p><b>Câu 3 — Parse .env.</b> Đọc các dòng <code>.env</code>. Bỏ qua dòng trống và dòng bắt đầu bằng <code>#</code>. Với <code>KEY=value</code>, dựng một object. In dưới dạng JSON.</p>'),
          'INPUT:\n# db\nDATABASE_URL=postgres://x\n\nPORT=3000\nOUTPUT:\n{"DATABASE_URL":"postgres://x","PORT":"3000"}',
          `${R}\nconst obj={};\nfor(const l of IN.split(/\\n/)){ const s=l.trim(); if(!s||s.startsWith("#")) continue; const i=s.indexOf("="); if(i<0) continue; obj[s.slice(0,i)]=s.slice(i+1); }\nconsole.log(JSON.stringify(obj));`),
        cq(2,
          B('<p><b>Q4 — NEXT_PUBLIC filter.</b> Only <code>NEXT_PUBLIC_*</code> vars are safe to expose to the browser. Read <code>KEY=value</code> lines and print, space-separated, only the keys starting with <code>NEXT_PUBLIC_</code>, in input order.</p>',
            '<p><b>Câu 4 — Lọc NEXT_PUBLIC.</b> Chỉ biến <code>NEXT_PUBLIC_*</code> mới an toàn để lộ ra trình duyệt. Đọc các dòng <code>KEY=value</code> và in ra, cách nhau bằng dấu cách, chỉ những key bắt đầu bằng <code>NEXT_PUBLIC_</code>, theo thứ tự nhập.</p>'),
          'INPUT:\nDATABASE_URL=x\nNEXT_PUBLIC_API=y\nJWT_SECRET=z\nNEXT_PUBLIC_CDN=w\nOUTPUT:\nNEXT_PUBLIC_API NEXT_PUBLIC_CDN',
          `${R}\nconst keys=[];\nfor(const l of IN.split(/\\n/)){ const s=l.trim(); if(!s||s.startsWith("#")) continue; const k=s.split("=")[0]; if(k.startsWith("NEXT_PUBLIC_")) keys.push(k); }\nconsole.log(keys.join(" "));`),
        cq(2,
          B('<p><b>Q5 — Deploy smoke-test.</b> Read lines <code>route code</code> (HTTP status). A <code>404</code> means the route is not mounted (a stale/partial build). Print <code>FAIL &lt;route&gt;</code> for the FIRST 404, or <code>OK</code> if none.</p>',
            '<p><b>Câu 5 — Smoke-test khi deploy.</b> Đọc các dòng <code>route code</code> (mã HTTP). Một <code>404</code> nghĩa là route chưa được mount (build cũ/dở). In <code>FAIL &lt;route&gt;</code> cho lần 404 ĐẦU TIÊN, hoặc <code>OK</code> nếu không có.</p>'),
          'INPUT:\n/api/v1/gifs 401\n/api/v1/posts 200\n/api/v1/new 404\nOUTPUT:\nFAIL /api/v1/new',
          `${R}\nconst lines=IN.trim().split(/\\n/).map(s=>s.trim()).filter(Boolean);\nlet bad=null; for(const l of lines){ const [route,code]=l.split(/\\s+/); if(code==="404"){ bad=route; break; } }\nconsole.log(bad?"FAIL "+bad:"OK");`),
      ]),
  ],
};
