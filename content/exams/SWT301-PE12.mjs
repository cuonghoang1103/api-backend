export default {
  "course": {
    "courseCode": "SWT301"
  },
  "exams": [
    {
      "kind": "PE",
      "peType": "WRITE",
      "code": "PE12-PESAMPLEFA20",
      "title": "SWT301 — Practical Exam Đề 12 (PE - SAMPLE - FA 2024)|||SWT301 — Đề thi thực hành số 12 (PE - SAMPLE - FA 2024)",
      "description": "Software Testing PE (WRITE): code review + test design, AI-graded.|||Đề PE Kiểm thử (viết): review code + thiết kế test, chấm AI.",
      "durationMinutes": 90,
      "totalPoints": 10,
      "passMark": 5,
      "source": "FUOverflow",
      "instructions": "<div class=\"ml-en\"><p><b>How to take this practical exam.</b></p><ol><li>This is a real FPTU <b>Practical Exam</b> for <b>Software Testing</b>. Read the system/context, then answer each question <b>in writing</b> (English) in the answer box.</li><li>For diagrams (context / use-case / ERD), describe them clearly in text or list form — the model answer shows the reference diagram.</li><li>When you submit, AI grades each answer against a <b>rubric</b> and a reference solution, then shows a bilingual model answer.</li></ol></div><div class=\"ml-vi\"><p><b>Cách làm bài thi thực hành.</b></p><ol><li>Đây là đề <b>thi thực hành (PE)</b> thật của FPTU môn <b>Software Testing</b>. Đọc hệ thống/bối cảnh, rồi trả lời mỗi câu <b>bằng chữ</b> (tiếng Anh) trong ô trả lời.</li><li>Với sơ đồ (context / use-case / ERD), mô tả rõ bằng chữ hoặc liệt kê — đáp án mẫu có sơ đồ tham chiếu.</li><li>Khi nộp, AI chấm từng câu theo <b>rubric</b> và một đáp án mẫu, rồi hiện đáp án mẫu song ngữ.</li></ol></div>",
      "isPublished": true,
      "questions": [
        {
          "kind": "WRITE",
          "points": 3,
          "prompt": "<div class=\"ml-en\"><div class=\"pe-system\"><b>System / Context:</b><br/><p>You are an expert examiner and grader for <b>SWT301 - Software Testing (ISTQB Foundation aligned)</b>. This is the Practical Exam (PE), SAMPLE, FA 2024. The paper has 3 questions worth 10 points total: (1) static code review to identify defects using a defect-report template, (2) white-box/unit testing with Equivalence Partitioning (EP) + Boundary Value Analysis (BVA) and branch coverage in JUnit, and (3) functional black-box, use-case-based test case design. Grade strictly against the reference answers and rubric. Award marks for correctly located defects, correct EP/BVA partitions and boundaries, correct expected values, achievement of 100% branch coverage, and complete use-case-based test cases (normal, alternative, exception flows). Accept technically equivalent answers.</p></div></div><div class=\"ml-vi\"><div class=\"pe-system\"><b>Hệ thống / Bối cảnh:</b><br/><p>Bạn là giám khảo và người chấm chuyên môn cho <b>SWT301 - Kiểm thử phần mềm (theo ISTQB Foundation)</b>. Đây là bài thi thực hành (PE), đề SAMPLE, FA 2024. Đề gồm 3 câu, tổng 10 điểm: (1) review code tĩnh để tìm lỗi theo mẫu báo cáo lỗi, (2) kiểm thử đơn vị hộp trắng với Phân vùng tương đương (EP) + Phân tích giá trị biên (BVA) và bao phủ nhánh bằng JUnit, và (3) thiết kế test case hộp đen theo use case. Chấm chặt theo đáp án mẫu và rubric. Cho điểm khi định vị đúng lỗi, chia đúng phân vùng/biên EP-BVA, tính đúng giá trị kỳ vọng, đạt 100% bao phủ nhánh, và test case theo use case đầy đủ (luồng chính, luồng thay thế, luồng ngoại lệ). Chấp nhận các đáp án tương đương về mặt kỹ thuật.</p></div></div><div class=\"ml-en\"><p><b>Question 1 (3 points)</b></p><p>The class below contains <b>six defects</b> relating to code standards, logic, and best practices. Identify and report each defect using the provided template. <i>(Use question 1 template)</i></p><p><b>Template fields:</b></p><ul><li><b>Defect ID:</b> Number assigned to each defect (e.g., Defect 1).</li><li><b>Defect Name:</b> Brief title of the issue (e.g., Unreachable Code).</li><li><b>Line of Code:</b> Exact line(s) where the defect is located.</li><li><b>Defect Description:</b> What's wrong and why it's a problem.</li><li><b>Fixing Solution:</b> Suggested fix for the issue.</li></ul><pre><code>public class DataProcessor {\n    private int dataCount = 0;\n    private boolean isProcessing;\n    private BufferedWriter writer;\n\n    public void InitializeProcess() {\n        isProcessing = true;\n        if (dataCount == 0 || isProcessing == false) {\n            System.out.println(\"No data to process.\");\n        }\n    }\n\n    public void writeData(String data) {\n        if (data != null &amp;&amp; data.isEmpty()) {\n            return;\n        }\n        try {\n            writer.write(data);\n        } catch (IOException e) {\n            System.out.println(\"Error writing data: \" + e.getMessage());\n        }\n    }\n\n    public void closeWriter() {\n        writer.close();\n    }\n\n    public void processAllData() {\n        InitializeProcess();\n        if (dataCount &gt; 100) {\n            System.out.println(\"Processing too much data.\");\n            return;\n        }\n    }\n\n    public void shutdown() {\n        writer = null;\n    }\n}</code></pre></div><div class=\"ml-vi\"><p><b>Câu 1 (3 điểm)</b></p><p>Lớp dưới đây chứa <b>sáu lỗi</b> liên quan đến chuẩn code, logic và best practice. Hãy xác định và báo cáo từng lỗi theo mẫu đã cho. <i>(Dùng mẫu câu 1)</i></p><p><b>Các trường của mẫu:</b></p><ul><li><b>Defect ID (Mã lỗi):</b> Số thứ tự gán cho mỗi lỗi (ví dụ: Defect 1).</li><li><b>Defect Name (Tên lỗi):</b> Tiêu đề ngắn của vấn đề (ví dụ: Unreachable Code - mã không thể chạy tới).</li><li><b>Line of Code (Dòng code):</b> Dòng chính xác nơi lỗi xuất hiện.</li><li><b>Defect Description (Mô tả lỗi):</b> Sai chỗ nào và vì sao đó là vấn đề.</li><li><b>Fixing Solution (Cách sửa):</b> Đề xuất cách khắc phục.</li></ul><pre><code>public class DataProcessor {\n    private int dataCount = 0;\n    private boolean isProcessing;\n    private BufferedWriter writer;\n\n    public void InitializeProcess() {\n        isProcessing = true;\n        if (dataCount == 0 || isProcessing == false) {\n            System.out.println(\"No data to process.\");\n        }\n    }\n\n    public void writeData(String data) {\n        if (data != null &amp;&amp; data.isEmpty()) {\n            return;\n        }\n        try {\n            writer.write(data);\n        } catch (IOException e) {\n            System.out.println(\"Error writing data: \" + e.getMessage());\n        }\n    }\n\n    public void closeWriter() {\n        writer.close();\n    }\n\n    public void processAllData() {\n        InitializeProcess();\n        if (dataCount &gt; 100) {\n            System.out.println(\"Processing too much data.\");\n            return;\n        }\n    }\n\n    public void shutdown() {\n        writer = null;\n    }\n}</code></pre></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><b>Reference answer - six defects:</b></p><table><tr><td><b>#</b></td><td><b>Defect Name</b></td><td><b>Line(s)</b></td><td><b>Description (why it is a problem)</b></td><td><b>Fixing Solution</b></td></tr><tr><td>Defect 1</td><td>Method naming convention violation</td><td><code>public void InitializeProcess()</code></td><td>Java method names must be camelCase starting with a lowercase letter. <code>InitializeProcess</code> starts with an uppercase letter, violating the coding standard and making it look like a constructor/class.</td><td>Rename to <code>initializeProcess()</code> (and update the call inside <code>processAllData()</code>).</td></tr><tr><td>Defect 2</td><td>Contradictory / dead condition (logic)</td><td><code>isProcessing = true;</code> then <code>if (dataCount == 0 || isProcessing == false)</code></td><td><code>isProcessing</code> is set to <code>true</code> immediately before it is tested against <code>false</code>, so <code>isProcessing == false</code> can never be true - it is dead sub-condition and the guard is contradictory. Also, comparing a boolean to <code>false</code> is bad practice.</td><td>Remove the premature <code>isProcessing = true;</code>, or restructure the guard, and use <code>!isProcessing</code> instead of <code>isProcessing == false</code>.</td></tr><tr><td>Defect 3</td><td>Incorrect null guard (logic / NPE risk)</td><td><code>if (data != null &amp;&amp; data.isEmpty())</code></td><td>The intent is to skip null or empty input, but this condition is true only when data is NOT null AND empty. When <code>data == null</code> the condition is false, so execution falls through to <code>writer.write(data)</code> writing a null and risking behaviour errors; the null case is not guarded.</td><td>Change to <code>if (data == null || data.isEmpty()) { return; }</code>.</td></tr><tr><td>Defect 4</td><td>Uninitialized field / NullPointerException</td><td><code>private BufferedWriter writer;</code> used in <code>writer.write(data)</code> and <code>writer.close()</code></td><td><code>writer</code> is declared but never instantiated (no <code>new BufferedWriter(...)</code>), so every call to <code>writer.write()</code>/<code>writer.close()</code> throws a NullPointerException.</td><td>Initialize the writer before use, e.g. in a constructor/init method: <code>writer = new BufferedWriter(new FileWriter(\"out.txt\"));</code> and null-check before use.</td></tr><tr><td>Defect 5</td><td>Unhandled checked exception / no null check on close (best practice)</td><td><code>public void closeWriter() { writer.close(); }</code></td><td><code>BufferedWriter.close()</code> throws the checked <code>IOException</code>, which is neither caught nor declared with <code>throws</code> - this does not compile. There is also no null check and no try/finally, so the resource may leak.</td><td>Wrap in try/catch or declare <code>throws IOException</code>, guard for null, and prefer try-with-resources: <code>if (writer != null) { try { writer.close(); } catch (IOException e) { ... } }</code>.</td></tr><tr><td>Defect 6</td><td>Unreachable / dead code - counter never updated (logic)</td><td><code>private int dataCount = 0;</code> and <code>if (dataCount &gt; 100)</code></td><td><code>dataCount</code> is initialized to 0 and never incremented anywhere in the class, so <code>dataCount &gt; 100</code> is always false and the block is unreachable (dead code). <code>processAllData()</code> also never actually writes/processes any data.</td><td>Increment <code>dataCount</code> when data is added/written, or remove the dead condition; implement real processing (e.g., loop calling <code>writeData</code>).</td></tr></table><p><b>Note:</b> other defensible defects (e.g., <code>InitializeProcess</code> prints a message but does not stop processing; <code>isProcessing</code> never reset in <code>shutdown()</code>) may substitute for any of the above if clearly justified. Full marks require six distinct, correctly located defects with a valid fix each.</p></div><div class=\"ml-vi\"><p><b>Đáp án mẫu - sáu lỗi:</b></p><table><tr><td><b>#</b></td><td><b>Tên lỗi</b></td><td><b>Dòng</b></td><td><b>Mô tả (vì sao là vấn đề)</b></td><td><b>Cách sửa</b></td></tr><tr><td>Defect 1</td><td>Vi phạm quy ước đặt tên phương thức</td><td><code>public void InitializeProcess()</code></td><td>Tên phương thức trong Java phải viết camelCase, bắt đầu bằng chữ thường. <code>InitializeProcess</code> bắt đầu bằng chữ hoa, vi phạm chuẩn code và dễ nhầm với tên lớp/hàm dựng.</td><td>Đổi tên thành <code>initializeProcess()</code> (và sửa lời gọi trong <code>processAllData()</code>).</td></tr><tr><td>Defect 2</td><td>Điều kiện mâu thuẫn / mã chết (logic)</td><td><code>isProcessing = true;</code> rồi <code>if (dataCount == 0 || isProcessing == false)</code></td><td><code>isProcessing</code> vừa được gán <code>true</code> ngay trước khi so với <code>false</code>, nên <code>isProcessing == false</code> không bao giờ đúng - đây là điều kiện con chết và guard tự mâu thuẫn. So sánh boolean với <code>false</code> cũng là thói quen xấu.</td><td>Bỏ dòng <code>isProcessing = true;</code> đặt sai chỗ hoặc bố trí lại guard, và dùng <code>!isProcessing</code> thay cho <code>isProcessing == false</code>.</td></tr><tr><td>Defect 3</td><td>Guard null sai (logic / nguy cơ NPE)</td><td><code>if (data != null &amp;&amp; data.isEmpty())</code></td><td>Ý định là bỏ qua input null hoặc rỗng, nhưng điều kiện chỉ đúng khi data KHÁC null VÀ rỗng. Khi <code>data == null</code> điều kiện sai, chương trình chạy tiếp tới <code>writer.write(data)</code> ghi null - trường hợp null không được chặn.</td><td>Sửa thành <code>if (data == null || data.isEmpty()) { return; }</code>.</td></tr><tr><td>Defect 4</td><td>Trường chưa khởi tạo / NullPointerException</td><td><code>private BufferedWriter writer;</code> dùng ở <code>writer.write(data)</code> và <code>writer.close()</code></td><td><code>writer</code> được khai báo nhưng không bao giờ được khởi tạo (không có <code>new BufferedWriter(...)</code>), nên mọi lời gọi <code>writer.write()</code>/<code>writer.close()</code> đều ném NullPointerException.</td><td>Khởi tạo writer trước khi dùng, ví dụ trong hàm dựng/khởi tạo: <code>writer = new BufferedWriter(new FileWriter(\"out.txt\"));</code> và kiểm null trước khi dùng.</td></tr><tr><td>Defect 5</td><td>Không xử lý checked exception / không kiểm null khi close (best practice)</td><td><code>public void closeWriter() { writer.close(); }</code></td><td><code>BufferedWriter.close()</code> ném checked exception <code>IOException</code>, không được bắt cũng không khai báo <code>throws</code> - đoạn này không biên dịch được. Ngoài ra thiếu kiểm null và không có try/finally nên có thể rò rỉ tài nguyên.</td><td>Bọc try/catch hoặc khai báo <code>throws IOException</code>, kiểm null, ưu tiên try-with-resources: <code>if (writer != null) { try { writer.close(); } catch (IOException e) { ... } }</code>.</td></tr><tr><td>Defect 6</td><td>Mã không thể chạy tới / bộ đếm không bao giờ cập nhật (logic)</td><td><code>private int dataCount = 0;</code> và <code>if (dataCount &gt; 100)</code></td><td><code>dataCount</code> khởi tạo bằng 0 và không bao giờ được tăng ở bất kỳ đâu, nên <code>dataCount &gt; 100</code> luôn sai và khối lệnh là mã chết (unreachable). <code>processAllData()</code> cũng thực sự không ghi/xử lý dữ liệu nào.</td><td>Tăng <code>dataCount</code> khi thêm/ghi dữ liệu, hoặc bỏ điều kiện chết; hiện thực xử lý thật (ví dụ vòng lặp gọi <code>writeData</code>).</td></tr></table><p><b>Ghi chú:</b> một số lỗi khác cũng chấp nhận được (ví dụ: <code>InitializeProcess</code> in thông báo nhưng không dừng xử lý; <code>isProcessing</code> không được reset trong <code>shutdown()</code>) có thể thay cho bất kỳ lỗi nào ở trên nếu lập luận rõ ràng. Đạt điểm tối đa cần đủ sáu lỗi riêng biệt, định vị đúng và mỗi lỗi có cách sửa hợp lệ.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Correctly locates and names all six distinct defects at the right line(s).|||Định vị và đặt tên đúng cả sáu lỗi riêng biệt tại đúng dòng.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c2",
              "criterion": "Provides a correct description (why it is wrong) and a valid fixing solution for each defect.|||Mô tả đúng (sai vì sao) và đưa cách sửa hợp lệ cho từng lỗi.",
              "weight": 1,
              "maxScore": 1.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/SWT301/PE12.png"
        },
        {
          "kind": "WRITE",
          "points": 3,
          "prompt": "<div class=\"ml-en\"><p><b>Question 2 (3 points)</b></p><p>The method below computes an employee's salary, applying bonuses and deductions based on years of service, basic salary, and performance rating. Your task is to design test cases using <b>JUnit</b> to validate this method. Use <b>equivalence partitioning</b> and <b>boundary value analysis</b> for the input parameters, and ensure that your test cases cover all <b>four branches</b> of the code for 100% code coverage. <i>(Use question 2 template)</i></p><pre><code>public class EmployeeSalary {\n    public double calculateSalary(int yearsOfService, double basicSalary, double performanceRating) {\n        if (yearsOfService &lt; 0 || basicSalary &lt;= 0 || performanceRating &lt; 0 || performanceRating &gt; 5) {\n            throw new IllegalArgumentException(\"Invalid input values\");\n        }\n\n        double bonus = 0.0;\n        double deduction = 0.0;\n\n        if (yearsOfService &gt; 10) {\n            bonus = 0.10 * basicSalary;\n        } else if (yearsOfService &gt;= 5) {\n            bonus = 0.05 * basicSalary;\n        }\n\n        if (performanceRating &lt; 3) {\n            deduction = 0.05 * basicSalary;\n        }\n\n        return basicSalary + bonus - deduction;\n    }\n}</code></pre><p><b>Sample JUnit Test Class for Completion.</b> Below is a partially written JUnit test class for the <code>EmployeeSalary</code> method. Complete this class by adding the required test cases to achieve full <b>equivalence partitioning</b>, <b>boundary value analysis</b>, and <b>branch coverage</b> for the <code>calculateSalary</code> method.</p><pre><code>import static org.junit.Assert.*;\nimport org.junit.Test;\n\npublic class EmployeeSalaryTest {\n    @Test(expected = IllegalArgumentException.class)\n    public void testInvalidYearsOfService() {\n        EmployeeSalary salaryCalculator = new EmployeeSalary();\n        salaryCalculator.calculateSalary(-1, 50000, 4);  // Invalid years of service\n    }\n\n    @Test\n    public void testYearsOfServiceGreaterThanTen() {\n        EmployeeSalary salaryCalculator = new EmployeeSalary();\n        double result = salaryCalculator.calculateSalary(11, 50000, 4);  // Branch 1\n        assertEquals(55000.0, result, 0.01);  // 10% bonus\n    }\n\n    // Need add more test code...\n}</code></pre></div><div class=\"ml-vi\"><p><b>Câu 2 (3 điểm)</b></p><p>Phương thức dưới đây tính lương của nhân viên, áp dụng thưởng và khấu trừ dựa trên số năm công tác, lương cơ bản và điểm hiệu suất. Nhiệm vụ của bạn là thiết kế test case bằng <b>JUnit</b> để kiểm chứng phương thức này. Dùng <b>phân vùng tương đương (equivalence partitioning)</b> và <b>phân tích giá trị biên (boundary value analysis)</b> cho các tham số đầu vào, và đảm bảo test case bao phủ đủ cả <b>bốn nhánh</b> của code để đạt 100% độ bao phủ. <i>(Dùng mẫu câu 2)</i></p><pre><code>public class EmployeeSalary {\n    public double calculateSalary(int yearsOfService, double basicSalary, double performanceRating) {\n        if (yearsOfService &lt; 0 || basicSalary &lt;= 0 || performanceRating &lt; 0 || performanceRating &gt; 5) {\n            throw new IllegalArgumentException(\"Invalid input values\");\n        }\n\n        double bonus = 0.0;\n        double deduction = 0.0;\n\n        if (yearsOfService &gt; 10) {\n            bonus = 0.10 * basicSalary;\n        } else if (yearsOfService &gt;= 5) {\n            bonus = 0.05 * basicSalary;\n        }\n\n        if (performanceRating &lt; 3) {\n            deduction = 0.05 * basicSalary;\n        }\n\n        return basicSalary + bonus - deduction;\n    }\n}</code></pre><p><b>Lớp test JUnit mẫu cần hoàn thiện.</b> Dưới đây là lớp test JUnit viết dở cho phương thức <code>EmployeeSalary</code>. Hãy hoàn thiện lớp này bằng cách thêm các test case cần thiết để đạt đủ <b>phân vùng tương đương</b>, <b>phân tích giá trị biên</b> và <b>bao phủ nhánh</b> cho phương thức <code>calculateSalary</code>.</p><pre><code>import static org.junit.Assert.*;\nimport org.junit.Test;\n\npublic class EmployeeSalaryTest {\n    @Test(expected = IllegalArgumentException.class)\n    public void testInvalidYearsOfService() {\n        EmployeeSalary salaryCalculator = new EmployeeSalary();\n        salaryCalculator.calculateSalary(-1, 50000, 4);  // Invalid years of service\n    }\n\n    @Test\n    public void testYearsOfServiceGreaterThanTen() {\n        EmployeeSalary salaryCalculator = new EmployeeSalary();\n        double result = salaryCalculator.calculateSalary(11, 50000, 4);  // Branch 1\n        assertEquals(55000.0, result, 0.01);  // 10% bonus\n    }\n\n    // Need add more test code...\n}</code></pre></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><b>1) Equivalence Partitioning (EP)</b></p><table><tr><td><b>Parameter</b></td><td><b>Invalid partitions</b></td><td><b>Valid partitions (by behaviour)</b></td></tr><tr><td>yearsOfService (int)</td><td>&lt; 0</td><td>[0..4] no bonus; [5..10] 5% bonus; [11..&#8734;) 10% bonus</td></tr><tr><td>basicSalary (double)</td><td>&lt;= 0</td><td>&gt; 0</td></tr><tr><td>performanceRating (double)</td><td>&lt; 0 ; &gt; 5</td><td>[0..3) deduction applies; [3..5] no deduction</td></tr></table><p><b>2) Boundary Value Analysis (BVA)</b></p><ul><li>yearsOfService: -1 (invalid) | 0 (min valid) | 4 / 5 (5% bonus threshold) | 10 / 11 (10% bonus threshold)</li><li>basicSalary: 0 (invalid) | 0.01 or 1 (just valid)</li><li>performanceRating: -1 (invalid low) | 0 (min valid) | 2.99 / 3 (deduction threshold) | 5 (max valid) | 5.01 / 6 (invalid high)</li></ul><p><b>3) Four branches to cover (100% branch coverage)</b></p><ol><li>Validation <code>if</code> true -&gt; throws IllegalArgumentException.</li><li><code>yearsOfService &gt; 10</code> true -&gt; 10% bonus.</li><li><code>else if yearsOfService &gt;= 5</code> true -&gt; 5% bonus.</li><li><code>performanceRating &lt; 3</code> true -&gt; 5% deduction (plus the implicit false paths / no-bonus else).</li></ol><p><b>4) Test case table (with hand-computed expected values)</b></p><table><tr><td><b>TC</b></td><td><b>years</b></td><td><b>salary</b></td><td><b>rating</b></td><td><b>Technique</b></td><td><b>Expected</b></td></tr><tr><td>T1</td><td>-1</td><td>50000</td><td>4</td><td>Invalid EP / BVA years</td><td>IllegalArgumentException</td></tr><tr><td>T2</td><td>5</td><td>0</td><td>4</td><td>Invalid EP salary (&lt;=0)</td><td>IllegalArgumentException</td></tr><tr><td>T3</td><td>5</td><td>50000</td><td>-1</td><td>Invalid EP rating low</td><td>IllegalArgumentException</td></tr><tr><td>T4</td><td>5</td><td>50000</td><td>6</td><td>Invalid EP/BVA rating high</td><td>IllegalArgumentException</td></tr><tr><td>T5</td><td>11</td><td>50000</td><td>4</td><td>Branch 2 (&gt;10) + BVA 11</td><td>55000.0 (bonus 5000)</td></tr><tr><td>T6</td><td>5</td><td>50000</td><td>4</td><td>Branch 3 (&gt;=5) + BVA 5</td><td>52500.0 (bonus 2500)</td></tr><tr><td>T7</td><td>10</td><td>50000</td><td>4</td><td>BVA 10 (still 5%)</td><td>52500.0</td></tr><tr><td>T8</td><td>4</td><td>50000</td><td>4</td><td>No-bonus else + BVA 4</td><td>50000.0</td></tr><tr><td>T9</td><td>4</td><td>50000</td><td>2</td><td>Branch 4 (rating&lt;3) deduction</td><td>47500.0 (deduction 2500)</td></tr><tr><td>T10</td><td>4</td><td>50000</td><td>3</td><td>BVA 3 (no deduction)</td><td>50000.0</td></tr></table><p><b>5) Minimum number of test cases for 100% branch coverage = 4, and why:</b> the validation branch needs 1 case (it throws and exits early, touching nothing else); the <code>if / else if</code> on years has three outcomes (&gt;10, [5..10], &lt;5) needing 3 separate cases; and the <code>performanceRating &lt; 3</code> if (true and false) can be folded into those 3 year-cases by choosing the ratings. So 1 (throw) + 3 (bonus paths, with the deduction true/false folded in) = <b>4</b>. Fuller EP+BVA coverage uses ~10 cases as above.</p><p><b>6) Completed JUnit test methods (added after the two given ones):</b></p><pre><code>@Test(expected = IllegalArgumentException.class)\npublic void testInvalidBasicSalary() {\n    EmployeeSalary c = new EmployeeSalary();\n    c.calculateSalary(5, 0, 4);              // salary &lt;= 0\n}\n\n@Test(expected = IllegalArgumentException.class)\npublic void testInvalidRatingLow() {\n    EmployeeSalary c = new EmployeeSalary();\n    c.calculateSalary(5, 50000, -1);         // rating &lt; 0\n}\n\n@Test(expected = IllegalArgumentException.class)\npublic void testInvalidRatingHigh() {\n    EmployeeSalary c = new EmployeeSalary();\n    c.calculateSalary(5, 50000, 6);          // rating &gt; 5\n}\n\n@Test\npublic void testYearsBetweenFiveAndTen() {   // Branch 3: 5% bonus\n    EmployeeSalary c = new EmployeeSalary();\n    assertEquals(52500.0, c.calculateSalary(5, 50000, 4), 0.01);\n}\n\n@Test\npublic void testBoundaryYearsTen() {         // BVA 10 -&gt; still 5%\n    EmployeeSalary c = new EmployeeSalary();\n    assertEquals(52500.0, c.calculateSalary(10, 50000, 4), 0.01);\n}\n\n@Test\npublic void testYearsLessThanFiveNoBonus() { // else path: no bonus\n    EmployeeSalary c = new EmployeeSalary();\n    assertEquals(50000.0, c.calculateSalary(4, 50000, 4), 0.01);\n}\n\n@Test\npublic void testLowPerformanceDeduction() {  // Branch 4: rating &lt; 3\n    EmployeeSalary c = new EmployeeSalary();\n    assertEquals(47500.0, c.calculateSalary(4, 50000, 2), 0.01);\n}\n\n@Test\npublic void testBoundaryRatingThree() {      // BVA 3 -&gt; no deduction\n    EmployeeSalary c = new EmployeeSalary();\n    assertEquals(50000.0, c.calculateSalary(4, 50000, 3), 0.01);\n}</code></pre></div><div class=\"ml-vi\"><p><b>1) Phân vùng tương đương (EP)</b></p><table><tr><td><b>Tham số</b></td><td><b>Phân vùng không hợp lệ</b></td><td><b>Phân vùng hợp lệ (theo hành vi)</b></td></tr><tr><td>yearsOfService (int)</td><td>&lt; 0</td><td>[0..4] không thưởng; [5..10] thưởng 5%; [11..&#8734;) thưởng 10%</td></tr><tr><td>basicSalary (double)</td><td>&lt;= 0</td><td>&gt; 0</td></tr><tr><td>performanceRating (double)</td><td>&lt; 0 ; &gt; 5</td><td>[0..3) có khấu trừ; [3..5] không khấu trừ</td></tr></table><p><b>2) Phân tích giá trị biên (BVA)</b></p><ul><li>yearsOfService: -1 (không hợp lệ) | 0 (min hợp lệ) | 4 / 5 (ngưỡng thưởng 5%) | 10 / 11 (ngưỡng thưởng 10%)</li><li>basicSalary: 0 (không hợp lệ) | 0.01 hoặc 1 (vừa hợp lệ)</li><li>performanceRating: -1 (không hợp lệ thấp) | 0 (min hợp lệ) | 2.99 / 3 (ngưỡng khấu trừ) | 5 (max hợp lệ) | 5.01 / 6 (không hợp lệ cao)</li></ul><p><b>3) Bốn nhánh cần bao phủ (100% branch coverage)</b></p><ol><li>Guard kiểm tra <code>if</code> đúng -&gt; ném IllegalArgumentException.</li><li><code>yearsOfService &gt; 10</code> đúng -&gt; thưởng 10%.</li><li><code>else if yearsOfService &gt;= 5</code> đúng -&gt; thưởng 5%.</li><li><code>performanceRating &lt; 3</code> đúng -&gt; khấu trừ 5% (kèm các nhánh false ngầm / else không thưởng).</li></ol><p><b>4) Bảng test case (giá trị kỳ vọng tính tay)</b></p><table><tr><td><b>TC</b></td><td><b>năm</b></td><td><b>lương</b></td><td><b>điểm</b></td><td><b>Kỹ thuật</b></td><td><b>Kỳ vọng</b></td></tr><tr><td>T1</td><td>-1</td><td>50000</td><td>4</td><td>EP/BVA năm không hợp lệ</td><td>IllegalArgumentException</td></tr><tr><td>T2</td><td>5</td><td>0</td><td>4</td><td>EP lương không hợp lệ (&lt;=0)</td><td>IllegalArgumentException</td></tr><tr><td>T3</td><td>5</td><td>50000</td><td>-1</td><td>EP điểm không hợp lệ thấp</td><td>IllegalArgumentException</td></tr><tr><td>T4</td><td>5</td><td>50000</td><td>6</td><td>EP/BVA điểm không hợp lệ cao</td><td>IllegalArgumentException</td></tr><tr><td>T5</td><td>11</td><td>50000</td><td>4</td><td>Nhánh 2 (&gt;10) + BVA 11</td><td>55000.0 (thưởng 5000)</td></tr><tr><td>T6</td><td>5</td><td>50000</td><td>4</td><td>Nhánh 3 (&gt;=5) + BVA 5</td><td>52500.0 (thưởng 2500)</td></tr><tr><td>T7</td><td>10</td><td>50000</td><td>4</td><td>BVA 10 (vẫn 5%)</td><td>52500.0</td></tr><tr><td>T8</td><td>4</td><td>50000</td><td>4</td><td>Else không thưởng + BVA 4</td><td>50000.0</td></tr><tr><td>T9</td><td>4</td><td>50000</td><td>2</td><td>Nhánh 4 (điểm&lt;3) khấu trừ</td><td>47500.0 (khấu trừ 2500)</td></tr><tr><td>T10</td><td>4</td><td>50000</td><td>3</td><td>BVA 3 (không khấu trừ)</td><td>50000.0</td></tr></table><p><b>5) Số test case tối thiểu để đạt 100% bao phủ nhánh = 4, và vì sao:</b> nhánh guard cần 1 ca (nó ném lỗi và thoát sớm, không chạm tới gì khác); khối <code>if / else if</code> theo năm có ba kết quả (&gt;10, [5..10], &lt;5) cần 3 ca riêng; và <code>if performanceRating &lt; 3</code> (true và false) có thể gộp vào 3 ca theo năm bằng cách chọn điểm hiệu suất. Vậy 1 (ném lỗi) + 3 (các nhánh thưởng, đã lồng khấu trừ true/false) = <b>4</b>. Muốn phủ EP+BVA đầy đủ thì dùng ~10 ca như bảng trên.</p><p><b>6) Các phương thức JUnit đã hoàn thiện (thêm sau hai phương thức có sẵn):</b></p><pre><code>@Test(expected = IllegalArgumentException.class)\npublic void testInvalidBasicSalary() {\n    EmployeeSalary c = new EmployeeSalary();\n    c.calculateSalary(5, 0, 4);              // luong &lt;= 0\n}\n\n@Test(expected = IllegalArgumentException.class)\npublic void testInvalidRatingLow() {\n    EmployeeSalary c = new EmployeeSalary();\n    c.calculateSalary(5, 50000, -1);         // diem &lt; 0\n}\n\n@Test(expected = IllegalArgumentException.class)\npublic void testInvalidRatingHigh() {\n    EmployeeSalary c = new EmployeeSalary();\n    c.calculateSalary(5, 50000, 6);          // diem &gt; 5\n}\n\n@Test\npublic void testYearsBetweenFiveAndTen() {   // Nhanh 3: thuong 5%\n    EmployeeSalary c = new EmployeeSalary();\n    assertEquals(52500.0, c.calculateSalary(5, 50000, 4), 0.01);\n}\n\n@Test\npublic void testBoundaryYearsTen() {         // BVA 10 -&gt; van 5%\n    EmployeeSalary c = new EmployeeSalary();\n    assertEquals(52500.0, c.calculateSalary(10, 50000, 4), 0.01);\n}\n\n@Test\npublic void testYearsLessThanFiveNoBonus() { // else: khong thuong\n    EmployeeSalary c = new EmployeeSalary();\n    assertEquals(50000.0, c.calculateSalary(4, 50000, 4), 0.01);\n}\n\n@Test\npublic void testLowPerformanceDeduction() {  // Nhanh 4: diem &lt; 3\n    EmployeeSalary c = new EmployeeSalary();\n    assertEquals(47500.0, c.calculateSalary(4, 50000, 2), 0.01);\n}\n\n@Test\npublic void testBoundaryRatingThree() {      // BVA 3 -&gt; khong khau tru\n    EmployeeSalary c = new EmployeeSalary();\n    assertEquals(50000.0, c.calculateSalary(4, 50000, 3), 0.01);\n}</code></pre></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Correct Equivalence Partitioning and Boundary Value Analysis for all three inputs (valid/invalid partitions and boundary values).|||Phân vùng tương đương và phân tích giá trị biên đúng cho cả ba đầu vào (phân vùng hợp lệ/không hợp lệ và giá trị biên).",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "Test cases achieve 100% branch coverage (all four branches) with correctly hand-computed expected values (55000 / 52500 / 50000 / 47500 and the exceptions).|||Test case đạt 100% bao phủ nhánh (cả bốn nhánh) với giá trị kỳ vọng tính tay đúng (55000 / 52500 / 50000 / 47500 và các ngoại lệ).",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c3",
              "criterion": "States and justifies the minimum number of test cases and provides valid completed JUnit methods.|||Nêu và lý giải số test case tối thiểu, và cung cấp các phương thức JUnit hoàn thiện hợp lệ.",
              "weight": 1,
              "maxScore": 0.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/SWT301/PE12.png"
        },
        {
          "kind": "WRITE",
          "points": 4,
          "prompt": "<div class=\"ml-en\"><p><b>Question 3 (4 points)</b></p><p>You are tasked with performing functional (black-box) testing on an <b>online food ordering system</b>. Create functional black-box test cases based on this use case using <b>use-case-based testing</b> techniques. <i>(Use question 3 template)</i></p><p><b>Use Case: Place a Food Order</b><br/><b>Primary Actor:</b> Customer</p><p><b>Preconditions:</b></p><ul><li>The customer must be logged into the system.</li><li>The customer must have at least one food item in their cart.</li></ul><p><b>Main Success Scenario (Normal Flow):</b></p><ol><li>The customer adds food items to their cart.</li><li>The customer selects a delivery option (e.g., standard delivery, express delivery).</li><li>The customer applies a discount code (optional).</li><li>The system validates the discount code.</li><li>The customer enters delivery details (e.g., address, phone number).</li><li>The customer proceeds to payment.</li><li>The system processes the payment.</li><li>The system confirms the order and provides an estimated delivery time.</li></ol><p><b>Alternative Flows:</b></p><ul><li><b>A1:</b> If the customer selects express delivery, the system adds an extra delivery charge.</li><li><b>A2:</b> If the discount code is expired, the system displays an error message, and the customer can proceed without a discount.</li><li><b>A3:</b> If the customer chooses to pay with a credit card, the system prompts for card details (e.g., card number, expiry date, CVV).</li><li><b>A4:</b> If the customer opts for a pickup instead of delivery, the system omits the delivery charge and prompts for a pickup time.</li></ul><p><b>Exception Scenarios:</b></p><ul><li><b>E1:</b> If the payment fails (e.g., insufficient funds or incorrect card details), the system notifies the customer and prompts them to retry payment.</li><li><b>E2:</b> If the cart is empty when attempting to place the order, the system prompts the customer to add items before proceeding.</li></ul><p><b>Postconditions:</b></p><ul><li>The customer receives an order confirmation and an estimated delivery/pickup time.</li><li>The system updates the inventory based on the order.</li></ul><p><b>Tasks:</b> Using the use case above, create functional black-box test cases to verify the system's behavior. Ensure that your test cases cover: (1) the normal flow, (2) the Alternative flows, (3) the exception scenarios. For each test case, specify: Test case ID, Test case description, Preconditions, Test steps (Test Procedure) with test data, Expected results, Notes (NF, AL or Ex). Make sure each test case exercises a unique aspect of the use case.</p></div><div class=\"ml-vi\"><p><b>Câu 3 (4 điểm)</b></p><p>Bạn được giao thực hiện kiểm thử chức năng (hộp đen) cho một <b>hệ thống đặt món ăn trực tuyến</b>. Hãy tạo các test case hộp đen dựa trên use case này bằng kỹ thuật <b>kiểm thử theo use case (use-case-based testing)</b>. <i>(Dùng mẫu câu 3)</i></p><p><b>Use Case: Đặt một đơn món ăn (Place a Food Order)</b><br/><b>Tác nhân chính:</b> Khách hàng</p><p><b>Điều kiện tiên quyết:</b></p><ul><li>Khách hàng phải đã đăng nhập hệ thống.</li><li>Khách hàng phải có ít nhất một món trong giỏ hàng.</li></ul><p><b>Kịch bản thành công chính (Luồng bình thường):</b></p><ol><li>Khách hàng thêm món vào giỏ.</li><li>Khách hàng chọn hình thức giao hàng (ví dụ: giao thường, giao nhanh).</li><li>Khách hàng áp mã giảm giá (tùy chọn).</li><li>Hệ thống kiểm tra mã giảm giá.</li><li>Khách hàng nhập thông tin giao hàng (ví dụ: địa chỉ, số điện thoại).</li><li>Khách hàng chuyển sang thanh toán.</li><li>Hệ thống xử lý thanh toán.</li><li>Hệ thống xác nhận đơn và cung cấp thời gian giao dự kiến.</li></ol><p><b>Luồng thay thế:</b></p><ul><li><b>A1:</b> Nếu khách chọn giao nhanh, hệ thống cộng thêm phí giao hàng.</li><li><b>A2:</b> Nếu mã giảm giá đã hết hạn, hệ thống hiển thị thông báo lỗi, và khách có thể tiếp tục không dùng giảm giá.</li><li><b>A3:</b> Nếu khách chọn thanh toán bằng thẻ tín dụng, hệ thống yêu cầu nhập thông tin thẻ (số thẻ, ngày hết hạn, CVV).</li><li><b>A4:</b> Nếu khách chọn tự đến lấy (pickup) thay vì giao hàng, hệ thống bỏ phí giao hàng và yêu cầu chọn giờ đến lấy.</li></ul><p><b>Kịch bản ngoại lệ:</b></p><ul><li><b>E1:</b> Nếu thanh toán thất bại (ví dụ: không đủ tiền hoặc sai thông tin thẻ), hệ thống báo cho khách và yêu cầu thử thanh toán lại.</li><li><b>E2:</b> Nếu giỏ hàng rỗng khi cố đặt đơn, hệ thống yêu cầu khách thêm món trước khi tiếp tục.</li></ul><p><b>Hậu điều kiện:</b></p><ul><li>Khách nhận được xác nhận đơn và thời gian giao/đến lấy dự kiến.</li><li>Hệ thống cập nhật tồn kho theo đơn.</li></ul><p><b>Nhiệm vụ:</b> Dùng use case trên, tạo các test case hộp đen để kiểm chứng hành vi hệ thống. Đảm bảo test case bao phủ: (1) luồng bình thường, (2) các luồng thay thế, (3) các kịch bản ngoại lệ. Mỗi test case ghi rõ: Mã test case, Mô tả, Điều kiện tiên quyết, Các bước (thủ tục) kèm dữ liệu test, Kết quả mong đợi, Ghi chú (NF, AL hoặc Ex). Mỗi test case phải kiểm một khía cạnh riêng của use case.</p></div>",
          "sampleSolution": "<div class=\"ml-en\"><p><b>Reference set of use-case-based test cases</b> (NF = Normal Flow, AL = Alternative, Ex = Exception). At least one per flow is required for full marks; TC1 covers NF, TC2-TC6 cover A1-A4 (+ valid discount), TC7-TC8 cover E1-E2.</p><table><tr><td><b>ID</b></td><td><b>Description</b></td><td><b>Preconditions</b></td><td><b>Test steps + data</b></td><td><b>Expected result</b></td><td><b>Notes</b></td></tr><tr><td>TC001</td><td>Normal flow - successful order, standard delivery</td><td>Logged in; cart has 1 pizza ($10)</td><td>1. Add pizza. 2. Select standard delivery. 3. Enter address + phone. 4. Proceed to payment. 5. Pay with valid details. 6. System processes payment.</td><td>Payment succeeds; order confirmed; estimated delivery time shown; inventory updated.</td><td>NF</td></tr><tr><td>TC002</td><td>Express delivery adds extra charge</td><td>Logged in; cart has 1 item</td><td>1. Add item. 2. Select express delivery. 3. Enter delivery details. 4. Proceed to payment.</td><td>System adds an extra express delivery charge; total = subtotal + express fee; order proceeds.</td><td>AL (A1)</td></tr><tr><td>TC003</td><td>Valid discount code applied</td><td>Logged in; cart has items</td><td>1. Add item. 2. Enter valid discount code \"SAVE10\". 3. System validates code.</td><td>Discount accepted; total reduced by the discount amount.</td><td>NF (step 3-4)</td></tr><tr><td>TC004</td><td>Expired discount code</td><td>Logged in; cart has items</td><td>1. Add item. 2. Enter expired code \"OLD2020\". 3. System validates code.</td><td>Error message \"discount code expired\" shown; customer can continue without discount; total unchanged.</td><td>AL (A2)</td></tr><tr><td>TC005</td><td>Pay with credit card - card details prompted</td><td>Logged in; cart has items</td><td>1. Proceed to payment. 2. Choose credit card. 3. Enter card number, expiry, CVV.</td><td>System prompts for and accepts card details, then processes payment.</td><td>AL (A3)</td></tr><tr><td>TC006</td><td>Pickup instead of delivery</td><td>Logged in; cart has items</td><td>1. Add item. 2. Choose pickup option. 3. Select a pickup time.</td><td>Delivery charge omitted (total has no delivery fee); system prompts for and records pickup time; order confirmed.</td><td>AL (A4)</td></tr><tr><td>TC007</td><td>Payment fails - insufficient funds</td><td>Logged in; cart has items</td><td>1. Proceed to payment. 2. Enter a card with insufficient funds / incorrect details. 3. Submit payment.</td><td>System notifies the customer of failure and prompts to retry payment; order NOT confirmed; inventory unchanged.</td><td>Ex (E1)</td></tr><tr><td>TC008</td><td>Empty cart on placing order</td><td>Logged in; cart is empty</td><td>1. Attempt to place order with an empty cart.</td><td>System prompts the customer to add items before proceeding; order not placed.</td><td>Ex (E2)</td></tr></table><p><b>Coverage check:</b> NF (TC001, TC003), A1 (TC002), A2 (TC004), A3 (TC005), A4 (TC006), E1 (TC007), E2 (TC008) - all normal, alternative and exception paths exercised, each test case targeting a unique aspect.</p></div><div class=\"ml-vi\"><p><b>Bộ test case mẫu theo use case</b> (NF = luồng bình thường, AL = luồng thay thế, Ex = ngoại lệ). Cần tối thiểu một test case cho mỗi luồng để đạt điểm tối đa; TC1 phủ NF, TC2-TC6 phủ A1-A4 (+ mã giảm giá hợp lệ), TC7-TC8 phủ E1-E2.</p><table><tr><td><b>Mã</b></td><td><b>Mô tả</b></td><td><b>Điều kiện tiên quyết</b></td><td><b>Các bước + dữ liệu</b></td><td><b>Kết quả mong đợi</b></td><td><b>Ghi chú</b></td></tr><tr><td>TC001</td><td>Luồng bình thường - đặt đơn thành công, giao thường</td><td>Đã đăng nhập; giỏ có 1 pizza ($10)</td><td>1. Thêm pizza. 2. Chọn giao thường. 3. Nhập địa chỉ + SĐT. 4. Sang thanh toán. 5. Thanh toán với thông tin hợp lệ. 6. Hệ thống xử lý thanh toán.</td><td>Thanh toán thành công; đơn được xác nhận; hiển thị thời gian giao dự kiến; cập nhật tồn kho.</td><td>NF</td></tr><tr><td>TC002</td><td>Giao nhanh cộng thêm phí</td><td>Đã đăng nhập; giỏ có 1 món</td><td>1. Thêm món. 2. Chọn giao nhanh. 3. Nhập thông tin giao. 4. Sang thanh toán.</td><td>Hệ thống cộng thêm phí giao nhanh; tổng = tạm tính + phí nhanh; đơn tiếp tục.</td><td>AL (A1)</td></tr><tr><td>TC003</td><td>Áp mã giảm giá hợp lệ</td><td>Đã đăng nhập; giỏ có món</td><td>1. Thêm món. 2. Nhập mã hợp lệ \"SAVE10\". 3. Hệ thống kiểm tra mã.</td><td>Mã được chấp nhận; tổng giảm đúng mức giảm giá.</td><td>NF (bước 3-4)</td></tr><tr><td>TC004</td><td>Mã giảm giá hết hạn</td><td>Đã đăng nhập; giỏ có món</td><td>1. Thêm món. 2. Nhập mã hết hạn \"OLD2020\". 3. Hệ thống kiểm tra mã.</td><td>Hiện thông báo lỗi \"mã đã hết hạn\"; khách tiếp tục được mà không giảm giá; tổng không đổi.</td><td>AL (A2)</td></tr><tr><td>TC005</td><td>Thanh toán bằng thẻ tín dụng - yêu cầu nhập thông tin thẻ</td><td>Đã đăng nhập; giỏ có món</td><td>1. Sang thanh toán. 2. Chọn thẻ tín dụng. 3. Nhập số thẻ, ngày hết hạn, CVV.</td><td>Hệ thống yêu cầu và chấp nhận thông tin thẻ, rồi xử lý thanh toán.</td><td>AL (A3)</td></tr><tr><td>TC006</td><td>Tự đến lấy thay vì giao hàng</td><td>Đã đăng nhập; giỏ có món</td><td>1. Thêm món. 2. Chọn hình thức pickup. 3. Chọn giờ đến lấy.</td><td>Bỏ phí giao hàng (tổng không có phí giao); hệ thống yêu cầu và lưu giờ đến lấy; đơn được xác nhận.</td><td>AL (A4)</td></tr><tr><td>TC007</td><td>Thanh toán thất bại - không đủ tiền</td><td>Đã đăng nhập; giỏ có món</td><td>1. Sang thanh toán. 2. Nhập thẻ không đủ tiền / sai thông tin. 3. Gửi thanh toán.</td><td>Hệ thống báo thất bại và yêu cầu thử lại; đơn KHÔNG được xác nhận; tồn kho không đổi.</td><td>Ex (E1)</td></tr><tr><td>TC008</td><td>Giỏ hàng rỗng khi đặt đơn</td><td>Đã đăng nhập; giỏ rỗng</td><td>1. Cố đặt đơn với giỏ rỗng.</td><td>Hệ thống yêu cầu khách thêm món trước khi tiếp tục; đơn không được đặt.</td><td>Ex (E2)</td></tr></table><p><b>Kiểm tra độ bao phủ:</b> NF (TC001, TC003), A1 (TC002), A2 (TC004), A3 (TC005), A4 (TC006), E1 (TC007), E2 (TC008) - đã phủ đủ luồng bình thường, thay thế và ngoại lệ, mỗi test case nhắm một khía cạnh riêng.</p></div>",
          "rubric": [
            {
              "id": "c1",
              "criterion": "Normal flow test case is complete and correct (successful order, all fields specified).|||Test case luồng bình thường đầy đủ và đúng (đặt đơn thành công, ghi đủ các trường).",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c2",
              "criterion": "All four alternative flows (A1-A4) covered by distinct, correct test cases.|||Cả bốn luồng thay thế (A1-A4) được phủ bởi các test case riêng biệt, đúng.",
              "weight": 1,
              "maxScore": 1.5
            },
            {
              "id": "c3",
              "criterion": "Both exception scenarios (E1 payment failure, E2 empty cart) covered with correct expected results.|||Cả hai kịch bản ngoại lệ (E1 thanh toán lỗi, E2 giỏ rỗng) được phủ với kết quả mong đợi đúng.",
              "weight": 1,
              "maxScore": 1
            },
            {
              "id": "c4",
              "criterion": "Each test case specifies all required template fields (ID, description, preconditions, steps with data, expected results, notes).|||Mỗi test case ghi đủ các trường của mẫu (mã, mô tả, điều kiện tiên quyết, các bước kèm dữ liệu, kết quả mong đợi, ghi chú).",
              "weight": 1,
              "maxScore": 0.5
            }
          ],
          "imageUrl": "https://media.cuongthai.com/images/exam-papers/SWT301/PE12.png"
        }
      ]
    }
  ]
};
