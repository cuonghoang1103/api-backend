/**
 * Parser lịch FAP — đã CHUYỂN sang `frontend/src/lib/lich/fap.ts`.
 *
 * Web cũng cần đúng bộ đọc này (trang Tổng quan trên web có cùng bảng soạn
 * lịch). Giữ hai bản là mầm trôi dạt: vá một dạng dán ở bên này thì bên kia
 * vẫn đọc hụt, và không có gì báo. Tệp này còn lại để mọi chỗ đang `import`
 * không phải sửa.
 */
export * from '@/lib/lich/fap';
