# Bài giảng Code Lab LAB211 — bản theo tờ checklist giấy (22/09/2026)

`module_<id>.json` = `CodeModule.lessonBlocks` ĐANG CHẠY trên production cho track LAB211, sau khi
sửa theo tờ "Coding check sheet" 25 mục của thầy (repository ở mọi bài, View nhận ResponseDTO qua
thuộc tính + render 1 lần/luồng, Main nhập/validate/đọc tệp qua utils, code mẫu đạt 2.x/3.x).

| id | module |
|---|---|
| 2375 | Hướng dẫn của thầy — nguồn là `scripts/data/lab211-huongdan.mjs` (không có JSON ở đây) |
| 2376 | Java nền tảng |
| 2377 | Kiến trúc 9 package & bộ khung P0055 |
| 2378 | OOP · SOLID · Design Pattern |
| 847 | LAB211 Assignments |
| 855 / 856 / 857 | Java API / Algorithm Reference / Error Handbook |

⛔ **Đây là nguồn chuẩn.** Các script cũ `codelab-lab211-lesson.mjs`, `codelab-lab211-tach-module.mjs`,
`codelab-lab211-847-lesson.mjs`, `codelab-lab211-bai-luyen.mjs` sinh ra bản CŨ (entity/bo/ui, View
nhận tham số…) — chạy lại chúng là đè mất bản đã sửa. Muốn đổi nội dung: sửa JSON ở đây rồi ghi
`lessonBlocks` (có chốt so mã băm bản đang chạy trước khi ghi).
