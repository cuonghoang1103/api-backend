
*
    box-sizing border-box
    margin 0
    padding 0
    -webkit-tap-highlight-color transparent

button
input
    background none
    color inherit
    border none
    padding 0
    font inherit
    outline inherit

button
    cursor pointer

html
body
.game
.modals
.menu
    position fixed
    top 0
    left 0
    width 100%
    height 100%
    font-family "Pally-Regular", sans-serif
    font-optical-sizing auto
    font-weight 400
    font-style normal
    -webkit-font-smoothing antialiased
    -moz-osx-font-smoothing grayscale
    overflow hidden

.experience
    touch-action none

    .input-filter-wandering &,
    .input-filter-intro &
        cursor grab

        &:active
            cursor grabbing

html
    background radial-gradient(farthest-side at 0 0, #251f2b, #1d1721)
    font-size 20px
    touch-action manipulation // Prevents double tap zoom

    @media only screen and (max-width 520px)
        font-size 18px

    @media only screen and (max-width 440px)
        font-size 16px;

ul
ol
    padding-left 25px

.title
    margin-bottom 0.25em
    font-size 2.5rem
    font-family "Pally-Bold", sans-serif
    font-weight 700
    font-style normal

.text
    margin-bottom 0.75em
    font-size 1rem
    // Trước đây KHÔNG khai báo line-height, nên mọi đoạn văn thừa hưởng mặc
    // định của trình duyệt (~1,2). Với phông Pally — chữ cao, thân rộng — 1,2
    // là quá chật: các dòng dính nhau và mắt khó bắt đầu dòng tiếp theo. Đây là
    // chữ để ĐỌC (bảng Behind the scene, các hộp thoại), không phải tiêu đề,
    // nên cần khoảng thở. 1,65 là mức dễ đọc cho cỡ chữ 1rem.
    line-height 1.65

    a
        color inherit
        text-decoration underline

.text-small
    font-size 0.8rem

.text-danger
    color #ff87a2
    
    .icon
        vertical-align sub

.text-faded
    opacity 0.8

.text-align-right
    text-align right

.text-align-center
    text-align center

.horizontal-group
    display flex
    align-items center
    justify-content center
    gap 10px

    @media only screen and (max-width 520px)
        flex-direction column

    > *
        // margin-right 10px

        // &:last-child
        //     margin-right 0
        
a// Nhóm NÚT CHỌN — mấy lựa chọn bày ra cùng lúc thay vì một nút bấm xoay vòng.
// Dùng cho mục Time trong bảng Cài đặt: người chơi thấy ngay có ba chế độ
// (Auto / Day / Night) chứ không phải bấm ba lần mới biết.
.segmented
    position relative
    display inline-flex
    vertical-align top
    gap 6px

    .button.is-active
        color #d5ff95
        border-color alpha(#d5ff95, 0.9)

.button
        display inline-block

.button
    vertical-align top
    padding 0 16px
    height 58px
    line-height 58px
    border-radius 8px
    white-space nowrap
    color inherit
    text-decoration none
    border 1px solid alpha(#ffffff, 0.8)
    color alpha(#ffffff, 0.8)
    cursor default

    @media only screen and (max-width 520px)
        padding 0 13px
        height 50px
        line-height 50px

    @media only screen and (max-width 440px)
        padding 0 10px
        height 45px
        line-height 45px

    &:not(.is-disabled)
        cursor pointer

    &.is-disabled
        border-style dashed

        &.has-tooltip
            cursor help

    &.is-success
        color #d5ff95

    &.is-danger
        color #ff6a7c

    html:not(.is-mode-touch) &:not(.is-disabled):hover
        border 1px solid alpha(#ffffff, 1)
        color alpha(#ffffff, 1)

    .icon
        position relative
        vertical-align middle

        &.icon-audio
            top -2px

        &.icon-restart
            top -2px

        &.icon-flag
            top -1px

        &.icon-controller
            top -1px

.input-group
    display flex
    height 58px

    &.is-message
        width 100%
        margin-top 1.5em

        .input-text
            width calc(100% - 62px * 2)
            padding 0 15px 0 5px

    &.is-name-tag
        .input-text
            width 100px
            font-size 25px
            letter-spacing 8px
            text-transform uppercase
            font-weight 700
            // padding 0 15px 0 5px

            &::placeholder
                letter-spacing 14px
                padding-left 4px

    > *
        border-top 1px solid #555555
        border-bottom 1px solid #555555

    >:first-child
        border-radius 8px 0 0 8px
        border-left 1px solid #555555

    &.is-valide
        button
            cursor pointer
            border-color #ffceca
            color #ffceca

            &.has-icon
                svg
                    *
                        stroke #ffceca

    .input-text
        padding 0 15px 0 5px

    .submit
        border-radius 0 8px 8px 0
        border 1px solid #555555
        cursor default
        color #555555

        &.has-text
            padding 0 20px

        &.has-icon
            width 62px
            padding 0 8px 0 4px

            svg
                *
                    stroke #555555

    .input-flag
        position relative
        width 62px
        font-size 16px

        .flag-button
            position absolute
            top 0
            left 0
            width 100%
            height 100%
            display flex
            justify-content center
            align-items center
            cursor pointer

            &.has-flag
                .flag
                    display block

                svg
                    display none

            .flag
                display none
                width 27px
                height 18px

            html:not(.is-mode-touch) &:hover
                svg
                    *
                        fill #cccccc

            svg
                display block

                *
                    fill #555555

        .flag-select
            display none
            position absolute
            bottom 0
            left 8px
            width 350px
            height 240px
            background radial-gradient(ellipse at top left, #251f2b, #1d1721)
            border-radius 8px 8px 0 0
            border 1px solid #767676
            border-bottom none
            overflow hidden
            white-space nowrap
            z-index 1

            &.is-visible
                display block

            .close-mini
                display flex
                align-items center
                justify-content center
                position absolute
                height 40px
                width 34px
                top 0
                right 0
                opacity 0.65
                cursor pointer

                html:not(.is-mode-touch) &:hover
                    opacity 1

                img
                    position relative
                    top 4px
                    left -3px
            

            .remove
                display flex
                align-items center
                justify-content center
                position absolute
                height 40px
                width 34px
                top 0
                right 30px
                opacity 0.65
                cursor pointer

                html:not(.is-mode-touch) &:hover
                    opacity 1

                img
                    position relative
                    top 3px
            
            .search
                height 40px
                width 100%
                padding 4px 35px 0 10px

            .no-result
                display none
                justify-content center
                align-items center
                position absolute
                top 30px
                bottom 0
                left 0
                right 0
                opacity 0.65
                pointer-events none

                &.is-visible
                    display flex
            
            .choices
                position absolute
                top 40px
                bottom 0
                left 0
                right 0
            
                .scroller
                    position absolute
                    top 0
                    left 0
                    width calc(100% + 20px)
                    height 100%
                    overflow-y scroll
                    overflow-x hidden

                .choice
                    display flex
                    padding 4px 8px
                    cursor pointer

                    html:not(.is-mode-touch) &:hover
                        .label
                            opacity 1

                    .label
                        opacity 0.65

                    .flag
                        margin-right 8px
                        width 27px
                        height 18px

html:not(.is-server-offline) .offline-element
    display none !important

html:not(.is-server-online) .online-element
    display none !important


// Ô "đừng hỏi lại nữa" dưới hộp thoại cổng trường. Chỉ có tác dụng trong PHIÊN
// chơi hiện tại — tải lại trang là hỏi lại, đúng như user chốt.
.fptu-gate-mute
    display flex
    align-items center
    justify-content center
    gap 10px
    margin-top 18px
    color alpha(#ffffff, 0.62)
    font-size 15px
    cursor pointer
    user-select none

    input
        position absolute
        opacity 0
        width 0
        height 0

    .fptu-gate-mute-box
        position relative
        flex-shrink 0
        width 20px
        height 20px
        border-radius 6px
        border 1px solid alpha(#ffffff, 0.35)
        transition border-color 0.15s, background 0.15s

    input:checked + .fptu-gate-mute-box
        border-color #f0821e
        background #f0821e

        &:after
            content ''
            position absolute
            left 6px
            top 2px
            width 5px
            height 10px
            border solid #1a1a1c
            border-width 0 2px 2px 0
            transform rotate(45deg)

    &:hover
        color alpha(#ffffff, 0.9)
