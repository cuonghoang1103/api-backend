-- ============================================================
-- Phase 7: Content Creator — Script field (additive only)
-- ------------------------------------------------------------
-- Adds one optional column `script` to content_projects for
-- the long-form markdown-ish script body. Distinct from
-- `concept` (a 1-2 sentence description that already
-- exists). Owned by the Script editor tab.
--
-- Schema drift note
-- ------------------------------------------------------------
-- Same exclusion as previous content_creator migrations:
-- `projects.search_vector` + its GIN index are kept out
-- of the autogenerated diff. They are managed by the
-- raw-SQL migration `20260623120000_add_project_search_index`
-- and are not part of the Prisma schema on purpose.
-- ============================================================

-- AddColumn
ALTER TABLE "content_projects" ADD COLUMN "script" TEXT;
